// Plan 01 Wave-1 stub. Plan 03 (`auth/AuthService`) replaces this file with
// the real login/logout service. Keeping the class export name lets Plan 01's
// `src/main.ts` field-stub (`auth!: AuthService`) type-check across Wave 1.
export class AuthService {
  readonly __brand!: 'AuthService';
}
