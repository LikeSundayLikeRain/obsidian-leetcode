// Plan 01 Wave-1 stub. Plan 05 (`settings/SettingsStore`) replaces this file
// with the real loadData/saveData wrapper. Keeping the class export name and
// shape lets Plan 01's `src/main.ts` field-stub (`settings!: SettingsStore`)
// type-check across Wave 1 before real dependencies land.
export class SettingsStore {
  readonly __brand!: 'SettingsStore';
}
