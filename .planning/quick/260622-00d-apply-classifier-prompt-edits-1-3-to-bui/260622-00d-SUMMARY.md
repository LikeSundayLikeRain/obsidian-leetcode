---
quick_id: 260622-00d
status: complete
date: 2026-06-22
commit: f464195
files_changed:
  - src/graph/buildKgPrompt.ts
  - tests/graph/buildKgPrompt.test.ts
---

# Quick Task 260622-00d — Classifier prompt edits #1–#3

The prompt-tuning half of the pattern-classification fix (pairs with the
`normalizePatternName` + `sanitizeHubFilename` code fixes in 260621-154 and the
vault re-classification migration). User endorsed edits #1–#3 and **rejected**
the seed-rename #4.

## Edits applied (all in `buildKgPrompt.ts`, string-only)

1. **Verbatim names** — new Constraints bullet: output seed names exactly as
   written (no re-casing/pluralizing), Title Case + filesystem-safe chars for
   invented names. Defense-in-depth alongside the now seed-aware normalizer.
2. **Demote enabling steps** — appended to rule 1: Sorting / Arrays & Hashing /
   Prefix Sum are enabling steps, not primary unless the ordering/hashing IS the
   whole insight (3Sum → Two Pointers; Merge Intervals → Intervals).
3. **Tighten 2nd-pattern bar** — appended to rule 3: emit a second pattern only
   if removing it makes the solution incorrect or asymptotically worse; "when in
   doubt, emit one pattern."

Rule 2/2a (Monotonic Stack vs Queue) untouched; JSON schema untouched; seed
list untouched (no #4).

## Verification
- `vitest run tests/graph/buildKgPrompt.test.ts` → 9/9 pass (new edit-assertion
  test + unchanged determinism + Monotonic-Queue regression).
- `tsc -noEmit -skipLibCheck` → exit 0.
- Diff scoped to the 2 intended files only.

## Deviations
Executed directly on `main` (string-only single-file change — no planner/executor
subagent ceremony needed). No worktree isolation. Commit `f464195`.
