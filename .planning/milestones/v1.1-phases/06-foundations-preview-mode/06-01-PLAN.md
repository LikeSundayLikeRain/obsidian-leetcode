---
phase: 06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - scripts/check-bundle-size.mjs
  - scripts/check-bundle-size.sh
  - .github/workflows/ci.yml
  - tests/foundations/eslint-config.test.ts
  - tests/foundations/check-bundle-size.test.ts
  - tests/foundations/ci-workflow.test.ts
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
tags:
  - foundations
  - lint
  - ci
  - bundle-size
must_haves:
  truths:
    - "`npm run lint` exits 0 with eslint-plugin-obsidianmd@^0.3.0 installed."
    - "`npm run check:bundle-size` exits 1 when main.js > 500_000 bytes."
    - "`npm run check:bundle-size` exits 0 with a soft-warning log when 400_000 < size <= 500_000."
    - "`.github/workflows/ci.yml` runs `npm ci`, `npm run lint`, `npm test`, `npm run build`, `npm run check:bundle-size` in that exact order."
    - "All 7 existing v1.0 command IDs continue to pass 0.3.0 command-ID rules (re-verified post-bump)."
  artifacts:
    - path: package.json
      provides: "eslint-plugin-obsidianmd pinned to ^0.3.0; new check:bundle-size script"
      contains: '"eslint-plugin-obsidianmd": "^0.3.0"'
    - path: scripts/check-bundle-size.mjs
      provides: "Platform-portable Node bundle-size gate (HARD_LIMIT=500_000, SOFT_WARN=400_000)"
      min_lines: 12
    - path: .github/workflows/ci.yml
      provides: "GitHub Actions CI workflow with 5-step pipeline"
      contains: "check:bundle-size"
    - path: tests/foundations/eslint-config.test.ts
      provides: "Asserts eslint.config.mts still imports obsidianmd.configs.recommended"
    - path: tests/foundations/check-bundle-size.test.ts
      provides: "Asserts the Node script exits 1 / 0 with warning at the right thresholds"
    - path: tests/foundations/ci-workflow.test.ts
      provides: "Asserts the YAML workflow has the 5 expected steps in order"
  key_links:
    - from: package.json
      to: scripts/check-bundle-size.mjs
      via: "npm run check:bundle-size"
      pattern: "check:bundle-size"
    - from: .github/workflows/ci.yml
      to: scripts/check-bundle-size.mjs
      via: "npm run check:bundle-size step"
      pattern: "run: npm run check:bundle-size"
---

<objective>
Land the Phase 06 Foundations: bump `eslint-plugin-obsidianmd` from `0.2.9` to `^0.3.0`, fix any 0.3.0 cascade lint violations across the v1.0 codebase, replace the legacy `scripts/check-bundle-size.sh` with a platform-portable Node script (`scripts/check-bundle-size.mjs`) using a 500 KB hard limit and 400 KB soft warning, register a `npm run check:bundle-size` script, and bootstrap a GitHub Actions CI workflow at `.github/workflows/ci.yml` that runs `npm ci → lint → test → build → check:bundle-size`. This plan gates every other Phase 06 plan — Plan 06-02 cannot start until lint is green at HEAD.

Purpose: Locks the lint baseline against the latest plugin-store ruleset before any v1.1 feature code lands (per CONTEXT.md decision D, decision E, and STATE.md "Foundation work gates all v1.1 feature code"). The CI gate enforces FOUND-02 from the first PR forward; the lint cascade ensures FOUND-01 holds against drift.

Output: Bumped lint package; new portable Node bundle-size script; new CI workflow; foundations test stubs (Wave 0 spine for FOUND-01/02); README "Bundle size" subsection (Plan 06-04 owns the README polish — this plan only wires the gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-foundations-preview-mode/06-CONTEXT.md
@.planning/phases/06-foundations-preview-mode/06-RESEARCH.md
@.planning/phases/06-foundations-preview-mode/06-PATTERNS.md
@.planning/phases/06-foundations-preview-mode/06-VALIDATION.md
@CLAUDE.md
@package.json
@scripts/check-bundle-size.sh
@eslint.config.mts

<interfaces>
<!-- Key facts the executor needs without re-reading source. -->

From package.json (current state):
- `"eslint-plugin-obsidianmd": "0.2.9"` (line 21) → bump to `"^0.3.0"`.
- `"scripts"` block (lines 6-13) — register `"check:bundle-size": "node scripts/check-bundle-size.mjs"`.
- Pinned `@eslint/js@9.30.1` and `typescript-eslint@8.35.1` already satisfy `eslint-plugin-obsidianmd@0.3.0` peer ranges (RESEARCH §Open Q4 — no co-bump required).

From eslint.config.mts (existing flat config): uses `obsidianmd.configs.recommended` so 0.3.0 rules auto-enable on bump. New rules to expect: `no-plugin-as-component`, `prefer-instanceof`, `vault/iterate`, `commands/no-command-in-command-id`, `commands/no-command-in-command-name`. Existing rules deepened: `no-forbidden-elements`, `no-global-this`, `regex-lookbehind`.

From RESEARCH §Open Q7 — all 7 existing v1.0 command IDs already pass 0.3.0 command rules (re-verified 2026-05-15):
- `open-problem-browser` (main.ts:239), `refresh-current-problem` (main.ts:253), `submit` (main.ts:293), `reset-code` (main.ts:311), `view-past-submissions` (main.ts:330), `cancel-submission` (main.ts:348), `run` (solve/runCommandRegistration.ts:45). No `hotkeys` field anywhere. Bump should NOT need command-ID edits.

From scripts/check-bundle-size.sh (existing bash version, lines 28-40): hardcoded `LIMIT_BYTES=$((250 * 1024))` / `WARN_BYTES=$((200 * 1024))`. Phase 06 thresholds are different: 500_000 / 400_000 bytes per CONTEXT.md decision E. The new Node script REPLACES the bash version entirely — delete the bash file as part of this plan.

Bundle-size script contract (Node ESM):
- Reads `main.js` size via `fs.statSync('main.js').size`.
- HARD_LIMIT = 500_000; SOFT_WARN = 400_000.
- Logs `main.js: {bytes} bytes ({kb} KB)` always.
- If size > HARD_LIMIT → `console.error('BUNDLE CHECK FAIL: ...')` and `process.exit(1)`.
- If size > SOFT_WARN → `console.warn('BUNDLE CHECK WARN: ...')`.
- If file missing → `console.error('BUNDLE CHECK FAIL: main.js missing — run "npm run build" first')` and exit 1.
- Otherwise log `BUNDLE CHECK OK`.

CI workflow contract (.github/workflows/ci.yml — first GitHub Actions file in repo):
- Triggers: `push` to `main` and `pull_request`.
- Runner: `ubuntu-latest`.
- Steps in order: `actions/checkout@v4`, `actions/setup-node@v4` (node-version: 20, cache: npm), `npm ci`, `npm run lint`, `npm test`, `npm run build`, `npm run check:bundle-size`.

Test stubs (Wave 0 — RED-shell — vitest already configured):
- `tests/foundations/eslint-config.test.ts` — read `eslint.config.mts` source via `fs.readFileSync`, assert it imports/uses `obsidianmd.configs.recommended` (string match).
- `tests/foundations/check-bundle-size.test.ts` — spawn `node scripts/check-bundle-size.mjs` via `child_process.execFileSync` against fixture files of size 100_000, 450_000, 600_000 bytes; assert exit codes 0 / 0 (with warn) / 1.
- `tests/foundations/ci-workflow.test.ts` — read `.github/workflows/ci.yml`, assert it contains the 5 `run:` step strings in order.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bump eslint-plugin-obsidianmd to ^0.3.0 and resolve any cascade lint violations</name>
  <files>package.json, package-lock.json, src/**/*.ts (only files surfaced by the new lint rules)</files>
  <read_first>
    - package.json (current pin: `eslint-plugin-obsidianmd@0.2.9` at line 21)
    - eslint.config.mts (verify it uses `obsidianmd.configs.recommended` so 0.3.0 rules auto-enable)
    - .planning/phases/06-foundations-preview-mode/06-RESEARCH.md §Open Q4 (peer-deps already satisfied; no co-bump) and §Open Q7 (existing command IDs already 0.3.0-clean)
    - .planning/phases/06-foundations-preview-mode/06-PATTERNS.md §`package.json` MODIFIED block (exact bump shape)
    - CLAUDE.md (project conventions — `'leetcode.*'` userEvent rule, `app.vault.process` discipline, `requestUrl` discipline; do not violate during cascade fixes)
  </read_first>
  <behavior>
    - After `npm install`, `npm ls eslint-plugin-obsidianmd` reports `0.3.0`.
    - `npm run lint` exits 0 (no violations) — including the new 0.3.0 rules `no-plugin-as-component`, `prefer-instanceof`, `vault/iterate`, `commands/no-command-in-command-id`, `commands/no-command-in-command-name`.
    - All 7 existing command IDs (`open-problem-browser`, `refresh-current-problem`, `submit`, `reset-code`, `view-past-submissions`, `cancel-submission`, `run`) remain unchanged in source — pre-audit confirms they pass 0.3.0 rules.
    - If new rules surface real violations in v1.0 modules (e.g. `prefer-instanceof` on a `typeof x === 'object'` check, or `vault/iterate` on a `vault.getFiles()` loop), fix in-place WITHOUT relaxing rules; do NOT add `eslint-disable` comments.
    - Cascade fix budget: ≤30 violations expected per RESEARCH Assumption A1; if more surface, halt and surface to checker.
  </behavior>
  <action>
    Edit `package.json` line 21: change `"eslint-plugin-obsidianmd": "0.2.9"` to `"eslint-plugin-obsidianmd": "^0.3.0"`. Run `npm install` (background — long-running). After install completes, run `npm run lint`. If lint fails, fix each violation in source files following the patterns documented in 06-PATTERNS.md (e.g. `prefer-instanceof` rewrites a `typeof x === 'object' && x !== null && 'foo' in x` chain to `x instanceof Foo`; `vault/iterate` replaces `vault.getFiles().forEach(...)` with `vault.iterateAllFiles(...)` if available, else justify in a code comment why the existing pattern stays). Do NOT add `eslint-disable` directives unless the rule is a confirmed false positive — in that case, document the rationale inline. Do NOT touch any v1.0 command IDs (RESEARCH §Open Q7 confirms all 7 already pass). Commit `package-lock.json` regenerated by `npm install`. (Cascade fixes — per FOUND-01.)
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'eslint-plugin-obsidianmd' package.json` returns a line containing `"^0.3.0"`.
    - `npm ls eslint-plugin-obsidianmd` exits 0 and reports a version starting with `0.3.`.
    - `npm run lint` exits 0.
    - `grep -rn 'eslint-disable' src/` produces NO output that is new in this plan (existing v1.0 disables are allowed, but no new ones added without justification).
    - `package-lock.json` is committed alongside `package.json`.
  </acceptance_criteria>
  <done>
    Lint package bumped to ^0.3.0; full lint suite green at HEAD; any cascade fixes applied in-place without weakening rules; package-lock.json regenerated.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace bash bundle-size check with platform-portable Node script and register npm script</name>
  <files>scripts/check-bundle-size.mjs, scripts/check-bundle-size.sh, package.json, tests/foundations/check-bundle-size.test.ts</files>
  <read_first>
    - scripts/check-bundle-size.sh (existing bash version — lines 28-40 contain the threshold-cascade structure; thresholds are 250_000 / 200_000 — Phase 06 changes to 500_000 / 400_000 per CONTEXT.md §E)
    - .planning/phases/06-foundations-preview-mode/06-RESEARCH.md §Pitfall 6 + §Code Examples (exact 12-line Node shim)
    - .planning/phases/06-foundations-preview-mode/06-PATTERNS.md §`scripts/check-bundle-size.mjs` (NEW) section (Node version listed verbatim)
    - .planning/phases/06-foundations-preview-mode/06-CONTEXT.md §E (CONTEXT decision: 500 KB hard / 400 KB soft, hardcoded constants, no baseline file, no PR comments)
    - tests/helpers/setup.ts (vitest setup precedent for child_process-style tests)
    - tests/manifest-version.test.ts (analog: filesystem-shape test using `readFileSync`)
  </read_first>
  <behavior>
    - `scripts/check-bundle-size.mjs` is a 12–20 line ESM Node script.
    - When `main.js` does not exist → exit 1 with `BUNDLE CHECK FAIL: main.js missing — run "npm run build" first`.
    - When `main.js` <= 400_000 bytes → exit 0, log `main.js: {bytes} bytes ({kb} KB)` and `BUNDLE CHECK OK`.
    - When 400_000 < `main.js` <= 500_000 bytes → exit 0, log size + `BUNDLE CHECK WARN: main.js > 400000 bytes — heading toward the gate`.
    - When `main.js` > 500_000 bytes → exit 1 with `BUNDLE CHECK FAIL: main.js exceeds 500000 bytes`.
    - `package.json` has a new `"check:bundle-size": "node scripts/check-bundle-size.mjs"` script entry.
    - Legacy `scripts/check-bundle-size.sh` is deleted (its 250 KB / 200 KB thresholds are now stale and the bash flag set is non-portable per RESEARCH §Pitfall 6).
    - `tests/foundations/check-bundle-size.test.ts` spawns the script against three fixture files (in `os.tmpdir()`, NOT modifying the real `main.js`) and asserts exit codes 0 / 0 (with stderr containing `WARN`) / 1.
  </behavior>
  <action>
    Create `scripts/check-bundle-size.mjs` per 06-PATTERNS.md §`scripts/check-bundle-size.mjs` (NEW) — ESM Node script with constants `HARD_LIMIT = 500_000`, `SOFT_WARN = 400_000`, reads `main.js` via `fs.statSync(...).size`, branches on the three thresholds, logs to stdout/stderr per the contract above. Delete `scripts/check-bundle-size.sh` (legacy bash version with stale thresholds and non-portable `du`/`wc -c` invocations). Edit `package.json` to register `"check:bundle-size": "node scripts/check-bundle-size.mjs"` in the `"scripts"` block; if the existing `"grep:vault": "./scripts/grep-no-vault-modify.sh"` entry coexists with the deleted bash file, leave the grep:vault entry untouched (separate concern). Create `tests/foundations/check-bundle-size.test.ts` that uses `child_process.execFileSync` (Node built-in, no new dep) to spawn the script with `cwd` pointing at a tmpdir containing fixture `main.js` files at sizes 100_000, 450_000, and 600_000 bytes — assert exit codes 0, 0 (and stderr matches `/WARN/`), and 1 respectively; the missing-file path also asserts exit code 1 with stderr matching `/FAIL.*main\.js missing/`. (FOUND-02.)
  </action>
  <verify>
    <automated>npm test -- foundations/check-bundle-size</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-bundle-size.mjs` exists and starts with `import fs from 'node:fs';`.
    - `grep -n 'HARD_LIMIT' scripts/check-bundle-size.mjs` returns a line with the value `500_000` or `500000`.
    - `grep -n 'SOFT_WARN' scripts/check-bundle-size.mjs` returns a line with the value `400_000` or `400000`.
    - `scripts/check-bundle-size.sh` does NOT exist (deleted in this plan).
    - `grep -n '"check:bundle-size"' package.json` returns `"check:bundle-size": "node scripts/check-bundle-size.mjs"`.
    - `tests/foundations/check-bundle-size.test.ts` exists and `npm test -- foundations/check-bundle-size` exits 0.
    - Manually running `node scripts/check-bundle-size.mjs` from repo root after `npm run build` reports `BUNDLE CHECK OK` (current bundle ≈163 KB per RESEARCH §11).
  </acceptance_criteria>
  <done>
    Node bundle-size script in place; legacy bash script removed; npm script registered; behavior asserted against the three threshold cases via vitest.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bootstrap GitHub Actions CI workflow with bundle-size gate + foundations tests</name>
  <files>.github/workflows/ci.yml, tests/foundations/eslint-config.test.ts, tests/foundations/ci-workflow.test.ts</files>
  <read_first>
    - .planning/phases/06-foundations-preview-mode/06-RESEARCH.md §Code Examples §Example 7 (exact YAML shape — copy verbatim)
    - .planning/phases/06-foundations-preview-mode/06-CONTEXT.md §E (locked CI step order: `npm ci → npm run lint → npm test → npm run build → npm run check:bundle-size`)
    - .planning/phases/06-foundations-preview-mode/06-PATTERNS.md §`.github/workflows/ci.yml` (NEW) — no in-repo analog; use RESEARCH verbatim
    - eslint.config.mts (verify it imports `obsidianmd` and references `obsidianmd.configs.recommended`)
    - tests/manifest-version.test.ts (analog: file-shape test using `readFileSync`)
  </read_first>
  <behavior>
    - `.github/` directory and `.github/workflows/ci.yml` file exist (first GitHub Actions workflow in the repo).
    - Workflow triggers: `push` (branches: [main]) AND `pull_request`.
    - Job `ci` runs on `ubuntu-latest`.
    - Steps in exact order: `actions/checkout@v4`, `actions/setup-node@v4` (node-version: 20, cache: npm), `npm ci`, `npm run lint`, `npm test`, `npm run build`, `npm run check:bundle-size`.
    - `tests/foundations/ci-workflow.test.ts` parses the YAML (no `js-yaml` dep needed — substring match on the 5 `run:` lines is sufficient and avoids a new devDep) and asserts the 5 step commands appear in order.
    - `tests/foundations/eslint-config.test.ts` reads `eslint.config.mts` via `fs.readFileSync` and asserts it contains the substring `obsidianmd.configs.recommended` (so 0.3.0 rule activation cannot regress silently).
  </behavior>
  <action>
    Create `.github/workflows/ci.yml` with the YAML from 06-RESEARCH.md §Code Examples §Example 7 verbatim (`name: CI`, push/pull_request triggers, ubuntu-latest, node 20, the 5-step pipeline ending in `npm run check:bundle-size`). Create `tests/foundations/ci-workflow.test.ts` — read the YAML file as a string, assert the 5 step strings (`npm ci`, `npm run lint`, `npm test`, `npm run build`, `npm run check:bundle-size`) appear in order via successive `indexOf` calls returning monotonically increasing values; also assert `node-version: 20` and `runs-on: ubuntu-latest` substrings. Create `tests/foundations/eslint-config.test.ts` — read `eslint.config.mts` as a string, assert it contains `obsidianmd.configs.recommended` (drift-protection — if a future refactor inlines rules instead of the recommended preset, this test fails and the planner must re-audit). Both tests use `fs.readFileSync` from `node:fs`; no new devDep. Do NOT add a `js-yaml` devDep; substring assertion is sufficient given the workflow file is owned by this plan and not user-edited. (FOUND-02 + FOUND-01 drift gate.)
  </action>
  <verify>
    <automated>npm test -- foundations</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists.
    - `grep -n 'npm run check:bundle-size' .github/workflows/ci.yml` returns at least one match.
    - `grep -nE 'npm ci|npm run lint|npm test|npm run build|npm run check:bundle-size' .github/workflows/ci.yml` returns 5 matches in that order (verified via the test file's monotonic-index assertion).
    - `tests/foundations/ci-workflow.test.ts` exists and passes.
    - `tests/foundations/eslint-config.test.ts` exists and passes.
    - `npm test -- foundations` exits 0.
    - `npm run lint && npm test && npm run build && npm run check:bundle-size` ALL exit 0 locally (full Phase 06 gate dry-run).
  </acceptance_criteria>
  <done>
    CI workflow committed; foundations test stubs in place and green; the full local pipeline (lint + test + build + size check) passes at HEAD.
  </done>
</task>

</tasks>

<verification>
**Per-task verify commands** (above) cover unit-level assertions. Phase-level gate after this plan completes:

```bash
npm run lint && npm test && npm run build && npm run check:bundle-size
```

All 4 commands MUST exit 0 before Plan 06-02 can start.

**Manual UAT:**
- Push the branch and confirm GitHub Actions runs the workflow on the PR (one PR cycle).
- Confirm `BUNDLE CHECK OK` line in the CI logs (current bundle ≈163 KB, well under the 400 KB soft warn).
</verification>

<success_criteria>
- `eslint-plugin-obsidianmd` pinned to `^0.3.0` in `package.json`; `npm ls` confirms 0.3.x installed.
- `npm run lint` green at HEAD with no new `eslint-disable` directives in `src/`.
- `scripts/check-bundle-size.mjs` enforces a 500 KB hard / 400 KB soft threshold; legacy `scripts/check-bundle-size.sh` deleted.
- `.github/workflows/ci.yml` runs `npm ci → lint → test → build → check:bundle-size` on push-to-main and on every PR.
- 3 foundations test files green (`eslint-config`, `check-bundle-size`, `ci-workflow`).
- Existing v1.0 command IDs unchanged (RESEARCH §Open Q7 confirmed all 7 already 0.3.0-clean — bump should be a non-event for command surface).
</success_criteria>

<output>
After completion, create `.planning/phases/06-foundations-preview-mode/06-01-SUMMARY.md` per the GSD summary template:
- Files modified (full list)
- Lint cascade fix count (expected ≤30; record actual)
- New CI workflow status (green / red on first PR)
- Bundle size baseline at HEAD (post-`npm run build`)
- Any deviations from this plan (e.g. unexpected co-bumps, additional cascade fixes outside `src/`)
</output>
