---
phase: 15-focus-undo-cursor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/childEditorFactory.ts
  - src/main/childEditorSync.ts
  - tests/main/childEditorFactory.test.ts
  - tests/main/childEditorSync.test.ts
autonomous: true
requirements: [INDENT-01, INDENT-02, INDENT-03, INDENT-04]

must_haves:
  truths:
    - "Tab in child editor inserts indentation (4 spaces default)"
    - "Shift-Tab in child editor removes indentation"
    - "Multi-line selection + Tab indents all lines as a single undo step"
    - "Cmd-Z in parent does NOT undo child-sync changes (undo isolation)"
    - "Cmd-Z in child undoes child edits only"
  artifacts:
    - path: "src/main/childEditorFactory.ts"
      provides: "indentWithTab keymap + indentUnit facet"
      contains: "indentWithTab"
    - path: "src/main/childEditorSync.ts"
      provides: "addToHistory:false annotation on all parent dispatches"
      contains: "Transaction.addToHistory.of(false)"
  key_links:
    - from: "src/main/childEditorFactory.ts"
      to: "@codemirror/commands"
      via: "indentWithTab import"
      pattern: "indentWithTab"
    - from: "src/main/childEditorSync.ts"
      to: "@codemirror/state"
      via: "Transaction.addToHistory annotation"
      pattern: "addToHistory\\.of\\(false\\)"
---

<objective>
Add indentWithTab keymap and undo isolation to the child editor so Tab/Shift-Tab indent/dedent code and Cmd-Z histories are properly isolated between child and parent.

Purpose: Delivers INDENT-01 through INDENT-04 requirements and undo isolation (D-10, D-11) — the two most critical UX behaviors for a code editor.
Output: Modified childEditorFactory.ts with indent support, modified childEditorSync.ts with addToHistory:false on all parent dispatch paths, and updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-focus-undo-cursor/15-CONTEXT.md
@.planning/phases/15-focus-undo-cursor/15-RESEARCH.md
@.planning/phases/15-focus-undo-cursor/15-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/main/childEditorFactory.ts:
- `export function createChildEditor(content: string, parent: HTMLElement, syncExtensions?: Extension[]): EditorView`
- Extensions array at line 40-65 — keymap.of is at line 47
- `@codemirror/commands` import at line 21 currently imports: `history, defaultKeymap, historyKeymap`

From src/main/childEditorSync.ts:
- `export const syncAnnotation = Annotation.define<boolean>()`
- `Transaction` already imported from `@codemirror/state` (line 18)
- Two dispatch sites to parent:
  1. Lines 108-112 (fence repair retry path)
  2. Lines 155-161 (primary propagateChildChanges path)
- Both currently pass single annotation: `Transaction.userEvent.of('leetcode.child-sync')`

From @codemirror/commands (verified in node_modules):
- `declare const indentWithTab: KeyBinding` (line 648 of dist/index.d.ts)

From @codemirror/language (verified in node_modules):
- `declare const indentUnit: Facet<string, string>` (line 435 of dist/index.d.ts)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add indentWithTab keymap and indentUnit facet to child editor</name>
  <files>src/main/childEditorFactory.ts, tests/main/childEditorFactory.test.ts</files>
  <read_first>
    - src/main/childEditorFactory.ts (current extensions array, import pattern)
    - tests/main/childEditorFactory.test.ts (current mock structure for @codemirror/commands)
    - .planning/phases/15-focus-undo-cursor/15-PATTERNS.md (import convention, keymap priority pattern)
  </read_first>
  <behavior>
    - Test: indentWithTab is the FIRST entry in the keymap.of() call array (before defaultKeymap and historyKeymap)
    - Test: indentUnit facet is included in extensions with value "    " (4 spaces)
    - Test: indentWithTab mock is included in extensions via keymap.of return value
  </behavior>
  <action>
    In childEditorFactory.ts:
    1. Add `indentWithTab` to the existing `@codemirror/commands` import destructure (line 21) — no new eslint-disable needed since import line already has one.
    2. Add `import { indentUnit } from '@codemirror/language'` to the existing `@codemirror/language` import destructure (line 15-18).
    3. Change keymap line (line 47) from `keymap.of([...defaultKeymap, ...historyKeymap])` to `keymap.of([indentWithTab, ...defaultKeymap, ...historyKeymap])` — per D-05, indentWithTab MUST be first for priority.
    4. Add `indentUnit.of("    ")` (4 spaces) to the extensions array before the keymap line — per INDENT-04 / RESEARCH open question 2, default 4 spaces covers Python/Java/C++ (Phase 16 will make dynamic).

    In tests/main/childEditorFactory.test.ts:
    1. Add `indentWithTab: { key: 'Tab', run: vi.fn() }` to the `@codemirror/commands` mock.
    2. Add `indentUnit: { of: vi.fn().mockReturnValue('mock-indent-unit') }` to the `@codemirror/language` mock.
    3. Add test: "includes indentWithTab as first entry in keymap" — verify keymap.of was called with indentWithTab at index 0.
    4. Add test: "includes indentUnit.of with 4 spaces" — verify indentUnit.of called with "    " and result is in extensions.
  </action>
  <verify>
    <automated>cd /Users/moxu/projects/obsidian-leetcode && npm test -- --run tests/main/childEditorFactory.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - childEditorFactory.ts line with keymap.of contains `indentWithTab` as the first array element before `...defaultKeymap`
    - childEditorFactory.ts extensions array contains `indentUnit.of("    ")`
    - childEditorFactory.ts imports `indentWithTab` from `@codemirror/commands` and `indentUnit` from `@codemirror/language`
    - Test "includes indentWithTab as first entry in keymap" passes
    - Test "includes indentUnit.of with 4 spaces" passes
    - All existing tests in childEditorFactory.test.ts continue to pass
  </acceptance_criteria>
  <done>Tab and Shift-Tab indent/dedent with 4-space default; keymap priority verified by test</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add addToHistory:false annotation to all child-to-parent sync dispatches</name>
  <files>src/main/childEditorSync.ts, tests/main/childEditorSync.test.ts</files>
  <read_first>
    - src/main/childEditorSync.ts (both dispatch sites: lines 108-112 and lines 155-161)
    - tests/main/childEditorSync.test.ts (existing test structure and mock helpers)
    - .planning/phases/15-focus-undo-cursor/15-PATTERNS.md (annotations array pattern)
  </read_first>
  <behavior>
    - Test: primary sync dispatch (propagateChildChanges) includes Transaction.addToHistory.of(false) in annotations array
    - Test: fence repair retry dispatch includes Transaction.addToHistory.of(false) in annotations array
    - Test: both dispatches still include Transaction.userEvent.of('leetcode.child-sync') (not lost)
  </behavior>
  <action>
    In childEditorSync.ts:
    1. At line 155-161 (propagateChildChanges dispatch): change `annotations: Transaction.userEvent.of('leetcode.child-sync')` to `annotations: [Transaction.userEvent.of('leetcode.child-sync'), Transaction.addToHistory.of(false)]` — per D-11.
    2. At lines 108-112 (fence repair retry dispatch): same change — convert single annotation to array with both `Transaction.userEvent.of('leetcode.child-sync')` and `Transaction.addToHistory.of(false)`.
    No new imports needed — `Transaction` is already imported from `@codemirror/state` at line 18.

    In tests/main/childEditorSync.test.ts:
    1. Add a describe block "undo isolation (D-11)" inside the existing createChildSyncExtension describe.
    2. Add test: "propagateChildChanges dispatch includes addToHistory:false" — use makeMockParentView with CANONICAL_NOTE, simulate scenario where parentView.dispatch is called, verify the annotations argument contains addToHistory.of(false). Since we cannot easily trigger the updateListener, test by reading the source for the pattern (grep-based assertion OR integration-style test that exercises the full path).
    3. Alternative approach if mock-based testing is impractical: add a source-level assertion test that greps childEditorSync.ts for `addToHistory.of(false)` appearing exactly 2 times (one per dispatch site).
  </action>
  <verify>
    <automated>cd /Users/moxu/projects/obsidian-leetcode && npm test -- --run tests/main/childEditorSync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - childEditorSync.ts line ~157 contains `Transaction.addToHistory.of(false)` in an array with the userEvent annotation
    - childEditorSync.ts line ~110 contains `Transaction.addToHistory.of(false)` in an array with the userEvent annotation
    - grep -c "addToHistory.of(false)" src/main/childEditorSync.ts returns exactly 2
    - grep -c "Transaction.userEvent.of('leetcode.child-sync')" src/main/childEditorSync.ts returns at least 2 (not lost)
    - All existing tests in childEditorSync.test.ts continue to pass
    - New undo isolation test passes
  </acceptance_criteria>
  <done>Parent undo stack does not include child-sync changes; Cmd-Z in parent only undoes parent-originated edits per D-11</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | No network calls, no user data handling, no auth — purely editor configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | npm packages (@codemirror/commands, @codemirror/language) | accept | Already installed peer deps of obsidian; no new installs; verified in node_modules |
</threat_model>

<verification>
```bash
cd /Users/moxu/projects/obsidian-leetcode && npm test -- --run tests/main/childEditorFactory.test.ts tests/main/childEditorSync.test.ts
```
All tests pass. Source assertions:
- `grep -c "indentWithTab" src/main/childEditorFactory.ts` returns >= 2 (import + usage)
- `grep -c "addToHistory.of(false)" src/main/childEditorSync.ts` returns 2
</verification>

<success_criteria>
- Tab inserts 4 spaces of indentation in child editor (INDENT-01)
- Shift-Tab removes indentation (INDENT-02)
- Multi-line indent is single undo step (INDENT-03 — native CM6 behavior)
- Indent unit is 4 spaces default (INDENT-04 partial — Phase 16 makes dynamic)
- Cmd-Z in parent never undoes child-sync changes (D-11)
- Child undo stack is isolated and complete (D-10)
- All existing tests continue to pass (no regression)
</success_criteria>

<output>
Create `.planning/phases/15-focus-undo-cursor/15-01-SUMMARY.md` when done
</output>
