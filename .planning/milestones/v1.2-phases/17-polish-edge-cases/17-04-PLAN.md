---
phase: 17-polish-edge-cases
plan: 04
type: execute
wave: 2
depends_on: ["17-01", "17-02", "17-03"]
files_modified:
  - src/main.ts
  - tests/main/fmReactivity.test.ts
  - .planning/phases/17-polish-edge-cases/17-UAT.md
autonomous: true
requirements: [LANG-01, INDENT-04, ENTER-02, ENTER-03, ENTER-04, COMMENT-01, BRACKET-01, BRACKET-02, BRACKET-03, BRACKET-04]
requirements_addressed: [LANG-01, INDENT-04, ENTER-02, ENTER-03, ENTER-04, COMMENT-01, BRACKET-01, BRACKET-02, BRACKET-03, BRACKET-04]

must_haves:
  truths:
    - "When an external write changes a note's lc-language frontmatter (e.g., user edits frontmatter directly, or another plugin/sync rewrites it) AND the note is open in a MarkdownView with a registered child editor, the child editor's language is reconfigured via Compartment.reconfigure to match the new lc-language value within one metadataCache.changed event"
    - "The fm-reactivity listener does NOT rewrite the fence opener tag in the parent doc — frontmatter is the source of truth in this scenario (CONTEXT D-14)"
    - "The listener gates correctly: lc-slug missing → no-op; child not registered → no-op; lc-language unchanged from current child language → no-op (Pitfall 3 dedupe via Gate 3 slug equality)"
    - "The listener auto-detaches on plugin unload (registered via this.registerEvent — standard Obsidian lifecycle)"
    - "D-07: Edge-Input UAT uses verify-first / fix-only-if-broken policy — Tests 1-12 in 17-UAT.md are scaffolded and marked result:pending; execution happens in Plan 17-06; no pre-emptive fixes are made before the manual UAT reveals actual failures."
    - "D-13: External fm-reactivity listener registered via this.registerEvent on metadataCache.changed dispatches Compartment.reconfigure on the child when slug→language differs — implemented in Task 2."
    - "The Edge-Input UAT script (17-UAT.md) covers paste (D-08 four sources), IME (D-09 three languages), and Source↔Live Preview (D-10 Cmd-E flip with pending edits) cases — ready for Plan 17-06's manual UAT execution pass"
  artifacts:
    - path: "src/main.ts"
      provides: "metadataCache.on('changed') listener registered in onload via this.registerEvent; dispatches Compartment.reconfigure on the child editor for fm-driven language changes"
      contains: "metadataCache.on('changed'"
    - path: "tests/main/fmReactivity.test.ts"
      provides: "Vitest spec covering listener gate logic + dispatch shape + no-body-rewrite invariant"
      min_lines: 100
    - path: ".planning/phases/17-polish-edge-cases/17-UAT.md"
      provides: "Manual UAT script with PASTE-01..04, IME-01..03, SRCLIV-01 sections plus Phase 16 sanity regressions"
      min_lines: 80
  key_links:
    - from: "src/main.ts onload"
      to: "this.app.metadataCache.on('changed', handler)"
      via: "this.registerEvent(...)"
      pattern: "registerEvent\\([^)]*metadataCache\\.on\\(['\"]changed['\"]"
    - from: "fm-reactivity handler"
      to: "languageCompartment.reconfigure(buildLanguageExtensions(...))"
      via: "childView.dispatch({ effects: ... })"
      pattern: "languageCompartment\\.reconfigure"
    - from: "tests/main/fmReactivity.test.ts"
      to: "the listener registration in src/main.ts"
      via: "exercises the handler via a mocked metadataCache.on event"
      pattern: "lc-language"
---

<objective>
Add an external `lc-language` frontmatter reactivity listener (CONTEXT D-13/D-14): when a user (or another plugin) writes a different `lc-language` value to the frontmatter of an open `lc-slug` note, the child editor reconfigures its language via `Compartment.reconfigure` — reusing the Phase 16 chevron switch plumbing. The listener does NOT rewrite the fence opener tag (D-14 — frontmatter is the source of truth in this passive-listener scenario; users who want the fence opener to flip use the chevron). Concurrently scaffold the Edge-Input UAT script (CONTEXT D-07..D-10) covering paste/IME/Source↔Live Preview — content is the test cases; execution happens in Plan 17-06.

Purpose: Closes the Phase 16 D-14 carry-over (external fm reactivity). Provides the manual-UAT scaffolding required by the verify-first / fix-only-if-broken policy (CONTEXT D-07). Wave 2 is independent of Wave 1's write-path refactor (no shared file edits beyond `src/main.ts` onload section, which Plan 17-01 only touches at lines ~2791-2803 — disjoint from the onload area at lines ~820-880).

Output: A `metadataCache.on('changed')` listener registered in `src/main.ts` onload, a vitest spec covering the listener's gate logic and dispatch shape, a 17-UAT.md manual script ready for Plan 17-06 execution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-polish-edge-cases/17-CONTEXT.md
@.planning/phases/17-polish-edge-cases/17-RESEARCH.md
@.planning/phases/17-polish-edge-cases/17-PATTERNS.md
@.planning/phases/17-polish-edge-cases/17-VALIDATION.md
@.planning/phases/16-language-packs-switching/16-UAT.md
@.planning/phases/17-polish-edge-cases/17-01-SUMMARY.md
@.planning/phases/17-polish-edge-cases/17-02-SUMMARY.md
@.planning/phases/17-polish-edge-cases/17-03-SUMMARY.md

<interfaces>
<!-- Reusable APIs and the chevron switch precedent that this plan mirrors verbatim. -->

src/main/childEditorLanguage.ts (export — already used by chevron):
  export const languageCompartment: Compartment;
  export function buildLanguageExtensions(slug: string, override: IndentOverride): Extension[];

src/main/codeActionsEditorExtension.ts:329-359 (Analog A — chevron-driven metadataCache subscription, EXISTS in repo):
  plugin.registerEvent(
    plugin.app.metadataCache.on('changed', (file) => {
      try {
        const view = plugin.app.workspace.getActiveViewOfType(MarkdownView);
        if (!view || view.file !== file) return;
        const cm = (view.editor as unknown as { cm: EditorView }).cm;
        const fm = plugin.app.metadataCache.getFileCache(file)?.frontmatter as
          | Record<string, unknown> | undefined;
        const lcLanguageRaw = fm?.['lc-language'];
        const freshSlug =
          typeof lcLanguageRaw === 'string' && lcLanguageRaw.length > 0
            ? lcLanguageRaw : undefined;
        cm.dispatch({ effects: languageRefreshEffect.of(freshSlug) });
      } catch { /* defensive */ }
    }),
  );
This is the PARENT-side reactivity. This plan adds the CHILD-side reactivity: same gate, but dispatches Compartment.reconfigure on the child instead of an effect on the parent.

src/main.ts:794 + 2463-2475 (chevron switch on child — copy this dispatch shape):
  const childView = this.childEditorRegistry?.get(filePath);
  if (childView) {
    childView.dispatch({
      effects: languageCompartment.reconfigure(
        buildLanguageExtensions(newSlug, this.settings.getIndentSizeOverride()),
      ),
      userEvent: 'leetcode.lang-switch',
    });
  }

src/solve/languages.ts: resolveLangSlug (utility — read; reuse for slug validation if needed):
  export function resolveLangSlug(slug: string): string | undefined;

Helper to add — readActiveFenceSlug(file, app): string | undefined:
  Reads the parent doc, runs findCodeFence (from src/main/codeActionsEditorExtension.ts), extracts the slug from the opener line (e.g., '```java' → 'java'). Returns undefined if no fence or unparseable.

Phase 14 ECHO_PRONE_USER_EVENTS Set (src/main/nestedEditorExtension.ts:265-268):
  - 'leetcode.lang-switch' is NOT in this Set, so the dispatch propagates correctly through the externalChangeListener (verified Phase 16 chevron switch path).
  - Effect-only dispatches (no `changes` payload) are not subject to the section-lock changeFilter.

16-UAT.md format anchor (lines 1-15 frontmatter + Tests format):
  ---
  status: in-progress
  phase: 17-polish-edge-cases
  source: [17-01-SUMMARY.md, ..., 17-06-SUMMARY.md]
  started: <ISO>
  updated: <ISO>
  ---
  ## Current Test
  [testing in progress | testing complete]
  ## Tests
  ### N. <Title> (<REQ-ID or D-ID>)
  expected: <success criteria>
  result: <pass | fail | pending>
  notes: ""
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing test for fm reactivity listener (D-13/D-14 + Pitfall 3)</name>
  <files>tests/main/fmReactivity.test.ts</files>
  <read_first>
    - tests/main/childEditorSync.test.ts (lines 9-19 vi.mock('obsidian'); lines 87-97 makeMockChildView; lines 99-109 makeMockRegistry)
    - tests/helpers/obsidian-stub.ts (full file — confirm metadataCache stub or extend with a `metadataCacheStub.fire(...)` helper)
    - src/main/codeActionsEditorExtension.ts (lines 329-359 — chevron metadataCache analog)
    - src/main/childEditorLanguage.ts (lines 44, 132-148 — languageCompartment + buildLanguageExtensions)
    - 17-RESEARCH.md Pattern 5 (lines 354-389) for the listener body
    - 17-PATTERNS.md "tests/main/fmReactivity.test.ts" entry (lines 346-392) for the test cases
  </read_first>
  <behavior>
    - The listener function under test is the handler passed to `metadataCache.on('changed', handler)`. Task 2 will export it (or extract it as a named function) so the test can invoke it directly without spinning up the full plugin.
    - Test 1 ("external lc-language change dispatches Compartment.reconfigure on child"): set up mock plugin with childEditorRegistry containing an entry; mock metadataCache.getFileCache to return `{ frontmatter: { 'lc-slug': 'two-sum', 'lc-language': 'python' } }`; mock the parent fence opener to currently say `java` (via a `readActiveFenceSlug` stub). Invoke the handler with `(file, _data, cache)` matching the `frontmatter: { 'lc-slug': 'two-sum', 'lc-language': 'python' }` shape. Assert: `childView.dispatch` called once with `effects: languageCompartment.reconfigure(...)`-shaped payload. Use `expect.objectContaining({ effects: expect.anything() })` since Compartment effect identity is opaque; assert the dispatch spec has no `changes` payload (effect-only dispatch).
    - Test 2 ("fm change to same slug — no dispatch / Pitfall 3 dedupe"): same setup but parent fence opener already says `python` and frontmatter `lc-language: python`. Invoke handler. Assert: `childView.dispatch` NOT called (Gate 3 equality check).
    - Test 3 ("note without lc-slug — no dispatch"): cache.frontmatter has only `lc-language` but no `lc-slug`. Assert: handler returns early; childView.dispatch not called.
    - Test 4 ("file not in registry — no dispatch"): cache has `lc-slug` and changed `lc-language`, but registry.get returns undefined. Assert: childView.dispatch not called.
    - Test 5 ("D-14 — listener does NOT call vault.process or processFrontMatter"): exhaustive — mock both `app.vault.process` and `app.fileManager.processFrontMatter`. Run all four scenarios. Assert: NEITHER spy was called in any scenario.
    - Test 6 ("D-13 dispatch carries no userEvent annotation — effect-only path"): assert the dispatch spec object has no `userEvent` key (effect dispatches don't need it; not subject to section-lock changeFilter per Pattern F in PATTERNS). The test body MUST include an inline comment immediately above the no-`changes`/no-`userEvent` assertions reading verbatim: `// Effect-only dispatches (no changes: payload) are not subject to the section-lock changeFilter per CLAUDE.md §Conventions — this assertion guards that no changes payload was accidentally added.` This guards a future maintainer from silently adding a `changes:` payload (which would make the dispatch subject to the section lock without the userEvent escape) without re-deriving the convention.
  </behavior>
  <action>
    Create `tests/main/fmReactivity.test.ts` from scratch. Bootstrap with `vi.mock('obsidian', ...)`. Build a `makeMockPlugin()` factory returning an object with: `childEditorRegistry: makeMockRegistry()`, `app: { metadataCache: { getFileCache: vi.fn() }, vault: { process: vi.fn() }, fileManager: { processFrontMatter: vi.fn() }, workspace: { getActiveViewOfType: vi.fn() } }`, `settings: { getIndentSizeOverride: vi.fn(() => undefined) }`. Build a `makeFmHandler(plugin)` helper that constructs the same handler body Task 2 will register — OR (preferred for testability) Task 2 exports `createFmReactivityHandler(plugin)` from a new co-located helper, and this test imports it directly. Cover the six test cases above. The tests MUST FAIL on `main` because the handler does not yet exist (import error or missing function). Run `npm test -- tests/main/fmReactivity.test.ts` and confirm RED before commit.
  </action>
  <verify>
    <automated>npm test -- tests/main/fmReactivity.test.ts 2>&1 | grep -E "FAIL|✗|×|Cannot find" ; test ${PIPESTATUS[1]} -eq 0 || echo "RED state confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/main/fmReactivity.test.ts` exists with at least 6 `it(...)` blocks
    - File contains the literal strings `lc-language`, `lc-slug`, `Compartment.reconfigure` (or `languageCompartment.reconfigure`)
    - `npm test -- tests/main/fmReactivity.test.ts` exits non-zero on `main` (RED)
    - Test 5 explicitly asserts both `vault.process` AND `processFrontMatter` were NOT called (D-14 guard)
    - Test 6 explicitly asserts the dispatch spec has no `userEvent` field (effect-only)
    - Test 6 contains the literal explanatory comment string `Effect-only dispatches (no changes: payload) are not subject to the section-lock changeFilter` inline (CLAUDE.md §Conventions guard) — verifiable via `grep -c "Effect-only dispatches" tests/main/fmReactivity.test.ts` returning >= 1
    - Test 2 covers Pitfall 3 (no recursive dispatch on same-slug fm write)
  </acceptance_criteria>
  <done>Test file exists in RED state covering all five gates from Pattern 5 plus the D-14 no-body-rewrite invariant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register fm reactivity listener in src/main.ts onload (D-13)</name>
  <files>src/main.ts</files>
  <read_first>
    - src/main.ts (lines 820-880 — existing registerEvent block; locate the cluster near `workspace.on('file-open', ...)` for the new listener placement)
    - src/main.ts (lines 125, 255 — childEditorRegistry import + field declaration confirms registry availability in onload)
    - src/main/codeActionsEditorExtension.ts (lines 329-359 — Analog A; copy the structure exactly with the child-dispatch substitution)
    - src/main/childEditorLanguage.ts (lines 44, 132-148 — languageCompartment + buildLanguageExtensions imports)
    - src/solve/languages.ts (resolveLangSlug — for the readActiveFenceSlug helper if needed)
    - src/main/codeActionsEditorExtension.ts (lines 177-212 — findCodeFence SSoT, used by readActiveFenceSlug)
    - tests/main/fmReactivity.test.ts (Task 1 output — the assertions to satisfy)
    - 17-RESEARCH.md Pattern 5 (lines 354-389) for the handler body
    - 17-PATTERNS.md "src/main.ts metadataCache.on" entry (lines 261-330) for the exact target shape
  </read_first>
  <behavior>
    - Imports added to `src/main.ts`: `import { languageCompartment, buildLanguageExtensions } from './main/childEditorLanguage';` (if not already imported — check current import block).
    - In `onload(...)`, alongside the other `this.registerEvent(...)` calls (target line ~870, after the existing `workspace.on('file-open', ...)` registration), add:
      ```
      this.registerEvent(
        this.app.metadataCache.on('changed', (file, _data, cache) => {
          // Gate 1: lc-slug note only
          const slug = cache?.frontmatter?.['lc-slug'];
          if (typeof slug !== 'string' || slug.length === 0) return;
          // Gate 2: child registered
          const childView = this.childEditorRegistry?.get(file.path);
          if (!childView) return;
          // Gate 3: lc-language present and differs from current child language
          const fmLang = cache.frontmatter?.['lc-language'];
          if (typeof fmLang !== 'string' || fmLang.length === 0) return;
          const currentSlug = readActiveFenceSlug(file, this.app);
          if (currentSlug === fmLang) return;
          // Dispatch Compartment.reconfigure on child (D-13)
          childView.dispatch({
            effects: languageCompartment.reconfigure(
              buildLanguageExtensions(fmLang, this.settings.getIndentSizeOverride()),
            ),
            // No userEvent — effect-only, not subject to section-lock changeFilter
          });
        }),
      );
      ```
    - Add a private method `private readActiveFenceSlug(file: TFile, app: App): string | undefined` (or a free function near the top of the file) that uses `findCodeFence(EditorState.create({ doc: <parent text> }))` from `src/main/codeActionsEditorExtension.ts` to extract the slug from the opener line. If no parent CM6 view available, fall back to reading via `this.app.vault.cachedRead(file)` (synchronous read of cached content). For testability, prefer extracting the entire handler into a named helper exported by the source — `export function createFmReactivityHandler(plugin: Plugin)` returning the handler function — so the Task 1 test can import and exercise it directly without registering the event.
    - Per CONTEXT D-14: the handler must NOT call `app.vault.process(...)` or `app.fileManager.processFrontMatter(...)`. Frontmatter is the SoT in the listener's view; the fence opener stays whatever it currently says.
    - All six tests from Task 1 turn GREEN.
    - `npm run build` clean. No new TypeScript errors.
    - The listener is registered exactly once (no double-registration).
  </behavior>
  <action>
    Edit `src/main.ts`. Add the imports per Behavior. Locate the `onload(...)` method and add the new `this.registerEvent(this.app.metadataCache.on('changed', handler))` block after the existing `workspace.on('file-open', ...)` registration (use the analog at `src/main/codeActionsEditorExtension.ts:329-359` as the structural template; this is a direct port to the plugin-level `onload` with the child-dispatch substitution). Implement the handler as a private method or a co-located helper (preferred: extract `createFmReactivityHandler(plugin)` so Task 1's test can import it). Implement `readActiveFenceSlug(file, app)` using `findCodeFence` from `src/main/codeActionsEditorExtension.ts`; if `findCodeFence` is not exported, prefer importing it (add to its export list — small change in `codeActionsEditorExtension.ts`) over reimplementing. After implementation, run `npm test -- tests/main/fmReactivity.test.ts` and confirm GREEN; run `npm test` (full suite) — confirm no regressions in the chevron-switch tests; run `npm run build`.
  </action>
  <verify>
    <automated>npm test -- tests/main/fmReactivity.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/main/fmReactivity.test.ts` exits 0 (all 6 tests pass)
    - `npm test` (full suite) exits 0
    - `npm run build` exits 0
    - `grep -n "metadataCache\\.on('changed'" src/main.ts` returns at least one line in `onload`
    - `grep -c "languageCompartment\\.reconfigure" src/main.ts` returns >= 1
    - `grep -c "lc-language" src/main.ts` returns >= 1 (the gate check)
    - `grep -c "vault\\.process\\|processFrontMatter" src/main.ts` shows the same count as before this plan landed (no new vault.process or processFrontMatter calls added by the listener — D-14 guard)
    - The new listener block is registered via `this.registerEvent(...)` (auto-cleanup on plugin unload)
  </acceptance_criteria>
  <done>Listener registered, handler exported for testability, all tests green, build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold the Edge-Input UAT script (D-07..D-10, ready for Plan 17-06 execution)</name>
  <files>.planning/phases/17-polish-edge-cases/17-UAT.md</files>
  <read_first>
    - .planning/phases/16-language-packs-switching/16-UAT.md (full file — exact format anchor; copy frontmatter shape and Tests structure)
    - 17-CONTEXT.md D-07..D-10 (manual UAT scope)
    - 17-VALIDATION.md "Manual-Only Verifications" table (lines 79-94 — already enumerates PASTE-01..04, IME-01..03, SRCLIV-01)
    - 17-RESEARCH.md "Common Pitfalls" Pitfall 4 + Pitfall 5 (vim Esc shadowing; bracketMatching not dropped) — referenced in vim and theme UAT sections later but acknowledged here as cross-references
    - 17-PATTERNS.md "17-UAT.md (NEW manual UAT)" entry (lines 583-622) for required test sections
  </read_first>
  <behavior>
    - The UAT file is the structured script. Tests run in Plan 17-06 (Wave 3); this task ONLY produces the scaffold.
    - Frontmatter exactly matches 16-UAT.md format: `status: in-progress`, `phase: 17-polish-edge-cases`, `source: [17-01-SUMMARY.md, ..., 17-06-SUMMARY.md]`, `started: <ISO>`, `updated: <ISO>`.
    - File contains a `## Current Test` line set to `[testing pending — to be executed in Plan 17-06]`.
    - File contains a `## Tests` section with these test entries (each entry has the format from 16-UAT.md: numbered heading + `expected:` + `result: pending` + `notes: ""`):
      - **Test 1 — PASTE-01: Paste from VS Code → child editor (D-08)**: expected — code arrives raw in the child fence body, NOT formatted as markdown by Obsidian's clipboard interceptor; preserves indentation; multi-line block intact.
      - **Test 2 — PASTE-02: Paste from StackOverflow HTML → child (D-08)**: expected — HTML formatting stripped to raw code; no `<code>` tags or syntax-highlighted spans landing in the child doc.
      - **Test 3 — PASTE-03: Paste from LeetCode web copy → child (D-08)**: expected — LC's clipboard format arrives as raw code (LC sometimes wraps in markdown fences itself; the child must NOT preserve those fences).
      - **Test 4 — PASTE-04: Obsidian clipboard interceptor — markdown formatting suppressed (D-08)**: expected — paste arrives as raw text, no auto-link conversion, no smart-quote substitution inside the child fence.
      - **Test 5 — IME-01: Pinyin (Chinese) IME composition (D-09)**: expected — multi-keystroke pinyin compose → committed Chinese character lands once in the child doc; no duplication, no truncation, no early-commit during composition.
      - **Test 6 — IME-02: Romaji → kanji (Japanese) IME (D-09)**: expected — romaji input + space-bar conversion → committed kanji lands correctly; cursor advances by one composed character.
      - **Test 7 — IME-03: Hangul (Korean) IME (D-09)**: expected — Hangul jamo composition → composed syllable lands as one character; no orphaned jamo.
      - **Test 8 — SRCLIV-01: Source ↔ Live Preview parity with pending edits (D-10)**: expected — type into child editor (do NOT save), press Cmd-E to flip to Reading mode, press Cmd-E to flip back; child editor preserves cursor + pending edits; no widget re-mount visible to user.
      - **Test 9 — Phase 16 sanity regressions**: expected — re-run a curated subset of 16-UAT.md tests (Tests 2, 4, 5, 6, 9, 13, 14, 15, 16, 17, 18, 19) on the Phase-17 build. Each must still pass.
      - **Test 10 — Phase 17 Wave 1 regression sanity (Reset undo restored)**: expected — Open a populated solution; Cmd-P → Reset code → confirm; Cmd-Z (focus in editor) restores prior body; Cmd-Z (focus in ## Notes) is a no-op for code state. Notes section text NEVER receives the prior solution body. (Validates Plan 17-01.)
      - **Test 11 — Phase 17 Wave 1 regression sanity (Tab mid-line)**: expected — In a populated Java fence, place cursor mid-line (after `if (x)`), press Tab → 4 spaces inserted at cursor (mid-line behavior). Place cursor at line-start of the next line, press Tab → line indents by 4 spaces. Select two lines and press Tab → both indent in a single undo step. (Validates Plan 17-03.)
      - **Test 12 — Phase 17 Wave 2 regression sanity (fm reactivity)**: expected — Open a Java problem note; in a different pane, edit the note's frontmatter to `lc-language: python`; switch back to the note pane → child editor language reconfigures to Python (syntax highlight + indent unit reflect Python) without re-opening the note. (Validates this plan's Task 2.)
    - Placeholders for the Wave 3 sections (THEME, GO, VIM, LIFE, BUNDLE) are NOT in this scaffold; Plan 17-05 and 17-06 will append those sections when their work lands.
    - The file is NOT executed in this task — `result:` for each entry is `pending`.
  </behavior>
  <action>
    Create `.planning/phases/17-polish-edge-cases/17-UAT.md`. Use the 16-UAT.md frontmatter format exactly, with the `source:` array listing all six Phase 17 plan SUMMARYs (17-01-SUMMARY.md through 17-06-SUMMARY.md). Add `## Current Test` set to `[testing pending — execute in Plan 17-06]`. Add `## Tests` heading. Append the 12 numbered test entries above, each with the exact 4-line shape from 16-UAT.md: `### N. <Title> (<D-XX or REQ-ID>)`, blank line, `expected: <single-paragraph success criteria>`, `result: pending`, `notes: ""`. Tests 1-8 cover the Wave 2 manual UAT scope (paste/IME/Source-LP). Tests 9-12 are Phase-17 regression sanity checks for the Wave 1 fixes plus this plan's listener. Do NOT add Wave-3 sections (Plans 17-05 and 17-06 own those). Set frontmatter `status: in-progress` and `started: <today's ISO date>`.
  </action>
  <verify>
    <automated>test -f .planning/phases/17-polish-edge-cases/17-UAT.md && grep -c "^### " .planning/phases/17-polish-edge-cases/17-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/17-polish-edge-cases/17-UAT.md` exists
    - File contains exactly 12 `### N.` numbered test headings (numbered 1-12, none missing)
    - File contains the literal strings `PASTE-01`, `PASTE-02`, `PASTE-03`, `PASTE-04`, `IME-01`, `IME-02`, `IME-03`, `SRCLIV-01`
    - File contains 12 `result: pending` lines (one per test)
    - File frontmatter contains `phase: 17-polish-edge-cases` and `status: in-progress`
    - File frontmatter `source:` array references all six 17-XX-SUMMARY.md files
    - File does NOT contain any of the strings `THEME-01`, `VIM-01`, `LIFE-01`, `BUNDLE-01` (Wave 3 sections deferred to Plans 17-05/17-06)
  </acceptance_criteria>
  <done>Manual UAT script scaffolded with Wave 2 test sections + Wave 1 regression sanity sections, ready for Plan 17-06 execution.</done>
</task>

</tasks>

<verification>
- The fm reactivity listener is registered in onload via this.registerEvent (auto-cleanup on plugin unload)
- All Pitfall 3 cases (recursive metadataCache.changed during the plugin's own processFrontMatter) are dedupe-prevented by Gate 3
- 17-UAT.md follows the 16-UAT.md format exactly so Plan 17-06 can execute it without format friction
- Wave 1 plans (17-01, 17-02, 17-03) have all completed and committed before this plan starts (depends_on enforces this)
- This plan's edits to src/main.ts (onload area at lines ~820-880) do NOT overlap with Plan 17-01's edits (Reset wiring at lines ~2791-2803) — no merge conflict
</verification>

<success_criteria>
- External lc-language change → child editor reconfigures via Compartment.reconfigure within one metadataCache.changed event
- Listener does NOT rewrite fence opener tag (D-14 guard verified by test)
- Pitfall 3 dedupe (Gate 3 slug equality) prevents redundant dispatches on plugin's own fm writes
- 17-UAT.md scaffold exists with 12 test entries covering paste/IME/Source-LP plus Wave 1 regression sanity
- All tests in tests/main/fmReactivity.test.ts pass
- No regressions; build clean
</success_criteria>

<output>
Create `.planning/phases/17-polish-edge-cases/17-04-SUMMARY.md` when done summarizing:
- Files modified with line numbers
- New listener registration site (file:line)
- Test count + green run
- 17-UAT.md test count + section breakdown
</output>
