---
phase: 17-polish-edge-cases
plan: 06
type: execute
wave: 4
depends_on: ["17-04", "17-05"]
files_modified:
  - package.json
  - package-lock.json
  - src/main/childEditorFactory.ts  # ship-vim branch only (Task 3 conditional — see plan objective)
  - tests/main/lifecycle.test.ts
  - .planning/phases/17-polish-edge-cases/17-UAT.md
  - .planning/phases/17-polish-edge-cases/17-BUNDLE-AUDIT.md
  - .planning/phases/17-polish-edge-cases/17-LIFE-SNAPSHOT.md
autonomous: false
requirements: [INDENT-01, INDENT-02, INDENT-03, INDENT-04, ENTER-01, ENTER-02, ENTER-03, ENTER-04, BRACKET-01, BRACKET-02, BRACKET-03, BRACKET-04, LANG-01, COMMENT-01, HIGHLIGHT-01]
requirements_addressed: [INDENT-01, INDENT-02, INDENT-03, INDENT-04, ENTER-01, ENTER-02, ENTER-03, ENTER-04, BRACKET-01, BRACKET-02, BRACKET-03, BRACKET-04, LANG-01, COMMENT-01, HIGHLIGHT-01]

must_haves:
  truths:
    - "When app.vault.getConfig('vimMode') is true at child editor mount, the vim() extension from @replit/codemirror-vim is included in the child's extension array; when false, vim is excluded (no runtime cost beyond the bundled package — accepted per CONTEXT D-21)"
    - "Vim mode bundle delta is measured before commit; if final main.js size exceeds 1,600,000 bytes raw, vim is uninstalled and excluded from v1.2 (CONTEXT D-19 hard gate); the decision is recorded in 17-BUNDLE-AUDIT.md"
    - "ChildEditorRegistry.destroyAll calls destroy() on every cached EditorView and clears the cache (D-23 arm a — automated)"
    - "Plugin onunload invokes registry.destroyAll (D-23 — verified in source)"
    - "LRU eviction destroys the evicted view and unwires sync (D-23 arm a)"
    - "Manual UAT script in 17-UAT.md is executed end-to-end and every test has a result of pass / fail / deferred (Plan 17-06 closes the UAT loop) — including paste/IME/Source-LP/theme/Go/vim/heap-snapshot/bundle"
    - "Bundle audit doc 17-BUNDLE-AUDIT.md records final main.js size raw + gzipped, esbuild metafile contributor breakdown (top 10 modules by bytes), hard-gate verdict against 1.6 MB ceiling, and decisions on vim ship/defer + Go ship/defer"
    - "Heap-snapshot UAT result file (17-LIFE-SNAPSHOT.md) is committed in the phase directory per CONTEXT D-23 arm b — UAT notes alone are not sufficient; the snapshot result must be a standalone artifact with pass/fail verdict + numeric findings + DevTools snapshot summary"
    - "D-01: Scope Perimeter — v1.2 ships without plugin-store re-submission; that release step is handled in a separate post-Phase-17 release phase; this plan closes the phase internally."
    - "D-02: BRACKET-05, modular panel layout, and full IDE features remain v1.3 deferrals — not reopened in this phase."
    - "D-18: Vim mode mount logic — vim() extension is conditionally included in the child extension array only when app.vault.getConfig('vimMode') is true at child editor mount; excluded otherwise (no runtime cost for non-vim users)."
    - "D-20: Vim keymap interactions UAT — VIM-01..05 tests in 17-UAT.md verify Tab/Cmd-Z/Esc/Cmd-/ behavior in vim Insert and Normal modes, executed in Task 4 manual UAT pass."
    - "D-22 (advisory): suggested 3-wave shape from CONTEXT.md was implemented as 4 waves to honor file-disjointness in Wave 1 — Wave 4 split out for childEditorFactory.ts overlap with Wave 3."
  artifacts:
    - path: "package.json"
      provides: "@replit/codemirror-vim ^6.3.0 added (IF bundle gate passes); reverted otherwise"
    - path: "src/main/childEditorFactory.ts"
      provides: "vim() conditional spread inserted in extensions array per Pattern 4 — gated by app.vault.getConfig('vimMode')"
      contains: "vimMode"
    - path: "tests/main/lifecycle.test.ts"
      provides: "Vitest spec for ChildEditorRegistry.destroyAll, LRU eviction, set-replacement destroy, delete unwiresync, plugin onunload integration grep"
      min_lines: 80
    - path: ".planning/phases/17-polish-edge-cases/17-UAT.md"
      provides: "All 16 (or 16+VIM-01..05+LIFE-01+BUNDLE-01) test entries marked pass/fail/deferred with notes; status: complete"
    - path: ".planning/phases/17-polish-edge-cases/17-BUNDLE-AUDIT.md"
      provides: "Phase-close bundle audit per CONTEXT D-24 — raw/gzipped sizes, contributor breakdown, gate verdict, vim/Go decisions"
      min_lines: 30
    - path: ".planning/phases/17-polish-edge-cases/17-LIFE-SNAPSHOT.md"
      provides: "Heap-snapshot UAT result (LIFE-01) per CONTEXT D-23 arm b — pass/fail verdict + numeric findings (detached EditorView count, decoration set growth, registry size at end of cycle) + DevTools snapshot summary. Created and committed in Task 4."
      min_lines: 20
  key_links:
    - from: "src/main/childEditorFactory.ts createChildEditor extensions array"
      to: "vim() from @replit/codemirror-vim"
      via: "...(vimEnabled ? [vim()] : []) spread"
      pattern: "vimEnabled\\s*\\?\\s*\\[vim\\(\\)\\]"
    - from: "tests/main/lifecycle.test.ts"
      to: "src/main/childEditorRegistry.ts destroyAll, evictIfNeeded, delete"
      via: "direct import + makeMockEditorView fixture"
      pattern: "destroyAll\\|evictIfNeeded"
    - from: "17-BUNDLE-AUDIT.md"
      to: "scripts/check-bundle-size.mjs and esbuild metafile"
      via: "command output excerpts"
      pattern: "1,?600,?000\\|main\\.js"
---

<objective>
<!-- Scope note (revision 1, 2026-05-23): This plan has 6 tasks at the upper bound of the standard plan size, with autonomous: false correctly declared because Tasks 2 + 4 are blocking checkpoints. The 6-task count is acknowledged and intentional — splitting would fracture the bundle-gate decision tree across plans (Task 2 install → Task 3 apply → Task 6 audit must remain co-located so the bundle measurements are taken on the same finalised artifact per Pitfall 6). The autonomous: false declaration plus the linear, dependency-driven task graph is the mitigation. -->

Close v1.2 internally by (1) conditionally adding vim mode (CONTEXT D-18..D-21) gated on a hard bundle ceiling check at 1.6 MB, (2) shipping automated lifecycle tests covering ChildEditorRegistry.destroyAll/LRU/onunload (CONTEXT D-23 arm a), (3) executing the full 17-UAT.md manual script (paste/IME/Source-LP/theme/Go/vim/heap-snapshot — D-07..D-10/D-15..D-17/D-20/D-23b), and (4) producing the formal bundle-audit document (CONTEXT D-24).

Purpose: This is the final plan of v1.2. It is the only plan with checkpoints because vim install/decision (D-19), Go highlighting decision (D-17), heap-snapshot UAT (D-23b), and bundle gate verdict all require a human-in-the-loop pass/fail call. The Plan combines the conditional install + lifecycle test + UAT execution + audit doc because they all depend on the same finalised bundle state — bundle audit must be the last task per Pitfall 6 (premature measurement gives misleading numbers).

Output: Final v1.2 build with vim mode (or formally deferred), lifecycle tests passing, every 17-UAT.md test resolved, 17-BUNDLE-AUDIT.md committed, 17-LIFE-SNAPSHOT.md committed (D-23 arm b artifact), REQUIREMENTS.md updated if Go/vim deferred.

**File modification scope note:** `src/main/childEditorFactory.ts` is modified ONLY in the ship-vim branch of Task 3 (resume-signal == "ship-vim"). On the defer-vim or defer-vim-anyway branches, Task 3 reverts any pre-flight edits and the file ends Task 3 with no Phase-17 vim-related changes (Plan 17-05's themed-highlight changes remain). The static `files_modified` list includes the file to keep planner-tooling visibility, but the runtime touch is conditional on the bundle-gate verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-polish-edge-cases/17-CONTEXT.md
@.planning/phases/17-polish-edge-cases/17-RESEARCH.md
@.planning/phases/17-polish-edge-cases/17-PATTERNS.md
@.planning/phases/17-polish-edge-cases/17-VALIDATION.md
@.planning/phases/16-language-packs-switching/16-UAT.md
@.planning/phases/17-polish-edge-cases/17-04-SUMMARY.md
@.planning/phases/17-polish-edge-cases/17-05-SUMMARY.md
@scripts/check-bundle-size.mjs

<interfaces>
<!-- Final-wave conditional, lifecycle, audit primitives. -->

@replit/codemirror-vim 6.3.0 API (NEW dep — gated by D-19):
  // Returns the CM6 vim Extension. Adds keybindings, mode indicator, command palette.
  export function vim(): Extension;
- Peer deps: @codemirror/{view,state,search,commands,language} all 6.x — match the project's installed majors (verified 17-RESEARCH.md Package Legitimacy Audit).
- Estimated raw size: 60-80 KB; gzipped delta ~25 KB (assumption A2 in 17-RESEARCH).

Obsidian app.vault.getConfig API (TYPED via cast — undocumented):
  (app as unknown as { vault: { getConfig(key: string): unknown } }).vault.getConfig('vimMode'): unknown;
- Returns true when the user has enabled "Editor → Vim key bindings" in Obsidian settings.
- Read ONCE at child mount; matches Obsidian's behavior (vim setting changes don't take effect until note reopen).

src/main/childEditorRegistry.ts (READ; NOT modified — only tested):
  class ChildEditorRegistry {
    private cache: Map<string, EditorView>;
    private capacity: number;
    constructor(capacity: number);
    get(filePath: string): EditorView | undefined;       // touches LRU
    set(filePath: string, view: EditorView): void;       // evicts oldest if at capacity
    delete(filePath: string): void;
    has(filePath: string): boolean;
    destroyAll(): void;                                   // calls destroy() on every cached view
    get size(): number;
  }
- Plugin invokes destroyAll() in src/main.ts:939 onunload.
- Constructed at src/main.ts:794 with capacity 5.

scripts/check-bundle-size.mjs (project standard — invoked via npm run check:bundle-size):
  // Reads main.js, asserts size <= HARD_LIMIT (currently 1,600,000 bytes raw per Phase 16 ceiling-bump)
  // Logs raw + gzipped sizes; exits non-zero on breach.

esbuild metafile (project standard — generated by `npm run build` if --metafile flag is set):
  // Produces a JSON tree of input modules with byte-cost breakdown
  // Used in 17-BUNDLE-AUDIT.md to identify top contributors

src/main/childEditorFactory.ts:158-207 extensions array (the single child-editor mount point):
  - Plan 17-03 added customTabCommand at line ~185
  - Plan 17-05 added ...createThemedHighlight() at line ~177
  - This plan adds ...(vimEnabled ? [vim()] : []) BEFORE the keymap.of([customTabCommand, ...]) line
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add automated lifecycle tests for ChildEditorRegistry (D-23 arm a)</name>
  <files>tests/main/lifecycle.test.ts</files>
  <read_first>
    - src/main/childEditorRegistry.ts (full file — destroyAll, evictIfNeeded, set, delete, has, size, get behaviors)
    - src/main.ts (lines 794, 939 — registry init + destroyAll call in onunload)
    - tests/main/childEditorSync.test.ts (lines 9-19 vi.mock; lines 87-97 makeMockEditorView pattern; lines 99-109 makeMockRegistry — adapt for the real registry under test)
    - 17-RESEARCH.md "Lifecycle leak test" code block (lines 620-659) for the test cases
    - 17-PATTERNS.md "tests/main/lifecycle.test.ts" entry (lines 553-580) for read_first guidance and the makeMockEditorView shape
  </read_first>
  <behavior>
    - Test 1 ("destroyAll calls destroy on every cached EditorView and clears the cache"): construct ChildEditorRegistry(5); set 3 mock views; call destroyAll(); assert each view's destroy() spy was called exactly once; assert registry.size === 0.
    - Test 2 ("LRU eviction destroys the evicted view"): construct ChildEditorRegistry(2); set v1 at 'a', v2 at 'b'; touch 'a' via get('a') (b becomes LRU); set v3 at 'c' (evicts b); assert v2.destroy called once; assert registry.has('b') === false; assert registry.has('a') === true; assert registry.has('c') === true.
    - Test 3 ("set on existing key destroys old view before replacing"): construct ChildEditorRegistry(5); set v1 at 'a'; set v2 at 'a' (replace); assert v1.destroy called once; assert registry.get('a') === v2.
    - Test 4 ("delete calls destroy on the view and removes from map"): set v1; call delete('a'); assert v1.destroy called once; assert registry.has('a') === false; assert registry.size === 0.
    - Test 5 ("plugin onunload integration — registry.destroyAll is invoked"): use grep (or `node:fs.readFileSync`) to confirm `src/main.ts` contains the literal string `this.childEditorRegistry?.destroyAll();` (or equivalent) inside an `onunload` method body. This is a static assertion — it does not actually exercise the plugin lifecycle, but it pins the integration point so a refactor that drops the call breaks this test.
    - Test 6 ("destroyAll is idempotent — calling twice does not throw"): call destroyAll() twice in succession; assert no exception, registry.size remains 0.
    - The test must use the REAL `ChildEditorRegistry` class under test, NOT a mock of it. Mocks are only for the EditorView instances passed in as values.
  </behavior>
  <action>
    Create `tests/main/lifecycle.test.ts` from scratch. Bootstrap with `vi.mock('obsidian', ...)`. Import `ChildEditorRegistry` from `'../../src/main/childEditorRegistry'`. Build `makeMockEditorView()` per 17-RESEARCH.md lines 565-572: `{ state: { doc: { length: 0, toString: () => '' } }, dispatch: vi.fn(), destroy: vi.fn(), dom: { parentElement: null } } as unknown as EditorView`. Cover the six test cases above. For Test 5, use `node:fs.readFileSync('src/main.ts', 'utf-8')` and `expect(content).toMatch(/onunload[\\s\\S]*childEditorRegistry\\??\\.destroyAll\\(\\)/)`. Run `npm test -- tests/main/lifecycle.test.ts` and confirm GREEN. Run `npm run build`.
  </action>
  <verify>
    <automated>npm test -- tests/main/lifecycle.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/main/lifecycle.test.ts` exists with at least 6 `it(...)` blocks
    - `npm test -- tests/main/lifecycle.test.ts` exits 0
    - `npm run build` exits 0
    - File contains the literal string `destroyAll` in at least 3 places (test names + assertions)
    - File contains the literal string `evictIfNeeded` OR a test case named "LRU eviction"
    - Test 5 contains a static grep/readFileSync assertion against `src/main.ts` for the `onunload` + `destroyAll` integration
  </acceptance_criteria>
  <done>Lifecycle test suite covers destroyAll / LRU eviction / set-replace / delete / onunload integration / idempotency.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Vim mode install + bundle gate decision (D-19 hard gate)</name>
  <decision>Install @replit/codemirror-vim and add the conditional spread, OR formally defer vim to v1.3 if the bundle delta exceeds the 1.6 MB ceiling.</decision>
  <context>
CONTEXT D-19 sets a HARD ceiling at 1,600,000 bytes raw. Current Phase 16 baseline is 1,578,000 bytes (per 16-UAT.md Test 20). Headroom is ~22 KB. Estimated vim package raw size is 60-80 KB unminified, ~25 KB gzipped (17-RESEARCH.md A2 — assumption). Per CONTEXT D-21, esbuild's CJS+nosplit config means the vim package will be bundled even when unused at runtime; the conditional only saves runtime cost, not bundle cost.

Plan-of-record: measure → install → measure → decide (one-shot, no iteration; if it fails the gate, the package is uninstalled and the decision is recorded as v1.3 deferral).

This task is interactive because the decision changes downstream artifacts:
- If vim ships: package.json gets the new dep, childEditorFactory.ts gets the conditional spread, 17-UAT.md gets VIM-01..05 entries, REQUIREMENTS.md is unchanged.
- If vim defers: package.json untouched, childEditorFactory.ts untouched, 17-UAT.md gets a single "VIM-01 — DEFERRED to v1.3" stub entry, RETROSPECTIVE.md or 17-06-SUMMARY.md records the deferral.

Pre-flight automated work the executor MUST perform BEFORE asking the user:
1. Run `npm run build` and `npm run check:bundle-size`. Record the baseline raw + gzipped bytes from the script output.
2. Run `npm install @replit/codemirror-vim@^6.3.0` (creates the package-lock.json change).
3. Edit `src/main/childEditorFactory.ts` per 17-PATTERNS.md lines 506-516: add `import { vim } from '@replit/codemirror-vim';` (with eslint-disable-next-line for external); read `app.vault.getConfig('vimMode')` once at the top of `createChildEditor`; spread `...(vimEnabled ? [vim()] : [])` in the extensions array BEFORE the keymap.of([customTabCommand, ...]) line. Update inline comments to note Phase 17 D-18.
4. Run `npm run build` again and `npm run check:bundle-size`. Record the new raw + gzipped bytes. Compute delta (new - baseline).
5. Compute `GATE_VERDICT`:
   - PASS if `new_raw <= 1600000`
   - FAIL otherwise
6. Surface BOTH outcomes (numbers + verdict + recommended action) to the user via this checkpoint and wait for the user's resume signal.
  </context>
  <options>
    <option id="ship-vim">
      <name>Ship vim — gate PASSES (new raw size ≤ 1,600,000)</name>
      <pros>D-18 satisfied; vim users get parity with Obsidian's global vim setting; no per-user setting added; aligns with CONTEXT "Vim mode should match the user's existing Obsidian vim experience"</pros>
      <cons>Bundle headroom shrinks further; future v1.2.x patches must keep the same ceiling</cons>
    </option>
    <option id="defer-vim">
      <name>Defer vim to v1.3 — gate FAILS (new raw size > 1,600,000)</name>
      <pros>v1.2 ships at the agreed 1.6 MB ceiling; no risk of late-stage size-regression rollback; D-17 cuts scope rather than raising the ceiling (CONTEXT D-19 explicit instruction)</pros>
      <cons>Vim users still need a workaround; D-18 carries to v1.3; one fewer polish item closes in v1.2</cons>
    </option>
    <option id="defer-vim-anyway">
      <name>Defer vim regardless — even if gate would pass</name>
      <pros>Conservative; preserves headroom for unknowns</pros>
      <cons>Loses a CONTEXT-locked feature for no quantitative reason; needs explicit user override</cons>
    </option>
  </options>
  <resume-signal>Reply with: ship-vim | defer-vim | defer-vim-anyway</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply the vim decision (post-checkpoint actions)</name>
  <files>package.json, package-lock.json, src/main/childEditorFactory.ts, .planning/phases/17-polish-edge-cases/17-UAT.md</files>
  <read_first>
    - The checkpoint resume signal from Task 2 (one of: ship-vim | defer-vim | defer-vim-anyway)
    - 17-PATTERNS.md "src/main/childEditorFactory.ts extension array swap" Target shape (lines 506-516) — for ship-vim path
    - 17-RESEARCH.md Pattern 4 (lines 311-352) and "Vim keymap interactions" table (lines 333-343) — for ship-vim path
    - .planning/REQUIREMENTS.md (Future Requirements section — for defer-vim entry shape if needed)
    - 17-CONTEXT.md D-18..D-21 (locked decisions and conditional rules)
  </read_first>
  <behavior>
    - **If resume-signal == "ship-vim"** (gate passed):
      - Keep the package.json + package-lock.json + childEditorFactory.ts edits made during Task 2 pre-flight.
      - Append VIM-01..05 test entries to `17-UAT.md` (continuing numbering after Test 16 from Plan 17-05 → Tests 17-21):
        - VIM-01 (D-18): Toggle Obsidian's global vim setting ON; reopen a problem note; verify the child editor enters vim Normal mode (cursor block visible; can press `i` to enter Insert mode).
        - VIM-02 (D-20): In vim Insert mode, press Tab → behavior follows Plan 17-03 customTabCommand (mid-line vs line-start). In vim Normal mode, Tab is the vim default (next-jump).
        - VIM-03 (D-20): In vim Insert mode, Cmd-/ toggles line comment with the language's comment syntax (COMMENT-01 still works — verifies the Phase 16 Scope-based override fires regardless of vim mode).
        - VIM-04 (D-20): Esc returns to vim Normal mode; second Esc OR clicking into ## Notes returns focus to parent (Phase 15 escape hatch — vim Esc shadowing is documented per Pitfall 4, not fixed; this test confirms the documented two-press path works).
        - VIM-05 (D-20): `:w` in vim Normal mode is a no-op (Obsidian auto-saves; users use Cmd-S for explicit save). Document if behavior differs.
    - **If resume-signal == "defer-vim" or "defer-vim-anyway"** (deferred):
      - Run `npm uninstall @replit/codemirror-vim` to remove the dep + lockfile entry.
      - Revert all edits to `src/main/childEditorFactory.ts` (vim import + vimEnabled read + conditional spread + comment block).
      - `git diff src/main/childEditorFactory.ts package.json package-lock.json` after this step shows only Plan 17-05's themed-highlight changes (no vim-related diffs).
      - Append a single VIM-01 stub entry to 17-UAT.md (becomes Test 17): "VIM-01 (D-18 — DEFERRED): Vim mode deferred to v1.3 per CONTEXT D-19 hard bundle gate (delta + current > 1,600,000 bytes raw). See 17-BUNDLE-AUDIT.md for measurements." `result: deferred`.
      - Add a Future Requirements entry to `.planning/REQUIREMENTS.md`: `**FUTURE-05**: Vim mode in child editor (matches Obsidian's vimMode setting) — deferred from v1.2 Phase 17 per bundle ceiling gate; revisit in v1.3.`
    - In both branches, run `npm run build` and `npm test` (full suite) and confirm GREEN.
  </behavior>
  <action>
    Read the resume signal from Task 2. Branch accordingly per Behavior. For ship-vim: confirm the pre-flight edits are still in place; append VIM-01..05 to 17-UAT.md (Tests 17-21) using the same `### N. Title (D-XX)` + `expected:` + `result: pending` + `notes: ""` format; run full test suite + build. For defer-vim/defer-vim-anyway: run `npm uninstall @replit/codemirror-vim`; revert `src/main/childEditorFactory.ts` to its pre-Task-2 state (re-apply Plan 17-05's themed-highlight changes if they were preserved correctly through the npm operations); append a single VIM-01 stub to 17-UAT.md (Test 17) with `result: deferred`; add FUTURE-05 to REQUIREMENTS.md; run full test suite + build. Either way, end this task with `npm test` GREEN, `npm run build` clean, and the 17-UAT.md test count reflecting the chosen path (Tests 1-21 if shipped; Tests 1-17 if deferred).
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0
    - `npm run build` exits 0
    - `npm run check:bundle-size` exits 0 (size at or under 1,600,000 bytes raw)
    - If ship-vim: `grep -c "@replit/codemirror-vim" package.json` returns 1; `grep -c "vimEnabled\\|vimMode" src/main/childEditorFactory.ts` returns >= 2; 17-UAT.md `### ` count is 21
    - If defer-vim: `grep -c "@replit/codemirror-vim" package.json` returns 0; `grep -c "vim()" src/main/childEditorFactory.ts` returns 0; 17-UAT.md contains the literal string `DEFERRED` and `result: deferred`; REQUIREMENTS.md contains `FUTURE-05`
    - 17-UAT.md `### ` count is exactly 21 (ship) or 17 (defer) — pinned by the chosen branch
  </acceptance_criteria>
  <done>Vim decision applied; build clean; bundle gate passing; UAT and requirements docs updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Execute the manual UAT script (17-UAT.md PASTE/IME/SRCLIV/THEME/HIGHLIGHT-DARK/GO/[VIM]/LIFE)</name>
  <what-built>
Plan 17-04 scaffolded Tests 1-12 (paste, IME, Source-LP, Wave 1 regression sanity, Wave 2 fm reactivity sanity).
Plan 17-05 added Tests 13-16 (THEME-01..02, HIGHLIGHT-DARK-01, GO-01).
This plan's Task 3 added Tests 17-21 (VIM-01..05) OR Test 17 (VIM-01 deferred) per the bundle gate.
Plus this Task adds Test N+1 (LIFE-01 — heap-snapshot UAT, D-23 arm b) — append before executing.

The user opens the dev vault, follows each test's `expected:` script, marks `result: pass` / `fail` / `deferred`, and adds short `notes:` capturing observations. Manual-only: edge inputs (paste/IME/Source-LP) and visual checks (theme legibility, bracket-match contrast, Go colorization, vim mode functional) and DevTools heap snapshots cannot be reliably automated.
  </what-built>
  <how-to-verify>
1. **Pre-flight (executor performs BEFORE handing off):** Append a final test entry to 17-UAT.md — `### N. LIFE-01: Heap-snapshot lifecycle UAT (D-23 arm b)` (numbered 22 if vim shipped, 18 if vim deferred). `expected:` — Open and close 20 different lc-slug problem notes in the dev vault; on the 20th close, take a Chromium DevTools heap snapshot via Memory tab → Take snapshot; verify the snapshot contains zero detached `EditorView` instances (filter by class name); decoration set count is stable (not growing); registry capacity is 5 (oldest views were destroyed on capacity overflow). `result: pending` `notes: ""`.

2. **Pre-flight (executor performs BEFORE handing off):** Update the 17-UAT.md frontmatter `updated:` field to the current ISO timestamp, set `status: in-progress`, and set the `## Current Test` line to `[testing in progress — manual UAT execution]`.

3. **Hand off to user.** The user opens Obsidian dev vault and works through every test in 17-UAT.md sequentially. For each:
   - Reads the test's `expected:` paragraph
   - Performs the action(s)
   - Marks `result: pass | fail | deferred`
   - Appends a brief `notes: "..."` line capturing what they saw

4. **Heap snapshot guidance for LIFE-01:** Use Obsidian's built-in DevTools (Cmd-Shift-I on macOS / Ctrl-Shift-I on Win/Linux); switch to Memory tab; take a baseline snapshot before opening any notes; open and close 20 different lc-slug notes (any combination — the list is in the dev vault); take a second snapshot after the 20th close; in the diff view filter by `EditorView` and verify the count is stable (a small registry-capacity number — 5 — is acceptable; growth beyond that indicates a leak).

4a. **CONTEXT D-23 arm b — Snapshot result artifact (mandatory):** After completing LIFE-01, the executor (post-resume) MUST create `.planning/phases/17-polish-edge-cases/17-LIFE-SNAPSHOT.md` and commit it. UAT notes alone are NOT sufficient per D-23 arm b. The file body has these required sections (markdown headings):
   - `# Phase 17 Heap-Snapshot UAT Result (LIFE-01, CONTEXT D-23 arm b)`
   - `## Verdict` — single line: `PASS` or `FAIL`
   - `## Numeric Findings` — table with rows: `Detached EditorView count (after 20 cycles)`, `Decoration set count growth (baseline vs final)`, `Registry size at end of cycle`, `Notes section count change` (each with measured value)
   - `## DevTools Snapshot Summary` — pasted summary from DevTools Memory tab (top retainers + filter-by-EditorView count)
   - `## Test Conditions` — Obsidian version, dev-vault problem note count, OS, plugin commit SHA at time of snapshot
   - `## Decision` — if PASS: "D-23 satisfied; no v1.3 follow-up required." If FAIL: "Filed as v1.3 follow-up; see <ticket/note path>." File length must be at least 20 lines.

5. **Go highlighting decision (Test 16 GO-01):** If colorized → `result: pass`. If plain text → `result: fail`, `notes: "Defer Go highlighting to v1.3 per CONTEXT D-17 escape clause"`, AND the executor (post-resume) updates REQUIREMENTS.md to add a `FUTURE-06` entry analogous to FUTURE-05.

6. **After all tests are resolved:** User responds with summary — total tests, pass count, fail count, deferred count, blockers (if any).
  </how-to-verify>
  <resume-signal>Reply with: "UAT complete: pass=X fail=Y deferred=Z [blockers: ...]" — or describe issues that need a follow-up plan</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Apply UAT outcomes — update REQUIREMENTS.md if Go deferred + finalise 17-UAT.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/17-polish-edge-cases/17-UAT.md, .planning/phases/17-polish-edge-cases/17-LIFE-SNAPSHOT.md</files>
  <read_first>
    - The Task 4 resume signal (UAT pass/fail/deferred counts)
    - .planning/phases/17-polish-edge-cases/17-UAT.md (current state — every test now has a result)
    - .planning/REQUIREMENTS.md (Future Requirements section — for FUTURE-06 Go entry if needed; FUTURE-05 vim entry from Task 3 if added)
  </read_first>
  <behavior>
    - Read every test's final `result:` value in 17-UAT.md.
    - **CONTEXT D-23 arm b artifact (mandatory):** Create `.planning/phases/17-polish-edge-cases/17-LIFE-SNAPSHOT.md` from the LIFE-01 results captured during Task 4. The file body must contain the six sections enumerated in Task 4 step 4a (Verdict, Numeric Findings, DevTools Snapshot Summary, Test Conditions, Decision). The file length must be at least 20 lines. UAT notes alone are NOT sufficient per CONTEXT D-23 arm b — this standalone artifact is required.
    - If Test 16 GO-01 result is `fail` (or `deferred`):
      - Add a Future Requirements entry to `.planning/REQUIREMENTS.md`: `**FUTURE-06**: Go syntax highlighting in child editor (legacy-modes StreamLanguage tag-binding) — deferred from v1.2 Phase 17 per CONTEXT D-17 escape clause; binding to themedHighlightStyle did not produce colorization in UAT; revisit in v1.3.`
    - If any test has result `fail` AND was not converted to a deferral, surface the failure: list the failing test IDs in this plan's SUMMARY and STOP — do not mark the phase complete. The user can then triage and either convert to a deferral or run `/gsd:plan-phase 17 --gaps` to add a follow-up plan.
    - If all tests are `pass` or `deferred`:
      - Update 17-UAT.md frontmatter `status: complete`.
      - Update 17-UAT.md `## Current Test` line to `[testing complete]`.
      - Append a `## Summary` block to 17-UAT.md mirroring the 16-UAT.md format (lines 104-114 of 16-UAT.md): `total: N`, `passed: X`, `failed: Y`, `deferred: Z`, `pending: 0`, `phase17_polish_carry: 0`.
    - The plan does NOT proceed to bundle audit (Task 6) until UAT is fully resolved (no pending or unresolved-fail tests).
  </behavior>
  <action>
    Read the Task 4 resume signal and inspect 17-UAT.md. If Test 16 GO-01 failed/deferred, append `FUTURE-06` to `.planning/REQUIREMENTS.md` Future Requirements section. If any other test failed without conversion to deferral, list the failures in this task's output and STOP — surface to user. Otherwise, update 17-UAT.md frontmatter `status: complete`, set `## Current Test` to `[testing complete]`, and append a `## Summary` block matching 16-UAT.md format.
  </action>
  <verify>
    <automated>grep -c "result: pending" .planning/phases/17-polish-edge-cases/17-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "result: pending" .planning/phases/17-polish-edge-cases/17-UAT.md` returns 0 (every test resolved)
    - 17-UAT.md frontmatter contains `status: complete`
    - 17-UAT.md contains a `## Summary` block with `total:`, `passed:`, `failed:`, `deferred:`, `pending: 0` lines
    - File `.planning/phases/17-polish-edge-cases/17-LIFE-SNAPSHOT.md` exists with the six required sections (Verdict / Numeric Findings / DevTools Snapshot Summary / Test Conditions / Decision) and at least 20 lines — `grep -c "^## " .planning/phases/17-polish-edge-cases/17-LIFE-SNAPSHOT.md` returns >= 5; `grep -E "^(PASS|FAIL)$|^## Verdict$" .planning/phases/17-polish-edge-cases/17-LIFE-SNAPSHOT.md` returns at least one match (D-23 arm b artifact mandatory per CONTEXT)
    - If any test had `result: fail`, REQUIREMENTS.md contains a corresponding FUTURE-XX entry OR the SUMMARY explicitly enumerates the unresolved failures (no silent failures)
    - If GO-01 deferred, REQUIREMENTS.md contains `FUTURE-06`
  </acceptance_criteria>
  <done>UAT outcomes recorded; 17-LIFE-SNAPSHOT.md committed (D-23 arm b); REQUIREMENTS.md updated with any new FUTURE-XX deferrals; UAT marked complete OR failures surfaced.</done>
</task>

<task type="auto">
  <name>Task 6: Produce 17-BUNDLE-AUDIT.md (D-24)</name>
  <files>.planning/phases/17-polish-edge-cases/17-BUNDLE-AUDIT.md</files>
  <read_first>
    - scripts/check-bundle-size.mjs (full file — confirms HARD_LIMIT and SOFT_WARN values; current ceiling 1,600,000)
    - .planning/phases/16-language-packs-switching/16-UAT.md (lines 99-102 — Test 20 narrative bundle decision; precedent format)
    - 17-CONTEXT.md D-19, D-24 (bundle gate + audit requirements)
    - 17-RESEARCH.md "Architectural Responsibility Map" row "Bundle audit (D-24)"
    - 17-PATTERNS.md "17-BUNDLE-AUDIT.md (NEW docs)" entry (lines 624-633)
    - .planning/phases/17-polish-edge-cases/17-UAT.md (final state — Task 5 closed it; this audit references its conclusions)
  </read_first>
  <behavior>
    - Pre-flight: run `npm run build`, `npm run check:bundle-size`. Capture raw + gzipped bytes from the script output. Record exact values.
    - Pre-flight: regenerate the esbuild metafile by running the build with `--metafile` flag (or its existing equivalent — check `esbuild.config.mjs` or the package.json build script). If a metafile script is not available, parse the output of `node esbuild.config.mjs production --metafile` if such a path exists; if not, document "metafile not available — use `npm run check:bundle-size` output for top-level breakdown" and use `npx esbuild-bundle-analyzer` if available, or skip the contributor-table column with a note. The contributor table is best-effort if tooling is missing — the raw + gzipped + ceiling-verdict are MANDATORY.
    - Document body must contain these sections (markdown headings):
      - `# Phase 17 Bundle Audit (CONTEXT D-24)`
      - `## Final Build Sizes` — table with rows: Raw (bytes), Gzipped (bytes), CLAUDE.md ceiling (1,600,000), Headroom (calculated: 1,600,000 - raw)
      - `## Hard Gate Verdict` — single line: `PASS — under 1.6 MB ceiling` OR `FAIL — over ceiling, blocking`
      - `## Top Contributors` — table from esbuild metafile listing the top 10 modules by raw bytes (best-effort; if metafile not available, document "metafile not regenerated; using check:bundle-size summary"). Columns: Module path, Raw bytes, % of total
      - `## Decisions Recorded` — list of phase-end decisions:
        - Vim mode: SHIPPED (per Task 2/3 ship-vim path) OR DEFERRED to v1.3 (per Task 2/3 defer paths)
        - Go highlighting: SHIPPED (per Task 4 GO-01 pass) OR DEFERRED to v1.3 (per Task 4 GO-01 fail/defer)
        - Other deferrals: cite FUTURE-XX entries added to REQUIREMENTS.md
      - `## Comparison vs Phase 16 Baseline` — table comparing this phase's final raw/gzipped vs Phase 16 ending values (1,577,935 raw / 418,581 gzipped per 16-UAT.md Test 20)
      - `## Phase Close-Out Statement` — one-paragraph summary: v1.2 milestone state, what shipped, what deferred, ceiling status
    - The audit doc is the LAST artifact created in Phase 17 (per Pitfall 6 — bundle measurement must happen after all Wave-3 work merges; this task runs after Tasks 1-5 are complete and committed).
    - File length must be at least 30 lines.
  </behavior>
  <action>
    Run `npm run build`. Run `npm run check:bundle-size` and capture the script's stdout — extract raw + gzipped bytes. Attempt to regenerate the esbuild metafile (check `esbuild.config.mjs` for an existing metafile flag; if `npm run build` already produces one, parse it; if not, run `node esbuild.config.mjs production --metafile=meta.json` if that path exists; otherwise document the limitation and proceed without the contributor table). Create `.planning/phases/17-polish-edge-cases/17-BUNDLE-AUDIT.md` from scratch with the seven sections in Behavior. Use the actual measured byte values; do not estimate. The Decisions Recorded section reflects the actual outcomes from Task 3 (vim ship/defer) and Task 5 (Go ship/defer). Compute headroom and confirm hard-gate verdict. The phase close-out statement summarises which CONTEXT decisions D-01..D-24 were satisfied (every D-XX referenced in the document).
  </action>
  <verify>
    <automated>test -f .planning/phases/17-polish-edge-cases/17-BUNDLE-AUDIT.md && grep -c "PASS\\|FAIL" .planning/phases/17-polish-edge-cases/17-BUNDLE-AUDIT.md && npm run check:bundle-size</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/17-polish-edge-cases/17-BUNDLE-AUDIT.md` exists
    - File contains the seven required section headings (Final Build Sizes, Hard Gate Verdict, Top Contributors, Decisions Recorded, Comparison vs Phase 16 Baseline, Phase Close-Out Statement) — `grep -c "^## " .planning/phases/17-polish-edge-cases/17-BUNDLE-AUDIT.md` returns >= 6
    - File contains the literal string `1,600,000` or `1600000` (the ceiling)
    - File contains either `PASS — under` or `FAIL — over` (the gate verdict — exactly one outcome line)
    - File contains a row referencing the Phase 16 baseline (`1,577,935` or similar)
    - `npm run check:bundle-size` exits 0 (the build is at or under 1,600,000)
    - File length is at least 30 lines
  </acceptance_criteria>
  <done>Bundle audit recorded; gate verdict on file; vim and Go decisions documented; v1.2 phase-close statement written.</done>
</task>

</tasks>

<verification>
- All Wave-1, Wave-2, Wave-3 tasks have completed and committed before this plan's Task 6 audit runs (Pitfall 6 mitigation)
- 17-UAT.md is fully resolved (status: complete; no pending tests) before the bundle audit task runs
- Vim install is gated on hard ceiling check, not on a planner judgment of difficulty (CONTEXT D-19 explicit)
- Manual UAT execution covers every locked decision that requires visual or interactive verification (D-07..D-10, D-15..D-17, D-20, D-23b)
- Heap-snapshot UAT (D-23 arm b) is appended to 17-UAT.md right before manual handoff and executed by the user — no automated alternative is in scope (per CONTEXT D-23: "manual is probably enough for v1.2")
</verification>

<success_criteria>
- ChildEditorRegistry lifecycle tests are GREEN (D-23 arm a)
- Vim mode is shipped (gate PASS) or deferred (gate FAIL with REQUIREMENTS FUTURE-05 entry) — explicit, evidence-based decision
- Every 17-UAT.md test has result: pass / fail / deferred (no pending)
- Go highlighting decision recorded (ship or defer with FUTURE-06 entry)
- 17-BUNDLE-AUDIT.md committed with raw + gzipped sizes, top-contributors table (best-effort), gate verdict, vim/Go decisions, phase close-out statement
- npm run build clean; full test suite GREEN; npm run check:bundle-size exits 0
- v1.2 milestone is internally complete; plugin-store re-submission is OUT of scope per CONTEXT D-01 (handled in a separate post-Phase-17 release phase)
</success_criteria>

<output>
Create `.planning/phases/17-polish-edge-cases/17-06-SUMMARY.md` when done summarizing:
- Lifecycle test count + green run output
- Vim decision (shipped / deferred) with bundle measurements
- UAT execution outcomes (pass / fail / deferred counts)
- Go highlighting decision
- Bundle audit final raw + gzipped + verdict
- v1.2 phase close-out summary
- FUTURE-XX entries added to REQUIREMENTS.md (if any)
</output>
