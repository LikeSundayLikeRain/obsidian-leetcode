---
phase: 17-polish-edge-cases
plan: 05
type: execute
wave: 3
depends_on: ["17-03", "17-04"]
files_modified:
  - src/main/childEditorTheme.ts
  - src/main/childEditorFactory.ts
  - tests/main/childEditorTheme.test.ts
  - .planning/phases/17-polish-edge-cases/17-UAT.md
autonomous: true
requirements: [HIGHLIGHT-01]
requirements_addressed: [HIGHLIGHT-01]

must_haves:
  truths:
    - "The child editor renders syntax colors using Obsidian CSS variables (--code-keyword, --code-string, --code-comment, --code-function, --code-tag, --code-property, --code-operator, --code-value, --text-error) so colors track Obsidian's theme switches without code changes"
    - "Bracket-match highlight in the child editor uses high-contrast Obsidian theme variables (--code-keyword foreground + --background-modifier-active-hover background + 1px outline) — visible in BOTH dark and light Obsidian themes (resolves Phase 16 cosmetic gap from 16-UAT.md Test 9)"
    - "bracketMatching() extension at src/main/childEditorFactory.ts:178 is preserved — only the syntaxHighlighting(defaultHighlightStyle) line at ~177 is swapped for createThemedHighlight() (Pitfall 5 mitigation)"
    - "Go syntax highlighting status is determined empirically: if the themed HighlightStyle binds to legacy-modes Go tags (UAT pass), Go ships colorized in v1.2; if not, Go remains plain text and is documented as a v1.3 deferral per CONTEXT D-17 escape clause — decision recorded in 17-UAT.md GO-01 result"
    - "HIGHLIGHT-01 (bracket-match visibility) regression test in 16-UAT.md Test 9 still passes — bracket match still fires; only the styling changed"
    - "D-15: Themed HighlightStyle build — HighlightStyle.define entries map Lezer tags to Obsidian CSS variables (var(--code-keyword), var(--code-string), etc.) so syntax colors track theme switches without code changes."
    - "D-16: Bracket-match contrast — .cm-matchingBracket uses var(--code-keyword) foreground + var(--background-modifier-active-hover) background + 1px outline, visible in both dark and light Obsidian themes."
  artifacts:
    - path: "src/main/childEditorTheme.ts"
      provides: "createThemedHighlight() — Extension array combining HighlightStyle bound to Obsidian CSS variables and a EditorView.theme block for .cm-matchingBracket / .cm-nonmatchingBracket"
      min_lines: 30
      contains: "createThemedHighlight"
    - path: "src/main/childEditorFactory.ts"
      provides: "syntaxHighlighting(defaultHighlightStyle) replaced with ...createThemedHighlight(); defaultHighlightStyle import removed; bracketMatching() preserved untouched"
      contains: "createThemedHighlight"
    - path: "tests/main/childEditorTheme.test.ts"
      provides: "Vitest spec verifying createThemedHighlight returns an Extension array of expected shape and the .cm-matchingBracket selectors are present in the theme spec"
      min_lines: 40
    - path: ".planning/phases/17-polish-edge-cases/17-UAT.md"
      provides: "Append THEME-01..02 (dark+light theme legibility), HIGHLIGHT-DARK-01 (bracket-match contrast), GO-01 (Go colorization conditional) test entries"
  key_links:
    - from: "src/main/childEditorFactory.ts extensions array"
      to: "createThemedHighlight()"
      via: "...createThemedHighlight() spread, replacing syntaxHighlighting(defaultHighlightStyle)"
      pattern: "createThemedHighlight\\(\\)"
    - from: "src/main/childEditorTheme.ts HighlightStyle.define"
      to: "Obsidian CSS variables"
      via: "var(--code-keyword), var(--code-string), etc."
      pattern: "var\\(--code-(keyword|string|comment|function|tag|property|operator|value)\\)"
    - from: "src/main/childEditorTheme.ts EditorView.theme block"
      to: ".cm-matchingBracket selector"
      via: "background-modifier-active-hover + outline"
      pattern: "\\.cm-matchingBracket"
---

<objective>
Build a single Obsidian-CSS-variable-bound `HighlightStyle` for the child editor (CONTEXT D-15), replacing `defaultHighlightStyle` at `src/main/childEditorFactory.ts:177`. Bundle a high-contrast bracket-match theme (CONTEXT D-16) that solves the Phase 16 carry-over dark-mode contrast gap. Conditionally ship Go syntax highlighting (CONTEXT D-17) — if applying the themed style to legacy-modes Go produces colorization, ship it; if not (UAT-driven decision), leave Go as plain text and document as v1.3 deferral.

Purpose: Closes the Phase 16 carry-over cosmetic gap (16-UAT.md Test 9 — "hard to see in dark mode") and the Go highlighting gap (16-UAT.md Test 3 secondary — "Go is plain text"). Establishes theme-tracking colors so syntax highlight feels native to Obsidian regardless of theme switches.

Output: New `src/main/childEditorTheme.ts` module exporting `createThemedHighlight()`, the swap in `childEditorFactory.ts`, a vitest spec verifying the module's export shape, UAT script entries for THEME-01..02 / HIGHLIGHT-DARK-01 / GO-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-polish-edge-cases/17-CONTEXT.md
@.planning/phases/17-polish-edge-cases/17-RESEARCH.md
@.planning/phases/17-polish-edge-cases/17-PATTERNS.md
@.planning/phases/16-language-packs-switching/16-UAT.md
@.planning/phases/17-polish-edge-cases/17-03-SUMMARY.md
@.planning/phases/17-polish-edge-cases/17-04-SUMMARY.md

<interfaces>
<!-- Extension factories and CM6 APIs reused. -->

@codemirror/language API (already a project dep):
  // Used to bind tag-color rules
  export const HighlightStyle: {
    define(specs: Array<{ tag: Tag | Tag[]; color?: string; fontStyle?: string; fontWeight?: string; }>): HighlightStyle;
  };

  // Wraps a HighlightStyle as a CM6 Extension
  export function syntaxHighlighting(style: HighlightStyle): Extension;

@lezer/highlight (transitive via @codemirror/language — already loaded):
  export const tags: {
    keyword: Tag;
    string: Tag;
    comment: Tag;
    function: (inner: Tag) => Tag;
    variableName: Tag;
    tagName: Tag;
    angleBracket: Tag;
    propertyName: Tag;
    className: Tag;
    operator: Tag;
    number: Tag;
    bool: Tag;
    null: Tag;
    typeName: Tag;
    regexp: Tag;
    escape: Tag;
    special: (inner: Tag) => Tag;
    invalid: Tag;
    // ... others
  };

@codemirror/view API:
  // Returns an Extension that injects CSS rules
  EditorView.theme(spec: { [selector: string]: { [property: string]: string } }, options?: { dark?: boolean }): Extension;

src/main/childEditorFactory.ts current state (READ; modify ONLY the syntaxHighlighting line and its import):
  // line ~35-37 imports:
  import { syntaxHighlighting, defaultHighlightStyle, bracketMatching } from '@codemirror/language';
                                ^^^^^^^^^^^^^^^^^^^^^^ <-- REMOVE this import
  // line ~177-178:
  syntaxHighlighting(defaultHighlightStyle),    // <-- REPLACE with ...createThemedHighlight()
  bracketMatching(),                             // <-- DO NOT TOUCH (Pitfall 5)

src/main/childEditorLanguage.ts (Pattern analog for the new module — similar import header + Extension factory shape):
  // eslint-disable-next-line import/no-extraneous-dependencies -- transitive peer of obsidian; external in esbuild
  import { Compartment, type Extension } from '@codemirror/state';
  // ... etc
  export function buildLanguageExtensions(slug, override): Extension[];

Obsidian CSS variables (published in Obsidian theme contract — confirmed in 17-RESEARCH.md Pattern 3 lines 248-268):
  --code-keyword, --code-string, --code-comment, --code-function, --code-tag,
  --code-property, --code-operator, --code-value, --code-normal,
  --background-modifier-active-hover, --text-error, --text-normal
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/main/childEditorTheme.ts with createThemedHighlight (D-15/D-16)</name>
  <files>src/main/childEditorTheme.ts, tests/main/childEditorTheme.test.ts</files>
  <read_first>
    - src/main/childEditorLanguage.ts (full file — pattern analog for the new module: import header with eslint-disable-next-line for CM6 externals, exported factory function returning Extension/Extension[])
    - src/main/childEditorFactory.ts (lines 33-44 — current imports, including the `defaultHighlightStyle` to be removed; lines 174-180 — the swap point)
    - tests/main/childEditorSync.test.ts (lines 9-19 vi.mock pattern for the test bootstrap)
    - 17-RESEARCH.md Pattern 3 (lines 246-310) for the HighlightStyle.define spec + bracket-match theme
    - 17-PATTERNS.md "src/main/childEditorTheme.ts" entry (lines 396-465) for the exact module body
    - 17-CONTEXT.md D-15, D-16, D-17 (locked decisions for theme + bracket-match + Go conditional)
  </read_first>
  <behavior>
    - New module `src/main/childEditorTheme.ts` exports a single function `createThemedHighlight(): Extension` (returning `Extension` rather than `Extension[]` — CM6 accepts `Extension | Extension[]` and the spread at the consumer site handles either; choose the array form for clarity if it matches the existing `buildLanguageExtensions` shape).
    - Inside the module:
      - Define `themedHighlightStyle` via `HighlightStyle.define([...])` mapping these Lezer tags to Obsidian CSS variables (per 17-RESEARCH.md Pattern 3 lines 248-268 and 17-PATTERNS.md lines 439-450):
        - `t.keyword` → `var(--code-keyword)`
        - `[t.string, t.special(t.string), t.regexp, t.escape]` → `var(--code-string)`
        - `t.comment` → `var(--code-comment)`, `fontStyle: 'italic'`
        - `t.function(t.variableName)` → `var(--code-function)`
        - `[t.tagName, t.angleBracket]` → `var(--code-tag)`
        - `[t.propertyName, t.className]` → `var(--code-property)`
        - `t.operator` → `var(--code-operator)`
        - `[t.number, t.bool, t.null]` → `var(--code-value)`
        - `t.typeName` → `var(--code-keyword)`
        - `t.invalid` → `var(--text-error)`
      - Define `themedBracketMatchTheme` via `EditorView.theme({ '.cm-matchingBracket': { color: 'var(--code-keyword)', backgroundColor: 'var(--background-modifier-active-hover)', outline: '1px solid var(--code-keyword)', borderRadius: '2px' }, '.cm-nonmatchingBracket': { color: 'var(--text-error)' } })`.
      - Export `createThemedHighlight(): Extension[]` returning `[syntaxHighlighting(themedHighlightStyle), themedBracketMatchTheme]`.
    - Each CM6 import in `childEditorTheme.ts` carries the same `// eslint-disable-next-line import/no-extraneous-dependencies -- transitive peer of obsidian; external in esbuild` directive used by `childEditorLanguage.ts:18-19`.
    - Companion test `tests/main/childEditorTheme.test.ts` covers:
      - Test 1 ("createThemedHighlight returns a non-empty Extension array"): import the function; call it; assert the result is an array (or has Symbol.iterator) with length >= 2 (HighlightStyle wrapper + theme block).
      - Test 2 ("HighlightStyle uses Obsidian CSS variables"): inspect the returned Extensions; convert to string (or use the HighlightStyle instance directly via the exported style if exported separately). Assert the HighlightStyle's spec list contains the literal strings `'var(--code-keyword)'`, `'var(--code-string)'`, `'var(--code-comment)'` (any of these proves the theming binding is in place; we don't need to assert all). Implementation tip: export `themedHighlightStyle` (named export) for testability; mark as `@internal` or omit from index re-exports.
      - Test 3 (".cm-matchingBracket theme block exists"): export `themedBracketMatchTheme` for testability and assert its inspected/stringified spec contains `.cm-matchingBracket` and `var(--background-modifier-active-hover)` (or use the theme spec object directly — even simpler: export the raw spec object as `bracketMatchThemeSpec` for assertion).
      - Test 4 ("bracketMatching extension is NOT included in createThemedHighlight"): assert the returned array does NOT include CM6's `bracketMatching()` extension. Pitfall 5 says `bracketMatching()` stays in `childEditorFactory.ts:178` — this module only owns the highlight + theme.
    - The test must FAIL on `main` because the module doesn't yet exist (import error).
  </behavior>
  <action>
    Create `src/main/childEditorTheme.ts` from scratch using the full module body from 17-PATTERNS.md (lines 425-464) verbatim, with the eslint-disable directives. Export named `themedHighlightStyle`, `themedBracketMatchTheme`, AND a `bracketMatchThemeSpec` constant (the raw spec object passed to `EditorView.theme`) so the test can introspect it. Export `createThemedHighlight()` as the main consumer entry point. Concurrently create `tests/main/childEditorTheme.test.ts` from scratch with the four test cases above. Bootstrap with `vi.mock('obsidian', ...)`. Run `npm test -- tests/main/childEditorTheme.test.ts` and confirm GREEN (the module just created provides everything the test asserts). Run `npm run build` and confirm the new file compiles cleanly.
  </action>
  <verify>
    <automated>npm test -- tests/main/childEditorTheme.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/main/childEditorTheme.ts` exists
    - File `tests/main/childEditorTheme.test.ts` exists with at least 4 `it(...)` blocks
    - `npm test -- tests/main/childEditorTheme.test.ts` exits 0
    - `npm run build` exits 0
    - `grep -c "var(--code-keyword)" src/main/childEditorTheme.ts` returns >= 1
    - `grep -c "var(--code-string)" src/main/childEditorTheme.ts` returns >= 1
    - `grep -c "var(--code-comment)" src/main/childEditorTheme.ts` returns >= 1
    - `grep -c "var(--background-modifier-active-hover)" src/main/childEditorTheme.ts` returns >= 1
    - `grep -c "cm-matchingBracket" src/main/childEditorTheme.ts` returns >= 1
    - `grep -c "cm-nonmatchingBracket" src/main/childEditorTheme.ts` returns >= 1
    - `grep -c "createThemedHighlight" src/main/childEditorTheme.ts` returns >= 2 (definition + export)
    - File contains the eslint-disable-next-line directive matching the pattern from `childEditorLanguage.ts:18`
  </acceptance_criteria>
  <done>Themed highlight module exists with HighlightStyle bound to Obsidian CSS variables + bracket-match theme block; tests green; build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Swap defaultHighlightStyle for createThemedHighlight in childEditorFactory.ts (D-15/D-16, Pitfall 5 guard)</name>
  <files>src/main/childEditorFactory.ts</files>
  <read_first>
    - src/main/childEditorFactory.ts (lines 33-44 imports — confirm current `syntaxHighlighting` and `defaultHighlightStyle` imports; lines 174-180 — confirm the exact lines to swap)
    - src/main/childEditorTheme.ts (Task 1 output — exports to consume)
    - tests/main/childEditorTheme.test.ts (Task 1 output)
    - 17-PATTERNS.md "src/main/childEditorFactory.ts extension array swap" entry (lines 469-518) for the exact swap shape
    - 16-UAT.md Test 9 (HIGHLIGHT-01 bracket-match — must still pass after the swap)
  </read_first>
  <behavior>
    - In `src/main/childEditorFactory.ts:35-37`, edit the `@codemirror/language` import line: REMOVE `defaultHighlightStyle` from the named imports. KEEP `syntaxHighlighting` (it is used inside `childEditorTheme.ts`; the factory does not directly import it anymore — confirm no other reference) and KEEP `bracketMatching`. If `syntaxHighlighting` is no longer referenced in `childEditorFactory.ts` after the swap, remove it from the imports too.
    - Add a new import: `import { createThemedHighlight } from './childEditorTheme';` (relative path matching the sibling-module convention).
    - At line ~177, replace `syntaxHighlighting(defaultHighlightStyle),` with `...createThemedHighlight(),` (spread because `createThemedHighlight` returns `Extension[]`).
    - DO NOT touch line ~178 `bracketMatching(),` — Pitfall 5 mitigation. The bracket-match firing logic stays; only the styling is themed via the rule in `childEditorTheme.ts`.
    - Update the inline comment at lines ~175-178 to reflect the change: replace "Syntax highlighting + bracket matching (HIGHLIGHT-01 / D-15 unchanged from Phase 13)" with "Syntax highlighting (Phase 17 D-15/D-16: themed via Obsidian CSS variables — see src/main/childEditorTheme.ts) + bracket matching (HIGHLIGHT-01 firing logic unchanged; Phase 17 D-16 bracket-match contrast theme is bundled inside createThemedHighlight)".
    - All existing tests pass (especially `tests/main/childEditorSync.test.ts`, `tests/main/childEditorTheme.test.ts`, `tests/main/tabMidLine.test.ts`, `tests/main/fmReactivity.test.ts`).
    - `npm run build` clean.
    - `grep -c "defaultHighlightStyle" src/main/childEditorFactory.ts` returns 0 (the import is fully removed; no orphan reference).
  </behavior>
  <action>
    Edit `src/main/childEditorFactory.ts`. Modify the `@codemirror/language` import statement to remove `defaultHighlightStyle`. Add the import `import { createThemedHighlight } from './childEditorTheme';` near the existing local-module imports (alphabetical placement near `childEditorLanguage` import if present, else after the CM6 import block). Replace the line `syntaxHighlighting(defaultHighlightStyle),` with `...createThemedHighlight(),`. Update the surrounding inline comment per the Behavior contract. Run `npm test` (full suite) and confirm zero regressions; in particular, confirm `tests/main/childEditorTheme.test.ts` and any factory-level tests pass. Run `npm run build`.
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` (full suite) exits 0
    - `npm run build` exits 0
    - `grep -c "defaultHighlightStyle" src/main/childEditorFactory.ts` returns 0
    - `grep -c "createThemedHighlight" src/main/childEditorFactory.ts` returns >= 2 (import + spread call)
    - `grep -n "bracketMatching()" src/main/childEditorFactory.ts` returns at least one line in the extensions array (Pitfall 5 — preserved)
    - The line containing `...createThemedHighlight()` appears in the extensions array (between the keymap.of([closeBracketsKeymap]) section and the keymap.of([customTabCommand, ...]) section — verifiable by line ordering)
    - `npm test -- tests/main/childEditorSync.test.ts` exits 0 (no Phase 14 sync regressions)
  </acceptance_criteria>
  <done>defaultHighlightStyle removed; createThemedHighlight wired in via spread; bracketMatching preserved; all tests green; build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Append THEME-01..02, HIGHLIGHT-DARK-01, GO-01 to 17-UAT.md</name>
  <files>.planning/phases/17-polish-edge-cases/17-UAT.md</files>
  <read_first>
    - .planning/phases/17-polish-edge-cases/17-UAT.md (Plan 17-04's scaffold — confirm the format and existing test numbering up to Test 12)
    - .planning/phases/16-language-packs-switching/16-UAT.md (format anchor; Test 9 for the HIGHLIGHT-01 sanity check expected behavior)
    - 17-CONTEXT.md D-15, D-16, D-17 (locked decisions)
    - 17-PATTERNS.md "17-UAT.md (NEW manual UAT)" entry (lines 612-622) for the test sections to add
  </read_first>
  <behavior>
    - Append the following test entries to `.planning/phases/17-polish-edge-cases/17-UAT.md` after Test 12 (so they become Tests 13-16). Use the same `### N. <Title> (<D-XX>)` + `expected:` + `result: pending` + `notes: ""` format from 16-UAT.md and 17-04's scaffold:
      - **Test 13 — THEME-01: Themed HighlightStyle dark theme legibility (D-15)**: expected — Open a Java problem note in dark theme; verify keywords (class, public, return), strings, comments, function names, types use Obsidian's dark-theme code colors; visually distinct from each other; no white-on-white, no fully-invisible tokens.
      - **Test 14 — THEME-02: Themed HighlightStyle light theme legibility (D-15)**: expected — Switch Obsidian to a light theme (e.g., default light); reload the same note; verify the same syntax tokens now use light-theme colors automatically (no plugin reload, no note reopen needed); same legibility properties.
      - **Test 15 — HIGHLIGHT-DARK-01: Bracket-match contrast in dark theme (D-16)**: expected — In dark theme, place cursor adjacent to a `{`; the matching `}` is visibly highlighted with high-contrast foreground (--code-keyword color) on a tinted background (--background-modifier-active-hover); 1px outline visible; the highlight is clearly distinguishable from regular text. (Resolves 16-UAT.md Test 9 cosmetic carry-over.)
      - **Test 16 — GO-01: Go syntax highlighting after themed swap (D-17 conditional)**: expected — Switch to a Go fence (use chevron); type a Go function. CASE A: keywords (`func`, `package`, `return`), strings, comments are colorized with the themed HighlightStyle — Go ships colorized in v1.2; mark `result: pass`. CASE B: Go remains plain text — mark `result: fail`, set `notes: "Defer Go highlighting to v1.3 per CONTEXT D-17 escape clause — legacy-modes StreamLanguage tag binding requires explicit work beyond the ~20 LOC ceiling"`, AND update REQUIREMENTS.md to flag Go as a v1.3 deferral item; create a one-line entry in `.planning/RETROSPECTIVE.md` or the phase 17 SUMMARY that records the deferral.
    - The Test 16 description must explicitly enumerate both pass and fail outcomes per CONTEXT D-17 escape clause (small footprint else defer). Plan 17-06 will execute the test and record CASE A or CASE B.
    - File frontmatter `updated:` field is touched to today's ISO date (recommended; not strictly required).
    - No other tests are modified; no removals; numbering is contiguous (13, 14, 15, 16).
  </behavior>
  <action>
    Edit `.planning/phases/17-polish-edge-cases/17-UAT.md`. Append the four new test entries (Tests 13-16) after the existing Test 12 from Plan 17-04's scaffold. Each entry follows the `### N. Title (D-XX)` heading + blank line + `expected: ...` + `result: pending` + `notes: ""` shape. Test 16 must enumerate the two-outcome decision tree (CASE A: ship; CASE B: defer to v1.3 per D-17 escape clause) with explicit phrasing. Update the frontmatter `updated:` field to today's ISO date.
  </action>
  <verify>
    <automated>grep -c "^### " .planning/phases/17-polish-edge-cases/17-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^### " .planning/phases/17-polish-edge-cases/17-UAT.md` returns exactly 16 (Tests 1-12 from Plan 17-04 + Tests 13-16 added by this plan)
    - File contains the literal strings `THEME-01`, `THEME-02`, `HIGHLIGHT-DARK-01`, `GO-01`
    - Test 16 contains both `CASE A` and `CASE B` literal strings (two-outcome decision tree)
    - Test 16 contains the phrase `v1.3` (deferral language for D-17 escape clause)
    - Tests 13-16 each have `result: pending` (not yet executed; Plan 17-06 will execute)
  </acceptance_criteria>
  <done>UAT script extended with theme + bracket-match + Go test entries; result fields pending; ready for Plan 17-06 execution.</done>
</task>

</tasks>

<verification>
- The themed HighlightStyle module is a pure factory module — no side effects on import; all CM6 imports use external+eslint-disable directives matching `childEditorLanguage.ts` precedent
- bracketMatching() at `childEditorFactory.ts:178` is untouched (Pitfall 5 mitigation verified by grep)
- 16-UAT.md Test 9 (HIGHLIGHT-01) regression sanity is captured in 17-UAT.md Test 9 (Phase 16 sanity regressions, added by Plan 17-04)
- Go highlighting decision (D-17) is deferred to UAT execution in Plan 17-06 — this plan only sets up the infrastructure and the decision tree
- No bundle-impact concern in this plan (themed style is small, ~30 LOC, no new packages); bundle audit happens in Plan 17-06
</verification>

<success_criteria>
- src/main/childEditorTheme.ts exists with createThemedHighlight + themedHighlightStyle + themedBracketMatchTheme
- All HighlightStyle.define entries use Obsidian CSS variables
- .cm-matchingBracket theme uses --background-modifier-active-hover for bracket-match contrast
- src/main/childEditorFactory.ts:177 swap landed; defaultHighlightStyle import removed
- bracketMatching() preserved at line ~178 (Pitfall 5)
- 17-UAT.md scaffolded with THEME/HIGHLIGHT-DARK/GO test entries
- All tests green; build clean
</success_criteria>

<output>
Create `.planning/phases/17-polish-edge-cases/17-05-SUMMARY.md` when done summarizing:
- New module path + LOC
- Files modified with line numbers
- Tests added + assertion counts
- UAT entries appended (Tests 13-16)
- Confirmation that bracketMatching() is untouched at childEditorFactory.ts:178
</output>
