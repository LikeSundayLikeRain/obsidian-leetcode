---
phase: 13-nested-editor-foundation
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - src/main.ts
  - styles.css
autonomous: false
requirements:
  - INDENT-01
  - INDENT-02
  - INDENT-03
  - INDENT-04
  - ENTER-01
  - ENTER-02
  - ENTER-03
  - ENTER-04
  - BRACKET-01
  - BRACKET-02
  - BRACKET-03
  - BRACKET-04
  - BRACKET-05
  - LANG-01
  - COMMENT-01
  - HIGHLIGHT-01

must_haves:
  truths:
    - "Opening an lc-slug note with a Python fence shows child CM6 editor with syntax highlighting in the fence area"
    - "The raw fence lines (opener, body, closer) are visually hidden"
    - "The button row (Run/Submit/chevron) remains visible below the code editor"
    - "Switching notes and switching back preserves child editor state (cursor position)"
    - "Closing a note and reopening creates a fresh child editor"
    - "Plugin unload destroys all child editors (no memory leaks)"
    - "Section lock, code-actions, and language chevron continue to function"
  artifacts:
    - path: "src/main.ts"
      provides: "Extension registration + registry lifecycle on plugin instance"
      contains: "buildNestedEditorExtension"
    - path: "styles.css"
      provides: "CSS rules for lc-fence-hidden and lc-nested-editor"
      contains: "lc-fence-hidden"
  key_links:
    - from: "src/main.ts"
      to: "src/main/nestedEditorExtension.ts"
      via: "import buildNestedEditorExtension"
      pattern: "import.*buildNestedEditorExtension.*nestedEditorExtension"
    - from: "src/main.ts"
      to: "src/main/childEditorRegistry.ts"
      via: "import ChildEditorRegistry for instantiation"
      pattern: "import.*ChildEditorRegistry.*childEditorRegistry"
    - from: "styles.css"
      to: "src/main/nestedEditorExtension.ts"
      via: "lc-fence-hidden class name agreement"
      pattern: "lc-fence-hidden"
---

<objective>
Wire the nested editor extension into the plugin lifecycle (registration between code-actions and section-lock), add CSS rules for fence hiding and child editor styling, and instantiate/teardown the registry on plugin load/unload.

Purpose: This plan connects Plans 01 and 02 to the running plugin, making the nested editor visible and functional in Obsidian. It also adds the CSS that actually hides fence lines and styles the child editor container.

Output: Working end-to-end nested editor rendering in Source Mode (and Live Preview if CSS works), verified by human in Obsidian dev vault.
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/moxu/projects/obsidian-leetcode/.planning/phases/13-nested-editor-foundation/13-RESEARCH.md
@/Users/moxu/projects/obsidian-leetcode/.planning/phases/13-nested-editor-foundation/13-CONTEXT.md
@/Users/moxu/projects/obsidian-leetcode/src/main.ts (lines 780-800 for registration site, line 894 for onunload)
@/Users/moxu/projects/obsidian-leetcode/styles.css (lines 957-972 for CSS pattern)

<interfaces>
<!-- From Plan 01 -->
From src/main/childEditorRegistry.ts:
```typescript
export class ChildEditorRegistry {
  constructor(cap?: number);
  destroyAll(): void;
}
```

<!-- From Plan 02 -->
From src/main/nestedEditorExtension.ts:
```typescript
export function buildNestedEditorExtension(plugin: PluginHost): Extension;
```

<!-- Plugin host must expose childEditorRegistry property -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plugin wiring — registry lifecycle and extension registration</name>
  <files>src/main.ts</files>
  <read_first>
    - src/main.ts (lines 150-250 for property declarations, lines 780-800 for extension registration site, lines 894-950 for onunload)
    - src/main/childEditorRegistry.ts (exports — just created)
    - src/main/nestedEditorExtension.ts (exports — just created)
    - .planning/phases/13-nested-editor-foundation/13-PATTERNS.md (src/main.ts section — registration between code-actions and section-lock)
  </read_first>
  <action>
    Modify `src/main.ts`:

    1. **Import** at top: `import { ChildEditorRegistry } from './main/childEditorRegistry';` and `import { buildNestedEditorExtension } from './main/nestedEditorExtension';`

    2. **Property declaration** (near the other property declarations around line 190-230): Add `childEditorRegistry!: ChildEditorRegistry;` with a comment: `// Phase 13 — LRU cache for nested child EditorViews (cap=5, per D-12).`

    3. **Instantiation in onload** (before extension registration, around Step 6 area): Add `this.childEditorRegistry = new ChildEditorRegistry(5);` with comment `// Phase 13 — child editor registry (must exist before extensions fire).`

    4. **Extension registration** (between the `buildCodeActionsEditorExtension` call on line 787 and the `buildSectionLockExtension` call on line 797): Insert:
       ```
       // Step 6f-nested — Phase 13: nested child EditorView for ## Code fence.
       // Mounts a block widget containing a child CM6 EditorView with Python
       // syntax highlighting; hides raw fence lines via CSS Decoration.line.
       // Registered BETWEEN code-actions and section-lock so the cursor-redirect
       // transactionFilter processes before section-lock's cursor snap (Pitfall 3).
       this.registerEditorExtension(buildNestedEditorExtension(this));
       ```

    5. **Cleanup in onunload** (inside `onunload()` method, after existing teardown calls): Add `this.childEditorRegistry.destroyAll();` with comment `// Phase 13 — destroy all child EditorViews (D-12 cleanup).`
  </action>
  <acceptance_criteria>
    - src/main.ts contains `import { ChildEditorRegistry } from './main/childEditorRegistry'`
    - src/main.ts contains `import { buildNestedEditorExtension } from './main/nestedEditorExtension'`
    - src/main.ts contains `childEditorRegistry!: ChildEditorRegistry` property declaration
    - src/main.ts contains `this.childEditorRegistry = new ChildEditorRegistry(5)` before extension registration
    - src/main.ts contains `this.registerEditorExtension(buildNestedEditorExtension(this))` between the code-actions and section-lock registration calls
    - src/main.ts `onunload()` contains `this.childEditorRegistry.destroyAll()`
    - `npm run build` succeeds without errors
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/moxu/projects/obsidian-leetcode && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>main.ts registers the nested editor extension between code-actions and section-lock. Registry instantiated in onload, destroyed in onunload. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: CSS rules for fence hiding and nested editor styling</name>
  <files>styles.css</files>
  <read_first>
    - styles.css (lines 957-972 for existing section-lock CSS pattern, then check end of file for append location)
    - .planning/phases/13-nested-editor-foundation/13-RESEARCH.md (Pattern 2: CSS Line-Hiding, Pattern 3 theme block)
    - .planning/phases/13-nested-editor-foundation/13-PATTERNS.md (styles.css section with full CSS)
    - .planning/phases/13-nested-editor-foundation/13-CONTEXT.md (D-05: darker background matching code block styling)
  </read_first>
  <action>
    Append to `styles.css` (after existing rules, before any final closing comment):

    **Block 1 — Fence line hiding** (class `lc-fence-hidden` applied by Decoration.line in nestedEditorExtension.ts):
    - Scoped under `.cm-editor .lc-fence-hidden`
    - Properties: `height: 0 !important`, `overflow: hidden !important`, `opacity: 0 !important`, `pointer-events: none !important`, `padding: 0 !important`, `margin: 0 !important`, `border: 0 !important`, `line-height: 0 !important`
    - Comment header: `/* Phase 13 — Nested editor: hide fence lines (opener, body, closer). Content stays in document model; visually replaced by child EditorView widget. */`

    **Block 2 — Nested editor container** (class `lc-nested-editor` on widget container div):
    - `.cm-editor .lc-nested-editor`: `background: var(--code-background, var(--background-secondary))`, `border-radius: 4px`, `padding: 8px 0`
    - `.cm-editor .lc-nested-editor .cm-editor`: `background: transparent` (child editor inherits container bg)
    - `.cm-editor .lc-nested-editor .cm-content`: `font-family: var(--font-monospace)`, `font-size: 14px`
    - `.cm-editor .lc-nested-editor .cm-gutters`: `background: transparent`, `border-right: none`
    - `.cm-editor .lc-nested-editor .cm-activeLine`: `background: var(--background-modifier-hover)` (subtle active line in child)
    - Comment header: `/* Phase 13 — Nested editor container styling. Darker background matching Obsidian code block rendering (D-05). Uses only CSS variables — no hardcoded colors. */`

    No hardcoded hex colors (CLAUDE.md conventions). All values use Obsidian CSS variables.
  </action>
  <acceptance_criteria>
    - styles.css contains `.cm-editor .lc-fence-hidden` rule with `height: 0 !important`
    - styles.css contains `.cm-editor .lc-nested-editor` rule with `var(--code-background, var(--background-secondary))`
    - styles.css contains `.cm-editor .lc-nested-editor .cm-editor` rule with `background: transparent`
    - styles.css contains `.cm-editor .lc-nested-editor .cm-content` rule with `var(--font-monospace)`
    - styles.css contains `.cm-editor .lc-nested-editor .cm-gutters` rule
    - No hardcoded hex colors in the new CSS
    - `npm run build` succeeds (CSS is copied to output)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/moxu/projects/obsidian-leetcode && grep -c "lc-fence-hidden" styles.css && grep -c "lc-nested-editor" styles.css && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>CSS rules added for fence hiding and nested editor styling. Build passes. No hardcoded colors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end verification in Obsidian dev vault</name>
  <files>src/main.ts, styles.css</files>
  <action>
    Human verifies the complete Phase 13 nested editor foundation in Obsidian dev vault. All automation (code, CSS, wiring) completed in Tasks 1-2. This checkpoint confirms visual and interactive correctness that cannot be automated (requires live Obsidian instance with the plugin loaded).
  </action>
  <verify>
    <human-check>
      1. Run `npm run dev` in the project root
      2. Open Obsidian dev vault with hot-reload enabled
      3. Open any `lc-slug` note that has a Python code fence under `## Code`
      4. Source Mode check (non-negotiable): verify raw fence markers hidden, child editor with Python syntax highlighting visible, Run/Submit/chevron button row below, clicking into editor shows cursor and typing works, Python keywords have syntax coloring
      5. Live Preview check (best effort): toggle Cmd-E, verify nested editor renders correctly
      6. State preservation: type text + place cursor, switch notes and back, verify preserved
      7. Fresh creation on reopen: close tab, reopen, verify fresh content from vault
      8. Regression check: Run/Submit buttons work, language chevron works, section lock prevents editing Problem
      9. Focus redirect: from Notes section, arrow-up toward fence zone, verify cursor redirects to child
    </human-check>
  </verify>
  <done>Human confirms: child editor renders with Python highlighting in Source Mode, fence lines hidden, button row visible below, state preserved across note switches, clean lifecycle on close/reopen, no regressions in existing functionality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin → Obsidian host | Extension registration uses official API; no bypass of Obsidian's plugin sandbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Denial of Service | Memory leak from undestroyed child editors | mitigate | onunload calls registry.destroyAll(); LRU cap=5 prevents unbounded growth |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
```bash
cd /Users/moxu/projects/obsidian-leetcode && npm run build && npx vitest run tests/main/
```
</verification>

<success_criteria>
- Build succeeds with zero errors
- All existing tests pass (no regressions)
- All new Phase 13 tests pass
- Human verification confirms: child editor renders with Python highlighting, fence lines hidden, button row visible, state preserved across note switches, clean lifecycle on close/reopen
</success_criteria>

<output>
Create `.planning/phases/13-nested-editor-foundation/13-03-SUMMARY.md` when done
</output>
