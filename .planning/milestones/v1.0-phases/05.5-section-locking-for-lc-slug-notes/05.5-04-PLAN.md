---
phase: 05.5
plan: 04
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/main.ts
  - src/main/sectionLockExtension.ts
  - styles.css
  - README.md
  - CLAUDE.md
autonomous: true
requirements: []
requirements_addressed:
  - D-02
  - D-04
  - D-05
tags:
  - obsidian
  - codemirror6
  - wiring
  - polish
  - section-lock
must_haves:
  truths:
    - "main.ts onload registers buildSectionLockExtension(this) via this.registerEditorExtension immediately after the Phase 5.1 codeActionsEditorExtension registration (Step 6f-bis); the new line is positioned for chronological readability (5.1 → 05.5) but load order is functionally insensitive (CM6 facets compose)."
    - "sectionLockExtension.ts gains an optional Decoration.mark visual-dim layer composed into the returned Extension array; CSS rule `.cm-editor .leetcode-section-locked { background: var(--background-secondary); }` lives in styles.css using only Obsidian semantic CSS variables (no hardcoded colors per CF and Phase 5.4 D-06)."
    - "README.md gains a section under Phase 5 D-25 troubleshooting documenting the section lock UX: which sections are locked (## Problem entire region; ## Code heading + fence opener + closing fence; ## Techniques heading; ## Notes heading; ## Custom Tests NOT locked); silent keystroke-drop is intentional (no Notice — D-04); language switching still happens via the chevron (Phase 5.3 D-04)."
    - "CLAUDE.md Conventions section gains a single entry: `'leetcode.*' userEvent annotation is the bypass convention for plugin-internal CM6 dispatches; future plugin dispatches MUST set this annotation if they target locked ranges (RESEARCH Pitfall 5)`."
    - "All five smoke gates green: `npm test -- --run` exits 0; `npm run lint` exits 0; `npx tsc --noEmit` exits 0; `npm run build` exits 0; `bash scripts/check-bundle-size.sh` exits 0 (250 KB hard limit holds)."
    - "D-02: NO new vault writes introduced by this plan (lock is purely an editor-side filter); existing write paths (NoteWriter.backgroundRefresh, KnowledgeGraphWriter.onAccepted, copyToCode, switchFenceLanguage) remain byte-identical — verified by absence of vault.modify/vault.process/processFrontMatter additions in files_modified."
    - "D-05: NO migration of existing lc-slug notes — the lock applies uniformly from first plugin re-load after 05.5 ships; no scanner, no Move-to-Notes command, no first-load Notice, no `lc-locked` per-note frontmatter flag introduced (CF-12 preserved)."
  artifacts:
    - path: src/main.ts
      provides: "Step 6f-bis registers buildSectionLockExtension(this); new import at top of file"
      contains: "buildSectionLockExtension"
    - path: src/main/sectionLockExtension.ts
      provides: "Optional Decoration.mark visual-dim extension composed into the returned array"
      contains: "leetcode-section-locked"
    - path: styles.css
      provides: ".cm-editor .leetcode-section-locked { background: var(--background-secondary); }"
      contains: "leetcode-section-locked"
    - path: README.md
      provides: "Section Locking subsection under Phase 5 D-25 Troubleshooting"
      contains: "Section Locking"
    - path: CLAUDE.md
      provides: "Conventions entry for 'leetcode.*' userEvent bypass annotation"
      contains: "leetcode.* userEvent"
  key_links:
    - from: src/main.ts
      to: src/main/sectionLockExtension.ts
      via: "this.registerEditorExtension(buildSectionLockExtension(this)) in onload Step 6f-bis"
      pattern: "registerEditorExtension\\(buildSectionLockExtension"
    - from: src/main/sectionLockExtension.ts
      to: styles.css
      via: "Decoration.mark({ class: 'leetcode-section-locked' }) targets the CSS rule"
      pattern: "leetcode-section-locked"
    - from: README.md
      to: src/main/sectionLockExtension.ts
      via: "documents the user-facing behavior under Phase 5 D-25 Troubleshooting"
      pattern: "Section Locking"
    - from: CLAUDE.md
      to: src/main/sectionLockExtension.ts
      via: "documents the leetcode.* userEvent bypass convention future plugin code MUST honor"
      pattern: "leetcode\\.\\*"
---

<objective>
Wave 2 wiring + polish for Phase 05.5. Plan 02 ships the section-lock extension as a standalone module; Plan 04 wires it into the plugin's onload, adds the optional visual-dim CSS layer (CONTEXT D-04 planner discretion — research Open Question 4 recommended shipping with the lock for discoverability), and documents the user-facing behavior + the load-bearing `'leetcode.*'` userEvent convention so future contributors don't accidentally break the bypass.

Purpose: Make the section lock an active part of the running plugin (not just a built-but-unregistered module) and capture the convention knowledge in human-readable docs so the next plugin dispatcher Knows what to do.

Output:
- `src/main.ts` (MODIFIED) — one new import + one new `registerEditorExtension` call.
- `src/main/sectionLockExtension.ts` (MODIFIED) — append the optional Decoration.mark visual-dim extension into the returned Extension array; add the supporting helper.
- `styles.css` (MODIFIED) — single new CSS rule `.cm-editor .leetcode-section-locked { background: var(--background-secondary); }`.
- `README.md` (MODIFIED) — new "Section Locking" subsection under Phase 5 D-25 Troubleshooting/Configuration.
- `CLAUDE.md` (MODIFIED) — new Conventions entry documenting the `'leetcode.*'` userEvent bypass.

This plan shares `src/main/sectionLockExtension.ts` with Plan 02 — Plan 02 ran first (depends_on: [02] sequences this plan strictly after); the additions here are append-only on the returned Extension array and additive on a private helper. No risk of merge conflict because Plan 02's two-task unit set is fully complete before this plan starts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.5-section-locking-for-lc-slug-notes/05.5-CONTEXT.md
@.planning/phases/05.5-section-locking-for-lc-slug-notes/05.5-RESEARCH.md
@.planning/phases/05.5-section-locking-for-lc-slug-notes/05.5-PATTERNS.md
@.planning/phases/05.5-section-locking-for-lc-slug-notes/05.5-VALIDATION.md
@.planning/phases/05.5-section-locking-for-lc-slug-notes/05.5-01-PLAN.md
@.planning/phases/05.5-section-locking-for-lc-slug-notes/05.5-02-PLAN.md
@CLAUDE.md

# Files to modify
@src/main.ts
@src/main/sectionLockExtension.ts
@styles.css
@README.md

<interfaces>
<!-- Existing exports from Plan 02 — Plan 04 only ADDS to the returned Extension array. -->

```typescript
// src/main/sectionLockExtension.ts (Plan 02 GREEN; Plan 04 amends ONLY the returned array)
export function buildSectionLockExtension(plugin: Plugin): Extension {
  return [
    EditorState.changeFilter.of(...),       // Plan 02 — DO NOT TOUCH
    EditorView.atomicRanges.of(...),        // Plan 02 — DO NOT TOUCH
    EditorView.decorations.of((view) => buildLockedDecorations(view.state)),  // Plan 04 ADDS
  ];
}

// Plan 04 ADDS this private helper:
function buildLockedDecorations(state: EditorState): DecorationSet;
```

```typescript
// src/main.ts onload — Plan 04 adds Step 6f-bis registration
// Existing Step 6f line at ~line 378:
//   this.registerEditorExtension(buildCodeActionsEditorExtension(this));
// NEW Step 6f-bis line immediately after:
//   this.registerEditorExtension(buildSectionLockExtension(this));
```

```css
/* styles.css — single new rule */
.cm-editor .leetcode-section-locked {
  background: var(--background-secondary);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add visual-dim Decoration.mark layer to sectionLockExtension.ts and register in main.ts</name>
  <files>src/main/sectionLockExtension.ts, src/main.ts, styles.css</files>
  <read_first>
    - .planning/phases/05.5-section-locking-for-lc-slug-notes/05.5-CONTEXT.md (D-04 enforcement combinator stack — visual dim is planner-discretion; ship-by-default per RESEARCH Open Question 4)
    - .planning/phases/05.5-section-locking-for-lc-slug-notes/05.5-RESEARCH.md §Pattern 4 (lines 469-495 — Decoration.mark visual dim with Obsidian semantic CSS var); §Open Question 4 (recommendation: ship visual dim default-on for discoverability — without it, users only learn the lock exists by typing into a locked region and seeing nothing happen)
    - .planning/phases/05.5-section-locking-for-lc-slug-notes/05.5-PATTERNS.md (lines 158-170 — main.ts registration analog; lines 442-481 — styles.css analog with `.leetcode-language-chevron` rules using `var(--background-secondary)`)
    - src/main/sectionLockExtension.ts (Plan 02's GREEN file; Plan 04 appends to the returned Extension array — non-destructive)
    - src/main.ts (read lines 370-400 — existing Step 6e/6f/6g registration block; new line goes after the Phase 5.1 line at ~378)
    - styles.css (read lines 891-1036 — existing `.leetcode-code-actions` and `.leetcode-language-chevron` rules; new rule mirrors their `.cm-editor` selector + `var(--background-secondary)` idiom)
  </read_first>
  <behavior>
    - sectionLockExtension.ts gains a private function `buildLockedDecorations(state: EditorState): DecorationSet` that:
      - Calls `computeLockedRanges(state)` (existing pure helper from Plan 02 — no double scan; can also share via internal cache, but plain re-call is fine for v1).
      - Builds a `RangeSetBuilder<Decoration>`.
      - For each `[from, to]` pair where `from < to`, adds `Decoration.mark({ class: 'leetcode-section-locked' })` over that range.
      - Returns `b.finish()`.
    - The returned Extension array from buildSectionLockExtension gains a third entry: `EditorView.decorations.of((view) => buildLockedDecorations(view.state))`. Append AFTER the atomicRanges entry.
    - The visual dim is achieved purely through the CSS rule. The Decoration.mark only attaches the class.
    - main.ts gains:
      - One new import at the top: `import { buildSectionLockExtension } from './main/sectionLockExtension';` (positioned alphabetically/topically near the existing `buildCodeActionsEditorExtension` import).
      - One new line in onload Step 6f-bis: `this.registerEditorExtension(buildSectionLockExtension(this));` placed immediately after `this.registerEditorExtension(buildCodeActionsEditorExtension(this));` at the existing Step 6f location (~line 378).
      - A short prose comment block (3-5 lines) above the new line explaining what Step 6f-bis is, citing CONTEXT D-04 and the section-lock contract: "Step 6f-bis — Phase 05.5 (POLISH) section locking for lc-slug notes. Hard read-only enforcement via CM6 EditorState.changeFilter; gated on lc-slug frontmatter (D-06) + Edit Mode (D-07). Locks `## Problem` entirely; `## Code` heading + fence opener + closing fence; `## Techniques` heading; `## Notes` heading. `## Code` body and `## Techniques`/`## Notes` bodies stay editable. Plugin-side dispatches with userEvent='leetcode.*' bypass the lock so chevron switch keeps working (RESEARCH Pitfall 5)."
    - styles.css gains a single new rule (no other changes): `.cm-editor .leetcode-section-locked { background: var(--background-secondary); }` — placed near the existing `.cm-editor .leetcode-code-actions` rule for topical cohesion. Add a comment line above it: `/* Phase 05.5 (POLISH) — section-lock visual dim per CONTEXT D-04. */`.
    - All test suites stay green.
    - Bundle size remains within the 250 KB hard limit / 200 KB warn (existing scripts/check-bundle-size.sh gate). The new code is ~15 lines + 3 lines CSS — sub-1 KB delta.
  </behavior>
  <action>
    Step A — Edit `src/main/sectionLockExtension.ts`:

    1. Add a new private (non-exported) function `buildLockedDecorations(state: EditorState): DecorationSet` near `buildAtomicRangeSet`. Implementation mirrors `buildAtomicRangeSet` exactly except the Decoration class is `'leetcode-section-locked'` (the user-facing class that styles.css targets) instead of the atomic-only marker class.

    2. Modify the returned array of `buildSectionLockExtension` to append a third element: `EditorView.decorations.of((view) => buildLockedDecorations(view.state))`. Place it after the atomicRanges entry. Add an inline comment: `// Phase 05.5 D-04 (planner discretion / RESEARCH Open Q4 recommendation): visual-dim locked ranges for discoverability. CSS in styles.css uses var(--background-secondary).`

    3. Do NOT touch the existing changeFilter or atomicRanges entries. The contract Plan 02 locked stays byte-identical.

    Step B — Edit `src/main.ts`:

    1. Add the new import alongside the existing Phase 5.1 import. The exact location is wherever `buildCodeActionsEditorExtension` is currently imported; add `buildSectionLockExtension` import on the next line (or in a sibling import statement — match existing project import-organization style).

    2. Locate the existing line `this.registerEditorExtension(buildCodeActionsEditorExtension(this));` (current Step 6f at ~line 378). Immediately AFTER that line, insert:
       ```typescript
       // Step 6f-bis — Phase 05.5 (POLISH) section locking for lc-slug notes.
       // Hard read-only enforcement via CM6 EditorState.changeFilter; gated on
       // lc-slug frontmatter (D-06) + Edit Mode (D-07). Locks `## Problem`
       // entirely; `## Code` heading + fence opener + closing fence;
       // `## Techniques` heading; `## Notes` heading. `## Code` body and
       // `## Techniques`/`## Notes` bodies stay editable. Plugin-side dispatches
       // with userEvent='leetcode.*' bypass the lock so chevron switch keeps
       // working (RESEARCH Pitfall 5).
       this.registerEditorExtension(buildSectionLockExtension(this));
       ```

    3. Do NOT touch any other registration call or onload step.

    Step C — Edit `styles.css`:

    1. Locate the existing `.cm-editor .leetcode-code-actions` rule (~line 909). Immediately AFTER that rule's closing `}`, add:
       ```css
       /* Phase 05.5 (POLISH) — section-lock visual dim per CONTEXT D-04.
        * Only Obsidian semantic CSS vars; no hardcoded colors (CLAUDE.md §Conventions, Phase 5.4 D-06). */
       .cm-editor .leetcode-section-locked {
         background: var(--background-secondary);
       }
       ```

    2. Do NOT modify any other rule.

    Validate immediately:
      - `npx tsc --noEmit` exits 0.
      - `npm test -- --run` exits 0 (Plan 02 + Plan 03 baselines preserved; new wiring does not affect any test because Plan 02/03 tests do not load main.ts onload, and the visual-dim Decoration extension is functionally invisible to filter-only tests).
      - `npm run lint` exits 0.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "registerEditorExtension(buildSectionLockExtension(this))" src/main.ts` returns 1 match.
    - `grep -n "import.*buildSectionLockExtension" src/main.ts` returns 1 match (the new import).
    - `grep -n "Step 6f-bis" src/main.ts` returns 1 match (the comment landmark).
    - `grep -n "EditorView.decorations.of" src/main/sectionLockExtension.ts` returns 1 match (the new visual-dim layer).
    - `grep -n "leetcode-section-locked" src/main/sectionLockExtension.ts` returns at least 1 match (Decoration.mark class string).
    - `grep -n ".leetcode-section-locked" styles.css` returns at least 1 match (CSS rule).
    - `grep -n "var(--background-secondary)" styles.css | grep -c "leetcode-section-locked\|^$"` returns a non-zero match within 3 lines of the new selector (semantic-var compliance).
    - `grep -E "background: ?#[0-9a-fA-F]" styles.css | grep "leetcode-section-locked"` returns 0 matches (no hardcoded color in the new rule — CF + Phase 5.4 D-06).
    - `npx tsc --noEmit` exits 0.
    - `npm run lint` exits 0.
    - `npm test -- --run` exits 0 (full suite baseline preserved — Plan 02 + Plan 03 tests still green).
  </acceptance_criteria>
  <done>
    Section lock is wired into the plugin onload; visual dim CSS rule landed; type check + lint + full test suite all green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Document section lock in README.md and 'leetcode.*' userEvent convention in CLAUDE.md; final smoke gates</name>
  <files>README.md, CLAUDE.md</files>
  <read_first>
    - .planning/phases/05.5-section-locking-for-lc-slug-notes/05.5-CONTEXT.md (D-04 + the Specifics block "User wants discoverability concern documented in README under Phase 5 D-25 troubleshooting")
    - .planning/phases/05.5-section-locking-for-lc-slug-notes/05.5-RESEARCH.md §Pitfall 5 (lines 609-639 — the userEvent bypass surface; Plan 04 documents this convention permanently)
    - README.md (full file — locate the Phase 5 D-25 Troubleshooting / Configuration section that the planner referenced; pick the most specific subsection where Section Locking belongs)
    - CLAUDE.md (read the §Conventions section — the new entry slots in there; mirror the style of existing entries)
  </read_first>
  <behavior>
    - README.md gains a new "Section Locking" subsection (heading level 3 or 4 — match existing README hierarchy under Phase 5 D-25's Troubleshooting/Configuration block). Content covers:
      - What is locked: `## Problem` entire region; `## Code` heading + fence opener + closing fence; `## Techniques` heading; `## Notes` heading.
      - What is editable: `## Code` body (the active solving surface), `## Techniques` body (manual + future-AI insertion), `## Notes` body (user-owned content).
      - What is NOT locked: `## Custom Tests` (legacy section, plugin doesn't read or write).
      - UX behavior: typing or pasting into a locked region silently drops the keystroke (no Notice — D-04). Cursor can be placed inside via mouse click but typing has no effect.
      - Language switching: still goes through the chevron dropdown (Phase 5.3) — typing into the fence opener tag is locked; the chevron is the only path to change languages.
      - Why: prevents user edits from accidentally landing in regions the plugin overwrites on background-refresh (## Problem) / chevron-switch (## Code body) / on-AC writes (## Techniques + frontmatter). Existing notes work uniformly without migration (D-05).
      - Recovery: the chevron dropdown rewrites the fence opener atomically (Cmd-Z reverts); the Past Submissions / Copy-to-Code path overwrites the fence body silently (Phase 5.2 + 5.3 carry).
    - CLAUDE.md §Conventions gains a single new bullet/entry. Format matches existing entries; suggested wording: `'leetcode.*' userEvent annotation is the bypass convention for plugin-internal CM6 dispatches. Any future plugin dispatch that targets a locked range (## Problem entire region, ## Code heading + fence opener + closing fence, ## Techniques heading, ## Notes heading) MUST set userEvent: 'leetcode.<verb>' on its cm.dispatch spec, otherwise the Phase 05.5 section lock will silently drop the change. Audited callsites: src/main.ts:799-805 (switchFenceLanguage — sets 'leetcode.lang-switch'); src/main/codeActionsEditorExtension.ts:~320 (languageRefreshEffect — dispatches an effect with no changes; not subject to the filter). Vault.process / processFrontMatter writes (e.g., src/graph/copyToCode.ts) bypass the lock by design — they happen at the vault layer below CM6.`
    - All five smoke gates remain green:
      - `npm test -- --run` exits 0
      - `npm run lint` exits 0
      - `npx tsc --noEmit` exits 0
      - `npm run build` exits 0
      - `bash scripts/check-bundle-size.sh` exits 0
  </behavior>
  <action>
    Step A — Edit `README.md`:

    1. Locate the Phase 5 D-25 Troubleshooting / Configuration section. If the README's existing structure has a "Troubleshooting" header, place the new subsection there. If it has a "Configuration" header documenting plugin behaviors, place there. The exact target subsection is read from the file in `read_first`.

    2. Add a new heading (level 3 `###` or level 4 `####` matching the surrounding hierarchy): `### Section Locking` (or `#### Section Locking` if subordinate to a level-3 header).

    3. Below the heading, write 3-6 short paragraphs covering the bullets listed in the Behavior section above. Use plain prose; no code blocks needed unless illustrating the heading literals (a single inline code-fence with the four heading lines is fine).

    4. Do NOT touch any other section of README.md.

    Step B — Edit `CLAUDE.md`:

    1. Locate the §Conventions section. Identify the existing entry style (likely a hyphen-bulleted list of single-paragraph entries).

    2. Add a new entry as the LAST item in the Conventions list. Wording per Behavior section above. Cite the exact callsites verified by Plan 03's audit (src/main.ts:799-805 + src/graph/copyToCode.ts uses vault.process not cm.dispatch).

    3. Do NOT modify any other section of CLAUDE.md.

    Step C — Run all five smoke gates and confirm green:

    ```
    npm test -- --run
    npm run lint
    npx tsc --noEmit
    npm run build
    bash scripts/check-bundle-size.sh
    ```

    All five MUST exit 0. If any fails, the failure is a regression introduced by Task 1 or 2 — diagnose and fix before considering this plan complete. The README and CLAUDE.md edits are pure documentation and cannot fail any of these gates; the failure is upstream.
  </action>
  <verify>
    <automated>npm test -- --run &amp;&amp; npm run lint &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; bash scripts/check-bundle-size.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Section Locking" README.md` returns at least 1 match (the new subsection heading).
    - `grep -n "## Custom Tests.*not locked\|NOT locked\|never locked" README.md` returns at least 1 match (Custom Tests carve-out documented).
    - `grep -n "chevron" README.md | grep -i "language\|switch"` returns at least 1 match within the new subsection (language-switch path documented).
    - `grep -n "leetcode\\.\\*\|'leetcode\\." CLAUDE.md` returns at least 1 match (the new userEvent convention entry).
    - `grep -n "userEvent" CLAUDE.md` returns at least 1 match (matches the convention text).
    - `grep -n "lang-switch" CLAUDE.md` returns at least 1 match (cites the audited callsite).
    - `npm test -- --run` exits 0 — full suite green; cumulative count = Plan 01 baseline (~567) + Plan 01's 13+ + Plan 03's 6+ = ~586+.
    - `npm run lint` exits 0.
    - `npx tsc --noEmit` exits 0.
    - `npm run build` exits 0.
    - `bash scripts/check-bundle-size.sh` exits 0.
    - Bundle size has not increased materially: compare `du -k main.js` before / after; delta should be < 2 KB.
  </acceptance_criteria>
  <done>
    README documents the user-facing section-lock UX; CLAUDE.md documents the leetcode.* userEvent bypass convention; all five smoke gates pass; Phase 05.5 implementation is complete and ready for live-smoke verification.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keystroke / paste / IME → CM6 transaction → changeFilter | Inbound editor surface. Plan 04 changes nothing about how the filter consumes it; only adds visual dim + registration. |
| plugin-side cm.dispatch → CM6 transaction → changeFilter | Same surface. Plan 04 documents the `'leetcode.*'` bypass convention permanently in CLAUDE.md so future contributors honor it. |
| README/CLAUDE.md prose | Documentation only; no executable surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.5-01 | Tampering | computeLockedRanges (malformed-fence path) | mitigate | Plan 02 implementation pinned by Plan 01 RED tests + Plan 03 integration scenario 2. Plan 04 does not modify the filter logic. Severity: LOW. |
| T-05.5-02 | Spoofing | userEvent: 'leetcode.*' bypass | accept | Plan 04 PERMANENTLY documents the convention in CLAUDE.md §Conventions so future plugin contributions know they MUST set the annotation if they target locked ranges. The audit list (src/main.ts:799-805 = chevron; src/graph/copyToCode.ts uses vault.process not cm.dispatch — bypass-by-design per RESEARCH Pitfall 6) is recorded. If a future PR adds a new cm.dispatch callsite that touches locked ranges WITHOUT the annotation, that PR will break the chevron-or-similar UX in dogfood — and the CLAUDE.md entry surfaces the requirement during code review. Severity: LOW. |
</threat_model>

<verification>
After both tasks complete:

1. `npm test -- --run` → 0 failures, full suite green.
2. `npm run lint` → 0 violations.
3. `npx tsc --noEmit` → 0 type errors.
4. `npm run build` → bundle produced.
5. `bash scripts/check-bundle-size.sh` → bundle within 250 KB hard / 200 KB warn limits.
6. `grep -n "buildSectionLockExtension" src/main.ts` → 2 matches (import + registerEditorExtension call).
7. `grep -n "Step 6f-bis" src/main.ts` → 1 match (comment landmark).
8. `grep -n "leetcode-section-locked" styles.css src/main/sectionLockExtension.ts` → at least 2 matches (Decoration class + CSS selector).
9. `grep -n "Section Locking" README.md` → 1 match (new subsection).
10. `grep -n "leetcode\\.\\*" CLAUDE.md` → 1 match (new convention entry).
</verification>

<success_criteria>
- Section lock is wired into the plugin onload (Step 6f-bis registered alongside Phase 5.1 + 5.3 extensions).
- Visual-dim CSS layer ships default-on (per RESEARCH Open Question 4 recommendation; uses Obsidian semantic CSS variable only — no hardcoded colors).
- README documents the section-lock UX so users discovering "I typed and nothing happened" find the explanation immediately.
- CLAUDE.md documents the `'leetcode.*'` userEvent bypass convention so future plugin contributions don't accidentally break the chevron or any future plugin-side dispatch.
- All five smoke gates green — Phase 05.5 ready for live-smoke verification (which is out-of-scope for this plan; a future `/gsd-verify-work` or human smoke test against a dev vault closes the phase).
- D-02 invariants preserved: NO new vault writes; NO divergence detection; NO new commands; NO new frontmatter keys; NO new Notices.
</success_criteria>

<output>
After completion, create `.planning/phases/05.5-section-locking-for-lc-slug-notes/05.5-04-SUMMARY.md` capturing:
- Files modified (with line-count deltas)
- Pre / post bundle size (kb)
- Test count delta (Plan 03 baseline → after Plan 04)
- README + CLAUDE.md excerpt of the new entries
- Five-gate confirmation table (test / lint / tsc / build / bundle-size — each marked ✅)
- Any deviations from the plan + rationale
</output>
