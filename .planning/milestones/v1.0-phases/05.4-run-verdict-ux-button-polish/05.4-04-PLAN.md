---
phase: 05.4
plan: 04
type: execute
wave: 2
depends_on: ["05.4-01"]
files_modified:
  - styles.css
  - src/main/languageChevronWidget.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "All non-CTA buttons (RunModal tabs, +, ×, Reset, Verdict-modal case tabs, Verdict-modal Close-secondary if any) carry the .lc-fm__picker token set verbatim — D-09"
    - "RunModal tabs and VerdictModal case tabs share visual shape, active highlight, and pill sizing — D-10"
    - "Inline Run/Submit buttons (.leetcode-code-action-run / -submit) carry the new neutral-button polish — D-12a"
    - "Inline language chevron renders a Lucide chevron-down icon (via setIcon) followed by a label span — D-12b"
    - "Chevron pill shape matches .lc-fm__picker tokens (already ported in Phase 5.3 — verify still applies)"
    - "Output value text and Expected value text are class-gated: --fail variant shows var(--text-error)/var(--text-success); --pass variant shows var(--text-normal) — D-07 CSS half"
    - "Title color modifiers leetcode-verdict-title--success / --error apply var(--text-success) / var(--text-error) — D-07 title half"
    - "Verdict modal section surfaces use var(--background-secondary); section labels use var(--text-muted) — D-06 + D-07"
    - "PASS/FAIL chip variants leetcode-verdict-case-chip--pass/--fail apply theme-aware fills"
    - "Footer hierarchy stays primary-right / secondary-left via existing space-between flex layout — D-11"
    - "All 4 chevron-glyph RED tests in tests/main/languageChevronWidget.test.ts go GREEN"
    - "Zero hardcoded hex / rgb in any new/modified CSS rule (only the documented popover-shadow rgba exception at styles.css:957 is allowed and is unchanged) — D-06"
  artifacts:
    - path: "styles.css"
      provides: "Theme-aware CSS rules for new verdict-mode classes + neutral button polish + chevron pill refinements"
      contains: "leetcode-verdict-case-tab"
    - path: "src/main/languageChevronWidget.ts"
      provides: "Lucide chevron-down icon span + separate label span (replaces literal ▼ glyph)"
      contains: "setIcon"
  key_links:
    - from: "src/main/languageChevronWidget.ts"
      to: "obsidian.setIcon"
      via: "named import"
      pattern: "import .*setIcon.*from ['\"]obsidian['\"]"
    - from: "styles.css (.leetcode-verdict-case--fail .leetcode-verdict-output-value)"
      to: "DOM nodes emitted by renderRunResult (Plan 03)"
      via: "class selector"
      pattern: "leetcode-verdict-case--fail"
---

<objective>
Implement the visible polish layer of Phase 5.4 — port the canonical `.lc-fm__picker` neutral-button token set to all non-CTA buttons (D-09), unify RunModal and VerdictModal tab visuals (D-10), polish the inline Run/Submit buttons (D-12a), and swap the language chevron glyph from the literal Unicode `▼` to a Lucide `chevron-down` icon via `setIcon` (D-12b). Also adds the D-07 state-driven CSS rules that complete the renderer-level theming hooks Plan 03 emits.

Purpose: Match the user's "select-input pill" mental model for the chevron and unify the visual language of every neutral surface across the three Run/Submit/Verdict surfaces. This plan runs in parallel with Plan 03 (Wave 2) because it touches a disjoint file set (`styles.css` + `languageChevronWidget.ts` only — Plan 03 owns the renderer / VerdictModal / main.ts).

Output: Updated `styles.css` with new `.leetcode-verdict-case-*` rules + ported `.lc-fm__picker` rules to RunModal tabs and inline action row + D-07 class-gated value-color rules; updated `src/main/languageChevronWidget.ts` swapping the literal `▼` glyph for a `setIcon('chevron-down')` icon-span + separate label-span. The 4 RED chevron-glyph tests from Plan 01 Task 3 go GREEN.
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/moxu/projects/obsidian-leetcode/.planning/PROJECT.md
@/Users/moxu/projects/obsidian-leetcode/.planning/ROADMAP.md
@/Users/moxu/projects/obsidian-leetcode/.planning/STATE.md
@/Users/moxu/projects/obsidian-leetcode/.planning/phases/05.4-run-verdict-ux-button-polish/05.4-CONTEXT.md
@/Users/moxu/projects/obsidian-leetcode/.planning/phases/05.4-run-verdict-ux-button-polish/05.4-RESEARCH.md
@/Users/moxu/projects/obsidian-leetcode/.planning/phases/05.4-run-verdict-ux-button-polish/05.4-PATTERNS.md
@/Users/moxu/projects/obsidian-leetcode/.planning/phases/05.4-run-verdict-ux-button-polish/05.4-01-SUMMARY.md
@/Users/moxu/projects/obsidian-leetcode/CLAUDE.md

<interfaces>
<!-- Token sets and class names the executor must use verbatim. -->

Canonical .lc-fm__picker tokens (from styles.css:312-336 — verified verbatim, copy these verbatim):
```css
.lc-filter-modal .lc-fm__picker {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 6px;
  min-height: 30px;
  padding: 2px 8px;
  border: 1px solid var(--background-modifier-border);
  border-radius: 6px;
  background: var(--background-primary);
  cursor: pointer;
  font-size: 13px;
  transition: border-color 120ms ease;
}
.lc-filter-modal .lc-fm__picker:hover {
  border-color: var(--text-muted);
}
```

Existing port to chevron in Phase 5.3 (styles.css:931-946 — DO NOT regress):
```css
.leetcode-language-chevron {
  min-height: 30px;
  padding: 2px 8px;
  font-size: 13px;
  background: var(--background-primary);
  color: var(--text-normal);
  cursor: pointer;
  border: 1px solid var(--background-modifier-border);
  border-radius: 6px;
  transition: border-color 120ms ease;
}
.leetcode-language-chevron:hover { border-color: var(--text-muted); }
```

Existing chevron DOM (src/main/languageChevronWidget.ts lines 78-86 — current state to swap):
```typescript
const button = doc.createElement('button');
button.className = 'leetcode-language-chevron';
button.setAttribute('aria-haspopup', 'listbox');
button.setAttribute('aria-expanded', 'false');
const label = LC_LANG_DISPLAY_LABELS[currentSlug] ?? currentSlug;
button.textContent = `▼ ${label}`;  // ← swap this line
wrapper.appendChild(button);
```

setIcon precedent (src/browse/FilterModal.ts:179):
```typescript
import { setIcon } from 'obsidian';
setIcon(chev, 'chevron-down');
```

Class names emitted by Plan 03's renderRunResult (Plan 04 must style these — names are the contract):
- `.leetcode-verdict-case-tabs`
- `.leetcode-verdict-case-tab` (+ `.is-active` modifier)
- `.leetcode-verdict-case-chip` (+ `--pass` / `--fail` variants)
- `.leetcode-verdict-case-body` (+ `.leetcode-verdict-case--pass` / `--fail` modifier)
- `.leetcode-verdict-input-section`, `.leetcode-verdict-input-param`, `.leetcode-verdict-input-param-name`, `.leetcode-verdict-input-param-value`
- `.leetcode-verdict-output-value`, `.leetcode-verdict-expected-value`
- `.leetcode-verdict-runtime`
- `.leetcode-verdict-title--success`, `.leetcode-verdict-title--error`

CSS scoping rule (Pitfall 6): NEVER write `.leetcode-verdict button { ... }` — too broad; always scope to specific class names. Verified pattern at styles.css:898-913 (`.leetcode-code-actions .leetcode-code-action-run / -submit`).

D-06 / D-07 color palette (theme-aware vars only):
- `var(--text-error)` — failing output text + error title
- `var(--text-success)` — passing accent + expected text on fail
- `var(--text-muted)` — section labels + runtime line + chevron icon
- `var(--text-normal)` — passing value text + default body text
- `var(--text-on-accent)` — chip foreground
- `var(--background-primary)` — neutral button bg
- `var(--background-secondary)` — section surface
- `var(--background-modifier-border)` — border + active-tab fill
- `var(--background-modifier-hover)` — hover bg
- `var(--background-modifier-success)` — pass chip bg base (color-mix to deepen)
- `var(--background-modifier-error)` — fail chip bg base (color-mix to deepen)

Documented hex exception (DO NOT add new exceptions): popover-shadow `rgba(0, 0, 0, 0.15)` at styles.css:957 — already used by Phase 5.3 chevron dropdown; unchanged.

Existing test selectors LOCKED (DO NOT rename):
- `.leetcode-run-tab`, `.is-active`, `.leetcode-run-tab-add`, `.leetcode-run-tab-delete`, `.leetcode-run-reset`, `.leetcode-run-submit`, `.leetcode-run-textarea` (RunModal.test.ts)
- `.leetcode-language-chevron`, `.leetcode-language-chevron-item` (languageChevronWidget.test.ts)
- `.leetcode-code-action-run`, `.leetcode-code-action-submit` (codeBlockButtonRow tests)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap chevron glyph to Lucide chevron-down via setIcon (D-12b)</name>
  <files>src/main/languageChevronWidget.ts, tests/main/languageChevronWidget.test.ts</files>
  <read_first>
    - src/main/languageChevronWidget.ts (full file — confirm current `▼` glyph line, current imports, `doc` parameter pattern)
    - src/browse/FilterModal.ts lines 175-185 (setIcon('chevron-down') precedent — confirm import shape)
    - tests/main/languageChevronWidget.test.ts (full file — RED scaffolding from Plan 01 Task 3 at lines 76, 84, 92, 100; confirm assertion shape)
    - tests/helpers/obsidian-stub.ts (verify the obsidian-stub exports `setIcon` as a noop or supports import; if not, add a no-op `setIcon` export)
    - .planning/phases/05.4-run-verdict-ux-button-polish/05.4-CONTEXT.md (D-12b)
    - .planning/phases/05.4-run-verdict-ux-button-polish/05.4-PATTERNS.md (Pattern Assignments §languageChevronWidget.ts)
  </read_first>
  <behavior>
    - The chevron button DOM is `<button class="leetcode-language-chevron" aria-haspopup="listbox" aria-expanded="false"><span class="leetcode-language-chevron-icon"></span><span class="leetcode-language-chevron-label">{label}</span></button>` — the icon span is populated via `setIcon(iconSpan, 'chevron-down')`.
    - All other chevron behavior is unchanged: `aria-haspopup`, `aria-expanded`, dropdown open/close, outside-click + Esc + scroll/resize listeners, `doc` parameter pattern (popout-window safe), G-CLICK-THROUGH wrapper-level pointerdown stopPropagation, G-LABEL-LAG metadataCache→languageRefreshEffect dispatch.
    - The 4 RED chevron-glyph tests at tests/main/languageChevronWidget.test.ts (Plan 01 Task 3 updated lines 76/84/92/100) go GREEN: each test verifies `.leetcode-language-chevron-label` has the expected label text AND `.leetcode-language-chevron-icon` is non-null.
    - Other chevron behavior tests in the same file (~lines 105-570 covering aria-haspopup, dropdown, item click, Esc, outside-click, scroll/resize, current-slug-no-op) remain GREEN.
    - obsidian-stub.ts exports a no-op `setIcon` so happy-dom tests don't error on the import (Phase 5.3 verdictModalRenderer tests already use this pattern).
  </behavior>
  <action>Edit `src/main/languageChevronWidget.ts`. Add `setIcon` to the existing `from 'obsidian'` named-imports line (or add a new `import { setIcon } from 'obsidian';` if the file has no obsidian imports yet — verify by reading). Locate the chevron button construction (currently around lines 78-86 where `button.textContent = '▼ ${label}'`). Replace that single line with: (a) `const icon = doc.createElement('span'); icon.className = 'leetcode-language-chevron-icon'; setIcon(icon, 'chevron-down'); button.appendChild(icon);` then (b) `const labelSpan = doc.createElement('span'); labelSpan.className = 'leetcode-language-chevron-label'; labelSpan.textContent = LC_LANG_DISPLAY_LABELS[currentSlug] ?? currentSlug; button.appendChild(labelSpan);`. Do NOT use the global `document` — use the existing `doc` parameter. Do NOT add `innerHTML`. Do NOT change any other DOM construction or event handler in the file. If `tests/helpers/obsidian-stub.ts` does not yet export `setIcon`, add `export function setIcon(_el: HTMLElement, _name: string): void { /* noop in tests */ }` (mirror the noop pattern at tests/solve/verdictModalRenderer.test.ts:41 — confirm the existing pattern by reading). Run `npm test -- --run tests/main/languageChevronWidget.test.ts` and confirm all it-blocks pass.</action>
  <verify>
    <automated>npm test -- --run tests/main/languageChevronWidget.test.ts &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/languageChevronWidget.ts` imports `setIcon` from `'obsidian'` (verify: `grep -E "import .*\\bsetIcon\\b.*from ['\"]obsidian['\"]" src/main/languageChevronWidget.ts`)
    - The literal Unicode glyph `▼` no longer appears anywhere in `src/main/languageChevronWidget.ts` (verify: `! grep '▼' src/main/languageChevronWidget.ts`)
    - The chevron button DOM has both `.leetcode-language-chevron-icon` and `.leetcode-language-chevron-label` child spans (verify: `grep -c 'leetcode-language-chevron-icon\\|leetcode-language-chevron-label' src/main/languageChevronWidget.ts` ≥ 2)
    - `setIcon(...,  'chevron-down')` is called exactly once in the chevron build path (verify: `grep -c "setIcon.*chevron-down" src/main/languageChevronWidget.ts` === 1)
    - Verify command exits 0; the 4 chevron-glyph it-blocks at tests/main/languageChevronWidget.test.ts (lines 76, 84, 92, 100 in Plan 01 RED state) are GREEN; other chevron behavior tests remain green
    - No global `document` usage introduced (verify: `! grep -E '\\bdocument\\.createElement' src/main/languageChevronWidget.ts` — every createElement goes through `doc`)
  </acceptance_criteria>
  <done>Chevron renders Lucide chevron-down icon + label-span; D-12b RED tests GREEN; existing chevron interaction tests still GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CSS polish — port .lc-fm__picker tokens, add D-07 state-driven rules, polish inline Run/Submit + verdict layout</name>
  <files>styles.css</files>
  <read_first>
    - styles.css (full file or at least lines 300-340 for .lc-fm__picker; lines 540-700 for existing verdict + run-modal rules; lines 870-1000 for inline action row + chevron rules)
    - .planning/phases/05.4-run-verdict-ux-button-polish/05.4-CONTEXT.md (D-06, D-07, D-09, D-10, D-11, D-12a, D-12b)
    - .planning/phases/05.4-run-verdict-ux-button-polish/05.4-RESEARCH.md (§Code Examples "Theme-aware CSS rules" — copy structure of the proposed CSS additions; §Pitfall 6 — scoping rule)
    - .planning/phases/05.4-run-verdict-ux-button-polish/05.4-PATTERNS.md (§styles.css — full canonical token set + scoping rules)
    - .planning/phases/05.4-run-verdict-ux-button-polish/05.4-03-SUMMARY.md if available — confirm the exact class-name list emitted by Plan 03's renderer (the renderer is the source of truth; styles must match those names)
  </read_first>
  <behavior>
    - styles.css gains the following NEW rules (group them in a clearly commented "/* Phase 5.4 — Run-mode redesign + neutral button polish */" block, anchored after the existing `.leetcode-language-chevron` rules around line ~990 OR positioned in the appropriate section per existing file layout):
      Group 1 — Neutral button polish (D-09 + D-10), scoped:
        Selectors: `.leetcode-run .leetcode-run-tab`, `.leetcode-run .leetcode-run-tab-add`, `.leetcode-run .leetcode-run-reset`, `.leetcode-verdict .leetcode-verdict-case-tab`. Hover variant + `.is-active` variant. Token values: `min-height: 30px`, `padding: 2px 8px`, `border: 1px solid var(--background-modifier-border)`, `border-radius: 6px`, `background: var(--background-primary)`, `color: var(--text-normal)`, `cursor: pointer`, `font-size: 13px`, `transition: border-color 120ms ease`, `display: inline-flex`, `align-items: center`, `gap: 6px`. Hover: `border-color: var(--text-muted)`. `.is-active`: `background: var(--background-modifier-border)`, `font-weight: 600`. (Existing `.leetcode-run .leetcode-run-tab` rules at styles.css:634+ may overlap — REPLACE the conflicting properties; preserve `.leetcode-run-tab-delete` rules at lines 652-661.)
      Group 2 — Tab strip wrapper:
        `.leetcode-verdict .leetcode-verdict-case-tabs`: `display: flex`, `flex-wrap: wrap`, `gap: 6px`, `margin-bottom: 12px`.
      Group 3 — D-04 PASS/FAIL chip (theme-aware via color-mix):
        `.leetcode-verdict .leetcode-verdict-case-chip` base: `font-size: 11px`, `padding: 0 6px`, `border-radius: 8px`, `min-width: 32px`, `text-align: center`. `--pass` variant: `background: color-mix(in srgb, var(--background-modifier-success) 80%, transparent)`, `color: var(--text-on-accent)`. `--fail` variant: `background: color-mix(in srgb, var(--background-modifier-error) 80%, transparent)`, `color: var(--text-on-accent)`. (color-mix precedent: Phase 5.2 D-09 chip-contrast fix already uses this exact pattern — read styles.css for confirmation.)
      Group 4 — D-07 state-driven title coloring:
        `.leetcode-verdict-title--success { color: var(--text-success); font-weight: 600; }`. `.leetcode-verdict-title--error { color: var(--text-error); font-weight: 600; }`.
      Group 5 — D-13 runtime line:
        `.leetcode-verdict .leetcode-verdict-runtime { color: var(--text-muted); font-size: 13px; margin-bottom: 16px; }`.
      Group 6 — D-07 value-text coloring (class-gated; refines/replaces the existing always-on rules at styles.css:548-580 which were unconditional):
        `.leetcode-verdict .leetcode-verdict-case--fail .leetcode-verdict-output-value { color: var(--text-error); }`. `.leetcode-verdict .leetcode-verdict-case--fail .leetcode-verdict-expected-value { color: var(--text-success); }`. `.leetcode-verdict .leetcode-verdict-case--pass .leetcode-verdict-output-value`, `.leetcode-verdict .leetcode-verdict-case--pass .leetcode-verdict-expected-value { color: var(--text-normal); }`. The pre-existing unconditional rules at styles.css:548-580 (`.leetcode-verdict-section-label--output { color: var(--text-error); }` and the always-on `.leetcode-verdict-diff-actual` border-left) remain unchanged because they target the older Submit-mode WA body — D-14 protects those rules.
      Group 7 — D-08 Input section:
        `.leetcode-verdict .leetcode-verdict-input-section { background: var(--background-secondary); padding: 8px 12px; border-radius: 6px; margin-bottom: 12px; }`. `.leetcode-verdict .leetcode-verdict-input-param { display: flex; gap: 8px; align-items: baseline; }`. `.leetcode-verdict .leetcode-verdict-input-param-name { color: var(--text-muted); font-size: 12px; min-width: 60px; }`. `.leetcode-verdict .leetcode-verdict-input-param-value { font-family: var(--font-monospace); white-space: pre-wrap; margin: 0; }`.
      Group 8 — D-12a Inline Run/Submit polish (refines the existing rules at styles.css:898-914 — KEEP existing class names, REPLACE conflicting property values):
        `.leetcode-code-actions .leetcode-code-action-run`, `.leetcode-code-actions .leetcode-code-action-submit`: `min-height: 30px`, `padding: 2px 12px`, `border: 1px solid var(--background-modifier-border)`, `border-radius: 6px`, `background: var(--background-primary)`, `color: var(--text-normal)`, `cursor: pointer`, `font-size: 13px`, `transition: border-color 120ms ease`. Hover: `border-color: var(--text-muted)`, preserve `background: var(--background-primary)` (override any pre-existing hover bg-change).
      Group 9 — D-12b chevron icon span:
        `.leetcode-language-chevron-icon`: `display: inline-flex`, `align-items: center`, `width: 14px`, `height: 14px`, `color: var(--text-muted)`. `.leetcode-language-chevron-label`: `display: inline-flex`, `align-items: center` (no other overrides — inherits chevron-button typography).
    - All new rules use Obsidian semantic CSS vars only. The single documented hex exception (rgba(0,0,0,0.15) popover shadow at styles.css:957) is unchanged. No new hex/rgb literal added.
    - All new rules are scoped — never `.leetcode-verdict button { ... }` (Pitfall 6).
    - Existing rules at styles.css:548-619 (`.leetcode-verdict-section-label--output`, `.leetcode-verdict-section-label--expected`, `.leetcode-verdict-error-pre`, `.leetcode-verdict-action-row`) are PRESERVED unless their property values conflict with Phase 5.4 rules — in which case the newer scoped class-gated rules take precedence via specificity.
    - D-11 footer hierarchy: existing `.leetcode-verdict-action-row` (justify-content: flex-end at styles.css:597-603) is correct for the Run-side single-Close-button footer. No change required for D-11 unless Plan 03's footer DOM uses a different class — re-confirm via the Plan 03 SUMMARY.
  </behavior>
  <action>Edit `styles.css`. Add a single clearly-commented section (e.g., `/* ============================== Phase 5.4 — Run-mode verdict redesign + neutral button polish ==============================*/`) at an appropriate file location (recommended: after the existing `.leetcode-language-chevron-*` rules around line ~995, OR group with the existing `.leetcode-verdict-*` rules near line ~610 — pick one location for cohesion). Add the 9 rule groups described in `<behavior>` verbatim. For Group 1 + Group 8 (overlap with existing rules at styles.css:634-661 and 898-914): READ those existing rules first, then EDIT the existing rules in place to match the new token values where conflicts exist (do NOT delete and re-add — preserve adjacent sub-rules like `.leetcode-run-tab-delete`); only ADD new selectors that don't already exist. Run the D-06 grep gate locally: `grep -vE '^\\s*(/\\*|\\*|//)' styles.css | grep -E '#[0-9a-fA-F]{3,6}\\b|rgb\\(' | grep -v "rgba(0, 0, 0, 0.15)"` should return zero matches (only the documented popover-shadow exception is allowed). Run `npm run build` to confirm the bundle still builds (CSS is bundled as a static asset; just verify no parse error). Manually verify that no existing CSS rule is removed except where its property values were superseded by a new scoped class-gated rule. NO test command will catch CSS regressions — visual gating happens in Plan 05.</action>
  <verify>
    <automated>! grep -vE '^\s*(/\*|\*|//)' styles.css | grep -E '#[0-9a-fA-F]{3,6}\b|rgb\(' | grep -vF 'rgba(0, 0, 0, 0.15)'</automated>
  </verify>
  <acceptance_criteria>
    - styles.css contains scoped selectors for `.leetcode-verdict-case-tab`, `.leetcode-verdict-case-tabs`, `.leetcode-verdict-case-chip`, `.leetcode-verdict-case-chip--pass`, `.leetcode-verdict-case-chip--fail`, `.leetcode-verdict-case--fail`, `.leetcode-verdict-case--pass`, `.leetcode-verdict-output-value`, `.leetcode-verdict-expected-value`, `.leetcode-verdict-input-section`, `.leetcode-verdict-input-param`, `.leetcode-verdict-input-param-name`, `.leetcode-verdict-input-param-value`, `.leetcode-verdict-runtime`, `.leetcode-verdict-title--success`, `.leetcode-verdict-title--error`, `.leetcode-language-chevron-icon`, `.leetcode-language-chevron-label` (verify each via `grep -F '<class-name>' styles.css` returning ≥ 1 match)
    - The verify command exits 0 (D-06 grep gate: no new hardcoded hex/rgb in CSS body, the documented rgba(0,0,0,0.15) popover shadow being the only exception)
    - Verify no `.leetcode-verdict button { ... }` rule was added (verify: `! grep -E '\\.leetcode-verdict\\s+button\\s*\\{' styles.css` — Pitfall 6 scoping rule)
    - `npm run build` exits 0 (no bundle parse error)
    - `npm test -- --run` exits 0 (CSS doesn't run in tests, but this confirms no regressions in JS tests caused by accidental file edits)
    - styles.css line count increased by approximately 60-100 lines (verify: `wc -l styles.css` before vs. after; this is a sanity bound only)
  </acceptance_criteria>
  <done>All Phase 5.4 CSS classes are defined with theme-aware semantic vars; D-06 grep gate clean; bundle builds; full test suite still GREEN.</done>
</task>

</tasks>

<verification>
- `npm test -- --run tests/main/languageChevronWidget.test.ts` exits 0 (4 chevron-glyph RED tests now GREEN)
- `npm test -- --run` exits 0 (full suite green)
- `npm run build` exits 0 (bundle clean)
- D-06 grep gate: `grep -vE '^\s*(/\*|\*|//)' styles.css | grep -E '#[0-9a-fA-F]{3,6}\b|rgb\(' | grep -vF 'rgba(0, 0, 0, 0.15)'` returns zero matches
- D-14 guardrail: `git diff src/solve/verdictModalRenderer.ts` is empty (Plan 04 does not touch this file)
- Selector inventory: every renderer-emitted class name (from Plan 03 SUMMARY's class list) has at least one matching CSS selector
</verification>

<success_criteria>
- D-09 / D-10: RunModal tabs and VerdictModal case tabs share token values (.lc-fm__picker port verified)
- D-12a: Inline Run/Submit have neutral-button polish
- D-12b: Chevron renders Lucide chevron-down via setIcon; 4 RED chevron tests GREEN
- D-07 CSS half: state-driven title colors and class-gated value-text colors are defined
- D-06: zero new hardcoded colors; only Obsidian semantic vars (single documented popover-shadow exception preserved)
- D-11: footer hierarchy preserved (no structural change required given Plan 03's single-Close-button Run-path footer)
- D-14: no edits to renderSubmitVerdict / VerdictModal / main.ts (these belong to Plan 03)
- Pitfall 6 scoping rule held: no `.leetcode-verdict button { ... }` rule
</success_criteria>

<output>
After completion, create `.planning/phases/05.4-run-verdict-ux-button-polish/05.4-04-SUMMARY.md` documenting:
- Lines added / modified / removed in styles.css (approximate line counts per group)
- Where the new section was placed (line range)
- Confirmation that `.leetcode-language-chevron` Phase 5.3 rules are preserved
- Confirmation that the D-06 grep gate passes
- Confirmation that the 4 chevron-glyph RED tests are GREEN
- Open visual-only items deferred to Plan 05 live-smoke (RunModal tabs ↔ VerdictModal tabs visual match; chevron pill "feels like a textbox"; light/dark theme parity)
</output>
