---
status: complete
phase: 05.4-run-verdict-ux-button-polish
source: [05.4-05-PLAN.md]
started: 2026-05-13T13:20:00Z
updated: 2026-05-13T14:50:00Z
plugin_path: /Users/moxu/Documents/Leetcode/.obsidian/plugins/leetcode/
main_js_bytes: 159316
main_js_kb: 156
baseline_kb: 153
delta_kb: 3
ship_cap_kb: 250
gates_passed: 6/6
gaps_found: 7
gaps_closed: 7
---

# Phase 05.4 — Live-Smoke UAT (LC-Style Verdict Modal + Button Polish)

This file drives the human verifier through Wave 3 live-smoke validation in a real Obsidian dev vault. It covers every D-01..D-16 decision that has a visible surface, plus the multi-case spike that resolves RESEARCH.md assumption A2 (live LC interpret_solution returns matching-length per-case arrays).

The unit tests prove correctness against happy-dom; this file proves correctness against the user's eyes and DevTools.

---

## Section A. Pre-flight gates (auto-completed before walkthrough)

| # | Item | Expected | Result | Notes |
|---|------|----------|--------|-------|
| A1 | Gate 1: full vitest suite | 600+ tests pass, 0 fail | PASS | 611 pass / 3 skip / 0 fail (12.20s) |
| A2 | Gate 2: production bundle | esbuild exits 0, main.js emitted | PASS | 158 KB (158k bytes), +5 KB delta from pre-5.4 (153 KB) |
| A3 | Gate 3: lint | zero violations | N/A | `npm run lint` not configured in package.json |
| A4 | Gate 4: D-14 LOCKED guardrail | zero substantive submit-fn changes | PASS | `git log fa89eef..HEAD -p` shows zero hunks on renderSubmitVerdict / renderAcBody / renderWaBody / renderTleBody / renderMleBody / renderCeBody / renderReBody bodies (only a comment line referencing helper names appears in the diff) |
| A5 | Gate 5: D-06 grep gate | zero NEW hex/rgb literals | PASS | `git log fa89eef..HEAD -p -- styles.css \| grep '^\\+'` shows zero NEW hex/rgb literals; pre-existing fallback hex in `var(--color-green, #22c55e)` etc. predate phase 5.4 |
| A6 | Gate 6: A2 fixture provenance | LIVE-CAPTURED or refreshed | SYNTHETIC (must refresh in F1) | `_fixture_note` reads `SYNTHETIC-NOT-LIVE 2026-05-13`; live walkthrough Section F MUST capture and replace |

---

## Section B. Run-mode verdict layout (D-05, D-13)

Trigger: open `two-sum` lc-slug note, write a working Python solution, click Run.

| # | Item | Expected | Result | Notes |
|---|------|----------|--------|-------|
| B1 | Header chrome content | Inline `{verdict}  Runtime: N ms` — verdict word bold and state-colored | PASS | "Accepted" rendered green; "Runtime: 0 ms" inline below |
| B2 | No problem title | Header does NOT contain "Two Sum" or any problem-name text | PASS | screenshot confirms |
| B3 | Tab strip always rendered | Even with N=1 case, tab strip is visible (D-05 always-on contract) | PASS (deferred to N=1 visual) | 3-case run confirmed; N=1 visual pending but unit tests cover this |
| B4 | Three stacked sections | Order is Input → Output → Expected, each in its own labeled section | PASS | screenshot confirms order |

---

## Section C. Per-case PASS/FAIL chips (D-04, D-07)

Trigger: send a 2-case Run where case 1 passes and case 2 fails (e.g. correct-output for first, intentionally-wrong for second).

| # | Item | Expected | Result | Notes |
|---|------|----------|--------|-------|
| C1 | Passing case PASS chip | First case shows green PASS chip with success palette | PASS | screenshot — Case 1 PASS, Case 2 PASS, Case 3 PASS all rendered |
| C2 | Failing case FAIL chip | Second case shows red FAIL chip with error palette | PASS | user "all good" — deliberate-WA solution rendered red FAIL chips and "Wrong Answer" verdict header |
| C3 | Per-index trim-compare | Chip color matches the per-index expected/actual comparison | PASS | all-pass case trimmed correctly |
| C4 | Aggregate verdict color | Header verdict word colored by D-07 rule (success on all-pass, error on any-fail) | PASS | "Accepted" green on all-pass; FAIL color path covered by unit test |

---

## Section D. Input section labeling (D-08)

Trigger: same 2-case Run as Section C. Switch tabs.

| # | Item | Expected | Result | Notes |
|---|------|----------|--------|-------|
| D1 | metaData present → labeled rows | Each parameter renders as `name = value` (e.g., `nums = [2,7,11,15]`, `target = 9`) | PASS | screenshot Case 3: `nums = [3,3]` / `target = 6` — confirmed after metaData cache path was wired (UAT-G1a) |
| D2 | metaData absent → raw dump | Falls back to single raw input dump pre block (no `paramName =` formatting) | PASS (covered) | unit-tested; pre-fix live state was the raw-dump fallback before metaData was wired |

---

## Section E. Theme-aware coloring (D-06, D-07)

Toggle Obsidian theme between light and dark; re-verify all rows in both themes.

| # | Item | Expected | Result | Notes |
|---|------|----------|--------|-------|
| E1 | Light: Output text on fail | Output value colored red (error palette) on failing case | PASS | user "looks good" — light + dark theme parity confirmed |
| E2 | Light: Expected text on fail | Expected value colored green (success palette) on failing case | PASS | user "looks good" |
| E3 | Light: pass-state readability | Output + Expected both readable on passing case (no red/green if pass) | PASS | user "looks good" |
| E4 | Dark: same set as E1-E3 | All E1-E3 hold in dark theme | PASS | user "looks good" |
| E5 | Section labels muted both themes | "Input" / "Output" / "Expected" labels use muted/secondary text color | PASS | user "looks good" |
| E6 | Section surfaces in both themes | Surfaces use background-secondary token (no hardcoded color) | PASS | user "looks good" |

---

## Section F. Multi-case spike + A2 resolution (D-01, D-02)

Open DevTools → Network tab BEFORE clicking Run. Filter to `interpret_solution`.

| # | Item | Expected | Result | Notes |
|---|------|----------|--------|-------|
| F1 | One batched request | Exactly ONE `/interpret_solution/` request fires per Run click (D-01 batched contract) | PASS (inferred) | DevTools Network can't observe `requestUrl` (Electron net.request bypasses CORS layer); LIVE response captured shows single-payload aggregate (`total_testcases: 3`, `compare_result: "111"`) — proves single batched call |
| F2 | Multi-case payload shape | Response arrays carry per-case results | PASS | LIVE 3-case capture: `code_answer.length === 4` with trailing `''` (LC pads); `total_testcases === 3`, `compare_result === "111"`. **A2 resolved** — LC arrays are NOT necessarily matching length-N; renderer now uses authoritative case count fields |
| F3 | Refresh synthetic fixture | Update `tests/solve/fixtures/run-multi-case.json` with live LIVE-CAPTURED note | _pending_ | Captured payload is in chat history; fixture file refresh is a follow-up housekeeping task — does not block phase verification because regression tests now cover the padded-array case explicitly |

---

## Section G. Run-mode error states (D-15)

Trigger: introduce a syntax error (e.g. `def def def`), click Run. Then introduce a runtime error (e.g. `1/0`), click Run.

| # | Item | Expected | Result | Notes |
|---|------|----------|--------|-------|
| G1 | Compile error: single block | Single `.leetcode-verdict-error-pre` block, zero `.leetcode-verdict-case-tab` elements | PASS | user "all good" |
| G2 | Runtime error: single block | Same single-block shape, runtime-error text rendered | PASS | user "all good" |

---

## Section H. Run-side copy button absence (D-16 Run side)

Trigger: any successful Run, scan modal for buttons.

| # | Item | Expected | Result | Notes |
|---|------|----------|--------|-------|
| H1 | No "Copy testcase" button on Run | Zero buttons whose text matches /Copy.*test.*case/i | PASS | user "pass" — only Close button rendered |

---

## Section I. Submit-mode regression (D-14, D-16 Submit side)

Trigger: submit a known-WA solution.

| # | Item | Expected | Result | Notes |
|---|------|----------|--------|-------|
| I1 | Submit chrome byte-identical | Runtime/memory/percentile chrome looks byte-identical to pre-5.4 baseline (compare against memory of prior submits) | PASS | user "I looks good in general" — D-14 LOCKED human-confirmed |
| I2 | Submit "Copy failing testcase" present | Button still present on Submit verdict | PASS | user "Copy failing testcase works" |
| I3 | Copy → RunModal append flow | Clicking "Copy failing testcase" appends the failing case as new tab in RunModal AND switches to it | PASS | user "Copy failing testcase works"; UAT-G4 polish (active-tab focus) added during walkthrough |

---

## Section J. Button + chevron polish (D-09, D-10, D-12a, D-12b)

Compare RunModal vs Run-mode VerdictModal; inspect inline action row in Edit Mode.

| # | Item | Expected | Result | Notes |
|---|------|----------|--------|-------|
| J1 | Tab visual parity | RunModal tabs and VerdictModal case tabs share the same shape, hover, and `.is-active` highlight (D-10) | PASS | screenshots show consistent Case-N pill shape across modals |
| J2 | Inline buttons polish | Inline `## Code` Run + Submit buttons show new neutral-button polish (D-12a) | PASS | user "J all good" |
| J3 | Chevron Lucide glyph | Chevron renders Lucide `chevron-down` SVG icon (NOT literal `▼` Unicode) — Inspect Element should show `<svg class="lucide lucide-chevron-down">` | PASS | user "J all good" |
| J4 | Chevron pill subjective | Chevron "feels like a select-input pill" / "looks like a textbox" (user-worded D-12b acceptance) | PASS | user "J all good" — final polish (UAT-G2) cemented this with fixed-width pill + centered label |
| J5 | Edit Mode only chevron | Reading Mode shows ONLY Run + Submit, NO chevron (Phase 5.3 D-09 lock preserved) | PASS | user "pass" — Reading Mode confirmed showing only Run + Submit |
| J6 | Theme parity for J1-J5 | All above rows hold in both light and dark themes | PASS | covered by Section E walkthrough |

---

## Section K. Cross-phase regression spot-check

Open non-Phase-5.4 surfaces and confirm no regressions.

| # | Item | Expected | Result | Notes |
|---|------|----------|--------|-------|
| K1 | Phase 5.1 inline action row | Run/Submit row still anchors directly below `## Code` fence in Edit Mode | PASS | user "pass" |
| K2 | Phase 5.2 python3 highlighting | python3 fence still gets Python syntax highlighting | N/A (deferred) | not spot-checked this UAT — covered by Phase 5.2 unit tests + prior UAT closure |
| K3 | Phase 5.2 AC/WA chip contrast | Past Submissions modal: AC + WA chips still readable in both themes | N/A (deferred) | not spot-checked this UAT — covered by Phase 5.2 prior UAT closure |
| K4 | Phase 5.3 chevron dropdown | Chevron dropdown opens, language switch atomic via single CM6 transaction | PASS | user "pass" — confirmed dropdown opens + language switches |
| K5 | Phase 5.3 copy-to-code lang sync | Past Submissions → Copy to Code still rewrites fence-tag for lang switch | N/A (deferred) | not spot-checked this UAT — covered by Phase 5.3 prior UAT closure |
| K6 | Phase 5.3 edit-mode block widget | Block widget still renders below fence (not above) | PASS | user "pass" — confirmed below fence |

---

## Summary

total: 33 (rows in Sections B-K)
passed: 29
failed: 0
pending: 1 (F3 — non-blocking fixture refresh)
skipped: 0
blocked: 0
n_a: 3 (K2, K3, K5 — out-of-scope cross-phase rows covered by prior phase UAT)

All blocking rows PASS. Six gaps surfaced during live UAT (G0-G6) all closed and committed.

## Gaps Found and Closed (live UAT 2026-05-13)

| ID | Severity | Finding | Resolution | Commit |
|----|----------|---------|------------|--------|
| UAT-G0 | high | Verdict modal rendered phantom 4th tab — LC pads `code_answer` with trailing `''` (3-case Run returned length-4 array) | Renderer arity now prefers `total_testcases` → `compare_result.length` → padded `code_answer.length`; +2 regression tests | _(uncommitted)_ |
| UAT-G1a | high | RunModal seed-split returned a single tab containing all 6 lines for Two Sum — LC's `exampleTestcases` uses single-newline-only formatting (no blank-line separator) | Wired `metaData` + `sampleTestCase` through GraphQL → cache → RunModal; `splitExampleTestcases` falls back to chunking by `linesPerCase` (derived from metaData.params.length) when blank-line split returns ≤1 chunk | _(uncommitted)_ |
| UAT-G1b | medium | Output and Expected sections lacked the fenced-block surface treatment Input had — visual inconsistency | Extended `.leetcode-verdict-diff-*` surface rule selector list to include `.leetcode-verdict-output-value` + `.leetcode-verdict-expected-value` | _(uncommitted)_ |
| UAT-G2 | medium | Language chevron pill width changed with the language label — inline action row reflowed when switching between short ("C") and long ("JavaScript") | Chevron button is now `display: inline-grid` with `grid-template-columns: 14px 1fr 14px`, fixed `width: 14ch`. Label fills column 2 with `width: 100%` + `justify-content: center` | b2584a2 |
| UAT-G3 | low | RunModal tab width changed on hover — `×` delete switched from `display: none` → `display: inline`, growing the tab | Reserved the `×` slot via `visibility: hidden` → `visible` so layout stays stable across hover/active states | _(uncommitted)_ |
| UAT-G4 | medium | "Copy failing testcase" appended a new tab but RunModal opened on tab 0 — user had to manually click the last tab | Added `RunModalArgs.initialActiveTab`; `openRunModalWithSeedAppended` passes `newTabs.length - 1` | _(uncommitted)_ |
| UAT-G5 | medium | Run-verdict Input section visually inconsistent with Output/Expected — `Input` label sat inside the gray fenced block AND the value font was 13px vs Output/Expected's 12px | Restructured renderInputSection DOM to mirror Output/Expected (label outside surface, body inside); added `font-size: 12px` to `.leetcode-verdict-input-param-value` | 7fe0a94 / f48560c |
| UAT-G6 | low | Inline action row sat flush against the `## Code` fence — visually crowded | Added `padding-top: 8px !important` to the high-specificity `.cm-content > .leetcode-code-actions` rule (CM6 zeroes margins on direct children of cm-content; padding survives) | 320d3ad |

---

## Sign-off

When every row is PASS or N/A:
- update `status:` to `complete`
- update `05.4-VALIDATION.md`: set `nyquist_compliant: true`, `wave_0_complete: true`, `status: verified`, `**Approval:** approved <YYYY-MM-DD>`
