---
phase: 05.4
slug: run-verdict-ux-button-polish
status: verified
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-13
verified: 2026-05-13
---

# Phase 05.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 4.1.x |
| **Config file** | vitest.config.ts |
| **Quick run command** | `npm test -- --run tests/solve/verdictModalRenderer.test.ts tests/solve/RunModal.test.ts tests/main/languageChevronWidget.test.ts` |
| **Full suite command** | `npm test -- --run` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run quick run command (focused tests for renderer + RunModal + chevron widget)
- **After every plan wave:** Run full suite command
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

> Filled per-plan during PLAN.md generation. Each task receives a row when its plan is written.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD     | TBD  | TBD  | TBD         | —          | N/A             | unit      | `npm test`        | TBD         | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/solve/fixtures/run-multi-case.json` — captured live 2-case `interpret_solution` response (resolves A2 wire-shape MEDIUM-confidence assumption)
- [ ] `src/solve/runArity.ts` (or equivalent helper module) — `linesPerCase(sampleTestCase)` + `splitDataInput(joined, arity)` + `splitResponseArray(arr, n)` pure functions
- [ ] `tests/solve/runArity.test.ts` — RED unit tests for arity computation, request join, response split (covers single-line, multi-line, 2D-array cases)
- [ ] `tests/solve/RunModal.test.ts` — multi-tab join assertion stub (RED)
- [ ] `tests/solve/verdictModalRenderer.test.ts` — RED `it`-blocks for new Run-mode layout (per-case tabs, D-07 colors, D-08 input parse, D-13 header, D-15 error state, D-16 no copy on Run)
- [ ] `tests/main/languageChevronWidget.test.ts` — update existing 4 lines (76, 84, 92, 100) that assert the `▼` glyph; new assertions match `chevron-down` icon presence (per D-12b)

*Existing vitest 4.1 + happy-dom infra is sufficient — no framework install needed.*

---

## Manual-Only Verifications

> Visual / live-smoke gates for surfaces unit tests cannot prove. Wave N (final) checkpoint.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Run-mode VerdictModal looks like LC.com screenshots (header, tab strip, Input/Output/Expected sections, theme-aware colors) | D-05, D-07, D-13 | Visual fidelity — pixel-level matching against user-provided screenshots; CSS var resolution depends on live theme | Open dev vault, run a 2-case Two-Sum solution, confirm AC + WA paths render per screenshot in both light + dark theme |
| Inline `## Code` action row chevron reads as a select-input pill (not a button) | D-12a, D-12b | Subjective — affordance is "feels like a textbox" per user wording | Open `lc-slug` note in Edit Mode + Reading Mode; chevron must look like the problem-browser's filter dropdown (`.lc-fm__picker`); `chevron-down` glyph renders correctly |
| RunModal tabs and VerdictModal tabs feel like the same surface | D-10 | Cross-modal visual consistency check | Open Run modal, then trigger Run; confirm tab strips share shape, active highlight, +/× pill style |
| Submit verdict modal is bytes-identical to pre-5.4 (D-14 scope guardrail) | D-14 | Regression safety; CSS+renderer changes must not bleed into Submit path | Run a Submit, confirm `renderSubmitVerdict` chrome (runtime/memory/percentile row) is unchanged from Phase 5 baseline |
| Live multi-case `interpret_solution` returns matching-length arrays for `code_answer` + `expected_code_answer` (A2 spike) | D-01, D-02, D-04 | LC server-side behavior; cannot mock | Wave 0 spike — capture 2-case response into `tests/solve/fixtures/run-multi-case.json`; assert `arr.length === expected.length === arity` |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags (`npm test -- --run` everywhere)
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-13

**Approval notes:** Wave 3 live-smoke surfaced 7 gaps (UAT-G0..G6) covering verdict-modal arity, RunModal seed-split, Output/Expected surface treatment, chevron pill centering, RunModal tab hover, Copy-failing-testcase active tab, Input section DOM shape, font-size unification, and inline-action-row gap. All 7 closed during the same UAT session (commits 4fad9d3 / b2584a2 / 7fe0a94 / f48560c / 320d3ad). Final test suite: 615 pass / 3 skip / 0 fail. Bundle: 156 KB (+3 KB delta). 29/33 UAT rows PASS, 1 deferred (F3 — non-blocking fixture refresh), 3 N/A (K2/K3/K5 — covered by prior phase UAT closures).
