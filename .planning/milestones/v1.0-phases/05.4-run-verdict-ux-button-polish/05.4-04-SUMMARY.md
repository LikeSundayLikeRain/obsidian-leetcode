---
phase: 05.4
plan: 04
subsystem: solve
tags: [wave-2, css-polish, neutral-button, chevron-icon, theme-vars, d-09, d-10, d-12a, d-12b, d-07-css]
requires:
  - .planning/phases/05.4-run-verdict-ux-button-polish/05.4-CONTEXT.md
  - .planning/phases/05.4-run-verdict-ux-button-polish/05.4-01-SUMMARY.md
provides:
  - "Lucide chevron-down icon span + label span DOM in src/main/languageChevronWidget.ts"
  - "9 new CSS rule groups in styles.css covering the Run-mode verdict redesign"
  - "Ported .lc-fm__picker neutral-button tokens to RunModal tabs + inline Run/Submit"
  - "D-07 state-driven title + value-text class-gated coloring"
affects:
  - "Plan 03's Run-mode renderer (verdictModalRenderer rewrite) — CSS rules above match the renderer's emitted class names"
  - "Plan 05 live-smoke checkpoint — visual gating of the new tab + button shape, light/dark theme parity"
tech_stack:
  added: []
  patterns:
    - "setIcon('chevron-down') for Lucide glyphs (FilterModal precedent)"
    - "color-mix theme-aware chip backgrounds (Phase 5.2 D-09 precedent)"
    - "Class-gated value-text coloring via .leetcode-verdict-case--{pass,fail} parent (raises specificity over unconditional Phase 3 rules)"
key_files:
  created:
    - ".planning/phases/05.4-run-verdict-ux-button-polish/05.4-04-SUMMARY.md"
  modified:
    - "src/main/languageChevronWidget.ts"
    - "tests/helpers/obsidian-stub.ts"
    - "styles.css"
decisions:
  - "Plan 04 ships the DOM swap (Task 1) AND the matching CSS rules (Task 2 Group 9 — `.leetcode-language-chevron-icon` + `-label` selectors) in one plan; the chevron now reads as a select-input pill via setIcon + theme-aware muted icon color."
  - "Pre-existing fallback hex literals at styles.css lines 727-741 (Phase 5.2/5 chip rules using `var(--name, #fallback)` pattern) are out of scope per the scope boundary rule — they predate Plan 04 and were not added/modified by it. The plan's D-06 grep gate is naive about CSS-var fallback syntax (a documented Obsidian pattern); strict reading of the acceptance criterion (\"zero hardcoded hex/rgb in any NEW/MODIFIED rule\") still passes — Plan 04 added zero hex/rgb."
  - "Pre-existing tsc errors (35 lines) in unrelated test files (codeActionsPostProcessor.test.ts, SubmissionPickerModal.test.ts, python3Highlighter.test.ts, SessionExpiredNotice.test.ts, codeActionsPostProcessor.ts) predate this plan (verified by `git stash` round-trip — same 35 errors before AND after Plan 04). Out of scope per scope boundary rule."
metrics:
  duration_minutes: 5
  completed_date: 2026-05-13
  tasks_completed: 2
  files_modified: 3
  files_created: 1 # this SUMMARY.md
  tests_added: 0 # CSS doesn't run in tests; chevron tests already authored RED in Plan 01 and now go GREEN
  red_tests_resolved: 4 # 4 D-12b chevron-glyph it-blocks (lines 73, 86, 97, 108) RED → GREEN
---

# Phase 5.4 Plan 04: CSS Polish + Chevron Icon Swap Summary

Wave 2 (parallel with Plan 03) lands the visible-polish layer: ports the canonical `.lc-fm__picker` neutral-button token set to all non-CTA buttons (RunModal tabs + add-pill + Reset, inline Run/Submit, VerdictModal case tabs); swaps the language chevron's literal `▼` glyph for a Lucide `chevron-down` icon mounted via `setIcon` so the chevron reads as a select-input pill; adds 9 CSS rule groups covering Plan 03's Run-mode renderer DOM contract (per-case tabs, PASS/FAIL chips, state-driven title coloring, class-gated value-text colors, input section). All D-12b RED chevron-glyph tests from Plan 01 Task 3 now go GREEN.

## Outcomes

### Task 1 — Chevron icon swap (D-12b) — commit `2f0b938`

- **`src/main/languageChevronWidget.ts`**: imported `setIcon` from `'obsidian'`. Replaced the line `button.textContent = '▼ ${label}'` with two child spans:
  - `<span class="leetcode-language-chevron-icon">` populated by `setIcon(iconSpan, 'chevron-down')` — Obsidian's sanctioned API for Lucide glyphs.
  - `<span class="leetcode-language-chevron-label">` carrying the `LC_LANG_DISPLAY_LABELS[currentSlug] ?? currentSlug` text via `textContent`.
- **`tests/helpers/obsidian-stub.ts`**: added a no-op `setIcon` export so `vi.mock('obsidian', () => obsidian-stub)` pass-through resolves the new import in happy-dom (mirrors the per-test factory pattern at `tests/solve/verdictModalRenderer.test.ts:47`).
- **All 25 chevron behavior it-blocks pass** — including the 4 D-12b RED chevron-glyph tests (lines 73, 86, 97, 108) which now go GREEN. Existing dropdown / aria / portal / Esc / scroll-resize / outside-click / G-CLICK-THROUGH coverage continues to pass.
- Acceptance gates verified: `setIcon` import present; literal `▼` glyph absent (0 matches); both `-icon` and `-label` child-span selectors emitted (2 unique class names); `setIcon('chevron-down')` called exactly once in the chevron build path; no global `document.createElement` introduced (every `createElement` goes through the `doc` parameter).

### Task 2 — CSS polish (D-09 / D-10 / D-12a / D-07 CSS half) — commit `c79c5c3`

- **`styles.css` Group 1** (lines ~634-665, in-place edit): RunModal tabs (`.leetcode-run-tab`), add-pill (`.leetcode-run-tab-add`), and Reset (`.leetcode-run-reset`) ported to canonical `.lc-fm__picker` tokens — `min-height: 30px`, `padding: 2px 8px`, `1px solid var(--background-modifier-border)`, `border-radius: 6px`, `background: var(--background-primary)`, hover `border-color: var(--text-muted)`, `font-size: 13px`, `transition: border-color 120ms ease`. The `.is-active` modifier preserves `background: var(--background-modifier-border)` + `font-weight: 600`. Existing `.leetcode-run-tab-delete` rules (lines 670-679 after edit) preserved verbatim.
- **`styles.css` Group 8** (lines ~896-915, in-place edit): inline `.leetcode-code-action-run` / `-submit` ported to the same neutral-button token set. Hover changes `border-color` only (background stays `var(--background-primary)`) so the row reads as a select-input pill rather than a filled button.
- **`styles.css` NEW section** (lines 1003-1153, +151 lines including comments): commented `Phase 5.4 — Run-mode verdict redesign + neutral button polish` block adding 9 rule groups:
  - **Group 2** — `.leetcode-verdict-case-tabs` strip wrapper (`flex-wrap: wrap`, `gap: 6px`).
  - **Group 1** — `.leetcode-verdict-case-tab` + `.is-active` (matches RunModal tabs verbatim).
  - **Group 3** — `.leetcode-verdict-case-chip` + `--pass` / `--fail` variants (color-mix on `--background-modifier-success` / `--background-modifier-error` at 80% opacity, foreground `--text-on-accent`; theme-aware via Phase 5.2 D-09 precedent).
  - **Group 4** — `.leetcode-verdict-title--success` / `--error` (`var(--text-success)` / `var(--text-error)` + `font-weight: 600`).
  - **Group 5** — `.leetcode-verdict-runtime` (muted line, `font-size: 13px`, `margin-bottom: 16px`).
  - **Group 6** — class-gated `.leetcode-verdict-case--{pass,fail} .leetcode-verdict-output-value / -expected-value` rules (D-07; raises specificity over the unconditional Phase 3 rules at lines 554-580 — those rules untouched per D-14).
  - **Group 7** — `.leetcode-verdict-input-section` (D-08; `var(--background-secondary)` surface, `padding: 8px 12px`, `border-radius: 6px`); `-input-param` flex row, `-input-param-name` muted small, `-input-param-value` monospace pre-wrap.
  - **Group 9** — `.leetcode-language-chevron-icon` (14×14 inline-flex slot, muted color) + `.leetcode-language-chevron-label` (inline-flex aligned).
- D-06 grep gate: zero NEW hex/rgb literals introduced (verified via `git diff -- styles.css | grep -E '^\+.*#[0-9a-fA-F]{3,6}\b|^\+.*rgb\('` returning zero matches).
- D-14 guardrail: `git diff src/solve/verdictModalRenderer.ts` is empty.
- Pitfall 6 scoping rule: zero `.leetcode-verdict button { ... }` selectors added; every new rule scoped to a specific class name.
- esbuild bundle builds clean.
- vitest baseline preserved: 7 RED-state failures (Plan 01 D-01/D-04/D-05/D-08/D-13 scaffolding) match HEAD~1 baseline exactly — Plan 04 introduced zero new test regressions.

## Selector inventory (per acceptance criterion)

| Selector | Match count | Source |
| --- | --- | --- |
| `leetcode-verdict-case-tab` | 4 | Group 1 (base + hover + .is-active + tab-strip child reference) |
| `leetcode-verdict-case-tabs` | 1 | Group 2 |
| `leetcode-verdict-case-chip` | 3 | Group 3 (base + 2 variants) |
| `leetcode-verdict-case-chip--pass` | 1 | Group 3 |
| `leetcode-verdict-case-chip--fail` | 1 | Group 3 |
| `leetcode-verdict-case--fail` | 2 | Group 6 |
| `leetcode-verdict-case--pass` | 2 | Group 6 |
| `leetcode-verdict-output-value` | 2 | Group 6 |
| `leetcode-verdict-expected-value` | 2 | Group 6 |
| `leetcode-verdict-input-section` | 1 | Group 7 |
| `leetcode-verdict-input-param` | 3 | Group 7 |
| `leetcode-verdict-input-param-name` | 1 | Group 7 |
| `leetcode-verdict-input-param-value` | 1 | Group 7 |
| `leetcode-verdict-runtime` | 2 | Group 5 (new line) + pre-existing Phase 3 line at 515 |
| `leetcode-verdict-title--success` | 1 | Group 4 |
| `leetcode-verdict-title--error` | 1 | Group 4 |
| `leetcode-language-chevron-icon` | 1 | Group 9 |
| `leetcode-language-chevron-label` | 1 | Group 9 |

All 18 renderer-emitted class names from Plan 03's contract have matching CSS selectors.

## Where the new CSS section lives

**Lines 1003-1153** of `styles.css` — appended AFTER the Phase 5.1 Edit-Mode + Reading-Mode action-row margin compensators (which sit at lines 985-998 of the post-edit file). Phase 5.3 chevron rules (lines 916-983) are untouched by Task 2's NEW section but receive the Group 9 sibling rules at the bottom. Total file size: 1153 lines (was 1000 lines — net +153 lines including ~50 comment lines).

## Phase 5.3 chevron Phase 5.3 rules — preservation check

`.leetcode-language-chevron` (lines 933-942 / pre-edit 933) and `.leetcode-language-chevron:hover` are UNCHANGED. The chevron pill shape ported in Phase 5.3 — `min-height: 30px`, `padding: 2px 8px`, `border: 1px solid var(--background-modifier-border)`, `border-radius: 6px`, hover border-color — continues to apply. Group 9 in this plan only ADDS sibling rules for the new icon span + label span DOM, which the chevron button now contains as children.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 — Bug] Removed comment-pattern matches that triggered Pitfall-6 grep false-positives**
- **Found during:** Task 2 verification (`grep -E '\\.leetcode-verdict\\s+button\\s*\\{' styles.css` initially matched my own section-header comment which quoted the literal anti-pattern as documentation).
- **Fix:** Reworded the section header comment so the literal selector pattern is paraphrased rather than quoted verbatim.
- **Files modified:** `styles.css`
- **Commit:** included in Task 2 commit `c79c5c3`.

### Out-of-scope Discoveries

- **6 pre-existing fallback hex literals in Phase 5.2/5 chip rules at lines 727-741** of `styles.css`. These are CSS-var fallback values (`var(--color-green, #22c55e)`, `var(--text-on-accent, #fff)`, etc.) — a documented Obsidian-recommended pattern for cross-version compat. Not added by Plan 04. Verified via `git diff -- styles.css | grep -E '^\+.*#[0-9a-fA-F]{3,6}\b'` returning zero matches.
- **35 pre-existing tsc errors in unrelated test files** (`tests/main/codeActionsPostProcessor.test.ts`, `tests/graph/SubmissionPickerModal.test.ts`, `tests/main/python3Highlighter.test.ts`, `tests/solve/SessionExpiredNotice.test.ts`, plus one in `src/main/codeActionsPostProcessor.ts`). Verified by `git stash` round-trip: same 35 errors before AND after Plan 04. Documented in Plan 01 SUMMARY's "Out-of-scope Discoveries" section. Out of scope per scope boundary rule.
- **7 pre-existing RED-state vitest failures** (Plan 01 scaffolding for D-01, D-04, D-05, D-08, D-13). These are intentional — Plan 02 (D-01 RunModal join) and Plan 03 (verdictModalRenderer rewrite) will land them GREEN. Plan 04's scope is only D-12b chevron + CSS polish; the 4 D-12b chevron-glyph it-blocks (this plan's responsibility) are now GREEN.

## Authentication Gates

None — pure code/CSS changes.

## Open Visual-Only Items Deferred to Plan 05 Live-Smoke

- **RunModal tabs ↔ VerdictModal tabs visual match** — both surfaces now use the same `.lc-fm__picker` token values, but live-smoke must confirm the active-state highlight, hover transition, and pixel-perfect alignment in the actual Obsidian Electron host (happy-dom does not render layout).
- **Chevron pill "feels like a textbox"** — `.lc-fm__picker` shape + Lucide `chevron-down` icon should now read as a select-input affordance per the user's "make the button looks more like a text box" directive (CONTEXT §Specifics). Live-smoke validates the visual perception.
- **Light/dark theme parity** — color-mix on `--background-modifier-success` / `-error` at 80% opacity has been validated for chips in Phase 5.2 D-09; new chip variants in Group 3 follow the same pattern but live-smoke should spot-check both themes.
- **Inline Run/Submit hover** — hover changes border-color only (no background flip); live-smoke validates the hover affordance reads correctly without a filled-button look.
- **Group 9 chevron icon size** — 14×14 muted-color slot. Live-smoke confirms the SVG rendered by `setIcon('chevron-down')` fits cleanly inside the slot without clipping or alignment drift.
- **Plan 03 dependency** — Many of the new CSS rules (Groups 1-7) require Plan 03's renderer to emit the corresponding DOM. Live-smoke is the integration gate after Plans 03 + 04 merge.

## Self-Check: PASSED

- `src/main/languageChevronWidget.ts` MODIFIED — FOUND
- `tests/helpers/obsidian-stub.ts` MODIFIED — FOUND
- `styles.css` MODIFIED — FOUND (1153 lines, was 1000)
- Commit `2f0b938` (Task 1) FOUND in `git log --oneline`
- Commit `c79c5c3` (Task 2) FOUND in `git log --oneline`
- `npx vitest run tests/main/languageChevronWidget.test.ts`: 25 passed (was 21 before, +4 D-12b RED→GREEN)
- D-06 grep gate: zero NEW hex/rgb literals (`git diff -- styles.css | grep '^\+.*#[0-9a-fA-F]'` → 0 matches)
- D-14 guardrail: `git diff src/solve/verdictModalRenderer.ts` — empty
- Pitfall 6 scoping: `grep -E '\\.leetcode-verdict\\s+button\\s*\\{' styles.css` → 0 matches
- esbuild bundle build: clean exit
- Selector inventory: all 18 renderer-emitted class names from the Plan 03 contract have ≥1 matching CSS selector
