---
phase: 05.4
plan: 05
type: execute
wave: 3
depends_on: ["05.4-01", "05.4-02", "05.4-03", "05.4-04"]
files_modified:
  - .planning/phases/05.4-run-verdict-ux-button-polish/05.4-UAT.md
  - .planning/phases/05.4-run-verdict-ux-button-polish/05.4-VALIDATION.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Run-mode VerdictModal renders the LC-style layout (header inline with verdict + runtime, always-on tab strip, three stacked Input/Output/Expected sections) verified against user-provided LC.com screenshots"
    - "Tab strip layout in RunModal and Run-mode VerdictModal feels like the same surface (D-10 cross-modal visual consistency)"
    - "Inline `## Code` action row chevron renders the Lucide chevron-down glyph and reads as a select-input pill, not a button (D-12b)"
    - "Inline `## Code` action row Run/Submit buttons render with the new neutral-button polish (D-12a)"
    - "Submit verdict modal is bytes-identical to pre-5.4 (D-14 guardrail)"
    - "Live multi-case interpret_solution returns matching-length code_answer + expected_code_answer arrays (resolves Plan 01 A2 assumption if it was synthetic)"
    - "All visible colors adapt correctly between light theme and dark theme (D-06 + D-07 theme-aware contract)"
    - "No regression in Phase 5.1 / 5.2 / 5.3 surfaces (Edit-mode Run/Submit row, Reading-mode buttons, language chevron interactions, copy-to-code lang sync, edit-mode block widget below fence)"
    - "All Phase 5.4 unit tests are GREEN; full suite is GREEN; bundle builds; D-06 grep gate clean"
  artifacts:
    - path: ".planning/phases/05.4-run-verdict-ux-button-polish/05.4-UAT.md"
      provides: "Live-smoke checklist with PASS/FAIL per item; sign-off block"
      contains: "Section A"
    - path: ".planning/phases/05.4-run-verdict-ux-button-polish/05.4-VALIDATION.md"
      provides: "Updated with nyquist_compliant: true, sign-off date, full per-task verification map (filled in across Plans 01–04)"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "05.4-UAT.md (live-smoke checklist)"
      to: "user-provided LC.com screenshots"
      via: "visual fidelity checks per D-05/D-07/D-13"
      pattern: "LC.com screenshot"
    - from: "05.4-UAT.md Section H (multi-case spike)"
      to: "tests/solve/fixtures/run-multi-case.json"
      via: "A2 confirmation"
      pattern: "code_answer\\.length === expected_code_answer\\.length"
---

<objective>
Wave 3 live-smoke checkpoint. Human-verified validation in the dev vault that every D-09 / D-10 / D-12a / D-12b / D-14 surface behaves correctly on a real Obsidian build, plus the multi-case spike that resolves RESEARCH.md assumption A2 (live LC interpret_solution returns matching-length per-case arrays). Captures results in `05.4-UAT.md` (preserving the existing 05.3-UAT.md format), confirms D-14 Submit-modal regression safety, and signs off `05.4-VALIDATION.md` with `nyquist_compliant: true`.

Purpose: Plans 02-04 prove correctness against happy-dom under vitest, but D-09 / D-10 / D-12a / D-12b are visual-only ("looks like a select-input pill", "tabs feel like the same surface", "matches LC.com screenshots") — these gates are unfalsifiable in unit tests. This plan is the gate.

Output: A completed `05.4-UAT.md` with per-item PASS/FAIL across ~10 sections; an updated `05.4-VALIDATION.md` with `nyquist_compliant: true` and the per-task verification map filled in; a `05.4-05-SUMMARY.md` capturing the live-smoke run with screenshots if any defects are found.
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/moxu/projects/obsidian-leetcode/.planning/PROJECT.md
@/Users/moxu/projects/obsidian-leetcode/.planning/ROADMAP.md
@/Users/moxu/projects/obsidian-leetcode/.planning/STATE.md
@/Users/moxu/projects/obsidian-leetcode/.planning/phases/05.4-run-verdict-ux-button-polish/05.4-CONTEXT.md
@/Users/moxu/projects/obsidian-leetcode/.planning/phases/05.4-run-verdict-ux-button-polish/05.4-RESEARCH.md
@/Users/moxu/projects/obsidian-leetcode/.planning/phases/05.4-run-verdict-ux-button-polish/05.4-VALIDATION.md
@/Users/moxu/projects/obsidian-leetcode/.planning/phases/05.4-run-verdict-ux-button-polish/05.4-01-SUMMARY.md
@/Users/moxu/projects/obsidian-leetcode/.planning/phases/05.4-run-verdict-ux-button-polish/05.4-02-SUMMARY.md
@/Users/moxu/projects/obsidian-leetcode/.planning/phases/05.4-run-verdict-ux-button-polish/05.4-03-SUMMARY.md
@/Users/moxu/projects/obsidian-leetcode/.planning/phases/05.4-run-verdict-ux-button-polish/05.4-04-SUMMARY.md
@/Users/moxu/projects/obsidian-leetcode/.planning/phases/05.3-language-aware-editor/05.3-UAT.md
@/Users/moxu/projects/obsidian-leetcode/CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-flight automated gates + UAT.md skeleton</name>
  <files>.planning/phases/05.4-run-verdict-ux-button-polish/05.4-UAT.md</files>
  <read_first>
    - .planning/phases/05.3-language-aware-editor/05.3-UAT.md (format precedent — copy section/header structure verbatim)
    - .planning/phases/05.4-run-verdict-ux-button-polish/05.4-VALIDATION.md (Manual-Only Verifications table — every row becomes a UAT section)
    - .planning/phases/05.4-run-verdict-ux-button-polish/05.4-CONTEXT.md (D-01..D-16 — every visible decision needs a UAT row)
    - All four 05.4-XX-SUMMARY.md files from prior plans (capture any open items / synthetic-fixture flags / visual-only deferrals)
  </read_first>
  <action>
    Run six pre-flight automated gates and capture stdout/stderr for each. Each gate must PASS before the human-verify task begins; a FAIL routes to remediation rather than to live smoke.

    Gate 1: `npm test -- --run` — full suite must be GREEN; every D-* RED test from Plan 01 should now be GREEN; submit-side regression tests must remain GREEN.
    Gate 2: `npm run build` — bundle must build cleanly with no TS/esbuild errors.
    Gate 3: `npm run lint` if configured — zero `eslint-plugin-obsidianmd` violations.
    Gate 4: D-14 LOCKED guardrail — run `git log --pretty=format:"" -p -- src/solve/verdictModalRenderer.ts | grep -E "^[-+]" | grep -E "renderSubmitVerdict|renderAcBody|renderWaBody|renderTleBody|renderMleBody|renderCeBody|renderReBody"` against the Phase 5.4 commits and confirm zero output (renderSubmitVerdict and submit-body helpers are byte-identical).
    Gate 5: D-06 grep gate — run `! grep -vE '^\s*(/\*|\*|//)' styles.css | grep -E '#[0-9a-fA-F]{3,6}\b|rgb\(' | grep -vF 'rgba(0, 0, 0, 0.15)'` and confirm zero matches (no new hardcoded colors except the documented rgba popover-shadow exception).
    Gate 6: A2 fixture provenance — read `tests/solve/fixtures/run-multi-case.json` and confirm whether `_fixture_note` starts with `LIVE-CAPTURED` or `SYNTHETIC-NOT-LIVE`. If SYNTHETIC, the live-smoke task (Task 2) MUST capture a live multi-case response and refresh the fixture; flag this in the UAT.

    After all six gates pass (or the orchestrator is informed of failures), read all four `05.4-XX-SUMMARY.md` files from prior plans to surface any open items, synthetic-fixture flags, or visual-only deferrals each plan flagged.

    Then create `.planning/phases/05.4-run-verdict-ux-button-polish/05.4-UAT.md` mirroring the heading style of `05.3-UAT.md` with the following 11 sections (A through K):

    Section A. Pre-flight gates — six rows, one per gate above; columns Item / Expected / Result (PASS/FAIL/N/A) / Notes; pre-fill Result with the captured outputs from this task.
    Section B. Run-mode verdict layout (D-05, D-13) — rows: header chrome contains "{verdict}  Runtime: N ms" with verdict bold and state-colored; header chrome does NOT contain the problem title; tab strip is rendered even with N=1; three stacked sections render in order Input → Output → Expected.
    Section C. Per-case PASS/FAIL chips (D-04, D-07) — rows: passing case shows PASS chip with success palette; failing case shows FAIL chip with error palette; chip color matches the per-index trim-compare; aggregate title color follows D-07 (success on all-pass, error on any-fail).
    Section D. Input section labeling (D-08) — rows: when metaData is present, each param renders as `name = value`; when metaData is missing/malformed, falls back to a single raw input dump pre block.
    Section E. Theme-aware coloring (D-06, D-07) — rows: light theme — Output text red on fail, Expected green on fail, both readable on pass; dark theme — same; section labels muted in both themes; section surfaces use background-secondary in both themes.
    Section F. Multi-case spike + A2 resolution (D-01, D-02) — rows: a 2-case Run triggers exactly one `/interpret_solution/` request (DevTools → Network); the response shows `code_answer.length === expected_code_answer.length === 2`; if Plan 01 fixture was SYNTHETIC, capture the live response and update the fixture file then re-run `npm test -- --run`.
    Section G. Run-mode error states (D-15) — rows: forcing a compile error and clicking Run renders a single `.leetcode-verdict-error-pre` block with no `.leetcode-verdict-case-tab` elements; forcing a runtime error renders the same shape with the runtime-error text.
    Section H. Run-side copy button absence (D-16 Run side) — row: the Run verdict modal contains zero buttons whose text matches /Copy.*test.*case/i.
    Section I. Submit-mode regression (D-14, D-16 Submit side) — rows: submit a known-WA solution and confirm the runtime/memory/percentile chrome is byte-identical to the pre-5.4 baseline; the "Copy failing testcase" button is still present on the Submit verdict; clicking it appends the failing case as a new tab in RunModal via `openRunModalWithSeedAppended`.
    Section J. Button + chevron polish (D-09, D-10, D-12a, D-12b) — rows: RunModal tabs and VerdictModal case tabs share the same shape, hover, and `.is-active` highlight; inline Run/Submit buttons show the new neutral-button polish; chevron renders the Lucide `chevron-down` icon (no literal `▼`); chevron reads as a select-input pill ("feels like a textbox" per user wording); chevron is Edit-Mode only (Reading Mode shows ONLY Run + Submit per Phase 5.3 D-09 lock); both light + dark theme parity.
    Section K. Cross-phase regression — rows: Phase 5.1 inline action row still anchors below the `## Code` fence; Phase 5.2 python3 highlighting + AC/WA chip contrast still work; Phase 5.3 chevron dropdown opens + copy-to-code lang sync still triggers + edit-mode block widget still renders below fence.

    Each section row carries columns Item / Expected / Result (PASS/FAIL/N/A) / Notes. Pre-fill Item and Expected; leave Result and Notes blank for Task 2 to fill. Save UAT.md and confirm save.
  </action>
  <verify>
    <automated>test -f .planning/phases/05.4-run-verdict-ux-button-polish/05.4-UAT.md &amp;&amp; grep -E '^## Section [A-K]\b' .planning/phases/05.4-run-verdict-ux-button-polish/05.4-UAT.md | wc -l | xargs -I{} test {} -ge 11 &amp;&amp; ! git log --pretty=format:"" -p -- src/solve/verdictModalRenderer.ts | grep -E '^[-+].*function (renderSubmitVerdict|renderAcBody|renderWaBody|renderTleBody|renderMleBody|renderCeBody|renderReBody)'</automated>
  </verify>
  <done>UAT.md exists with all 11 sections (A-K), pre-flight gate outputs captured in Section A, and Result/Notes columns left blank for Task 2.</done>
  <what-built>
    Plans 01-04 have completed automated gates:
    - Plan 01: runArity helpers + multi-case fixture + RED test scaffolding
    - Plan 02: RunModal.onRun multi-tab join (D-01)
    - Plan 03: renderRunResult full LC-style redesign (D-04/05/07/08/13/15/16) + D-14 untouched
    - Plan 04: CSS polish + chevron Lucide glyph swap (D-09/10/12a/12b)
    UAT.md skeleton with 11 sections waiting for live-smoke results (Task 2).
  </what-built>
  <how-to-verify>
    Confirm `05.4-UAT.md` exists, all 11 sections (A-K) are present, and Section A has the six pre-flight gate results captured with command output.
  </how-to-verify>
  <resume-signal>Type "approved" once UAT.md exists and all 6 pre-flight gates show PASS. If any gate FAILED, type "blocked: <gate-name>" and the orchestrator will route back to a remediation plan.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-smoke walkthrough in dev vault</name>
  <files>.planning/phases/05.4-run-verdict-ux-button-polish/05.4-UAT.md, .planning/phases/05.4-run-verdict-ux-button-polish/05.4-VALIDATION.md</files>
  <read_first>
    - .planning/phases/05.4-run-verdict-ux-button-polish/05.4-UAT.md (Task 1 output — fill in Result + Notes columns)
    - User-provided LC.com screenshots (Wrong Answer + Accepted) — visual oracle for sections B, C, E, J
    - .planning/phases/05.4-run-verdict-ux-button-polish/05.4-VALIDATION.md (Manual-Only Verifications table + Validation Sign-Off block)
  </read_first>
  <action>
    Build the plugin into a dev vault using the Phase 5.3 hot-reload pattern (`npm run dev`, OR copy `main.js` + `manifest.json` + `styles.css` into `<vault>/.obsidian/plugins/obsidian-leetcode/` and reload Obsidian). Open a `two-sum` lc-slug note (fetch via Problem Browser if absent).

    Walk through every UAT section A-K in order, filling in Result (PASS / FAIL / N/A) and Notes for each row in `05.4-UAT.md`. For visual rows (sections B / C / E / J), compare against the user-provided LC.com Wrong-Answer + Accepted screenshots.

    Targeted scenarios required during the walkthrough:
    - Section B/C/D/E/F: trigger a 2-case Run with cases `[2,7,11,15]\n9` and `[3,2,4]\n6` (the canonical D-02 two-sum 2-case shape). Inspect DevTools → Network: confirm exactly one `/interpret_solution/` request (D-01 batched), confirm `code_answer.length === expected_code_answer.length === 2`. If `_fixture_note` in `tests/solve/fixtures/run-multi-case.json` was SYNTHETIC, copy this live response payload into the fixture file (replacing the synthetic content) and re-run `npm test -- --run` to confirm fixture-driven tests still PASS.
    - Section G: introduce a syntax error in the solution and click Run; observe the compile-error single-block render. Then introduce a runtime error (e.g., divide by zero) and observe the runtime-error single-block render. Both must show zero `.leetcode-verdict-case-tab` elements.
    - Section H: visually scan the Run verdict modal for any "Copy"-named button — none expected.
    - Section I (D-14 regression): submit a known-WA solution. Confirm the runtime/memory/percentile chrome is BYTE-IDENTICAL to the pre-5.4 baseline (compare against a Phase 5.3 screenshot or the existing renderSubmitVerdict tests). Click "Copy failing testcase" and confirm RunModal opens with the failing case appended as a new tab via `openRunModalWithSeedAppended`. If chrome diverges, capture a screenshot, mark the row FAIL, and route back to remediation.
    - Section J (chevron pill subjective check): the user confirms the chevron "feels like a textbox" / "looks like a select-input" — D-12b user-worded acceptance criterion. Confirm Reading Mode shows ONLY Run + Submit (no chevron) — Phase 5.3 D-09 lock preserved.
    - Section E + J theme parity: toggle theme to light then dark; re-verify visual rows in both themes.
    - Section K cross-phase spot-check: open a non-Phase-5.4 surface (Phase 5.1 inline row anchored below `## Code` fence; Phase 5.2 python3 highlighting + AC/WA chip contrast on Past Submissions; Phase 5.3 chevron dropdown open/close + copy-to-code lang sync + edit-mode block widget below fence) — confirm no regressions.

    After every row is filled, update `05.4-VALIDATION.md`:
    - Set `nyquist_compliant: true` in frontmatter
    - Set `wave_0_complete: true` in frontmatter
    - Update `status:` from `draft` to `verified` if every UAT row is PASS or N/A; otherwise `human_needed` with a list of failing rows
    - Fill in the per-task verification map at the top (one row per task across Plans 01-04 with their actual automated commands and statuses)
    - Set `**Approval:** approved <YYYY-MM-DD>` if every row is PASS/N/A; otherwise `**Approval:** pending — see UAT failures`

    Save both files.
  </action>
  <verify>
    <automated>grep -E '^nyquist_compliant: true' .planning/phases/05.4-run-verdict-ux-button-polish/05.4-VALIDATION.md &amp;&amp; ! grep -E '\| (FAIL|⬜ pending)\b' .planning/phases/05.4-run-verdict-ux-button-polish/05.4-UAT.md</automated>
  </verify>
  <done>Every UAT row is PASS or N/A (no FAIL, no pending), VALIDATION.md is signed off with nyquist_compliant: true and approval date, A2 is resolved (live or live-refreshed fixture), D-14 byte-identical regression is human-confirmed.</done>
  <what-built>
    UAT.md skeleton (Task 1 output) with 11 unchecked sections covering every visible decision in Phase 5.4. Pre-flight gates already PASS. The user is now expected to walk through the dev vault and verify each row visually against the LC.com screenshots and DevTools network traces.
  </what-built>
  <how-to-verify>
    Confirm 05.4-UAT.md is fully populated (no blank Result columns), 05.4-VALIDATION.md has `nyquist_compliant: true` and `wave_0_complete: true`, A2 is resolved (live response captured or fixture refreshed), and D-14 chrome is byte-identical to pre-5.4 baseline.
  </how-to-verify>
  <resume-signal>Type "approved — all UAT rows PASS or N/A" to close Phase 5.4. Type "regression: <section>" to route back to plan-phase --gaps for remediation.</resume-signal>
</task>

</tasks>

<verification>
- All 6 pre-flight automated gates PASS before live smoke begins
- 05.4-UAT.md exists with all 11 sections filled in (Result + Notes per row)
- 05.4-VALIDATION.md has `nyquist_compliant: true`, `wave_0_complete: true`, `status: verified` (or explicit `human_needed` with failing-row list)
- D-14 guardrail human-confirmed: Submit verdict modal looks bytes-identical to pre-5.4 baseline
- D-12b human-confirmed: chevron "feels like a select-input pill" per user wording
- A2 resolved: live multi-case interpret_solution response captured (or fixture re-captured if Plan 01 was synthetic)
- Cross-phase regression spot-check passes: Phase 5.1 inline action row, Phase 5.2 python3 + chip contrast, Phase 5.3 chevron dropdown + copy-to-code lang sync + edit-mode block widget below fence
</verification>

<success_criteria>
- 05.4-UAT.md is the canonical record of what was visually verified, in what theme, against what oracle (LC.com screenshots / live network response)
- 05.4-VALIDATION.md is signed off with nyquist_compliant: true
- Phase 5.4 is ready for ROADMAP closure
- If any UAT row FAILS, the orchestrator routes to plan-phase --gaps for remediation rather than silently passing
</success_criteria>

<output>
After completion, create `.planning/phases/05.4-run-verdict-ux-button-polish/05.4-05-SUMMARY.md` documenting:
- Pre-flight gate results (6 commands + outputs)
- UAT row results summary (X PASS / Y FAIL / Z N/A out of total)
- Whether A2 was confirmed live or remained synthetic (and any follow-up gap)
- Theme parity result (light + dark both pass)
- D-14 byte-identical regression confirmation
- Cross-phase regression spot-check results
- List of any visual defects found (with screenshots if available) and the gap-closure plan path if remediation is needed
</output>
