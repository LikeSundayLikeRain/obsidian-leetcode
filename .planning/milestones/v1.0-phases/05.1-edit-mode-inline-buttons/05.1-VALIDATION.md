---
phase: 5.1
slug: edit-mode-inline-buttons
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-10
---

# Phase 5.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 4.1.5 |
| **Config file** | `vitest.config.ts` |
| **Quick run command** | `npx vitest run tests/main/codeActionsEditorExtension.test.ts` |
| **Full suite command** | `npm test` |
| **Estimated runtime** | ~15 seconds full suite |

---

## Sampling Rate

- **After every task commit:** Run quick run command (module-scoped tests)
- **After every plan wave:** Run `npm test` (full suite — catches reading-mode regression per D-11)
- **Before `/gsd-verify-work`:** Full suite must be green AND live-smoke checkpoint passed
- **Max feedback latency:** 20 seconds

---

## Per-Task Verification Map

> Task IDs are placeholders filled by planner. The planner MUST preserve a test command or Wave 0 dependency on every task.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 5.1-01-01 | 01 | 0 | POLISH-07 | — | N/A | unit (stubs) | `npx vitest run tests/main/codeActionsEditorExtension.test.ts` | ❌ W0 | ⬜ pending |
| 5.1-02-01 | 02 | 1 | POLISH-07 | — | Widget non-block, no layout corruption | unit | `npx vitest run tests/main/codeActionsEditorExtension.test.ts -t "widget emits .leetcode-code-actions"` | ❌ W0 | ⬜ pending |
| 5.1-02-02 | 02 | 1 | POLISH-07 | — | `lc-slug` frontmatter gate | unit | `npx vitest run -t "lc-slug gate"` | ❌ W0 | ⬜ pending |
| 5.1-02-03 | 02 | 1 | POLISH-07 | — | `## Code` fence targeting only | unit | `npx vitest run -t "targets only ## Code fence"` | ❌ W0 | ⬜ pending |
| 5.1-02-04 | 02 | 1 | POLISH-07 | — | Idempotent rebuild across transactions | unit | `npx vitest run -t "idempotent across updates"` | ❌ W0 | ⬜ pending |
| 5.1-02-05 | 02 | 1 | POLISH-07 | — | Click → `plugin.runFromActive` / `submitFromActive` direct-call | unit | `npx vitest run -t "dispatches runFromActive"` | ❌ W0 | ⬜ pending |
| 5.1-02-06 | 02 | 1 | POLISH-07 | — | Reading-mode regression suite still green | unit | `npx vitest run tests/main/codeActionsPostProcessor.test.ts` | ✅ | ⬜ pending |
| 5.1-03-01 | 03 | 2 | POLISH-07 | — | No layout corruption live | **manual live-smoke** | see Manual-Only Verifications | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/main/codeActionsEditorExtension.test.ts` — stubs for POLISH-07 (frontmatter gate, `## Code` detection, idempotency, click dispatch)
- [ ] `tests/helpers/obsidian-stub.ts` — CM6 additions: stub `editorInfoField`, `editorLivePreviewField`, minimal `EditorView` + `EditorState` for unit tests (only if researcher's recommended regex-over-doc approach + head-less test harness is viable; otherwise tests live in harness-level and manual live-smoke covers gaps)

*Framework already installed: vitest 4.1.5 is in devDependencies.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| No layout corruption in Live Preview (no blank rectangles, no shifted headings, no inserted whitespace) | POLISH-07 / Success Criterion 4 | **D-10 LOCKED:** unit tests passed during 05-UAT G1 while Live Preview was visibly corrupted. Visual verification REQUIRES eyes on the actual rendered editor. | Dev vault open → `Cmd-R` reload → open an `lc-slug` note → toggle to **Live Preview** → scroll through entire note → verify buttons inline below `## Code` fence, no blank rectangles, no shifted headings, no duplicates → scroll to top, scroll to bottom, edit content above + below the fence → confirm buttons stay anchored. |
| No layout corruption in Source Mode | POLISH-07 / Success Criterion 4 | Same reason as above. | On same note toggle to **Source Mode** → scroll through → verify buttons anchored below closing fence line → edit content → confirm idempotency. |
| Reading Mode regression check | POLISH-07 / Success Criterion 5 | Human-visible confirmation that the 05-05 shipped affordance still works unchanged. | Toggle to **Reading Mode** → verify Run/Submit still render below `## Code` block → click Run → verify Notice fires correctly. |
| Light + Dark theme rendering | POLISH-07 / Success Criterion 6 | Visual-only concern; CSS variables driven, no programmatic check. | Toggle theme (Settings → Appearance → Base color scheme) → verify buttons remain readable with correct contrast in both themes. |
| Click actually submits code | POLISH-07 / Success Criterion 3 | Full end-to-end runs through LC API + Notice rendering; only meaningful live. | In Live Preview on a real LC problem note → click **Run** → verify "Running…" Notice → then "Run result: …" Notice → click **Submit** → verify "Submitting…" → "Accepted" / "Wrong Answer" Notice. |

---

## Nyquist Validation Dimensions

Per Research Architecture §Validation Architecture, the following Nyquist dimensions apply to this CM6 UI phase:

| Dimension | Concern | Coverage Mechanism |
|-----------|---------|--------------------|
| **D1. Correctness under typed input (pure logic)** | Frontmatter gate returns right value; fence detection picks right fence | Unit tests 5.1-02-02 + 5.1-02-03 |
| **D4. Behavior under user input** | Widget survives rapid edits above, below, inside `## Code` fence; click wiring invokes right method | Unit tests 5.1-02-04 + 5.1-02-05; manual live-smoke for visual integrity |
| **D6. Resilience to platform/environment changes** | Widget behaves on Live Preview vs Source Mode; theme toggle doesn't break layout | Manual live-smoke (5.1-03-01); visual theme check in Manual-Only table above |
| **D8. Live feedback / visual acceptance** | No layout corruption in rendered editor — **CANNOT be unit-tested** per 05-UAT G1 | Manual live-smoke checkpoint at Wave 2 (D-10 LOCKED) |

Dimensions D2 (boundary), D3 (error handling), D5 (concurrency), D7 (integration) are N/A for this phase — pure UI surface with no new error paths or concurrency primitives.

---

## Validation Sign-Off

- [ ] All tasks have automated verify OR Wave 0 dependencies OR sit on the manual-live-smoke path with explicit Manual-Only entry
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (manual live-smoke is the terminal Wave 2 gate)
- [ ] Wave 0 covers test infrastructure for all new unit tests
- [ ] No watch-mode flags (CI/local quick-run is single-shot)
- [ ] Feedback latency < 20s
- [ ] Manual live-smoke checkpoint scheduled as explicit Wave 2 task (D-10)
- [ ] `nyquist_compliant: true` set in frontmatter after planner populates all task IDs

**Approval:** pending
