---
phase: 05.3
plan: 03
type: execute
wave: 3
depends_on:
  - 05.3-02
files_modified:
  - scripts/check-bundle-size.sh
  - scripts/prerelease-check.sh
  - package.json
autonomous: true
requirements:
  - POLISH-09
tags:
  - build-gate
  - bundle-size
  - prerelease
  - shell-script

must_haves:
  truths:
    - "A standalone scripts/check-bundle-size.sh exits 0 when main.js ≤ 700 KB and exit 1 when above 700 KB"
    - "Warning fires at 600 KB soft threshold without failing the build"
    - "prerelease-check.sh Gate 12 delegates to the new script so the 700 KB ceiling is a single source of truth"
    - "npm run build still succeeds after the gate is wired (the current Phase 5.3 build is within the cap; assumption A9 validated with a concrete byte number)"
    - "Post-build output remains a single main.js file — no additional .js chunks (assumption A8 re-validated)"
  artifacts:
    - path: "scripts/check-bundle-size.sh"
      provides: "Standalone Phase 5.3 D-09 / D-15 bundle-size gate: 700 KB hard fail, 600 KB soft warn, works when invoked directly or chained from prerelease-check.sh"
      min_lines: 30
    - path: "scripts/prerelease-check.sh"
      provides: "Existing multi-gate script with Gate 12 replaced by a delegation to scripts/check-bundle-size.sh (700 KB ceiling replaces the old 200 KB local constant)"
      contains: "scripts/check-bundle-size.sh"
    - path: "package.json"
      provides: "Optional — if project convention uses a scripts entry for bundle checks, add 'check:bundle-size'; otherwise leave untouched"
      contains: "check-bundle-size"
  key_links:
    - from: "scripts/prerelease-check.sh"
      to: "scripts/check-bundle-size.sh"
      via: "shell-out: `bash scripts/check-bundle-size.sh`"
      pattern: "bash scripts/check-bundle-size\\.sh"
    - from: "scripts/check-bundle-size.sh"
      to: "main.js"
      via: "wc -c + byte-count threshold"
      pattern: "wc -c < \\\"\\$MAIN\\\""
---

<objective>
Install the Phase 5.3 D-09 / D-15 bundle-size gate as a hard build guard: a standalone `scripts/check-bundle-size.sh` that fails the build if `main.js` exceeds 700 KB (warning at 600 KB), plus a delegation hook in the existing `scripts/prerelease-check.sh` Gate 12 so the ceiling lives in exactly one place. Also re-measure `main.js` against the pre-install baseline captured in Plan 01 to close out assumptions A8 (esbuild inline-bundle — no chunk files emitted) and A9 (final bundle ≤ 700 KB with headroom).

Purpose: ROADMAP §Phase 5.3 success criterion 6 ("bundle size impact documented and accepted") is a ship gate. CONTEXT D-09 locked the 700 KB cap. D-15 mandates the gate runs on every build AND every live-smoke — not only at release time. Plan 04's live-smoke cannot begin until this script is in place.

Output: 1 new shell script (executable), 1 modified existing shell script (Gate 12 delegates), a recorded post-Wave-1 bundle size in this plan's SUMMARY, and a green `prerelease-check.sh` end-to-end run.
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05.3-language-aware-editor/05.3-CONTEXT.md
@.planning/phases/05.3-language-aware-editor/05.3-RESEARCH.md
@.planning/phases/05.3-language-aware-editor/05.3-PATTERNS.md
@.planning/phases/05.3-language-aware-editor/05.3-01-SUMMARY.md
@.planning/phases/05.3-language-aware-editor/05.3-02-SUMMARY.md
@scripts/prerelease-check.sh
@esbuild.config.mjs

<interfaces>
<!-- Shape of the existing prerelease-check.sh Gate 12, inherited from Phase 5 Plan 06. -->

Current `scripts/prerelease-check.sh` Gate 12 (lines 237–253 before edit):
```bash
# Gate 12: main.js <= 200 kB (warn at 100 kB)
# ... body uses wc -c < main.js and integer math ...
MAIN_BYTES=$(wc -c < main.js | tr -d ' ')
LIMIT_BYTES=$((200 * 1024))
if [ "$MAIN_BYTES" -gt "$LIMIT_BYTES" ]; then
  fail "..."
fi
```

The script's `fail()` / `ok()` / `warn()` helpers are defined earlier in the same script (Phase 5 Plan 06 pattern). The delegation approach preserves those helpers for context reporting.

`esbuild.config.mjs` externals (unchanged, verified in Plan 02 Task 3):
```javascript
external: ['@codemirror/autocomplete', '@codemirror/language', '@codemirror/state', '@codemirror/view', '@codemirror/commands', '@codemirror/lint', '@lezer/common', '@lezer/highlight', '@lezer/lr', 'obsidian', 'electron', '@codemirror/closebrackets', '@codemirror/panel', '@codemirror/rangeset']
```
(Full list confirmed; the 6 `@codemirror/lang-*` packs are NOT in this array — they MUST bundle per D-08.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/check-bundle-size.sh as the single-source-of-truth 700 KB gate</name>
  <files>scripts/check-bundle-size.sh</files>
  <read_first>
    scripts/prerelease-check.sh,
    .planning/phases/05.3-language-aware-editor/05.3-RESEARCH.md,
    .planning/phases/05.3-language-aware-editor/05.3-PATTERNS.md
  </read_first>
  <behavior>
    - Script begins with `#!/usr/bin/env bash` + `set -eo pipefail` (PATTERNS §Pattern: Bash Build Gate Structure).
    - Resolves `ROOT="$(cd "$(dirname "$0")/.." && pwd)"` and `MAIN="$ROOT/main.js"` (PATTERNS analog from prerelease-check.sh line 33 style).
    - If `main.js` missing, echo `BUNDLE CHECK FAIL: main.js missing — run 'npm run build' first` to stderr and `exit 1`.
    - `MAIN_BYTES=$(wc -c < "$MAIN" | tr -d ' ')` — byte count (matches analog line 243 idiom).
    - `MAIN_KB=$((MAIN_BYTES / 1024))` — integer division for reporting.
    - `LIMIT_BYTES=$((700 * 1024))` — D-09 cap.
    - `WARN_BYTES=$((600 * 1024))` — D-09 soft threshold (matches RESEARCH Pitfall 10 escalation trigger).
    - If `$MAIN_BYTES > $LIMIT_BYTES`, echo `BUNDLE CHECK FAIL: main.js is ${MAIN_KB} KB (>700 KB ceiling; D-09 escalation required)` to stderr and `exit 1`.
    - Else if `$MAIN_BYTES > $WARN_BYTES`, echo `BUNDLE CHECK WARN: main.js is ${MAIN_KB} KB (>600 KB soft warning; ceiling 700 KB)` to stdout (no exit).
    - Always ends with `echo "BUNDLE CHECK OK: main.js ${MAIN_KB} KB within budget"` and exit 0 (either after warn or after no-warn).
    - File executable via `chmod +x` after creation.
    - No use of `jq`, `node`, or any other external tool — pure bash + coreutils.
    - Body matches RESEARCH §Code Examples lines 852–887 verbatim where practical (that shape is already verified).
  </behavior>
  <action>
    Create `scripts/check-bundle-size.sh` with the exact body from RESEARCH §"scripts/check-bundle-size.sh (D-09/D-15)" lines 852–887. That script is canonical — copy it. After writing, `chmod +x scripts/check-bundle-size.sh`. Verify the file is executable (`test -x scripts/check-bundle-size.sh`). Do not introduce any additional tooling dependencies.
  </action>
  <verify>
    <automated>test -x scripts/check-bundle-size.sh &amp;&amp; bash scripts/check-bundle-size.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-bundle-size.sh` exists and is ≥ 30 lines.
    - `test -x scripts/check-bundle-size.sh` succeeds (file is executable).
    - `head -1 scripts/check-bundle-size.sh` is `#!/usr/bin/env bash`.
    - `grep -c "set -eo pipefail" scripts/check-bundle-size.sh` ≥ 1.
    - `grep -c "LIMIT_BYTES=\\\$((700 \\* 1024))" scripts/check-bundle-size.sh` ≥ 1.
    - `grep -c "WARN_BYTES=\\\$((600 \\* 1024))" scripts/check-bundle-size.sh` ≥ 1.
    - `grep -c "wc -c < \\\"\\\$MAIN\\\"" scripts/check-bundle-size.sh` ≥ 1.
    - `grep -E "BUNDLE CHECK (FAIL|WARN|OK):" scripts/check-bundle-size.sh` returns ≥ 3 (one per state).
    - Running `bash scripts/check-bundle-size.sh` immediately after a fresh `npm run build` exits 0 and prints a `BUNDLE CHECK OK: main.js NNN KB within budget` message where `NNN ≤ 700` (validates A9).
    - Record the observed `NNN` in SUMMARY — this is the empirical A9 datapoint (expected 500–550 KB per RESEARCH §Summary).
    - `ls main.js*.js 2>/dev/null | wc -l` ≤ 1 (A8 re-validation: esbuild still emits a single file after Wave 1).
  </acceptance_criteria>
  <done>
    Standalone gate script exists, is executable, fails above 700 KB, warns above 600 KB, prints OK below both thresholds, and exits 0 against the current Phase 5.3 Wave-1 build.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update scripts/prerelease-check.sh Gate 12 to delegate to scripts/check-bundle-size.sh</name>
  <files>scripts/prerelease-check.sh</files>
  <read_first>
    scripts/prerelease-check.sh,
    scripts/check-bundle-size.sh,
    .planning/phases/05.3-language-aware-editor/05.3-PATTERNS.md
  </read_first>
  <behavior>
    - Existing Gate 12 body (prerelease-check.sh lines 237–253 — header comment + byte-count + old 200 KB LIMIT) is replaced with a delegation block per PATTERNS §"Option A (preferred — single source of truth)" lines 329–337:
      ```bash
      # Gate 12: main.js <= 700 kB (Phase 5.3 D-09 bundle cap; delegated to
      # scripts/check-bundle-size.sh so the ceiling is a single source of truth).
      if ! bash scripts/check-bundle-size.sh; then
        fail "check-bundle-size.sh failed (see output above)"
      fi
      ok "gate 12 — main.js within 700 KB budget"
      ```
    - No other gate (1–11, 13+) is touched — minimum-surface edit (CONTEXT §"What to avoid" — do not modify unrelated phase artifacts).
    - Header comment for Gate 12 updates the stated cap from 200 kB to 700 kB, with an inline reference to Phase 5.3 D-09.
    - After edit, `bash scripts/prerelease-check.sh` runs end-to-end: Gate 12 calls the new script, which exits 0, and the prerelease script continues through its remaining gates without regression.
    - `fail` and `ok` functions from the existing script header are reused — do not redefine.
    - If any other gate in `prerelease-check.sh` was previously passing against the 200 KB constant (e.g., an explicit `npm run build` dependency earlier in the script), verify still passing. The current Phase 5.3 build will exceed 200 KB by design, so the old Gate 12 would start failing without this edit; that fact is the ship-blocker this plan clears.
  </behavior>
  <action>
    Open `scripts/prerelease-check.sh`, locate the Gate 12 block (approx lines 237–253 — the block starting with `# Gate 12: main.js <= 200 kB` and ending before the next gate's comment), and REPLACE the entire block body with the delegation code from PATTERNS §"Option A (preferred — single source of truth)" lines 329–337. Keep the `ok "..."` line so the human-readable gate report continues to show Gate 12 passing. Do NOT touch any other gate (1–11, 13+). Do NOT delete the Gate 12 numbering. Run `bash scripts/prerelease-check.sh` end-to-end to verify all gates pass after the change.
  </action>
  <verify>
    <automated>bash scripts/prerelease-check.sh</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "bash scripts/check-bundle-size.sh" scripts/prerelease-check.sh` returns ≥ 1 (delegation wired).
    - `grep -c "200 \\* 1024" scripts/prerelease-check.sh` returns 0 (old constant removed; single source of truth established).
    - `grep -E "Gate 12" scripts/prerelease-check.sh` returns ≥ 1 (gate numbering preserved).
    - `grep -E "Phase 5\\.3 D-09" scripts/prerelease-check.sh` returns ≥ 1 (attribution in the Gate 12 header comment).
    - Running `bash scripts/prerelease-check.sh` exits 0 end-to-end against the current repo state (all gates pass; Gate 12 delegates successfully to the new script and gets OK).
    - Git diff on `scripts/prerelease-check.sh` touches only the Gate 12 block — no other gate or helper function modified. Verify via `git diff scripts/prerelease-check.sh | grep -E "^(\\+|-)" | wc -l` — expect ≤ 20 lines changed (the Gate 12 block is ~17 lines replaced with ~6 lines).
  </acceptance_criteria>
  <done>
    Gate 12 delegates to `scripts/check-bundle-size.sh`; the 700 KB ceiling is defined in exactly one place; prerelease-check.sh runs end-to-end green against the current Phase 5.3 build.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build output (main.js) → Distribution | Bundle size affects user install download + Obsidian load time; uncapped growth is a UX regression |
| Local shell → CI or human runner | scripts/check-bundle-size.sh runs unattended; must fail loudly on threshold breach |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.3-03-01 | Denial of Service | Bundle grows above 700 KB on a future phase, unnoticed at build time, caught only at store-submission | mitigate | D-15 requires the gate run on every `npm run build` and every live-smoke — not only at release. This plan installs the script and wires prerelease delegation; the per-build invocation will be added in documentation / npm script wrapping as a follow-up if desired (non-blocking). |
| T-05.3-03-02 | Tampering | Contributor bypasses the gate by editing the 700 KB constant | accept | Bash script lives in tree and goes through code review; any ceiling change is reviewable diff against scripts/check-bundle-size.sh. D-09 escalation path (drop rust → re-discuss D-04 → spike splitting) documents when a ceiling bump is legitimate. |
| T-05.3-03-03 | Information Disclosure | Script echoes build paths containing username | N/A | Script echoes only `main.js ${MAIN_KB} KB` — no absolute paths, no secrets, no env vars. Standard shell output. |
| T-05.3-03-04 | Elevation of Privilege | N/A — script is read-only (wc + arithmetic + echo) | N/A | No fs writes, no network, no sudo, no eval. |
| T-05.3-03-05 | Spoofing | N/A | N/A | — |
| T-05.3-03-06 | Repudiation | N/A | N/A | — |
</threat_model>

<verification>
After both tasks:

```bash
# Standalone gate script works
bash scripts/check-bundle-size.sh
echo "standalone exit: $?"

# Prerelease runs green end-to-end
bash scripts/prerelease-check.sh
echo "prerelease exit: $?"

# Delta vs Plan 01 pre-install baseline (A8 / A9 close-out)
BEFORE=$(grep "pre-install" .planning/phases/05.3-language-aware-editor/05.3-01-SUMMARY.md | grep -oE '[0-9]+' | head -1)
AFTER=$(wc -c < main.js | tr -d ' ')
echo "baseline: ${BEFORE} B"
echo "current:  ${AFTER} B"
echo "delta:    $((AFTER - BEFORE)) B"

# A8 re-validation: single main.js
ls main.js*.js 2>/dev/null | wc -l    # expect ≤ 1
```

All must succeed before Plan 04 live-smoke can start.
</verification>

<success_criteria>
1. `scripts/check-bundle-size.sh` exists, is executable, and fails above 700 KB / warns above 600 KB / exits 0 otherwise.
2. `scripts/prerelease-check.sh` Gate 12 delegates to the new script (no duplicate 700 KB constant).
3. `bash scripts/prerelease-check.sh` end-to-end run exits 0.
4. Post-Wave-1 `main.js` is ≤ 700 KB (empirical A9 validation).
5. Post-Wave-1 output is a single `main.js` (no chunk .js files — A8 re-validation).
6. Plan's SUMMARY records the observed `main.js` byte count and the delta vs. Plan 01's pre-install baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-language-aware-editor/05.3-03-SUMMARY.md` recording:
- Post-Wave-1 `main.js` byte count (empirical A9 close-out; compare vs. RESEARCH prediction 500–550 KB)
- Delta vs. Plan 01 pre-install baseline (bytes added by the 6 lang-packs)
- Exit code of `bash scripts/prerelease-check.sh`
- Confirmation `ls main.js*.js | wc -l` ≤ 1 (A8 close-out)
- Whether the 600 KB WARN threshold fired (if yes, note for Plan 04 live-smoke observation)
</output>
