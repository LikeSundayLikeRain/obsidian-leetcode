---
phase: 05.3-language-aware-editor
plan: 07
type: execute
wave: 1
depends_on: [05.3-05, 05.3-06]
files_modified:
  - .planning/phases/05.3-language-aware-editor/05.3-UAT.md
  - .planning/phases/05.3-language-aware-editor/05.3-VERIFICATION.md
autonomous: false
requirements: [POLISH-09]
gap_closure: true
tags: [live-smoke, checkpoint, uat-reverify, gap-closure]
must_haves:
  truths:
    - "After Plan 05's chevron polish, all 7 verifier-prioritized chevron-affected UAT items re-pass in a real Obsidian dev vault (C1–C7 dropdown behavior, D1–D4 atomic switch with synchronous label, F1–F5 Run/Submit unaffected)."
    - "After Plan 06's copy-to-code lang-sync fix, opening a previous submission in a different language and clicking 'Copy to ## Code' updates `lc-language` frontmatter and refreshes the chevron label (G-COPY-TO-CODE-LANG-DRIFT closed end-to-end)."
    - "Bundle-size + automated regression gates still green: `bash scripts/check-bundle-size.sh` exits 0; `npm test --run` reports 0 failures; `node esbuild.config.mjs production` exits 0."
    - "All verifier-actionable gaps from Plan 04's UAT are resolved (C6 Esc, G-LABEL-LAG, G-CLICK-THROUGH, G-CHEVRON-STYLING, G-LAYOUT, G-PYTHON-LABEL, G-COPY-TO-CODE-LANG-DRIFT). G-UNDO-ORDER remains documented as accepted divergence (NOT addressed)."
    - "VERIFICATION.md `status: human_needed` items signed off; UAT.md updated with re-test results; phase ready to mark complete."
  artifacts:
    - path: ".planning/phases/05.3-language-aware-editor/05.3-UAT.md"
      provides: "Re-mark Sections C, D, F items + new Copy-to-Code spot-check + bundle/automated gates with re-test results from this checkpoint."
      contains: "re-test"
    - path: ".planning/phases/05.3-language-aware-editor/05.3-VERIFICATION.md"
      provides: "Move status from human_needed to verified once human approves the re-test outcomes."
      contains: "status: verified"
  key_links:
    - from: "Plan 05 chevron polish"
      to: "Sections C + D re-test items"
      via: "human verifier in dev vault"
      pattern: "C6.*PASS|G-LABEL-LAG.*PASS"
    - from: "Plan 06 copy-to-code lang sync"
      to: "Copy-to-Code spot-check section in this plan"
      via: "human verifier opens a previous submission in a different language and verifies frontmatter + chevron sync"
      pattern: "G-COPY-TO-CODE-LANG-DRIFT.*PASS"
---

<objective>
Re-run the chevron-affected subset of the Phase 5.3 live-smoke UAT after the
polish fixes from Plan 05 (6 chevron gaps) and the copy-to-code sync fix
from Plan 06 (G-COPY-TO-CODE-LANG-DRIFT) ship. This is a `human-verify`
checkpoint: Claude runs the automated pre-flight gates, then hands off to
the human verifier in a real Obsidian dev vault to re-test the specific
sections affected by the gap-closure changes. Sections that were previously
PASS and untouched by Plans 05/06 (Section A native highlighting, Section H
theme parity, Section G network failure) are NOT re-run.

Purpose: produce the "gaps closed, no regressions" sign-off needed to flip
`05.3-VERIFICATION.md` from `status: human_needed` to `status: verified`
and clear the phase for completion.

Output: Updated `05.3-UAT.md` with re-test results recorded next to each
re-tested item; updated `05.3-VERIFICATION.md` with the human's sign-off;
final committed state of the phase.

Skip explicitly: Section A (highlighting per language — chevron polish
didn't touch the lang-markdown nested-parser path), Section H (theme parity
— styling change in Plan 05 was a `.lc-fm__picker`-style port using the
same `var(--background-*)` theme variables, so light/dark inheritance is
unchanged in shape but the light-only spot-check below catches any
hard-coded color regressions), Section G (network failure — copyToCode
runs from already-fetched submission code, no network call introduced).

Skip explicitly: G-UNDO-ORDER (verifier-accepted informational divergence per
UAT.md `## Gaps`; total Cmd-Z step count = 2, end-state correct, the divergence
is order-of-stacks not behavior).
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05.3-language-aware-editor/05.3-CONTEXT.md
@.planning/phases/05.3-language-aware-editor/05.3-UAT.md
@.planning/phases/05.3-language-aware-editor/05.3-VERIFICATION.md
@.planning/phases/05.3-language-aware-editor/05.3-04-SUMMARY.md
@.planning/phases/05.3-language-aware-editor/05.3-05-PLAN.md
@.planning/phases/05.3-language-aware-editor/05.3-06-PLAN.md

<!--
This plan does not edit production source files; it edits planning artifacts
based on a human verifier's observations. The sections below describe exactly
what to re-test in dev-vault and how to record results.
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight automation — confirm Plan 05 + Plan 06 builds green before handoff</name>
  <files>.planning/phases/05.3-language-aware-editor/05.3-UAT.md</files>
  <action>
Before opening Obsidian, run the mechanical gates that the human verifier
should NOT have to babysit. These mirror the PF1–PF5 block at the top of
UAT.md, but capture the post-Plan-05 + post-Plan-06 state.

Run, in order:

1. `node esbuild.config.mjs production`
   Expected: exit 0; emits a single `main.js` (no chunk-split). Record:
   `wc -c < main.js` byte count and the resulting KB rounding.

2. `bash scripts/check-bundle-size.sh`
   Expected: exit 0 with `BUNDLE CHECK OK: main.js {N} KB within budget`.
   Record exact line. Fails this checkpoint if size exceeds 200 KB warn or
   250 KB hard cap.

3. `npm test -- --run`
   Expected: 0 failures; total passing count ≥ 552 (Plan 04 baseline 550
   passing + 2 skipped + Plan 06 added 5 new passing tests = 555 minimum;
   Plan 05 may have added more — total is implementer-supplied at the
   moment of this checkpoint).
   Record: full summary line `Test Files X passed | Y skipped (Z); Tests A passed | B skipped (C); Duration N s`.

4. `npx tsc --noEmit 2>&1 | grep -E '\.ts.*error TS' | wc -l`
   Expected: 24 (DEF-05.3-01-A pre-existing baseline). If number differs,
   investigate before proceeding — Plan 05 or 06 may have introduced new
   errors that need closing.

5. `wc -c < main.js`
   Expected: ≤ 250 KB (256,000 bytes); record exact byte count.

6. `ls main.js*.js | wc -l`
   Expected: 0 (single CJS bundle, no chunks).

7. `for f in $(awk -F\| '/Files Deleted/,/^$/' .planning/phases/05.3-language-aware-editor/05.3-01-SUMMARY.md | grep "tests/main" | awk -F\` '{print $2}'); do test ! -f "$f" || echo "ORPHAN: $f"; done`
   Expected: zero `ORPHAN:` lines (Plan 04 deletion integrity preserved).

8. `grep -RnE "from '@codemirror/lang-(python|java|cpp|javascript|go|rust)'" src/`
   Expected: 0 matches (Plan 04 import-cleanup integrity preserved).

Append a **pre-flight block** to `.planning/phases/05.3-language-aware-editor/05.3-UAT.md`
in a NEW section titled `## Re-test pre-flight (Plan 07 — gap-closure verification)`,
inserted directly after the existing `## Pre-flight (auto-completed by Claude before delegating to human)` block.
Mark each item PASS/FAIL with the exact recorded value. Do NOT modify any
existing PF1–PF5 entries (those record the Plan 04 baseline state).

Format each item exactly as the existing PF1–PF5 entries do — numbered
sub-block with `expected:` and `result:` lines.

If ANY of items 1–8 FAIL: STOP. Do NOT advance to Task 2 (the human
checkpoint). Surface the failure to the user with the specific failing
gate; the user decides whether the fix is in scope for Plan 07 or needs a
new gap-closure plan.
  </action>
  <read_first>
.planning/phases/05.3-language-aware-editor/05.3-UAT.md (the existing
PF1–PF5 block at top — match its exact formatting in the new re-test
pre-flight block)
.planning/phases/05.3-language-aware-editor/05.3-01-SUMMARY.md (the
"Files Deleted" table referenced by item 7)
.planning/phases/05.3-language-aware-editor/05.3-04-SUMMARY.md (the
final ship-gate metrics block — for the expected-baseline values)
  </read_first>
  <verify>
    <automated>bash -c 'set -e; node esbuild.config.mjs production && bash scripts/check-bundle-size.sh && npm test -- --run && [[ $(npx tsc --noEmit 2>&1 | grep -E "\.ts.*error TS" | wc -l | tr -d " ") -eq 24 ]] && [[ $(wc -c < main.js | tr -d " ") -le 256000 ]] && [[ $(ls main.js*.js 2>/dev/null | wc -l | tr -d " ") -eq 0 ]] && [[ $(grep -RnE "from '"'"'@codemirror/lang-(python|java|cpp|javascript|go|rust)'"'"'" src/ | wc -l | tr -d " ") -eq 0 ]] && echo "ALL PRE-FLIGHT GATES GREEN"'</automated>
  </verify>
  <done>
- All 8 pre-flight items recorded in `## Re-test pre-flight (Plan 07 — gap-closure verification)` block of UAT.md, each with PASS/FAIL marker and exact recorded value.
- The verify command exits 0 with `ALL PRE-FLIGHT GATES GREEN` printed.
- If verify exits non-zero: do NOT proceed to Task 2; surface the failure.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-smoke re-verification of chevron polish + copy-to-code sync</name>
  <files>.planning/phases/05.3-language-aware-editor/05.3-UAT.md, .planning/phases/05.3-language-aware-editor/05.3-VERIFICATION.md</files>
  <action>
**What was built (for the verifier's reference):**

Plan 05 closed 6 chevron polish gaps in the editor extension + chevron widget
+ styles.css + languages.ts label table:

- C6 — Esc key now closes the dropdown (document-level keydown handler in
  `languageChevronWidget.ts`).
- G-LABEL-LAG — chevron label refreshes synchronously via the new
  `languageRefreshEffect` triggered by a `metadataCache.on('changed')`
  listener in `codeActionsEditorExtension.ts`.
- G-CLICK-THROUGH — wrapper-level pointerdown stopPropagation so dropdown
  clicks no longer move the editor caret.
- G-CHEVRON-STYLING — `.leetcode-language-chevron` block in `styles.css`
  ported to `.lc-fm__picker`'s input-field look (1px border, 6px radius,
  `var(--background-primary)` background, hover border-color → `var(--text-muted)`).
- G-LAYOUT — widget anchor moved to end of closer-fence line so indent
  guides on the line below no longer shift the chevron horizontally.
- G-PYTHON-LABEL — `LC_LANG_DISPLAY_LABELS['python3']` now renders as
  `'Python 3'` (disambiguating from deprecated Python 2).

Plan 06 closed the MEDIUM-severity G-COPY-TO-CODE-LANG-DRIFT in
`src/graph/copyToCode.ts`:

- After `vault.process` rewrites the fence body for a copied submission,
  `app.fileManager.processFrontMatter` writes the submission's langSlug to
  `lc-language` (mirrors `switchFenceLanguage` Step C). Plan 05's
  metadataCache→languageRefreshEffect listener observes the write and
  refreshes the chevron label automatically.

Both plans landed without modifying:
- `src/main.ts` `switchFenceLanguage` (the chevron-driven path is unchanged
  in shape — Plan 05's StateField effect is dispatched by an Obsidian
  metadataCache event, not a caller-side change)
- `src/main/codeActionsPostProcessor.ts` (Reading-Mode chevron — D-09 still
  locks chevron out of Reading Mode)
- Phase 5.1 Run/Submit button row (regression-watched in Section F)
- Section A native highlighting paths (lang-markdown nested parser
  unchanged; not re-tested)
- Section H theme variables (chevron now uses different background/border
  variables but still pulls from theme — light/dark inheritance preserved
  by construction; one spot-check below catches hard-coded color regressions)

**How to verify (human verifier — execute in dev vault):**

**Setup:**

1. Build the plugin: `node esbuild.config.mjs production`. Confirm `main.js`
   appears in the project root.
2. Copy `main.js` + `manifest.json` + `styles.css` into the dev vault
   plugin folder: `/Users/moxu/Documents/Leetcode/.obsidian/plugins/leetcode/`.
3. Reload Obsidian (Cmd+R or use the hot-reload plugin).
4. Open an existing lc-slug Python note that has a `## Code` fence (e.g.,
   `LeetCode/<problemNumber>-two-sum.md`). Verify chevron is visible LEFT
   of Run+Submit.

**Section C — Dropdown re-test (was 6/7; targeting 7/7 after Plan 05):**

Re-run UAT.md items C1–C7. Update each `result:` line in `05.3-UAT.md` to
PASS/FAIL with a brief observation. Specifically:

- C1: Click chevron — dropdown opens immediately. EXPECT: PASS (was PASS).
- C2: Dropdown lists exactly 8 items in `LC_CHEVRON_LANG_ORDER`.
  EXPECT: PASS, with Python item now labeled `Python 3` (per G-PYTHON-LABEL fix).
- C3: Currently-selected language renders with `.is-current` class —
  accent color + bold. EXPECT: PASS (Plan 05 did not change this rule).
- C4: Click outside dropdown — dropdown closes. EXPECT: PASS.
- C5: Re-click chevron while open — closes (toggle). EXPECT: PASS.
- **C6: Press Esc while dropdown is open — dropdown closes.**
  EXPECT: PASS (was FAIL — fixed by Plan 05 Task 1's document-level keydown
  handler). If still FAIL: regression in Plan 05 — surface to user.
- C7: Stretch full keyboard a11y. EXPECT: stretch — same as before.

**Section D — Atomic switch re-test (was 4/4 PASS; targeting 4/4 with
synchronous label):**

Re-run D1–D4. Specifically:

- **D1: Switch python → java; chevron label updates SYNCHRONOUSLY on first
  click.** EXPECT: PASS with chevron label flipping from `▼ Python 3` to
  `▼ Java` on the SAME click that rewrites the fence (was: label lagged
  one click — fixed by Plan 05 Task 2's `languageRefreshEffect`). If
  label still lags: regression in Plan 05.
- D2: Cmd-Z reverts opener + body atomically. EXPECT: PASS (Plan 05
  did not touch the dispatch shape).
- D3: Second Cmd-Z reverts frontmatter. EXPECT: PASS (with G-UNDO-ORDER
  divergence still present — verifier-accepted, NOT a regression).
- D4: Click the currently-selected language — no-op, no fence rewrite,
  **AND no editor-caret jump**. EXPECT: PASS with `(with click-through bug)`
  removed from the result line — Plan 05 Task 1's wrapper-level
  pointerdown stopPropagation fixes G-CLICK-THROUGH.

**Section F — Run/Submit regression re-test (was 5/5 PASS; targeting 5/5):**

Re-run F1–F5 to confirm Plan 05's `metadataCache` listener registration
and `languageRefreshEffect` dispatch did not regress Phase 5.1's button row:

- F1: Caret stable through fence enter/exit. EXPECT: PASS.
- F2: Run modal opens with current language. EXPECT: PASS.
- F3: Submit modal opens. EXPECT: PASS.
- F4: Source Mode parity. EXPECT: PASS.
- F5: Reading Mode chevron absent (D-09); Run+Submit work. EXPECT: PASS.

**New Spot-Check — G-COPY-TO-CODE-LANG-DRIFT (Plan 06 verification):**

This is NOT in the existing UAT checklist; record results in a NEW section
appended to UAT.md titled `## Re-test — Copy-to-Code lang sync (G-COPY-TO-CODE-LANG-DRIFT, Plan 07)`.
Steps:

1. Open an lc-slug note where you have ≥ 2 past submissions in DIFFERENT
   languages (e.g., one Python and one Java). Note the current `lc-language`
   in frontmatter (e.g., `python3`) and the chevron label (e.g., `▼ Python 3`).
2. Open the submissions list (whatever command surfaces SubmissionPickerModal —
   typically `Submissions: Show recent for active note`).
3. Select a submission whose language differs from the current note's
   language (e.g., the Java submission while the note is on Python). The
   `SubmissionDetailModal` opens.
4. Click `Copy to ## Code`.

EXPECTED RESULTS (record each PASS/FAIL):

- **Sync-1:** Fence body in the note rewrites to the Java submission's code.
  EXPECT: PASS (existing Plan 05.2 silent-copy contract — unchanged by Plan 06).
- **Sync-2:** Fence opener tag becomes ` ```java ` (per `lcSlugToFenceTag('java')`
  identity entry — was already correct via existing `codeBlockFor` path).
  EXPECT: PASS.
- **Sync-3:** Note's frontmatter `lc-language` field updates to `java`.
  EXPECT: PASS (was: stayed `python3` — fixed by Plan 06).
- **Sync-4:** Chevron label re-renders from `▼ Python 3` to `▼ Java` —
  no need to refocus or click elsewhere.
  EXPECT: PASS (rides on Plan 05's `languageRefreshEffect` plumbing
  triggered by Plan 06's frontmatter write).
- **Sync-5:** Click `Run` immediately after copy — RunModal dispatches
  with the JAVA slug (you can confirm this by inspecting the request payload
  in dev tools, OR by clicking Submit and watching the verdict modal report
  Java; the practical proof is the LC API accepts the submission with the
  correct language).
  EXPECT: PASS (was: would have dispatched with stale `python3` slug).
- **Sync-6 (same-slug short-circuit):** Open a second submission whose
  language MATCHES the note's current language; click Copy. Confirm fence
  body rewrites but `lc-language` is unchanged (still the same slug — Plan 06
  short-circuits the frontmatter write when `currentLangSlug === langSlug`).
  EXPECT: PASS — observable via Obsidian's vault undo stack: Cmd-Z once
  reverts the fence body alone (no second Cmd-Z needed because no
  frontmatter write happened).

**Section I subset — Bundle + automated regression sanity (re-run after
real-vault session, in case of test pollution):**

After completing the live-smoke above, return to the project root and re-run:

- I1: `node esbuild.config.mjs production` exits 0.
- I2: `bash scripts/check-bundle-size.sh` exits 0; record `main.js` size.
- I6: `npm test -- --run` reports 0 failures.

These triple-confirm the pre-flight (Task 1) values held through the live
session. (Task 1 ran them before the session; Task 2's I1/I2/I6 confirms
no in-session change snuck in.) Record results next to the existing I1/I2/I6
entries in UAT.md, prefixing each line with `[Plan 07 re-test]`.

**Section A spot-check (NOT exhaustive — only confirms Plan 05's chevron-
button styling change didn't accidentally break native highlighting):**

Open a Python note. Confirm: keywords (`def`, `class`), strings, and `#`
comments still render with colors in Live Preview. EXPECT: PASS.

(One language is sufficient — Plan 05 did not touch fence-tag remap or
the lang-markdown parser path; this is a paranoia spot-check, not a full
re-run of A1–A18.)

**Theme spot-check (light only — Plan 05 G-CHEVRON-STYLING changed the
chevron's color rule from `--background-secondary` to `--background-primary`;
confirms no hard-coded colors leaked):**

In Settings → Appearance, ensure Default light theme is selected. Open a
Python note. Confirm chevron renders with the expected outlined-button
look (1px border, white-ish background per light theme), and the text
inside is readable (normal text color, not muted).

If the theme switch shows the chevron with broken contrast or missing
border in light mode: regression in Plan 05 Task 3 — surface to user.
(Dark mode parity is delegated to Plan 05's Task 3 unit tests + the
inherent CSS variable indirection — not re-verified here.)

**Recording results:**

For each section above, update `.planning/phases/05.3-language-aware-editor/05.3-UAT.md`:

1. Sections C, D, F, A spot-check: re-mark each existing item's `result:`
   line, prefixing the new mark with `[Plan 07 re-test]:` followed by
   PASS/FAIL and a one-line observation. Preserve the original
   `result:` line on the line BELOW so the historical record is not lost
   (e.g., the Plan 04 line documents the original FAIL for C6).
2. Copy-to-Code spot-check: append a NEW section
   `## Re-test — Copy-to-Code lang sync (G-COPY-TO-CODE-LANG-DRIFT, Plan 07)`
   at the bottom of UAT.md (above the existing `## Environment` block) with
   Sync-1..Sync-6 numbered results.
3. After all results land, update the top-of-file `## Summary` block: bump
   `total`, `passed`, `failed`, `pending`, `blocked` accordingly. The new
   verdict line reads `verdict: SHIP-COMPLETE — all chevron-affected gaps closed; G-UNDO-ORDER remains documented as accepted divergence`.
4. Update `.planning/phases/05.3-language-aware-editor/05.3-VERIFICATION.md`
   frontmatter: change `status: human_needed` to `status: verified` and
   add a note in the body referencing this Plan 07 checkpoint as the
   sign-off source. Record the human verifier's name/handle and the
   observation date next to the status flip.

If ANY item in Sections C, D, F, or the new Copy-to-Code spot-check FAILS,
record the failure with reproduction notes and surface to the user — the
expected outcome of this checkpoint is "all chevron-affected items PASS";
a failure indicates a regression in Plan 05 or Plan 06 that needs another
gap-closure plan, not a quiet sign-off.

If the verifier explicitly accepts a remaining divergence as non-blocking
(matching the way G-UNDO-ORDER was originally accepted), record it under a
`## New non-blocking observations (Plan 07)` heading at the bottom of UAT.md
with the same shape as the original `## Gaps` entries.

**Resume signal:** Type `approved` to confirm:

- All re-tested Section C, D, F items PASSED (with the gap fixes observably present).
- The new Copy-to-Code spot-check items Sync-1..Sync-6 all PASSED.
- The Section A spot-check + light-theme spot-check confirm no regression.
- UAT.md is updated with re-test results and the verdict line flipped to SHIP-COMPLETE.
- VERIFICATION.md frontmatter is flipped to `status: verified`.

If anything FAILED, describe the issue inline (item ID + reproduction
notes); the user decides whether to spawn another gap-closure plan or accept.
  </action>
  <read_first>
.planning/phases/05.3-language-aware-editor/05.3-UAT.md (full file — match
existing item format when re-marking; preserve historical Plan 04 result
lines)
.planning/phases/05.3-language-aware-editor/05.3-VERIFICATION.md (full file
— locate the `status: human_needed` line in frontmatter and the body
sections referencing it)
.planning/phases/05.3-language-aware-editor/05.3-05-PLAN.md (Plan 05's
must_haves for the 6 chevron polish gaps — confirms the verifier's mental
model of what's been built)
.planning/phases/05.3-language-aware-editor/05.3-06-PLAN.md (Plan 06's
must_haves for G-COPY-TO-CODE-LANG-DRIFT — confirms the new spot-check
expectations)
  </read_first>
  <verify>
    <automated>bash -c 'set -e; grep -q "Plan 07 re-test" .planning/phases/05.3-language-aware-editor/05.3-UAT.md && grep -q "G-COPY-TO-CODE-LANG-DRIFT, Plan 07" .planning/phases/05.3-language-aware-editor/05.3-UAT.md && grep -q "status: verified" .planning/phases/05.3-language-aware-editor/05.3-VERIFICATION.md && echo "HUMAN VERIFY RECORDED"'</automated>
  </verify>
  <done>
- UAT.md contains at least one `[Plan 07 re-test]` marker on the re-tested items.
- UAT.md contains the new section `## Re-test — Copy-to-Code lang sync (G-COPY-TO-CODE-LANG-DRIFT, Plan 07)` with Sync-1..Sync-6 results.
- UAT.md `## Summary` block updated; `verdict:` line flipped to SHIP-COMPLETE.
- VERIFICATION.md frontmatter flipped from `status: human_needed` to `status: verified`.
- Verifier identity + sign-off date recorded in VERIFICATION.md body.
- Verify command exits 0 with `HUMAN VERIFY RECORDED` printed.
- If any item FAILED: STOP and surface to user; do NOT proceed to Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit the resolved UAT + VERIFICATION sign-off</name>
  <files>.planning/phases/05.3-language-aware-editor/05.3-UAT.md, .planning/phases/05.3-language-aware-editor/05.3-VERIFICATION.md, .planning/phases/05.3-language-aware-editor/05.3-07-SUMMARY.md</files>
  <action>
After Task 2's checkpoint resolves with `approved`, commit the resolved
UAT.md + VERIFICATION.md updates so the phase ships with a clean
verification trail.

Use `gsd-sdk query commit` (or fall back to a plain `git commit -m` if the
SDK helper is not available in the working tree) with this message format:
`docs(05.3-07): close gap-closure live-smoke checkpoint`. Include a body
explaining that Plan 07 re-tested the chevron-affected UAT subset after
Plan 05 (6 chevron polish gaps) and Plan 06 (G-COPY-TO-CODE-LANG-DRIFT)
landed; all re-tested items PASSED; G-UNDO-ORDER remains documented as
accepted divergence; VERIFICATION.md flipped to status: verified.

Files to stage:

- `.planning/phases/05.3-language-aware-editor/05.3-UAT.md`
- `.planning/phases/05.3-language-aware-editor/05.3-VERIFICATION.md`
- `.planning/phases/05.3-language-aware-editor/05.3-07-SUMMARY.md` (created
  in this task — see below)

Do NOT stage source files in this commit — Plan 07 is a verification
checkpoint, not a code change. (Plan 05 and Plan 06 commits already
exist for the source modifications.)

Before committing, write `.planning/phases/05.3-language-aware-editor/05.3-07-SUMMARY.md`
with the standard summary template (mirror Plan 04's SUMMARY shape):

- Frontmatter: plan, phase, status: complete, wave: 1, autonomous: false,
  requirements: [POLISH-09], checkpoint_resolved (timestamp), verifier
  (human), verdict (ship-complete).
- Outcome: gap closure live-smoke verified; phase ready to mark complete in ROADMAP.md.
- Re-test results table (Section + items + PASS/FAIL + observation).
- Pre-flight final metrics (bundle size, test count, tsc baseline) at moment of sign-off.
- Verifier identity + sign-off date.
- Confirmation that no Plan 05/06 source change is needed; the verification
  trail is self-contained in the planning artifacts.
  </action>
  <read_first>
.planning/phases/05.3-language-aware-editor/05.3-UAT.md (the post-Task-2
state — confirm re-test results landed)
.planning/phases/05.3-language-aware-editor/05.3-VERIFICATION.md (confirm
status flipped)
.planning/phases/05.3-language-aware-editor/05.3-04-SUMMARY.md (template
shape for the new SUMMARY.md)
  </read_first>
  <verify>
    <automated>bash -c 'set -e; git log -1 --pretty=%B | grep -q "docs(05.3-07): close gap-closure live-smoke checkpoint" && grep -q "status: verified" .planning/phases/05.3-language-aware-editor/05.3-VERIFICATION.md && test -f .planning/phases/05.3-language-aware-editor/05.3-07-SUMMARY.md && echo "PLAN 07 SIGNED OFF"'</automated>
  </verify>
  <done>
- New commit exists with the message starting `docs(05.3-07): close gap-closure live-smoke checkpoint`.
- `.planning/phases/05.3-language-aware-editor/05.3-VERIFICATION.md` frontmatter has `status: verified` (not `status: human_needed`).
- `.planning/phases/05.3-language-aware-editor/05.3-07-SUMMARY.md` exists with the standard template content.
- The verify command exits 0 with `PLAN 07 SIGNED OFF` printed.
- ROADMAP.md may now flip Phase 5.3 to ✅; that flip is the user's discretion (separate from this plan's scope).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 05/06 implementations → human verifier | Verifier observes real Obsidian behavior in the dev vault and judges PASS/FAIL against the gap descriptions |
| Verifier sign-off → VERIFICATION.md status flip | Status flip from human_needed to verified is the gate that closes the phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.3.07-01 | Spoofing | Sign-off without real verification | mitigate | Task 2 explicitly enumerates 6 chevron items, 5 D-section items, 5 F-section items, 6 Sync items — each requires a concrete observation in the dev vault. The resume-signal includes a checklist that the verifier types "approved" against. |
| T-05.3.07-02 | Tampering | UAT.md re-write loses historical PASS/FAIL state | mitigate | Task 2 explicitly preserves the original `result:` line and prefixes the new line with `[Plan 07 re-test]:` so historical record stays intact for audit. |
| T-05.3.07-03 | Information disclosure | Live-smoke session shows real LC submissions | accept | Verifier uses their own dev-vault submissions; no test data leaves the local machine; `requestUrl` only talks to leetcode.com per CLAUDE.md plugin policy. |
| T-05.3.07-04 | Repudiation | No verifier identity recorded | mitigate | Task 3 SUMMARY explicitly records verifier identity + sign-off date, traceable in git history via the commit author + SUMMARY.md content. |
</threat_model>

<verification>
- Task 1 pre-flight automation passes: 8 mechanical gates green; Plan 04 baseline metrics confirmed unchanged (bundle ≤ 200 KB, tests 552+ passing, tsc errors = 24).
- Task 2 human verification produces a complete re-test record in UAT.md with PASS marks on all chevron-affected items + the new Copy-to-Code spot-check.
- Task 3 commits the resolved UAT + VERIFICATION updates and writes SUMMARY.md.
- VERIFICATION.md frontmatter flips from `status: human_needed` to `status: verified`.
- No source files (`src/**`, `tests/**`, `scripts/**`, `styles.css`) are modified by Plan 07. (If a source change is needed during the live smoke, Plan 07 STOPS and the user spawns another gap-closure plan.)
- The G-UNDO-ORDER divergence remains documented as accepted (NOT addressed in this plan).
</verification>

<success_criteria>
- All 6 chevron polish gaps from Plan 05 (C6, G-LABEL-LAG, G-CLICK-THROUGH, G-CHEVRON-STYLING, G-LAYOUT, G-PYTHON-LABEL) verified PASS in the dev vault.
- G-COPY-TO-CODE-LANG-DRIFT verified PASS via the new Copy-to-Code spot-check (Sync-1..Sync-6).
- No regression in Sections A spot-check (highlighting), F (Phase 5.1 Run/Submit), D atomic-switch shape, light theme.
- Bundle size, test count, tsc baseline unchanged from Plan 04 baseline (or improved).
- UAT.md updated with traceable re-test results; VERIFICATION.md flipped to `status: verified`.
- Commit lands with the documented message; SUMMARY.md captures sign-off.
- Phase 5.3 ready for ROADMAP.md completion flip (user's discretion, separate from this plan).
- G-UNDO-ORDER remains documented as accepted divergence (NOT addressed).
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-language-aware-editor/05.3-07-SUMMARY.md`
documenting:
- Re-test outcome table (Section + item + Plan 04 result + Plan 07 result + observation)
- Final pre-flight metrics at sign-off (bundle size, test count, tsc baseline)
- Verifier identity + sign-off date + Obsidian version + theme(s) used
- VERIFICATION.md status flip confirmation
- Any new non-blocking observations recorded under "## New non-blocking observations (Plan 07)" in UAT.md
- Phase 5.3 readiness statement: "ready for ROADMAP.md completion flip — all blocking and verifier-priority gaps closed"
</output>
