---
phase: 05.3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tests/main/codeFenceLanguageExtension.test.ts
  - tests/main/languagePackRegistry.test.ts
  - tests/main/whitespaceCopyIndent.test.ts
autonomous: true
requirements:
  - POLISH-09
tags:
  - codemirror
  - obsidian-plugin
  - language-pack
  - lazy-import

must_haves:
  truths:
    - "Six @codemirror/lang-* packs are installed as direct dependencies and resolve at import time"
    - "Vitest files exist that express the Wave 1 behavior contract for mapSlugToPack, computeFenceState, getLanguagePack cache + error recovery, and whitespace-copy indent math"
    - "Wave 0 tests fail (RED) for the exact reason that the Wave 1 source modules do not yet exist"
    - "Pre-install baseline main.js size is recorded so Plan 03 can measure Δ against it"
    - "Assumption A1 (Obsidian runtime @codemirror/language major == packs' peer-dep major) is verified by inspecting node_modules/@codemirror/language/package.json"
  artifacts:
    - path: "tests/main/codeFenceLanguageExtension.test.ts"
      provides: "RED contract for mapSlugToPack (10 slugs) + computeFenceState (gate/in-fence/out-of-fence/fallback scenarios) + buildCodeFenceLanguageExtension public API"
      min_lines: 180
    - path: "tests/main/languagePackRegistry.test.ts"
      provides: "RED contract for getLanguagePack cache-hit per-PackId (7) + drops-failed-promise-on-error retry + typescript passes { typescript: true }"
      min_lines: 90
    - path: "tests/main/whitespaceCopyIndent.test.ts"
      provides: "RED contract for computeWhitespaceCopyIndent (first-line, skip-empty-lines, 4-space, 2-tab, mixed, all-empty cases)"
      min_lines: 60
    - path: "package.json"
      provides: "Direct dependencies for 6 @codemirror/lang-* packs at the exact versions verified in RESEARCH §Standard Stack"
      contains: "@codemirror/lang-python"
  key_links:
    - from: "tests/main/codeFenceLanguageExtension.test.ts"
      to: "src/main/codeFenceLanguageExtension.ts"
      via: "import { mapSlugToPack, computeFenceState, buildCodeFenceLanguageExtension }"
      pattern: "from '\\.\\./\\.\\./src/main/codeFenceLanguageExtension'"
    - from: "tests/main/languagePackRegistry.test.ts"
      to: "src/main/languagePackRegistry.ts"
      via: "import { getLanguagePack, slugToPackId }"
      pattern: "from '\\.\\./\\.\\./src/main/languagePackRegistry'"
    - from: "tests/main/whitespaceCopyIndent.test.ts"
      to: "src/main/whitespaceCopyIndent.ts"
      via: "import { computeWhitespaceCopyIndent, whitespaceCopyIndentExtension }"
      pattern: "from '\\.\\./\\.\\./src/main/whitespaceCopyIndent'"
---

<objective>
Land Wave 0 for Phase 5.3 per CONTEXT §"Established Patterns" + RESEARCH §"Wave 0 Gaps": install the six @codemirror/lang-* packs as direct dependencies, author three RED-state vitest files that express the behavior contract for every Wave 1 export, verify assumption A1 (Obsidian's runtime @codemirror/language peer-dep major matches the packs' declared peer major), and capture a pre-install baseline main.js size to enable Plan 03's delta measurement.

Purpose: Wave 1 (Plan 02) cannot ship without tests that fail for the right reason, and cannot install new dependencies safely without the A1 peer-dep verification. This plan de-risks the three empirical assumptions A1 (peer-dep major), A8 (esbuild inline-bundle — Plan 03 completes), and A9 (final bundle size — Plans 01 and 03 split).

Output: 3 new test files (all RED), 6 dependency entries in package.json, package-lock.json updated, and a recorded baseline main.js byte count in this plan's SUMMARY.
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.3-language-aware-editor/05.3-CONTEXT.md
@.planning/phases/05.3-language-aware-editor/05.3-RESEARCH.md
@.planning/phases/05.3-language-aware-editor/05.3-PATTERNS.md
@.planning/phases/05.3-language-aware-editor/05.3-VALIDATION.md
@src/main/codeActionsEditorExtension.ts
@src/main/python3Highlighter.ts
@src/solve/languages.ts
@tests/main/codeActionsEditorExtension.test.ts
@tests/main/python3Highlighter.test.ts
@tests/helpers/obsidian-stub.ts

<interfaces>
<!-- Extracted from codebase. Executor uses these directly; no exploration required. -->

From src/main/codeActionsEditorExtension.ts (REUSE in Wave 1; import in test fixtures via tests/helpers/obsidian-stub to match):
```typescript
export class CodeActionsWidget extends WidgetType { /* ... */ }
export function findCodeFence(state: EditorState): { openerLine: number; closerLine: number } | null;
export function buildDecorations(state: EditorState, plugin: Plugin): DecorationSet;
export function buildCodeActionsEditorExtension(plugin: Plugin): Extension;
```

From src/solve/languages.ts (REUSE for slug normalization):
```typescript
export const LC_LANG_SLUGS: ReadonlySet<string>;
export const FENCE_TAG_ALIASES: Readonly<Record<string, string>>;
export function resolveLangSlug(tag: string | null, fallback: string): string;
```

From tests/helpers/obsidian-stub.ts (ALREADY PROVIDES CM6 stubs — Phase 5.1):
```typescript
export const editorInfoField: StateField<{ file: { path: string } | null }>;
export const editorLivePreviewField: StateField<boolean>;
```
(Lines 129–134 confirmed. No stub additions needed.)

From tests/solve/mocks/fakeWorkspace.ts (REUSE for createFakePlugin + createFakeMetadataCache — existing Phase 5.1/5.2 fixture):
```typescript
export function createFakePlugin(opts?: { metadataCache?: unknown; settings?: unknown }): Plugin;
export function createFakeMetadataCache(): { setFrontmatter(path: string, fm: Record<string, unknown>): void; /* Obsidian-shaped methods */ };
```
</interfaces>

<package_versions>
<!-- VERIFIED via npm registry 2026-05-11 per RESEARCH §Standard Stack — Installation. -->
@codemirror/lang-python      6.2.1
@codemirror/lang-java        6.0.2
@codemirror/lang-cpp         6.0.3
@codemirror/lang-javascript  6.2.5
@codemirror/lang-go          6.0.1
@codemirror/lang-rust        6.0.2
</package_versions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install the 6 @codemirror/lang-* packs + verify peer-dep assumption A1 + capture baseline main.js size</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    package.json,
    package-lock.json,
    esbuild.config.mjs,
    .planning/phases/05.3-language-aware-editor/05.3-RESEARCH.md,
    .planning/phases/05.3-language-aware-editor/05.3-CONTEXT.md,
    node_modules/@codemirror/language/package.json,
    node_modules/@codemirror/autocomplete/package.json
  </read_first>
  <behavior>
    - After install, `npm ls @codemirror/lang-python @codemirror/lang-java @codemirror/lang-cpp @codemirror/lang-javascript @codemirror/lang-go @codemirror/lang-rust` resolves each at the verified version with no peer-dep WARN that blocks resolution.
    - `node_modules/@codemirror/language/package.json` reports a major version that satisfies each lang-pack's declared `peerDependencies["@codemirror/language"]` range — the load-bearing check for assumption A1 from RESEARCH (Pitfall 6).
    - `node_modules/@codemirror/autocomplete/package.json` reports a major version that satisfies `@codemirror/lang-javascript` and `@codemirror/lang-python` peer-dep ranges.
    - A pre-install baseline of `main.js` bytes is captured and recorded in the plan SUMMARY (used by Plan 03 to compute Δ and validate A9).
    - `npm run build` still succeeds after install with no new esbuild errors, warnings about missing externals, or unresolved `@codemirror/lang-*` imports (none imported in src/ yet).
    - None of the 6 lang-* packs appear in esbuild.config.mjs `external` array — they MUST bundle per D-08. Verified by re-reading esbuild.config.mjs after install; no edits to that file in this task.
  </behavior>
  <action>
    1. Before any install, run `npm run build` and capture the byte count of `main.js` (`wc -c < main.js`). Record this as the "pre-Phase-5.3 baseline" in the Task-1 section of this plan's SUMMARY. This number is the input Plan 03 uses to validate assumption A9 (RESEARCH §Summary: expected landed size 500–550 KB).
    2. Install the 6 lang-* packs as direct dependencies, pinning the exact versions from RESEARCH §Standard Stack:
       `npm install --save @codemirror/lang-python@6.2.1 @codemirror/lang-java@6.0.2 @codemirror/lang-cpp@6.0.3 @codemirror/lang-javascript@6.2.5 @codemirror/lang-go@6.0.1 @codemirror/lang-rust@6.0.2`.
       Do NOT use version ranges like `^6.2.1` — pin exact per version-pinning hygiene.
    3. Verify assumption A1 (RESEARCH Pitfall 6). Read `node_modules/@codemirror/language/package.json` and `node_modules/@codemirror/autocomplete/package.json` and confirm their `version` fields. Then read each of the 6 `node_modules/@codemirror/lang-*/package.json` and confirm their `peerDependencies["@codemirror/language"]` and `peerDependencies["@codemirror/autocomplete"]` (where declared) ranges are satisfied by the resolved `@codemirror/language` and `@codemirror/autocomplete` majors. Record the observed majors + highest demanded peer-range in the SUMMARY. If the observed major is outside the peer range, STOP and escalate — do not proceed to subsequent tasks; Plan 02 cannot ship.
    4. Re-read `esbuild.config.mjs` and confirm the `external` array contains: `@codemirror/autocomplete`, `@codemirror/language`, `@codemirror/state`, `@codemirror/view`, `@codemirror/commands`, `@codemirror/lint`, `@lezer/common`, `@lezer/highlight`, `@lezer/lr` — and does NOT contain any of the 6 `@codemirror/lang-*` packs just installed. Do NOT modify esbuild.config.mjs in this task. If any of the 6 lang-* packs accidentally appear in `external`, remove them (that edit is the only exception to the no-modify rule; record in SUMMARY).
    5. Run `npm run build` again and re-capture the `main.js` byte count. Record both the before and after numbers in the SUMMARY. The post-install build should succeed even though no src/ code imports the lang-packs yet (they are tree-shaken away in this state); the after number may equal the before number within a small delta. This establishes A9's measurement protocol for Plan 03.
    6. Do NOT add any src/ imports of the new packs in this task — Wave 1 (Plan 02) owns import wiring. This task is install + verify + baseline only.
  </action>
  <verify>
    <automated>npm ls @codemirror/lang-python @codemirror/lang-java @codemirror/lang-cpp @codemirror/lang-javascript @codemirror/lang-go @codemirror/lang-rust &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls @codemirror/lang-python` reports `@codemirror/lang-python@6.2.1` (exact) with exit 0.
    - Repeat for the other 5 packs at their pinned versions. All resolve; none appear under "extraneous" or "UNMET PEER DEPENDENCY".
    - `node_modules/@codemirror/language/package.json` exists and its `version` satisfies `^6.6.0` (the highest peer demanded across the 6 packs — verify actual ranges during execution and update this criterion with the observed highest).
    - `node_modules/@codemirror/autocomplete/package.json` exists and its `version` satisfies `^6.8.0` (the highest peer demanded across the 6 packs — verify and update).
    - `grep -E '@codemirror/lang-(python|java|cpp|javascript|go|rust)' esbuild.config.mjs` returns zero matches (the 6 packs are NOT externalised).
    - `npm run build` after install exits 0 with no errors.
    - SUMMARY.md Task-1 section records: pre-install `main.js` bytes, post-install `main.js` bytes, observed `@codemirror/language` version, observed `@codemirror/autocomplete` version, and the highest peer-range demanded by the 6 packs for each.
    - No changes to `src/` or `tests/` files in this task.
  </acceptance_criteria>
  <done>
    6 lang-* packs pinned in package.json; package-lock.json updated; baseline main.js size recorded; A1 peer-dep assumption verified with concrete version numbers in SUMMARY; esbuild external array verified unchanged; `npm run build` green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write RED-state vitest file for codeFenceLanguageExtension (mapSlugToPack + computeFenceState + public-API sanity)</name>
  <files>tests/main/codeFenceLanguageExtension.test.ts</files>
  <read_first>
    tests/main/codeActionsEditorExtension.test.ts,
    tests/main/python3Highlighter.test.ts,
    tests/helpers/obsidian-stub.ts,
    tests/solve/mocks/fakeWorkspace.ts,
    src/main/codeActionsEditorExtension.ts,
    src/solve/languages.ts,
    .planning/phases/05.3-language-aware-editor/05.3-RESEARCH.md,
    .planning/phases/05.3-language-aware-editor/05.3-PATTERNS.md
  </read_first>
  <behavior>
    - File starts with the same header shape as `tests/main/codeActionsEditorExtension.test.ts` — identifies the phase (05.3 / POLISH-09), flags Wave 0 RED status, and disables the standard @typescript-eslint unsafe-call block for the scaffold duration.
    - Imports `vi.mock('obsidian', async () => await import('../helpers/obsidian-stub'))` before importing any SUT symbol (parity with Phase 5.1 analog, lines 17–27).
    - Imports `createFakePlugin` + `createFakeMetadataCache` from `tests/solve/mocks/fakeWorkspace`.
    - Attempts to import `mapSlugToPack`, `computeFenceState`, `buildCodeFenceLanguageExtension` from `../../src/main/codeFenceLanguageExtension` — this import MUST fail at Wave 0 time because the SUT file does not yet exist. That failure is the RED state.
    - Declares a describe/it tree with 5 groups matching RESEARCH §Unit Test Shape (lines 898–929 + PATTERNS §Test structure):
      Group 1 `describe('mapSlugToPack', ...)` with exactly 10 `it` cases: `python → python`, `python3 → python`, `java → java`, `cpp → cpp`, `c → cpp`, `javascript → javascript`, `typescript → typescript`, `golang → go`, `rust → rust`, `csharp → fallback`, `ruby → fallback`, `mysql → fallback`, `empty-string → fallback`. (Match RESEARCH lines 898–929 verbatim, expand to cover every D-05 slug + a few fallback representatives; final count >= 10.)
      Group 2 `describe('computeFenceState — lc-slug gate', ...)` — file with no frontmatter returns EMPTY; file with lc-slug empty string returns EMPTY; file with lc-slug present but no `## Code` fence returns EMPTY. 3 cases minimum.
      Group 3 `describe('computeFenceState — caret-in-fence', ...)` — caret on opener line returns caretInCodeFence:false; caret on closer line returns false; caret strictly between returns true; caret above opener returns false; caret below closer returns false. 5 cases.
      Group 4 `describe('computeFenceState — language routing', ...)` — python3 fence routes to desiredPack:'python'; c fence routes to 'cpp'; golang fence routes to 'go'; csharp fence routes to 'fallback'. 4 cases.
      Group 5 `describe('buildCodeFenceLanguageExtension — public API exists', ...)` — single `it` that asserts `typeof buildCodeFenceLanguageExtension === 'function'` (sanity-check; mirrors analog lines 304–313).
    - Includes a `PYTHON_NOTE` + `JAVA_NOTE` + `CSHARP_NOTE` fixture constant at top of file (verbatim shape from PATTERNS §Full-note fixture, lines 411–427).
    - State-builder helpers `makeState(body: string)` and `makeStateWithFile(body, { path, frontmatter })` copied verbatim from analog (lines 64–134). These stub `state.field(editorInfoField)` to return `{ file: { path } }` and `state.field(editorLivePreviewField)` to return `true`, satisfying the gate code without real CM6 internals.
    - All 20+ it-blocks should fail at run time with a module-resolution error ("Cannot find module '../../src/main/codeFenceLanguageExtension'") — this is the expected Wave 0 RED signal. Do NOT skip or .todo these tests; they must actively fail.
  </behavior>
  <action>
    Create `tests/main/codeFenceLanguageExtension.test.ts` following PATTERNS §"tests/main/codeFenceLanguageExtension.test.ts (NEW)" instructions verbatim. Copy header + import pattern + state-builder helpers from `tests/main/codeActionsEditorExtension.test.ts`; do not invent new test infrastructure. Replicate analog's describe-block organization, substituting 5.3's load-bearing behaviors. Use only `vi` + `describe` + `it` + `expect` from vitest; no integration helpers. Fixture notes must include `---\nlc-slug: two-sum\n---\n\n## Problem\n...\n\n## Code\n\n\`\`\`python3\n...\n\`\`\`\n` frame from PATTERNS §Full-note fixture. All Group-2 and Group-3 tests must use `makeStateWithFile` so the frontmatter gate is exercised. All `it.skip` / `it.todo` usage is forbidden; every test must execute and fail for the Wave-0-correct reason. Do NOT create the SUT file in this task — that is Plan 02's job.
  </action>
  <verify>
    <automated>npm test -- --run tests/main/codeFenceLanguageExtension.test.ts 2>&amp;1 | tee /tmp/gsd-5.3-01-t2.log; grep -E "Cannot find module.*codeFenceLanguageExtension" /tmp/gsd-5.3-01-t2.log</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `tests/main/codeFenceLanguageExtension.test.ts` and is ≥ 180 lines.
    - `grep -c "^  it(" tests/main/codeFenceLanguageExtension.test.ts` reports ≥ 20 (10 mapSlugToPack + 3 gate + 5 caret-position + 4 routing + 1 public-API = 23 minimum; exact count may exceed).
    - `grep -c "describe(" tests/main/codeFenceLanguageExtension.test.ts` reports ≥ 5.
    - `grep -E "^import.*\\.\\./\\.\\./src/main/codeFenceLanguageExtension'" tests/main/codeFenceLanguageExtension.test.ts` returns exactly one match (Wave 0 RED anchor).
    - `grep -c "\\.skip\\|\\.todo" tests/main/codeFenceLanguageExtension.test.ts` returns 0 (no skipped tests).
    - `grep -c "vi.mock('obsidian'" tests/main/codeFenceLanguageExtension.test.ts` returns ≥ 1.
    - Running `npm test -- --run tests/main/codeFenceLanguageExtension.test.ts` fails with an error mentioning `codeFenceLanguageExtension` module resolution — the load-bearing Wave-0 RED signal.
    - The Phase 5.1 regression file `tests/main/codeActionsEditorExtension.test.ts` continues to pass (no edits, no cross-file fallout).
  </acceptance_criteria>
  <done>
    RED-state vitest file in place for the 5.3 extension module's full behavioral contract; `npm test -- --run tests/main/codeFenceLanguageExtension.test.ts` fails with module-resolution against the not-yet-existent SUT; Phase 5.1 analog tests still green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write RED-state vitest files for languagePackRegistry + whitespaceCopyIndent</name>
  <files>tests/main/languagePackRegistry.test.ts, tests/main/whitespaceCopyIndent.test.ts</files>
  <read_first>
    tests/main/python3Highlighter.test.ts,
    tests/solve/languages.test.ts,
    tests/helpers/obsidian-stub.ts,
    .planning/phases/05.3-language-aware-editor/05.3-RESEARCH.md,
    .planning/phases/05.3-language-aware-editor/05.3-PATTERNS.md
  </read_first>
  <behavior>
    languagePackRegistry.test.ts (≥ 90 lines):
    - `beforeEach(() => { vi.resetModules(); })` to reset the module-level cache in the SUT between tests (RESEARCH lines 939–941).
    - `describe('getLanguagePack — cache', ...)` with 3 cases: (a) `python` returns same Promise on second call; the underlying factory `python()` invoked once; (b) `typescript` calls `javascript({ typescript: true })` (pass-through arg verification per RESEARCH lines 799 + PATTERNS lines 466–474); (c) `java`, `cpp`, `javascript`, `go`, `rust` each resolve via their matching factory exactly once when called twice — one per-PackId cache-hit assertion.
    - `describe('getLanguagePack — error recovery', ...)` with 1 case: first import throws → `getLanguagePack('python')` rejects; second call retries and succeeds (cache.delete on error verified via the module's post-error second call succeeding; per RESEARCH lines 957–971 + PATTERNS §"Error-recovery test").
    - `describe('slugToPackId', ...)` with at least 10 cases mirroring the mapSlugToPack table but verifying the pure slug→PackId-or-null mapping (one case per pack + a few null returns for unknown / csharp / empty string).
    - Each `vi.doMock('@codemirror/lang-XXX', ...)` call must be set up BEFORE the dynamic `import()` of the SUT per vitest ordering rules.
    - All imports of the SUT are dynamic via `await import('../../src/main/languagePackRegistry')` AFTER the vi.doMock calls — matches the RESEARCH canonical test shape lines 937–972.

    whitespaceCopyIndent.test.ts (≥ 60 lines):
    - `describe('computeWhitespaceCopyIndent', ...)` with 6 cases per RESEARCH §Wave 0 Gaps:
      (a) first line (line.number === 1) → returns 0;
      (b) previous non-empty line has 4 leading spaces → returns 4;
      (c) previous non-empty line has 2 leading tabs → returns 4 (tab-to-2-space convention per PATTERNS line 234);
      (d) mixed `\t  ` (1 tab + 2 spaces) → returns 4;
      (e) previous lines are all empty → returns 0;
      (f) skips empty intermediate line to find the most-recent non-empty line's leading whitespace.
    - Import is dynamic via `await import('../../src/main/whitespaceCopyIndent')` — SUT does not exist yet, so tests fail with module-resolution (RED).
    - Tests call `computeWhitespaceCopyIndent(fakeState, pos)` with a minimal fake state shape `{ doc: { lineAt(p): { number }; line(n): { text } } }` — matching the PATTERNS §Option A signature (the SUT will export this pure function alongside the indentService-wrapped extension).
  </behavior>
  <action>
    Create the two test files using PATTERNS §"tests/main/languagePackRegistry.test.ts (NEW)" and §"tests/main/whitespaceCopyIndent.test.ts (NEW)" as verbatim templates. Test bodies for the languagePackRegistry file come from RESEARCH §"Unit test shape — tests/main/languagePackRegistry.test.ts" (lines 937–972) plus the per-PackId expansion to cover all 7 packs. Test bodies for the whitespaceCopyIndent file come from RESEARCH §Wave 0 Gaps enumeration + PATTERNS §"Test cases" (lines 511–519) enumerating 6 cases. Do NOT use `.skip` / `.todo`; every test must actively fail at Wave 0 due to missing SUT. Do NOT create the SUT files — Plan 02 owns them.
  </action>
  <verify>
    <automated>npm test -- --run tests/main/languagePackRegistry.test.ts tests/main/whitespaceCopyIndent.test.ts 2>&amp;1 | tee /tmp/gsd-5.3-01-t3.log; grep -E "Cannot find module.*(languagePackRegistry|whitespaceCopyIndent)" /tmp/gsd-5.3-01-t3.log</automated>
  </verify>
  <acceptance_criteria>
    - `tests/main/languagePackRegistry.test.ts` exists and is ≥ 90 lines.
    - `grep -c "^  it(" tests/main/languagePackRegistry.test.ts` reports ≥ 12 (cache hits per 7 packs + error recovery + at least 4 slugToPackId cases → ≥ 12).
    - `grep -E "vi\\.doMock\\('@codemirror/lang-(python|java|cpp|javascript|go|rust)'" tests/main/languagePackRegistry.test.ts` returns ≥ 6 matches (one per pack).
    - `grep "beforeEach" tests/main/languagePackRegistry.test.ts` contains `vi.resetModules`.
    - `grep -E "javascript\\(\\{ typescript: true \\}\\)" tests/main/languagePackRegistry.test.ts` returns ≥ 1 (typescript pass-through assertion).
    - `tests/main/whitespaceCopyIndent.test.ts` exists and is ≥ 60 lines.
    - `grep -c "^  it(" tests/main/whitespaceCopyIndent.test.ts` reports ≥ 6.
    - `grep -c "\\.skip\\|\\.todo" tests/main/whitespaceCopyIndent.test.ts tests/main/languagePackRegistry.test.ts` returns 0.
    - `npm test -- --run tests/main/languagePackRegistry.test.ts tests/main/whitespaceCopyIndent.test.ts` fails with module-resolution errors naming both `languagePackRegistry` and `whitespaceCopyIndent`.
    - The full suite (`npm test -- --run`) regression check: all Phase 5.1 / 5.2 tests still green (no test modifications outside the 3 new files).
  </acceptance_criteria>
  <done>
    Two RED-state test files written; `npm test -- --run tests/main/languagePackRegistry.test.ts tests/main/whitespaceCopyIndent.test.ts` fails for the load-bearing reason (missing SUT modules); no regression in the existing test suite.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | 6 new @codemirror/lang-* packages installed; supply-chain surface |
| Plugin bundle → Obsidian runtime | @codemirror/language / @codemirror/autocomplete resolved from Obsidian's bundle at runtime (externals); peer-dep mismatch would break import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.3-01-01 | Tampering (supply chain) | 6 @codemirror/lang-* npm installs | accept | All 6 packs are from the official `@codemirror` npm scope — the same scope as `@codemirror/state` and `@codemirror/view` already shipped by Obsidian. Maintained by Marijn Haverbeke (CM6 core author). Pinned to exact versions (no `^` ranges) to prevent silent upgrade. `package-lock.json` locks the transitive @lezer/* grammar tables. No dynamic runtime install path is created (D-07/D-08 lock inline bundling). |
| T-05.3-01-02 | Denial of Service | Bundle size regression from 6 new deps | mitigate | Task 1 records pre-install `main.js` baseline so Plan 03's gate can measure Δ against it and fail the build if the cumulative bundle exceeds 700 KB. A9 assumption (500–550 KB landed) is empirically checked in Plan 03. |
| T-05.3-01-03 | Information Disclosure | Test files leak internal paths / secrets | N/A | Test files use only in-repo fixture constants (`PYTHON_NOTE`, `JAVA_NOTE`, `CSHARP_NOTE`); no env vars, no secrets, no network calls |
| T-05.3-01-04 | Elevation of Privilege | N/A — no auth, no network, no permission surface | N/A | Phase 5.3 is a pure editor-extension surface (CF-06); Wave 0 is test + install only |
| T-05.3-01-05 | Spoofing | N/A — no identity claims | N/A | — |
| T-05.3-01-06 | Repudiation | N/A — no audit-logged user action | N/A | — |
</threat_model>

<verification>
After all three tasks:

```bash
# Dependencies resolve
npm ls @codemirror/lang-python @codemirror/lang-java @codemirror/lang-cpp @codemirror/lang-javascript @codemirror/lang-go @codemirror/lang-rust

# Build still green post-install
npm run build

# Wave 0 RED signal across all 3 new test files
npm test -- --run tests/main/codeFenceLanguageExtension.test.ts tests/main/languagePackRegistry.test.ts tests/main/whitespaceCopyIndent.test.ts 2>&1 | grep -E "Cannot find module.*(codeFenceLanguageExtension|languagePackRegistry|whitespaceCopyIndent)"

# Regression: Phase 5.1/5.2 tests still green
npm test -- --run tests/main/codeActionsEditorExtension.test.ts tests/main/python3Highlighter.test.ts tests/main/codeActionsPostProcessor.test.ts tests/main/fileOpenRetrofit.test.ts tests/main/resetCommand.test.ts
```

All must succeed in order.
</verification>

<success_criteria>
1. `package.json` declares all 6 `@codemirror/lang-*` packs at the exact pinned versions.
2. `node_modules/@codemirror/language/package.json` + `node_modules/@codemirror/autocomplete/package.json` major versions satisfy every pack's peer-dep range (assumption A1 verified in SUMMARY with concrete version numbers).
3. `esbuild.config.mjs` unchanged; 6 lang-* packs NOT in externals.
4. Pre-install + post-install `main.js` byte count recorded in SUMMARY.
5. 3 new test files in `tests/main/` — all RED, all failing for missing-SUT reason, no `.skip` / `.todo`.
6. 5.1 / 5.2 regression test files unchanged and still green.
7. `npm run build` exits 0 after install.
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-language-aware-editor/05.3-01-SUMMARY.md` recording:
- Pre-install and post-install `main.js` byte counts (feeds Plan 03 Δ + A9 check)
- Observed `@codemirror/language` version and the highest peer-range demanded across the 6 packs
- Observed `@codemirror/autocomplete` version and the highest peer-range demanded
- Line-count of each new test file
- Confirmation that `npm test` full-suite run is green except for the 3 new RED files (which fail with module-not-found as intended)
</output>
