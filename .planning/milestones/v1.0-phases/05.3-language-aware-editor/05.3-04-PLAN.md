---
phase: 05.3-language-aware-editor
plan: 04
type: execute
wave: 3
depends_on: [05.3-01, 05.3-02, 05.3-03]
files_modified:
  - scripts/check-bundle-size.sh
  - scripts/prerelease-check.sh
  - .planning/phases/05.3-language-aware-editor/05.3-UAT.md
autonomous: false
requirements: [POLISH-09]
must_haves:
  truths:
    - "Human verified all 8 supported LC languages render with native markdown highlighting in Edit Mode (Live Preview + Source) on lc-slug notes"
    - "Human verified the chevron renders LEFT of Run/Submit on every lc-slug note with a ## Code fence in Edit Mode"
    - "Human verified the chevron does NOT render in Reading Mode (D-09)"
    - "Human verified clicking a different language destructively replaces the fence body with LC starter code AND rewrites the fence opener tag AND writes lc-language frontmatter"
    - "Human verified Cmd-Z reverts the fence (opener + body) atomically as ONE undo step (frontmatter undo is a separate vault-undo step — Pitfall 1 accepted)"
    - "Human verified clicking the currently-selected language is a no-op (no fetch, no Notice)"
    - "Human verified network failure shows the locked Notice copy 'Couldn't fetch starter code for {Label}.' and leaves the fence body unchanged"
    - "Human verified Phase 5.1 Run + Submit work end-to-end in Edit + Reading Mode after the chevron ships"
    - "Human verified Phase 5.2 python3 to python Prism alias still works in Reading Mode for legacy notes"
    - "Human verified light + dark theme parity (no plugin-authored color overrides; Obsidian theme variables drive everything)"
    - "scripts/check-bundle-size.sh tightened to 250 KB hard / 200 KB warn (or planner-justified alternative; D-13 PRESERVES the script's existence regardless)"
    - "scripts/prerelease-check.sh Gate 12 docstring/legend updated to reflect the new cap"
    - "Final main.js byte count recorded; bundle no greater than the new cap"
  artifacts:
    - path: ".planning/phases/05.3-language-aware-editor/05.3-UAT.md"
      provides: "REWRITTEN with the new live-smoke checklist (chevron + remap, NOT the old Compartment-swap items); all results filled in by human verifier"
      contains: "Section A native highlighting; Section B chevron rendering; Section C dropdown; Section D destructive switch; Section E frontmatter; Section F Run/Submit regression; Section G network failure; Section H theme parity; Section I bundle + automated regression"
    - path: "scripts/check-bundle-size.sh"
      provides: "Tightened cap (recommended 250 KB hard / 200 KB warn); single script remains the authoritative bundle gate"
      contains: "LIMIT_BYTES=$((250 * 1024)); WARN_BYTES=$((200 * 1024))"
    - path: "scripts/prerelease-check.sh"
      provides: "Gate 12 docstring + legend updated to reference the new cap; delegation to check-bundle-size.sh unchanged"
      contains: "<= 250 kB"
  key_links:
    - from: ".planning/phases/05.3-language-aware-editor/05.3-UAT.md"
      to: "live dev vault smoke run"
      via: "human verifier executes the checklist; records pass/fail per item"
      pattern: "result: PASS|FAIL"
    - from: "scripts/prerelease-check.sh Gate 12"
      to: "scripts/check-bundle-size.sh"
      via: "delegation (no logic duplication)"
      pattern: "bash scripts/check-bundle-size\\.sh"
---

<objective>
Live-smoke verify Phase 5.3's chevron + write-time fence-tag remap implementation against a real Obsidian dev vault, then tighten the ship-gate bundle cap to a value sized for the post-revert reality (~155 KB) rather than the failed-implementation artifact (520 KB). This wave is autonomous: false — a human verifier exercises the checklist, records pass/fail, and signs off OR routes any gaps to a follow-up gap-closure plan.

Purpose: D-14 was the locked human-verified checkpoint that surfaced the failed Compartment-swap approach in the original Phase 5.3. Its successor (this plan) is non-negotiable — RESEARCH §Q11 Bucket 4 explicitly notes the chevron+remap approach has the same testability constraint: unit tests prove `lcSlugToFenceTag('python3')` returns `'python'`, but they CANNOT prove "Obsidian's markdown nested parser actually renders Python keywords as colored." That assertion is irreducibly empirical. Live smoke is the only proof.

Output:
1. `.planning/phases/05.3-language-aware-editor/05.3-UAT.md` rewritten as the chevron+remap live-smoke matrix (the existing UAT.md is the failed-implementation checklist; this plan replaces it).
2. `scripts/check-bundle-size.sh` cap tightening — recommend 250 KB hard / 200 KB warn per RESEARCH §Q7. PRESERVE the script's existence (D-13).
3. `scripts/prerelease-check.sh` Gate 12 docstring + legend updated; delegation logic unchanged (the check script remains the single source of truth).
4. Human-verifier sign-off captured in UAT.md results table — total / passed / failed / blocked tallies + any gap items routed to a follow-up Plan 05 only if needed.
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.3-language-aware-editor/05.3-CONTEXT.md
@.planning/phases/05.3-language-aware-editor/05.3-UI-SPEC.md
@.planning/phases/05.3-language-aware-editor/05.3-RESEARCH.md
@.planning/phases/05.3-language-aware-editor/05.3-VALIDATION.md
@.planning/phases/05.3-language-aware-editor/05.3-UAT.md
@CLAUDE.md
@scripts/check-bundle-size.sh
@scripts/prerelease-check.sh
</context>

<truths>
Locked CONTEXT decisions this plan implements:
- **D-14 (successor):** Human-verified live smoke is mandatory. The failed implementation passed all 53 unit tests + dispatch logging but failed the empirical "does highlighting actually appear?" check. The new approach has the same constraint — markdown nested parser behavior is not testable in vitest.
- **D-13:** PRESERVE `scripts/check-bundle-size.sh`. This plan tightens the cap inside the script but does not delete the script.
- **D-04:** Live-smoke matrix MUST cover all 8 supported LC languages (Python/python3, Java, C++, C, JavaScript, TypeScript, Go, Rust). Each fence tag must visibly highlight in Edit Mode (Live Preview + Source). Per RESEARCH §A1+A2, the 7 native tags (`python`, `java`, `cpp`, `javascript`, `typescript`, `go`, `rust`) are assumed-to-work; live smoke validates. The `c → cpp` remap requires the cpp grammar to tokenize idiomatic C cleanly — also live-smoke validated.
- **D-09:** Live-smoke MUST verify Reading Mode shows ONLY Run + Submit (no chevron). UI-SPEC §"Coexistence with Phase 5.1 Run/Submit buttons" + RESEARCH §Pitfall 2 lock this.
- **D-08:** Live-smoke MUST verify Cmd-Z reverts the visible fence (opener + body) as ONE undo step. The frontmatter `lc-language` undo is on a separate vault-undo stack (Pitfall 1 — accepted divergence).

Per RESEARCH §Q7 — recommended bundle cap is 250 KB hard / 200 KB warn:
- Post-revert + chevron bundle is ~155 KB (Wave 0 baseline 149 KB + ~5-6 KB chevron DOM + remap helpers + atomic-dispatch glue).
- Current cap of 700 KB / 600 KB warn was sized for the failed Compartment-swap (520 KB landing). Leaving it at 700 KB after revert means any future regression up to 4.5x current size silently passes — the gate ceases to be a regression gate.
- 250 KB hard / 200 KB warn gives ~95 KB headroom for Phase 5.3 polish + future minor phases while still catching ~60% bloat regressions.
- Planner may overrule (e.g., 300 KB if a Phase 6.x is anticipated to add ~50 KB) but should NOT keep at 700 KB — that's a no-op gate.

Per UI-SPEC §"Live-Smoke Acceptance Checklist (Wave 4 — D-14 successor)" — the authoritative checklist is already drafted in UI-SPEC.md. This plan's UAT rewrite copies that structure and converts each bullet to a numbered item with `result: [pending]` placeholders.
</truths>

<must_haves>
- truths: see frontmatter
- artifacts: see frontmatter
- key_links: see frontmatter
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite 05.3-UAT.md as the chevron + remap live-smoke checklist</name>
  <files>.planning/phases/05.3-language-aware-editor/05.3-UAT.md</files>
  <read_first>
    .planning/phases/05.3-language-aware-editor/05.3-UAT.md (current state — 44 items targeting the failed Compartment-swap; this plan supersedes it; preserve a "DEPRECATED" stub header at the top of the new file noting the rewrite reason),
    .planning/phases/05.3-language-aware-editor/05.3-UI-SPEC.md (§"Live-Smoke Acceptance Checklist (Wave 4 — D-14 successor)" — the authoritative checklist; copy its 9 sections verbatim into the rewritten UAT.md and convert each `[ ]` bullet to a numbered item with explicit `result: [pending]` placeholder),
    .planning/phases/05.3-language-aware-editor/05.3-VALIDATION.md (§"Manual-Only Verifications" — confirms which behaviors are unit-testable vs live-only),
    .planning/phases/05.3-language-aware-editor/05.3-CONTEXT.md (D-04, D-08, D-09, D-11)
  </read_first>
  <action>
    Rewrite `.planning/phases/05.3-language-aware-editor/05.3-UAT.md` so it becomes the canonical live-smoke checklist for the chevron + remap implementation.

    Required structure (executor implements; do NOT inline a verbatim Markdown body inside this PLAN.md — pull the section bodies from UI-SPEC.md §"Live-Smoke Acceptance Checklist (Wave 4 — D-14 successor)"):

    1. **Frontmatter** with status, phase, source `[05.3-04-PLAN.md]`, started/updated ISO timestamps, plugin_path placeholder, main_js_bytes/main_js_kb placeholders, baseline_kb 149, delta_kb placeholder, ship_cap_kb 250 (matching Task 2 default).
    2. **Title + lead paragraph** stating this is the successor to the original D-14 live-smoke and verifies the chevron+remap approach in a real Obsidian dev vault.
    3. **DEPRECATED stub** (3-5 lines) at the top noting the original 44-item Compartment-swap UAT was replaced by this rewrite; original preserved in git history.
    4. **Pre-flight section** (mechanical checks, run by Claude before delegating to human):
       - `npm run build` succeeds, single `main.js`, no chunks
       - `bash scripts/check-bundle-size.sh` exits 0 against the new cap
       - `npm test --run` green
       - `npx tsc --noEmit` produces no NEW errors beyond preexisting `tests/solve/SessionExpiredNotice` TS18048
    5. **Tests section** organized into Sections A through I, drawing the bullet text from UI-SPEC §"Live-Smoke Acceptance Checklist". Convert each bullet to a numbered item (`A1`, `A2`, ..., `I11`) with `expected:` and `result: [pending]` placeholders the verifier fills in:
       - **Section A — Native fence-tag highlighting (Edit + Reading Mode)** — Python/python3, Java, C++, C, JavaScript, TypeScript, Go, Rust each in Live Preview + Source + Reading Mode; plus C# unsupported (plain monospace fallback)
       - **Section B — Chevron rendering (Edit Mode only)** — appears LEFT of Run+Submit in Live Preview + Source; absent in Reading Mode (D-09); absent on non-lc-slug notes; absent on lc-slug notes without `## Code` fence
       - **Section C — Chevron click dropdown** — opens, lists 8 items in `LC_CHEVRON_LANG_ORDER`, current item is `is-current` (accent + bold), outside-click closes, Esc closes, re-click toggles
       - **Section D — Language switch (destructive — D-08)** — destructive replace, single Cmd-Z reverts opener+body atomically, second Cmd-Z reverts frontmatter (Pitfall 1 two-step), current-language no-op
       - **Section E — Frontmatter updates** — `lc-language` writes on switch (D-03); `lc-runtime-ms` and `lc-memory-mb` absent on new AC writes (D-01/D-02); orphan values on legacy notes are inert
       - **Section F — Run/Submit regression** — Phase 5.1 Run + Submit work in Edit + Reading after chevron ships
       - **Section G — Network failure** — disconnected click shows locked Notice `Couldn't fetch starter code for {Label}.`; fence body unchanged
       - **Section H — Theme parity** — Default light, Default dark, Minimal light/dark; Obsidian theme variables drive all colors
       - **Section I — Bundle-size + automated regression** — `npm run build` succeeds; `bash scripts/check-bundle-size.sh` exits 0 against new 250 KB cap; `wc -c < main.js` value recorded; `ls main.js*.js | wc -l` reports 0; full vitest suite green; deleted Compartment-swap test files are gone
    6. **Summary section** — total / passed / failed / pending / blocked tallies (verifier fills).
    7. **Gaps section** — verifier records any failures; if non-empty, follow-up plan generated via `/gsd-plan-phase 5.3 --gaps`.
    8. **Environment section** at the end — OS, Obsidian version, theme used in H1/H2, main.js bytes, baseline 149,167 bytes, delta, D-08 atomic-undo outcome, Pitfall 4/7 observations.

    DO NOT inline a verbatim copy of the entire UI-SPEC checklist text inside this PLAN.md (the executor MUST read UI-SPEC §"Live-Smoke Acceptance Checklist" to source the actual bullet text). The executor's job is the format conversion + numbering + result-placeholder addition.

    DO NOT execute the smoke checks in this Task — Task 3 is the human-verified checkpoint. This task only produces the checklist that the verifier consumes.

    Item count target: at least 30 numbered items across Sections A through I (UI-SPEC has roughly 45 bullets; preserve them all). Verify with `grep -cE '^[0-9]+\\.' 05.3-UAT.md` after writing.
  </action>
  <verify>
    <automated>test -f .planning/phases/05.3-language-aware-editor/05.3-UAT.md &amp;&amp; grep -E 'chevron' .planning/phases/05.3-language-aware-editor/05.3-UAT.md &amp;&amp; grep -E 'fence-tag' .planning/phases/05.3-language-aware-editor/05.3-UAT.md &amp;&amp; ! grep -E 'Compartment swap|languagePackRegistry|whitespaceCopyIndent' .planning/phases/05.3-language-aware-editor/05.3-UAT.md &amp;&amp; COUNT=$(grep -cE "^[0-9]+\\." .planning/phases/05.3-language-aware-editor/05.3-UAT.md) &amp;&amp; test "$COUNT" -ge 30</automated>
  </verify>
  <acceptance_criteria>
    - File assertion: `.planning/phases/05.3-language-aware-editor/05.3-UAT.md` exists
    - Source assertion: file contains the strings `chevron` and `fence-tag` (chevron+remap signature)
    - Source assertion: file does NOT contain the strings `Compartment swap`, `languagePackRegistry`, or `whitespaceCopyIndent` (the failed-implementation vocabulary is gone)
    - Source assertion: file contains at least 30 numbered checklist items (`grep -cE '^[0-9]+\\.' >= 30`)
    - Source assertion: file contains all 9 section headers `Section A`, `Section B`, ..., `Section I`
    - Source assertion: file contains the locked Notice copy `Couldn't fetch starter code for` (Section G)
    - Source assertion: frontmatter has `status: pending`, `phase: 05.3-language-aware-editor`, `source: [05.3-04-PLAN.md]`
  </acceptance_criteria>
  <done>
    UAT.md is the chevron+remap checklist. The file's structure mirrors UI-SPEC §"Live-Smoke Acceptance Checklist" with numbered items + result placeholders. Verifier can run it without referring back to UI-SPEC.
  </done>
</task>

<task type="auto">
  <name>Task 2: Tighten bundle cap in check-bundle-size.sh + update prerelease-check.sh Gate 12 legend</name>
  <files>scripts/check-bundle-size.sh, scripts/prerelease-check.sh</files>
  <read_first>
    scripts/check-bundle-size.sh (full file — pay attention to LIMIT_BYTES, WARN_BYTES, and the docstring around line 11; D-13 PRESERVES the script — only edit values, do NOT delete or restructure),
    scripts/prerelease-check.sh (search for `Gate 12` — typically around lines 29 + 238-244; only the docstring + legend strings change; the actual `bash scripts/check-bundle-size.sh` delegation must remain),
    .planning/phases/05.3-language-aware-editor/05.3-RESEARCH.md (§Q7 — full reasoning for the 250/200 cap recommendation),
    .planning/phases/05.3-language-aware-editor/05.3-UI-SPEC.md (§"Bundle-Size Gate" — same recommendation; this plan executes the change UI-SPEC pre-locked)
  </read_first>
  <action>
    Two coupled edits. Recommended values are 250 KB hard / 200 KB warn per RESEARCH §Q7; if you have a strong reason to set a different cap (e.g., known Phase 6.x will add 50 KB → set 300 KB), document the reasoning in this plan's SUMMARY.md.

    A. **`scripts/check-bundle-size.sh`** — change three constants + their docstrings:
    - Update the file-header comment from `# Fails if main.js > 700 KB. Warns at 600 KB.` (or whatever the current line says) to `# Fails if main.js > 250 KB. Warns at 200 KB.` (or your chosen values).
    - Change `LIMIT_BYTES=$((700 * 1024))` to `LIMIT_BYTES=$((250 * 1024))`.
    - Change `WARN_BYTES=$((600 * 1024))` to `WARN_BYTES=$((200 * 1024))`.
    - Update the corresponding `BUNDLE CHECK FAIL` and `BUNDLE CHECK WARN` echo strings so the printed cap values match (e.g. "BUNDLE CHECK FAIL: main.js is ${MAIN_KB} KB (>250 KB ceiling; chevron+remap baseline ~155 KB)").
    - Preserve all other logic — `wc -c`, the `> $LIMIT_BYTES` arithmetic, the exit codes — verbatim. The script must still emit the SAME structured output shape; only the numerical thresholds change.

    B. **`scripts/prerelease-check.sh`** — update Gate 12 docstring + legend:
    - Around line 29 (file-header comment legend listing all gates): change `12. main.js <= 700 kB` (or current text) to `12. main.js <= 250 kB`.
    - Around lines 238-244 (the Gate 12 implementation block — comment + delegation + ok-message): change the leading comment from `# Gate 12: main.js <= 700 kB ...` to `# Gate 12: main.js <= 250 kB ...`. Change the `ok "gate 12 — main.js within 700 KB budget"` line to `ok "gate 12 — main.js within 250 KB budget"`.
    - DO NOT change the line `bash scripts/check-bundle-size.sh` (or equivalent delegation) — the script remains the single source of truth.

    After the edits, run `npm run build && bash scripts/check-bundle-size.sh` to confirm the new cap accepts the post-Wave-2 bundle (~155 KB). If the build artifact exceeds 200 KB, the WARN line should print but exit 0 is still expected (≤ 250 KB hard cap). If it exceeds 250 KB, that's a regression to investigate before this plan ships — escalate per UI-SPEC §"Bundle-Size Gate" escalation order: (a) inspect chevron CSS for redundant rules; (b) audit dropdown DOM for over-construction; (c) consider raising the cap by 25 KB increments with explicit ROADMAP justification.

    Do NOT touch any other scripts/. Do NOT add new gates to prerelease-check.sh.
  </action>
  <verify>
    <automated>grep -E 'LIMIT_BYTES=\\$\\(\\(250 \\* 1024\\)\\)' scripts/check-bundle-size.sh &amp;&amp; grep -E 'WARN_BYTES=\\$\\(\\(200 \\* 1024\\)\\)' scripts/check-bundle-size.sh &amp;&amp; grep -E '<=\\s*250\\s*kB' scripts/prerelease-check.sh &amp;&amp; ! grep -E 'LIMIT_BYTES=\\$\\(\\(700 \\* 1024\\)\\)' scripts/check-bundle-size.sh &amp;&amp; npm run build 2>&amp;1 | tail -3 &amp;&amp; bash scripts/check-bundle-size.sh</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `scripts/check-bundle-size.sh` contains `LIMIT_BYTES=$((250 * 1024))` (or planner-justified alternative documented in SUMMARY)
    - Source assertion: `scripts/check-bundle-size.sh` contains `WARN_BYTES=$((200 * 1024))` (or matching planner alternative)
    - Source assertion: `scripts/check-bundle-size.sh` does NOT contain the old `LIMIT_BYTES=$((700 * 1024))` value
    - Source assertion: `scripts/prerelease-check.sh` contains the new cap value in Gate 12 docstring (e.g. `<= 250 kB`)
    - Source assertion: `scripts/prerelease-check.sh` still contains `bash scripts/check-bundle-size.sh` (delegation preserved — single source of truth)
    - Source assertion: `scripts/check-bundle-size.sh` still exists (D-13 PRESERVE — only the cap values change)
    - CLI: `npm run build` exits 0
    - CLI: `bash scripts/check-bundle-size.sh` exits 0 against the post-Wave-2 bundle (~155 KB ≤ 250 KB)
  </acceptance_criteria>
  <done>
    Bundle cap is sized for the post-revert reality. The script remains the single source of truth (D-13 PRESERVE intact); prerelease-check.sh's delegation is unchanged. The build passes the new cap.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verified live smoke (D-14 successor)</name>
  <what-built>
    Phase 5.3 chevron + write-time fence-tag remap implementation:
    - Wave 0: failed Compartment-swap fully reverted (3 src files + 3 test files deleted; 6 @codemirror/lang-* packs uninstalled; src/main.ts Step 6i removed)
    - Wave 1: lcSlugToFenceTag remap helper + LC_LANG_DISPLAY_LABELS + LC_CHEVRON_LANG_ORDER in src/solve/languages.ts; codeBlockFor calls lcSlugToFenceTag at write time; lc-runtime-ms / lc-memory-mb writes deleted from NoteTemplate.applySolveTimeFrontmatter and KnowledgeGraphWriter.onAccepted
    - Wave 2: src/main/languageChevronWidget.ts (NEW) + buildCodeBlockButtonRow opts.prefix widening + CodeActionsWidget.toDOM passes chevron factory + LeetCodePlugin.switchFenceLanguage atomic-dispatch handler + styles.css chevron classes + space-between flex layout
    - Wave 3 Task 2: bundle cap tightened to 250 KB hard / 200 KB warn

    Auto-completed before delegating to human:
    - npm run build → main.js single file, ~155 KB
    - bash scripts/check-bundle-size.sh → exits 0 against new 250 KB cap
    - npm test --run → all green
    - ls main.js*.js | wc -l → 0 (no chunks)
    - tsc --noEmit → no NEW errors
  </what-built>
  <how-to-verify>
    Open `.planning/phases/05.3-language-aware-editor/05.3-UAT.md` (rewritten by Task 1) and execute every numbered item in Sections A through I. For each item:
    1. Read the `expected:` line.
    2. Perform the action in the dev vault (`/Users/moxu/Documents/Leetcode/.obsidian/plugins/leetcode/`).
    3. Edit the `result:` line to `result: PASS` or `result: FAIL: <reason>`.

    Pre-flight has already been auto-completed by Claude before this checkpoint fires; you should see PASS marks already on the Pre-flight section.

    Critical sections (must all PASS for ship — D-14 LOCKED):
    - **Section A** (native highlighting per language): every supported LC language must show colored keywords/strings/comments in Live Preview + Source. C# (unsupported) must render plain monospace with NO crash.
    - **Section B** (chevron placement): chevron LEFT in Edit Mode; absent in Reading Mode.
    - **Section D** (atomic destructive switch): single Cmd-Z reverts fence opener+body together. Second Cmd-Z reverts frontmatter (two-stack divergence is documented and accepted per Pitfall 1).
    - **Section G** (network failure): the locked Notice copy fires; fence body is preserved.

    Allowed-to-FAIL items (non-blocking; record observation but do not block ship):
    - Section C item C7 (full keyboard accessibility — arrow keys + Enter to select). UI-SPEC §Accessibility says "ship basic"; if Plan 03 Task 1 stretch goal was skipped, mark "stretch — not implemented this phase".
    - Section H item H3 (Minimal community theme): if you don't have Minimal installed, mark "blocked: theme not available".
    - Section I item I3 (warn-line not tripped): non-blocking; record outcome.

    If any item in Sections A, B, D, F, or G fails, do NOT type "approved". Open `/gsd-plan-phase 5.3 --gaps` and route the failures to a Plan 05 gap-closure plan; that plan addresses the gaps and re-runs this checkpoint.

    When every blocking item is PASS, fill in:
    - `## Summary` tallies (total, passed, failed, pending=0, blocked).
    - `## Environment` section (OS, Obsidian version, theme used, main.js bytes, delta from 149,167).

    Then type "approved" to release the checkpoint.
  </how-to-verify>
  <resume-signal>Type "approved" after every blocking UAT item is PASS and Summary + Environment sections are filled in. If any blocking item fails, type "gap closure needed: ${item-ID}" and run `/gsd-plan-phase 5.3 --gaps` instead.</resume-signal>
  <files>
    .planning/phases/05.3-language-aware-editor/05.3-UAT.md (verifier fills in result fields per item),
    .planning/phases/05.3-language-aware-editor/05.3-04-SUMMARY.md (created at end of plan)
  </files>
  <read_first>
    .planning/phases/05.3-language-aware-editor/05.3-UAT.md (the rewritten checklist from Task 1; this is the script the verifier executes),
    .planning/phases/05.3-language-aware-editor/05.3-UI-SPEC.md (full visual + interaction contract; verifier cross-references for ambiguous items),
    .planning/phases/05.3-language-aware-editor/05.3-CONTEXT.md (D-08 atomic-undo, D-09 Reading-Mode-only chevron, D-11 network-failure Notice copy — locked behaviors verified by this checkpoint)
  </read_first>
  <action>
    Human-verified live-smoke checkpoint per D-14 successor. Claude auto-runs the Pre-flight section of UAT.md (npm run build, check-bundle-size.sh, npm test --run, tsc --noEmit, ls main.js*.js) and marks those items PASS, then pauses and surfaces the checkpoint to the user. The user opens the dev vault at the plugin path documented in UAT.md frontmatter, executes Sections A through I, and edits each `result:` line in 05.3-UAT.md to PASS or FAIL with reason. When all blocking items pass (Sections A, B, D, F, G — items in C, H, I are mostly automated), the user fills in the Summary tally + Environment section, then types "approved". On any blocking failure, the user types `gap closure needed: <item-ID>` and runs `/gsd-plan-phase 5.3 --gaps` to route the failure to a follow-up gap-closure plan. See `<how-to-verify>` for the full procedure.
  </action>
  <verify>
    <automated>grep -cE "^[0-9]+\..*result:\s+PASS" .planning/phases/05.3-language-aware-editor/05.3-UAT.md | awk '$1 >= 30 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>
    Every blocking UAT item is marked PASS in 05.3-UAT.md; Summary tally is filled in; Environment section captures OS, Obsidian version, theme, main.js bytes, delta from 149,167. User has typed "approved". Phase 5.3 is ready to be marked complete in ROADMAP.
  </done>
  <acceptance_criteria>
    - File assertion: 05.3-UAT.md has at least 30 numbered items where `result:` is `PASS` (or where the user explicitly marked the item as `stretch — not implemented this phase` or `blocked: theme not available` per UAT §"Allowed-to-FAIL items")
    - File assertion: 05.3-UAT.md `## Summary` block has non-placeholder values for total / passed / failed / blocked
    - File assertion: 05.3-UAT.md `## Environment` block has non-placeholder values for OS, Obsidian version, theme, main.js bytes
    - User signal: "approved" received OR follow-up gap-closure plan exists
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bundle-size cap | New cap of 250 KB hard could trigger false positives if a future minor phase adds >95 KB; documented escalation order in UI-SPEC §"Bundle-Size Gate" handles this |
| Live-smoke verifier | Human in the loop; no automated bypass possible; D-14 LOCKED |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.3-04-01 | T (Tampering) | Verifier marks an item PASS without actually testing | accept | Solo developer + Claude workflow; no team to deceive. Verifier is the user themselves; misreporting harms only their own ship |
| T-05.3-04-02 | D (Denial of service) | Bundle cap of 250 KB blocks a legitimate future phase that needs to grow | mitigate | Cap is intentionally tight; UI-SPEC §"Bundle-Size Gate" escalation order documents the path to raise it (25 KB increments with explicit ROADMAP justification) |
| T-05.3-04-03 | I (Information disclosure) | UAT.md may capture the verifier's environment details (OS, Obsidian version) | accept | Vault-local file; not committed to public repo unless user chooses; standard for /gsd workflow |
| T-05.3-04-04 | E (Elevation of privilege) | The `view.editor.cm` cast in Wave 2 could break on an Obsidian update mid-Wave-3 | accept | RESEARCH §Pitfall 6 + CLAUDE.md acknowledge; live-smoke would surface the breakage as a Section D failure (Cmd-Z would not work as expected); gap-closure path exists |
</threat_model>

<verification>
After completing all 3 tasks:
- `05.3-UAT.md` is rewritten with the chevron+remap checklist (≥30 numbered items, 9 sections, no Compartment-swap vocabulary)
- `scripts/check-bundle-size.sh` cap is tightened to 250 KB / 200 KB (or planner-justified alternative); script still exists (D-13 PRESERVE)
- `scripts/prerelease-check.sh` Gate 12 legend reflects new cap; delegation to check-bundle-size.sh is intact
- `npm run build && bash scripts/check-bundle-size.sh` exits 0 against the new cap
- Human verifier has marked every blocking item PASS in UAT.md
- Summary tallies are filled in
- Environment section is filled in
- Verifier has typed "approved" OR routed gaps to a follow-up gap-closure plan
</verification>

<success_criteria>
- UAT.md rewrite covers all 9 sections from UI-SPEC §"Live-Smoke Acceptance Checklist"
- Bundle cap is sized for post-revert reality (recommended 250/200 KB; planner may overrule with ROADMAP justification)
- prerelease-check.sh Gate 12 legend matches the new cap
- All blocking UAT items pass in the live dev vault
- Phase 5.3 ships (or routes any gaps to Plan 05 closure)
- D-14 LOCKED human-verified checkpoint is honored
</success_criteria>

<output>
After human verifier signs off, create `.planning/phases/05.3-language-aware-editor/05.3-04-SUMMARY.md` documenting:
- Final bundle-cap values chosen (and reasoning if not 250/200)
- Final main.js byte count (post-build)
- Delta from Wave 0 baseline (149,167 bytes)
- UAT total / passed / failed counts
- Section-by-section pass summary (A/B/C/D/E/F/G/H/I)
- Any open observations: Pitfall 4 / 7 notes, theme-parity edge cases, stretch items skipped (C7 keyboard a11y)
- Confirmation Phase 5.3 (replan) supersedes the original failed implementation in ROADMAP
- Pointer to ROADMAP.md update (mark Phase 5.3 complete; POLISH-09 partial — indent/bracket-handling deferred to Phase 6.x per CONTEXT `<deferred>`)
</output>
