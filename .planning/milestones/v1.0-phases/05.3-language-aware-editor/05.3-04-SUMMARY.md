---
plan: 05.3-04
phase: 05.3-language-aware-editor
status: complete
wave: 3
autonomous: false
requirements: [POLISH-09]
checkpoint_resolved: 2026-05-12T00:30:00Z
verifier: human (live dev-vault smoke)
verdict: ship-ready (7 non-blocking gaps queued)
---

# Plan 05.3-04 SUMMARY — D-14 Live-Smoke + Bundle Ship-Gate

## Outcome

D-14 LOCKED checkpoint cleared. Phase 5.3's chevron + write-time fence-tag remap implementation passed live-vault verification across all blocking dimensions (native highlighting per language in Edit + Reading Mode, chevron placement and gating, atomic destructive switch, Phase 5.1 Run/Submit regression, network-failure Notice). 7 non-blocking polish gaps were surfaced and queued for a follow-up gap-closure plan; verifier explicitly accepted them as ship-ready with gaps documented.

## What was built (Plan 04 specifically)

| Task | Outcome | Commit |
|------|---------|--------|
| 1. Rewrite 05.3-UAT.md as chevron+remap live-smoke checklist | 55-item checklist (5 pre-flight + 50 numbered across Sections A–I) | `2bc50aa` |
| 2. Tighten bundle cap (250 KB hard / 200 KB warn) | `scripts/check-bundle-size.sh` + `scripts/prerelease-check.sh` Gate 12 legend updated | `96aad62` |
| 3. Pre-flight automation (PF1–PF5) | All PASS recorded in UAT.md | `9e015cd` |
| 3 (human). Live dev-vault smoke | 53 PASS / 1 FAIL non-blocking / 1 blocked / 1 stretch — 7 polish gaps documented | (verifier interaction; UAT.md updates) |

## D-14 results — blocking sections (all PASS)

- **Section A (native highlighting):** 18/18 PASS — Python (Live Preview + Source + Reading), Java, C++, C (cpp grammar), JavaScript, TypeScript, Go (golang→go remap), Rust, plus Kotlin substituted for C# (no crash; bonus: highlighting works because Obsidian's lang-markdown bundle includes Kotlin grammar)
- **Section B (chevron placement + gating):** 5/5 PASS — chevron LEFT in Live Preview + Source; absent in Reading Mode (D-09); not rendered on non-lc-slug or fence-less notes (Phase 5.1 gate parity)
- **Section D (atomic destructive switch):** 4/4 PASS — fence body + opener tag rewrite synchronously on first click; total Cmd-Z count = 2 (atomicity preserved within CM6 stack); same-language click is no-op
- **Section E (frontmatter):** 3/3 PASS — `lc-language` updates on switch; `lc-runtime-ms` and `lc-memory-mb` absent on post-cleanup AC; legacy notes' orphan keys harmless
- **Section F (Phase 5.1 regression):** 5/5 PASS — chevron stable through caret movement; Run + Submit work in Live Preview + Source + Reading Mode; chevron correctly absent from Reading Mode
- **Section G (network failure):** 2/2 PASS — locked Notice copy fires; fence + frontmatter intact on failure; recovery works after reconnect

## D-14 results — non-blocking sections (mixed)

- **Section C (chevron click + dropdown):** 6/7 — C1–C5 PASS; **C6 FAIL** (Esc dismissal not wired); C7 explicitly stretch
- **Section H (theme parity):** 2/3 — H1 + H2 (Default light + dark) PASS; H3 (Minimal) blocked — theme not installed on verifier's machine
- **Section I (bundle + automated regression):** 8/8 PASS — all mechanical gates green via Claude-run scripts

## Gaps queued for follow-up (verifier-accepted)

| ID | Severity | Summary |
|----|----------|---------|
| C6 | LOW | Esc key does not close the chevron dropdown — missing keydown listener for `Escape` |
| G-LAYOUT | LOW | Chevron button shifts horizontally with indentation of the line below — likely line-decoration vs block-anchor issue |
| G-LABEL-LAG | LOW | Chevron label lags one click behind the actual switch — fence/frontmatter update synchronously but the chevron's own re-render is stale |
| G-CLICK-THROUGH | LOW | Dropdown clicks propagate to underlying editor caret — missing `event.stopPropagation()` |
| G-UNDO-ORDER | INFO | Cmd-Z press-order is reversed from spec (vault undo fires first, CM6 undo second). Verifier explicitly accepts. Total step count matches spec; end-state correct |
| G-PYTHON-LABEL | LOW | Chevron labels Python ambiguously when LC distinguishes `python` (Python 2 deprecated) from `python3` (mainline) — recommend renaming label to `Python 3` or adding both |
| G-COPY-TO-CODE-LANG-DRIFT | MEDIUM | Copy-to-Code from previous submission overwrites fence body but doesn't update `lc-language` or chevron — note's stored language drifts from actual code; subsequent Run/Submit would dispatch with wrong slug. Phase-adjacent integration gap surfaced by chevron+frontmatter-as-source-of-truth model |
| G-CHEVRON-STYLING | LOW | Chevron styled as outlined-button look; verifier prefers `.lc-fm__picker`-style input-field appearance for cross-feature visual consistency with the browser's filter dropdown |

All 7 gaps are documented in `.planning/phases/05.3-language-aware-editor/05.3-UAT.md` `## Gaps` section with reproduction notes, root-cause hypotheses, and likely-fix suggestions.

## Final ship-gate metrics

- `main.js`: **151,577 bytes (~148 KB)**
- Wave 0 baseline: 149,167 bytes (~149 KB)
- Δ for chevron + remap: **+2,410 bytes (+2 KB)**
- Tightened bundle cap: 250 KB hard / 200 KB warn (set by Plan 04 Task 2)
- Status vs cap: **under both** (148 KB ≪ 200 KB warn ≪ 250 KB hard)
- Tests: 550 passed / 2 skipped / 0 failed (vitest, 87 test files)
- TypeScript errors: 24 (4 src/ + 20 tests/, identical to Plan 03 baseline; all DEF-05.3-01-A pre-existing — no new errors introduced by Plan 04)
- Bundle chunks: 0 (single CJS emit)
- Deleted source files: 3/3 absent (no orphan tests, no orphan imports)

## Deferred / out-of-scope

- **DEF-05.3-01-A** (Plan 01 inheritance): pre-existing tsc strict-null-check errors in 20 test files block `npm run build` end-to-end. Wave 0 reduced count from 29 → 18 source-side; Plan 04 modified zero src/ or tests/*.test.ts files (only `.planning/.../05.3-UAT.md`, `scripts/check-bundle-size.sh`, `scripts/prerelease-check.sh`); zero new errors introduced. Workaround: `node esbuild.config.mjs production` runs cleanly. Status unchanged from Plan 01/02/03.

## Files modified

| File | Action |
|------|--------|
| `.planning/phases/05.3-language-aware-editor/05.3-UAT.md` | Rewrote as chevron+remap live-smoke checklist; recorded all 55 results + Environment block + 7 gap entries |
| `scripts/check-bundle-size.sh` | Tightened `LIMIT_BYTES` to 250 KB and `WARN_BYTES` to 200 KB |
| `scripts/prerelease-check.sh` | Updated Gate 12 legend to reference the new caps |
| `.planning/phases/05.3-language-aware-editor/05.3-04-SUMMARY.md` | This file |

## Verifier sign-off

D-14 LOCKED checkpoint cleared with verdict **ship-ready (gaps queued)**. The chevron+remap pivot empirically delivers what the failed Compartment-swap approach could not: native syntax highlighting inside ` ## Code ` fences across all 8 supported LC languages plus Kotlin (bonus), Edit-Mode chevron switching with atomic destructive replace, frontmatter-as-source-of-truth for LC API dispatch, and bundle reclaim from 520 KB → 148 KB (~72% reduction). The 7 polish gaps surfaced during live smoke are non-blocking and queued for a separate follow-up plan per verifier discretion.
