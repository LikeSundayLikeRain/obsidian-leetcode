---
phase: 05.3-language-aware-editor
plan: 02
type: execute
wave: 1
depends_on: [05.3-01]
files_modified:
  - src/solve/languages.ts
  - src/notes/NoteTemplate.ts
  - src/graph/KnowledgeGraphWriter.ts
  - tests/solve/languages.test.ts
  - tests/graph/onAccepted.frontmatter.test.ts
  - tests/graph/onAccepted.reAccepted.test.ts
  - tests/graph/fixture-integration.test.ts
autonomous: true
requirements: [POLISH-09]
must_haves:
  truths:
    - "src/solve/languages.ts exports LC_LANG_FENCE_TAG, lcSlugToFenceTag, LC_LANG_DISPLAY_LABELS, and LC_CHEVRON_LANG_ORDER"
    - "lcSlugToFenceTag('python3') === 'python', lcSlugToFenceTag('golang') === 'go', lcSlugToFenceTag('c') === 'cpp'"
    - "lcSlugToFenceTag passthrough returns the input slug unchanged for unsupported slugs (e.g., 'csharp' -> 'csharp')"
    - "NoteTemplate.applySolveTimeFrontmatter no longer writes lc-runtime-ms or lc-memory-mb"
    - "SolveTimeFrontmatterInput interface no longer declares runtimeMs or memoryMb fields"
    - "KnowledgeGraphWriter.onAccepted no longer parses or passes runtimeMs/memoryMb"
    - "buildNoteBody emits a fence opener tag remapped via lcSlugToFenceTag (so new lc-language='python3' notes write '```python', new lc-language='golang' notes write '```go')"
    - "All three tests/graph fixtures no longer assert lc-runtime-ms or lc-memory-mb writes"
    - "tests/solve/languages.test.ts has new describe blocks proving the remap, label, and order contracts"
    - "Full vitest suite green; no test asserts a removed write or a missing helper"
  artifacts:
    - path: "src/solve/languages.ts"
      provides: "LC slug normalization SSoT extended with fence-tag remap + display labels + dropdown order"
      contains: "LC_LANG_FENCE_TAG, lcSlugToFenceTag, LC_LANG_DISPLAY_LABELS, LC_CHEVRON_LANG_ORDER"
    - path: "src/notes/NoteTemplate.ts"
      provides: "Frontmatter writer + body builder; lc-runtime-ms / lc-memory-mb writes deleted; buildNoteBody emits remapped fence tag"
      contains: "no fm['lc-runtime-ms']; no fm['lc-memory-mb']; lcSlugToFenceTag invocation in buildNoteBody"
    - path: "src/graph/KnowledgeGraphWriter.ts"
      provides: "On-AC orchestrator no longer parses or passes runtime/memory"
      contains: "no parseRuntimeMs; no parseMemoryMb; applySolveTimeFrontmatter call without runtimeMs/memoryMb fields"
    - path: "tests/solve/languages.test.ts"
      provides: "Unit coverage for the new remap/label/order constants"
      contains: "describe('lcSlugToFenceTag'), describe('LC_LANG_DISPLAY_LABELS'), describe('LC_CHEVRON_LANG_ORDER')"
  key_links:
    - from: "src/notes/NoteTemplate.ts (buildNoteBody)"
      to: "src/solve/languages.ts (lcSlugToFenceTag)"
      via: "import + call"
      pattern: "import.*lcSlugToFenceTag.*from '\\.\\./solve/languages'"
    - from: "tests/solve/languages.test.ts"
      to: "src/solve/languages.ts new exports"
      via: "import + describe blocks"
      pattern: "import \\{ .*lcSlugToFenceTag.*LC_LANG_DISPLAY_LABELS.*LC_CHEVRON_LANG_ORDER.* \\}"
---

<objective>
Implement the write-time fence-tag remap (D-04) and the frontmatter cleanup (D-01/D-02) — the pure-data half of Phase 5.3. After this plan, every NEW lc-slug note ships with a markdown-recognized fence opener tag (so Obsidian's bundled lang-markdown nested parser highlights it natively in Edit Mode), and all on-AC frontmatter writes drop the never-read lc-runtime-ms / lc-memory-mb keys (D-01/D-02).

Purpose: The chevron widget in Wave 2 calls `lcSlugToFenceTag` and `LC_LANG_DISPLAY_LABELS`; both must exist before Wave 2 lands. The frontmatter cleanup is co-located here because it's the same surface (NoteTemplate.ts + the on-AC writer) and both changes update the same test files (tests/graph/*) — atomic plan minimizes context churn for the executor.

Output:
1. `src/solve/languages.ts` extended with three constants + one helper (purity preserved per file header).
2. `src/notes/NoteTemplate.ts::buildNoteBody` calls `lcSlugToFenceTag` for the fence opener tag; `applySolveTimeFrontmatter` and the `SolveTimeFrontmatterInput` interface no longer mention runtime/memory.
3. `src/graph/KnowledgeGraphWriter.ts` no longer parses runtime/memory or passes them to `applySolveTimeFrontmatter`.
4. New + updated vitest assertions covering the contract.
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.3-language-aware-editor/05.3-CONTEXT.md
@.planning/phases/05.3-language-aware-editor/05.3-RESEARCH.md
@.planning/phases/05.3-language-aware-editor/05.3-PATTERNS.md
@.planning/phases/05.3-language-aware-editor/05.3-VALIDATION.md
@CLAUDE.md
@src/solve/languages.ts
@src/notes/NoteTemplate.ts
@src/graph/KnowledgeGraphWriter.ts

<interfaces>
<!-- Concrete contracts the executor needs. Extracted from current source. -->

From src/solve/languages.ts (existing — extend, don't replace):
```typescript
export const LC_LANG_SLUGS: ReadonlySet<string> = new Set([
  'python3', 'python', 'java', 'cpp', 'c', 'csharp', 'javascript',
  'typescript', 'rust', 'golang', 'kotlin', 'swift', 'ruby', 'scala',
  'php', 'dart', 'elixir', 'erlang', 'racket', 'mysql', 'postgresql',
  'mssql', 'oraclesql',
]);

export const FENCE_TAG_ALIASES: Readonly<Record<string, string>> = {
  'py': 'python3', 'py3': 'python3', 'python2': 'python',
  'ts': 'typescript', 'js': 'javascript', 'c++': 'cpp',
  'c#': 'csharp', 'cs': 'csharp', 'go': 'golang',
  'kt': 'kotlin', 'rb': 'ruby', 'rs': 'rust',
  'pg': 'postgresql', 'sql': 'mysql',
};

export function resolveLangSlug(fenceTag: string | null | undefined, fallback: string): string;
```

File header purity contract (must be preserved):
> "// Purity: no runtime imports, no captured state, no I/O.
>  // Safe to call from pure transforms (codeExtractor consumers, vault.process callbacks)."

From src/notes/NoteTemplate.ts (lines 287-290 — interface fields to DELETE):
```typescript
/** Parsed runtime in milliseconds. Undefined when LC returns "N/A". */
runtimeMs: number | undefined;
/** Parsed memory in MB. Undefined when LC returns "N/A". */
memoryMb: number | undefined;
```

From src/notes/NoteTemplate.ts (lines 355-364 — frontmatter writes to DELETE):
```typescript
if (typeof input.runtimeMs === 'number' && Number.isFinite(input.runtimeMs)) {
  fm['lc-runtime-ms'] = input.runtimeMs;
} else {
  fm['lc-runtime-ms'] = undefined;
}
if (typeof input.memoryMb === 'number' && Number.isFinite(input.memoryMb)) {
  fm['lc-memory-mb'] = input.memoryMb;
} else {
  fm['lc-memory-mb'] = undefined;
}
```

From src/notes/NoteTemplate.ts (buildNoteBody and codeBlockFor — to be UPDATED):
```typescript
export function codeBlockFor(langSlug: string, starterCode: string): string {
  const code = starterCode.trim();
  return '```' + langSlug + '\n' + code + '\n```';
}

export function buildNoteBody(input: {
  problemMarkdown: string;
  langSlug?: string;
  starterCode?: string;
}): string {
  const langSlug = input.langSlug ?? 'python3';
  const starter = input.starterCode ?? '';
  const codeBlock = codeBlockFor(langSlug, starter);
  return `## Problem\n${input.problemMarkdown.trim()}\n\n${CODE_HEADING_LINE}\n${codeBlock}\n\n## Notes\n\n`;
}
```

From src/graph/KnowledgeGraphWriter.ts (lines 138-152 — call to be UPDATED):
```typescript
const runtimeMs = parseRuntimeMs(terminal.status_runtime);
const memoryMb = parseMemoryMb(terminal.status_memory);
const language = typeof terminal.lang === 'string' && terminal.lang.length > 0
  ? terminal.lang
  : 'unknown';
const solvedAt = new Date();
await applySolveTimeFrontmatter(this.app, ctx.file, {
  solvedAt,
  runtimeMs,
  memoryMb,
  language,
  currentPassTags: tagContribution,
});
```
And lines 222-244 (`parseRuntimeMs`, `parseMemoryMb` helpers to DELETE).
</interfaces>
</context>

<truths>
Locked CONTEXT decisions this plan implements:
- **D-01:** Drop `lc-runtime-ms` frontmatter. `NoteTemplate.ts:355-358` apply path AND lines 287-288 interface field DELETED. Display path uses fresh GraphQL via `SubmissionDetailModal.runtimeDisplay` (per RESEARCH §A7 — no production reader for `lc-runtime-ms` exists; verified by grep).
- **D-02:** Drop `lc-memory-mb` frontmatter. `NoteTemplate.ts:360-363` apply path AND lines 289-290 interface field DELETED. Same reader-audit as D-01.
- **D-03:** Keep `lc-language` frontmatter as canonical LC-language source. `applySolveTimeFrontmatter` STILL writes `fm['lc-language']`. The chevron in Wave 2 also writes here.
- **D-04:** Write-time fence-tag remap is the entire highlighting solution. Mapping table:
  | LC slug | Fence tag |
  | python3 | python (REMAPPED) |
  | golang | go (REMAPPED) |
  | c | cpp (REMAPPED) |
  | python, java, cpp, javascript, typescript, rust | identity |
  Unsupported LC slugs (csharp, kotlin, ruby, etc.) pass through verbatim — fence renders plain monospace.
- **D-10:** Chevron text shows current LC language label (`▼ Python`, `▼ Java`, `▼ C++`). Mapping `slug → label` lives in `src/solve/languages.ts` — added here so Wave 2 can import.

Per RESEARCH §"Don't Hand-Roll": the new `LC_LANG_FENCE_TAG` + `LC_LANG_DISPLAY_LABELS` + `LC_CHEVRON_LANG_ORDER` constants and the `lcSlugToFenceTag(slug)` helper MUST live in `src/solve/languages.ts` (purity preserved) — single source of truth, co-located with the existing `LC_LANG_SLUGS` / `FENCE_TAG_ALIASES` / `resolveLangSlug` pattern.

Per RESEARCH §A8 + §"Lines to delete": `KnowledgeGraphWriter.ts` private helpers `parseRuntimeMs` and `parseMemoryMb` become unused after the runtime/memory args are removed — delete the helpers AND any imports they pull in (none currently — they're inline).
</truths>

<must_haves>
- truths:
  - "lcSlugToFenceTag('python3') === 'python'"
  - "lcSlugToFenceTag('golang') === 'go'"
  - "lcSlugToFenceTag('c') === 'cpp'"
  - "lcSlugToFenceTag('java') === 'java' (identity)"
  - "lcSlugToFenceTag('csharp') === 'csharp' (passthrough for unsupported)"
  - "LC_LANG_DISPLAY_LABELS['python3'] === 'Python', LC_LANG_DISPLAY_LABELS['cpp'] === 'C++'"
  - "LC_CHEVRON_LANG_ORDER has length 8"
  - "applySolveTimeFrontmatter no longer writes fm['lc-runtime-ms'] or fm['lc-memory-mb']"
  - "SolveTimeFrontmatterInput interface no longer declares runtimeMs / memoryMb"
  - "KnowledgeGraphWriter.onAccepted no longer parses runtime/memory and no longer passes them"
  - "buildNoteBody emits ```python (not ```python3) when called with langSlug: 'python3'"
  - "buildNoteBody emits ```go (not ```golang) when called with langSlug: 'golang'"
  - "Full vitest suite green"
- artifacts: see frontmatter
- key_links: see frontmatter
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Extend src/solve/languages.ts with fence-tag remap, display labels, chevron order</name>
  <files>src/solve/languages.ts, tests/solve/languages.test.ts</files>
  <read_first>
    src/solve/languages.ts (full current file — preserve purity contract from header lines 1-9; extend, don't replace),
    tests/solve/languages.test.ts (existing describe blocks for LC_LANG_SLUGS / FENCE_TAG_ALIASES / resolveLangSlug — mirror their shape),
    .planning/phases/05.3-language-aware-editor/05.3-CONTEXT.md (D-04 fence-tag table; D-10 display labels),
    .planning/phases/05.3-language-aware-editor/05.3-RESEARCH.md (§"Code Examples — src/solve/languages.ts additions" — exact constant shapes),
    .planning/phases/05.3-language-aware-editor/05.3-PATTERNS.md (§"src/solve/languages.ts" — purity contract details)
  </read_first>
  <action>
    Add four new exports to `src/solve/languages.ts`, AFTER the existing `resolveLangSlug` function so the file's existing surface (LC_LANG_SLUGS, FENCE_TAG_ALIASES, resolveLangSlug) is preserved unchanged:

    1. `LC_LANG_FENCE_TAG` — `Readonly<Record<string, string>>` mapping the 9 covered LC slugs (python, python3, java, cpp, c, javascript, typescript, golang, rust) to their markdown-recognized fence tags. Per D-04: `python3 → python`, `golang → go`, `c → cpp`; the rest are identity.

    2. `lcSlugToFenceTag(slug: string): string` — pure helper returning `LC_LANG_FENCE_TAG[slug] ?? slug` so unsupported slugs (csharp, kotlin, etc.) pass through verbatim.

    3. `LC_LANG_DISPLAY_LABELS` — `Readonly<Record<string, string>>` per D-10 mapping the 9 covered slugs to user-friendly labels: `python3` and `python` both render as `Python`; `cpp` renders as `C++`; `golang` renders as `Go`; `c` renders as `C`; `java`, `javascript`, `typescript`, `rust` use natural-case display names (`Java`, `JavaScript`, `TypeScript`, `Rust`).

    4. `LC_CHEVRON_LANG_ORDER` — `ReadonlyArray<string>` of exactly 8 LC slugs in dropdown order: `['python3', 'java', 'cpp', 'c', 'javascript', 'typescript', 'golang', 'rust']`. Per UI-SPEC §"Per-language behavior contract" — Python first as most common, Rust last as least common; matches CONTEXT D-04's "8 supported LC languages" wording. Note `python3` is the canonical LC slug (chevron writes `python3` to `lc-language` even though the fence tag is `python`).

    Preserve the file header purity contract (no runtime imports, no captured state, no I/O). All four additions are pure data + one pure function.

    Then in `tests/solve/languages.test.ts`, add three new `describe` blocks mirroring the existing test style (vitest `describe`/`it`/`expect`):

    - `describe('lcSlugToFenceTag (Phase 5.3 D-04)')` — at least 5 cases: `python3 → python`, `golang → go`, `c → cpp`, identity for `java`/`cpp`/`javascript`/`typescript`/`rust`/`python`, passthrough for `csharp` and `kotlin`.
    - `describe('LC_LANG_DISPLAY_LABELS (Phase 5.3 D-10)')` — assert all 9 entries, particularly `python3 → 'Python'`, `cpp → 'C++'`, `golang → 'Go'`, `javascript → 'JavaScript'`.
    - `describe('LC_CHEVRON_LANG_ORDER (Phase 5.3 D-04 + D-10)')` — assert length is 8, `python3` is first, `rust` is last, and every entry has a corresponding `LC_LANG_DISPLAY_LABELS` entry.

    Do NOT modify any existing assertion in `languages.test.ts`. Do NOT remove or rename `LC_LANG_SLUGS`, `FENCE_TAG_ALIASES`, or `resolveLangSlug` — Wave 2 does not need them removed.
  </action>
  <verify>
    <automated>npx vitest run tests/solve/languages.test.ts 2>&amp;1 | tail -10 &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -v 'tests/solve/SessionExpiredNotice' | grep -E 'error TS' &amp;&amp; echo "TS errors found" &amp;&amp; exit 1 || true</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `npx vitest run tests/solve/languages.test.ts` exits 0 with all assertions green
    - Source assertion: `grep -E 'export (const|function) (LC_LANG_FENCE_TAG|lcSlugToFenceTag|LC_LANG_DISPLAY_LABELS|LC_CHEVRON_LANG_ORDER)' src/solve/languages.ts` returns 4 matches
    - Source assertion: `src/solve/languages.ts` still contains the existing `LC_LANG_SLUGS`, `FENCE_TAG_ALIASES`, `resolveLangSlug` exports (preserve, don't replace)
    - Source assertion: `src/solve/languages.ts` still has the `// Purity:` header comment from line 7-9
    - Behavior assertion: `lcSlugToFenceTag('python3')` returns `'python'`; `lcSlugToFenceTag('golang')` returns `'go'`; `lcSlugToFenceTag('c')` returns `'cpp'`; `lcSlugToFenceTag('java')` returns `'java'`; `lcSlugToFenceTag('csharp')` returns `'csharp'`
    - Behavior assertion: `LC_LANG_DISPLAY_LABELS['cpp']` is `'C++'`; `LC_CHEVRON_LANG_ORDER.length` is `8` and `LC_CHEVRON_LANG_ORDER[0]` is `'python3'`
    - CLI: `npx tsc --noEmit` produces no NEW TS errors (preexisting `tests/solve/SessionExpiredNotice.test.ts` TS18048 errors are out of scope)
  </acceptance_criteria>
  <done>
    `src/solve/languages.ts` exports the four new constants/helper. The languages.test.ts suite has new describe blocks proving each contract. TypeScript compiles cleanly. Wave 2's chevron widget can now import `lcSlugToFenceTag`, `LC_LANG_DISPLAY_LABELS`, and `LC_CHEVRON_LANG_ORDER` directly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontmatter cleanup (D-01/D-02) + write-time fence-tag remap in NoteTemplate.ts</name>
  <files>
    src/notes/NoteTemplate.ts,
    src/graph/KnowledgeGraphWriter.ts,
    tests/graph/onAccepted.frontmatter.test.ts,
    tests/graph/onAccepted.reAccepted.test.ts,
    tests/graph/fixture-integration.test.ts
  </files>
  <read_first>
    src/notes/NoteTemplate.ts (full file — pay attention to lines 25-33 PLUGIN_LC_KEYS, lines 60-66 codeBlockFor, lines 143-152 buildNoteBody, lines 283-296 SolveTimeFrontmatterInput interface, lines 340-376 applySolveTimeFrontmatter writer),
    src/graph/KnowledgeGraphWriter.ts (full file — pay attention to lines 138-152 onAccepted Step 1 frontmatter call, lines 218-244 parseRuntimeMs / parseMemoryMb helpers),
    tests/graph/onAccepted.frontmatter.test.ts (lines 70, 71, 107, 108, 119 — assertions to remove or invert per RESEARCH §"Outdated test assertion deletions"),
    tests/graph/onAccepted.reAccepted.test.ts (lines 39, 40, 67, 68 — preconditions and post-conditions to remove),
    tests/graph/fixture-integration.test.ts (lines 149, 150 — assertions to remove),
    .planning/phases/05.3-language-aware-editor/05.3-CONTEXT.md (D-01, D-02, D-03, D-04),
    .planning/phases/05.3-language-aware-editor/05.3-RESEARCH.md (§"src/notes/NoteTemplate.ts D-01/D-02 cleanup", §"Outdated test assertion deletions", §Pitfall 8),
    .planning/phases/05.3-language-aware-editor/05.3-PATTERNS.md (§"src/notes/NoteTemplate.ts", §"src/graph/KnowledgeGraphWriter.ts", §"tests/graph/*")
  </read_first>
  <action>
    Two coupled edits, executed in order so tests catch regressions:

    A. **`src/notes/NoteTemplate.ts` — D-01/D-02 frontmatter cleanup + D-04 write-time remap:**
    - Add an import at the top: `import { lcSlugToFenceTag } from '../solve/languages';` (the existing module, no circular risk — `languages.ts` is pure data and pulls nothing from notes/).
    - In `codeBlockFor(langSlug, starterCode)`: change the body from `return '```' + langSlug + '\n' + code + '\n```';` to `return '```' + lcSlugToFenceTag(langSlug) + '\n' + code + '\n```';` so EVERY emitter (including any callers other than `buildNoteBody`) writes the markdown-recognized tag. This is the load-bearing single-line change for D-04 highlighting in Edit Mode.
    - Verify `buildNoteBody` continues to delegate to `codeBlockFor` (no separate string concat); leave the `langSlug ?? 'python3'` fallback unchanged — `lcSlugToFenceTag` handles the `python3 → python` remap downstream.
    - In `SolveTimeFrontmatterInput` interface (around lines 283-296): DELETE the `runtimeMs: number | undefined` and `memoryMb: number | undefined` fields and their JSDoc comments. Preserve `solvedAt`, `language`, `currentPassTags`.
    - In `applySolveTimeFrontmatter` (around lines 340-376): DELETE the entire `if (typeof input.runtimeMs === 'number' ... fm['lc-runtime-ms'] = ...) else { fm['lc-runtime-ms'] = undefined; }` block AND the matching `lc-memory-mb` block. Preserve the writes for `fm['lc-status'] = 'accepted'`, `fm['lc-solved-date'] = formatIsoLocalTz(...)`, `fm['lc-language'] = input.language`, and the tags union-merge. Update the JSDoc above `applySolveTimeFrontmatter` to remove the lines describing `lc-runtime-ms` / `lc-memory-mb` semantics — just delete those two paragraphs; the surrounding documentation about lc-status non-downgrade and lc-language overwrite stays.
    - DO NOT touch the `PLUGIN_LC_KEYS` constant (line 25). Per CLAUDE.md the schema SSoT is documentary, not load-bearing for this change. (If your reading shows `lc-runtime-ms` / `lc-memory-mb` listed in `PLUGIN_LC_KEYS`, leave it — those keys are still part of the documented schema even though they're no longer written; users with legacy notes still see them in YAML. The constant exists for grep-gate enforcement, not for runtime use.)

    B. **`src/graph/KnowledgeGraphWriter.ts` — drop runtime/memory parsing + passing:**
    - In `onAccepted` Step 1 (around lines 138-152): DELETE `const runtimeMs = parseRuntimeMs(terminal.status_runtime);` and `const memoryMb = parseMemoryMb(terminal.status_memory);`. Then DELETE the `runtimeMs,` and `memoryMb,` properties from the `applySolveTimeFrontmatter(this.app, ctx.file, { ... })` call object. Preserve `solvedAt`, `language`, `currentPassTags`.
    - DELETE the `parseRuntimeMs` and `parseMemoryMb` private helpers at the bottom of the file (around lines 218-244). Confirm via `grep -n 'parseRuntimeMs\|parseMemoryMb' src/` they have no other call sites.
    - Confirm the JSDoc at the top of the file (the comment block lines 12-15 that says "writes lc-status, lc-solved-date, lc-runtime-ms, lc-memory-mb, lc-language") gets updated: remove `lc-runtime-ms` and `lc-memory-mb` from that summary so future readers don't get misled. (This is documentation hygiene; not a behavior change.)

    C. **Test updates** — RESEARCH §Pitfall 8 mandates source change + test change in the same wave:
    - `tests/graph/onAccepted.frontmatter.test.ts`: at the line numbers `70`, `71`, `107`, `108`, `119` (per RESEARCH §"Outdated test assertion deletions" + PATTERNS §"tests/graph/onAccepted.frontmatter.test.ts") DELETE the `expect(fm!['lc-runtime-ms']).toBe(...)` and `expect(fm!['lc-memory-mb']).toBe(...)` assertions. If the surrounding `it(...)` block becomes empty, leave it with a single `expect(true).toBe(true)` placeholder OR (preferred) delete the entire orphaned `it(...)` block — your judgment: empty tests are not failing, they're noise. Update any nearby JSDoc / comment referring to the deleted assertions.
    - `tests/graph/onAccepted.reAccepted.test.ts`: at lines `39`, `40` DELETE the `'lc-runtime-ms': 8,` and `'lc-memory-mb': 13.1,` precondition fixture entries; at lines `67`, `68` DELETE the `expect(fm['lc-runtime-ms']).toBe(24); expect(fm['lc-memory-mb']).toBe(15.8);` post-condition assertions. Update line 7's comment from `// lc-runtime-ms and lc-memory-mb OVERWRITE...` to a comment describing the surviving overwrite semantics (`lc-status`, `lc-solved-date`, `lc-language`).
    - `tests/graph/fixture-integration.test.ts`: at lines `149`, `150` DELETE the `expect(fm['lc-runtime-ms']).toBe(2); expect(fm['lc-memory-mb']).toBeCloseTo(47.1, 1);` assertions.

    Run the full suite at the end (`npm test --run`) — if any assertion still references `lc-runtime-ms` or `lc-memory-mb`, the grep gate (acceptance criterion) catches it.
  </action>
  <verify>
    <automated>npx vitest run tests/graph tests/notes tests/solve 2>&amp;1 | tail -20 &amp;&amp; ! grep -RnE "lc-runtime-ms|lc-memory-mb" src/ &amp;&amp; ! grep -RnE "fm\\[\\s*['\"]lc-runtime-ms['\"]|fm\\[\\s*['\"]lc-memory-mb['\"]" tests/ &amp;&amp; ! grep -RnE "parseRuntimeMs|parseMemoryMb" src/</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -RnE "lc-runtime-ms|lc-memory-mb" src/` returns no matches (no production code mentions either key)
    - Source assertion: `grep -RnE "parseRuntimeMs|parseMemoryMb" src/` returns no matches (helpers deleted)
    - Source assertion: `grep -nE "fm\\[\\s*['\"]lc-runtime-ms['\"]|fm\\[\\s*['\"]lc-memory-mb['\"]" tests/` returns no `expect(...).toBe(...)` assertions (descriptive `it()` titles or JSDoc may still mention the keys for historical context — the search is narrowly scoped to the `fm[...]` access pattern)
    - Source assertion: `grep -E "lcSlugToFenceTag" src/notes/NoteTemplate.ts` returns at least one match (codeBlockFor uses the helper)
    - Source assertion: `grep -E "runtimeMs|memoryMb" src/notes/NoteTemplate.ts` returns no matches (interface fields deleted)
    - Source assertion: `grep -E "runtimeMs|memoryMb" src/graph/KnowledgeGraphWriter.ts` returns no matches (parse + pass deleted)
    - Behavior assertion: `codeBlockFor('python3', 'pass')` produces a string starting with `'```python\n'` (NOT `'```python3\n'`) — D-04 take-effect verification
    - Behavior assertion: `codeBlockFor('golang', 'package main')` produces a string starting with `'```go\n'`
    - Behavior assertion: `codeBlockFor('csharp', 'class Foo {}')` produces a string starting with `'```csharp\n'` (passthrough)
    - CLI: `npx vitest run tests/graph tests/notes tests/solve` exits 0 with all assertions green
    - CLI: `npx tsc --noEmit` produces no NEW TS errors beyond preexisting `tests/solve/SessionExpiredNotice.test.ts` issues
  </acceptance_criteria>
  <done>
    `applySolveTimeFrontmatter` writes only the surviving lc-* keys (lc-status, lc-solved-date, lc-language) plus tags. `KnowledgeGraphWriter.onAccepted` no longer parses or passes runtime/memory. New notes ship with markdown-recognized fence opener tags via `codeBlockFor → lcSlugToFenceTag`. The three tests/graph fixtures no longer assert removed writes. Full vitest suite is green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build + bundle-size verification (~149-155 KB target)</name>
  <files>main.js (build artifact), no source edits</files>
  <read_first>
    scripts/check-bundle-size.sh (current cap is 700/600 KB after Wave 0; will be tightened in a later step or wave),
    .planning/phases/05.3-language-aware-editor/05.3-CONTEXT.md (D-13 — script preserved),
    .planning/phases/05.3-language-aware-editor/05.3-RESEARCH.md (§Q7 — recommend tightening to 250/200 KB; planner may overrule. This plan does NOT change the cap; Wave 3 may.)
  </read_first>
  <action>
    Run `npm test --run` to confirm the full suite is green after Tasks 1-2. Then run `npm run build` to produce `main.js`. Then run `bash scripts/check-bundle-size.sh` (still at 700/600 KB; Wave 3 may tighten). Record the resulting `main.js` byte count; expectation per RESEARCH §A4 is ~150-155 KB (the post-Wave-0 baseline of 149 KB plus any incidental size from the new exports in `languages.ts` — three small constants and one helper, ~0.5 KB minified).

    Do NOT change `scripts/check-bundle-size.sh` — Wave 3 (the live-smoke + ship-gate plan) is the right place to revisit the cap, paired with the chevron's bundle contribution from Wave 2.

    Do NOT introduce the chevron, do NOT modify `codeActionsEditorExtension.ts`, do NOT modify `codeBlockButtonRow.ts` — those are Wave 2 concerns. This plan ships only the pure-data changes.
  </action>
  <verify>
    <automated>npm test --run 2>&amp;1 | tail -5 &amp;&amp; npm run build 2>&amp;1 | tail -3 &amp;&amp; bash scripts/check-bundle-size.sh &amp;&amp; SIZE=$(wc -c &lt; main.js) &amp;&amp; test "$SIZE" -lt 200000 &amp;&amp; test "$SIZE" -gt 100000</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `npm test --run` exits 0
    - CLI: `npm run build` exits 0 and produces `main.js`
    - CLI: `bash scripts/check-bundle-size.sh` exits 0
    - CLI: `wc -c < main.js` reports a value between 100,000 and 200,000 bytes (~149-155 KB; sanity range allowing for both build-artifact noise and the small additions from this plan)
    - CLI: `ls main.js*.js 2>/dev/null | wc -l` reports `0` (no chunk-split)
  </acceptance_criteria>
  <done>
    Tests pass, build succeeds, bundle size is in the expected ~149-155 KB range. Wave 1 ships only the pure-data changes; Wave 2 will add the chevron widget on top.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontmatter writer ↔ user vault | `applySolveTimeFrontmatter` writes via `app.fileManager.processFrontMatter` (atomic per Obsidian). Phase 5.3 narrows the write surface — this strictly REDUCES the boundary's footprint by removing two keys |
| (no new boundaries) | Wave 1 introduces no new network calls, no new persisted state, no new external integration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.3-02-01 | T (Tampering) | Pre-existing user notes have orphan `lc-runtime-ms` / `lc-memory-mb` entries | accept | Per RESEARCH §Pitfall 8 + §A7: zero production reader exists for either key; orphan values are inert YAML residue. No data migration needed |
| T-05.3-02-02 | I (Information disclosure) | The `lcSlugToFenceTag('python3')` write-time remap means `lc-language: python3` notes ship with ` ```python ` opener tags — could surprise a user who edits the fence opener manually | accept | UI-SPEC §"Coexistence with Phase 5.2" documents this; chevron widget (Wave 2) writes the remapped tag too, so the behavior is internally consistent. Reading-Mode Phase 5.2 alias still aliases `python3 → python` for legacy notes |
| T-05.3-02-03 | T (Tampering) | `codeBlockFor` is now non-pure-string-concat (calls `lcSlugToFenceTag`) — could break if `lcSlugToFenceTag` is replaced with non-pure later | mitigate | Test in tests/solve/languages.test.ts asserts purity (no I/O). languages.ts file header purity contract stays. Future drift would surface in vitest |
| T-05.3-02-04 | E (Elevation) | If `lcSlugToFenceTag` were given an attacker-controlled slug (e.g., from external API), passthrough behavior could let the slug into the fence opener verbatim | accept | Slug source is `LeetCodeClient.getProblemDetail` which already validates langSlug shape; LC's own slug vocabulary is closed. CONTEXT D-04 explicitly accepts passthrough for unsupported slugs (renders plain monospace). No attacker controls the input |
</threat_model>

<verification>
After completing all 3 tasks:
- `npx vitest run tests/solve/languages.test.ts tests/graph tests/notes` exits 0
- `grep -RnE "lc-runtime-ms|lc-memory-mb" src/` returns no matches
- `grep -RnE "parseRuntimeMs|parseMemoryMb" src/` returns no matches
- `src/solve/languages.ts` exports the four new symbols
- `src/notes/NoteTemplate.ts::codeBlockFor` calls `lcSlugToFenceTag`
- `npm run build` produces a `main.js` between 100-200 KB
- `bash scripts/check-bundle-size.sh` exits 0
</verification>

<success_criteria>
- Three new constants + one helper land in `src/solve/languages.ts` (purity preserved)
- `lcSlugToFenceTag` covers all 9 LC slugs in the D-04 table plus passthrough
- `LC_LANG_DISPLAY_LABELS` and `LC_CHEVRON_LANG_ORDER` are ready for Wave 2 to import
- `applySolveTimeFrontmatter` no longer writes lc-runtime-ms / lc-memory-mb
- `KnowledgeGraphWriter.onAccepted` no longer parses or passes runtime/memory
- New notes ship with markdown-recognized fence opener tags via `codeBlockFor → lcSlugToFenceTag`
- All three tests/graph fixtures + tests/solve/languages.test.ts pass
- Full vitest suite green
- Bundle size still in the ~149-155 KB range
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-language-aware-editor/05.3-02-SUMMARY.md` documenting:
- Final shape of `src/solve/languages.ts` new exports (with the actual constant values)
- Diff summary for `src/notes/NoteTemplate.ts` (interface + writer + codeBlockFor)
- Diff summary for `src/graph/KnowledgeGraphWriter.ts` (call site + helper deletions)
- Test-file edits (line ranges deleted/changed)
- Final main.js byte count
- Open assumptions A1 (markdown nested parser recognizes `python`/`java`/`cpp`/`javascript`/`typescript`/`go`/`rust`) — note that Wave 3 live-smoke validates these
</output>
