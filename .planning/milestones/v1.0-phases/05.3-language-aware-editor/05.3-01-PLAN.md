---
phase: 05.3-language-aware-editor
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/main/codeFenceLanguageExtension.ts
  - src/main/languagePackRegistry.ts
  - src/main/whitespaceCopyIndent.ts
  - tests/main/codeFenceLanguageExtension.test.ts
  - tests/main/languagePackRegistry.test.ts
  - tests/main/whitespaceCopyIndent.test.ts
  - src/main.ts
  - package.json
  - package-lock.json
autonomous: true
requirements: [POLISH-09]
must_haves:
  truths:
    - "Failed Compartment-swap implementation is deleted from the working tree"
    - "Six @codemirror/lang-* packs are uninstalled from package.json + node_modules"
    - "src/main.ts no longer imports buildCodeFenceLanguageExtension or warmDefaultPack"
    - "src/main.ts no longer registers the Step 6i editor extension"
    - "scripts/check-bundle-size.sh is preserved unchanged (D-13 explicit)"
    - "main.js bundle drops from 520 KB to ~149 KB after revert"
    - "npm test green; no broken-import errors"
  artifacts:
    - path: "src/main/codeFenceLanguageExtension.ts"
      provides: "ABSENT — file deleted"
    - path: "src/main/languagePackRegistry.ts"
      provides: "ABSENT — file deleted"
    - path: "src/main/whitespaceCopyIndent.ts"
      provides: "ABSENT — file deleted"
    - path: "tests/main/codeFenceLanguageExtension.test.ts"
      provides: "ABSENT — test file deleted"
    - path: "tests/main/languagePackRegistry.test.ts"
      provides: "ABSENT — test file deleted"
    - path: "tests/main/whitespaceCopyIndent.test.ts"
      provides: "ABSENT — test file deleted"
    - path: "src/main.ts"
      provides: "Plugin entry; lines 64-66 imports + lines 395-406 Step 6i block REMOVED"
      contains: "no codeFenceLanguageExtension import; no warmDefaultPack call"
    - path: "package.json"
      provides: "Dependencies after uninstall; six @codemirror/lang-* packs absent"
      contains: "no @codemirror/lang-python; no @codemirror/lang-java; no @codemirror/lang-cpp; no @codemirror/lang-javascript; no @codemirror/lang-go; no @codemirror/lang-rust"
    - path: "scripts/check-bundle-size.sh"
      provides: "PRESERVED unchanged (D-13)"
  key_links:
    - from: "src/main.ts"
      to: "(removed) ./main/codeFenceLanguageExtension"
      via: "import deleted"
      pattern: "no match for 'codeFenceLanguageExtension' in src/main.ts"
    - from: "src/main.ts"
      to: "(removed) ./main/languagePackRegistry"
      via: "import deleted"
      pattern: "no match for 'languagePackRegistry' or 'warmDefaultPack' in src/main.ts"
---

<objective>
Revert the failed D-10/D-11 Compartment-swap implementation per D-13. Forward-deletion (rm + uninstall + edit Step 6i) — not git revert — produces one focused commit with a clean audit trail. After this plan ships, the codebase is back to a chevron-ready baseline (~149 KB main.js, no @codemirror/lang-* packs, no Step 6i editor extension) so Wave 1 (frontmatter cleanup + fence-tag remap) and Wave 2 (chevron widget) can land cleanly.

Purpose: Wave 0 must complete BEFORE Waves 1-2 because (a) the deleted source files and tests are referenced by stale imports/test paths in the current tree; (b) bundle-size measurement at the end of Wave 1 only makes sense relative to the post-revert baseline; (c) future readers should not see the failed approach commingled with the chevron approach in the same commit history.

Output: Six deleted files, six fewer npm dependencies, src/main.ts with Step 6i removed, full vitest suite green, build emits ~149 KB main.js.
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.3-language-aware-editor/05.3-CONTEXT.md
@.planning/phases/05.3-language-aware-editor/05.3-RESEARCH.md
@.planning/phases/05.3-language-aware-editor/05.3-PATTERNS.md
@CLAUDE.md
@src/main.ts
@scripts/check-bundle-size.sh
</context>

<truths>
Locked CONTEXT decisions this plan implements:
- **D-13:** Revert and supersede in place. Delete the 3 implementation source files, the 3 test files, the Step 6i wiring in src/main.ts (lines 64-66 imports + lines 395-406 registration block), and uninstall the 6 `@codemirror/lang-*` packs. PRESERVE `scripts/check-bundle-size.sh` (useful infrastructure regardless of approach).

Per RESEARCH §"Revert order — concrete commands" — forward-deletion is preferred over `git revert` because (a) the failed implementation spans 4 commits including a 1-line setTimeout fix that makes no sense to revert in isolation; (b) `40b48b3` modified src/main.ts Step 6i which would need manual re-edit anyway; (c) one focused forward-deletion commit gives a cleaner audit trail.

Per RESEARCH §A8 — `languagePackRegistry.ts` is the only module that imports the 6 `@codemirror/lang-*` packs; deleting it is sufficient to remove all transitive references before uninstall.
</truths>

<must_haves>
- truths:
  - "Three failed-implementation source files (codeFenceLanguageExtension.ts, languagePackRegistry.ts, whitespaceCopyIndent.ts) no longer exist on disk"
  - "Three failed-implementation test files (codeFenceLanguageExtension.test.ts, languagePackRegistry.test.ts, whitespaceCopyIndent.test.ts) no longer exist on disk"
  - "src/main.ts has no import of codeFenceLanguageExtension; no import of languagePackRegistry; no import of warmDefaultPack"
  - "src/main.ts has no Step 6i registerEditorExtension(buildCodeFenceLanguageExtension(this)) call; no warmDefaultPack(this.settings.getDefaultLanguage()) call"
  - "package.json has no entry for @codemirror/lang-python, @codemirror/lang-java, @codemirror/lang-cpp, @codemirror/lang-javascript, @codemirror/lang-go, @codemirror/lang-rust"
  - "node_modules/@codemirror/ contains zero directories matching lang-*"
  - "scripts/check-bundle-size.sh exists unchanged (D-13 PRESERVE)"
  - "npm test passes (no broken imports / orphan test references)"
  - "npm run build emits a single main.js around 149 KB"
- artifacts: see frontmatter
- key_links: see frontmatter
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Delete failed-implementation source + test files and uninstall lang-* packs</name>
  <files>
    src/main/codeFenceLanguageExtension.ts (DELETE),
    src/main/languagePackRegistry.ts (DELETE),
    src/main/whitespaceCopyIndent.ts (DELETE),
    tests/main/codeFenceLanguageExtension.test.ts (DELETE),
    tests/main/languagePackRegistry.test.ts (DELETE),
    tests/main/whitespaceCopyIndent.test.ts (DELETE),
    package.json,
    package-lock.json
  </files>
  <read_first>
    .planning/phases/05.3-language-aware-editor/05.3-CONTEXT.md (D-13 lineage),
    .planning/phases/05.3-language-aware-editor/05.3-RESEARCH.md (§"Revert order — concrete commands"),
    src/main/languagePackRegistry.ts (verify it is the only module importing the 6 lang-* packs — RESEARCH §A8),
    package.json (verify the 6 entries exist before uninstall)
  </read_first>
  <action>
    Per D-13 forward-deletion plan: delete the 3 source files (`src/main/codeFenceLanguageExtension.ts`, `src/main/languagePackRegistry.ts`, `src/main/whitespaceCopyIndent.ts`) and the 3 corresponding test files (`tests/main/codeFenceLanguageExtension.test.ts`, `tests/main/languagePackRegistry.test.ts`, `tests/main/whitespaceCopyIndent.test.ts`) using `rm`. Then run `npm uninstall @codemirror/lang-python @codemirror/lang-java @codemirror/lang-cpp @codemirror/lang-javascript @codemirror/lang-go @codemirror/lang-rust` to remove the 6 packs from `package.json` + `package-lock.json` + `node_modules`. DO NOT TOUCH `scripts/check-bundle-size.sh` — D-13 explicit PRESERVE. DO NOT TOUCH `scripts/prerelease-check.sh` Gate 12 docstring (Wave 1 may revisit the cap separately; this task is purely deletion). After uninstall verify `ls node_modules/@codemirror/ | grep '^lang-' | wc -l` returns 0.
  </action>
  <verify>
    <automated>test ! -f src/main/codeFenceLanguageExtension.ts &amp;&amp; test ! -f src/main/languagePackRegistry.ts &amp;&amp; test ! -f src/main/whitespaceCopyIndent.ts &amp;&amp; test ! -f tests/main/codeFenceLanguageExtension.test.ts &amp;&amp; test ! -f tests/main/languagePackRegistry.test.ts &amp;&amp; test ! -f tests/main/whitespaceCopyIndent.test.ts &amp;&amp; ! grep -E '"@codemirror/lang-(python|java|cpp|javascript|go|rust)"' package.json &amp;&amp; test "$(ls node_modules/@codemirror/ 2>/dev/null | grep -c '^lang-')" = "0" &amp;&amp; test -f scripts/check-bundle-size.sh</automated>
  </verify>
  <acceptance_criteria>
    - Source assertions: all six target files (3 src + 3 test) absent from working tree
    - Source assertion: `package.json` has no `"@codemirror/lang-python"`, `"@codemirror/lang-java"`, `"@codemirror/lang-cpp"`, `"@codemirror/lang-javascript"`, `"@codemirror/lang-go"`, `"@codemirror/lang-rust"` entries
    - CLI: `ls node_modules/@codemirror/ | grep -c '^lang-'` outputs `0`
    - Source assertion: `scripts/check-bundle-size.sh` still exists (D-13 PRESERVE)
    - CLI: `find . -path ./node_modules -prune -o -type f \( -name '*.ts' -o -name '*.tsx' -o -name '*.js' -o -name '*.cjs' -o -name '*.mjs' \) -print | xargs grep -l '@codemirror/lang-' 2>/dev/null` outputs nothing (no source still imports a deleted pack)
  </acceptance_criteria>
  <done>
    Six target files are deleted; six packs are uninstalled; scripts/check-bundle-size.sh is unchanged; no source file imports any deleted pack.
  </done>
</task>

<task type="auto">
  <name>Task 2: Strip Step 6i wiring from src/main.ts</name>
  <files>src/main.ts</files>
  <read_first>
    src/main.ts (current state, especially lines 60-66 imports block + lines 393-406 Step 6h/6i block),
    .planning/phases/05.3-language-aware-editor/05.3-RESEARCH.md (§"Revert order — concrete commands" item 3),
    .planning/phases/05.3-language-aware-editor/05.3-PATTERNS.md (§"src/main.ts" — exact lines to delete)
  </read_first>
  <action>
    Edit `src/main.ts`. Remove the two Phase 5.3 (POLISH-09) imports near the top of the file: the line that imports `buildCodeFenceLanguageExtension` from `./main/codeFenceLanguageExtension` AND the line that imports `warmDefaultPack` from `./main/languagePackRegistry` AND any associated comment line that introduces the Phase 5.3 import block (the `// Phase 5.3 (POLISH-09) — Edit-Mode language-aware editor for ## Code fence.` comment). Then remove the entire `Step 6i` block in `onload()` — that is the multi-line comment introducing Step 6i (`Step 6i — Phase 5.3 (POLISH-09) language-aware editor for ## Code fence. ...`) AND the two statements `this.registerEditorExtension(buildCodeFenceLanguageExtension(this));` AND `warmDefaultPack(this.settings.getDefaultLanguage());`. Do NOT remove Step 6h (Phase 5.2 `registerPython3Highlighter`) or any other step — only the Step 6i block. Confirm Step 6h immediately precedes the deletion site and the deletion site immediately precedes the `void this.checkLegacyLeetcodeBase().catch(...)` call.

    Per D-13 PRESERVE: do NOT touch `scripts/check-bundle-size.sh` or `scripts/prerelease-check.sh` from this task. Do NOT remove the Phase 5.2 `python3Highlighter` import (Step 6h owns its own) — that is unrelated to Phase 5.3.

    Run `npx tsc --noEmit` after the edits to confirm no orphan-import or undefined-symbol errors. Keep the surrounding 6e/6f/6g/6h Step structure exactly as it was — only the 6i block is excised.
  </action>
  <verify>
    <automated>! grep -E 'codeFenceLanguageExtension|languagePackRegistry|warmDefaultPack' src/main.ts &amp;&amp; ! grep -E 'Step 6i|POLISH-09' src/main.ts &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -v 'tests/solve/SessionExpiredNotice' | grep -E 'error TS' &amp;&amp; echo "TS errors found" &amp;&amp; exit 1 || true</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/main.ts` does not contain the strings `codeFenceLanguageExtension`, `languagePackRegistry`, `warmDefaultPack`, `Step 6i`, or `POLISH-09`
    - Source assertion: `src/main.ts` still contains `Step 6h` (Phase 5.2 python3Highlighter must NOT be removed) and `registerPython3Highlighter`
    - Source assertion: `src/main.ts` still contains `void this.checkLegacyLeetcodeBase()` (the line after the removed Step 6i block must still exist)
    - CLI: `npx tsc --noEmit` produces no NEW TS errors beyond the pre-existing TS18048 errors in `tests/solve/SessionExpiredNotice.test.ts` (those are out-of-scope per UAT.md preflight)
    - CLI: `grep -nE '@codemirror/lang-' src/main.ts` returns no matches
  </acceptance_criteria>
  <done>
    src/main.ts contains no Phase 5.3 imports, no Step 6i block, and compiles cleanly with the deleted modules absent.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build + verify bundle returns to baseline</name>
  <files>main.js (build artifact), no source edits</files>
  <read_first>
    scripts/check-bundle-size.sh (current cap is 700/600 KB — Wave 1 may tighten this; do NOT change it in this plan),
    .planning/phases/05.3-language-aware-editor/05.3-CONTEXT.md (D-13 PRESERVE clause),
    .planning/phases/05.3-language-aware-editor/05.3-01-SUMMARY.md.deprecated.md or any 05.3-01-SUMMARY.md if present (pre-install baseline 149,167 bytes per RESEARCH §A4)
  </read_first>
  <action>
    Run `npm test --run` to confirm the surviving Phase 5.1 / 5.2 / 5 / 4 / 3 / 2 / 1 unit tests are green after the deletions in Tasks 1-2. Then run `npm run build` to produce `main.js`. Then run `bash scripts/check-bundle-size.sh` (still at the 700 KB hard / 600 KB warn cap from before — Wave 1 will revisit). Record the resulting `main.js` byte count; expectation per RESEARCH §A4 is ~149 KB (149,167 bytes ± 5 KB) — the pre-Phase-5.3 baseline. If size is materially higher (>200 KB) without an obvious cause, investigate before declaring this plan done. If the test run fails, the most likely cause is a stale import in a non-deleted test file referring to one of the three removed modules; fix by inspecting the failing test's imports and either deleting the test (if it was specifically about the failed implementation) or removing the broken import (if a regression test pulled in `findCodeFence` from a deleted module — though `findCodeFence` lives in `codeActionsEditorExtension.ts`, not in any of the three deleted files, so this should not happen).

    Do NOT change `scripts/check-bundle-size.sh` here — Wave 1 / 3 may tighten the cap. The script must still exit 0 against the new ~149 KB output (149 KB ≤ 700 KB hard cap), which is the correctness signal for this task.

    Do NOT add a new test, do NOT introduce the chevron, do NOT modify languages.ts — those are Wave 1 / Wave 2 concerns.
  </action>
  <verify>
    <automated>npm test --run 2>&amp;1 | tail -5 &amp;&amp; npm run build 2>&amp;1 | tail -3 &amp;&amp; bash scripts/check-bundle-size.sh &amp;&amp; test "$(wc -c < main.js)" -lt 200000</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `npm test --run` exits 0 (all surviving tests green)
    - CLI: `npm run build` exits 0 and produces `main.js`
    - CLI: `bash scripts/check-bundle-size.sh` exits 0 (still at 700 KB hard cap)
    - CLI: `wc -c < main.js` reports a value &lt; 200,000 bytes (expected ~149 KB; well under the still-700 KB cap)
    - CLI: `ls main.js*.js 2>/dev/null | wc -l` reports `0` (no chunk-split files; esbuild emits a single main.js)
  </acceptance_criteria>
  <done>
    Tests pass, build succeeds, bundle size is back near the 149 KB pre-Phase-5.3 baseline. Wave 0 revert is complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Wave 0 is purely deletion; introduces no new code paths, no new network calls, no new persisted state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.3-01-01 | T (Tampering) | npm uninstall touches package-lock.json | accept | Same risk every npm install/uninstall has; verified via `npm test` and bundle size after Task 3 |
| T-05.3-01-02 | I (Information disclosure) | Deleted .ts files retain content in git history | accept | Phase 5.3 chose forward-deletion specifically to keep the failed-approach evidence in git history (D-13 + RESEARCH §Q6); no secrets or PII in the deleted files |
| T-05.3-01-03 | D (Denial of service) | Stale node_modules/@codemirror/lang-* could remain after npm uninstall | mitigate | Task 1 acceptance includes `ls node_modules/@codemirror/ | grep -c '^lang-'` check explicitly verifying directories are gone |
</threat_model>

<verification>
After completing all 3 tasks:
- `npm test --run` exits 0
- `npm run build` produces a single `main.js` &lt; 200 KB
- `bash scripts/check-bundle-size.sh` exits 0
- `grep -RnE '@codemirror/lang-(python|java|cpp|javascript|go|rust)' src/ tests/` returns no matches
- `grep -nE 'codeFenceLanguageExtension|languagePackRegistry|warmDefaultPack' src/main.ts` returns no matches
- `scripts/check-bundle-size.sh` is unchanged (D-13 PRESERVE)
</verification>

<success_criteria>
- Six failed-implementation files deleted (3 src + 3 test)
- Six @codemirror/lang-* npm packs uninstalled
- src/main.ts Step 6i imports + registration block removed
- Bundle returns to ~149 KB pre-Phase-5.3 baseline
- Full test suite green
- scripts/check-bundle-size.sh PRESERVED unchanged
- Plan 02 (Wave 1) can begin without import-cycle / orphan-test issues
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-language-aware-editor/05.3-01-SUMMARY.md` documenting:
- Exact files deleted
- Exact packs uninstalled (with version numbers from package.json before uninstall)
- src/main.ts diff summary (which lines were removed)
- Final main.js byte count
- npm test summary (count of tests pre/post)
- Confirmation scripts/check-bundle-size.sh is byte-identical to before this plan
</output>
