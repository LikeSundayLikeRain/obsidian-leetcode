---
phase: 05.2-pre-ship-ux-polish
plan: 02
subsystem: ui
tags: [obsidian, settings, leetcode, language-list, setting-row-layout]

# Dependency graph
requires:
  - phase: 05.2-pre-ship-ux-polish
    provides: Wave 0 RED test shell at tests/settings/SettingsTab.test.ts asserting D-12 pinned LC 2026 language key set + locked labels (python3/python/cpp)
provides:
  - Single merged Status+Logout row in SettingsTab.ts via Obsidian's native Setting flex layout (D-01)
  - Exported LANGUAGE_OPTIONS constant with 19-entry pinned LC 2026 language list and locked display labels (D-12)
affects: [05.2-06-live-smoke-validation, "downstream Plans that consume LANGUAGE_OPTIONS"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Obsidian Setting flex layout: `.setting-item-info` (left: name/desc) + `.setting-item-control` (right: button) via a single Setting.addButton() — no custom CSS"
    - "File-scope `export const` for UI option maps so tests can pin exact key sets"

key-files:
  created: []
  modified:
    - src/settings/SettingsTab.ts
    - tests/settings/SettingsTab.test.ts

key-decisions:
  - "D-01: Use Obsidian's built-in Setting flex cells (single Setting.addButton()) instead of a custom flexbox CSS class — matches the plan's UI-SPEC Q1 LOCKED decision"
  - "D-12: Mirror LC's current 19-entry submission-language list (algorithm scope); SQL dialects intentionally excluded; static pin (no dynamic fetch) per RESEARCH §Item 8"
  - "D-12 label: python3 renders as 'Python3' (was previously mis-labeled 'Python') to disambiguate from the python2 `python` slug"
  - "No migration code for legacy persisted defaultLanguage slugs — T-05.2-03 accepted: dropdown renders empty if persisted slug dropped, user re-selects"

patterns-established:
  - "Pattern: merged Setting row with conditional .addButton() — preferred over parallel two-row Status/button layout when name+desc is short and the action is singular"
  - "Pattern: LANGUAGE_OPTIONS is the single source of truth for the default-language dropdown; insertion order = rendered order"

requirements-completed: [POLISH-08]

# Metrics
duration: 8min
completed: 2026-05-11
---

# Phase 5.2 Plan 05.2-02: Settings Status+Logout merge + LC 2026 language list Summary

**Merged the Status row and Login/Logout button into a single Setting row via Obsidian's native flex layout, and expanded the default-language dropdown from 5 placeholder entries to the pinned 19-entry LC 2026 submission-language list with locked display labels.**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-05-11T17:31:09Z
- **Completed:** 2026-05-11T17:38:23Z
- **Tasks:** 2 (both auto-executed, TDD for Task 2)
- **Files modified:** 2 (src/settings/SettingsTab.ts, tests/settings/SettingsTab.test.ts)

## Accomplishments

- **D-01 Status+Logout merge:** Two-Setting Status block collapsed into a single `Setting(containerEl).setName('Status').setDesc(statusText).addButton(...)` row. Name+Desc render on the left via `.setting-item-info`; button renders on the right via `.setting-item-control`. No custom CSS needed — Obsidian's built-in flex layout does the work.
- **D-12 Language list expansion:** `LANGUAGE_OPTIONS` replaced with the 19-entry LC 2026 pinned list (python3, python, java, cpp, c, csharp, javascript, typescript, php, swift, kotlin, dart, golang, ruby, scala, rust, racket, erlang, elixir). SQL dialects excluded per D-12 scope. Constant now `export`ed so tests can pin it.
- **Label fix (latent bug):** The old `python3` label was `'Python'`, visually identical to `python` in the dropdown. New label is `'Python3'`, disambiguating it from `python` ('Python'). Labels for `cpp` ('C++'), `csharp` ('C#'), `golang` ('Go') were never present in the old 5-entry list — locked in now.
- **TDD RED → GREEN:** The two Wave 0 shells in `tests/settings/SettingsTab.test.ts` went from `it.skip` → RED (export missing) → GREEN (implementation committed). Committed as `test(...)` (RED) then `feat(...)` (GREEN) to preserve the gate sequence.

## Task Commits

Each task was committed atomically. Task 2 followed TDD (test commit, then feat commit):

1. **Task 1: Merge Status + Logout rows (D-01)** — `f78a8da` (feat)
2. **Task 2 RED: Unskip D-12 LANGUAGE_OPTIONS shells** — `4b8405f` (test)
3. **Task 2 GREEN: Expand LANGUAGE_OPTIONS to LC pinned list (D-12)** — `79e47a7` (feat)

Plus setup commits (Wave 0 test shells cherry-picked from parallel agent's worktree):

- `763c869` (test) — Wave 0 RED shells for FilterModal D-02/D-03/D-04 (cherry-picked, not in scope of this plan but pulled in so `tests/settings/SettingsTab.test.ts` was available to unskip)
- `7e74fc4` (test) — Wave 0 RED shells for settings/python3/copy/file-open/reset (cherry-picked)

## Files Created/Modified

- `src/settings/SettingsTab.ts` — D-01 merged Status row (lines 44–73), D-12 exported 19-entry LANGUAGE_OPTIONS (lines 14–39)
- `tests/settings/SettingsTab.test.ts` — removed `.skip` on both D-12 shells, removed the RED-state header comment

## Decisions Made

- **Extended `.setCta()` gate awareness**: The plan's grep gate (`grep -c '\.setCta()'` → 1) counts substring matches regardless of comment context. When I added a comment mentioning `.setCta()` literally, the count rose to 2. I rewrote the comment to say "call-to-action modifier" instead, matching the original file's convention. The invariant survives as intended.
- **Two commits for Task 2 instead of one**: Wave 0 shipped the test shells `it.skip`'d. Proper TDD for Task 2 means (a) unskipping the shells as a `test(...)` commit that is expected to fail, then (b) shipping the production change as a `feat(...)` commit. This preserves the RED→GREEN gate sequence and makes the TDD intent explicit in git history.
- **Cherry-picked Wave 0 test shells from sibling agent's branch**: The orchestrator's prompt stated "Wave 0 already shipped (merged into this worktree's base)" but the test file was not present on my branch — Wave 0 lived on `worktree-agent-adb45069c006615ba` only. I cherry-picked the two `test(...)` commits (`b59f8bf`, `9cbfc90`) onto my branch to make the success criterion achievable. I did NOT cherry-pick the `docs(...)` STATE.md/ROADMAP.md update since my prompt explicitly forbade modifying those files.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] Cherry-picked Wave 0 test shells onto this branch**

- **Found during:** Initial plan load — `tests/settings/SettingsTab.test.ts` was claimed to exist in my prompt but was missing from the worktree filesystem.
- **Issue:** My worktree branch was created from main (`0f4cca4`) before Wave 0 (plan 05.2-01) landed on any reachable branch. Wave 0 test shells existed only on a sibling agent's worktree branch (`worktree-agent-adb45069c006615ba`). Success criterion "`tests/settings/SettingsTab.test.ts` GREEN" was unachievable without those shells.
- **Fix:** `git cherry-pick b59f8bf 9cbfc90` — pulled in the two `test(...)` commits that added the Wave 0 shells across the full Wave 0 set (8 new test files + obsidian-stub extension). Did NOT cherry-pick the `docs(05.2-01)` plan-completion commit (would have touched STATE.md/ROADMAP.md, explicitly forbidden).
- **Files modified:** 9 files added under `tests/` (all Wave 0 RED shells, 8 of which are out of scope for this plan's assertions but needed to be present for the shared obsidian-stub extension to resolve).
- **Verification:** `npm test` → 479 passed / 20 skipped / 0 failed. All cherry-picked shells load and skip/fail as Wave 0 intended.
- **Committed in:** `763c869`, `7e74fc4` (cherry-picks; preserved original author info).

**2. [Rule 1 — Bug] Old `python3` label rendered as 'Python', identical to forthcoming `python` entry**

- **Found during:** Task 2 (reading the old 5-entry LANGUAGE_OPTIONS).
- **Issue:** The original `python3: 'Python'` label was a latent bug: once `python` slug was added (per D-12), two rows in the dropdown would both render as "Python" and the user couldn't distinguish them. D-12's locked labels (python3→'Python3', python→'Python') fix this, but the plan frames it as D-12 scope, not a bug fix — it's both.
- **Fix:** Applied D-12's label map exactly — python3 is now 'Python3', python is 'Python'. (This was part of the plan's explicit work, so arguably not a deviation, but flagging because the label change alone would also have been a Rule 1 fix had D-12 not specified it.)
- **Files modified:** src/settings/SettingsTab.ts (LANGUAGE_OPTIONS values).
- **Verification:** The D-12 test asserts `expect(mod.LANGUAGE_OPTIONS.python3).toBe('Python3')` — GREEN.
- **Committed in:** `79e47a7` (Task 2 GREEN).

---

**Total deviations:** 2 (1 Rule 3 blocking — cherry-pick; 1 Rule 1 bug — included naturally in D-12 fix).
**Impact on plan:** No scope creep. Deviation 1 was necessary infrastructure to run the plan at all. Deviation 2 is subsumed by D-12's intended change.

## Issues Encountered

- **Stale orchestrator context about Wave 0 state** — see deviation 1. Resolved via cherry-pick.
- **Pre-existing tsc errors in `tests/main/*`, `tests/solve/*`** — `Object is possibly 'undefined'` warnings in other test files. Verified these exist on main (via `git stash` → `npx tsc --noEmit`). Not my changes, not in scope, left alone. Logged for future cleanup if the team prefers strict type gating in tests.
- **Pre-existing lint errors in `tests/solve/*`** — 88 total (75 errors, 13 warnings). Verified same count (actually 90, 2 fewer after my change — the merged Setting row eliminated some instances). All in files I did not touch. Not in scope; not regressions.

## Self-Check: PASSED

- [x] `src/settings/SettingsTab.ts` present and modified — FOUND
- [x] `tests/settings/SettingsTab.test.ts` present and tests unskipped — FOUND (2 tests, 0 skipped)
- [x] `f78a8da` (Task 1 D-01) — FOUND in git log
- [x] `4b8405f` (Task 2 RED) — FOUND in git log
- [x] `79e47a7` (Task 2 GREEN) — FOUND in git log
- [x] Grep gates: `.setCta()` = 1; `export const LANGUAGE_OPTIONS` present; all 19 slugs present
- [x] Test suite: 479 passed / 20 skipped / 0 failed
- [x] Lint: 0 new violations from my changes

## Threat Flags

None. The threat model in the plan (T-05.2-03/04/05) was mitigated as designed: `.setCta()` grep gate holds at 1; persisted `defaultLanguage` slug preserved on dropdown-key-miss (no crash); logout button wired to `plugin.auth.logout()` explicitly (spoofing T-05.2-05 addressed).

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **Plan 05.2-06 (live-smoke validation)** can now smoke-test the merged Status row visually (D-01 has no automated test per plan — visual verification is the proof).
- **Downstream consumers of LANGUAGE_OPTIONS** (e.g., starter-code injector, editor default-language resolver) can now `import { LANGUAGE_OPTIONS } from '../settings/SettingsTab'` if they need the canonical list.
- **No blockers.** Wave 0 test shells for the remaining Wave 1 plans (05.2-03/04/05) remain `it.skip`'d until those plans ship.

---
*Phase: 05.2-pre-ship-ux-polish*
*Completed: 2026-05-11*
