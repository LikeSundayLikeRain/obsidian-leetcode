---
phase: 05-polish-ship
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/settings/SettingsStore.techniques-override.test.ts
  - tests/settings/SettingsTab.knowledge-graph.test.ts
  - tests/api/throttle.rate-limit-retry.test.ts
  - tests/api/throttle.timeout.test.ts
  - tests/shared/errors.isNetworkError.test.ts
  - tests/solve/SessionExpiredNotice.test.ts
  - tests/solve/ephemeralTabStore.test.ts
  - tests/solve/RunModal.test.ts
  - tests/main/codeActionsPostProcessor.test.ts
  - tests/solve/run-command-registration.test.ts
  - tests/solve/mocks/fakeSettingsStore.ts
  - tests/solve/mocks/fakeWorkspace.ts
autonomous: true
requirements: [POLISH-01, POLISH-02, POLISH-07]
must_haves:
  truths:
    - "Every Phase 5 behavior-changing task in Waves 1-5 has a pre-existing failing test file it can target (Nyquist compliance)"
    - "fakeSettingsStore mock carries a techniquesFolderOverride field"
    - "fakeWorkspace mock exposes getLeavesOfType + workspace.on('layout-change') + workspace.on('active-leaf-change') stubs"
  artifacts:
    - path: "tests/settings/SettingsStore.techniques-override.test.ts"
      provides: "failing test stub for POLISH-01 override field"
      contains: "techniquesFolderOverride"
    - path: "tests/settings/SettingsTab.knowledge-graph.test.ts"
      provides: "failing test stub for POLISH-01 new section"
      contains: "Knowledge Graph"
    - path: "tests/api/throttle.rate-limit-retry.test.ts"
      provides: "failing test stub for POLISH-02 D-18"
      contains: "rate limit"
    - path: "tests/api/throttle.timeout.test.ts"
      provides: "failing test stub for POLISH-02 D-20"
      contains: "TimeoutError"
    - path: "tests/shared/errors.isNetworkError.test.ts"
      provides: "failing test stub for POLISH-02 D-19"
      contains: "ERR_NAME_NOT_RESOLVED"
    - path: "tests/solve/SessionExpiredNotice.test.ts"
      provides: "failing test stub for POLISH-02 D-21"
      contains: "showSessionExpiredNotice"
    - path: "tests/solve/ephemeralTabStore.test.ts"
      provides: "failing test stub for POLISH-07 D-02/D-09"
      contains: "EphemeralTabStore"
    - path: "tests/solve/RunModal.test.ts"
      provides: "failing test stub for POLISH-07 D-03/D-05/D-07"
      contains: "RunModal"
    - path: "tests/main/codeActionsPostProcessor.test.ts"
      provides: "failing test stub for D-11/D-12/D-13"
      contains: "registerCodeBlockActionProcessor"
    - path: "tests/solve/run-command-registration.test.ts"
      provides: "failing test stub for D-01 command-set change"
      contains: "run-sample"
    - path: "tests/solve/mocks/fakeSettingsStore.ts"
      provides: "mock helper extended with techniquesFolderOverride"
      contains: "techniquesFolderOverride"
    - path: "tests/solve/mocks/fakeWorkspace.ts"
      provides: "workspace event + getLeavesOfType mock helper"
      contains: "getLeavesOfType"
  key_links:
    - from: "tests/solve/RunModal.test.ts"
      to: "src/solve/RunModal.ts"
      via: "dynamic import"
      pattern: "import.*RunModal"
    - from: "tests/solve/ephemeralTabStore.test.ts"
      to: "src/solve/ephemeralTabStore.ts"
      via: "dynamic import"
      pattern: "import.*EphemeralTabStore|ephemeralTabStore"
    - from: "tests/settings/SettingsStore.techniques-override.test.ts"
      to: "src/settings/SettingsStore.ts"
      via: "import"
      pattern: "getTechniquesFolderOverride|setTechniquesFolderOverride"
---

<objective>
Wave 0: Create the 10 failing-test files + 2 mock helper files enumerated in
`05-VALIDATION.md §Wave 0 Requirements`. Every Waves 1-5 task references one of
these tests in its `<automated>` verify command; creating the stubs first satisfies
Nyquist compliance (no `MISSING` references in downstream plans).

Purpose: Seed the RED half of every Phase 5 behavior-changing task so downstream
plans can target a real test path, not a placeholder.
Output: 10 failing `.test.ts` files + 2 mock helper files committed in a single
atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-ship/05-CONTEXT.md
@.planning/phases/05-polish-ship/05-RESEARCH.md
@.planning/phases/05-polish-ship/05-UI-SPEC.md
@.planning/phases/05-polish-ship/05-PATTERNS.md
@.planning/phases/05-polish-ship/05-VALIDATION.md

<interfaces>
<!-- Test infrastructure the executor reuses. All patterns pre-verified in PATTERNS.md. -->

From tests/solve/throttled-request-url.test.ts (module-level mock pattern — PATTERNS §Shared Patterns):
```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
const requestUrlMock = vi.fn();
vi.mock('obsidian', () => ({
  requestUrl: (params: unknown) => requestUrlMock(params),
}));
// beforeEach: requestUrlMock.mockReset(); vi.resetModules();
// In-test: const { throttledRequestUrl } = await import('../../src/api/requestUrlFetcher');
```

From tests/graph/SubmissionDetailModal.test.ts (happy-dom modal pattern — PATTERNS §Shared Patterns):
```typescript
import { describe, it, expect, vi } from 'vitest';
import { makeMockVaultApp } from '../helpers/mock-vault';
// Modal under test is constructed with mock App + deps, then onOpen/onClose exercised.
```

From tests/settings-store.test.ts (store round-trip pattern):
- Build `makeMockPlugin` + call `SettingsStore.load()` → assert data-shape; setter + reload → assert round-trip.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 10 failing test stubs covering every Waves 1-5 verify command</name>
  <read_first>
    - .planning/phases/05-polish-ship/05-VALIDATION.md §Wave 0 Requirements (the authoritative list)
    - .planning/phases/05-polish-ship/05-PATTERNS.md §Test files (analogs + exact mock shapes for every new file)
    - tests/solve/throttled-request-url.test.ts (module-level vi.mock('obsidian') pattern to mirror)
    - tests/graph/SubmissionDetailModal.test.ts (happy-dom modal pattern + makeMockVaultApp usage)
    - tests/settings-store.test.ts (store round-trip pattern)
    - tests/helpers/mock-vault.ts (helper already used by SubmissionDetailModal.test.ts)
  </read_first>
  <files>
    tests/settings/SettingsStore.techniques-override.test.ts,
    tests/settings/SettingsTab.knowledge-graph.test.ts,
    tests/api/throttle.rate-limit-retry.test.ts,
    tests/api/throttle.timeout.test.ts,
    tests/shared/errors.isNetworkError.test.ts,
    tests/solve/SessionExpiredNotice.test.ts,
    tests/solve/ephemeralTabStore.test.ts,
    tests/solve/RunModal.test.ts,
    tests/main/codeActionsPostProcessor.test.ts,
    tests/solve/run-command-registration.test.ts
  </files>
  <behavior>
    Each stub MUST fail now and turn green exactly once its companion Waves 1-5 task
    ships. Every file uses `describe` + `it` + `expect`. Stubs target the exact
    behavior contract described in the referenced D-XX; they are written
    intentionally to FAIL so downstream plans can turn them green.
  </behavior>
  <action>
    Use the Write tool (never `cat << EOF`). For each file below, use the analog and
    pattern pointer listed. All new test files use vitest 4.1.5 APIs.

    1. `tests/settings/SettingsStore.techniques-override.test.ts`
       - Analog: tests/settings-store.test.ts.
       - Target behavior: `techniquesFolderOverride` round-trip via `SettingsStore.load` + `setTechniquesFolderOverride`.
       - Tests (minimum 3): (a) default empty string when absent from data.json; (b) non-string raw value coerces to empty string; (c) when non-empty, `getTechniquesFolder()` returns override verbatim, when empty it returns `${problemsFolder}/Techniques` (D-15).
       - Will fail today because getters `getTechniquesFolderOverride`/`setTechniquesFolderOverride` do not exist. Do NOT implement them here — that is Plan 02's job.

    2. `tests/settings/SettingsTab.knowledge-graph.test.ts`
       - Analog: tests/graph/SubmissionDetailModal.test.ts for happy-dom + the `makeMockPlugin` shape in tests/settings-store.test.ts.
       - Target behavior: after `tab.display()` against a happy-dom container, assert heading element with text `Knowledge Graph` exists, a text input bound to `techniquesFolderOverride` exists, and a toggle bound to `autoBacklinksEnabled` exists (D-14, D-15, D-16).
       - Tests (minimum 3): (a) `Knowledge Graph` heading rendered via `setHeading()`; (b) override `<input>` has placeholder `${problemsFolder}/Techniques`; (c) toggle initial value binds to `getAutoBacklinksEnabled()`.

    3. `tests/api/throttle.rate-limit-retry.test.ts`
       - Analog: tests/solve/throttled-request-url.test.ts (exact mock shape).
       - Target behavior: D-18 single retry after 5s backoff.
       - Tests (minimum 2): (a) first call returns 429 → `vi.useFakeTimers()` + `vi.advanceTimersByTimeAsync(5000)` → second call resolves; assert `requestUrlMock` called exactly 2 times; (b) second 429 after retry re-throws `RateLimitError`; assert the import path throws.
       - Use `RATE_LIMIT_RETRY_MS = 5000` constant literally (D-18 locked).

    4. `tests/api/throttle.timeout.test.ts`
       - Analog: tests/solve/throttled-request-url.test.ts.
       - Target behavior: D-20 10s `Promise.race` timeout.
       - Tests (minimum 2): (a) call that never resolves → `vi.advanceTimersByTimeAsync(10_000)` → promise rejects with `TimeoutError` (err.name === 'TimeoutError'); (b) opts.timeoutMs override honored (e.g., 20_000 keeps call alive past 10_000).

    5. `tests/shared/errors.isNetworkError.test.ts`
       - Analog: tests/solve/errors.test.ts.
       - Target behavior: helper classifies 9 Chromium `ERR_*` tokens (RESEARCH §Example 2).
       - Tests (minimum 3): (a) each of 9 tokens → true; (b) plain Error with unrelated message → false; (c) non-Error value (string / null / undefined) → false.
       - Tokens: `ERR_NAME_NOT_RESOLVED`, `ERR_CONNECTION_REFUSED`, `ERR_INTERNET_DISCONNECTED`, `ERR_NETWORK_CHANGED`, `ERR_CONNECTION_CLOSED`, `ERR_CONNECTION_RESET`, `ERR_CONNECTION_TIMED_OUT`, `ERR_PROXY_CONNECTION_FAILED`, `ERR_NAME_RESOLUTION_FAILED`.

    6. `tests/solve/SessionExpiredNotice.test.ts`
       - Analog: tests/graph/SubmissionDetailModal.test.ts (happy-dom DOM assertions).
       - Target behavior: D-21 DocumentFragment-based notice with Log in button (RESEARCH Pattern 1).
       - Mock `Notice` as a class that captures constructor args; expose `messageEl` + `hide()` spy.
       - Tests (minimum 3): (a) constructs `Notice(frag, 0)` where frag contains both the CF-04 locked text `LeetCode session expired. Log in again.` AND a `<button class="leetcode-notice-action mod-cta">Log in</button>`; (b) clicking the button calls the injected `login` callback; (c) clicking the button calls `notice.hide()` before invoking login (sequence).

    7. `tests/solve/ephemeralTabStore.test.ts`
       - Analog: tests/settings-store.test.ts (store round-trip pattern) + use fakeWorkspace mock from Task 2.
       - Target behavior: D-02/D-09 lifecycle.
       - Tests (minimum 5): (a) `getOrSeed(slug, exampleTestcases)` seeds tabs split on blank lines; (b) second `getOrSeed` for same slug returns existing state; (c) `resetToSamples` wipes + re-seeds; (d) `setTabs` round-trips; (e) when fakeWorkspace reports zero leaves showing the slug (via simulated `layout-change` fire), slug state is wiped; reopening via `getOrSeed` re-seeds.
       - Slug should persist when leaves count > 0 (popout windows still count — RESEARCH §Open Question 5).

    8. `tests/solve/RunModal.test.ts`
       - Analog: tests/graph/SubmissionDetailModal.test.ts (modal happy-dom pattern).
       - Target behavior: D-03/D-05/D-07 — seed, reset, single-active-tab Run.
       - Tests (minimum 5): (a) modal opens with tabs seeded from `exampleTestcases` via store; (b) Reset button wipes + re-seeds from `exampleTestcases`; (c) clicking `Run` button passes ONLY the active tab's input to `onRun` callback (assert NOT concatenated); (d) `×` delete hidden when tabs.length === 1 (D-06); (e) onClose pushes current tabs back to `store.setTabs` (no vault writes).
       - Use fakeWorkspace + mock `EphemeralTabStore` (stub `getOrSeed`/`setTabs`/`resetToSamples`).

    9. `tests/main/codeActionsPostProcessor.test.ts`
       - Analog: tests/graph/SubmissionDetailModal.test.ts (DOM + frontmatter gate).
       - Target behavior: D-11/D-12/D-13 — reading-mode MDP gated on `lc-slug`, idempotent, dispatches fully-qualified command IDs.
       - Tests (minimum 4): (a) no-op when frontmatter lacks `lc-slug` (D-12); (b) appends `<div class="leetcode-code-actions">` with two buttons (`Run` + `Submit`) after `<pre>` when `lc-slug` present (D-13); (c) processor invoked twice on the same element does NOT duplicate the div (Pitfall 3); (d) clicking Run dispatches `app.commands.executeCommandById('leetcode:run')` (Pitfall 14 — fully qualified with `plugin.manifest.id`).

   10. `tests/solve/run-command-registration.test.ts`
       - Analog: none direct; scaffold against the `Plugin` interface. Mock a minimal plugin with `addCommand` spy; invoke the command-registration helper (not yet implemented) and assert.
       - Target behavior: D-01 — command set changed.
       - Tests (minimum 2): (a) after command registration, `addCommand` was called with `id: 'run'` exactly once; (b) `addCommand` was NOT called with `id: 'run-sample'` or `id: 'run-custom'` (the Phase 3 commands are deleted).
       - Stubbed import path: `../../src/main` or a new helper file; choose the path that Plan 04 will implement against (document the chosen path in a code comment).

    Every test file begins with a top comment block:
    ```
    // Phase 5 Wave 0 — failing stub (Nyquist).
    // Target: <requirement> <D-XX>
    // Turns green when <plan NN> ships <file or behavior>.
    ```

    After writing all 10 files, run `npm test -- --run` and confirm these 10
    files are the ones failing (pre-existing tests remain green). Record the failing
    count in SUMMARY for Plan 02 to verify the baseline.
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | tail -40</automated>
  </verify>
  <done>
    - All 10 test files exist at the paths in `files`
    - Each file contains at least 2 `it(...)` blocks covering the listed behaviors
    - Each file has a Wave 0 header comment naming its D-XX target
    - `npm test -- --run` reports failures from exactly these 10 files (pre-existing suite otherwise green)
    - No file imports production code that does not yet exist (use lazy `await import(...)` + wrap in try/expect to ensure stubs are actual test failures, not compile errors)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add fakeSettingsStore extension + fakeWorkspace mock helper</name>
  <read_first>
    - tests/solve/mocks/fakeSettingsStore.ts (existing — must add new field without breaking callers)
    - tests/solve/mocks/fakeFetcher.ts (sibling mock pattern for reference)
    - .planning/phases/05-polish-ship/05-RESEARCH.md §Pattern 4 (ephemeralTabStore layout-change + ref-count surface)
    - .planning/phases/05-polish-ship/05-CONTEXT.md §Existing Test Infrastructure
  </read_first>
  <files>
    tests/solve/mocks/fakeSettingsStore.ts,
    tests/solve/mocks/fakeWorkspace.ts
  </files>
  <behavior>
    - `fakeSettingsStore` carries `techniquesFolderOverride: string` with getter/setter
      that round-trip + a `getTechniquesFolder()` that honors override.
    - `fakeWorkspace` exposes `getLeavesOfType(type: string)`, `on('layout-change' | 'active-leaf-change', cb)`, plus helpers `fire('layout-change')` / `setLeaves(leaves)` so tests deterministically simulate open/close transitions.
  </behavior>
  <action>
    1. Edit `tests/solve/mocks/fakeSettingsStore.ts`:
       - Add `techniquesFolderOverride: string` to the backing state (default `''`).
       - Add `getTechniquesFolderOverride(): string` + `async setTechniquesFolderOverride(v: string): Promise<void>` mirroring `autoBacklinksEnabled` getter/setter shape.
       - Update `getTechniquesFolder()` to `return override && override.length > 0 ? override : \`\${getProblemsFolder()}/Techniques\`` (D-15 exactly).
       - Do NOT change other fields. Existing callers must continue to work.

    2. Create `tests/solve/mocks/fakeWorkspace.ts`:
       - Export `createFakeWorkspace()` that returns an object exposing:
         - `on(name: 'layout-change' | 'active-leaf-change', cb: () => void): { // EventRef shape matching obsidian Plugin.registerEvent }`
         - `off(ref)` (noop OK)
         - `getLeavesOfType(type: string): FakeLeaf[]` returning a configurable array
         - `setLeaves(leaves: FakeLeaf[])` + `fire(name: string)` helper methods for tests
       - `FakeLeaf` shape: `{ view: { file?: { path: string } | null } }` sufficient for EphemeralTabStore `reconcile()` to iterate.
       - Export a `createFakePlugin(): Plugin` helper that returns `{ app: { workspace, metadataCache }, registerEvent(ref) {}, manifest: { id: 'leetcode' } }` — minimum surface for `EphemeralTabStore` constructor + `CodeBlockActionProcessor` registration tests.
       - No dependencies on real `obsidian` imports — mock ALL imports so happy-dom env works.

    3. Grep-check: after writing, run `grep -n "techniquesFolderOverride" tests/solve/mocks/fakeSettingsStore.ts` — must return ≥ 2 hits (field + getter).

    4. No .md doc files. No export from root `tests/` index.
  </action>
  <verify>
    <automated>grep -n "techniquesFolderOverride" tests/solve/mocks/fakeSettingsStore.ts && grep -n "getLeavesOfType" tests/solve/mocks/fakeWorkspace.ts && npm test -- --run tests/solve/mocks 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `fakeSettingsStore` round-trips `techniquesFolderOverride` and `getTechniquesFolder()` honors override per D-15
    - `fakeWorkspace` exposes `on`/`off`/`getLeavesOfType`/`setLeaves`/`fire`
    - `createFakePlugin()` exports a minimal Plugin-like shape sufficient for Wave 0 tests (`ephemeralTabStore.test.ts`, `codeActionsPostProcessor.test.ts`) to instantiate
    - No test file imports from real `obsidian` package via the helper (everything vi.mock-able upstream)
    - `npm test -- --run` shows the Wave 0 stubs failing for the RIGHT reason (missing src files), not failing to import their mock helpers
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer workstation → vitest runner | Node-only; no network; mocks intercept all `obsidian` imports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-0-01 | Tampering | Test stub file naming drift | mitigate | Names locked to 05-VALIDATION.md; executor must use exact strings in `files` list |
| T-05-0-02 | Denial of Service | Fake timers never restored | accept | Each test file resets `vi.useRealTimers()` in `afterEach`; low-risk leak inside vitest-only scope |
</threat_model>

<verification>
- `npm test -- --run` confirms exactly 10 newly-failing test files (Wave 0 stubs)
- No production code changed — only tests + mocks
- `git diff --stat` shows 12 files changed under `tests/`
- Every test file cites its source D-XX in a header comment
</verification>

<success_criteria>
- [ ] 10 Wave 0 test stub files exist (listed in `files_modified`)
- [ ] 2 mock helper files exist (one extension + one new)
- [ ] `fakeSettingsStore` round-trips `techniquesFolderOverride`
- [ ] `fakeWorkspace` exposes `getLeavesOfType` + event hooks used by `EphemeralTabStore` + `CodeBlockActionProcessor`
- [ ] All 10 stubs fail now (RED state) and will turn green exactly as their Waves 1-5 counterparts ship
- [ ] No pre-existing test file regresses
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-ship/05-01-SUMMARY.md` recording:
- Exact failing-test count (expected: all 10 Wave 0 stubs + extended existing where relevant)
- Any naming deltas between VALIDATION.md's suggested file names and the actual committed names (should be zero)
- Baseline `npm test` failure count for Plan 02 to compare against
</output>
