# Phase 02 deferred items

| Item | Surfaced in | Owner plan | Notes |
|------|-------------|------------|-------|
| 9 tsc errors: Wave 0 RED tests import `../src/notes/NoteWriter` (7) and `../src/notes/BaseFile` (2) | Plan 02-02 execution (2026-05-08) | Plans 02-03 / 02-04 / 02-05 | These test files (cache-ttl, new-note-fetch-failure, note-language-uses-settings, note-path-uses-settings, note-writer-folder, offline-regenerate, re-open-silent-offline, base-file-ship, base-file-preserve) were committed by 02-01 as RED tests for downstream modules. `npm run build` runs `tsc -noEmit` so the whole suite surfaces here. Plans 02-03 (NoteWriter) and 02-04 (BaseFile + NoteOrchestrator) will create the missing modules and turn those tests GREEN. Out-of-scope for 02-02. |
| Pre-existing lint findings (35 errors, 4 warnings) across `src/notes/NoteWriter.ts` (TFile casts, setTimeout vs activeWindow), `src/browse/ProblemBrowserView.ts` (unused `FilterRule` import, type assertions at 239/240, `document.createElementNS` at 347/353/360, `document` vs `activeDocument`), `src/main.ts` (2 unused eslint-disable directives around revealLeaf), `tests/cache-ttl.test.ts`, `tests/re-open-silent-offline.test.ts` (setTimeout vs activeWindow) | Plan 02-05 execution (2026-05-08) | TBD (lint-hygiene pass) | Not introduced by Plan 02-05's edits. Plan 02-05 only modified row-click / pickRandom handlers in `ProblemBrowserView.ts` and added NoteWriter wiring in `main.ts`; neither touched the lines surfaced in the lint report. Scope boundary — deferred to a dedicated lint-hygiene plan (candidate: Plan 02-06 or later phase). |
