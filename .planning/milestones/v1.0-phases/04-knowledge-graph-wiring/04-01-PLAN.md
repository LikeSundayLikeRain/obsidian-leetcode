---
phase: 04-knowledge-graph-wiring
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/graph/mergeTechniquesSection.test.ts
  - tests/graph/stubNoteCreator.test.ts
  - tests/graph/stubFilename.test.ts
  - tests/graph/dateFormat.test.ts
  - tests/graph/submissionHistoryClient.test.ts
  - tests/graph/onAccepted.frontmatter.test.ts
  - tests/graph/onAccepted.tags.test.ts
  - tests/graph/onAccepted.gate.test.ts
  - tests/graph/onAccepted.optOut.test.ts
  - tests/graph/onAccepted.reAccepted.test.ts
  - tests/graph/onAccepted.missingTopicTags.test.ts
  - tests/graph/SubmissionPickerModal.test.ts
  - tests/graph/SubmissionDetailModal.test.ts
  - tests/graph/copyToCode.test.ts
  - tests/graph/copyToCode.confirm.test.ts
  - tests/graph/mocks/fakeSubmissionHistoryFetcher.ts
  - tests/graph/mocks/fakeKnowledgeGraphDeps.ts
  - tests/fixtures/lc-submissions/list-many.json
  - tests/fixtures/lc-submissions/list-empty.json
  - tests/fixtures/lc-submissions/list-session-expired.json
  - tests/fixtures/lc-submissions/list-many.graphql.json
  - tests/fixtures/lc-submissions/detail-ac.graphql.json
  - tests/fixtures/lc-submissions/detail-wa.graphql.json
  - tests/fixtures/lc-submissions/README.md
  - scripts/grep-no-vault-modify.sh
autonomous: false
requirements:
  - GRAPH-01
  - GRAPH-02
  - GRAPH-03
  - GRAPH-04
  - GRAPH-05

must_haves:
  truths:
    - "All 15 Phase 4 test stubs exist under tests/graph/ and fail with a clear red reason (not import error)"
    - "Two fake helpers (fakeSubmissionHistoryFetcher, fakeKnowledgeGraphDeps) exist under tests/graph/mocks/"
    - "Six live-captured LC submission fixtures exist under tests/fixtures/lc-submissions/ (list-many.json + list-empty.json — legacy REST list JSON, retained per D-27 revised; list-session-expired.json — REST JSON 401 body for D-30 signal (a); list-many.graphql.json — GraphQL questionSubmissionList; detail-ac.graphql.json — GraphQL submissionDetails Accepted; detail-wa.graphql.json — GraphQL submissionDetails Wrong Answer)"
    - "scripts/grep-no-vault-modify.sh includes src/graph/ in the guarded path list"
    - "npm test runs all Phase 4 stubs without file-not-found / import errors"
  artifacts:
    - path: "tests/graph/mergeTechniquesSection.test.ts"
      provides: "Wave 0 stub for GRAPH-03 merge transform"
    - path: "tests/graph/dateFormat.test.ts"
      provides: "Wave 0 stub for GRAPH-02 ISO-8601 local-tz + DST boundary"
    - path: "tests/graph/submissionHistoryClient.test.ts"
      provides: "Wave 0 stub for D-27 revised GraphQL wrapper (questionSubmissionList + submissionDetails) + D-30 session-expiry signal set"
    - path: "tests/graph/onAccepted.frontmatter.test.ts"
      provides: "Wave 0 stub for GRAPH-02 frontmatter writes"
    - path: "tests/graph/onAccepted.tags.test.ts"
      provides: "Wave 0 stub for GRAPH-03 topic-tag frontmatter union + Phase 2 D-05 carry"
    - path: "tests/graph/onAccepted.gate.test.ts"
      provides: "Wave 0 stub for D-23 unknown/non-AC guard"
    - path: "tests/graph/onAccepted.optOut.test.ts"
      provides: "Wave 0 stub for GRAPH-05 opt-out"
    - path: "tests/graph/stubNoteCreator.test.ts"
      provides: "Wave 0 stub for GRAPH-04 never-overwrite + concurrent folder-create"
    - path: "tests/graph/stubFilename.test.ts"
      provides: "Wave 0 stub for D-17 filename normalization"
    - path: "tests/graph/SubmissionPickerModal.test.ts"
      provides: "Wave 0 stub for D-03/D-05/D-06 picker states"
    - path: "tests/graph/SubmissionDetailModal.test.ts"
      provides: "Wave 0 stub for D-04 detail render + MarkdownRenderer lifecycle"
    - path: "tests/graph/copyToCode.test.ts"
      provides: "Wave 0 stub for GRAPH-01-revised copy-to-code helper"
    - path: "tests/graph/copyToCode.confirm.test.ts"
      provides: "Wave 0 stub for ConfirmOverwriteModal gate"
    - path: "tests/graph/mocks/fakeSubmissionHistoryFetcher.ts"
      provides: "FIFO-queue fake for GraphQL POST /graphql/ operations `submissionList` (questionSubmissionList) + `submissionDetails`; both requests match on url+method+operationName and are backed by the GraphQL JSON fixtures (list-many.graphql.json, detail-ac.graphql.json, detail-wa.graphql.json) per D-27 revised. Legacy REST list JSON (list-many.json, list-empty.json) is still exposed for any caller that needs the pre-drift list shape, and list-session-expired.json (REST JSON 401) is exposed for the D-30 signal (a) session-expiry subtest."
    - path: "tests/graph/mocks/fakeKnowledgeGraphDeps.ts"
      provides: "Vault + Settings DI fake for KnowledgeGraphWriter tests"
    - path: "tests/fixtures/lc-submissions/list-many.json"
      provides: "Live-captured 20-row submissions_dump fixture (REST list shape; retained per D-27 revised)"
    - path: "tests/fixtures/lc-submissions/list-empty.json"
      provides: "Live-captured empty submissions_dump fixture (REST list shape; D-06 empty state)"
    - path: "tests/fixtures/lc-submissions/list-session-expired.json"
      provides: "Live-captured REST JSON 401 body `{detail: 'Authentication credentials were not provided.'}` (D-30 signal (a) session-expiry fixture)"
    - path: "tests/fixtures/lc-submissions/list-many.graphql.json"
      provides: "Live-captured GraphQL questionSubmissionList response — data.questionSubmissionList.submissions[] shape (D-27 revised, A2)"
    - path: "tests/fixtures/lc-submissions/detail-ac.graphql.json"
      provides: "Live-captured GraphQL submissionDetails response — Accepted submission (statusCode 10); data.submissionDetails shape (D-27 revised, A3)"
    - path: "tests/fixtures/lc-submissions/detail-wa.graphql.json"
      provides: "Live-captured GraphQL submissionDetails response — Wrong Answer submission (statusCode 11); data.submissionDetails shape (D-27 revised, A3)"
    - path: "scripts/grep-no-vault-modify.sh"
      provides: "Extended grep gate — src/graph/ added (CF-06)"
  key_links:
    - from: "tests/graph/**"
      to: "src/graph/** (does not exist yet — Wave 1 creates)"
      via: "import paths that will resolve once Wave 1 ships; stubs allowed to red-fail on missing module"
      pattern: "import.*from '\\.\\./\\.\\./src/graph/"
    - from: "tests/graph/mocks/fakeSubmissionHistoryFetcher.ts"
      to: "tests/fixtures/lc-submissions/*.json"
      via: "fs.readFileSync at test-init; JSON fixtures drive scripted GraphQL POST responses (plus the REST JSON-401 fixture for the D-30 session-expiry subtest)"
      pattern: "readFileSync.*lc-submissions"
    - from: "scripts/grep-no-vault-modify.sh"
      to: "src/graph/"
      via: "for-loop path argument; must include src/graph/ so Wave 1 code is covered"
      pattern: "src/graph/"
---

<objective>
Create Phase 4's complete Wave 0 test infrastructure so Wave 1 (pure utilities
+ REST) and Wave 2 (modals) can execute TDD-style against a red baseline.
This is the Phase 3 D-31 pattern applied to Phase 4's new surface area.

Purpose: Every Phase 4 code-producing task (Waves 1-2) has a matching test
stub that fails red until the implementation ships. Eliminates the "I'll
write tests later" anti-pattern. Also locks the LC submission-history wire
shape via live-captured fixtures BEFORE implementation (per RESEARCH §A2, A3
— the `/api/submissions/{slug}` endpoint is undocumented, shape may drift).

Output:
- 15 vitest stubs under tests/graph/
- 2 fake helpers under tests/graph/mocks/
- 6 live-captured LC submission fixtures under tests/fixtures/lc-submissions/
  (list-many.json, list-empty.json, list-session-expired.json — REST shapes;
  list-many.graphql.json, detail-ac.graphql.json, detail-wa.graphql.json — GraphQL shapes)
- scripts/grep-no-vault-modify.sh extended to cover src/graph/
- A human checkpoint that gates fixture capture against the live LC service
  (the author's own LC account, dogfooding).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-knowledge-graph-wiring/04-CONTEXT.md
@.planning/phases/04-knowledge-graph-wiring/04-RESEARCH.md
@.planning/phases/04-knowledge-graph-wiring/04-PATTERNS.md
@.planning/phases/04-knowledge-graph-wiring/04-VALIDATION.md
@tests/solve/mocks/fakeFetcher.ts
@tests/solve/mocks/fakeSettingsStore.ts
@tests/helpers/mock-vault.ts
@tests/solve/fixtures/accepted.json
@tests/solve/leetcodeRest.test.ts
@tests/base-file-ship.test.ts
@scripts/grep-no-vault-modify.sh

<interfaces>
<!-- Test-only contracts the executor needs to match. Extracted from existing repo. -->
<!-- tests/solve/mocks/fakeFetcher.ts (Phase 3 — EXACT analog for fakeSubmissionHistoryFetcher): -->

export interface ScriptedResponse { status?: number; headers?: Record<string,string>; text?: string; json?: unknown }
export interface FakeFetcher {
  script(matcher: RegExp | ((url: string) => boolean), responses: ScriptedResponse[]): void;
  fetch: (params: RequestUrlParam) => Promise<RequestUrlResponse>;
  calls: RequestUrlParam[];
}
export function makeFakeFetcher(): FakeFetcher;

<!-- tests/helpers/mock-vault.ts (Phase 2 — EXACT analog for fakeKnowledgeGraphDeps): -->
export function makeMockVaultApp(files: Record<string, string>): {
  app: unknown; // App-shaped fake with vault.read/process/create/createFolder, fileManager.processFrontMatter
  getFrontmatter(path: string): Record<string, unknown> | null;
  getBody(path: string): string | null;
  listFiles(): string[];
};

<!-- scripts/grep-no-vault-modify.sh (current — must be extended): -->
for d in src/notes/ src/browse/; do
  if [ -d "$d" ]; then
    if grep -rE "vault\.modify\s*\(" "$d" --include='*.ts'; then matches=1; fi
  fi
done
<!-- ↑ Phase 4 MUST add src/graph/ to this loop. -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → repo | Live-captured LC HTML/JSON enters the repo; must be scrubbed of PII before commit |
| grep gate → CI | Script exit code gates phase completion; false-negative (missed path) allows vault.modify to ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | tests/fixtures/lc-submissions/*.json | mitigate | Author scrubs LEETCODE_SESSION / csrftoken cookie values, real username, real email, and submission_id that could correlate to the account BEFORE committing. Fixture capture checkpoint (human smoke) includes explicit scrub step; reviewer grep-checks committed fixtures for `LEETCODE_SESSION=`, `csrftoken=`, and any email regex. |
| T-04-01-02 | Tampering | scripts/grep-no-vault-modify.sh | mitigate | Extend to include `src/graph/` in the loop; Plan 05 execution runs this script as a gate. Acceptance criterion below greps for literal `src/graph/` in the script body. |
| T-04-01-03 | Information Disclosure | tests/fixtures/lc-submissions/detail-*.graphql.json | accept | Submission code is the author's own code; committing to a public repo is acceptable per user choice (same posture as Phase 3 fixtures). No secret material in code. |
| T-04-01-04 | Denial of Service | tests/graph/mocks/fakeSubmissionHistoryFetcher.ts | accept | Test-only fake; no production reach. FIFO queue mirrors Phase 3 `fakeFetcher` posture. |

</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create 15 test stubs + 2 fake helpers under tests/graph/</name>
  <files>
    tests/graph/mergeTechniquesSection.test.ts,
    tests/graph/stubNoteCreator.test.ts,
    tests/graph/stubFilename.test.ts,
    tests/graph/dateFormat.test.ts,
    tests/graph/submissionHistoryClient.test.ts,
    tests/graph/onAccepted.frontmatter.test.ts,
    tests/graph/onAccepted.tags.test.ts,
    tests/graph/onAccepted.gate.test.ts,
    tests/graph/onAccepted.optOut.test.ts,
    tests/graph/onAccepted.reAccepted.test.ts,
    tests/graph/onAccepted.missingTopicTags.test.ts,
    tests/graph/SubmissionPickerModal.test.ts,
    tests/graph/SubmissionDetailModal.test.ts,
    tests/graph/copyToCode.test.ts,
    tests/graph/copyToCode.confirm.test.ts,
    tests/graph/mocks/fakeSubmissionHistoryFetcher.ts,
    tests/graph/mocks/fakeKnowledgeGraphDeps.ts
  </files>
  <read_first>
    .planning/phases/04-knowledge-graph-wiring/04-CONTEXT.md (D-01..D-29 authoritative; every stub's describe block cites the D-XX it enforces),
    .planning/phases/04-knowledge-graph-wiring/04-RESEARCH.md §Validation Architecture (exact test names in the "Phase Requirements → Test Map" table — copy them verbatim so checker's grep passes),
    .planning/phases/04-knowledge-graph-wiring/04-PATTERNS.md §Test Analogs — Detailed (exact analog files listed per stub; copy file skeleton, swap imports),
    tests/solve/mocks/fakeFetcher.ts (analog for fakeSubmissionHistoryFetcher),
    tests/solve/mocks/fakeSettingsStore.ts (analog for fakeKnowledgeGraphDeps' settings DI),
    tests/helpers/mock-vault.ts (vault-app harness reused verbatim via import; NOT duplicated),
    tests/solve/leetcodeRest.test.ts (analog for submissionHistoryClient.test.ts),
    tests/note-frontmatter-write.test.ts (analog for onAccepted.frontmatter.test.ts),
    tests/note-frontmatter-tags.test.ts (analog for onAccepted.tags.test.ts),
    tests/solve/CaseRegion.test.ts (analog for mergeTechniquesSection.test.ts),
    tests/base-file-ship.test.ts (analog for stubNoteCreator.test.ts)
  </read_first>
  <action>
Create 15 vitest stub files + 2 fakes. Each stub:
1. Imports from the TARGET module (which does not exist yet in src/graph/). ts-vitest will red-fail with "Cannot find module" — this is the intended TDD red state.
2. Contains `describe` blocks whose names match the exact test names listed in 04-RESEARCH.md §Phase Requirements → Test Map, so the checker's verify-work grep passes on Wave 1 completion.
3. Asserts the behavior the Wave 1/2 implementation will satisfy.

**Per stub — exact describe-block names (copy verbatim from 04-RESEARCH.md):**

`tests/graph/mergeTechniquesSection.test.ts` — describe `'mergeTechniquesSection (GRAPH-03, D-13)'`, its:
- `'appends ## Techniques after ## Notes when section is absent'`
- `'writes wikilink per topic tag'` (GRAPH-03 row)
- `'insertion after ## Notes'` (GRAPH-03 row)
- `'preserves user-added wikilinks'` (GRAPH-03 row — user-line preservation)
- `'idempotent'` (GRAPH-03 row)
- `'no-ops when topicTags is empty and section does not exist'` (D-25)
- `'tolerates * and + bullets as link markers'` (D-13)

`tests/graph/dateFormat.test.ts` — describe `'toIsoLocalTz (GRAPH-02, D-10)'`, its:
- `'ISO-8601 local-tz'` (GRAPH-02 row, exact name)
- `'DST boundary'` (GRAPH-02 row, exact name)

`tests/graph/submissionHistoryClient.test.ts` — describe `'submissionHistoryClient (D-27, D-28, D-29, D-30)'`, its (post-2026-05-09 GraphQL drift rename per CONTEXT L237 + 04-03-PLAN):
- `'list calls questionSubmissionList GraphQL'` (D-27 revised — renamed from the obsolete `'list maps wire shape'` to make the transport explicit; still asserts the GraphQL-list mapper)
- `'detail calls submissionDetails GraphQL'` (D-27 revised — REPLACES obsolete `'detail scrapes pageData'` entirely; submissions no longer served as HTML with a `var pageData` block, so the HTML-scrape subtest is gone)
- `'detail rejects non-numeric submission id'` (D-27 revised — T-04-03-02 path-segment guard test; new subtest)
- `'list fires SessionExpiredError on JSON 401'` (D-30 new — REPLACES `'list fires SessionExpiredError on 302/401/403/login-HTML'`; LC's current failure shape for unauthenticated GraphQL is HTTP 401 + JSON `{detail:'Authentication credentials were not provided.'}`)
- `'list fires SessionExpiredError on 403 bare'` (D-30 new — LC's GraphQL auth-failure status-only shape)
- `'detail fires SessionExpiredError on 200 with errors[] auth message'` (D-30 new — REPLACES `'detail fires SessionExpiredError on login-redirect'`; GraphQL returns HTTP 200 with `body.errors[]` containing an auth message instead of an HTML login-redirect)

**Wave 0 catch-up note:** the committed stub file `tests/graph/submissionHistoryClient.test.ts` still carries the obsolete describe strings (`'list maps wire shape'`, `'detail scrapes pageData'`, `'list fires SessionExpiredError on 302/401/403/login-HTML'`, `'detail fires SessionExpiredError on login-redirect'`) from the pre-drift commit 6bddd3e. The executor rewrites those four `it(...)` blocks to the names above (and the one new `'detail rejects non-numeric submission id'` + `'list fires SessionExpiredError on 403 bare'` + `'detail fires SessionExpiredError on 200 with errors[] auth message'` subtests) as part of the Wave 0 catch-up before running 04-03's Task 1 tests. Fixture loader calls inside the file must also retarget to the GraphQL fixtures (`list-many.graphql`, `detail-ac.graphql`, `detail-wa.graphql`) + the REST JSON-401 fixture (`list-session-expired`) per the 04-03-PLAN Task 1 Step 4 fixture-loader retarget. All other 14 stub files are unaffected.

`tests/graph/onAccepted.frontmatter.test.ts` — describe `'KnowledgeGraphWriter.onAccepted — frontmatter (GRAPH-02)'`, its:
- `'on AC writes frontmatter fields'` (GRAPH-02 row)
- `'parses runtime/memory'` (GRAPH-02 row)
- `'on AC does not modify ## Code'` (GRAPH-01 row)

`tests/graph/onAccepted.tags.test.ts` — describe `'KnowledgeGraphWriter.onAccepted — tags (GRAPH-03, Phase 2 D-05 carry)'`, its:
- `'topic tags union-merge'` (GRAPH-03 row)
- `'writes topic tags on first AC'` (Phase 2 D-05 carry row)

`tests/graph/onAccepted.gate.test.ts` — describe `'KnowledgeGraphWriter.onAccepted — gate (D-23)'`, its:
- `'unknown verdict skips pipeline'` (D-23 row)
- `'non-AC verdict skips pipeline'` (D-23 row)

`tests/graph/onAccepted.optOut.test.ts` — describe `'KnowledgeGraphWriter.onAccepted — opt-out (GRAPH-05)'`, its:
- `'opt-out skips ## Techniques and stubs'` (GRAPH-05 row, EXACT name)

`tests/graph/onAccepted.reAccepted.test.ts` — describe `'KnowledgeGraphWriter.onAccepted — re-AC (D-24)'`, its:
- `'re-AC reflects latest not best'` (D-24 row)

`tests/graph/onAccepted.missingTopicTags.test.ts` — describe `'KnowledgeGraphWriter.onAccepted — missing topicTags cache (Pitfall 10)'`, its:
- `'stub failure does not block section write'` (D-19 row)
- `'skips ## Techniques write when detail.topicTags is undefined'` (Pitfall 10)

`tests/graph/stubNoteCreator.test.ts` — describe `'StubNoteCreator (GRAPH-04, D-15, D-18)'`, its:
- `'creates stub on missing'` (GRAPH-04 row, EXACT)
- `'never overwrites existing stub'` (GRAPH-04 row, EXACT)
- `'creates folder once'` (GRAPH-04 row, EXACT — concurrent-safe)
- `'recreates after delete'` (GRAPH-04 row, EXACT — Phase 2 BaseFile divergence)

`tests/graph/stubFilename.test.ts` — describe `'buildTechniqueFilename (D-17)'`, its:
- `'filename normalization'` (GRAPH-04 row, EXACT)
- `'preserves + for C++'` (A1 defensive case)

`tests/graph/SubmissionPickerModal.test.ts` — describe `'SubmissionPickerModal (D-03, D-05, D-06)'`, its:
- `'session expired fires locked Notice'` (D-06 row)
- `'empty state renders placeholder'` (D-06 row)
- `'network error renders inline'` (D-06 row)

`tests/graph/SubmissionDetailModal.test.ts` — describe `'SubmissionDetailModal (D-04)'`, its:
- `'copy to code does not create ## Solution'` (GRAPH-01 row, EXACT)
- `'copy-to-code confirms overwrite'` (Copy-to-Code row, EXACT)
- `'copy uses submission language'` (Copy-to-Code row, EXACT)

`tests/graph/copyToCode.test.ts` — describe `'copyToCode (GRAPH-01 revised)'`, its:
- `'overwrites ## Code fenced block via vault.process'`

`tests/graph/copyToCode.confirm.test.ts` — describe `'hasExistingCodeBlock'`, its:
- `'returns false for whitespace-only fence'`
- `'returns true for non-empty fence'`

**For each stub:** import from the target path under `src/graph/` OR `src/notes/NoteTemplate` OR `src/settings/SettingsStore` (use the canonical import path from 04-RESEARCH.md §Recommended Project Structure). Write one `it(...)` per bullet above. Each `it` body MAY contain TODO-body like `expect(true).toBe(false); // TODO: implement in Wave 1` — but the IMPORT must reference the real target module so the test naturally fails red until the module exists.

**fakeSubmissionHistoryFetcher** (tests/graph/mocks/fakeSubmissionHistoryFetcher.ts): copy tests/solve/mocks/fakeFetcher.ts verbatim, rename the export to `makeFakeSubmissionHistoryFetcher`. Add two loader helpers whose on-disk targets match the current (post-drift) fixture set — all fixtures are `.json` on disk (the legacy `.html` fixtures were removed in the 2026-05-09 drift commit in favor of GraphQL JSON captures per 04-01-SUMMARY.md / tests/fixtures/lc-submissions/README.md). Per 04-03-PLAN Task 1 Step 4, both loaders read `.json` and return the parsed unknown JSON object (caller casts):
- `loadListFixture(name: 'list-many' | 'list-empty' | 'list-session-expired' | 'list-many.graphql' | 'detail-ac.graphql' | 'detail-wa.graphql'): unknown` → `JSON.parse(readFileSync('tests/fixtures/lc-submissions/' + name + '.json', 'utf-8'))`
- `loadDetailFixture(name): unknown` — alias for `loadListFixture` (all fixtures are JSON now; detail fixtures are GraphQL JSON bodies, not HTML).

**fakeKnowledgeGraphDeps** (tests/graph/mocks/fakeKnowledgeGraphDeps.ts): export `makeFakeKnowledgeGraphDeps({ app, problemDetails, autoBacklinksEnabled, problemsFolder })` → returns `{ app, settings }` shape matching the `KnowledgeGraphWriterDeps` spec from 04-PATTERNS.md §KnowledgeGraphWriter section:
```typescript
{
  app: /* makeMockVaultApp(...)-produced */,
  settings: {
    getProblemDetail: (slug: string) => DetailCacheEntry | null,
    getAutoBacklinksEnabled: () => boolean,
    getProblemsFolder: () => string,
    getTechniquesFolder: () => string,    // default impl: `${problemsFolder}/Techniques`
    getDefaultLanguage: () => string,
  },
}
```
Default `autoBacklinksEnabled = true`, default `problemsFolder = 'LeetCode'`, default `getDefaultLanguage = () => 'python3'`.

**Directory creation:** use `mkdir -p tests/graph tests/graph/mocks tests/fixtures/lc-submissions` before writing files.

**Sentence-case + Notice copy** — stubs that test Notice copy MUST assert the exact locked string (e.g., `'LeetCode session expired. Log in again.'`) per CF-19 and 04-UI-SPEC.md §Notice strings.
  </action>
  <verify>
    <automated>npm test -- --run tests/graph/ 2>&1 | head -80 | grep -E "(Test Files.*failed|Cannot find module|describe|Expected)" | head -10</automated>
  </verify>
  <done>All 15 test files + 2 mocks exist. `npm test -- --run tests/graph/` reports red failures (not file-not-found at the vitest harness level; module-not-found within test imports is the expected red state since src/graph/** does not yet exist). Every describe block name in the list above appears verbatim in the committed files (grep-verifiable).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture live LC submission-history fixtures (6 files)</name>
  <files>
    tests/fixtures/lc-submissions/list-many.json,
    tests/fixtures/lc-submissions/list-empty.json,
    tests/fixtures/lc-submissions/list-session-expired.json,
    tests/fixtures/lc-submissions/list-many.graphql.json,
    tests/fixtures/lc-submissions/detail-ac.graphql.json,
    tests/fixtures/lc-submissions/detail-wa.graphql.json,
    tests/fixtures/lc-submissions/README.md
  </files>
  <read_first>
    .planning/phases/04-knowledge-graph-wiring/04-RESEARCH.md §Pattern B (submission-history endpoint URLs + GraphQL query bodies — revised post-2026-05-09 drift),
    .planning/phases/04-knowledge-graph-wiring/04-RESEARCH.md §Assumption A2, A3 (MEDIUM-risk live-shape assumption this task resolves),
    .planning/phases/04-knowledge-graph-wiring/04-CONTEXT.md §"GraphQL / API Mechanics (revised 2026-05-09)" (D-27/D-28/D-29 revised transport + D-30 new signal set),
    tests/solve/fixtures/README.md (analog — Phase 3 verdict-fixture capture README; mirror format)
  </read_first>
  <what-built>
    Task 1 created empty fixture placeholders referenced by fakeSubmissionHistoryFetcher. The user now captures REAL JSON from live LC (author's own account) to replace the placeholders, so Waves 1-2 can unit-test against the real wire shape. This resolves RESEARCH.md A2/A3 (MEDIUM risk → LOW risk) — post-2026-05-09 the submission-detail surface is served via GraphQL POST /graphql/ (operations `submissionList` / `submissionDetails`) rather than the legacy HTML `var pageData` scrape, and session-expiry surfaces as JSON 401 / bare 403 / GraphQL 200 errors[] auth message rather than an HTML login-redirect. Capturing once at phase start locks the shape.
  </what-built>
  <how-to-verify>
    1. Open Obsidian DevTools (Ctrl/Cmd+Shift+I) while logged into leetcode.com in a regular browser with the author's session active.
    2. In the browser's Network tab, navigate to a problem the author has submitted to 2+ times (e.g., Two Sum: https://leetcode.com/problems/two-sum/submissions/).
    3. Inspect the XHR requests (both REST list + GraphQL):
       a. **list-many fixture (REST list shape, retained per D-27 revised):** Copy the JSON response from `GET https://leetcode.com/api/submissions/two-sum` (or whichever slug you picked) into `tests/fixtures/lc-submissions/list-many.json`. Must contain a `submissions_dump` array with ≥10 rows, mixed verdicts (at least 1 AC + 1 WA ideally).
       b. **list-empty fixture (REST list shape):** Navigate to a problem you've NEVER submitted to. Copy the JSON response (should have `submissions_dump: []` or an empty array) into `list-empty.json`.
       c. **list-many.graphql fixture (new GraphQL list shape — D-27 revised):** On the submissions page, observe the XHR `POST https://leetcode.com/graphql/` whose request body has `operationName: 'submissionList'`. Copy the full JSON response (`{data: {questionSubmissionList: {submissions: [...], lastKey, hasNext}}}`) into `list-many.graphql.json`. Must contain ≥10 rows with mixed verdicts.
       d. **detail-ac.graphql fixture (new GraphQL detail shape — D-27 revised):** Click an Accepted submission; observe the XHR `POST https://leetcode.com/graphql/` whose request body has `operationName: 'submissionDetails'`. Copy the full JSON response (`{data: {submissionDetails: {...}}}`) into `detail-ac.graphql.json`. Must have `data.submissionDetails.statusCode === 10`.
       e. **detail-wa.graphql fixture (new GraphQL detail shape — D-27 revised):** Repeat (d) for a Wrong Answer submission → `detail-wa.graphql.json`. Must have `data.submissionDetails.statusCode === 11`.
       f. **list-session-expired fixture (REST JSON 401 — D-30 signal (a)):** Log out (or open an incognito window / curl with no cookies against the REST list endpoint, e.g., `curl https://leetcode.com/api/submissions/two-sum`) and capture the JSON 401 body into `list-session-expired.json`. Must be the JSON `{"detail": "Authentication credentials were not provided."}` body (NOT HTML).
    4. **SCRUB BEFORE COMMIT** — open each file and replace:
       - Any `LEETCODE_SESSION=<long-hex-string>` → `LEETCODE_SESSION=REDACTED`
       - Any `csrftoken=<hex>` → `csrftoken=REDACTED`
       - Your real email address → `author@example.com`
       - Your real username inside the GraphQL response blob → `testuser` (visible in `data.submissionDetails.user.username` and `.user.profile.realName`)
       - Any avatar URL → `avatar_REDACTED.png`
    5. Write a tests/fixtures/lc-submissions/README.md (≤60 lines) describing:
       - When each fixture was captured (date + LC account owner = author)
       - The slug each fixture is keyed to
       - The scrubbing steps applied
       - The exact GraphQL query strings used for list + detail (inline — these become the source of truth for the LIST_QUERY / DETAIL_QUERY constants in Plan 04-03's submissionHistoryClient)
       - How to recapture (point to this checkpoint's steps)
    6. Verify the files parse as valid JSON and carry the expected keys:
       - `node -e "JSON.parse(require('fs').readFileSync('tests/fixtures/lc-submissions/list-many.json'))"` exits 0
       - `node -e "JSON.parse(require('fs').readFileSync('tests/fixtures/lc-submissions/list-empty.json'))"` exits 0
       - `node -e "const j = JSON.parse(require('fs').readFileSync('tests/fixtures/lc-submissions/list-session-expired.json')); process.exit(typeof j.detail === 'string' && /authenticat/i.test(j.detail) ? 0 : 1)"` exits 0
       - `node -e "const j = JSON.parse(require('fs').readFileSync('tests/fixtures/lc-submissions/list-many.graphql.json')); process.exit(Array.isArray(j.data && j.data.questionSubmissionList && j.data.questionSubmissionList.submissions) ? 0 : 1)"` exits 0
       - `node -e "const j = JSON.parse(require('fs').readFileSync('tests/fixtures/lc-submissions/detail-ac.graphql.json')); process.exit(j && j.data && j.data.submissionDetails && j.data.submissionDetails.statusCode === 10 ? 0 : 1)"` exits 0
       - `node -e "const j = JSON.parse(require('fs').readFileSync('tests/fixtures/lc-submissions/detail-wa.graphql.json')); process.exit(j && j.data && j.data.submissionDetails && j.data.submissionDetails.statusCode === 11 ? 0 : 1)"` exits 0
    7. Re-run `npm test -- --run tests/graph/submissionHistoryClient.test.ts` — should still red-fail (src/graph/submissionHistoryClient.ts doesn't exist yet) BUT should no longer complain about fixture file-not-found.
  </how-to-verify>
  <resume-signal>Type "approved" after all 6 fixtures + README are committed and scrubbing is confirmed. If LC's wire shape has changed such that fakeSubmissionHistoryFetcher can't adapt, describe the delta so Plan 03 can adjust submissionHistoryClient accordingly.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Extend grep-no-vault-modify.sh to cover src/graph/</name>
  <files>scripts/grep-no-vault-modify.sh</files>
  <read_first>
    scripts/grep-no-vault-modify.sh (the current script — line 10's for-loop is the edit site),
    .planning/phases/04-knowledge-graph-wiring/04-CONTEXT.md §CF-06 (grep gate requirement),
    .planning/phases/02-problems-as-notes/02-CONTEXT.md §D-22 (original discipline)
  </read_first>
  <action>
Modify scripts/grep-no-vault-modify.sh. Change line 10 from:
```bash
for d in src/notes/ src/browse/; do
```
to:
```bash
for d in src/notes/ src/browse/ src/graph/; do
```
Also update line 16's echo messages so it lists all three directories. Exact final state of the loop:
```bash
for d in src/notes/ src/browse/ src/graph/; do
  if [ -d "$d" ]; then
    if grep -rE "vault\.modify\s*\(" "$d" --include='*.ts'; then
      matches=1
    fi
  fi
done
if [ "$matches" -eq 1 ]; then
  echo "ERROR: vault.modify() is forbidden in src/notes/, src/browse/, and src/graph/ — use vault.process() instead."
  exit 1
fi
echo "OK: no vault.modify() calls in src/notes/, src/browse/, or src/graph/"
```
No other changes to the script (shebang, set -euo pipefail, matches=0 init all stay).

**Test it:** running `bash scripts/grep-no-vault-modify.sh` before Wave 1 must succeed (exit 0, "OK" line printed) since src/graph/ doesn't yet exist — the `[ -d "$d" ]` guard handles that case.
  </action>
  <verify>
    <automated>grep -c "src/graph/" scripts/grep-no-vault-modify.sh</automated>
  </verify>
  <done>The grep-no-vault-modify.sh script contains at least 1 literal occurrence of `src/graph/`. `bash scripts/grep-no-vault-modify.sh` exits 0 (src/graph/ directory is absent; loop skips via the -d guard). The "OK" line includes `src/graph/`.</done>
</task>

</tasks>

<verification>
Phase-wide Wave 0 gate:
- `ls tests/graph/` shows exactly 15 .test.ts files + mocks/ subdirectory
- `ls tests/graph/mocks/` shows 2 files (fakeSubmissionHistoryFetcher.ts, fakeKnowledgeGraphDeps.ts)
- `ls tests/fixtures/lc-submissions/` shows 6 real captured files + README.md (list-many.json, list-empty.json, list-session-expired.json, list-many.graphql.json, detail-ac.graphql.json, detail-wa.graphql.json)
- `npm test -- --run tests/graph/` runs (no harness errors) and reports red failures with "Cannot find module" for src/graph/** imports (expected — Wave 1 creates those modules)
- `bash scripts/grep-no-vault-modify.sh` exits 0 (OK line)
- `grep -c "src/graph/" scripts/grep-no-vault-modify.sh` ≥ 1
- `grep -cE "LEETCODE_SESSION=[a-f0-9]{20,}|csrftoken=[a-f0-9]{20,}" tests/fixtures/lc-submissions/*.json 2>/dev/null` returns 0 (no un-redacted session tokens)
</verification>

<success_criteria>
- 15 test stubs exist, each importing from the intended target path under src/graph/ (or NoteTemplate/SettingsStore for the 3 that extend existing files)
- 2 fake helpers (fakeSubmissionHistoryFetcher, fakeKnowledgeGraphDeps) exist and are importable by the stubs
- 6 live-captured fixtures exist, scrubbed, parseable, committed
- fixtures README documents capture procedure + verbatim GraphQL queries
- grep gate script extended and passes
- No PII / auth tokens visible in committed fixtures (grep returns 0 matches)
- Red-baseline established — Wave 1 can start against a failing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/04-knowledge-graph-wiring/04-01-SUMMARY.md` summarizing:
- Files created (paths + line counts)
- Fixture capture anomalies or shape deltas vs RESEARCH.md §Pattern B (if any)
- Any test stub where the describe name deviated from 04-RESEARCH.md's table (should be none)
</output>
</content>
</invoke>