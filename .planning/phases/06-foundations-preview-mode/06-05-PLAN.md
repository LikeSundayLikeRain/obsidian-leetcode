---
phase: 06-foundations-preview-mode
plan: 05
type: execute
wave: 5
depends_on: ["06-03"]
files_modified:
  - src/preview/ProblemPreviewView.ts
  - src/notes/htmlToMarkdown.ts
  - styles.css
  - tests/preview/header-render.test.ts
  - tests/preview/regression-grep.test.ts
  - tests/preview/enter-key.test.ts
  - tests/notes/htmlToMarkdown-fenced.test.ts
autonomous: true
gap_closure: true
requirements:
  - PREVIEW-03
  - PREVIEW-04
  - PREVIEW-05
tags:
  - preview
  - reading-mode-parity
  - sticky-header
  - keyboard
  - gap-closure

must_haves:
  truths:
    - "Examples in the preview body render as fenced code blocks (grey monospace boxes), not as flat prose with stray copy buttons."
    - "The preview body's typography matches Obsidian reading-mode (no font-size shrink applied to the body container)."
    - "The sticky header is a single horizontal strip containing the title, the difficulty pill, and the action button (Start Problem / Open Problem). Topic chips are absent."
    - "Pressing Enter while the preview tab is focused fires the action button (same handler as click)."
  artifacts:
    - path: "src/notes/htmlToMarkdown.ts"
      provides: "Fenced-only code-block contract for LC content"
      contains: "codeBlockStyle: 'fenced'"
    - path: "src/preview/ProblemPreviewView.ts"
      provides: "renderHeader without topic chips + Enter-key handler wired in onOpen/onClose"
      contains: "scope.register"
    - path: "styles.css"
      provides: "Sticky header bar with right-aligned action button; markdown-rendered body parity (no chrome font override leaking onto body)"
      contains: ".leetcode-preview__header"
    - path: "tests/preview/enter-key.test.ts"
      provides: "Locked Enter-key contract for the preview action button"
      exports: ["describe", "it"]
    - path: "tests/notes/htmlToMarkdown-fenced.test.ts"
      provides: "Locked fenced-code-block contract for the LC HTML→Markdown pipeline"
      exports: ["describe", "it"]
  key_links:
    - from: "src/preview/ProblemPreviewView.ts:renderRendered"
      to: "src/notes/htmlToMarkdown.ts:htmlToMarkdown"
      via: "htmlToMarkdown(detail.contentHtml) → MarkdownRenderer.render"
      pattern: "htmlToMarkdown\\(.*contentHtml\\)"
    - from: "src/preview/ProblemPreviewView.ts:onOpen"
      to: "this.scope"
      via: "scope.register([], 'Enter', () => this.handleActionClick(...))"
      pattern: "scope\\.register\\(\\s*\\[\\s*\\]\\s*,\\s*['\"]Enter['\"]"
    - from: "src/preview/ProblemPreviewView.ts:renderHeader"
      to: "the action button"
      via: "single .leetcode-preview__header containing the title + .lc-diff pill + .lc-preview__action button"
      pattern: "lc-preview__action"
---

<objective>
Close three Phase 06 UAT gaps by reshaping the preview's presentation layer so the rendered body matches Obsidian reading mode, the sticky header is one horizontal strip with the action button (no topic chips), and the action button is keyboard-activatable via Enter.

Purpose: Phase 06 Plan 03 shipped the preview tab and passed every regression-grep gate, but manual UAT against test #2 (single-click vs shift-click) flagged three presentation defects that block the phase's user-visible success criterion ("Single-click previews — and looks like a real reading-mode tab"). This plan does NOT touch the data path, the cache contract, the routeProblemClick decision flow, or any other Plan 06-03 invariant — only the DOM shape, the Turndown→Markdown contract for examples, the CSS that styles the header strip and body region, and the keyboard wiring.

Output: Three minimum-diff source changes (`htmlToMarkdown.ts`, `ProblemPreviewView.ts`, `styles.css`) plus the test deltas that lock the new contract (one updated test file, one updated regression-grep test, two new test files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-foundations-preview-mode/06-CONTEXT.md
@.planning/phases/06-foundations-preview-mode/06-UAT.md
@.planning/phases/06-foundations-preview-mode/06-UI-SPEC.md
@.planning/phases/06-foundations-preview-mode/06-RESEARCH.md
@.planning/phases/06-foundations-preview-mode/06-03-SUMMARY.md
@CLAUDE.md

@src/preview/ProblemPreviewView.ts
@src/notes/htmlToMarkdown.ts
@tests/preview/header-render.test.ts
@tests/preview/regression-grep.test.ts
@tests/preview/start-button.test.ts
@styles.css

<interfaces>
<!-- Key interface contracts the executor relies on. Sourced verbatim from the
     files above so the executor does NOT need to re-explore. -->

From src/preview/ProblemPreviewView.ts (current — to be reshaped):
```typescript
export const PREVIEW_VIEW_TYPE = 'leetcode-preview';
// renderHeader signature is preserved; the topic-chips loop at lines 142-147
// is removed in Task 2; the chip-row container's role becomes
// "single horizontal header strip" (title + diff pill + action button).
export function renderHeader(
  container: HTMLElement,
  detail: DetailCacheEntry,
  noteExists: boolean,
): HTMLButtonElement;

// Class lifecycle (preserved):
//   onOpen()   — initializes rootEl, optionally renders for slug
//   setState() — stores slug, lazy-inits rootEl, renders for slug
//   onClose()  — cancels detach timer, bumps renderToken
// Task 3 adds:
//   - scope.register([], 'Enter', () => activeAction?.click())
//     wired in onOpen, automatically released on onClose by Obsidian
//   - a private `activeAction: HTMLButtonElement | null` field updated by
//     renderRendered() so the Enter handler always targets the live button
```

From src/notes/htmlToMarkdown.ts (current):
```typescript
export function htmlToMarkdown(html: string): string;
// Already configured with codeBlockStyle: 'fenced' (line 112) and an
// `lc-example-block` rule that wraps Shape A LC <pre> blocks in fenced text.
// Task 1 hardens the contract via a new test file — no behavior change to the
// service config; the test simply locks the existing fenced-emit invariant
// against future regressions and adds a fixture for the LC two-sum example
// shape so executors can verify reading-mode parity.
```

From src/preview/ProblemPreviewView.ts:renderRendered (current — to be patched):
```typescript
// Body container today: createDiv({ cls: 'leetcode-preview__body' })
// Task 2 changes the class list to:
//   createDiv({ cls: 'leetcode-preview__body markdown-rendered' })
// so Obsidian's reading-mode CSS cascade (font, code-block backgrounds,
// copy-button styling) applies. MarkdownRenderer.render still receives the
// SAME container; we only co-add the upstream class.
```

From styles.css (`.leetcode-preview { font-size: var(--font-ui-small); }`):
```css
/* Today the .leetcode-preview ROOT carries font-size: var(--font-ui-small),
   which leaks onto MarkdownRenderer's output below the sticky header.
   Task 2 lifts that font-size declaration off the root and onto the
   sticky header / chip row only — the body should inherit the default
   reading-mode font cascade. */
```
</interfaces>

<gap_origin>
Phase 06 Plan 03 shipped 731 passing tests and all 6 UI-SPEC acceptance grep gates green, but the user's manual UAT against test scenario #2 (06-UAT.md, "Single-click vs shift-click default behavior") flagged three presentation defects:

  1. **Body parity (MAJOR).** Examples render as plain text + stray copy buttons. User wants reading-mode parity (fenced grey code blocks, reading-mode body font).
  2. **Header chrome (MAJOR).** Topic chips run together with no separator and the user wants them removed. The action button floats below the chip row instead of sitting in a single sticky strip.
  3. **Enter key (MINOR).** No keyboard activation for the action button.

User-confirmed scope override: CONTEXT.md decision C originally included topic chips in the sticky header chrome. The user has overridden that decision for this gap-closure plan — topic chips are dropped entirely. The original PREVIEW-03 requirement ("difficulty + topic chips") becomes "difficulty pill only" in the v1.1 ship; topic-chip surfacing remains a deferred backlog candidate (CONTEXT.md `<deferred>` already lists clickable topic-chip filtering).

Three of CONTEXT.md's locked decisions are preserved verbatim: D-A (single-click previews), D-B (one-tab reuse + post-Start detach), D-D (clean command IDs and 0.3.0 lint posture). Only D-C's chrome content changes, and even then only the topic-chip line.
</gap_origin>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the fenced-code-block contract for LC HTML→Markdown</name>

  <files>
    src/notes/htmlToMarkdown.ts (no behavior change — config is already correct)
    tests/notes/htmlToMarkdown-fenced.test.ts (new)
  </files>

  <read_first>
    src/notes/htmlToMarkdown.ts (full file — note the existing `codeBlockStyle: 'fenced'` at line 112 and the `lc-example-block` rule at lines 184-212 that already wraps Shape A LC `<pre>` blocks in fenced `text` blocks).
    .planning/phases/06-foundations-preview-mode/06-UAT.md (gap #1 — examples render as plain text).
    src/preview/ProblemPreviewView.ts:434 (the call site `htmlToMarkdown(detail.contentHtml)` is what feeds the preview body).
    tests/notes/NoteWriter.starter-retrofit.test.ts (the only existing notes-test file — mirror its imports/helpers shape).
  </read_first>

  <behavior>
    - Test 1: Given LC's standard Example shape (`<p><strong class="example">Example 1:</strong></p><pre>Input: nums = [2,7,11,15], target = 9\nOutput: [0,1]\nExplanation: ...</pre>`), `htmlToMarkdown` returns Markdown that contains a fenced ` ```text\n...\n``` ` block with the example body, NOT a four-space-indented prose block and NOT bare paragraph text.
    - Test 2: Given a real-language fenced block (`<pre><code class="language-python">def f(): pass</code></pre>`), the output is a fenced ```python block — confirms the `lc-example-block` rule's filter correctly defers to the built-in fencedCodeBlock rule for real code, so we don't accidentally double-fence Python in `text`.
    - Test 3: The output for any LC `<pre>` content NEVER begins with `    ` (four spaces) — locks `codeBlockStyle: 'fenced'` against future regressions where someone flips the default back to `indented`.
    - Test 4: Given Shape B LC examples (`<p><strong>Example 1:</strong></p><p><strong>Input:</strong> s = "0"</p><p><strong>Output:</strong> true</p>`), `htmlToMarkdown` collapses the consecutive label paragraphs into a single ` ```text\n...\n``` ` block (locks the existing `reshapeShapeBExamples` post-processing pass).
  </behavior>

  <action>
    Create `tests/notes/htmlToMarkdown-fenced.test.ts` mirroring the import shape of `tests/notes/NoteWriter.starter-retrofit.test.ts` (use `vi.mock('obsidian', ...)` only if turndown's runtime path imports anything Obsidian-flavoured — it does not, per the `htmlToMarkdown.ts` header comment "imports ONLY from 'turndown'"). Write the four tests above as deterministic input→output assertions. Do NOT modify `src/notes/htmlToMarkdown.ts` itself — the contract is already correct (`codeBlockStyle: 'fenced'`, `fence: '```'`, `lc-example-block` rule, `reshapeShapeBExamples` post-pass). The test simply LOCKS that contract so any future executor who flips it breaks Vitest before they break the preview.

    Edge handling notes for the test:
    - The `lc-example-block` rule emits `\n\n```text\n...\n```\n\n`. Assertions should use `toMatch(/```text\n[\s\S]+?\n```/)` rather than exact string equality (whitespace trimming and surrounding paragraphs vary).
    - The `reshapeShapeBExamples` pass operates on the post-turndown Markdown string, so a Shape B input may produce ` ```text\nInput: ...\nOutput: ...\n``` ` even though the source had no `<pre>` tag.
    - Do NOT try to assert on the surrounding `**Example N:**` text — it stays as plain bold per the rule's documented contract; the only locked invariant is "the example body lives inside a fence".
  </action>

  <verify>
    <automated>npx vitest run tests/notes/htmlToMarkdown-fenced.test.ts --reporter=verbose</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/notes/htmlToMarkdown-fenced.test.ts` exits 0.
    - `grep -nE "codeBlockStyle:\s*'fenced'" src/notes/htmlToMarkdown.ts` returns at least one match (line 112 today; the test fails if anyone changes this).
    - `grep -n "lc-example-block" src/notes/htmlToMarkdown.ts` returns at least one match (line 184 today).
    - `grep -n "reshapeShapeBExamples" src/notes/htmlToMarkdown.ts` returns at least one match (lines 270, 344 today).
    - `npm run lint` exits 0 (no `@typescript-eslint/no-explicit-any` or unused-import flags introduced).
    - Diff to `src/notes/htmlToMarkdown.ts` is empty (this task is test-only).
  </acceptance_criteria>

  <done>
    `tests/notes/htmlToMarkdown-fenced.test.ts` exists with the four behavioral tests above. The full vitest suite passes (regression-clean — 731 → 735 expected; +4 tests, no failures). `src/notes/htmlToMarkdown.ts` is unchanged. The fenced-code-block contract for LC content is now locked against future regressions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restructure the sticky header to a single strip and remove topic chips; promote the body to markdown-rendered for reading-mode parity</name>

  <files>
    src/preview/ProblemPreviewView.ts (modify renderHeader + renderRendered)
    styles.css (drop `.lc-preview__topic` rule + lift the body's font-size override + ensure the chip row uses flex with action button on the right)
    tests/preview/header-render.test.ts (drop topic-chip assertions; add no-topic-chip assertion + action-button-is-child-of-header assertion + body-class assertion)
    tests/preview/regression-grep.test.ts (add a gate that asserts NO `.lc-preview__topic` selector remains anywhere in `src/` or `styles.css`)
  </files>

  <read_first>
    src/preview/ProblemPreviewView.ts:116-167 (renderHeader — note the topic-chips loop at lines 142-147 is the deletion target; the action-button creation at lines 152-167 stays unchanged).
    src/preview/ProblemPreviewView.ts:410-442 (renderRendered — the body container is created at line 433 with class `'leetcode-preview__body'`; this becomes `'leetcode-preview__body markdown-rendered'`).
    styles.css:1233-1336 (the entire Phase 06 Plan 03 preview CSS block; the `.leetcode-preview__topic` rule at lines 1291-1301 is the deletion target; the root `.leetcode-preview { font-size: var(--font-ui-small); }` at lines 1233-1236 must move OFF the root and ONTO `.leetcode-preview__header` only so the body inherits reading-mode defaults).
    tests/preview/header-render.test.ts (full file — the test at lines 58-70 asserts topic-chip count; that assertion is replaced with an "exactly zero topic chips" guard).
    tests/preview/regression-grep.test.ts:31-100 (the existing GATE structure — add a parallel GATE 8 for the no-topic-chip-CSS contract).
    .planning/phases/06-foundations-preview-mode/06-UI-SPEC.md §Layout (the original spec showed the action button on the chip row already; this task removes the topic chips between the difficulty pill and the action button so the visual collapses to a single strip).
  </read_first>

  <behavior>
    - Test 1: After `renderHeader(container, detail, false)`, `container.querySelectorAll('.lc-preview__topic').length === 0` for ANY `topicSlugs` array on the detail (including a non-empty `['array', 'hash-table']`).
    - Test 2: After `renderHeader(container, detail, false)`, the action button (`button.lc-preview__action`) is a descendant of the header element (the same container or its child); the test asserts `headerEl.contains(button) === true` AND `button.parentElement.classList.contains('lc-preview__chips')` (the action button stays inside the chip row, which is now a single horizontal strip with `margin-left: auto` pushing it right).
    - Test 3: The existing assertions for the title (`h2.lc-preview__title`), the difficulty pill (`.lc-diff--{difficulty}`), the action button label (`Start Problem` / `Open Problem`), and the aria-label all continue to pass unchanged.
    - Test 4: The body container created in `renderRendered` has class `leetcode-preview__body` AND `markdown-rendered`. Add this assertion to the existing `tests/preview/start-button.test.ts` happy-path test OR a new compact assertion in `header-render.test.ts` that drives `renderRendered` indirectly — pick the lighter path (likely: extend `header-render.test.ts` with a `it('body container carries markdown-rendered class for reading-mode parity', ...)` block that constructs a minimal view stub and inspects `root.querySelector('.leetcode-preview__body')?.classList.contains('markdown-rendered')`).
    - Test 5 (regression-grep GATE 8): `grep -n 'lc-preview__topic' src/ styles.css` returns zero matches (across .ts and .css). Lock the deletion at the source-tree level so a future regression that re-adds the topic-chip CSS or class fails CI.
  </behavior>

  <action>
    A. **`src/preview/ProblemPreviewView.ts` — `renderHeader` (lines 116-167):**
       - DELETE the topic-chips emission loop at lines 142-147 (`for (const topic of detail.topicSlugs ?? []) { chipRow.createSpan({...}); }`).
       - DELETE the now-unused `topicSlugToDisplay` helper at lines 172-178 if no other call site references it (grep confirms it's the only user).
       - LEAVE the difficulty pill emission and action button emission unchanged — they stay siblings inside the existing `chipRow` div.
       - ADD a comment block above renderHeader explaining the chrome contract: "Single-strip header (gap-closure 06-05): title, difficulty pill, action button. Topic chips dropped per user override of CONTEXT.md decision C."

    B. **`src/preview/ProblemPreviewView.ts` — `renderRendered` (line 433):**
       - Change `root.createDiv({ cls: 'leetcode-preview__body' })` to `root.createDiv({ cls: 'leetcode-preview__body markdown-rendered' })`. The space-separated class list ensures Obsidian's reading-mode CSS cascade (code-block backgrounds, copy buttons, prose typography) applies to the rendered body. `MarkdownRenderer.render(this.app, md, body, '', this)` is unchanged — same signature, same Component arg, same regression-grep gate result.

    C. **`styles.css` — Phase 06 preview block (lines 1233-1336):**
       - LIFT `font-size: var(--font-ui-small);` OFF the `.leetcode-preview { ... }` root rule (line 1235). Move it INTO `.leetcode-preview__header` only (or alternatively into `.leetcode-preview__chips` if the title's `font-size: 18px` already overrides). The body must NOT inherit a chrome font; reading-mode parity = body inherits Obsidian's normal `markdown-rendered` cascade.
       - DELETE the `.leetcode-preview__topic { ... }` rule entirely (lines 1291-1301; ten lines of CSS).
       - VERIFY `.leetcode-preview__chips` retains `display: flex; align-items: center;` so the existing `margin-left: auto` on `.leetcode-preview__action` (line 1304) keeps pushing the action button to the right edge of the strip.
       - DO NOT touch the v1.0 `.leetcode-browser` rules at styles.css:184-186 (the `.lc-diff--*` browser-scoped colors). That's a hard rule from CONTEXT.md and the UI-SPEC.

    D. **`tests/preview/header-render.test.ts`:**
       - REPLACE the test at lines 58-70 ("emits N topic chips equal to topicSlugs.length…") with: "emits ZERO `.lc-preview__topic` elements regardless of topicSlugs length" — assert `container.querySelectorAll('.lc-preview__topic').length === 0` even when `topicSlugs: ['array', 'hash-table', 'two-pointers']`.
       - ADD a new test "action button sits inside the chip row inside the header strip" that asserts: header element `.lc-preview__chips` is the action button's parent, AND the title `h2.lc-preview__title` and the chip-row are siblings under the same outer container (which is the sticky header).
       - LEAVE all other tests (title, difficulty pill, button label/class, aria-label, idempotent re-render) unchanged. They should continue to pass without edits.

    E. **`tests/preview/regression-grep.test.ts`:**
       - ADD `GATE 8: NO topic-chip CSS class survives in source tree (gap-closure 06-05)`. Read every `.ts` file under `src/` AND `styles.css` via `fs.readFileSync`; assert `expect(src).not.toMatch(/lc-preview__topic/)` for each. Use the existing GATE 1-7 pattern (the file already iterates `src/preview/` files; extend the search to ALL `src/` plus the project root `styles.css`).

    F. **Optional but recommended:** if `topicSlugToDisplay` (lines 172-178) is now unreferenced after step A, delete it. If a future task wants to surface topic chips elsewhere (per the deferred backlog item), they can recreate it.

    Implementation discipline:
    - No `cm.dispatch` introduced (preview is read-only — gate 4 stays green).
    - No new `vault.create` / `openLinkText` / `innerHTML =` (gates 1-3 stay green).
    - The `MarkdownRenderer.render(this.app, md, body, '', this)` call signature is BYTE-IDENTICAL — gate 5 stays green.
    - Color tokens stay Obsidian variables — no raw hex introduced (UI-SPEC hard rule).
    - The `is-primary` accent class is still applied EXACTLY ONCE in `renderHeader`, gated on `noteExists === false` — gate 6 stays green.
    - All copy strings ('Start Problem', 'Open Problem', etc.) stay exactly as defined in `COPY` constants (lines 59-75 of ProblemPreviewView.ts) — UI-SPEC §Copywriting is preserved.
  </action>

  <verify>
    <automated>npx vitest run tests/preview/header-render.test.ts tests/preview/regression-grep.test.ts tests/preview/start-button.test.ts --reporter=verbose</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/preview/` exits 0 with all preview tests green (header-render with the new no-topic-chip + action-in-header assertions, regression-grep with the new GATE 8, start-button still passing).
    - `grep -nE 'lc-preview__topic' src/ styles.css -r 2>/dev/null | grep -v '\.test\.ts:'` returns ZERO non-test matches.
    - `grep -nE 'lc-preview__topic' src/preview/ styles.css -r` returns ZERO matches (no production references).
    - `grep -n "markdown-rendered" src/preview/ProblemPreviewView.ts` returns at least one match (the body class addition).
    - `grep -nE "font-size:\s*var\(--font-ui-small\)" styles.css | head -5` — the FIRST match is no longer line 1235 (`.leetcode-preview` root); it has moved to `.leetcode-preview__header` or `.leetcode-preview__chips`.
    - `grep -n "MarkdownRenderer.render(" src/preview/ProblemPreviewView.ts` ends with `, this)` (unchanged from gate 5 — Component contract preserved).
    - `grep -n "is-primary" src/preview/ProblemPreviewView.ts | wc -l` reports exactly 1 (gate 6 — accent reserved).
    - `npm run lint` exits 0.
    - `npm run build` exits 0.
    - Manual UAT checkpoint (re-test #2 after fix): single-click a problem in the browser; the preview body shows examples in fenced grey code blocks identical to reading mode; the header is a single horizontal strip (title, difficulty pill, action button right-aligned) — no topic chips visible. Recorded on the next UAT pass; not blocking on this automated task.
  </acceptance_criteria>

  <done>
    `src/preview/ProblemPreviewView.ts` no longer emits topic-chip elements; `renderRendered` adds `markdown-rendered` to the body container. `styles.css` no longer carries the `.lc-preview__topic` rule, and the chrome font-size declaration has been lifted off the root selector so the body inherits reading-mode defaults. `tests/preview/header-render.test.ts` asserts zero topic chips and the action button's containment chain. `tests/preview/regression-grep.test.ts` GATE 8 locks the deletion at the source-tree level. The full Vitest suite is green; `npm run lint` and `npm run build` are clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Enter-key activation for the preview action button</name>

  <files>
    src/preview/ProblemPreviewView.ts (add scope.register in onOpen + a private `activeAction: HTMLButtonElement | null` field updated by renderRendered)
    tests/preview/enter-key.test.ts (new)
  </files>

  <read_first>
    src/preview/ProblemPreviewView.ts:228-285 (the `onOpen`, `onClose`, and `setState` lifecycle methods — Enter-key registration goes in `onOpen` so Obsidian auto-cleans on view close; the `this.scope` reference comes from `ItemView` via `Component`).
    src/preview/ProblemPreviewView.ts:410-494 (`renderRendered` and `handleActionClick` — the existing click handler is the SAME handler the Enter key fires; do NOT introduce a parallel handler — just `button.click()` from the Enter callback so disable/label-flip/openProblem/detach all run in the proven path).
    tests/preview/start-button.test.ts (lines 91-144 — the existing pattern for driving the view via `setState({slug})` with a hand-rolled stub plugin; mirror that setup in the new enter-key test).
    tests/helpers/obsidian-stub.ts (the obsidian stub — `ItemView` is currently `export class ItemView {}`; you may need to add a minimal `scope: { register, unregister }` field on the stub if the production code references `this.scope` and the test doesn't supply it. Mirror the additive polyfill pattern from Plan 06-03 deviation #2.)
    .planning/phases/06-foundations-preview-mode/06-UAT.md (gap #3 — Enter activation).
    CLAUDE.md (the `'leetcode.*'` userEvent rule does NOT apply here — preview never dispatches CM6 transactions; Enter goes through the DOM/Scope layer, not the editor's transaction filter).
  </read_first>

  <behavior>
    - Test 1: Mount a `ProblemPreviewView` via the start-button.test.ts pattern; render a cache-hit detail; capture the action button. Programmatically invoke the Enter handler (either by triggering `keydown` on the view container with `key: 'Enter'` OR by calling the function the view registered with `scope.register` — the test stub should expose registered handlers in a captured array). Assert: `plugin.openProblem` is called exactly once with `(slug, undefined)` — same call shape as a real click (verified by the existing start-button test).
    - Test 2: Pressing Enter when `noteExists === true` triggers the same delegate as clicking "Open Problem" (label flip to `Opening…`, `plugin.openProblem` called).
    - Test 3: Pressing Enter when there is no rendered action button (e.g., the view is in the loading state) is a no-op — no `plugin.openProblem` call, no error thrown.
    - Test 4: After `onClose`, the captured `scope.register` handler list either no longer contains the Enter handler OR the handler returns early because `this.activeAction === null` post-close. Pick the cleaner contract — Obsidian's real `Component.onunload` automatically releases scope registrations, so the test can simply assert "no `plugin.openProblem` call after `onClose`".
  </behavior>

  <action>
    A. **`src/preview/ProblemPreviewView.ts` — class additions:**
       - ADD a private field `private activeAction: HTMLButtonElement | null = null;` next to the existing private fields (around line 197-202).
       - In `renderRendered` (the cache-hit / post-fetch path), after capturing `actionBtn = renderHeader(...)`, set `this.activeAction = actionBtn;`.
       - In `renderLoading`, `renderError`, `renderEmpty`: clear `this.activeAction = null;` so the Enter handler is a no-op while the view is in a non-rendered state.
       - In `onClose`: clear `this.activeAction = null;` (defensive — Obsidian also tears down scope registrations).

    B. **`src/preview/ProblemPreviewView.ts` — `onOpen` Enter-key registration:**
       - In `onOpen` (after `this.rootEl = root;` near line 232), call `this.scope.register([], 'Enter', () => { this.activeAction?.click(); return false; });`. The empty modifier array `[]` matches an unmodified Enter (no Shift / Ctrl / Meta). Returning `false` stops the event from propagating to the workspace's default Enter handler. `this.scope` is provided by `ItemView` (it extends `View` which extends `Component` which provides `scope: Scope`). This is the canonical Obsidian pattern (see Obsidian developer docs §Scope).
       - Document in a code comment: "Enter activates the visible action button. `this.activeAction?.click()` reuses the existing click handler — same disable/label/openProblem/detach pipeline. Null-check guards loading/error states. Obsidian releases the scope registration automatically when the view closes (Component.onunload)."

    C. **`tests/helpers/obsidian-stub.ts` — Scope stub (additive):**
       - If `ItemView` in the stub does NOT have a `scope` field, ADD a minimal `Scope`-like stub: `class Scope { handlers: Array<{ mods: string[]; key: string; cb: () => unknown }> = []; register(mods: string[], key: string, cb: () => unknown) { this.handlers.push({ mods, key, cb }); return { mods, key, cb }; } }` and instantiate it on `ItemView` (`scope = new Scope();`). The test inspects this captured array OR invokes `plugin.app.scope.handlers.find(h => h.key === 'Enter')?.cb()`.
       - This mirrors Plan 06-03's deviation #2 (HTMLElement polyfills and Menu stub) — additive only, no existing test breaks.

    D. **`tests/preview/enter-key.test.ts` (new):**
       - Mirror the imports + `vi.mock('obsidian', ...)` setup from `tests/preview/start-button.test.ts` (lines 14-22, 36-72).
       - Set up the same view stub pattern (lines 91-119): hand-rolled `containerEl` with two children, `Object.create(ProblemPreviewView.prototype)`, attach `containerEl`, `leaf`, `app`, `plugin`, AND a `scope` stub as wired in step C.
       - Call `view.onOpen()` (or whatever lifecycle hook plants the scope registration; since the stub's `ItemView` is empty, you may need to call onOpen explicitly — `await view.onOpen();` is acceptable because the stub's containerEl already has `children[1]`).
       - Drive a cache-hit render via `await view.setState({slug: 'two-sum'}, ...)`.
       - Locate the registered Enter handler (`view.scope.handlers.find(h => h.key === 'Enter')`) and INVOKE it (`handler.cb()`).
       - Assert: `stub.openProblem` is called exactly once with `('two-sum', undefined)`.
       - Add the noteExists=true variant (Test 2) and the loading-state no-op variant (Test 3 — call the handler BEFORE setState; expect openProblem NOT called).
       - Add the post-close variant (Test 4 — call `onClose`, then invoke the captured handler; assert no extra openProblem call).
  </action>

  <verify>
    <automated>npx vitest run tests/preview/enter-key.test.ts tests/preview/start-button.test.ts tests/preview/regression-grep.test.ts --reporter=verbose</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/preview/enter-key.test.ts` exits 0 with the four tests above passing.
    - `npx vitest run tests/preview/` exits 0 (no regression in start-button, header-render, regression-grep, etc.).
    - `grep -nE "scope\.register\(\s*\[\s*\]\s*,\s*['\"]Enter['\"]" src/preview/ProblemPreviewView.ts` returns at least one match.
    - `grep -n "activeAction" src/preview/ProblemPreviewView.ts | wc -l` reports at least 4 matches (declaration + assignment in renderRendered + clear in renderLoading/renderError + clear in onClose).
    - `grep -n "cm\.dispatch(" src/preview/` returns ZERO matches (regression-grep GATE 4 — Enter goes through Scope, not CM6).
    - `grep -nE "MarkdownRenderer\.render\(.*, this\)" src/preview/ProblemPreviewView.ts` still matches (gate 5 unchanged).
    - `npm run lint` exits 0.
    - `npm run build` exits 0.
    - `npm run check:bundle-size` exits 0 (well under 500 KB; the changes net ~10-20 lines so bundle stays near 165 KB).
  </acceptance_criteria>

  <done>
    `src/preview/ProblemPreviewView.ts` registers `scope.register([], 'Enter', () => this.activeAction?.click())` in `onOpen`, with `activeAction` synchronized to the rendered button (cache-hit path) and cleared in loading/error/empty/onClose. `tests/preview/enter-key.test.ts` locks the four Enter contracts. `tests/helpers/obsidian-stub.ts` carries an additive Scope stub for the test path. The full Vitest suite is green; lint, build, and bundle-size gates all pass.
  </done>
</task>

</tasks>

<verification>
**Gap-closure verification — all three UAT gaps resolved.**

| UAT Gap | Verification |
|--------|--------------|
| #1 Body parity (MAJOR) | Task 1's fenced-block tests lock the Turndown contract; Task 2's `markdown-rendered` class on the body container ensures Obsidian's reading-mode CSS cascade applies. Manual UAT re-test of scenario #2: examples render as fenced grey code blocks; body font matches reading mode. |
| #2 Header chrome (MAJOR) | Task 2's renderHeader patch removes topic-chip emission; the `.lc-preview__topic` CSS rule is deleted; regression-grep GATE 8 locks the absence at the source-tree level; header-render.test asserts zero topic chips and action-button containment in the header strip. |
| #3 Enter key (MINOR) | Task 3's `scope.register([], 'Enter', ...)` wires the keyboard activation; enter-key.test locks the four behavioral contracts (cache hit + noteExists branch, loading no-op, post-close no-op). |

**Regression posture (Plan 06-03 invariants preserved):**

- All six original UI-SPEC acceptance grep gates remain green:
  1. Preview never creates files (gates 1-3) — no edits to file/link/innerHTML surfaces.
  2. MarkdownRenderer.render(..., this) — unchanged signature.
  3. Tab-reuse via getLeavesOfType — untouched.
  4. No raw hex in preview CSS — only deletions and a font-size lift.
  5. is-primary accent reserved for Start Problem — renderHeader's button branch unchanged.
  6. Clean command IDs — no command surface touched.
  7. No CM6 dispatch in preview — Enter goes through Scope.
- New gate 8: no `lc-preview__topic` selector anywhere in source.
- `routeProblemClick` decision flow, cache-miss persist contract, post-action detach lifecycle — all untouched.
- `npm run lint` / `npm test` / `npm run build` / `npm run check:bundle-size` all green.

**Phase plan-level gate (final):**

```
npm run lint              → 0 errors / 0 warnings
npm test                  → 731 + ~10 new = ~741 passing across 109 files (3 new test files)
npm run build             → tsc clean + production bundle
npm run check:bundle-size → ~165 KB (well under 400 KB soft warn)
```
</verification>

<success_criteria>
1. `tests/notes/htmlToMarkdown-fenced.test.ts` exists and locks four behavioral contracts (Shape A example → fenced text, Python code block → fenced python, no four-space indent, Shape B examples → fenced text).
2. `tests/preview/enter-key.test.ts` exists and locks four Enter-key contracts (cache hit, noteExists branch, loading no-op, post-close no-op).
3. `tests/preview/header-render.test.ts` asserts zero topic-chip elements + action-button containment in the header strip + body class includes `markdown-rendered`.
4. `tests/preview/regression-grep.test.ts` GATE 8 enforces "no `lc-preview__topic` selector survives".
5. `src/preview/ProblemPreviewView.ts` no longer emits topic chips; `renderRendered` body container has `markdown-rendered`; `onOpen` registers Enter via `this.scope.register([], 'Enter', ...)`; `activeAction` field tracks the live button.
6. `src/notes/htmlToMarkdown.ts` is BYTE-IDENTICAL to the pre-plan version (Task 1 is test-only).
7. `styles.css` no longer contains `.lc-preview__topic`; chrome font-size declaration is on the header (or chip row) only, not the root.
8. `npm run lint`, `npm test`, `npm run build`, `npm run check:bundle-size` all exit 0 at HEAD after the plan commit.
9. The three UAT gap truths in `06-UAT.md` are flippable from `failed` to `passed` after a manual re-test of scenario #2.
</success_criteria>

<output>
After completion, create `.planning/phases/06-foundations-preview-mode/06-05-SUMMARY.md` covering:
- Per-task commit hashes (3 atomic commits expected: test-only fenced lock, header restructure + body parity, Enter-key wiring).
- Bundle-size delta (expect within ±2 KB of baseline 165.0 KB).
- Final test count after the plan (expected ~741, +10 from baseline 731).
- The UAT scenario #2 re-test outcome (manual; recorded by the next UAT pass — this plan's SUMMARY notes "ready for re-test" rather than the test outcome itself).
- Deviation log if any auto-fixes were needed (e.g., obsidian-stub Scope addition mirrors Plan 06-03's pattern).
- Self-check: file-creation verification, commit verification, and the seven Plan 06-03 acceptance grep gates (still green) + the new GATE 8.
</output>

## PLANNING COMPLETE
