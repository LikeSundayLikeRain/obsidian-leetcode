---
phase: 06
plan: 04
type: execute
wave: 4
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
files_modified:
  - README.md
autonomous: true
requirements:
  - FOUND-02
tags:
  - documentation
  - readme
  - bundle-size
must_haves:
  truths:
    - "README has a `## Bundle size` (or equivalent) subsection that states the 500 KB hard ceiling, the 400 KB soft warning, and the current baseline (≈163 KB at HEAD)."
    - "README documents the v1.1 click-default change: single-click now previews; shift-click or `Preview › Click behavior = Open note directly` restores v1.0 behavior."
    - "README documents the right-click `Preview problem` affordance and the `Open in preview` palette command."
    - "Network use section still enumerates `leetcode.com` (no new endpoints in Phase 06)."
    - "Running `npm run build && npm run check:bundle-size` at HEAD reports a baseline well under 400 KB and exits 0 with no soft-warning."
  artifacts:
    - path: README.md
      provides: "v1.1 click-default behavior change docs + click-behavior toggle docs + bundle-size policy + baseline"
      contains: "Bundle size"
  key_links:
    - from: README.md
      to: scripts/check-bundle-size.mjs
      via: "Bundle size subsection cites the 500 KB hard / 400 KB soft thresholds enforced by the script"
      pattern: "500"
---

<objective>
Update `README.md` to capture three v1.1 documentation deltas: (1) the click-default behavior change (single-click previews; shift-click opens; `Preview › Click behavior` settings toggle restores v1.0); (2) the right-click `Preview problem` affordance and the `Open in preview` command palette entry; (3) a new `Bundle size` subsection that states the 500 KB hard ceiling, the 400 KB soft warning, the current baseline, and that violations fail CI. Re-confirm the baseline by running `npm run build && npm run check:bundle-size` and quoting the actual byte count in the README.

Purpose: Honors CONTEXT.md decision A (no onboarding modal — README is the documentation surface) and CONTEXT.md decision E (baseline noted in README only — no `bundle-baseline.txt` committed). This plan is intentionally documentation-only and runs last so the docs reflect the final shipped behavior, not a planning-time guess.

Output: Updated `README.md` with three new subsections / paragraph deltas; a verified bundle baseline number.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-foundations-preview-mode/06-CONTEXT.md
@.planning/phases/06-foundations-preview-mode/06-RESEARCH.md
@.planning/phases/06-foundations-preview-mode/06-PATTERNS.md
@.planning/phases/06-foundations-preview-mode/06-UI-SPEC.md
@CLAUDE.md
@README.md
@.github/workflows/ci.yml
@scripts/check-bundle-size.mjs

<interfaces>
<!-- Doc-only plan — interfaces are the locked copy strings and the section locations in README. -->

### Locked copy (lifted verbatim from 06-UI-SPEC.md §Copywriting Contract — no rewriting)

- Default click behavior: "Single-click on a problem in the LeetCode browser previews it in a new tab. Shift-click opens the note directly."
- Settings toggle: "Open Settings → Preview → Click behavior. Choose `Preview first` (default) or `Open note directly` to restore v1.0 behavior."
- Right-click affordance: "Right-click any problem in the browser and pick `Preview problem`."
- Palette command: "Run `Open in preview` from the command palette while viewing a problem note."

### Bundle size subsection content (CONTEXT.md decision E — locked)

Required statements:
1. Production bundle target: `main.js` MUST stay under 500 KB.
2. Soft warning at 400 KB (warning logged but build still passes).
3. Current baseline: the ACTUAL byte count from running `npm run build && npm run check:bundle-size` against this branch (baseline at planning time was ≈163 KB / 162,229 bytes per RESEARCH §11; verify it has not drifted dramatically — a drift > 50 KB requires an investigation note before continuing).
4. Violations fail CI (the gate runs as the last step of `.github/workflows/ci.yml`).
5. The check is enforced by `scripts/check-bundle-size.mjs` (Node, platform-portable).

### README structural anchors (executor reads README to find the right insertion points)

- The README likely already has section headings — typical Obsidian plugin READMEs include `## Features`, `## Installation`, `## Usage`, `## Network use`, `## Privacy`, `## Development`, `## License`. Executor reads README, identifies the existing structure, and inserts:
  - Click-default behavior change → either a new bullet/paragraph in the Usage / Features section OR a brief callout near the top noting the v1.1 behavior change (executor's discretion — match existing README tone).
  - Right-click + palette command docs → adjacent to the click-default copy.
  - Bundle size subsection → under `## Development` (or analog), since it's a contributor-facing concern.

If README has no `## Development` section, create one and place `### Bundle size` under it. Do NOT add a `### Bundle size` section at top level — it's a development-time concern, not a user-facing one.

### What NOT to change

- Do NOT change the `## Network use` section content — Phase 06 introduces zero new endpoints (preview reuses the existing v1.0 LeetCodeClient HTTP path).
- Do NOT add any onboarding-modal language (CONTEXT.md decision A explicitly rejects an onboarding modal — README is the only documentation surface for the click-default change).
- Do NOT commit a `bundle-baseline.txt` file (CONTEXT.md decision E explicitly rejects baseline file commit).
- Do NOT add PR-comment automation, pre-push hook docs, or any deferred items from CONTEXT.md `<deferred>`.
- Do NOT change `manifest.json`, `package.json` version, or `versions.json` — version bump to 1.1.0 is Phase 12's responsibility.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture verified bundle baseline by running the production build + size script</name>
  <files>(no source changes — this task only captures a number)</files>
  <read_first>
    - scripts/check-bundle-size.mjs (verify the script exists from Plan 06-01)
    - .github/workflows/ci.yml (verify the workflow exists from Plan 06-01)
    - .planning/phases/06-foundations-preview-mode/06-RESEARCH.md §11 (planning-time baseline reference: 162,229 bytes)
  </read_first>
  <behavior>
    - `npm run build` exits 0.
    - `npm run check:bundle-size` exits 0 with no `WARN` line in stderr (current bundle should be well under 400 KB even after Plan 06-03's preview view additions — the new code is small: ~3 source files in src/preview/ plus ~2 KB CSS).
    - The actual byte count is recorded for use in Task 2's README copy.
    - If the bundle has crossed 400 KB unexpectedly, halt and surface to checker — Phase 06 should not push the bundle over 200 KB.
  </behavior>
  <action>
    Run `npm run build` (allow it to complete; this is the production esbuild path). Then run `npm run check:bundle-size`. Capture the stdout (a line of shape `main.js: {bytes} bytes ({kb} KB)` followed by `BUNDLE CHECK OK`). Record the byte count and KB rounded to one decimal. If `npm run check:bundle-size` emits a `WARN` line OR exits non-zero, STOP — record the actual size and surface it before continuing. (FOUND-02 baseline verification.)
  </action>
  <verify>
    <automated>npm run build && npm run check:bundle-size</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `npm run check:bundle-size` exits 0.
    - The stdout line `main.js: {N} bytes ({K} KB)` is captured for use in Task 2.
    - Captured `{N}` is < 400_000 (no soft warning at HEAD).
  </acceptance_criteria>
  <done>
    Verified bundle baseline at HEAD; number recorded for Task 2 README insertion.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update README.md with v1.1 click-default change docs, right-click + palette docs, and Bundle size subsection</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — to identify existing section structure and insertion points)
    - .planning/phases/06-foundations-preview-mode/06-UI-SPEC.md §Copywriting Contract (locked strings — copy verbatim)
    - .planning/phases/06-foundations-preview-mode/06-CONTEXT.md decision A + decision E (the rules: no onboarding modal; baseline in README only)
    - .planning/phases/06-foundations-preview-mode/06-RESEARCH.md §1 (baseline 162,229 bytes — for cross-check against Task 1's captured number)
    - The captured byte count from Task 1
  </read_first>
  <behavior>
    - README's user-facing usage area documents:
      - Single-click previews; shift-click opens the note directly.
      - Right-click `Preview problem` opens the same preview regardless of the click-behavior setting.
      - `Open in preview` command palette entry (works while viewing a problem note).
      - The `Preview › Click behavior` settings dropdown lets users restore v1.0 click-to-create.
    - README has a `### Bundle size` subsection under `## Development` (creating the `## Development` section if it does not already exist) stating the 500 KB hard ceiling, 400 KB soft warning, the captured baseline byte count + KB, that the gate is enforced by `scripts/check-bundle-size.mjs` as the last step of CI, and that PRs that push `main.js` over 500 KB will fail CI.
    - `## Network use` section (if it exists) is UNCHANGED — Phase 06 introduces zero new endpoints.
    - `manifest.json`, `package.json` version, `versions.json` are NOT modified.
    - No `bundle-baseline.txt` file is committed (locked decision E).
    - No onboarding-modal language is added (locked decision A).
  </behavior>
  <action>
    Read `README.md` in full. Identify the existing top-level section structure. Insert the click-default change documentation at the most natural location: prefer a new bullet under an existing `## Usage` or `## Features` section; if neither exists, add a brief `## What's new in v1.1` paragraph (one paragraph; no heading proliferation). Use the locked copy from 06-UI-SPEC.md verbatim — do NOT paraphrase the dropdown labels (`Preview first` / `Open note directly`) or the menu item title (`Preview problem`). Add a `### Bundle size` subsection under `## Development` (create `## Development` only if it does not exist) stating: (a) `main.js` ceiling is 500 KB (CI fails on violation); (b) soft warning at 400 KB (CI passes with a warning log); (c) baseline at v1.1 entry: `~{K} KB ({N} bytes)` using the values from Task 1; (d) gate is enforced by `scripts/check-bundle-size.mjs` as the last step of `.github/workflows/ci.yml`. Do NOT modify the `## Network use` section, the `## Privacy` section, the `## License` section, the `manifest.json`, `package.json` version, or `versions.json`. Do NOT add any onboarding-modal language. Do NOT add PR-comment automation or pre-push hook docs (deferred per CONTEXT.md). (FOUND-02 README + click-default docs.)
  </action>
  <verify>
    <automated>grep -nE 'Bundle size|Click behavior|Preview problem|Open in preview' README.md | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'Bundle size' README.md` returns at least one match.
    - `grep -n '500' README.md` returns at least one match (hard ceiling).
    - `grep -n '400' README.md` returns at least one match (soft warning).
    - `grep -n 'Preview problem' README.md` returns at least one match (right-click menu copy).
    - `grep -n 'Open in preview' README.md` returns at least one match (palette command).
    - `grep -nE 'Preview first|Open note directly' README.md` returns at least one match each (settings dropdown labels — verbatim).
    - `grep -n 'shift-click' README.md` (case-insensitive) returns at least one match (`grep -ni`).
    - `grep -n 'check:bundle-size\|check-bundle-size' README.md` returns at least one match (script citation).
    - The captured byte count (or its KB equivalent) from Task 1 appears in the Bundle size subsection.
    - `git diff manifest.json` shows NO changes (version bump is Phase 12's job).
    - `git diff package.json | grep '"version"'` shows NO version-line changes from this task.
    - `ls bundle-baseline.txt 2>/dev/null` returns no result (file MUST NOT be committed).
    - The phrase `onboarding modal` does NOT appear anywhere in the README (decision A — locked rejection).
  </acceptance_criteria>
  <done>
    README documents the v1.1 click-default change, the right-click + palette affordances, and the bundle-size policy with a verified baseline; manifest / package.json / versions.json untouched; no baseline file committed; no onboarding-modal language.
  </done>
</task>

</tasks>

<verification>
**Per-task verify commands** (above) cover unit-level assertions.

**Phase 06 final gate** — runs across all four plans' artifacts:

```bash
npm run lint && npm test && npm run build && npm run check:bundle-size
```

All 4 commands MUST exit 0 before Phase 06 is declared shipped.

**Documentation review** (manual):
- Read the modified README sections aloud — language matches the existing README's tone and voice.
- Confirm the locked dropdown labels (`Preview first` / `Open note directly`) appear verbatim.
- Confirm no marketing-speak or roadmap-leak (no mention of Phase 07 AI features, Phase 10 Contest, etc.).
- Confirm the Bundle size subsection cites the script path and the CI workflow location.
</verification>

<success_criteria>
- README documents single-click preview default + shift-click opens + settings toggle.
- README documents right-click `Preview problem` and `Open in preview` palette command.
- README has a `### Bundle size` subsection with the 500 KB hard / 400 KB soft thresholds and a verified baseline.
- `manifest.json`, `package.json` version, `versions.json` UNCHANGED.
- No `bundle-baseline.txt` committed.
- No onboarding-modal language.
- Network use section unchanged (no new endpoints in Phase 06).
- Phase 06 final gate green at HEAD: `npm run lint && npm test && npm run build && npm run check:bundle-size`.
</success_criteria>

<output>
After completion, create `.planning/phases/06-foundations-preview-mode/06-04-SUMMARY.md`:
- Files modified (just README.md)
- Captured bundle baseline (bytes + KB)
- README sections touched (with line ranges)
- Phase 06 final gate result (record actual exit codes for lint / test / build / check:bundle-size)
- Confirmation that no out-of-scope files were touched
- Phase 06 sign-off note for STATE.md update
</output>
