---
phase: 10-contest-virtual-analysis
plan: 04
type: execute
wave: 2
depends_on:
  - 10-02
files_modified:
  - src/contest/ContestSolveView.ts
  - src/main.ts
  - styles.css
  - tests/contest/ContestSolveView.test.ts
autonomous: true
requirements:
  - CONTEST-04
  - CONTEST-05
must_haves:
  truths:
    - "User can navigate to a contest problem and see problem content + code editor (per D-10)"
    - "User can edit code in the ContestSolveView and changes persist in PluginData (per D-09)"
    - "User can Run and Submit code from the ContestSolveView using existing LC REST infrastructure"
    - "Verdicts update the ContestProblemState (per D-09)"
    - "ContestSolveView is registered as an ItemView with viewType 'leetcode-contest-solve'"
  artifacts:
    - path: "src/contest/ContestSolveView.ts"
      provides: "ItemView for solving contest problems — code editor, Run/Submit, language selector"
      exports: ["ContestSolveView", "CONTEST_SOLVE_VIEW_TYPE"]
    - path: "src/main.ts"
      provides: "registerView for ContestSolveView"
  key_links:
    - from: "src/contest/ContestSolveView.ts"
      to: "src/contest/ContestSessionManager.ts"
      via: "updateCode + recordVerdict calls"
      pattern: "sessionManager\\.updateCode\\|sessionManager\\.recordVerdict"
    - from: "src/contest/ContestSolveView.ts"
      to: "src/solve/leetcodeRest.ts"
      via: "interpretSolution + submit for Run/Submit"
      pattern: "interpretSolution\\|submit"
---

<objective>
Build the ContestSolveView — the dedicated ItemView where users write and test their code during a virtual contest. Includes problem description rendering, code textarea, language selector, and Run/Submit buttons that use the existing LC REST infrastructure.

Purpose: This is the contest editing surface. Users navigate to problems from the timer header's problem cards (Plan 05) and land here. Code persists in PluginData (ephemeral — no .md file created per D-09).

Output: ContestSolveView registered as an ItemView, with code editing, language selection, and Run/Submit functionality.
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-contest-virtual-analysis/10-RESEARCH.md
@.planning/phases/10-contest-virtual-analysis/10-PATTERNS.md
@.planning/phases/10-contest-virtual-analysis/10-UI-SPEC.md
@.planning/phases/10-contest-virtual-analysis/10-CONTEXT.md

<interfaces>
<!-- Key contracts -->

From src/contest/types.ts (Plan 01):
```typescript
export interface ContestProblemState {
  slug: string;
  title: string;
  credit: number;
  difficulty: number;
  verdict: 'unsolved' | 'attempted' | 'accepted';
  code: string;
  language: string;
  solvedAt: number | null;
}
```

From src/contest/ContestSessionManager.ts (Plan 02):
```typescript
export class ContestSessionManager {
  updateCode(problemIdx: number, code: string, language: string): void;
  recordVerdict(problemIdx: number, verdict: 'attempted' | 'accepted'): void;
  getSession(): ContestSession | null;
}
```

From src/preview/ProblemPreviewView.ts (ItemView analog):
```typescript
export const PREVIEW_VIEW_TYPE = 'leetcode-preview';
export class ProblemPreviewView extends ItemView {
  getViewType(): string { return PREVIEW_VIEW_TYPE; }
  getIcon(): string { return 'eye'; }
  async setState(state: unknown, _result: ViewStateResult): Promise<void> { /* ... */ }
  getState(): { slug: string | null } { return { slug: this.slug }; }
}
```

From src/solve/leetcodeRest.ts:
```typescript
export async function interpretSolution(args: {
  fetcher: Fetcher; slug: string; code: string; langSlug: string;
  testcases: string; questionId: string; headers: Record<string, string>;
}): Promise<{ interpret_id: string }>;
```

From src/ai/AIStreamModal.ts (AIStreamModalArgs.onStreamComplete for contest analysis):
```typescript
export interface AIStreamModalArgs {
  provider: AIProvider;
  prompt: string;
  aiClient: AIClient;
  model?: string;
  disclosureCopy?: { willSend: readonly string[]; neverSends: readonly string[] };
  onStreamComplete?: (fullText: string) => Promise<void>;
  title?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContestSolveView ItemView with code editing + Run/Submit</name>
  <files>src/contest/ContestSolveView.ts, styles.css, tests/contest/ContestSolveView.test.ts</files>
  <read_first>
    - src/preview/ProblemPreviewView.ts (ItemView pattern — constructor, getViewType, getIcon, setState, getState, onOpen, onClose, renderToken guard)
    - src/solve/leetcodeRest.ts (interpretSolution, submit function signatures — for Run/Submit)
    - src/solve/submissionOrchestrator.ts (SubmissionOrchestrator pattern for submit flow)
    - src/main.ts (registerView pattern at line 325-334)
    - src/solve/languages.ts (LC_LANG_DISPLAY_LABELS, resolveLangSlug — for language selector)
    - src/notes/htmlToMarkdown.ts (htmlToMarkdown — for rendering problem description)
    - .planning/phases/10-contest-virtual-analysis/10-UI-SPEC.md (ContestSolveView layout section)
  </read_first>
  <action>
    Create src/contest/ContestSolveView.ts:
    - Export const CONTEST_SOLVE_VIEW_TYPE = 'leetcode-contest-solve'
    - Export class ContestSolveView extends ItemView
    - Constructor(leaf: WorkspaceLeaf, private readonly plugin: LeetCodePlugin)
    - Private fields: problemIdx: number | null, rootEl: HTMLElement | null, renderToken: number, codeEl: HTMLTextAreaElement | null, saveTimer: TimerHandle | null
    - getViewType(): CONTEST_SOLVE_VIEW_TYPE
    - getIcon(): 'trophy' (per UI-SPEC)
    - getDisplayText(): 'Contest: {n}. {title}' (from session.problems[idx]) or 'Contest' if no problem loaded

    setState(state: { problemIdx: number }): Sets this.problemIdx, triggers renderProblem()
    getState(): { problemIdx: this.problemIdx }

    onOpen(): Get root element (this.containerEl.children[1]), empty it, addClass 'leetcode-contest-solve', store as rootEl. If problemIdx set from prior state, render.

    onClose(): Flush pending code save. Null refs. Bump renderToken.

    Private renderProblem(): 
    - Read session from plugin.contestSessionManager.getSession()
    - Get problem state: session.problems[this.problemIdx]
    - Get cached problem detail from plugin.settings.getProblemDetail(problem.slug)
    - Render sticky header: "{n}. {title}" + difficulty pill + "Problem {n}/4" label
    - Render problem description via MarkdownRenderer.render(htmlToMarkdown(detail.contentHtml))
    - Render code textarea: pre-filled with problem.code, monospace font, background-secondary
    - Render language selector: chevron dropdown using LC_LANG_DISPLAY_LABELS (same pattern as existing language chevron)
    - Render action row: [Run] [Submit] buttons matching .leetcode-code-actions pattern
    - Wire code textarea oninput: update in-memory code, debounce 30s save via plugin.contestSessionManager.updateCode(this.problemIdx, code, language)
    - Wire language selector: on change, update language in session + re-render code area if needed

    Private handleRun():
    - Get problem state (slug, code, language) + detail (exampleTestcases, internalQuestionId)
    - Call interpretSolution via existing REST infra (same pattern as src/main.ts run command)
    - Open RunModal with result (reuse existing RunModal)
    - On WA/TLE/RE/CE: recordVerdict(idx, 'attempted') if currently 'unsolved'

    Private handleSubmit():
    - Get problem state + detail
    - Use existing submit flow (same pattern as src/main.ts submit command — throttledRequestUrl + pollSubmission via pollingOrchestrator)
    - Open VerdictModal with result (reuse existing)
    - On AC: recordVerdict(idx, 'accepted')
    - On WA/TLE/RE/CE: recordVerdict(idx, 'attempted')

    Add CSS to styles.css (append):
    - .leetcode-contest-solve (padding: 16px)
    - .leetcode-contest-solve__header (sticky, same pattern as .leetcode-preview__header)
    - .leetcode-contest-solve__code (background-secondary, border-radius:4px, font-family:var(--font-monospace), font-size:12px, line-height:1.6, padding:12px, width:100%, min-height:200px, resize:vertical, border:1px solid var(--background-modifier-border))
    - .leetcode-contest-solve__actions (same layout as .leetcode-code-actions)

    Create tests/contest/ContestSolveView.test.ts:
    - Mock ContestSessionManager (getSession returns a session with 4 problems, updateCode and recordVerdict are vi.fn())
    - Mock plugin object with contestSessionManager and settings (getProblemDetail returns cached detail)
    - Test: CONTEST_SOLVE_VIEW_TYPE equals 'leetcode-contest-solve' (structural export check)
    - Test: ContestSolveView class is exported (structural import check)
    - Test: getState returns { problemIdx } matching what was set via setState
    - Test: setState/getState round-trip — call setState({ problemIdx: 2 }), verify getState() returns { problemIdx: 2 }
    - Test: handleRun method exists on prototype (grep-equivalent structural check via typeof instance.handleRun)
    - Test: handleSubmit method exists on prototype (grep-equivalent structural check via typeof instance.handleSubmit)
    - Test: updateCode is called by the code save logic with correct problemIdx, code string, and language slug
    - Test: recordVerdict is called with ('accepted') on AC verdict and ('attempted') on WA/TLE/RE
  </action>
  <verify>
    <automated>npx vitest run tests/contest/ContestSolveView.test.ts --reporter=verbose && npm run build && grep -c "CONTEST_SOLVE_VIEW_TYPE" src/contest/ContestSolveView.ts | grep -qE "^[1-9]" && grep -c "handleRun\|handleSubmit" src/contest/ContestSolveView.ts | grep -qE "^[2-9]"</automated>
  </verify>
  <acceptance_criteria>
    - CONTEST_SOLVE_VIEW_TYPE = 'leetcode-contest-solve' exported
    - ContestSolveView extends ItemView with getViewType/getIcon/getDisplayText/setState/getState
    - Problem description rendered via MarkdownRenderer.render
    - Code textarea saves to ContestSessionManager.updateCode debounced at 30s
    - Run button calls interpretSolution and opens RunModal
    - Submit button calls submit flow and opens VerdictModal
    - Verdicts recorded via ContestSessionManager.recordVerdict
    - CSS uses only var(--*) tokens, no raw hex
    - tests/contest/ContestSolveView.test.ts passes with setState/getState round-trip and verdict recording assertions
    - npm run build succeeds
  </acceptance_criteria>
  <done>ContestSolveView provides the full editing + test + submit experience for contest problems, verified by unit tests covering state round-trip and core method presence.</done>
</task>

<task type="auto">
  <name>Task 2: Register ContestSolveView in main.ts</name>
  <files>src/main.ts</files>
  <read_first>
    - src/main.ts (registerView pattern at lines 325-334 — BROWSER_VIEW_TYPE + PREVIEW_VIEW_TYPE registration)
    - src/contest/ContestSolveView.ts (Task 1 output — CONTEST_SOLVE_VIEW_TYPE export)
  </read_first>
  <action>
    Modify src/main.ts:
    - Add import: import { ContestSolveView, CONTEST_SOLVE_VIEW_TYPE } from './contest/ContestSolveView'
    - Add registerView call alongside existing preview view registration (around line 330): this.registerView(CONTEST_SOLVE_VIEW_TYPE, (leaf: WorkspaceLeaf) => new ContestSolveView(leaf, this))

    Add a helper method on LeetCodePlugin to open a contest problem in the solve view (used by Plan 05's timer header problem badges):
    - async openContestProblem(problemIdx: number): Promise<void>
    - Reuses the openOrReusePreview tab-reuse pattern: check workspace.getLeavesOfType(CONTEST_SOLVE_VIEW_TYPE) for existing leaf; if found, setViewState with new problemIdx; if not, create new tab leaf
    - Reveals the leaf
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && grep -n "CONTEST_SOLVE_VIEW_TYPE" src/main.ts && grep -n "openContestProblem" src/main.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/main.ts imports ContestSolveView and CONTEST_SOLVE_VIEW_TYPE
    - registerView call for CONTEST_SOLVE_VIEW_TYPE exists near other registerView calls
    - openContestProblem(problemIdx) method exists on LeetCodePlugin class
    - openContestProblem reuses existing leaf if one is open (tab-reuse pattern)
    - npm run build succeeds
  </acceptance_criteria>
  <done>ContestSolveView is registered as a plugin view and accessible via the openContestProblem helper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin → LC REST | Run/Submit use interpretSolution + submit to leetcode.com |
| User code → PluginData | Ephemeral code buffers stored in data.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering | Code submitted to LC | accept | User's own code submitted through existing verified REST pipeline — no new trust boundary |
| T-10-08 | Information Disclosure | Code buffer in PluginData | accept | Same trust model as existing problemDetails cache — local data.json only |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
- `npm run build` succeeds
- `npx vitest run tests/contest/ContestSolveView.test.ts` passes
- `grep -n "registerView.*CONTEST_SOLVE" src/main.ts` returns match
- `grep -n "CONTEST_SOLVE_VIEW_TYPE" src/contest/ContestSolveView.ts` returns export
- `grep -n "openContestProblem" src/main.ts` returns method definition
- `grep -c "handleRun\|handleSubmit" src/contest/ContestSolveView.ts` returns 2+
- `grep -rn "vault\\.modify\\|vault\\.create" src/contest/ContestSolveView.ts` returns zero (no vault writes during contest)
</verification>

<success_criteria>
Users can open contest problems in a dedicated view, write code in a monospace textarea, select their language, and Run/Submit using the existing LC REST infrastructure. Verdicts are recorded to the contest session. No vault files are created during the contest (ephemeral per D-09).
</success_criteria>

<output>
Create `.planning/phases/10-contest-virtual-analysis/10-04-SUMMARY.md` when done
</output>
