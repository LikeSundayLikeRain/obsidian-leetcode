---
phase: 10-contest-virtual-analysis
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/browse/ProblemBrowserView.ts
  - src/contest/ContestPreview.ts
  - styles.css
  - tests/contest/contestBrowserMode.test.ts
  - tests/contest/ContestPreview.test.ts
autonomous: true
requirements:
  - CONTEST-01
  - CONTEST-02
  - CONTEST-04
must_haves:
  truths:
    - "User can see a Problems/Contests toggle at the top of ProblemBrowserView (per D-01)"
    - "Contests mode shows a searchable list of past contests fetched from LC API (per CONTEST-01)"
    - "User can click Surprise me button to pick a random contest (per D-03)"
    - "Clicking a contest row opens a preview modal with contest details + Start Contest button (per D-04)"
    - "Start Contest fetches all 4 problem details and creates a ContestSession (per D-10)"
  artifacts:
    - path: "src/browse/ProblemBrowserView.ts"
      provides: "Mode toggle, contest list rendering, contest search, surprise-me button in contests mode"
    - path: "src/contest/ContestPreview.ts"
      provides: "Modal showing contest details + problems + Start Contest button"
    - path: "styles.css"
      provides: "Mode toggle CSS + contest row CSS from 10-UI-SPEC"
  key_links:
    - from: "src/browse/ProblemBrowserView.ts"
      to: "src/contest/ContestListService.ts"
      via: "refresh + search calls in contests mode"
      pattern: "contestListService\\.refresh\\|contestListService\\.search"
    - from: "src/contest/ContestPreview.ts"
      to: "src/api/LeetCodeClient.ts"
      via: "getContestQuestions + getProblemDetail calls on Start"
      pattern: "client\\.getContestQuestions\\|client\\.getProblemDetail"
---

<objective>
Add the Contests mode toggle to ProblemBrowserView, render the searchable contest list, implement the contest preview modal with "Start Contest" functionality, and wire the Surprise me button.

Purpose: This is the primary user entry point for starting a virtual contest. Users switch to Contests mode in the sidebar, browse/search past contests, preview them, and start one.

Output: Modified ProblemBrowserView with dual mode, new ContestPreview modal, CSS additions matching 10-UI-SPEC.
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-contest-virtual-analysis/10-RESEARCH.md
@.planning/phases/10-contest-virtual-analysis/10-PATTERNS.md
@.planning/phases/10-contest-virtual-analysis/10-UI-SPEC.md
@.planning/phases/10-contest-virtual-analysis/10-CONTEXT.md

<interfaces>
<!-- Key contracts from Plan 01 output -->

From src/contest/types.ts:
```typescript
export interface CachedContest {
  slug: string;
  title: string;
  startTime: number;
  duration: number;
  type: 'weekly' | 'biweekly';
}
export interface ContestSession { /* D-08 shape */ }
export interface ContestProblemState { /* per-problem state */ }
```

From src/contest/ContestListService.ts:
```typescript
export class ContestListService {
  constructor(client: LeetCodeClient, settings: SettingsStore);
  refresh(force?: boolean): Promise<CachedContest[]>;
  search(contests: CachedContest[], term: string): CachedContest[];
  surpriseMe(): Promise<CachedContest | null>;
}
```

From src/browse/ProblemBrowserView.ts (existing shell pattern):
```typescript
export class ProblemBrowserView extends ItemView {
  async onOpen(): Promise<void> {
    const root = this.containerEl.children[1] as HTMLElement;
    root.empty();
    root.addClass('leetcode-browser');
  }
  private async refreshAndRender(root: HTMLElement): Promise<void> { /* ... */ }
}
```

From node_modules/@leetnotion/leetcode-api (ContestQuestion shape):
```typescript
interface ContestQuestion {
  credit: number;
  title: string;
  title_slug: string;
  difficulty: number; // 1=Easy, 2=Medium, 3=Hard
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProblemBrowserView contests mode toggle + contest list rendering</name>
  <files>src/browse/ProblemBrowserView.ts, styles.css, tests/contest/contestBrowserMode.test.ts</files>
  <read_first>
    - src/browse/ProblemBrowserView.ts (full file — onOpen at line 103, renderShell/refreshAndRender patterns, search/filter bar rendering, shuffle button pattern)
    - styles.css (search for .leetcode-browser, .lc-row pattern around line 116-131, .lc-mode-toggle section if already exists)
    - .planning/phases/10-contest-virtual-analysis/10-UI-SPEC.md (Mode Toggle layout section, Contest list rows, CSS conventions section)
  </read_first>
  <action>
    Modify src/browse/ProblemBrowserView.ts:
    - Add private field: mode: 'problems' | 'contests' = 'problems'
    - Add private field: contestListService: ContestListService (constructed in constructor using plugin.client and plugin.settings)
    - Add private field: contestCache: CachedContest[] = []

    In onOpen(): After root.empty() and root.addClass, check if settings.getContestSession() is non-null — if active session exists, set this.mode = 'contests'. Then call renderModeToggle(root) before existing renderEmptyState + refreshAndRender.

    Add private renderModeToggle(root: HTMLElement): void:
    - Creates a div with class 'lc-mode-toggle'
    - Two buttons: 'Problems' and 'Contests' with class 'lc-mode-toggle__btn'
    - Active button gets 'is-active' class
    - ARIA: container role="tablist", buttons role="tab" + aria-selected
    - Click handler: swap this.mode, re-render the full view body below the toggle (call either existing refreshAndRender for problems or new renderContestsMode for contests)

    Add private async renderContestsMode(root: HTMLElement): void:
    - Auth gate check (same pattern as existing auth gate at line 133)
    - Renders search input (placeholder: 'Search contests…' per UI-SPEC)
    - Renders shuffle button (tooltip: 'Random contest' per UI-SPEC) — click calls surpriseMe flow
    - Calls contestListService.refresh() to load contests
    - Renders contest rows using .lc-contest-row pattern from UI-SPEC
    - Each row: title (flex:1) + meta (date formatted + '· 4 problems')
    - Row click: opens ContestPreview modal
    - Search input: debounced 200ms, calls contestListService.search and re-renders rows
    - Scroll pagination: loads more from cache in pages of 50

    Add private async handleSurpriseMe(): void:
    - Calls contestListService.surpriseMe()
    - If returns null: new Notice('Couldn\'t find a contest with valid problems. Try picking one manually.', 6000)
    - If returns CachedContest: opens ContestPreview modal for that contest

    Add CSS to styles.css (append to end of file):
    - .lc-mode-toggle container (flex, gap:4px, margin-bottom:12px)
    - .lc-mode-toggle__btn (height:28px, padding:0 12px, border-radius:6px, font-size:12px, border, background, color transitions)
    - .lc-mode-toggle__btn:hover (background-modifier-hover)
    - .lc-mode-toggle__btn.is-active (interactive-accent background, text-on-accent color, font-weight:600)
    - .lc-contest-row (flex, gap:12px, min-height:40px, padding:8px 12px, border-radius:8px, background-secondary, cursor:pointer)
    - .lc-contest-row:hover (background-modifier-hover)
    - .lc-contest-row__title (flex:1, font-size:14px, text-overflow:ellipsis, white-space:nowrap, overflow:hidden)
    - .lc-contest-row__meta (font-size:12px, text-muted, flex-shrink:0)
    All colors via CSS variables only (no hex — per UI-SPEC hard rule)

    Create tests/contest/contestBrowserMode.test.ts:
    - Test: mode defaults to 'problems'
    - Test: mode auto-switches to 'contests' when active session exists
    - Test: renderModeToggle produces two buttons with correct ARIA attributes
  </action>
  <verify>
    <automated>npx vitest run tests/contest/contestBrowserMode.test.ts --reporter=verbose && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - ProblemBrowserView has a mode field defaulting to 'problems'
    - Mode toggle renders two buttons with role="tab" and aria-selected
    - Active button has class 'is-active'
    - Contest mode renders rows with .lc-contest-row class
    - Shuffle button in contest mode calls surpriseMe()
    - Search debounces at 200ms and filters contest list by title substring
    - CSS uses only var(--*) tokens, zero raw hex values
    - styles.css additions scoped under .leetcode-browser .lc-mode-toggle and .lc-contest-row
    - npm run build succeeds
  </acceptance_criteria>
  <done>ProblemBrowserView supports dual-mode toggle with full contest list rendering, search, and surprise-me button. CSS matches UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: ContestPreview modal with Start Contest flow</name>
  <files>src/contest/ContestPreview.ts, src/browse/ProblemBrowserView.ts, tests/contest/ContestPreview.test.ts</files>
  <read_first>
    - src/solve/VerdictModal.ts (Modal pattern precedent — constructor, onOpen, onClose)
    - src/contest/ContestListService.ts (Plan 01 — getContestQuestions call pattern)
    - src/api/LeetCodeClient.ts (getProblemDetail method — for fetching problem details on start)
    - .planning/phases/10-contest-virtual-analysis/10-UI-SPEC.md (Contest preview section — copy, layout)
  </read_first>
  <action>
    Create src/contest/ContestPreview.ts:
    - Export class ContestPreviewModal extends Modal
    - Constructor(app: App, private contest: CachedContest, private onStart: (questions: ContestQuestion[]) => Promise<void>)
    - onOpen():
      - Title: contest.title (per UI-SPEC)
      - Body: Duration label "Duration: {N} min" (contest.duration / 60)
      - Problems heading "Problems" — populate by calling client.getContestQuestions(contest.slug)
      - Render problem list: "{n}. {title}" with difficulty pill (1=Easy, 2=Medium, 3=Hard) using .lc-diff--easy/medium/hard classes
      - "Start Contest" button (primary CTA — accent background per UI-SPEC): on click, disable button (label: 'Starting…'), call onStart(questions), then close()
      - On fetch error: show Notice('Couldn\'t fetch contest problems. Check your connection.', 4000), re-enable button
    - onClose(): cleanup (null refs)

    The onStart callback is provided by ProblemBrowserView — it:
    1. Fetches all 4 problem details via client.getProblemDetail for each question.title_slug
    2. Creates ContestSession via ContestSessionManager.start()
    3. Switches ProblemBrowserView to active contest state (rendering timer + problem cards — handled in Plan 04)

    Wire ContestPreview into ProblemBrowserView: contest row click and surpriseMe both instantiate new ContestPreviewModal(this.app, contest, async (questions) => { await this.startContest(contest, questions); }).open()

    Add private async startContest(contest: CachedContest, questions: ContestQuestion[]): Promise<void> to ProblemBrowserView:
    - Fetch all 4 problem details in parallel: Promise.all(questions.map(q => this.plugin.client.getProblemDetail(q.title_slug)))
    - If any fetch fails: Notice('Couldn\'t fetch contest problems. Check your connection.', 4000), return
    - Call this.plugin.contestSessionManager.start({ contestSlug: contest.slug, contestTitle: contest.title, contestType: contest.type, duration: contest.duration, problems: questions.map to { slug, title, credit, difficulty } })
    - Notice('Contest started: {title}. {N} min on the clock.', 4000) per UI-SPEC
    - Re-render contests mode to show active contest state

    Create tests/contest/ContestPreview.test.ts:
    - Mock LeetCodeClient (getProblemDetail returns test problem detail)
    - Mock ContestSessionManager (start method)
    - Test: startContest calls getProblemDetail for each question slug in parallel (verify Promise.all pattern via mock call count)
    - Test: startContest calls ContestSessionManager.start with correct contest shape (slug, title, type, duration, problems array)
    - Test: startContest sets all problems to initial state (verdict: 'unsolved', code: '', solvedAt: null)
    - Test: startContest handles fetch failure gracefully (one getProblemDetail rejects — does not call sessionManager.start)
    - Test: ContestPreviewModal exports class (structural import check)
  </action>
  <verify>
    <automated>npx vitest run tests/contest/ContestPreview.test.ts --reporter=verbose && npm run build && grep -c "startContest" src/browse/ProblemBrowserView.ts | grep -qE "^[1-9]" && grep -c "getProblemDetail" src/browse/ProblemBrowserView.ts | grep -qE "^[1-9]" && grep -c "contestSessionManager" src/browse/ProblemBrowserView.ts | grep -qE "^[1-9]"</automated>
  </verify>
  <acceptance_criteria>
    - ContestPreviewModal extends Modal and renders contest title, duration, problem list
    - Problems shown with difficulty pills using existing .lc-diff--{easy|medium|hard} classes
    - Start Contest button disables on click and shows 'Starting...' label
    - On start success: Notice with contest title fires, session created in PluginData
    - On start failure: Notice with connection error, button re-enabled
    - startContest fetches all 4 problem details in parallel
    - ContestSession created with correct shape (all problems initialized as unsolved)
    - tests/contest/ContestPreview.test.ts passes with all assertions green
    - npm run build succeeds
  </acceptance_criteria>
  <done>Users can browse contests in the sidebar, preview contest details, and start a virtual contest which fetches all problems and initializes the session. Start Contest flow verified by unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin UI → LC API | Contest questions fetched on preview open + problem details on start |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Spoofing | Contest slug from user click | accept | Slug originates from LC API response, not user input — trust chain intact |
| T-10-06 | Denial of Service | Bulk contest fetch on tab open | mitigate | Single-flight guard in ContestListService + 24h TTL cache prevents repeated fetches |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
- `npm run build` succeeds
- `npx vitest run tests/contest/ContestPreview.test.ts` passes
- `grep -n "lc-mode-toggle" styles.css` returns mode toggle CSS rules
- `grep -n "lc-contest-row" styles.css` returns contest row CSS rules
- `grep -rn "ContestPreviewModal" src/contest/` returns class definition
- `grep -n "mode.*problems.*contests" src/browse/ProblemBrowserView.ts` returns mode field
- `grep -n "startContest" src/browse/ProblemBrowserView.ts` returns method definition
- No raw hex in new CSS: `grep -nE '#[0-9a-fA-F]{3,8}' styles.css | grep -i "contest\|mode-toggle"` returns empty
</verification>

<success_criteria>
Users can toggle between Problems and Contests modes in the sidebar. Contest list is searchable, supports surprise-me, and clicking a contest opens a preview modal with Start Contest functionality that initializes the session.
</success_criteria>

<output>
Create `.planning/phases/10-contest-virtual-analysis/10-03-SUMMARY.md` when done
</output>
