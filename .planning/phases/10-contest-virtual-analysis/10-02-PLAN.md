---
phase: 10-contest-virtual-analysis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contest/ContestSessionManager.ts
  - src/contest/buildContestAnalysisPrompt.ts
  - src/contest/mergeAIContestAnalysisSection.ts
  - tests/contest/ContestSessionManager.test.ts
  - tests/contest/timer.test.ts
  - tests/contest/buildContestAnalysisPrompt.test.ts
  - tests/contest/mergeAIContestAnalysisSection.test.ts
autonomous: true
requirements:
  - CONTEST-03
  - CONTEST-06
  - CONTEST-07
must_haves:
  truths:
    - "Contest session state machine transitions correctly through idle → active → paused → ended"
    - "Timer survives plugin reload via Date.now()-baseline persistence (per D-08)"
    - "Pause stops the clock — paused time does not count toward total (per D-06)"
    - "AI contest analysis prompt assembles per-problem summary with holistic debrief instructions (per D-19)"
    - "mergeAIContestAnalysisSection inserts/replaces ## AI Analysis idempotently (per D-21)"
  artifacts:
    - path: "src/contest/ContestSessionManager.ts"
      provides: "State machine with start, pause, resume, abort, finish, tick, onVerdict"
      exports: ["ContestSessionManager"]
    - path: "src/contest/buildContestAnalysisPrompt.ts"
      provides: "Pure prompt assembly for contest AI analysis"
      exports: ["buildContestAnalysisPrompt", "BuildContestAnalysisPromptArgs"]
    - path: "src/contest/mergeAIContestAnalysisSection.ts"
      provides: "Idempotent vault-write transform for ## AI Analysis section"
      exports: ["mergeAIContestAnalysisSection", "AI_ANALYSIS_HEADING"]
  key_links:
    - from: "src/contest/ContestSessionManager.ts"
      to: "src/contest/types.ts"
      via: "ContestSession + ContestProblemState + getRemainingMs"
      pattern: "import.*from.*contest/types"
    - from: "src/contest/mergeAIContestAnalysisSection.ts"
      to: "src/notes/NoteTemplate.ts"
      via: "AI_ANALYSIS_HEADING_LINE import"
      pattern: "import.*AI_ANALYSIS_HEADING_LINE"
---

<objective>
Contest state machine (session lifecycle + timer logic) and AI analysis pure helpers (prompt builder + vault-write merge transform).

Purpose: The state machine drives all contest behavior (start, pause, resume, abort, finish, timer tick, verdict recording). The AI helpers are pure functions reusable by both automatic and manual analysis triggers. Both are tested independently of any UI.

Output: 3 production modules + 4 test files covering all state transitions, timer math, prompt assembly, and section merge logic.
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-contest-virtual-analysis/10-RESEARCH.md
@.planning/phases/10-contest-virtual-analysis/10-PATTERNS.md
@.planning/phases/10-contest-virtual-analysis/10-CONTEXT.md

<interfaces>
<!-- Contracts this plan builds against -->

From src/contest/types.ts (Plan 01 output):
```typescript
export interface ContestSession {
  contestSlug: string;
  contestTitle: string;
  contestType: 'weekly' | 'biweekly';
  duration: number; // seconds
  startedAt: number; // epoch ms
  pausedDuration: number; // ms
  isPaused: boolean;
  pausedAt: number | null;
  problems: ContestProblemState[];
}

export interface ContestProblemState {
  slug: string;
  title: string;
  credit: number;
  difficulty: number;
  verdict: 'unsolved' | 'attempted' | 'accepted';
  code: string;
  language: string;
  solvedAt: number | null;
}

export function getRemainingMs(session: ContestSession): number;
```

From src/solve/pollingOrchestrator.ts (timer registration pattern):
```typescript
export type RegisterIntervalFn = (fn: () => void, ms: number) => unknown;
```

From src/ai/mergeAIReviewSection.ts (section merge analog):
```typescript
export function mergeAIReviewSection(body: string, reviewContent: string): string;
```

From src/ai/buildReviewPrompt.ts (prompt assembly analog):
```typescript
export interface BuildReviewPromptArgs { problemMd: string; code: string; language: string; }
export function buildReviewPrompt(args: BuildReviewPromptArgs): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContestSessionManager state machine with timer</name>
  <files>src/contest/ContestSessionManager.ts, tests/contest/ContestSessionManager.test.ts, tests/contest/timer.test.ts</files>
  <read_first>
    - src/contest/types.ts (ContestSession, ContestProblemState, getRemainingMs — Plan 01 output)
    - src/solve/pollingOrchestrator.ts (RegisterIntervalFn type at line 39, AbortError class pattern at line 76)
    - src/settings/SettingsStore.ts (getContestSession/setContestSession — Plan 01 additions)
  </read_first>
  <action>
    Create src/contest/ContestSessionManager.ts:
    - Export type ContestSessionCallbacks = { onTick: (remainingMs: number) => void; onExpired: () => void; onVerdictChange: (problemIdx: number, verdict: ContestProblemState['verdict']) => void }
    - Export class ContestSessionManager with constructor(settings: SettingsStore, callbacks: ContestSessionCallbacks)
    - Private field: tickHandle for interval cleanup
    - start(params: { contestSlug, contestTitle, contestType, duration, problems: Array<{ slug, title, credit, difficulty }> }): void — creates ContestSession with startedAt=Date.now(), pausedDuration=0, isPaused=false, all problems initialized to verdict='unsolved'/code=''/language='python3'/solvedAt=null. Saves to settings. Starts tick interval.
    - pause(): void — sets isPaused=true, pausedAt=Date.now(). Saves. Stops tick interval.
    - resume(): void — adds (Date.now() - pausedAt) to pausedDuration, sets isPaused=false, pausedAt=null. Saves. Restarts tick interval.
    - abort(): ContestSession — snapshot the session, clear from settings, stop tick. Return snapshot for finalization.
    - finish(): ContestSession — same as abort but semantically "completed" (no aborted marker).
    - recordVerdict(problemIdx: number, verdict: 'attempted' | 'accepted'): void — update session.problems[idx].verdict (only upgrade: unsolved->attempted->accepted, never downgrade). If accepted: set solvedAt=Date.now(). Saves. Calls callbacks.onVerdictChange.
    - updateCode(problemIdx: number, code: string, language: string): void — updates code buffer + language. Saves (caller debounces).
    - getSession(): ContestSession | null — reads from settings.getContestSession()
    - isActive(): boolean — session exists, not ended
    - Private startTick(): uses registerInterval pattern (passed as dep or inlined via window.setInterval) — every 1000ms calls getRemainingMs; if <= 0 calls callbacks.onExpired + stops. Otherwise calls callbacks.onTick(remainingMs).
    - Private stopTick(): clears interval via clearInterval
    - restore(): void — called on plugin load to resume ticking if an active session exists. Checks if expired during offline time.

    Create tests/contest/ContestSessionManager.test.ts:
    - Mock SettingsStore with in-memory contestSession
    - Test: start() creates a valid session shape and persists it
    - Test: pause() sets isPaused=true, pausedAt to a recent epoch
    - Test: resume() accumulates pausedDuration correctly
    - Test: recordVerdict upgrades unsolved->attempted->accepted, never downgrades
    - Test: recordVerdict('accepted') sets solvedAt
    - Test: abort() clears session from settings, returns snapshot
    - Test: finish() clears session from settings, returns snapshot
    - Test: restore() calls onExpired if session has already timed out
    - Test: restore() resumes ticking if session is still active

    Create tests/contest/timer.test.ts:
    - Test getRemainingMs pure function directly (import from types.ts):
    - Test: running session with no pause returns duration*1000 - (now - startedAt)
    - Test: paused session freezes at pausedAt computation
    - Test: returns 0 (not negative) when elapsed > duration
    - Test: pausedDuration correctly subtracted from elapsed
  </action>
  <verify>
    <automated>npx vitest run tests/contest/ContestSessionManager.test.ts tests/contest/timer.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - ContestSessionManager exports start, pause, resume, abort, finish, recordVerdict, updateCode, getSession, isActive, restore methods
    - start() persists a ContestSession with startedAt close to Date.now()
    - pause() -> resume() cycle increments pausedDuration by the elapsed pause time
    - recordVerdict never downgrades (accepted stays accepted even if called with 'attempted')
    - abort/finish both return a ContestSession snapshot and set settings.contestSession to null
    - restore() detects already-expired sessions and fires onExpired callback
    - getRemainingMs returns 0 when contest is over, correct positive value otherwise
    - All timer.test.ts cases pass with deterministic time (vi.useFakeTimers or manual epoch values)
  </acceptance_criteria>
  <done>Contest state machine handles all lifecycle transitions with proper persistence and timer computation. Tests verify all state paths.</done>
</task>

<task type="auto">
  <name>Task 2: AI contest analysis prompt builder + merge section helper</name>
  <files>src/contest/buildContestAnalysisPrompt.ts, src/contest/mergeAIContestAnalysisSection.ts, tests/contest/buildContestAnalysisPrompt.test.ts, tests/contest/mergeAIContestAnalysisSection.test.ts</files>
  <read_first>
    - src/ai/buildReviewPrompt.ts (exact analog — pure function, zero imports, structured string output)
    - src/ai/mergeAIReviewSection.ts (exact analog — findExactHeading, insert/replace logic)
    - src/notes/NoteTemplate.ts (AI_ANALYSIS_HEADING_LINE — Plan 01 output)
  </read_first>
  <action>
    Create src/contest/buildContestAnalysisPrompt.ts:
    - Zero imports — pure function, no external deps (same purity contract as buildReviewPrompt)
    - Export interface BuildContestAnalysisPromptArgs: { contestTitle: string; contestType: 'weekly' | 'biweekly'; durationMin: number; problems: Array<{ slug: string; difficulty: string; verdict: string; timeToSolveMin: number | null; code: string; language: string }> }
    - Export function buildContestAnalysisPrompt(args: BuildContestAnalysisPromptArgs): string
    - System instruction: "You are analyzing a virtual LeetCode contest performance. Provide: 1) A holistic debrief (time allocation patterns, technique gaps, what to practice next), 2) Brief per-problem commentary (1-2 sentences each, NOT a full code review). Format in Markdown."
    - Per-problem section: slug, difficulty, verdict, time-to-solve (or "Did not solve"), code in fenced block with language
    - Instructions: focus on patterns across problems, not individual code review depth (per D-19)

    Create src/contest/mergeAIContestAnalysisSection.ts:
    - Import AI_ANALYSIS_HEADING_LINE from '../notes/NoteTemplate'
    - Export function mergeAIContestAnalysisSection(body: string, analysisContent: string): string
    - Same algorithm as mergeAIReviewSection but with differences:
      - Uses AI_ANALYSIS_HEADING_LINE ('## AI Analysis')
      - First-write: insert before '## Notes' heading (per D-17: Results -> AI Analysis -> Notes)
      - Replacement: discard from heading to next H2 or EOF, insert new content
      - Fallback: append at EOF if no ## Notes found

    Create tests/contest/buildContestAnalysisPrompt.test.ts:
    - Test: output contains system instruction about contest performance
    - Test: each problem appears with slug, difficulty, verdict, code block
    - Test: time-to-solve renders as "{N} min" when provided, "Did not solve" when null
    - Test: empty problems array produces valid prompt (edge case)

    Create tests/contest/mergeAIContestAnalysisSection.test.ts:
    - Test: first write inserts ## AI Analysis before ## Notes
    - Test: replacement path replaces existing ## AI Analysis content up to next H2
    - Test: replacement path replaces to EOF when ## AI Analysis is last section
    - Test: body with no ## Notes appends at EOF
    - Test: idempotent — calling twice with different content yields only the second content
    - Test: preserves all content before ## AI Analysis and all content after ## Notes
  </action>
  <verify>
    <automated>npx vitest run tests/contest/buildContestAnalysisPrompt.test.ts tests/contest/mergeAIContestAnalysisSection.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - buildContestAnalysisPrompt has zero imports (no external deps)
    - Output string contains "virtual LeetCode contest performance" system instruction
    - Each problem in args.problems appears in the output with its code in a fenced block
    - mergeAIContestAnalysisSection inserts before ## Notes on first write
    - mergeAIContestAnalysisSection replaces existing ## AI Analysis content on subsequent writes
    - Both functions are byte-for-byte deterministic (same args -> same output)
    - All tests pass: exits 0
  </acceptance_criteria>
  <done>Pure AI analysis helpers are complete with full test coverage. Both functions are side-effect-free and ready for consumption by the UI/wiring layer in Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin state → AI provider | Contest code buffers sent to configured AI provider via disclosure-gated call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Information Disclosure | buildContestAnalysisPrompt | mitigate | Disclosure gate (withContestAnalysisBullet) fires before any AI call; user sees exactly what data is sent |
| T-10-04 | Tampering | mergeAIContestAnalysisSection | mitigate | Heading match is exact string equality (===), not regex — prevents injection of fake headings |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
- `npx vitest run tests/contest/` passes (all 4 test files)
- `npm run build` succeeds
- `grep -c "import" src/contest/buildContestAnalysisPrompt.ts` returns 0 (zero imports = pure)
- `grep -n "AI_ANALYSIS_HEADING_LINE" src/contest/mergeAIContestAnalysisSection.ts` returns import match
</verification>

<success_criteria>
Contest state machine handles the full lifecycle (idle -> active -> paused -> ended) with proper persistence. Timer computation is drift-free via epoch math. AI analysis prompt builder and section merge helper are pure, tested, and ready for downstream consumption.
</success_criteria>

<output>
Create `.planning/phases/10-contest-virtual-analysis/10-02-SUMMARY.md` when done
</output>
