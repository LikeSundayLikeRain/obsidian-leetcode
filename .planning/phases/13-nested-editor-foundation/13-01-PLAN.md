---
phase: 13-nested-editor-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/childEditorRegistry.ts
  - src/main/childEditorFactory.ts
  - tests/main/childEditorRegistry.test.ts
  - tests/main/childEditorFactory.test.ts
autonomous: true
requirements:
  - INDENT-01
  - INDENT-02
  - INDENT-03
  - INDENT-04
  - ENTER-01
  - ENTER-02
  - ENTER-03
  - ENTER-04
  - BRACKET-01
  - BRACKET-02
  - BRACKET-03
  - BRACKET-04
  - BRACKET-05
  - LANG-01
  - COMMENT-01
  - HIGHLIGHT-01

must_haves:
  truths:
    - "ChildEditorRegistry stores up to 5 EditorView instances keyed by file path"
    - "LRU eviction destroys the least-recently-accessed EditorView when cap exceeded"
    - "destroyAll() calls destroy() on every cached EditorView and clears the map"
    - "createChildEditor() returns an EditorView with Python syntax highlighting extensions"
  artifacts:
    - path: "src/main/childEditorRegistry.ts"
      provides: "LRU cache for child EditorView instances"
      exports: ["ChildEditorRegistry"]
    - path: "src/main/childEditorFactory.ts"
      provides: "Factory function creating child EditorView with Python LanguageSupport"
      exports: ["createChildEditor"]
    - path: "tests/main/childEditorRegistry.test.ts"
      provides: "Unit tests for LRU registry behavior"
    - path: "tests/main/childEditorFactory.test.ts"
      provides: "Unit tests for child editor creation"
  key_links:
    - from: "src/main/childEditorRegistry.ts"
      to: "@codemirror/view EditorView"
      via: "import EditorView type for registry values"
      pattern: "import.*EditorView.*@codemirror/view"
    - from: "src/main/childEditorFactory.ts"
      to: "@codemirror/lang-python"
      via: "python() LanguageSupport in extensions array"
      pattern: "import.*python.*@codemirror/lang-python"
---

<objective>
Create the two utility modules that underpin the nested editor: an LRU registry for child EditorView lifecycle management, and a factory function that creates properly-configured child EditorViews with Python syntax highlighting.

Purpose: These two modules decouple child EditorView lifecycle from widget DOM destruction (D-13) and encapsulate the CM6 configuration for child editors — both are prerequisites for the extension in Plan 02.

Output: Two source modules with complete unit test coverage, independently testable without Obsidian runtime.
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/moxu/projects/obsidian-leetcode/.planning/phases/13-nested-editor-foundation/13-RESEARCH.md
@/Users/moxu/projects/obsidian-leetcode/.planning/phases/13-nested-editor-foundation/13-PATTERNS.md
@/Users/moxu/projects/obsidian-leetcode/src/main/codeActionsEditorExtension.ts (import conventions, eslint-disable pattern)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChildEditorRegistry with LRU eviction</name>
  <files>src/main/childEditorRegistry.ts, tests/main/childEditorRegistry.test.ts</files>
  <read_first>
    - src/main/codeActionsEditorExtension.ts (lines 40-54 for import/eslint-disable pattern)
    - .planning/phases/13-nested-editor-foundation/13-RESEARCH.md (Pattern 5: LRU Registry with Cleanup)
    - .planning/phases/13-nested-editor-foundation/13-PATTERNS.md (childEditorRegistry section)
    - tests/main/sectionLockExtension.test.ts (lines 1-40 for test file structure pattern)
  </read_first>
  <behavior>
    - get(key) returns undefined for missing keys
    - get(key) returns the EditorView for existing keys and updates lastAccess
    - set(key, view) stores the entry and is retrievable via get
    - set() when at cap evicts the entry with the oldest lastAccess timestamp
    - Eviction calls EditorView.destroy() on the evicted view
    - delete(key) calls EditorView.destroy() and removes from cache
    - delete(key) is a no-op for missing keys
    - destroyAll() calls destroy() on every entry and empties the cache
    - has(key) returns boolean presence check
    - size getter returns current entry count
  </behavior>
  <action>
    Create `src/main/childEditorRegistry.ts` exporting class `ChildEditorRegistry`. Constructor takes optional `cap` parameter (default 5, per D-12). Internal `Map<string, RegistryEntry>` where `RegistryEntry = { view: EditorView; lastAccess: number }`. Methods: `get(key: string): EditorView | undefined`, `set(key: string, view: EditorView): void`, `delete(key: string): void`, `destroyAll(): void`, `has(key: string): boolean`, getter `size: number`. Private `evictIfNeeded()` iterates the map to find min-lastAccess entry and calls `entry.view.destroy()` then deletes. Use the eslint-disable comment for the `@codemirror/view` import (same pattern as `codeActionsEditorExtension.ts` line 48).

    Create `tests/main/childEditorRegistry.test.ts` with vitest. Mock `EditorView` as a plain object with a `destroy` vi.fn(). Test all behaviors listed above. Use `vi.spyOn` or direct mock objects — no `vi.mock('obsidian')` needed since registry is pure logic.
  </action>
  <verify>
    <automated>cd /Users/moxu/projects/obsidian-leetcode && npx vitest run tests/main/childEditorRegistry.test.ts</automated>
  </verify>
  <done>All registry tests pass. ChildEditorRegistry exported with LRU eviction at cap=5, destroy delegation on evict/delete/destroyAll.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ChildEditorFactory with Python LanguageSupport</name>
  <files>src/main/childEditorFactory.ts, tests/main/childEditorFactory.test.ts</files>
  <read_first>
    - src/main/codeActionsEditorExtension.ts (lines 40-54 for import conventions)
    - .planning/phases/13-nested-editor-foundation/13-RESEARCH.md (Pattern 3: Child EditorView Creation)
    - .planning/phases/13-nested-editor-foundation/13-PATTERNS.md (childEditorFactory section)
    - package.json (confirm @codemirror/lang-python dependency exists)
    - esbuild.config.mjs (confirm @codemirror/language is external but lang-python is bundled)
  </read_first>
  <behavior>
    - createChildEditor(content, parent) returns an EditorView instance
    - The returned EditorView has its dom appended to the parent HTMLElement
    - The EditorView state doc equals the content string passed in
    - The extensions include python() LanguageSupport
    - The extensions include syntaxHighlighting with defaultHighlightStyle fallback
    - The extensions include bracketMatching, drawSelection, highlightActiveLine
    - The extensions include history() and keymap with defaultKeymap + historyKeymap
    - The extensions include an EditorView.theme with code-background CSS variable
    - The extensions include EditorView.lineWrapping
  </behavior>
  <action>
    Create `src/main/childEditorFactory.ts` exporting `createChildEditor(content: string, parent: HTMLElement): EditorView`. Imports: `EditorView`, `keymap`, `drawSelection`, `highlightActiveLine` from `@codemirror/view` (eslint-disable); `EditorState` from `@codemirror/state` (eslint-disable); `syntaxHighlighting`, `defaultHighlightStyle`, `bracketMatching` from `@codemirror/language` (eslint-disable); `history`, `defaultKeymap`, `historyKeymap` from `@codemirror/commands` (eslint-disable); `python` from `@codemirror/lang-python` (NO eslint-disable — it is a declared dependency). Creates `EditorState.create({ doc: content, extensions: [...] })` then `new EditorView({ state, parent })`. The theme block uses `var(--code-background, var(--background-secondary))` for `&` background, `var(--font-monospace)` for `.cm-content` font-family, `14px` font-size, transparent gutters with no border-right, and `borderRadius: '4px'`, `padding: '8px 0'` on `&`. Include `EditorView.lineWrapping`.

    Create `tests/main/childEditorFactory.test.ts`. Since creating a real EditorView requires JSDOM or similar, mock `@codemirror/view` (EditorView constructor), `@codemirror/state` (EditorState.create), `@codemirror/language`, `@codemirror/commands`, and `@codemirror/lang-python`. Verify that `createChildEditor` calls `EditorState.create` with a doc matching the content arg and an extensions array, then calls `new EditorView` with that state and the parent element. Verify `python()` is present in the extensions passed to `EditorState.create`.
  </action>
  <verify>
    <automated>cd /Users/moxu/projects/obsidian-leetcode && npx vitest run tests/main/childEditorFactory.test.ts</automated>
  </verify>
  <done>Factory tests pass. createChildEditor exported, creates EditorView with Python highlighting, history, bracket matching, theme variables, and lineWrapping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Phase 13 is entirely local — no network, no eval, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | child EditorView content | accept | Child is local-only; content comes from vault file already in user's trust domain; no sync to parent until Phase 14 |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — all deps already in package.json (verified in RESEARCH.md) |
</threat_model>

<verification>
```bash
cd /Users/moxu/projects/obsidian-leetcode && npx vitest run tests/main/childEditorRegistry.test.ts tests/main/childEditorFactory.test.ts
```
</verification>

<success_criteria>
- ChildEditorRegistry class exported from src/main/childEditorRegistry.ts with LRU cap=5
- createChildEditor function exported from src/main/childEditorFactory.ts with Python LanguageSupport
- All unit tests pass (registry: 8+ test cases; factory: 4+ test cases)
- No new npm dependencies required
</success_criteria>

<output>
Create `.planning/phases/13-nested-editor-foundation/13-01-SUMMARY.md` when done
</output>
