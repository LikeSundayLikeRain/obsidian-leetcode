---
phase: 05.1-edit-mode-inline-buttons
verified: 2026-05-11T10:54:50Z
status: human_needed
score: 5/6 must-haves verified (SC-4 and SC-6 require human confirmation; live-smoke evidence accepted from Plan 03 SUMMARY — see human_verification below)
overrides_applied: 0
human_verification:
  - test: "Visual layout correctness in Live Preview and Source Mode"
    expected: "Run + Submit buttons render inline directly below the ## Code fence closing backticks with no blank rectangles, no shifted headings, no duplicate widgets. Buttons scroll with document content."
    why_human: "Layout corruption (the 05-UAT G1 failure mode) is invisible to unit tests. D-10 is a locked live-smoke gate for this exact reason. Plan 03 SUMMARY records developer approval ('approved with spacing tweak') with 12/12 checklist items PASS — this is the evidence. A human reviewer must confirm the SUMMARY accurately reflects the developer's live observation."
  - test: "Light and dark theme rendering"
    expected: "Buttons legible with sufficient contrast using Obsidian CSS variables in both default light and default dark themes; hover state responsive; focus ring visible."
    why_human: "Visual contrast and hover behavior cannot be verified programmatically without a headless Obsidian renderer. Plan 03 checklist items 11 and 12 both record PASS — a human reviewer should confirm the developer's observation stands."
---

# Phase 5.1: Edit-mode Inline Run/Submit Buttons — Verification Report

**Phase Goal:** Run/Submit buttons are anchored inline directly below the `## Code` fenced block in Edit Mode (Source + Live Preview) without layout corruption, so users can submit without switching to Reading Mode during their normal coding flow.
**Verified:** 2026-05-11T10:54:50Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Note on Requirement ID POLISH-07

`REQUIREMENTS.md` line 75 defines POLISH-07 as "Run UX rework — replace Run code (sample) + Run code (custom input) with a single unified Run command." However, `ROADMAP.md` lines 178–179 explicitly re-assigns the POLISH-07 ID to Phase 5.1 with the annotation: "gap-closure from 05-UAT G1; reading-mode buttons shipped in 05-05, edit-mode path was deferred and escalated to ship-blocker during 05-05 live smoke." The ROADMAP is the authoritative planning contract; the REQUIREMENTS.md mapping table has not been updated to reflect this reassignment. This is a documentation consistency gap (not a code gap) — REQUIREMENTS.md line 168 still shows `POLISH-07 | Phase 5 | Pending`. The phase's deliverable (edit-mode inline buttons) is clearly specified in the ROADMAP goal and success criteria and is fully implemented. No code change is needed; the REQUIREMENTS.md tracking table should be updated to reflect Phase 5.1 and the new status.

---

## Goal Achievement

### Observable Truths (from ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC-1 | In Live Preview and Source Mode on an `lc-slug` note, Run + Submit buttons appear inline directly below the closing fence of the `## Code` block — anchored to that specific block, scrolling with note content | ✓ VERIFIED | `src/main/codeActionsEditorExtension.ts`: `StateField<DecorationSet>` with `buildDecorations()` anchors widget at `state.doc.line(fence.closerLine + 1).from` with `side: -1` (or end-of-doc fallback `side: 1`). `main.ts` line 363: `this.registerEditorExtension(buildCodeActionsEditorExtension(this))`. Extension covers both Live Preview and Source Mode via shared `EditorView`. Plan 03 SUMMARY: 12/12 checklist items PASS including items 1, 2, 3. |
| SC-2 | Buttons are NOT a floating/corner toolbar; they live inline with the document and remain positioned correctly through edits to surrounding content | ✓ VERIFIED | `Decoration.widget({ side: -1 })` (inline widget decoration, NOT `block: true`). `grep -c "block: true" src/main/codeActionsEditorExtension.ts` returns `0`. `WidgetType.eq()` returns `other instanceof CodeActionsWidget && other.plugin === this.plugin` ensuring DOM reuse across transactions. StateField `update()` rebuilds on `tr.docChanged` — widget repositions as fence lines shift. Plan 03 checklist items 2, 9, 10 all PASS. |
| SC-3 | Clicking dispatches `runFromActive` / `submitFromActive` (same command handlers already used by the reading-mode path) | ✓ VERIFIED | `toDOM()` delegates to `buildCodeBlockButtonRow(view.dom.ownerDocument, this.plugin)` which calls `plugin.runFromActive()` / `plugin.submitFromActive()` directly. `grep -c "executeCommandById" src/main/codeActionsEditorExtension.ts` returns `0`. Unit tests 9+10 in `codeActionsEditorExtension.test.ts` (12/12 GREEN) assert direct-call dispatch and verify `executeCommandById` NOT called. Plan 03 checklist item 5: PASS. |
| SC-4 | No layout corruption in either edit mode: no inserted whitespace, no duplicate widgets, no shifted heading positions, no large blank rectangles between existing blocks | ? HUMAN NEEDED | Code evidence: `block: true` forbidden (grep=0); `side: -1` inline widget; `WidgetType.eq()` guards idempotency. Plan 03 SUMMARY records developer live-smoke with resume signal "approved with spacing tweak" and 12/12 checklist items PASS — including item 1 (Live Preview, no blank rectangles), item 3 (Source Mode, no layout corruption), item 9 (rapid typing, no flicker/duplicates). Plan 03 also records that the original `side: 1` at closer-line end caused visual clipping inside the CM6 code-block widget; this was fixed to `side: -1` at next-line start. Cannot programmatically verify visual layout. |
| SC-5 | Reading Mode buttons from 05-05 continue to work unchanged — no regression | ✓ VERIFIED | `git diff HEAD~5..HEAD -- src/main/codeActionsPostProcessor.ts` = 0 bytes changed. `registerCodeBlockActionProcessor(this)` at `main.ts` line 355 preserved unchanged. `tests/main/codeActionsPostProcessor.test.ts` 5/5 GREEN (confirmed by live test run above). |
| SC-6 | Renders correctly in both light and dark themes | ? HUMAN NEEDED | CSS uses Obsidian CSS variables (`--background-secondary`, `--text-muted`, `--background-modifier-border`, `--background-modifier-hover`, `--text-normal`) — same variables used by the 05-05 reading-mode buttons which passed prior live smoke. `styles.css` adds `.cm-editor .leetcode-code-actions { margin-top: 12px }` and `.markdown-rendered pre + .leetcode-code-actions { margin-top: -4px }` using no hardcoded colors. Plan 03 checklist items 11+12 (light/dark) both record PASS. Cannot verify programmatically. |

**Score:** 4/6 truths fully verified programmatically; 2/6 require human confirmation (SC-4, SC-6). Automated evidence strongly supports both; Plan 03 developer sign-off provides live-smoke evidence.

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/main/codeActionsEditorExtension.ts` | CM6 StateField extension with 4 exports | ✓ VERIFIED | File exists, 229 lines. Exports `findCodeFence`, `buildDecorations`, `CodeActionsWidget`, `buildCodeActionsEditorExtension` (all confirmed by grep). |
| `src/main.ts` | `registerEditorExtension(buildCodeActionsEditorExtension(this))` in onload() | ✓ VERIFIED | Import at line 59, registration call at line 363 (Step 6f), adjacent to Step 6e (`registerCodeBlockActionProcessor` at line 355). |
| `tests/main/codeActionsEditorExtension.test.ts` | ≥10 unit tests covering the behavioral contract | ✓ VERIFIED | File exists with 12 `it()` blocks. All 12 pass GREEN. Covers: `findCodeFence` (3 cases), lc-slug gate (2), widget DOM shape (2), `eq()` idempotency (2), click dispatch (2), public API (1). |
| `tests/helpers/obsidian-stub.ts` | `editorInfoField` + `editorLivePreviewField` CM6 StateField exports | ✓ VERIFIED | Lines 105 and 110: both exports present as real `StateField` instances from `@codemirror/state`. |
| `styles.css` | `.cm-editor .leetcode-code-actions` scoped override | ✓ VERIFIED | Lines 889-891: `margin-top: 12px`. Lines 896-898: Reading-Mode compensator `-4px`. Base `.leetcode-code-actions` rule unchanged at lines 859-865. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/main/codeActionsEditorExtension.ts` | `src/main/codeBlockButtonRow.ts` | `buildCodeBlockButtonRow(view.dom.ownerDocument, plugin)` in `toDOM()` | ✓ WIRED | Line 65 of extension: `return buildCodeBlockButtonRow(view.dom.ownerDocument, this.plugin)` — confirmed by grep. |
| `src/main/codeActionsEditorExtension.ts` | `obsidian` (editorInfoField, editorLivePreviewField) | `state.field(editorInfoField)` and mode-flip detection | ✓ WIRED | Lines 41-44 import both fields; line 152 reads `editorInfoField`; lines 217-218 read `editorLivePreviewField` for mode-flip detection. |
| `src/main.ts` onload() | `src/main/codeActionsEditorExtension.ts` | `this.registerEditorExtension(buildCodeActionsEditorExtension(this))` | ✓ WIRED | Import line 59, registration line 363, Step 6f comment at line 357 — confirmed by grep. Exactly 1 `registerEditorExtension` call (no double-registration). |
| `tests/main/codeActionsEditorExtension.test.ts` | `src/main/codeActionsEditorExtension.ts` | `import { findCodeFence, buildDecorations, CodeActionsWidget, buildCodeActionsEditorExtension }` | ✓ WIRED | Line 34 of test file. All 12 tests pass GREEN against the live implementation. |
| `tests/main/codeActionsEditorExtension.test.ts` | `tests/helpers/obsidian-stub.ts` | `vi.mock('obsidian', async () => import('../helpers/obsidian-stub'))` | ✓ WIRED | Lines 23-26 of test file — confirmed by grep. |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `CodeActionsWidget.toDOM()` | `plugin` (PluginHost) | Constructor arg, passed from `buildDecorations` → `buildCodeActionsEditorExtension` → registered in `main.ts` with `this` (the live plugin instance) | Yes — `plugin.app.metadataCache.getFileCache(file)?.frontmatter` is a live Obsidian cache read; `state.field(editorInfoField)?.file` reads the live editor's active file | ✓ FLOWING |
| `buildDecorations` | `file` from `editorInfoField` | `state.field(editorInfoField)?.file` — Obsidian's CM6 built-in field for the active MarkdownFileInfo | Yes — live Obsidian field; gates widget rendering on real file presence | ✓ FLOWING |
| `buildDecorations` | `slug` from `metadataCache` | `plugin.app.metadataCache.getFileCache(file)?.frontmatter?.['lc-slug']` | Yes — live frontmatter cache; returns `null` for non-LC notes (correctly gates display off) | ✓ FLOWING |
| `findCodeFence` | `state.doc` line scan | CM6 `EditorState.doc` — live document content | Yes — real doc lines scanned by regex; returns null for unterminated/absent fences | ✓ FLOWING |

---

### Behavioral Spot-Checks

Step 7b: vitest suite executed directly.

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 12 unit tests GREEN | `npx vitest run tests/main/codeActionsEditorExtension.test.ts` | 12 passed (12) in 7ms | ✓ PASS |
| Reading-mode regression 5/5 GREEN | `npx vitest run tests/main/codeActionsPostProcessor.test.ts` | 5 passed (5) in 23ms | ✓ PASS |
| `block: true` forbidden | `grep -c "block: true" src/main/codeActionsEditorExtension.ts` | 0 | ✓ PASS |
| `executeCommandById` forbidden | `grep -c "executeCommandById" src/main/codeActionsEditorExtension.ts` | 0 | ✓ PASS |
| `innerHTML` forbidden | `grep -c "innerHTML" src/main/codeActionsEditorExtension.ts` | 0 | ✓ PASS |
| Single `registerEditorExtension` call | `grep -c "registerEditorExtension" src/main.ts` | 1 | ✓ PASS |
| Layout correctness, theme rendering | Live Obsidian dev vault — visual | Developer sign-off "approved with spacing tweak"; 12/12 checklist PASS | ? HUMAN (evidence present) |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| POLISH-07 | 05.1-01, 05.1-02, 05.1-03 | Edit-mode inline Run/Submit buttons (gap-closure from 05-UAT G1) per ROADMAP.md re-assignment | ✓ SATISFIED | All 6 ROADMAP success criteria implemented; 4 programmatically verified; 2 verified via live-smoke developer sign-off. Note: REQUIREMENTS.md tracking table at line 168 still shows `POLISH-07 | Phase 5 | Pending` — this is a stale tracking entry, not a code gap. |

---

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `src/main/codeActionsEditorExtension.ts` | `side: -1` diverges from PLAN frontmatter `must_haves.artifacts` which states `contains: "side: 1"` — the actual implementation conditionally uses `side: -1` for the normal case and `side: 1` only as an end-of-doc fallback | info | Not a defect — the Plan 02 frontmatter spec of `side: 1` was the initial design; Plan 03 live smoke revealed that anchoring at the next-line start with `side: -1` is required for correct visual placement. SUMMARY documents this as an intentional deviation. The behavioral contract (buttons appear below the fence, no layout corruption) is satisfied. |
| `.planning/REQUIREMENTS.md` line 168 | `POLISH-07 | Phase 5 | Pending` — tracking table not updated to reflect Phase 5.1 assignment and completion | info | Documentation inconsistency only; no code impact. |

---

### Human Verification Required

#### 1. Visual Layout Correctness in Live Preview and Source Mode (SC-4)

**Test:** Open an `lc-slug` note in the Obsidian dev vault in Live Preview mode. Verify buttons appear inline directly below the `## Code` fence with no blank rectangles, no shifted headings, no duplicate widgets. Toggle to Source Mode and confirm the same. Type rapidly into the code fence and confirm no flicker or duplicates.

**Expected:** Buttons are visually inline below the closing fence backticks, flush-right, neutral pill style. No layout corruption of any kind. Idempotent under rapid editing.

**Why human:** Layout corruption is invisible to unit tests — this is the 05-UAT G1 lesson that created the D-10 LOCKED gate. Plan 03 SUMMARY records developer approval with 12/12 checklist items PASS and documents the specific anchor fix (`side: -1` at next-line start). A reviewer should confirm this sign-off is authentic and the fix is in the code (confirmed: `src/main/codeActionsEditorExtension.ts` line 186 uses `side: hasLineAfterCloser ? -1 : 1`).

---

#### 2. Light and Dark Theme Rendering (SC-6)

**Test:** With an `lc-slug` note open in Live Preview, toggle Settings → Appearance → Base color scheme between Light and Dark. Confirm buttons remain legible, hover is responsive, and focus ring is visible in both themes.

**Expected:** Adequate contrast using Obsidian CSS variables in both themes. No hardcoded colors that would fail a theme.

**Why human:** Visual contrast cannot be verified programmatically. Plan 03 checklist items 11+12 both record PASS. The CSS implementation uses exclusively Obsidian CSS variables matching the already-reviewed Phase 5 reading-mode button styling.

---

### Gaps Summary

No programmatic gaps found. All code artifacts exist, are substantive, are correctly wired, and data flows through them. The two items requiring human verification (SC-4 layout correctness, SC-6 theme rendering) both have strong supporting evidence from the Plan 03 developer live-smoke sign-off, which recorded "approved with spacing tweak" with 12/12 checklist items PASS.

If the Plan 03 live-smoke sign-off is accepted as sufficient human evidence, this phase can be considered passed. If independent human re-verification is required, the two tests above should be run in the dev vault.

---

_Verified: 2026-05-11T10:54:50Z_
_Verifier: Claude (gsd-verifier)_
