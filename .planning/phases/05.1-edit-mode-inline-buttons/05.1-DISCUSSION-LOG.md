# Phase 5.1: Edit-mode Inline Run/Submit Buttons - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-10
**Phase:** 05.1-edit-mode-inline-buttons
**Areas discussed:** (all delegated to Claude's discretion — see below)

---

## Gray Areas Presented

| Option | Description | User Response |
|--------|-------------|---------------|
| CM6 anchoring strategy | How to attach the button row to the `## Code` fence without layout corruption — block widget (failed in 05-UAT), line-widget `side:1`, post-widget, DOM injection outside CM6 lifecycle, or floating toolbar (rejected). | Delegated to Claude |
| `## Code` block identification | Locate the `## Code` fence via Lezer syntax tree, regex over `EditorState.doc.text`, or CachedMetadata sections lookup. | Delegated to Claude |
| Source Mode vs Live Preview parity | Single CM6 extension handling both modes, or separate paths (Source shows raw fence text; Live Preview renders formatted block). | Delegated to Claude |
| Click handler wiring | `executeCommandById('leetcode:run')` (symmetric with D-11 intent) vs direct `plugin.runFromActive()` call (consistent with 05-05 live-smoke fix). | Delegated to Claude |

**User's response (verbatim):** "i don' have much context on the low level details. and seems these are all just implementation details, you should figure it out"

**Notes:** User is the visionary here; acceptance gate is "buttons anchored below `## Code`, no layout corruption in Live Preview or Source, Reading Mode 05-05 path continues to work." Phase 5.1 ship-blocker constraints are locked by ROADMAP success criteria + 05-UAT G1. Phase 5.1 researcher + planner have full latitude on CM6 API selection within the four hard constraints carried into CONTEXT.md §D-03:

1. Block `Decoration.widget` with `block: true` is OUT (failed 05-UAT attempt).
2. Floating bottom-right toolbar is OUT (user-rejected during 05-UAT).
3. Must anchor inline to the `## Code` fence and scroll with content.
4. Live-verify each iteration — unit tests not sufficient (D-10).

---

## Claude's Discretion

All implementation-level questions delegated:
- CM6 API choice for the anchoring widget (line-widget `side:1`, post-widget, ViewPlugin with DOM overlay, etc.)
- Lezer-tree vs regex for `## Code` fence detection
- Shared vs separate code paths between Source Mode and Live Preview
- ViewUpdate rebuild trigger (every transaction, only on docChanged, only on heading-structure changes)
- DOM-reuse strategy (wrap the shipped `buildCodeBlockButtonRow` helper vs CM6-native builder emitting same classes)

All captured in CONTEXT.md §decisions "Claude's Discretion" subsection.

## Deferred Ideas

Redirected out of Phase 5.1 scope (see CONTEXT.md §deferred for full list):

- Buttons under fences other than `## Code` (example fences, custom-input fences)
- Keybinding / hotkey shortcut for Run/Submit from within the editor
- Edit-mode status indicator ("last submission: AC / 42 ms / 14.2 MB")
- Diff-view between current `## Code` and last Accepted submission (Phase 4 deferred)
- Chevron/dropdown submission-navigator overlay on `## Code` (Phase 4 deferred)
- Any enhancement requiring writes to the note from Edit Mode (CF-06 forbids)
