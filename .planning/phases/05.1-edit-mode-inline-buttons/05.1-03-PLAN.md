---
phase: 5.1
plan: 03
type: execute
wave: 2
depends_on:
  - "5.1-02"
files_modified:
  - styles.css
autonomous: false
requirements:
  - POLISH-07
tags:
  - obsidian-plugin
  - live-smoke
  - checkpoint
  - visual-verification
  - 05-uat-g1-lock
must_haves:
  truths:
    - "D-01: On a real lc-slug problem note in a live Obsidian dev vault, Run + Submit buttons render inline directly below the closing backticks of the ## Code fence in Live Preview — no blank rectangles, no shifted headings, no duplicates"
    - "D-01, D-04: Same behavior verified visually in Source Mode on the same note — buttons appear at the end of the closing fence line, no layout corruption (single extension covers both modes)"
    - "D-11: Reading Mode on the same note still shows the Phase 5 Plan 05 shipped buttons — zero regression; npx vitest run tests/main/codeActionsPostProcessor.test.ts exits 0"
    - "D-05: Clicking Run in Live Preview dispatches the existing plugin.runFromActive() flow (not executeCommandById); clicking Submit dispatches plugin.submitFromActive()"
    - "D-06: Non-lc-slug notes in all three modes show zero Phase 5.1 buttons (frontmatter gate live-verified)"
    - "D-02: lc-slug notes without a ## Code fence (e.g., only ## Problem with example fences) show zero Phase 5.1 buttons (fence-targeting gate live-verified)"
    - "Buttons render correctly in both Obsidian default light theme and default dark theme — contrast acceptable, hover responsive, focus ring visible"
    - "D-08: Typing rapidly into the ## Code fence does NOT produce widget flicker or duplicates (WidgetType.eq() guard effective)"
    - "D-10: User has explicitly typed approval in response to the live-smoke checkpoint prompt — D-10 LOCKED, cannot be bypassed; unit tests alone are insufficient per 05-UAT G1"
  artifacts:
    - path: ".planning/phases/05.1-edit-mode-inline-buttons/05.1-03-SUMMARY.md"
      provides: "Human-verified live-smoke evidence closing Phase 5.1"
      contains: "Live smoke passed"
  key_links:
    - from: "Dev vault live reload"
      to: "src/main/codeActionsEditorExtension.ts (shipped by Plan 02)"
      via: "Cmd-R reload runs onload(); registerEditorExtension fires"
      pattern: "user observable: buttons appear below ## Code fence in Live Preview + Source Mode"
---

## Decisions Addressed (from CONTEXT.md)

This Wave 2 live-smoke checkpoint is the binding gate for the CONTEXT.md decisions listed below — unit tests alone are insufficient per 05-UAT G1 (D-10 LOCKED lesson).

- **D-01** — Eyes-on confirmation: buttons anchor inline directly below closing fence and scroll with document content (checklist items 1–2).
- **D-04** — Live Preview + Source Mode parity verified visually on the same note (checklist items 2, 3, 7).
- **D-10** — This plan IS the D-10 locked live-smoke gate; `autonomous: false`; developer must type the resume signal. No single unit test can cover "no layout corruption" — that's why D-10 exists.
- **D-11** — Reading Mode regression check: toggle to Reading Mode on the same `lc-slug` note, confirm Phase 5 / 05-05 shipped buttons still work unchanged (checklist item 4; re-runs `npx vitest run tests/main/codeActionsPostProcessor.test.ts` as part of verification).

## D-ID References Used In This Plan

D-01, D-04, D-10, D-11

<objective>
D-10 LOCKED live-smoke checkpoint: human-verified visual validation of Phase 5.1's Edit-Mode
inline Run/Submit buttons in a real Obsidian dev vault across Live Preview + Source Mode +
Reading Mode (regression) + light/dark themes. Plus a contingent CSS tweak task that runs
ONLY if live smoke surfaces visible spacing misalignment between the widget and the CM6 fence
rendering (UI-SPEC sanctioned scope per RESEARCH Open Question 3).

**Why live smoke is mandatory (non-negotiable):** 05-UAT G1 documented that unit tests passed
while Live Preview was visibly corrupted — the `block: true` widget looked fine under vitest
but produced a large blank rectangle mid-document in the live editor. Unit tests are
necessary but not sufficient for Phase 5.1 layout correctness. D-10 locks the live-smoke
gate between waves. This plan IS that gate.

This plan is `autonomous: false` — it contains a human-verify checkpoint that cannot be
auto-approved. The developer must personally eyeball the dev vault and explicitly approve.

Output:
- Human-verified visual evidence that all 12 checklist items from UI-SPEC §Live-Smoke Acceptance Checklist pass
- Typed developer approval (e.g., `approved`) or detailed issue notes
- If misalignment is visible: a contingent `.cm-editor`-scoped `margin-top` override in `styles.css`. If no misalignment: Task 2 is a no-op; styles.css is not modified.
- `.planning/phases/05.1-edit-mode-inline-buttons/05.1-03-SUMMARY.md` recording the outcome
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05.1-edit-mode-inline-buttons/05.1-CONTEXT.md
@.planning/phases/05.1-edit-mode-inline-buttons/05.1-UI-SPEC.md
@.planning/phases/05.1-edit-mode-inline-buttons/05.1-VALIDATION.md
@.planning/phases/05.1-edit-mode-inline-buttons/05.1-RESEARCH.md
@.planning/phases/05.1-edit-mode-inline-buttons/05.1-PATTERNS.md
@.planning/phases/05.1-edit-mode-inline-buttons/05.1-01-PLAN.md
@.planning/phases/05.1-edit-mode-inline-buttons/05.1-02-PLAN.md
@styles.css
@src/main/codeActionsEditorExtension.ts
@src/main/codeBlockButtonRow.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live-smoke checkpoint — Edit Mode (Live Preview + Source Mode), Reading-Mode regression, light + dark theme</name>
  <files>(none — pure human-verification; no files modified by this task unless Task 2 fires)</files>
  <read_first>
    - .planning/phases/05.1-edit-mode-inline-buttons/05.1-UI-SPEC.md §Live-Smoke Acceptance Checklist (D-10) lines 244-263 — the 12 explicit checklist items this checkpoint verifies
    - .planning/phases/05.1-edit-mode-inline-buttons/05.1-CONTEXT.md §D-10 + §D-11 (locked live-smoke requirement + reading-mode regression requirement)
    - .planning/phases/05.1-edit-mode-inline-buttons/05.1-VALIDATION.md §Manual-Only Verifications — test instructions
    - .planning/phases/05.1-edit-mode-inline-buttons/05.1-RESEARCH.md §Pitfall 1 (block-widget symptoms as failure modes), §Pitfall 2 (flicker symptoms), §Pitfall 4 (late-appearance symptoms), §Pitfall 5 (mode-flip-vanish symptoms), §Pitfall 6 (click no-op symptoms)
  </read_first>
  <what-built>
    Phase 5.1 Plan 02 shipped `src/main/codeActionsEditorExtension.ts` — a CM6 `StateField<DecorationSet>` editor extension that paints an inline Run/Submit button widget (`Decoration.widget({ side: 1 })` — explicitly NOT `block: true`) directly below the closing fence of the `## Code` section, gated on `lc-slug` frontmatter. The extension is registered in `main.ts` onload() as Step 6f alongside the existing Step 6e reading-mode MarkdownPostProcessor. Widget DOM is built by the existing `buildCodeBlockButtonRow` helper (reused verbatim from reading mode), emitting `<div class="leetcode-code-actions">` with `<button class="leetcode-code-action-run">Run</button>` and `<button class="leetcode-code-action-submit">Submit</button>`. Click handlers call `plugin.runFromActive()` / `submitFromActive()` directly (not via `executeCommandById`). Idempotency guaranteed by `WidgetType.eq()` returning true for same-plugin widgets.

    All Wave 0 unit tests (Plan 01) GREEN. Reading-mode regression suite GREEN. esbuild bundle compiles cleanly. Zero new ESLint Required violations. Zero `block: true` usage. Zero `innerHTML` usage.

    **The one thing unit tests CANNOT verify is layout correctness in the live CM6 editor — that is what this checkpoint is for.**
  </what-built>
  <how-to-verify>
**Prerequisites:**
1. Run `npm run build` — confirm it exits 0 and produces an up-to-date `main.js` in the plugin folder (the dev vault's `.obsidian/plugins/obsidian-leetcode/` if symlinked, or the built artifact location per the project's dev-vault setup).
2. Open Obsidian with the dev vault loaded. Reload the plugin via Cmd-R (or disable + re-enable in Settings → Community Plugins) so the new extension is registered in onload().
3. Authenticate to LC if not already logged in (Settings → Obsidian LeetCode → Login) — needed only for the click-dispatch live verification steps (checklist items 4 and 5). If login is fresh-blocked for any reason, skip items 4 and 5 and explicitly note in the resume signal.

**Run the following 12 checks IN ORDER. Mark each with PASS or FAIL + observed-issue notes.**

Live Preview anchoring (core G1 success):
1. PASS / FAIL — Open an `lc-slug` note (e.g., `LeetCode/0001-two-sum.md`) in **Live Preview** mode. Run + Submit buttons appear inline immediately below the `## Code` fence's closing backticks, right-aligned, neutral-styled pill buttons. **Expected:** neutral (not accented) buttons, flush-right, small (24 px high), in-flow with document. **Failure modes to flag:** large blank rectangle above/below buttons (→ block widget regression, investigate), buttons not present (→ frontmatter gate bug), buttons in wrong position (→ fence detection bug), buttons on a fence NOT under `## Code`.
2. PASS / FAIL — Scroll up and down through the note. Buttons scroll with the note content (not fixed/floating). **Failure modes to flag:** buttons stick to bottom-right of pane (→ accidental floating toolbar), buttons vanish on scroll (→ viewport-scoped decoration bug).

Source Mode parity:
3. PASS / FAIL — On the same note, toggle to **Source Mode** (`Cmd-E`). Buttons appear at the end of the closing-fence line (right-aligned at line end). **Expected:** identical anchoring to Live Preview; no blank rectangles; no shifted headings above or below. **Failure modes to flag:** buttons missing in Source Mode (→ mode-specific branching bug), buttons cause heading-position drift (→ block-widget regression).

Reading Mode regression (D-11 LOCKED):
4. PASS / FAIL — Toggle to **Reading Mode** (`Cmd-E`). Phase 5 Plan 05 shipped Run + Submit buttons still appear below the `## Code` block — no behavioral change. **Failure modes to flag:** buttons absent in Reading Mode (→ regression in existing reading-mode processor; Plan 02 should NOT have touched `codeActionsPostProcessor.ts`), buttons duplicated.

Click dispatch (success criterion 3):
5. PASS / FAIL / SKIP (only if LC is unreachable) — Back in Live Preview, ensure the `## Code` fence has runnable code for the note's language. Click **Run** → expect "Running..." Notice followed by "Run result:..." Notice. Click **Submit** → expect "Submitting..." Notice followed by "Accepted" / "Wrong Answer" / other verdict Notice + VerdictModal. **Failure modes to flag:** click is a no-op (→ `editorCheckCallback` gate regression; confirm Plan 02 uses `plugin.runFromActive` direct call, not `executeCommandById`), Notice copy differs from Reading Mode (→ unintended behavioral divergence).

Gate correctness (scope of affordance):
6. PASS / FAIL — Open a **non-`lc-slug` note** (any scratch markdown file in the vault). In all three modes (Live Preview, Source Mode, Reading Mode) — **zero** Phase 5.1 buttons appear. **Failure modes to flag:** buttons appear on non-LC notes (→ frontmatter gate bypass).
7. PASS / FAIL — Create or open an `lc-slug` note with **no `## Code` fence** (e.g., a freshly-created problem note before starter code injection). All three modes — **zero** buttons. **Failure modes to flag:** buttons appear without a Code fence (→ fence detection bug).
8. PASS / FAIL — Open an `lc-slug` note that has example fences under `## Problem` but **no `## Code` fence**. All three modes — **zero** buttons (example fences stay bare, mirroring reading-mode D-02). **Failure modes to flag:** buttons on example fences (→ D-02 scope violation).

Idempotency + mode transitions:
9. PASS / FAIL — In Live Preview, type rapidly into the `## Code` fence (e.g., hold a key for a second). **Expected:** no button flicker, no duplicate widgets, buttons remain at correct position after typing stops. **Failure modes to flag:** visible flicker (→ WidgetType.eq() broken), duplicate button rows (→ RangeSetBuilder coalescing bug).
10. PASS / FAIL — Toggle Live Preview ↔ Source Mode multiple times (Cmd-E). Buttons present and correctly anchored after each toggle. **Failure modes to flag:** buttons vanish after one toggle and only reappear after an edit (→ `editorLivePreviewField` rebuild not firing on mode flip).

Theme verification (success criterion 6):
11. PASS / FAIL — Settings → Appearance → Base color scheme → **Light**. On the `lc-slug` note in Live Preview, buttons are legible (sufficient contrast between `--text-muted` on `--background-secondary`), hover lifts to `--text-normal` on `--background-modifier-hover`. **Failure modes to flag:** washed-out labels, invisible border, hover state indistinct.
12. PASS / FAIL — Settings → Appearance → Base color scheme → **Dark**. Same verification as item 11 but in dark theme.

**Spacing check (enables Task 2 below if needed):**
After item 3 (Source Mode) and item 1 (Live Preview), visually assess the vertical spacing between the closing-fence line and the top edge of the button row. Compare against the Reading-Mode spacing in item 4.

- If the Edit-Mode spacing looks visually consistent with Reading-Mode (same breathing room, no visible too-tight / too-loose gap) → spacing is correct; Task 2 does NOT run.
- If Edit-Mode spacing looks visibly too tight (buttons crowding the fence's closing backticks) or too loose (awkward gap between fence end and buttons) → note this; Task 2 runs with the sanctioned `.cm-editor .leetcode-code-actions { margin-top: 8px; }` override from UI-SPEC §Spacing Scale + RESEARCH Open Question 3.

**If ANY item (1-10) fails:** Do NOT approve. Instead, describe the failure in the resume signal. The developer will triage: minor tweaks stay in this plan (contingent Task 2); systemic failures bounce back to Plan 02 for rework (not this plan's concern, but flag it).

**If items 11-12 fail (theme contrast):** Note it but do not automatically block — the shipped styling uses Obsidian CSS variables that satisfied Phase 5 Plan 05's live smoke in both themes. Any contrast issue here would likely indicate a theme incompatibility that is a pre-existing Phase 5 concern, not a Phase 5.1 regression.

**Evidence capture (recommended but not blocking):** Screenshots of the `lc-slug` note in Live Preview (buttons visible, anchored correctly) and in Source Mode (same). Save to `.planning/phases/05.1-edit-mode-inline-buttons/screenshots/05.1-03-livepreview.png` and `.../05.1-03-sourcemode.png`. These are regression artifacts for any future CM6 extension work.
  </how-to-verify>
  <resume-signal>
Type one of:
- `approved` — all 12 items pass (or items 4-5 skipped due to unreachable LC, explicitly called out). No spacing misalignment observed — Task 2 is a no-op.
- `approved with spacing tweak` — all items pass but Edit-Mode spacing looks visually off (too tight or too loose vs. Reading-Mode) — Task 2 runs with the `margin-top: 8px` override.
- `approved with notes: <description>` — all items pass but you observed cosmetic issues worth recording; no tweak required.
- `blocking — <description of failure mode, which items failed, reproduction notes>` — one or more items 1-10 failed. Do NOT proceed to Task 2. Developer triages: contingent minor fix here, OR bounce to Plan 02 rework, OR open a new gap-closure plan.
  </resume-signal>
  <acceptance_criteria>
    - A resume signal was typed by the developer matching one of the four templates above.
    - If signal is `approved` or `approved with notes: ...`: all 12 checklist items marked PASS (items 4-5 may be SKIP if LC unreachable, explicitly called out).
    - If signal is `approved with spacing tweak`: all 12 items still PASS; Task 2 will run next.
    - If signal is `blocking — ...`: this plan DOES NOT CLOSE until the blocker is resolved (either in a follow-up revision of Plan 02 or a new gap-closure plan).
  </acceptance_criteria>
  <done>
    Developer has typed one of the four resume-signal templates. Evidence (passed / failed items + any screenshots) is captured in the plan SUMMARY at close time.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (CONTINGENT — runs only if Task 1 resume signal is "approved with spacing tweak"): Add .cm-editor-scoped margin-top override to styles.css</name>
  <files>styles.css</files>
  <read_first>
    - styles.css — locate the existing `.leetcode-code-actions` rule block (approximately lines 859-882 per 05.1-PATTERNS.md §styles.css lines 341-369). Confirm the existing `margin-top: 4px` value and the other Reading-Mode-shipped declarations (`display: flex`, `justify-content: flex-end`, `gap: 8px`, `margin-bottom: 8px`).
    - .planning/phases/05.1-edit-mode-inline-buttons/05.1-UI-SPEC.md §Spacing Scale lines 36-50 — sanctioned exception: `.cm-editor .leetcode-code-actions { margin-top: 8px }` (still 8-scale-compliant)
    - .planning/phases/05.1-edit-mode-inline-buttons/05.1-RESEARCH.md §Open Question 3 lines 558-562 — canonical override shape
    - .planning/phases/05.1-edit-mode-inline-buttons/05.1-PATTERNS.md §styles.css (CONTINGENT) lines 336-378 — exact drop-in CSS; planner directive on scoping
    - Task 1 resume signal — MUST be exactly `approved with spacing tweak` for this task to run
  </read_first>
  <behavior>
    - ONLY RUNS if Task 1 resume signal is exactly `approved with spacing tweak`. For any other resume signal (`approved`, `approved with notes: ...`, `blocking — ...`), this task is SKIPPED and the executor MUST NOT modify styles.css.
    - When it runs: appends a `.cm-editor .leetcode-code-actions { margin-top: 8px; }` rule to `styles.css` that scopes to CM6-rendered editor contexts only. Reading-Mode spacing (`margin-top: 4px` on the base rule) is UNCHANGED — the more-specific `.cm-editor` descendant selector wins only inside the editor DOM.
    - The addition follows the existing styles.css append-only convention (observed across Phase 5 Plan 05 Task 1 and subsequent plans).
    - No existing rule in styles.css is modified, reordered, or removed.
    - Reading-Mode buttons (05-05 shipment) retain identical spacing behavior — developer re-runs Task 1's checklist items 1, 3, and 4 as a 30-second sanity check after the CSS change lands.
  </behavior>
  <action>
**Gate check (FIRST ACTION):** If Task 1 resume signal is NOT exactly `approved with spacing tweak`, STOP. Do not modify styles.css. Emit a log line "Task 2 skipped — no spacing tweak required per Task 1 resume signal." and mark the task complete as a no-op.

**If the gate passes:** Append the following block to the end of `styles.css` (verbatim — drawn from 05.1-PATTERNS.md §styles.css and cross-checked against UI-SPEC sanctioned exception):

```css

/* Phase 5.1 (POLISH-07 / 05-UAT G1) — Edit-Mode inline button row.
   CM6 fence rendering has a different baseline than Reading-Mode <pre> margins;
   scoped override keeps visual parity without touching Reading-Mode spacing.
   Sanctioned by UI-SPEC §Spacing Scale + RESEARCH Open Question 3. */
.cm-editor .leetcode-code-actions {
  margin-top: 8px;
}
```

Do NOT modify the existing `.leetcode-code-actions` base rule (lines 859-882 per PATTERNS).
Do NOT change any other `.leetcode-code-action-run` / `.leetcode-code-action-submit` rules.
Do NOT broaden the selector (e.g., to `.leetcode-code-actions` unscoped) — Reading-Mode
spacing was verified correct at `margin-top: 4px` during Phase 5 Plan 05 live smoke and must
not drift.

After writing, re-run `npm run build` so the updated `styles.css` is picked up by the dev
vault on next reload. The developer visually re-verifies checklist items 1, 3, and 4 as a
sanity check (30 seconds; no formal sub-task).
  </action>
  <verify>
    <automated>test -f styles.css && (if grep -q "^\.cm-editor \.leetcode-code-actions" styles.css; then grep -q "margin-top: 8px" styles.css && npm run build; else echo "Task 2 skipped — no spacing tweak applied (resume signal was not 'approved with spacing tweak')"; fi)</automated>
  </verify>
  <acceptance_criteria>
    - If Task 1 resume signal was `approved with spacing tweak`:
      - `grep -q "^\.cm-editor \.leetcode-code-actions" styles.css` exits 0
      - `grep -q "margin-top: 8px" styles.css` exits 0
      - The base `.leetcode-code-actions` rule's declarations are UNCHANGED (verify `grep -A 5 "^.leetcode-code-actions {$" styles.css | grep -q "margin-top: 4px"` exits 0 — Reading-Mode base value preserved)
      - `npm run build` exits 0 (bundle rebuilds with updated styles.css)
      - `npm run lint` exits 0 (no new violations — CSS rules are linted via the existing toolchain, if applicable)
    - If Task 1 resume signal was anything else:
      - `styles.css` is BYTE-IDENTICAL to its pre-Task-2 state (verify via `git diff --stat styles.css` showing no changes)
      - No new rules appended; no existing rules modified
  </acceptance_criteria>
  <done>
    Either the contingent `.cm-editor .leetcode-code-actions { margin-top: 8px; }` override is appended to styles.css and the bundle rebuilds successfully, OR the task is documented as a no-op because the Task 1 resume signal did not call for it. Developer re-verifies spacing parity visually as a final sanity check.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Phase 5.1 adds zero new trust boundaries. Plan 03 is a human-verification checkpoint plus a
contingent CSS rule — pure UI affordance validation, no new network paths, no new vault writes,
no new IPC. The STRIDE register below is identical in substance to Plans 01 and 02 (which own
the implementation) because this plan only observes / tweaks visual presentation.

| Boundary | Description |
|----------|-------------|
| Live CM6 EditorView → developer eyes | The live Obsidian editor renders the shipped widget; the developer visually validates correctness |
| styles.css → all rendered `.leetcode-code-actions` instances (CONTINGENT) | A new scoped CSS rule (if applied) adjusts Edit-Mode spacing without touching Reading-Mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5.1-01 | Tampering (XSS) | Widget DOM observed in live smoke | mitigate | Plans 01 + 02 verified CF-07 compliance (no `innerHTML`). Live smoke additionally verifies the DOM renders as expected — no unexpected injection vectors. Covered by existing acceptance grep gates in Plan 02 |
| T-5.1-02 | Information Disclosure | `lc-slug` gate observed in live smoke | accept | Checklist items 6-8 explicitly verify the gate works: non-LC notes and LC notes without `## Code` show zero buttons. Identical semantics to 05-05 D-12 |
| T-5.1-03 | Denial of Service | Rapid-typing idempotency observed in live smoke | mitigate | Checklist item 9 explicitly verifies the `WidgetType.eq()` guard prevents flicker under sustained editing. Any flicker surfaced here is a Plan 02 regression, not a Plan 03 concern |
| T-5.1-04 | Elevation of Privilege | Click dispatch observed in live smoke | accept | Checklist item 5 verifies Run/Submit clicks actually execute — if they no-op, the `editorCheckCallback` regression (Pitfall 6) has re-emerged and the developer bounces to Plan 02 rework |
| T-5.1-05 | Spoofing | Popout-window rendering (informal observation) | mitigate | `view.dom.ownerDocument` usage verified in Plan 02 acceptance. Live smoke can optionally verify in a popout window (File → Open current tab in new window); not a formal checklist item but encouraged |
| T-5.1-06 | Tampering | CSS scope creep (contingent Task 2) | mitigate | Task 2's rule uses a `.cm-editor`-scoped descendant selector. Task 2 acceptance criteria verify the base `.leetcode-code-actions` rule is unchanged; Reading-Mode spacing cannot drift. If Task 2 runs, the developer re-verifies items 1, 3, 4 as a sanity check |
</threat_model>

<verification>
After Task 1 checkpoint and (if applicable) Task 2 complete, run:

1. Developer typed a resume signal for Task 1 matching one of the four templates (`approved` / `approved with spacing tweak` / `approved with notes: ...` / `blocking — ...`)
2. If Task 2 ran: `grep -q "^\.cm-editor \.leetcode-code-actions" styles.css` exits 0 AND `grep -q "margin-top: 8px" styles.css` exits 0 AND the base `.leetcode-code-actions` rule's `margin-top: 4px` is UNCHANGED
3. If Task 2 was skipped: `git diff --stat styles.css` shows no changes
4. `npm run build` exits 0 (even if Task 2 was a no-op — smoke-test the build still compiles after any wave-2 activity)
5. `npm test` exits 0 (full suite — catches any inadvertent regression; nothing about Task 2 should change test outcomes, but verify)
6. `npm run lint` exits 0 (no new Required violations)
7. The phase-03 SUMMARY captures: (a) the resume signal text, (b) which checklist items passed / failed, (c) screenshots if captured, (d) the Task 2 outcome (applied / skipped), (e) any observations for future CM6 extension work
</verification>

<success_criteria>
- Developer has visually verified the live Obsidian dev vault and typed one of the four resume signals
- If `approved` or `approved with notes: ...` or `approved with spacing tweak`: all 12 checklist items from UI-SPEC §Live-Smoke Acceptance Checklist PASS (items 4-5 may be SKIP if LC is unreachable, explicitly called out)
- If `blocking — ...`: this plan does NOT close; a follow-up plan or Plan 02 revision is required
- Reading-mode regression suite stays GREEN (checklist item 4 + automated `tests/main/codeActionsPostProcessor.test.ts`)
- Build pipeline (`npm run build`, `npm run lint`, `npm test`) all GREEN post-checkpoint
- If Task 2 ran: styles.css contains the `.cm-editor .leetcode-code-actions { margin-top: 8px; }` override, base rule unchanged, bundle rebuilt
- Phase 5.1 is FUNCTIONALLY + VISUALLY COMPLETE — POLISH-07 ship-blocker G1 is closed
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-edit-mode-inline-buttons/05.1-03-SUMMARY.md` capturing:
- Task 1 resume-signal text (verbatim)
- Checklist results: 12 items with PASS / FAIL / SKIP + any failure-mode notes
- Screenshot paths (if captured)
- Task 2 outcome: applied (with diff summary) OR skipped (reason)
- Post-checkpoint verification: `npm run build`, `npm test`, `npm run lint` outputs (all GREEN)
- Any learnings for future CM6 extension work (e.g., "mode-flip rebuild lag was imperceptible in live smoke", "theme contrast fine with default Obsidian themes — third-party themes untested in this phase")
- Phase-closure signal: "Phase 5.1 CLOSED — POLISH-07 ship-blocker G1 resolved" OR "Phase 5.1 BLOCKED — follow-up plan required: <description>"
</output>
