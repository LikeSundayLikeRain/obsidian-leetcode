---
phase: 12-polish-plugin-store-resubmission
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/contest/ContestScratchManager.ts
  - src/contest/ContestSolveView.ts
  - src/contest/ContestFinalizer.ts
  - src/main.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Contest scratch files are invisible in Obsidian file explorer (dot-prefixed folder)"
    - "AC during contest updates the sidebar badge in real time without manual refresh"
    - "Clicking a contest problem that already has an open tab refocuses existing tab"
    - "AI review does not run during active contest (deferred to finalization)"
    - "Contest Finish writes notes + summary + AI analysis before returning to browser"
  artifacts:
    - path: "src/contest/ContestScratchManager.ts"
      provides: "Hidden scratch folder"
      contains: ".leetcode-contest"
    - path: "src/contest/ContestSolveView.ts"
      provides: "AI review suppression in contest VerdictModal"
    - path: "src/main.ts"
      provides: "Contest tab idempotency + onVerdictChange re-render + finish lifecycle fix"
  key_links:
    - from: "src/main.ts"
      to: "src/contest/ContestSessionManager.ts"
      via: "onVerdictChange callback triggers re-render"
      pattern: "onVerdictChange"
    - from: "src/main.ts"
      to: "src/contest/ContestFinalizer.ts"
      via: "handleContestEnd awaits finalizeContest"
      pattern: "finalizeContest"
---

<objective>
Fix 5 contest mode bugs: hidden scratch folder (D-05), sidebar AC status update (D-06), tab idempotency (D-07), AI review deferral during contest (D-08), and finish lifecycle completion (D-09).

Purpose: Contest mode currently has integration defects from Phase 10 — scratch files pollute the vault, AC badge never updates, duplicate tabs open, AI review blocks the contest flow, and Finish drops silently without output.
Output: All 5 contest bugs resolved. Scratch files invisible, sidebar updates on AC, tabs reused, AI review deferred, finish lifecycle completes end-to-end.
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-polish-plugin-store-resubmission/12-PATTERNS.md
@.planning/phases/12-polish-plugin-store-resubmission/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scratch folder + Tab idempotency + onVerdictChange re-render</name>
  <files>src/contest/ContestScratchManager.ts, src/main.ts</files>
  <read_first>
    - src/contest/ContestScratchManager.ts
    - src/main.ts (lines 380-420 for contestSessionManager construction, lines 925-950 for openContestProblem)
    - src/preview/previewRouter.ts (tab-reuse pattern reference)
  </read_first>
  <action>
**D-05 (scratch folder):** In `src/contest/ContestScratchManager.ts`, change `const SCRATCH_FOLDER = 'LeetCode/contest-scratch'` to `const SCRATCH_FOLDER = '.leetcode-contest'`. Single constant change — all downstream code uses this constant via `this.folder`.

**D-07 (tab idempotency):** In `src/main.ts` `openContestProblem()` method (line 932), add a leaf scan BEFORE the unconditional `getLeaf('tab')`. Contest problems open as markdown leaves (not `CONTEST_SOLVE_VIEW_TYPE` — they use regular MarkdownView on scratch files). Scan `this.app.workspace.getLeavesOfType('markdown')` and find a leaf whose `(leaf.view as { file?: { path: string } }).file?.path` matches the scratch file path (`.leetcode-contest/${problem.slug}.md`). If found, call `this.app.workspace.revealLeaf(existing)` and return early. Otherwise proceed with existing `getLeaf('tab')` + `openFile`. Follow the existing leaf-scan pattern at lines 1004-1008 of `handleContestEnd`.

**D-06 (onVerdictChange re-render):** In `src/main.ts` at the `onVerdictChange` callback (line 396), replace the no-op comment with a call that triggers a re-render of the active contest timer header. The timer header (`ContestTimerHeader`) is rendered via `ProblemBrowserView` which holds a reference to the contest session. The simplest fix: call `this.problemBrowserView?.refreshContestDisplay()` or the equivalent re-render method. If no such method exists, dispatch a workspace event or directly call `this.problemBrowserView?.onContestStateChange?.()`. Inspect `ProblemBrowserView` to find the appropriate re-render hook — the key insight is that `ProblemBrowserView` already polls `getSession()` for badge state per the comment, so verify whether the polling interval is too slow (>2s) and if so, add an explicit trigger.
  </action>
  <verify>
    <automated>grep -q "'.leetcode-contest'" src/contest/ContestScratchManager.ts && grep -q "revealLeaf" src/main.ts && npx vitest run tests/contest/ --reporter=verbose && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'SCRATCH_FOLDER' src/contest/ContestScratchManager.ts` shows `.leetcode-contest`
    - `openContestProblem` method contains a leaf scan before `getLeaf('tab')` — `grep -A 15 'openContestProblem' src/main.ts` shows `getLeavesOfType('markdown')` filter by file path
    - `onVerdictChange` callback at line ~396 is no longer a no-op — it triggers a UI update
    - `npx vitest run tests/contest/` passes
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Scratch folder is `.leetcode-contest`, contest tabs are reused via leaf scan, onVerdictChange triggers sidebar re-render</done>
</task>

<task type="auto">
  <name>Task 2: AI review deferral + Contest finish lifecycle</name>
  <files>src/contest/ContestSolveView.ts, src/main.ts</files>
  <read_first>
    - src/contest/ContestSolveView.ts (lines 320-470 for VerdictModal construction + submit handler)
    - src/main.ts (lines 958-1017 for handleContestEnd)
    - src/contest/ContestFinalizer.ts
  </read_first>
  <action>
**D-08 (AI review suppression):** Verify that `ContestSolveView.ts` VerdictModal construction sites (lines ~322-326 and ~398-402) do NOT pass `onStartReviewStream`. Per PATTERNS.md inspection, this is already correct — the contest path has no `onStartReviewStream` callback. Confirm by reading the file. If already suppressed, no code change needed — just verify. If somehow present, remove the callback.

**D-09 (finish lifecycle):** In `src/main.ts` `handleContestEnd()` (line 958):

The bug is that `contestSessionManager.finish()` clears the session BEFORE `finalizeContest` runs. If finalization fails, session data is gone and user loses results. Fix:

1. The current code already captures the session return from `.finish()` / `.abort()` at lines 969-971, so the snapshot IS available. The real issue per RESEARCH Pitfall 3 is that the `catch` block just shows a Notice and `return`s — the user has no recovery path.

2. Add a user-facing finish success Notice: after successful `finalizeContest`, change the existing Notice to include the summary path (already done per lines 988-993 — verify this is working).

3. The deeper bug (per success criteria #12) is likely that `handleContestEnd` is not properly awaited when the "Finish" button fires. In the timer header's finish button handler, ensure `handleContestEnd` is awaited before any UI transition (switching back to browser view). Search for where the Finish button calls this method — likely in `src/contest/ContestTimerHeader.ts` or via the `onFinish` callback. Ensure the call is `await`ed (not fire-and-forget `void`).

4. Wrap the AI analysis portion (`runContestAnalysis`) — currently fire-and-forget via `void` at line 1015. This is intentional (analysis should not block the finish flow). But ensure finalizeContest + Notice + leaf cleanup all complete BEFORE the method returns. Inspect for any `void` or missing `await` on `finalizeContest` itself — it should already be awaited at line 976.
  </action>
  <verify>
    <automated>npx vitest run tests/contest/ --reporter=verbose && npm run build && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `ContestSolveView.ts` VerdictModal constructions have no `onStartReviewStream` callback
    - `handleContestEnd` properly awaits `finalizeContest` before showing Notice and cleaning up leaves
    - The Finish button's call path to `handleContestEnd` is awaited (not fire-and-forget) so UI transition happens after completion
    - `npx vitest run tests/contest/` passes
    - `npm run build` succeeds with no type errors
  </acceptance_criteria>
  <done>AI review suppressed during contest, finish lifecycle completes end-to-end with notes + summary written before returning to browser</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contest slug -> filesystem path | Slug used to construct scratch file path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Tampering | ContestScratchManager dot-folder | accept | Slug regex validation (T-10-10) already prevents path traversal; dot-folder is at vault root |
| T-12-03 | Denial of Service | handleContestEnd | mitigate | Wrap finalizeContest in try/catch (existing); ensure partial failure surfaces via Notice rather than silent drop |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `grep 'SCRATCH_FOLDER' src/contest/ContestScratchManager.ts` shows `.leetcode-contest`
- `npx vitest run tests/contest/` passes
- `npm run build` succeeds
- Manual: start contest, AC a problem, verify sidebar badge updates
- Manual: click same contest problem twice, verify single tab
</verification>

<success_criteria>
All 5 contest bugs fixed: scratch folder hidden, sidebar updates on AC, tabs reused, AI review deferred, finish lifecycle completes before UI transition.
</success_criteria>

<output>
Create `.planning/phases/12-polish-plugin-store-resubmission/12-02-SUMMARY.md` when done
</output>
