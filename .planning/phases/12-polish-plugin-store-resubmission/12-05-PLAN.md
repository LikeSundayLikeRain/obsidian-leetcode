---
phase: 12-polish-plugin-store-resubmission
plan: 05
type: execute
wave: 3
depends_on: ["12-01", "12-02", "12-03", "12-04"]
files_modified:
  - manifest.json
  - package.json
  - versions.json
  - README.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "manifest.json version is 1.1.0"
    - "README Network usage section documents all v1.1 AI + contest endpoints"
    - "README Cost expectations subsection links provider pricing pages with per-AC estimate"
    - "Lint + bundle-size CI gates pass at the release commit"
    - "package.json version matches manifest.json"
  artifacts:
    - path: "manifest.json"
      provides: "Version 1.1.0 + validated minAppVersion"
      contains: "1.1.0"
    - path: "README.md"
      provides: "Network usage audit for v1.1"
      contains: "Contest API"
    - path: "versions.json"
      provides: "Version-to-minAppVersion mapping"
      contains: "1.1.0"
  key_links:
    - from: "manifest.json"
      to: "package.json"
      via: "version field must be consistent"
      pattern: "1.1.0"
---

<objective>
Version bump to 1.1.0, README network/cost audit for all v1.1 endpoints, and release validation (D-13, D-14, D-15).

Purpose: Prepare the plugin for community store re-submission. The README must disclose all network endpoints (LeetCode + AI providers + contest API calls). Version bump and CI gates must be green at the release commit.
Output: manifest.json at 1.1.0, README audit complete, lint + bundle pass, ready for GitHub release.
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-polish-plugin-store-resubmission/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version bump + README network/cost audit</name>
  <files>manifest.json, package.json, versions.json, README.md</files>
  <read_first>
    - manifest.json
    - package.json (for version field)
    - versions.json (for version-to-minAppVersion mapping)
    - README.md (lines 60-120 for current network usage section)
    - tests/ai/readme-network-use.test.ts (for the 16 substring assertions that must still pass)
  </read_first>
  <action>
**D-13 (version bump):**
1. In `manifest.json`: change `"version": "1.0.1"` to `"version": "1.1.0"`. Keep `"minAppVersion": "1.10.0"` unchanged (no new Obsidian APIs used beyond what 1.10.0 provides).
2. In `package.json`: change `"version": "1.0.1"` to `"version": "1.1.0"`.
3. In `versions.json`: add entry `"1.1.0": "1.10.0"` to the mapping object.

**D-14 (README network/cost audit):**

The existing `## Network usage` section (written in Plan 07-06) already covers:
- leetcode.com (problem fetch, submit, run)
- 5 AI provider base URLs (Anthropic, OpenAI, OpenRouter, Ollama, Custom)
- Authentication subsection (LC session cookie + AI API keys)
- Cost expectations subsection

For v1.1, add:
1. Under the existing endpoint list, add a bullet for **LeetCode Contest API**: `leetcode.com/graphql` (contest list, contest problems) and `leetcode.com/contest/` (contest ranking/detail). These are the same `leetcode.com` domain but the README should clarify that contest operations use the same authenticated session.
2. Under **Cost expectations**: add a note about AI Knowledge Graph classification (~1 AI call per AC'd problem for pattern classification) and AI Contest Analysis (~1 AI call per completed contest). Provide rough per-AC cost estimate: "Typical cost per accepted solution: ~$0.01-0.05 depending on provider and model (classification + optional review)."
3. Verify the existing 5 AI base URL bullets are accurate: `api.anthropic.com`, `api.openai.com`, `openrouter.ai/api`, `localhost:11434` (Ollama default), user-configured custom URL.
4. Add AWS Bedrock endpoint: `bedrock-runtime.{region}.amazonaws.com` (added in Phase 08.1).

After editing, run `npx vitest run tests/ai/readme-network-use.test.ts` to ensure the 16 existing substring assertions still pass. If any fail due to restructuring, update the test assertions to match the new text while preserving all required disclosures.
  </action>
  <verify>
    <automated>grep -q '"1.1.0"' manifest.json && grep -q '"1.1.0"' package.json && grep -q "bedrock-runtime" README.md && npx vitest run tests/ai/readme-network-use.test.ts && npm run lint && npm run build && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `manifest.json` version is `"1.1.0"`, minAppVersion is `"1.10.0"`
    - `package.json` version is `"1.1.0"`
    - `versions.json` contains `"1.1.0": "1.10.0"` entry
    - README `## Network usage` section mentions: leetcode.com, api.anthropic.com, api.openai.com, openrouter.ai, localhost:11434, bedrock-runtime, contest operations
    - README `### Cost expectations` mentions per-AC estimate and contest analysis cost
    - `npx vitest run tests/ai/readme-network-use.test.ts` passes
    - `npm run lint` passes
    - `npm run build` produces main.js under 1.2 MB
  </acceptance_criteria>
  <done>Version bumped to 1.1.0; README network/cost section audited for all v1.1 endpoints; lint + build green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Release validation checkpoint</name>
  <what-built>Version bumped to 1.1.0 with full README network/cost audit. All CI gates (lint, bundle size, tests) pass at the release commit.</what-built>
  <how-to-verify>
    1. Run `npm run build` — confirm main.js is produced and size is under 1.2 MB
    2. Run `npm run lint` — confirm zero errors
    3. Run `npx vitest run` — confirm full test suite passes
    4. Open `manifest.json` — confirm version 1.1.0, minAppVersion 1.10.0
    5. Open `README.md` — scan "Network usage" section for completeness (LC + 5 AI providers + Bedrock + contest)
    6. Confirm `versions.json` has the 1.1.0 entry
    7. (Optional) If ready to publish: create GitHub release with `main.js` + `manifest.json` as attached assets
  </how-to-verify>
  <resume-signal>Type "approved" to confirm release readiness, or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README disclosure | Public documentation of network endpoints — accuracy is a trust signal for plugin reviewers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Information Disclosure | README network section | mitigate | Enumerate ALL endpoints honestly — plugin store reviewers grep for undisclosed network calls; missing disclosure causes rejection |
| T-12-09 | Tampering | versions.json | accept | File only affects Obsidian's version-check UI; no security impact |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `manifest.json` version is 1.1.0
- `package.json` version is 1.1.0
- `versions.json` maps 1.1.0 -> 1.10.0
- README enumerates all v1.1 network endpoints
- `npm run lint && npm run build && npx vitest run` all pass
- Bundle size under 1.2 MB
</verification>

<success_criteria>
v1.1.0 release artifacts ready: manifest bumped, README audited, CI gates green, ready for GitHub release creation and community-plugins.json PR.
</success_criteria>

<output>
Create `.planning/phases/12-polish-plugin-store-resubmission/12-05-SUMMARY.md` when done
</output>
