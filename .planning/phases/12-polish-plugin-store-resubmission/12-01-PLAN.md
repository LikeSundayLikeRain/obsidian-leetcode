---
phase: 12-polish-plugin-store-resubmission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/solve/verdictModalRenderer.ts
  - src/solve/VerdictModal.ts
  - styles.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "Verdict modal has no Close button in any state — Obsidian X button is sole dismissal"
    - "AI review area streams below the Accepted banner with no footer chrome blocking it"
    - "Modal footer is hidden when it has no action buttons (AC-only state)"
  artifacts:
    - path: "src/solve/verdictModalRenderer.ts"
      provides: "Close button removal from all 5 render paths"
      contains: "data-lc-role"
    - path: "src/solve/VerdictModal.ts"
      provides: "focusCloseButton removal"
    - path: "styles.css"
      provides: "Empty footer hidden rule"
      contains: "leetcode-verdict-footer:empty"
  key_links:
    - from: "src/solve/VerdictModal.ts"
      to: "src/solve/verdictModalRenderer.ts"
      via: "renderVerdict call delegates DOM building to renderer"
      pattern: "renderVerdict"
---

<objective>
Remove the Close button from ALL verdict modal states (per D-01, D-02) so the AI review stream renders below the Accepted banner without footer chrome blocking it.

Purpose: Fix the layout regression where AI review content renders below the Close button on AC, forcing users to scroll past AI output to dismiss the modal. Obsidian's native X button handles dismissal universally.
Output: VerdictModal no longer renders any `data-lc-role="close"` buttons; the `focusCloseButton` method is removed as dead code; empty footers are hidden via CSS.
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-polish-plugin-store-resubmission/12-PATTERNS.md
@.planning/phases/12-polish-plugin-store-resubmission/12-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Close buttons from verdictModalRenderer.ts</name>
  <files>src/solve/verdictModalRenderer.ts</files>
  <read_first>
    - src/solve/verdictModalRenderer.ts
    - .planning/phases/12-polish-plugin-store-resubmission/12-PATTERNS.md
  </read_first>
  <action>
Remove the 3-line Close button pattern (`appendEl(footer, 'button', 'mod-cta')` + `setText(closeBtn, 'Close')` + `closeBtn.setAttribute('data-lc-role', 'close')`) from all 5 render functions: `renderTimeout`, `renderRunResult`, `renderRunErrorBlock`, `renderSubmitVerdict`, `renderUnknownVerdict`. Per D-01.

After removal, inspect each footer div:
- `renderTimeout`: footer becomes empty — remove the entire footer `appendEl(container, 'div', 'leetcode-verdict-footer')` creation and any related DOM wiring.
- `renderRunResult`: footer keeps the AI: Debug button — leave footer intact.
- `renderRunErrorBlock`: footer keeps the AI: Debug button — leave footer intact.
- `renderSubmitVerdict`: footer keeps Copy/AI: Debug buttons — leave footer intact.
- `renderUnknownVerdict`: footer keeps the Copy payload button — leave footer intact.

Also remove the Close button event listener registration (`btn.addEventListener('click', onClose)`) pattern that accompanies each button if present.
  </action>
  <verify>
    <automated>grep -c 'data-lc-role.*close' src/solve/verdictModalRenderer.ts | grep -q '^0$' && echo "PASS: no close buttons remain" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-lc-role.*close' src/solve/verdictModalRenderer.ts` returns 0
    - `grep -c "setText.*'Close'" src/solve/verdictModalRenderer.ts` returns 0
    - The `renderTimeout` function no longer creates a footer div
    - The other 4 render functions still create footer divs with their respective action buttons (AI: Debug, Copy)
    - `npx vitest run tests/solve/ --reporter=verbose` passes
  </acceptance_criteria>
  <done>Zero Close buttons in verdictModalRenderer.ts; renderTimeout has no footer; other render functions retain their action-button footers</done>
</task>

<task type="auto">
  <name>Task 2: Remove focusCloseButton from VerdictModal.ts and add empty-footer CSS rule</name>
  <files>src/solve/VerdictModal.ts, styles.css</files>
  <read_first>
    - src/solve/VerdictModal.ts
    - styles.css
  </read_first>
  <action>
In `src/solve/VerdictModal.ts`:
1. Remove the entire `focusCloseButton()` private method (lines 254-265 per PATTERNS.md — the method queries `button[data-lc-role="close"]`, adds click listeners, and focuses the first button).
2. Remove all call sites of `this.focusCloseButton()` — located in `renderVerdict` (after renderVerdict call), `renderUnknown` (after renderVerdict call), and `renderTimeout` (after renderVerdict call). Per PATTERNS.md these are at lines 175, 202, 212.
3. Remove the comment "Primary-focus the Close button if the renderer exposed it." that precedes each call.

In `styles.css`:
Add a rule to hide empty footer divs (per D-02 — no footer chrome on AC-only state):
`.leetcode-verdict .leetcode-verdict-footer:empty { display: none; }`

Per 12-UI-SPEC, place this near the existing `.leetcode-verdict-footer` styles.
  </action>
  <verify>
    <automated>grep -c 'focusCloseButton' src/solve/VerdictModal.ts | grep -q '^0$' && grep -q 'leetcode-verdict-footer:empty' styles.css && npx vitest run tests/solve/ --reporter=verbose && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'focusCloseButton' src/solve/VerdictModal.ts` returns 0
    - `styles.css` contains `.leetcode-verdict .leetcode-verdict-footer:empty { display: none; }`
    - `npx vitest run tests/solve/` exits 0
    - `npm run build` succeeds with no type errors
  </acceptance_criteria>
  <done>focusCloseButton method and all call sites removed; empty footer CSS rule added; all solve tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Modal DOM | Obsidian-managed modal; no external input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | verdictModalRenderer.ts | accept | No user input rendered; Close removal is pure DOM deletion; existing setText() XSS protection unchanged |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- `grep -c 'data-lc-role.*close' src/solve/verdictModalRenderer.ts` returns 0
- `grep -c 'focusCloseButton' src/solve/VerdictModal.ts` returns 0
- `npx vitest run tests/solve/` passes
- `npm run build` succeeds
</verification>

<success_criteria>
Verdict modal Close button removed from all 5 states. AI review streams below Accepted banner without footer chrome. All existing tests pass.
</success_criteria>

<output>
Create `.planning/phases/12-polish-plugin-store-resubmission/12-01-SUMMARY.md` when done
</output>
