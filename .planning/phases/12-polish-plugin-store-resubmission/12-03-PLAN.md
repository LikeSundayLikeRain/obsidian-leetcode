---
phase: 12-polish-plugin-store-resubmission
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/solve/VerdictModal.ts
  - src/notes/NoteTemplate.ts
  - src/notes/NoteWriter.ts
  - styles.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "After AC with AI classification, verdict modal shows clickable pattern chip between Accepted banner and AI review"
    - "Clicking pattern chip closes modal and opens the hub note"
    - "Problem notes include # {Title} H1 heading before ## Problem"
  artifacts:
    - path: "src/solve/VerdictModal.ts"
      provides: "Pattern chip render + click navigation"
      contains: "leetcode-verdict-pattern-chip"
    - path: "src/notes/NoteTemplate.ts"
      provides: "H1 title in buildNoteBody"
    - path: "styles.css"
      provides: "Pattern chip CSS styles"
      contains: "leetcode-verdict-pattern-chip"
  key_links:
    - from: "src/solve/VerdictModal.ts"
      to: "app.metadataCache"
      via: "reads lc-pattern frontmatter for chip text"
      pattern: "lc-pattern"
    - from: "src/solve/VerdictModal.ts"
      to: "app.workspace.openLinkText"
      via: "chip click navigates to hub note"
      pattern: "openLinkText"
    - from: "src/notes/NoteWriter.ts"
      to: "src/notes/NoteTemplate.ts"
      via: "buildNoteBody called with title param"
      pattern: "buildNoteBody"
---

<objective>
Add the pattern chip to the verdict modal on AC (D-03, D-04) and the H1 title to new problem notes (D-11).

Purpose: After AC, the AI-assigned pattern (e.g., "Two Pointers") is invisible to the user. The chip makes classification visible and clickable — navigating to the hub note. The H1 title ensures notes are readable when Obsidian's "Show inline title" is disabled.
Output: Pattern chip visible on AC when lc-pattern is set; chip navigates to hub note on click; new notes start with `# {Title}` above `## Problem`.
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-polish-plugin-store-resubmission/12-PATTERNS.md
@.planning/phases/12-polish-plugin-store-resubmission/12-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pattern chip in VerdictModal + CSS</name>
  <files>src/solve/VerdictModal.ts, styles.css</files>
  <read_first>
    - src/solve/VerdictModal.ts
    - src/solve/verdictModalRenderer.ts (for appendEl/setText imports and DOM pattern)
    - styles.css (search for "leetcode-verdict" and "chip" patterns)
    - .planning/phases/12-polish-plugin-store-resubmission/12-UI-SPEC.md
  </read_first>
  <action>
**Pattern chip in VerdictModal (D-03, D-04):**

In `VerdictModal.ts`, in the `renderVerdict` method — AFTER the `isAccepted(res)` check fires and BEFORE `this.startReviewStream()` is called:

1. The VerdictModal needs access to `app` (already available via `this.app` inherited from Obsidian Modal) and the problem's file (to read lc-pattern from metadataCache). The `VerdictModalArgs` interface needs a `file` field (type `TFile | null`) so the construction site in main.ts can pass the active file context. Check if `args` already has a file reference or slug — if not, add `file?: TFile` to the args interface.

2. Read lc-pattern: `const cache = this.app.metadataCache.getFileCache(this.args.file); const pattern = cache?.frontmatter?.['lc-pattern'] as string | undefined;`

3. If pattern is truthy, render the chip:
   - Create a div: `const chip = appendEl(this.contentEl, 'div', 'leetcode-verdict-pattern-chip');`
   - Set text: `setText(chip, pattern);`
   - Set attributes: `chip.setAttribute('data-lc-role', 'pattern-chip');` + `chip.setAttribute('tabindex', '0');` + `chip.setAttribute('role', 'link');` + `chip.setAttribute('aria-label', 'Open ' + pattern + ' hub note');`
   - Add click handler: on click, call `this.close()` then `void this.app.workspace.openLinkText('LeetCode/Patterns/' + pattern + '.md', '', false);`
   - Add keyboard handler: on Enter/Space, trigger the same click action (accessibility per UI-SPEC).

4. In `src/main.ts`, at the VerdictModal construction sites (lines ~1895-1919 for normal submit, and similar for contest), pass `file: ctx.file` in the args object. The contest path may pass `null` if no TFile is available.

**CSS (per 12-UI-SPEC verbatim):**

Add to `styles.css` near the existing verdict styles:
- `.leetcode-verdict .leetcode-verdict-pattern-chip` — inline-flex, height 22px, padding 0 8px, border-radius 11px, font-size 11px, font-weight 600, background `color-mix(in srgb, var(--background-secondary) 85%, var(--interactive-accent) 15%)`, color `var(--text-accent)`, cursor pointer, transition background+border-color 120ms ease, border 1px solid `var(--background-modifier-border)`, margin 8px auto 0, text-align center.
- `.leetcode-verdict .leetcode-verdict-pattern-chip:hover` — background `color-mix(in srgb, var(--background-secondary) 70%, var(--interactive-accent) 30%)`, border-color `var(--interactive-accent)`.
  </action>
  <verify>
    <automated>grep -q 'leetcode-verdict-pattern-chip' src/solve/VerdictModal.ts && grep -q 'leetcode-verdict-pattern-chip' styles.css && npm run build && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `src/solve/VerdictModal.ts` contains `leetcode-verdict-pattern-chip` DOM creation
    - Chip element has `data-lc-role="pattern-chip"`, `tabindex="0"`, `role="link"`, `aria-label`
    - Click handler calls `this.close()` then `openLinkText` with pattern hub path
    - `styles.css` contains both `.leetcode-verdict-pattern-chip` and `:hover` rules per UI-SPEC
    - `npm run build` succeeds with no type errors
    - `npx vitest run tests/solve/` passes
  </acceptance_criteria>
  <done>Pattern chip renders on AC when lc-pattern is set; click closes modal and navigates to hub note; keyboard accessible</done>
</task>

<task type="auto">
  <name>Task 2: H1 title in NoteTemplate</name>
  <files>src/notes/NoteTemplate.ts, src/notes/NoteWriter.ts</files>
  <read_first>
    - src/notes/NoteTemplate.ts (lines 185-196 for buildNoteBody)
    - src/notes/NoteWriter.ts (lines 325-335 for buildNoteBody call site)
    - src/notes/HeadingRegion.ts (line 22 for heading detection pattern)
  </read_first>
  <action>
**D-11: H1 title in problem notes.**

In `src/notes/NoteTemplate.ts`:
1. Extend the `buildNoteBody` input parameter interface to include `title?: string`.
2. In the function body, prepend the H1 if title is provided: `const h1 = input.title ? '# ' + input.title + '\n\n' : '';`
3. Change the return to: `return h1 + '## Problem\n' + input.problemMarkdown.trim() + '\n\n' + CODE_HEADING_LINE + '\n' + codeBlock + '\n\n## Notes\n\n';`

In `src/notes/NoteWriter.ts`:
1. At the `buildNoteBody` call site (line ~330), pass the title from the detail entry: add `title: newEntry.title` to the argument object. The `newEntry` variable (a `DetailCacheEntry`) has a `title` field (confirmed from `buildFrontmatterInput` which reads `detail.title` at line 217 of NoteTemplate.ts).

**Important:** The H1 applies only to NEW notes. Existing notes are not retroactively modified. The `buildNoteBody` function is only called during note creation (not during updates), so this is automatically scoped correctly.

**HeadingRegion impact:** Check if `src/notes/HeadingRegion.ts` line 22 has a constant that must match `buildNoteBody()` output. If it references `## Problem` as the first heading, it should still work since H1 is above the H2 hierarchy. Verify no regex assumes the body starts with `## Problem`.
  </action>
  <verify>
    <automated>grep -q "input.title" src/notes/NoteTemplate.ts && grep -q "title:" src/notes/NoteWriter.ts && npx vitest run tests/notes/ --reporter=verbose && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `buildNoteBody` accepts an optional `title` parameter
    - When title is provided, output starts with `# {Title}\n\n## Problem`
    - When title is omitted, output starts with `## Problem` (backward compat)
    - `NoteWriter.ts` passes `title: newEntry.title` at the call site
    - `npx vitest run tests/notes/` passes (including any existing NoteTemplate tests)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>New problem notes include # {Title} H1 heading before ## Problem; existing notes unchanged; backward compatible</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lc-pattern value -> DOM | Pattern name from frontmatter rendered as chip text |
| Problem title -> note body | Title string inserted as H1 heading |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering | Pattern chip XSS | mitigate | `setText()` (textContent) used for chip text — never innerHTML; pattern name from frozen 22-item taxonomy validated at write time |
| T-12-05 | Tampering | H1 title injection | accept | Title comes from LeetCode API (trusted source); rendered as plain text in markdown heading; no HTML interpretation |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- Pattern chip appears in VerdictModal on AC when lc-pattern is set
- Chip click closes modal and opens hub note
- New notes start with `# {Title}\n\n## Problem`
- `npx vitest run tests/solve/ tests/notes/` passes
- `npm run build` succeeds
</verification>

<success_criteria>
Pattern chip visible and clickable on AC. H1 title present in newly created problem notes. All tests pass.
</success_criteria>

<output>
Create `.planning/phases/12-polish-plugin-store-resubmission/12-03-SUMMARY.md` when done
</output>
