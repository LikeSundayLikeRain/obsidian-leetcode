---
status: partial
phase: 05.5-section-locking-for-lc-slug-notes
source: [05.5-VERIFICATION.md]
started: 2026-05-13T18:21:00Z
updated: 2026-05-13T22:00:00Z
---

## Current Test

[awaiting remaining human testing]

## Tests

### 1. Type a character inside ## Problem body in Edit Mode (Live Preview)
expected: Keystroke drops silently — no buffer mutation, no Notice
result: passed (2026-05-13)

### 2. Type a character inside ## Code body (between fence opener and closer) in Edit Mode
expected: Keystroke lands normally in the buffer
result: [pending]

### 3. Click the chevron dropdown, pick a different language — fence opener rewrites atomically
expected: Fence opener changes from e.g. ` ```python ` to ` ```java ` in one undo step; lc-language frontmatter updates; chevron label + dropdown highlight update synchronously (no stale state)
result: passed (2026-05-13, after UAT-driven chevron-staleness fix — see Gaps below)

### 4. Open a non-lc-slug note in Edit Mode and type freely throughout
expected: All keystrokes land normally — lock is entirely inactive for non-LC notes
result: [pending]

### 5. Switch a lc-slug note to Reading Mode, confirm no lock surfaces
expected: Reading Mode renders the note normally; no lock artifact visible; note is non-editable by definition (Obsidian rendering, not CM6)
result: [pending]

### 6. Arrow-key motion past locked ranges (e.g., press Down at end of ## Code body)
expected: Cursor glides past the closing fence + ## Techniques heading to start of ## Techniques body; cursor does not stop on the locked closing fence line
result: [pending]

### 7. Heading-marker visibility: locked headings hide the `## ` prefix (parity with Reading Mode)
expected: ## Problem, ## Code, ## Techniques, ## Notes all render WITHOUT the literal `## ` prefix in Edit Mode (Live Preview), regardless of cursor position. Original D-04 visual-dim spec was rejected during UAT in favor of marker-hide for cleaner UX.
result: passed (2026-05-13, after UAT-driven Decoration.line + cm-formatting-header CSS hide)

## Summary

total: 7
passed: 3
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps

### G1 — Chevron dropdown stale after language switch (RESOLVED)
- discovered: 2026-05-13 during item #3 UAT
- symptom: After picking a new language from the chevron dropdown, the fence opener and `lc-language` frontmatter updated correctly, but the chevron LABEL and the dropdown's `is-current` highlight remained on the OLD language until the user typed in the fence body
- root cause: `buildDecorations` re-read `metadataCache.getFileCache(file).frontmatter['lc-language']`, but `processFrontMatter`'s vault write → metadataCache flush is async; the StateField rebuild raced ahead of the cache update and painted the old language
- fix: `languageRefreshEffect` now carries `string | undefined` payload; chevron switch passes `newSlug` directly through the effect, bypassing metadataCache entirely. The metadataCache `'changed'` listener also passes the freshly-flushed slug. `buildDecorations` accepts `overrideSlug` parameter that wins over the cache read. Combined with consolidating switchFenceLanguage's two dispatches into ONE (changes + effect in one transaction).
- status: RESOLVED — UAT confirmed 2026-05-13

### G2 — D-04 visual-dim rejected; marker-hide adopted instead (RESOLVED)
- discovered: 2026-05-13 during item #7 UAT
- symptom: User found the visual dim (`background: var(--background-secondary)`) noisy/unwanted. Original concern was the `## ` heading prefix being visible when cursor sat on a locked heading line, breaking parity with Reading Mode.
- decision: dropped the dim layer entirely; replaced with `Decoration.line` + CSS `display:none` on `.cm-formatting-header` tokens for locked heading lines. Locked headings now render parity with Reading Mode regardless of cursor position. Original RESEARCH Open Q4 recommendation ("ship visual dim default-on") superseded by UAT preference.
- status: RESOLVED — UAT confirmed 2026-05-13
