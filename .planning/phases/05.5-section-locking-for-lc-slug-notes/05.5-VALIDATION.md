---
phase: 05.5
slug: section-locking-for-lc-slug-notes
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-13
---

# Phase 05.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 4.x |
| **Config file** | `vitest.config.ts` |
| **Quick run command** | `npm test -- --run --reporter=dot tests/main/sectionLockExtension.test.ts` |
| **Full suite command** | `npm test -- --run` |
| **Estimated runtime** | ~5 seconds (lock unit tests) / ~15 seconds (full suite) |

---

## Sampling Rate

- **After every task commit:** Run quick command (lock-extension unit tests)
- **After every plan wave:** Run full suite
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Decision | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|----------|------------|-----------------|-----------|-------------------|-------------|--------|
| 05.5-01-01 | 01 | 0 | D-01 | — | N/A | unit | `npm test -- --run tests/main/sectionLockExtension.test.ts` | ❌ W0 | ⬜ pending |
| 05.5-01-02 | 01 | 0 | D-04 | — | N/A | unit | `npm test -- --run tests/helpers/obsidian-stub.test.ts` | ✅ | ⬜ pending |
| 05.5-02-01 | 02 | 1 | D-04 | — | keystrokes inside locked range drop silently | unit | `npm test -- --run tests/main/sectionLockExtension.test.ts` | ❌ W0 | ⬜ pending |
| 05.5-02-02 | 02 | 1 | D-08 | — | heading-line edits rejected | unit | `npm test -- --run tests/main/sectionLockExtension.test.ts` | ❌ W0 | ⬜ pending |
| 05.5-02-03 | 02 | 1 | D-09 | — | fence opener + closer locked, body editable | unit | `npm test -- --run tests/main/sectionLockExtension.test.ts` | ❌ W0 | ⬜ pending |
| 05.5-02-04 | 02 | 1 | D-03 | — | `## Techniques` + `## Notes` body editable | unit | `npm test -- --run tests/main/sectionLockExtension.test.ts` | ❌ W0 | ⬜ pending |
| 05.5-02-05 | 02 | 1 | D-06 | — | non-`lc-slug` notes unaffected | unit | `npm test -- --run tests/main/sectionLockExtension.test.ts` | ❌ W0 | ⬜ pending |
| 05.5-02-06 | 02 | 1 | D-07 | — | Edit Mode only (Reading Mode unaffected) | unit | `npm test -- --run tests/main/sectionLockExtension.test.ts` | ❌ W0 | ⬜ pending |
| 05.5-03-01 | 03 | 2 | D-04 | — | userEvent bypass for `leetcode.*` dispatch | unit | `npm test -- --run tests/main/sectionLockExtension.test.ts` | ❌ W0 | ⬜ pending |
| 05.5-03-02 | 03 | 2 | D-04 | — | chevron switch passes through filter | integration | `npm test -- --run tests/main/languageChevronWidget.test.ts` | ✅ | ⬜ pending |
| 05.5-03-03 | 03 | 2 | D-09 | — | malformed fence falls through (heading-only lock) | unit | `npm test -- --run tests/main/sectionLockExtension.test.ts` | ❌ W0 | ⬜ pending |
| 05.5-04-01 | 04 | 2 | — | — | extension registered in main.ts onload | integration | `npm run build && grep -q sectionLockExtension main.js` | ✅ | ⬜ pending |
| 05.5-04-02 | 04 | 2 | — | — | type check passes | unit | `npm run lint && npx tsc -noEmit` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/main/sectionLockExtension.test.ts` — new RED-state test file covering D-01..D-09 (heading lock, body lock, fence opener/closer lock, body editable, lc-slug gate, Edit Mode gate, userEvent bypass, malformed fence fallthrough)
- [ ] `tests/helpers/obsidian-stub.ts` — extend with `runChangeFilter(state, tx)` helper or `buildLcSlugEditorState(body)` factory; build minimal `EditorState` with `editorInfoField` + `editorLivePreviewField` + frontmatter pre-loaded
- [ ] No new framework install — vitest 4.x already configured

---

## Manual-Only Verifications

| Behavior | Decision | Why Manual | Test Instructions |
|----------|----------|------------|-------------------|
| Cursor visually skips locked range on arrow-key motion | D-04 | `EditorView.atomicRanges` requires a real `EditorView`, not just state | Open an `lc-slug` note in Live Preview, place cursor at end of `## Code` body, press Down arrow — cursor should jump to start of `## Notes` body, skipping over the closing fence + `## Techniques` heading + body |
| Visual dimming of locked ranges (if `Decoration.mark` is added) | D-04 (planner discretion) | CSS rendering not testable in vitest unit | Open an `lc-slug` note, confirm `## Problem`, fence opener line, and locked headings have a dimmed background |
| Dropped keystroke is silent (no Notice / no toast) | D-04 | Notice surface not exposed in vitest stub | Type a character inside `## Problem` body, confirm no Notice appears at top-right of Obsidian |
| Background-refresh / chevron-switch / on-AC overwrites still work | D-02 | end-to-end editor + plugin write paths | Trigger background-refresh by stale-cache problem open, then chevron-switch language, then accept a graph completion — confirm all three writes land without errors |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (`tests/main/sectionLockExtension.test.ts`, helper extensions)
- [ ] No watch-mode flags (all commands use `--run`)
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter after Wave 0 lands

**Approval:** pending
