---
phase: 05.2-pre-ship-ux-polish
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/browse/FilterModal.test.ts
  - tests/browse/ProblemListService.premium.test.ts
  - tests/browse/ProblemBrowserView.badge.test.ts
  - tests/settings/SettingsTab.test.ts
  - tests/main/python3Highlighter.test.ts
  - tests/graph/SubmissionDetailModal.test.ts
  - tests/main/fileOpenRetrofit.test.ts
  - tests/main/resetCommand.test.ts
  - tests/helpers/obsidian-stub.ts
autonomous: true
requirements:
  - POLISH-08

must_haves:
  truths:
    - "Wave 0 RED test shells exist for every behavior that Wave 1 will change"
    - "D-02: test shell asserts `language` is absent from FilterModal field-selector menu"
    - "D-03: test shell asserts `premium` multi-value evaluator behavior (both / non-premium only / empty)"
    - "D-04: test shell asserts auto-default marker excluded from badge count + stripped on Apply"
    - "D-06: test shell asserts workspace.on('file-open') hook fires retrofit only when lc-slug present"
    - "D-07: test shell asserts `reset-code` command registration + confirm gate behavior"
    - "D-10: test shell asserts Copy-to-Code calls performCopy directly without confirm modal"
    - "D-12: test shell asserts SettingsTab default-language dropdown key set matches LC pinned list"
    - "D-13: test shell asserts registerPython3Highlighter rewrites language-python3 to language-python"
    - "tests/helpers/obsidian-stub.ts exposes workspace.on('file-open') stub"
  artifacts:
    - path: "tests/browse/FilterModal.test.ts"
      provides: "RED tests for items 2, 3, 4 (language removed, premium multi-select, badge auto-default)"
    - path: "tests/browse/ProblemListService.premium.test.ts"
      provides: "RED tests for item 3 evaluator semantics"
    - path: "tests/browse/ProblemBrowserView.badge.test.ts"
      provides: "RED tests for item 4 badge count"
    - path: "tests/settings/SettingsTab.test.ts"
      provides: "RED tests for item 8 language-dropdown key set"
    - path: "tests/main/python3Highlighter.test.ts"
      provides: "RED tests for item 9 post-processor class swap"
    - path: "tests/graph/SubmissionDetailModal.test.ts"
      provides: "RED tests for item 7 silent Copy-to-Code"
    - path: "tests/main/fileOpenRetrofit.test.ts"
      provides: "RED tests for item 5a file-open retrofit hook"
    - path: "tests/main/resetCommand.test.ts"
      provides: "RED tests for item 5b Reset command + confirm gate"
    - path: "tests/helpers/obsidian-stub.ts"
      provides: "workspace.on('file-open') event stub extension"
  key_links:
    - from: "tests/browse/FilterModal.test.ts"
      to: "src/browse/FilterModal.ts"
      via: "vi.mock + direct import of FIELD_DEFS/renderValueEditor surfaces"
      pattern: "FilterModal|FIELD_DEFS"
    - from: "tests/main/python3Highlighter.test.ts"
      to: "src/main/python3Highlighter.ts"
      via: "dynamic import (file doesn't exist yet — RED)"
      pattern: "registerPython3Highlighter"
    - from: "tests/main/fileOpenRetrofit.test.ts"
      to: "tests/helpers/obsidian-stub.ts"
      via: "workspace.on stub"
      pattern: "workspace\\.on\\('file-open'"
---

<objective>
Create Wave 0 RED-state test shells for every Wave 1 behavior change in Phase 5.2. Each test file asserts the target behavior; all shells MUST fail on main (because the production code has not been written yet) and MUST pass after the corresponding Wave 1 plan ships.

Purpose: TDD gate for Phase 5.2. Wave 1 cannot begin until these shells exist. The shells make the decision surface explicit and catch regressions during implementation.

Output: 8 new test files + 1 obsidian-stub extension. No production code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-pre-ship-ux-polish/05.2-CONTEXT.md
@.planning/phases/05.2-pre-ship-ux-polish/05.2-RESEARCH.md
@.planning/phases/05.2-pre-ship-ux-polish/05.2-PATTERNS.md
@.planning/phases/05.2-pre-ship-ux-polish/05.2-VALIDATION.md

<interfaces>
<!-- Exported surfaces Wave 0 tests probe. Wave 1 plans MUST implement these. -->

From src/browse/FilterModal.ts (existing):
```typescript
// FIELD_DEFS array (lines 21-37) — entries user sees in "+ Add filter" menu
// DEFERRED_STUB_FIELDS array (lines 48-55) — currently contains `language` entry
// PREMIUM_OPTIONS constant (lines 69-72) — {value, label}[] with premium + non-premium
// renderMultiSelect(parent, rule, options) method (line 320)
// renderPremiumEditor(cell, rule) method (line 425) — DELETION TARGET
```

From src/settings/SettingsStore.ts (existing):
```typescript
export type FilterRule =
  | { field: 'status'; op: 'is' | 'is-not'; values: string[] }
  | { field: 'difficulty'; op: 'is' | 'is-not'; values: string[] }
  | { field: 'topics'; op: 'is' | 'is-not'; values: string[] }
  | { field: 'question-id'; op: 'range'; min: number | null; max: number | null }
  | { field: 'acceptance'; op: 'range'; min: number | null; max: number | null }
  | { field: 'premium'; op: 'is'; value: 'premium' | 'non-premium' | null }; // CURRENT
  // After 05.2-03: { field: 'premium'; op: 'is'; values: string[] }
```

From src/main/python3Highlighter.ts (NEW — will be created in 05.2-05):
```typescript
export function registerPython3Highlighter(plugin: Plugin): void;
```

From src/solve/statusMap.ts (existing):
```typescript
export function classifyStatus(status: number, statusDisplay?: string): { kind: string; displayName: string };
// Returns 'Accepted' / 'Wrong Answer' / 'Time Limit Exceeded' / ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend obsidian-stub with workspace.on('file-open') + create filter test shells</name>
  <read_first>
    - tests/helpers/obsidian-stub.ts (current workspace stub — identify where to add event registration)
    - src/browse/FilterModal.ts (lines 21-37 FIELD_DEFS; 48-55 DEFERRED_STUB_FIELDS; 69-72 PREMIUM_OPTIONS; 311-315 renderValueEditor premium case; 552-560 applyBtn click)
    - src/browse/ProblemListService.ts (line 259 evaluator premium case)
    - src/browse/ProblemBrowserView.ts (lines 66-72 first-open default; line 386 updateFilterBadge)
    - .planning/phases/05.2-pre-ship-ux-polish/05.2-PATTERNS.md §3, §4, §5 (exact code targets)
  </read_first>
  <behavior>
    tests/helpers/obsidian-stub.ts extension:
      - Workspace stub gains `_fileOpenHandlers: ((file: TFile | null) => void)[]`
      - Workspace stub `on('file-open', handler)` pushes into array and returns EventRef
      - Workspace stub `offref(ref)` no-op acceptable
      - Exposed test helper `fireFileOpen(workspace, file)` invokes all registered handlers
    tests/browse/FilterModal.test.ts (RED — file exists in Wave 0, tests fail until 05.2-03):
      - Test: "D-02: field selector menu does not list Language option"
        Assert: the DEFERRED_STUB_FIELDS array exported from FilterModal.ts has length 0 (OR is absent)
      - Test: "D-03: premium renderValueEditor uses multi-select"
        Assert: opening the premium editor renders checkbox options, not a single-value picker
      - Test: "D-04: Apply handler strips __autoDefault markers from draft rules"
        Assert: draft with {__autoDefault: true} rule → onApply receives rule WITHOUT __autoDefault
    tests/browse/ProblemListService.premium.test.ts (RED until 05.2-03):
      - Test: "D-03: premium values=[] → rule is no-op (returns undefined)"
      - Test: "D-03: premium values=['premium','non-premium'] → both paid and free pass"
      - Test: "D-03: premium values=['non-premium'] → only free (paid=false) passes"
    tests/browse/ProblemBrowserView.badge.test.ts (RED until 05.2-03):
      - Test: "D-04: fresh install with auto-default premium rule → badge count is 0"
        Assert: updateFilterBadge with filter containing one {__autoDefault: true} rule sets count=0 (badge not visible)
      - Test: "D-04: filter with one user rule + one auto-default rule → badge count is 1"
  </behavior>
  <action>
    1) Extend tests/helpers/obsidian-stub.ts:
       - Find the existing Workspace class/stub. Add `_fileOpenHandlers: Array<(file: TFile | null) => void> = []`.
       - Modify `on(name, cb)`: if `name === 'file-open'`, push `cb` into `_fileOpenHandlers` and return `{}` as EventRef.
       - Add `offref(ref): void { /* no-op */ }` if not already present.
       - Export a `fireFileOpen(ws, file)` helper that loops handlers and calls each with `file`.
       - Do NOT remove any existing exports — this is an additive extension.

    2) Create tests/browse/FilterModal.test.ts:
       - `vi.mock('obsidian', () => import('../helpers/obsidian-stub'))`
       - Import FIELD_DEFS and DEFERRED_STUB_FIELDS from `../../src/browse/FilterModal` (note: these may not be exported yet — mark failing assertion as RED with TODO comment referencing D-02).
       - Write 3 tests matching behaviors above. Use `expect(DEFERRED_STUB_FIELDS).toHaveLength(0)` (will fail today; passes after 05.2-03 deletes entries).
       - For D-03 premium: instantiate FilterModal with a premium rule in `initial`, call open, find the value cell and assert it contains a `.lc-fm__picker` (the multi-select trigger) NOT a radio/single-value picker.
       - For D-04 apply: spy on `onApply` callback, construct a FilterModal with a draft rule `{field:'premium', op:'is', values:['non-premium'], __autoDefault:true}`, click the apply button, assert the `onApply` argument's `rules[0]` has NO `__autoDefault` property.

    3) Create tests/browse/ProblemListService.premium.test.ts:
       - Import the `applyCompoundFilter` or per-rule evaluator exported from `../../src/browse/ProblemListService` (verify actual export name in source).
       - Build fixture rows with `paid: true` and `paid: false`.
       - Assert empty-values rule is no-op; values=['non-premium'] excludes paid rows; values=['premium','non-premium'] passes everything.

    4) Create tests/browse/ProblemBrowserView.badge.test.ts:
       - Use happy-dom to create a container div, stub the filterBadgeEl, invoke updateFilterBadge through a minimal test harness or via direct method call on the instance (per 05.2-PATTERNS.md §4 code target).
       - Two assertions per D-04 behaviors above.

    5) Ensure each new test file imports types/surfaces that DO exist on main (even if their future values are what the test asserts). Shells MUST compile today — they fail via `expect()` runtime, not `tsc` errors. Where an export doesn't exist yet (e.g. if FilterModal doesn't export FIELD_DEFS today), add the test with a `TODO(05.2-03)` comment and either mark `it.skip` OR expect the import itself to throw — your choice; prefer the skip-with-TODO so CI still runs cleanly.
  </action>
  <verify>
    <automated>npx vitest run tests/helpers tests/browse/FilterModal.test.ts tests/browse/ProblemListService.premium.test.ts tests/browse/ProblemBrowserView.badge.test.ts 2>&1 | tail -40</automated>
  </verify>
  <done>
    - All 4 test files exist and tsc-compile (npx vitest must load them without TypeScript errors)
    - Tests fail for expected behaviors (RED state) OR are marked `it.skip` with TODO(05.2-03)
    - obsidian-stub.ts has `workspace.on('file-open')` handler registration + fireFileOpen helper
    - No production `src/` file modified
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create settings/file-open/reset/copy/python3 test shells</name>
  <read_first>
    - src/settings/SettingsTab.ts (lines 14-21 LANGUAGE_OPTIONS constant)
    - src/main.ts (lines 291-306 Insert starter code command; 885-894 insertStarterCodeForced helper; imports around line 77)
    - src/graph/SubmissionDetailModal.ts (lines 168-177 handleCopyToCode; 195-208 askConfirm)
    - src/solve/starterCodeInjector.ts (line 139 retrofit signature — App, TFile, DetailCacheEntry|null, {getDefaultLanguage})
    - src/solve/slugGuard.ts (isValidSlug)
    - tests/main/codeActionsPostProcessor.test.ts (test shell analog for python3Highlighter)
    - .planning/phases/05.2-pre-ship-ux-polish/05.2-PATTERNS.md §7, §8, §10, §11 (exact code targets for new files)
  </read_first>
  <behavior>
    tests/settings/SettingsTab.test.ts (RED until 05.2-02):
      - Test: "D-12: LANGUAGE_OPTIONS contains expected LC slug keys"
        Assert: keys include python3, python, java, cpp, c, csharp, javascript, typescript, php, swift, kotlin, dart, golang, ruby, scala, rust, racket, erlang, elixir
        Assert: label for python3 is 'Python3'; python is 'Python'; cpp is 'C++'
    tests/main/python3Highlighter.test.ts (RED until 05.2-05):
      - Test: "D-13: rewrites language-python3 → language-python on rendered code"
      - Test: "D-13: leaves language-java / language-cpp untouched"
    tests/graph/SubmissionDetailModal.test.ts (RED until 05.2-04):
      - Test: "D-10: handleCopyToCode calls performCopy directly — no confirm modal opened"
        Assert: with a non-empty existing fence, no ConfirmOverwriteModal is constructed; performCopy is invoked once.
    tests/main/fileOpenRetrofit.test.ts (RED until 05.2-04):
      - Test: "D-06: workspace file-open fires retrofit when frontmatter has lc-slug"
      - Test: "D-06: workspace file-open does NOT fire retrofit when lc-slug is missing"
      - Test: "D-06: workspace file-open swallows retrofit errors silently (no throw)"
    tests/main/resetCommand.test.ts (RED until 05.2-04):
      - Test: "D-07: reset-code command registered with id='reset-code' and name='Reset code'"
      - Test: "D-05: insert-starter-code command is NOT registered"
      - Test: "D-07: Reset on file with existing fence opens ConfirmOverwriteModal"
      - Test: "D-07: Reset on file with empty Code section proceeds without confirm"
      - Test: "D-07: editorCheckCallback returns false for non-lc-slug files"
  </behavior>
  <action>
    1) Create tests/settings/SettingsTab.test.ts:
       - `vi.mock('obsidian', () => import('../helpers/obsidian-stub'))`
       - Import LANGUAGE_OPTIONS from `../../src/settings/SettingsTab`. If not exported today, mark the test `it.skip` with `TODO(05.2-02)` comment asking 05.2-02 to export the constant.
       - Assertions per behavior block. Use `expect(Object.keys(LANGUAGE_OPTIONS).sort()).toEqual(EXPECTED_KEYS.sort())` and `expect(LANGUAGE_OPTIONS.python3).toBe('Python3')`.

    2) Create tests/main/python3Highlighter.test.ts:
       - Mirror tests/main/codeActionsPostProcessor.test.ts shell exactly (see 05.2-PATTERNS.md §11 for full template).
       - `const plugin = makeFakePlugin()` with registerMarkdownPostProcessor capturing the callback.
       - Import `registerPython3Highlighter` from `../../src/main/python3Highlighter`. File doesn't exist yet — test will fail at import resolution. Mark file with `// RED until 05.2-05` header comment.
       - Build a happy-dom fragment with `code.language-python3` and another with `code.language-java`; invoke captured callback; assert class swaps.

    3) Create tests/graph/SubmissionDetailModal.test.ts:
       - Mock obsidian. Construct a SubmissionDetailModal instance with test deps (file, code, lang).
       - Spy on `performCopy` method. Spy on `ConfirmOverwriteModal` constructor (via module mock).
       - Invoke `handleCopyToCode()`. Assert `performCopy` called once, `ConfirmOverwriteModal` constructor called zero times.
       - Note: the existing file `tests/graph/copyToCode.confirm.test.ts` may exercise the old confirm path — DO NOT delete or modify it in this task. 05.2-04 will reshape it. Leave a `TODO(05.2-04): reshape copyToCode.confirm.test.ts after silent-overwrite lands` comment in this new file.

    4) Create tests/main/fileOpenRetrofit.test.ts:
       - Import `fireFileOpen` helper from obsidian-stub extension (Task 1).
       - Mock `retrofit` from `../../src/solve/starterCodeInjector` with vi.mock.
       - Build a mock Plugin with settings.getDefaultLanguage() returning 'python3' and metadataCache.getFileCache returning frontmatter fixtures.
       - Construct the hook (Wave 1 plan 05.2-04 will add it in main.ts; this test imports a future surface). Mark `it.skip` with `TODO(05.2-04): unskip once workspace.on('file-open') hook registered in main.ts` IF the test requires a running Plugin instance. Otherwise assert on a pure helper function that 05.2-04 is expected to extract.
       - Preferred approach: write the test against a future exported helper `handleFileOpenForStarterCode(plugin, file)` and mark skip with TODO asking 05.2-04 to extract this helper for testability.

    5) Create tests/main/resetCommand.test.ts:
       - Instantiate the plugin with a mocked app + auth + settings, call onload, inspect `this.app.commands` / captured `addCommand` calls for `{id: 'reset-code', name: 'Reset code'}`.
       - Assert `insert-starter-code` command id is NOT among captured commands.
       - For confirm gate: spy on ConfirmOverwriteModal constructor; with fixture file body containing a non-empty fence, invoke the reset handler and assert constructor called once.
       - With fixture body having no existing fence, assert constructor called zero times.
       - For non-lc-slug gate: invoke editorCheckCallback with checking=true and frontmatter without lc-slug; assert return is false.
       - Mark `it.skip` where Plugin instantiation surface isn't yet exposed with `TODO(05.2-04)`.
  </action>
  <verify>
    <automated>npx vitest run tests/settings/SettingsTab.test.ts tests/main/python3Highlighter.test.ts tests/graph/SubmissionDetailModal.test.ts tests/main/fileOpenRetrofit.test.ts tests/main/resetCommand.test.ts 2>&1 | tail -60</automated>
  </verify>
  <done>
    - All 5 test files exist and the vitest loader parses them without TypeScript errors
    - Tests that cannot compile today (e.g. imports pointing at files not yet created) are correctly marked `it.skip` with TODO(05.2-NN) comments
    - Test names match D-IDs in their labels (e.g. describe block or it name includes `D-NN`)
    - No production `src/` file modified in this task
    - Full Wave 0 test file set: 8 new test files + 1 obsidian-stub extension present and loadable
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → source code | vitest loads source modules; no runtime trust boundary introduced. |
| obsidian-stub → test code | test helpers mimic Obsidian API surface; happy-dom isolates DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-01 | Tampering | tests/helpers/obsidian-stub.ts extension | mitigate | Additive-only changes; existing exports preserved; new `_fileOpenHandlers` + `fireFileOpen` namespaced to avoid collision. Verified by running existing test suite after the edit. |
| T-05.2-02 | Denial of service | vitest loader | accept | If a new shell fails to compile, `npm test` red-lights the whole suite — correct fail-fast posture for a Wave 0 gate. |
</threat_model>

<verification>
- All 8 new test files + obsidian-stub extension exist.
- `npm test` load phase completes without TypeScript errors (compile gate).
- Test shells fail or skip as expected (RED state); zero GREEN behavioral assertions that don't yet have production code.
- Existing test suite on main still passes (regression).
</verification>

<success_criteria>
- 8 new test files created under `tests/browse/`, `tests/settings/`, `tests/graph/`, `tests/main/`
- `tests/helpers/obsidian-stub.ts` exposes `workspace.on('file-open')` handler registration and a `fireFileOpen` helper
- Each test file includes a `// RED until 05.2-NN` header comment pointing at the Wave 1 plan that will turn it GREEN
- Every D-01..D-13 decision covered by this phase has at least one RED assertion referencing it by ID (except D-01 which is covered only by live-smoke in Plan 06 — this is acceptable per VALIDATION.md manual-smoke table)
- No production code in `src/` modified
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-pre-ship-ux-polish/05.2-01-SUMMARY.md`
</output>
