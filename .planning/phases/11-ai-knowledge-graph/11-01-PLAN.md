---
phase: 11-ai-knowledge-graph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/graph/patternTaxonomy.ts
  - src/graph/buildKgPrompt.ts
  - src/graph/parseKgResponse.ts
  - src/graph/mergeRelatedVariantsSection.ts
  - src/ai/disclosure.ts
  - src/notes/NoteTemplate.ts
  - src/settings/SettingsStore.ts
  - tests/graph/buildKgPrompt.test.ts
  - tests/graph/parseKgResponse.test.ts
  - tests/graph/mergeRelatedVariantsSection.test.ts
  - tests/ai/disclosure.withKgBullet.test.ts
autonomous: true
requirements:
  - AIKG-01
  - AIKG-05
  - AIKG-06
  - AIKG-07

must_haves:
  truths:
    - "22 seed patterns are defined as a typed constant array"
    - "AI classification prompt assembles problem markdown + user code with pattern taxonomy"
    - "Response parser handles valid JSON, fenced JSON, regex extraction, and fallback gracefully"
    - "Related Variants section merges after Techniques and before AI Review"
    - "Feature flag lookAheadEdges defaults to false in PluginData"
    - "Disclosure copy includes KG-specific bullet via withKgBullet factory"
  artifacts:
    - path: "src/graph/patternTaxonomy.ts"
      provides: "22 seed patterns constant + normalizePatternName"
      exports: ["SEED_PATTERNS", "normalizePatternName"]
    - path: "src/graph/buildKgPrompt.ts"
      provides: "Pure prompt assembler for classification"
      exports: ["buildKgPrompt", "BuildKgPromptArgs"]
    - path: "src/graph/parseKgResponse.ts"
      provides: "Defensive JSON parser for AI classification response"
      exports: ["parseKgResponse", "KgClassification"]
    - path: "src/graph/mergeRelatedVariantsSection.ts"
      provides: "Pure body transform for ## Related Variants section"
      exports: ["mergeRelatedVariantsSection"]
    - path: "src/notes/NoteTemplate.ts"
      provides: "RELATED_VARIANTS_HEADING_LINE constant + LOCKED_HEADINGS extension"
      contains: "RELATED_VARIANTS_HEADING_LINE"
  key_links:
    - from: "src/graph/mergeRelatedVariantsSection.ts"
      to: "src/notes/NoteTemplate.ts"
      via: "import RELATED_VARIANTS_HEADING_LINE, TECHNIQUES_HEADING_LINE"
      pattern: "import.*RELATED_VARIANTS_HEADING_LINE.*NoteTemplate"
    - from: "src/ai/disclosure.ts"
      to: "withKgBullet"
      via: "export function"
      pattern: "export function withKgBullet"
---

<objective>
Foundation layer for AI Knowledge Graph: all pure helpers, constants, schema extensions, and their unit tests.

Purpose: Establish the typed contracts and pure transforms that Plans 02 and 03 build upon. Every artifact is testable in isolation with no Obsidian API dependency.
Output: 4 new source files (taxonomy, prompt builder, response parser, section merge), 2 modified files (disclosure, NoteTemplate), 1 schema extension (SettingsStore), and 4 test files proving correctness.
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ai-knowledge-graph/11-CONTEXT.md
@.planning/phases/11-ai-knowledge-graph/11-RESEARCH.md
@.planning/phases/11-ai-knowledge-graph/11-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pattern taxonomy + prompt builder + response parser (with tests)</name>
  <files>src/graph/patternTaxonomy.ts, src/graph/buildKgPrompt.ts, src/graph/parseKgResponse.ts, tests/graph/buildKgPrompt.test.ts, tests/graph/parseKgResponse.test.ts</files>
  <read_first>
    - src/ai/buildReviewPrompt.ts (exact analog for prompt structure — pure, no imports, join pattern)
    - src/notes/NoteTemplate.ts (constants export pattern — as const arrays)
    - tests/ai/buildReviewPrompt.test.ts (test structure pattern — determinism, content assertions, empty-input safety)
    - .planning/phases/11-ai-knowledge-graph/11-RESEARCH.md (prompt wording from "AI Classification Prompt Structure" section)
    - .planning/phases/11-ai-knowledge-graph/11-PATTERNS.md (parseKgResponse defensive parsing strategy)
  </read_first>
  <behavior>
    - patternTaxonomy: SEED_PATTERNS has exactly 22 entries; normalizePatternName trims, collapses whitespace, title-cases
    - buildKgPrompt: output contains all 22 seed pattern names; output contains "## Problem" + trimmed problemMd; output contains fenced code block with language tag; output contains JSON schema instructions; same args produce byte-identical output (determinism); empty code does not throw
    - parseKgResponse: valid JSON string returns parsed KgClassification; JSON wrapped in markdown fences parses successfully; JSON embedded in explanatory text is extracted via regex; malformed input returns fallback { pattern: 'OTHER', variants: [], lookAhead: [] }; variants array capped at 2; lookAhead array capped at 2; pattern name is normalized via normalizePatternName
  </behavior>
  <action>
    Create `src/graph/patternTaxonomy.ts`: export `SEED_PATTERNS` as a `readonly string[]` const with exactly 22 entries (18 NeetCode + Prefix Sum + Monotonic Stack + Topological Sort + Union-Find + Simulation per 11-RESEARCH.md prompt). Export `normalizePatternName(name: string): string` that trims, collapses internal whitespace to single spaces, and title-cases each word. Pure file, no imports, purity-contract header comment.

    Create `src/graph/buildKgPrompt.ts`: export interface `BuildKgPromptArgs` with fields `problemMd: string`, `code: string`, `language: string`. Export `buildKgPrompt(args: BuildKgPromptArgs): string` using the join-array pattern from `buildReviewPrompt.ts`. Prompt instructs AI to classify into one of the seed patterns or create a new name, return JSON with fields `pattern`, `variants` (0-2 cross-cluster structural twins with slug + reason), `lookAhead` (0-2 unsolved problems with slug + reason). Pure file, no imports beyond patternTaxonomy (import SEED_PATTERNS for embedding in the prompt text). Per D-03, prompt input is problem statement + user code only — NO LC topic tags.

    Create `src/graph/parseKgResponse.ts`: export interface `KgClassification` with fields `pattern: string`, `variants: Array<{slug: string; reason: string}>`, `lookAhead: Array<{slug: string; reason: string}>`. Export `parseKgResponse(text: string): KgClassification`. Parsing strategy per RESEARCH Pitfall 1: (1) try direct JSON.parse, (2) strip markdown code fences then parse, (3) regex extract first `{...}` then parse, (4) fallback to `{ pattern: 'OTHER', variants: [], lookAhead: [] }`. After successful parse, normalize pattern name via `normalizePatternName`, cap variants at 2, cap lookAhead at 2. Import `normalizePatternName` from `./patternTaxonomy`. Pure file, purity-contract header.

    Write tests RED first, then implement to GREEN.
  </action>
  <verify>
    <automated>npx vitest run tests/graph/buildKgPrompt.test.ts tests/graph/parseKgResponse.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - src/graph/patternTaxonomy.ts exports SEED_PATTERNS with length === 22
    - src/graph/patternTaxonomy.ts exports normalizePatternName function
    - src/graph/buildKgPrompt.ts exports buildKgPrompt and BuildKgPromptArgs
    - src/graph/buildKgPrompt.ts has zero imports other than patternTaxonomy
    - src/graph/parseKgResponse.ts exports parseKgResponse and KgClassification
    - tests/graph/buildKgPrompt.test.ts has at least 5 test cases covering determinism, content markers, empty input
    - tests/graph/parseKgResponse.test.ts has at least 6 test cases covering all 4 parse strategies + cap enforcement
    - All tests pass
  </acceptance_criteria>
  <done>Three new pure source files created with comprehensive test coverage. Prompt assembler is deterministic, parser is resilient to AI format drift, taxonomy is frozen at 22 seed patterns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Related Variants section merge + disclosure bullet + schema extensions (with tests)</name>
  <files>src/graph/mergeRelatedVariantsSection.ts, src/ai/disclosure.ts, src/notes/NoteTemplate.ts, src/settings/SettingsStore.ts, tests/graph/mergeRelatedVariantsSection.test.ts, tests/ai/disclosure.withKgBullet.test.ts</files>
  <read_first>
    - src/ai/mergeAIReviewSection.ts (exact analog — heading find, replace/append pattern, purity contract)
    - src/ai/disclosure.ts (withReviewBullet pattern to mirror for withKgBullet)
    - src/notes/NoteTemplate.ts (LOCKED_HEADINGS tuple, heading constant declarations)
    - src/settings/SettingsStore.ts (PluginData interface around line 51-130, DEFAULT_DATA around line 221-246, shape-guard load around line 614-700)
    - tests/ai/disclosure.withReviewBullet.test.ts (exact test pattern to mirror)
    - tests/graph/mergeTechniquesSection.test.ts (section merge test patterns)
    - src/graph/mergeTechniquesSection.ts (findSectionEnd + spliceRegion helpers — structural reference)
  </read_first>
  <behavior>
    - mergeRelatedVariantsSection: inserts ## Related Variants after ## Techniques when section absent; replaces existing ## Related Variants content idempotently; renders variants as `- [[slug]] — reason` format; handles missing ## Techniques (appends before ## AI Review); handles missing both anchors (appends at EOF)
    - withKgBullet: returns fresh object (not mutating DISCLOSURE_BASE_COPY); result.willSend.length === base.willSend.length + 1; result.willSend ends with KG-specific bullet text; result.neverSends === base.neverSends (reference equality)
    - NoteTemplate: RELATED_VARIANTS_HEADING_LINE === '## Related Variants'; LOCKED_HEADINGS includes RELATED_VARIANTS_HEADING_LINE
    - SettingsStore: PluginData gains featureFlags.lookAheadEdges (boolean, default false); PluginData gains autoAIKnowledgeGraph (boolean, default true); shape-guard handles missing/corrupt values
  </behavior>
  <action>
    Create `src/graph/mergeRelatedVariantsSection.ts`: import `RELATED_VARIANTS_HEADING_LINE`, `TECHNIQUES_HEADING_LINE`, `AI_REVIEW_HEADING_LINE` from `../notes/NoteTemplate`. Export `mergeRelatedVariantsSection(body: string, variants: Array<{slug: string; reason: string}>): string`. Logic mirrors `mergeAIReviewSection.ts` structure: find heading via exact-match scan, if found replace from heading to next H2 (bounded, not EOF), if absent insert AFTER `## Techniques` end (find Techniques heading, find its end via next H2, insert there). If Techniques also absent, insert before `## AI Review`. If neither anchor exists, append at EOF. Render variants as `- [[{slug}]] — {reason}` lines. Purity contract header. Pure function safe inside vault.process.

    Modify `src/ai/disclosure.ts`: add `withKgBullet` export function following `withReviewBullet` pattern exactly. Bullet text: `'AI Knowledge Graph sends the problem statement and your accepted code for pattern classification'`. Position: after `withContestAnalysisBullet`.

    Modify `src/notes/NoteTemplate.ts`: add `export const RELATED_VARIANTS_HEADING_LINE = '## Related Variants' as const;` after `AI_REVIEW_HEADING_LINE`. Add `RELATED_VARIANTS_HEADING_LINE` to `LOCKED_HEADINGS` array (per D-15, AIKG-07).

    Modify `src/settings/SettingsStore.ts`: add `featureFlags: { lookAheadEdges: boolean }` to PluginData interface. Add `autoAIKnowledgeGraph: boolean` to PluginData interface. Add defaults in DEFAULT_DATA: `featureFlags: { lookAheadEdges: false }`, `autoAIKnowledgeGraph: true` (KG is a core feature, default ON when AI configured — per RESEARCH Open Question 1 recommendation). Add shape-guards in load path mirroring the `autoAIReviewOnAC` pattern for both fields. Add getter/setter methods: `getFeatureFlags()`, `setFeatureFlag(key, value)`, `getAutoAIKnowledgeGraph()`, `setAutoAIKnowledgeGraph(value)`.

    Write tests RED first for mergeRelatedVariantsSection and withKgBullet, then implement to GREEN.
  </action>
  <verify>
    <automated>npx vitest run tests/graph/mergeRelatedVariantsSection.test.ts tests/ai/disclosure.withKgBullet.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - src/graph/mergeRelatedVariantsSection.ts exports mergeRelatedVariantsSection function
    - src/notes/NoteTemplate.ts contains `RELATED_VARIANTS_HEADING_LINE = '## Related Variants'`
    - src/notes/NoteTemplate.ts LOCKED_HEADINGS array includes RELATED_VARIANTS_HEADING_LINE
    - src/ai/disclosure.ts exports withKgBullet function
    - src/settings/SettingsStore.ts PluginData interface contains `featureFlags: { lookAheadEdges: boolean }`
    - src/settings/SettingsStore.ts PluginData interface contains `autoAIKnowledgeGraph: boolean`
    - src/settings/SettingsStore.ts DEFAULT_DATA contains featureFlags with lookAheadEdges: false
    - src/settings/SettingsStore.ts DEFAULT_DATA contains autoAIKnowledgeGraph: true
    - tests/graph/mergeRelatedVariantsSection.test.ts has at least 5 test cases (insert after Techniques, replace existing, no Techniques fallback, no anchors fallback, idempotent)
    - tests/ai/disclosure.withKgBullet.test.ts has at least 4 test cases (fresh object, length +1, verbatim bullet, neverSends reference equality)
    - All tests pass
    - npx vitest run (full suite) passes — no regressions from LOCKED_HEADINGS or SettingsStore changes
  </acceptance_criteria>
  <done>Related Variants section transform is idempotent and correctly anchored. Disclosure bullet follows established composition pattern. PluginData schema extended with feature flag and KG toggle. LOCKED_HEADINGS now protects ## Related Variants heading.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI provider response -> plugin | Untrusted JSON from LLM classification response |
| Problem slug in AI response -> vault wikilink | AI-suggested slugs must be validated before rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | parseKgResponse | mitigate | Strict JSON schema validation; cap arrays at 2; normalize pattern names; fallback to safe default on parse failure |
| T-11-02 | Information Disclosure | buildKgPrompt | accept | Only problem statement + user code sent (D-03); no vault paths, no ## Notes, no frontmatter beyond what's needed; disclosure bullet enumerates exactly what ships |
| T-11-03 | Denial of Service | parseKgResponse | mitigate | Bounded parse attempts (4 strategies, then fallback); no unbounded regex; no eval |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 11 — zero supply chain risk |
</threat_model>

<verification>
npx vitest run tests/graph/buildKgPrompt.test.ts tests/graph/parseKgResponse.test.ts tests/graph/mergeRelatedVariantsSection.test.ts tests/ai/disclosure.withKgBullet.test.ts --reporter=dot
npx vitest run  # full suite — no regressions
</verification>

<success_criteria>
- All 4 new test files pass
- Full test suite passes (no regressions from NoteTemplate or SettingsStore changes)
- Pattern taxonomy has exactly 22 entries
- Prompt builder is deterministic (same input -> same output)
- Response parser handles all 4 failure modes gracefully
- Related Variants merge is idempotent and correctly anchored relative to Techniques and AI Review
- LOCKED_HEADINGS includes ## Related Variants
- PluginData schema has featureFlags.lookAheadEdges (default false) and autoAIKnowledgeGraph (default true)
</success_criteria>

<output>
Create `.planning/phases/11-ai-knowledge-graph/11-01-SUMMARY.md` when done
</output>
