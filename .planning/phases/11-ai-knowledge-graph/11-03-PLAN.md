---
phase: 11-ai-knowledge-graph
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - src/graph/KnowledgeGraphWriter.ts
  - src/main.ts
  - src/settings/SettingsTab.ts
  - tests/graph/onAccepted.aiClassification.test.ts
autonomous: true
requirements:
  - AIKG-01
  - AIKG-02
  - AIKG-03
  - AIKG-04
  - AIKG-07

must_haves:
  truths:
    - "On AC, KnowledgeGraphWriter calls PatternClusterEngine.onAccepted after existing step 1 (frontmatter)"
    - "After patternClusterEngine.onAccepted completes, a fire-and-forget background reconcile fires (D-07 mechanism 2)"
    - "Palette command reconcile-pattern-hubs triggers a full hub reconcile"
    - "1-hour registerInterval fires background reconcile automatically"
    - "Settings tab has AI Knowledge Graph toggle and look-ahead edges toggle"
    - "Pattern classification fires only when autoAIKnowledgeGraph is enabled AND an AI provider is configured"
  artifacts:
    - path: "src/graph/KnowledgeGraphWriter.ts"
      provides: "Extended pipeline with AI classification step + post-AC background reconcile"
      contains: "patternClusterEngine"
    - path: "src/main.ts"
      provides: "PatternClusterEngine construction + palette command + reconcile interval"
      contains: "reconcile-pattern-hubs"
    - path: "src/settings/SettingsTab.ts"
      provides: "AI Knowledge Graph settings section with toggles"
      contains: "autoAIKnowledgeGraph"
  key_links:
    - from: "src/main.ts"
      to: "src/graph/PatternClusterEngine.ts"
      via: "construction at onload + passed to KnowledgeGraphWriter"
      pattern: "new PatternClusterEngine"
    - from: "src/graph/KnowledgeGraphWriter.ts"
      to: "src/graph/PatternClusterEngine.ts"
      via: "patternClusterEngine.onAccepted call in pipeline"
      pattern: "patternClusterEngine\\.onAccepted"
    - from: "src/graph/KnowledgeGraphWriter.ts"
      to: "src/graph/ClusterHubWriter.ts"
      via: "fire-and-forget hubWriter.reconcile after classification (D-07 mechanism 2)"
      pattern: "void.*hubWriter\\.reconcile"
    - from: "src/main.ts"
      to: "reconcilePatternHubs"
      via: "addCommand + registerInterval"
      pattern: "reconcile-pattern-hubs"
---

<objective>
Wiring layer for AI Knowledge Graph: integrate PatternClusterEngine into the existing on-AC pipeline, add the reconcile palette command + 1-hour timer + post-AC background reconcile, and expose settings UI toggles.

Purpose: Connect Plans 01+02's pure logic and services into the live plugin lifecycle so AI classification actually fires on Accepted submissions and hub notes stay consistent over time. Implements all four D-07 reconcile mechanisms.
Output: Extended KnowledgeGraphWriter pipeline, main.ts palette command + interval, Settings tab toggles, integration test.
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ai-knowledge-graph/11-CONTEXT.md
@.planning/phases/11-ai-knowledge-graph/11-RESEARCH.md
@.planning/phases/11-ai-knowledge-graph/11-PATTERNS.md
@.planning/phases/11-ai-knowledge-graph/11-01-SUMMARY.md
@.planning/phases/11-ai-knowledge-graph/11-02-SUMMARY.md
</context>

<interfaces>
<!-- Key types from Plans 01+02 that this plan wires -->

From src/graph/PatternClusterEngine.ts:
  export class PatternClusterEngine {
    constructor(deps: PatternClusterEngineDeps)
    async onAccepted(file: TFile, slug: string, problemHtml: string, code: string, language: string): Promise<void>
  }
  export interface PatternClusterEngineDeps { app: App; aiClient: AIClient; settings: SettingsStore; hubWriter: ClusterHubWriter }

From src/graph/ClusterHubWriter.ts:
  export class ClusterHubWriter {
    constructor(deps: { app: App; problemsFolder: string })
    async reconcile(app: App): Promise<void>
  }

From src/graph/KnowledgeGraphWriter.ts (current):
  export interface KnowledgeGraphWriterDeps { app: App; settings: KnowledgeGraphSettings }
  export class KnowledgeGraphWriter { async onAccepted(ctx, terminal): Promise<void> }

From src/settings/SettingsStore.ts:
  getAutoAIKnowledgeGraph(): boolean
  setAutoAIKnowledgeGraph(value: boolean): void
  getFeatureFlags(): { lookAheadEdges: boolean }
  setFeatureFlag(key: string, value: boolean): void

From src/main.ts (current wiring pattern):
  this.knowledgeGraph = new KnowledgeGraphWriter({...})  // line ~299
  // AC gate at line ~1943 calls this.knowledgeGraph.onAccepted(...)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend KnowledgeGraphWriter pipeline + main.ts wiring</name>
  <files>src/graph/KnowledgeGraphWriter.ts, src/main.ts</files>
  <read_first>
    - src/graph/KnowledgeGraphWriter.ts (full file — understand current pipeline steps, DI deps, gate logic)
    - src/main.ts lines 295-310 (KnowledgeGraphWriter construction site)
    - src/main.ts lines 1935-1955 (AC gate that calls knowledgeGraph.onAccepted)
    - src/main.ts lines 1865-1875 (startAutoReview trigger pattern — parallel with KG classification)
    - src/main.ts lines 570-600 (addCommand patterns for palette commands)
    - src/main.ts lines 695-710 (editorCheckCallback guard pattern)
    - src/graph/PatternClusterEngine.ts (from Plan 02 — constructor, onAccepted signature)
    - src/graph/ClusterHubWriter.ts (from Plan 02 — constructor, reconcile)
    - src/settings/SettingsStore.ts (getProblemsFolder, getAutoAIKnowledgeGraph, getProblemDetail)
    - .planning/phases/11-ai-knowledge-graph/11-CONTEXT.md (D-01 inline blocking, D-07 four-mechanism reconcile, D-10 lazy-on-AC)
  </read_first>
  <action>
    Modify `src/graph/KnowledgeGraphWriter.ts`:
    - Extend `KnowledgeGraphWriterDeps` interface to add optional fields `patternClusterEngine?: PatternClusterEngine` and `hubWriter?: ClusterHubWriter`. Both optional so existing construction sites (tests) don't break.
    - Store both in the class.
    - Add a new Step 2.5 AFTER the existing Step 2 (Techniques body write) and BEFORE Step 3 (stub creation). This step calls `this.patternClusterEngine?.onAccepted(ctx.file, ctx.slug, problemHtml, code, language)` where:
      - `problemHtml` comes from `this.settings.getProblemDetail(ctx.slug)?.contentHtml ?? ''`
      - `code` comes from reading the file's `## Code` fence content (extract from file body via a simple regex or the existing code-fence-read pattern)
      - `language` comes from `terminal.lang`
    - Wrap in try/catch with `logger.debug('graph.onAccepted: step 2.5 AI classification failed', err)`. Per D-01, this is inline blocking (awaited, not fire-and-forget).
    - When patternClusterEngine is present AND classification succeeds, SKIP Step 3 (stub creation) — hub notes replace stubs. When patternClusterEngine is absent (old path), Step 3 still fires.
    - Add Step 2.6 (D-07 mechanism 2: background full reconcile after each AC): AFTER Step 2.5 completes (whether it succeeds or fails), fire a background reconcile: `void this.hubWriter?.reconcile(this.app).catch(e => logger.debug('graph.onAccepted: background reconcile failed', e))`. This is fire-and-forget (NOT awaited) — it catches drift from the incremental append without blocking the AC flow. The reconcile rebuilds all hubs from frontmatter truth, catching any inconsistencies from the append path.
    - Import `PatternClusterEngine` type from `./PatternClusterEngine` and `ClusterHubWriter` type from `./ClusterHubWriter`.

    Modify `src/main.ts`:
    - In onload (after AIClient construction at Step 5.9, after knowledgeGraph construction at ~line 299):
      - Construct `ClusterHubWriter` with `{ app: this.app, problemsFolder: this.settings.getProblemsFolder() }`.
      - Construct `PatternClusterEngine` with `{ app: this.app, aiClient: this.aiClient, settings: this.settings, hubWriter }`.
      - Pass both `patternClusterEngine` and `hubWriter` into KnowledgeGraphWriter construction (extend the existing deps object).
    - Add palette command `reconcile-pattern-hubs` (D-07 mechanism 4):
      - `id: 'reconcile-pattern-hubs'`, `name: 'Reconcile pattern hubs'`
      - Callback: `void hubWriter.reconcile(this.app)` (fire-and-forget with try/catch + Notice on completion: `'Pattern hubs reconciled'`).
    - Register 1-hour reconcile interval (D-07 mechanism 3):
      - `this.registerInterval(window.setInterval(() => { void hubWriter.reconcile(this.app); }, 60 * 60 * 1000))`
      - Placed after the knowledgeGraph construction block.
    - Store `hubWriter` as instance field for the palette command to reference.
    - Ensure the disclosure copy extension happens at module init time: at the top of main.ts (or in the PatternClusterEngine module), push the KG bullet to DISCLOSURE_BASE_COPY.willSend. Per the established pattern from Phase 10's `withContestAnalysisBullet`, this happens by calling the `withKgBullet` factory and ensuring the base copy includes the KG bullet BEFORE any AIClient.invoke fires. The actual mechanism: in `PatternClusterEngine.ts` module top-level, import and call `withKgBullet` to register the bullet. Check how Phase 10's contest analysis bullet was registered and follow the same pattern.

    D-07 reconcile mechanism coverage (all four implemented):
    1. Incremental append on each AC — handled in PatternClusterEngine.onAccepted (Plan 02) via hubWriter.appendEntry
    2. Background full reconcile after each AC — Step 2.6 above: `void this.hubWriter?.reconcile(...)` fire-and-forget after classification
    3. 1-hour interval timer — registerInterval in main.ts
    4. Palette command — `reconcile-pattern-hubs` in main.ts
  </action>
  <verify>
    <automated>npx vitest run tests/graph/onAccepted.gate.test.ts tests/graph/onAccepted.tags.test.ts tests/ai/lc-isolation.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - src/graph/KnowledgeGraphWriter.ts KnowledgeGraphWriterDeps has patternClusterEngine and hubWriter optional fields
    - src/graph/KnowledgeGraphWriter.ts onAccepted calls patternClusterEngine.onAccepted in a try/catch after Step 2
    - src/graph/KnowledgeGraphWriter.ts fires void hubWriter.reconcile after Step 2.5 (D-07 mechanism 2 — fire-and-forget background reconcile)
    - src/graph/KnowledgeGraphWriter.ts skips Step 3 (stubs) when patternClusterEngine is present and classification ran
    - src/main.ts constructs ClusterHubWriter and PatternClusterEngine in onload
    - src/main.ts passes patternClusterEngine and hubWriter to KnowledgeGraphWriter deps
    - src/main.ts registers `reconcile-pattern-hubs` palette command (D-07 mechanism 4)
    - src/main.ts registers 1-hour setInterval for hub reconcile via registerInterval (D-07 mechanism 3)
    - tests/ai/lc-isolation.test.ts passes (no obsidianFetch import in src/graph/)
    - Existing onAccepted tests pass (patternClusterEngine is optional — undefined in old tests)
  </acceptance_criteria>
  <done>KnowledgeGraphWriter pipeline extended with AI classification step that fires inline on AC, followed by a fire-and-forget background reconcile (D-07 mechanism 2). Hub reconcile also runs on 1-hour timer (mechanism 3) and via manual palette command (mechanism 4). All four D-07 reconcile mechanisms are implemented. PatternClusterEngine is constructed in main.ts with full DI.</done>
</task>

<task type="auto">
  <name>Task 2: Settings UI toggles + integration test</name>
  <files>src/settings/SettingsTab.ts, tests/graph/onAccepted.aiClassification.test.ts</files>
  <read_first>
    - src/settings/SettingsTab.ts (find the AI settings section — look for "AI" heading; find the autoAIReviewOnAC toggle pattern to mirror)
    - src/settings/SettingsStore.ts (getAutoAIKnowledgeGraph, setAutoAIKnowledgeGraph, getFeatureFlags, setFeatureFlag)
    - tests/graph/onAccepted.gate.test.ts (existing gate test pattern — mock structure, assertion style)
    - tests/graph/onAccepted.tags.test.ts (full pipeline test pattern with mocked vault/settings)
    - src/graph/PatternClusterEngine.ts (onAccepted signature and gate conditions)
    - .planning/phases/11-ai-knowledge-graph/11-CONTEXT.md (D-16 look-ahead flag-gated default OFF)
  </read_first>
  <action>
    Modify `src/settings/SettingsTab.ts`:
    - In the AI settings section (after the existing autoAIReviewOnAC toggle), add two new toggles:
      1. **AI Knowledge Graph toggle**: `new Setting(containerEl).setName('AI pattern classification on Accept').setDesc('When enabled, AI classifies accepted solutions into algorithmic patterns and maintains hub notes.').addToggle(toggle => toggle.setValue(this.settings.getAutoAIKnowledgeGraph()).onChange(async (value) => { this.settings.setAutoAIKnowledgeGraph(value); await this.settings.save(); }))`.
      2. **Look-ahead edges toggle**: `new Setting(containerEl).setName('Look-ahead edges (experimental)').setDesc('When enabled, AI may suggest unsolved problems related to the pattern. Adds wikilinks to unsolved problems in ## Related Variants.').addToggle(toggle => toggle.setValue(this.settings.getFeatureFlags().lookAheadEdges).onChange(async (value) => { this.settings.setFeatureFlag('lookAheadEdges', value); await this.settings.save(); }))`.
    - Both toggles use sentence-case names per eslint-plugin-obsidianmd ui/sentence-case rule.
    - Position: after the autoAIReviewOnAC toggle, before any other AI-related settings.

    Create `tests/graph/onAccepted.aiClassification.test.ts`:
    - Integration test that verifies the full AC → classification → vault write pipeline.
    - Test cases:
      1. When autoAIKnowledgeGraph is false, PatternClusterEngine.onAccepted is NOT called.
      2. When autoAIKnowledgeGraph is true and AI provider configured, PatternClusterEngine.onAccepted IS called with correct args (file, slug, problemHtml, code, language).
      3. When PatternClusterEngine.onAccepted throws, KnowledgeGraphWriter continues (never-throw posture preserved).
      4. When patternClusterEngine is undefined (legacy path), Step 3 stubs still fire.
      5. Classification result writes lc-pattern frontmatter (verify processFrontMatter called with correct value).
      6. Classification skips Step 3 stubs when engine is present.
      7. After classification completes, hubWriter.reconcile is called (fire-and-forget, D-07 mechanism 2).
    - Mock structure: mock AIClient.invoke to return canned JSON response, mock vault.process/processFrontMatter, mock settings with controlled return values, mock hubWriter.reconcile as vi.fn.
  </action>
  <verify>
    <automated>npx vitest run tests/graph/onAccepted.aiClassification.test.ts --reporter=dot</automated>
    <automated>npx vitest run --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - src/settings/SettingsTab.ts contains toggle for 'AI pattern classification on Accept'
    - src/settings/SettingsTab.ts contains toggle for 'Look-ahead edges (experimental)'
    - Both toggles call the correct SettingsStore getter/setter
    - tests/graph/onAccepted.aiClassification.test.ts has at least 7 test cases (including D-07 mechanism 2 reconcile check)
    - Test verifies gate logic (autoAIKnowledgeGraph false -> engine not called)
    - Test verifies never-throw (engine throws -> writer continues)
    - Test verifies stub-skip (engine present -> step 3 stubs skipped)
    - Test verifies background reconcile fires after classification (D-07 mechanism 2)
    - Full test suite passes with no regressions
  </acceptance_criteria>
  <done>Settings UI exposes both the KG toggle (default ON) and look-ahead flag (default OFF, experimental). Integration test validates the complete AC -> classify -> write -> background-reconcile pipeline with proper gating and error isolation. All four D-07 mechanisms verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User settings -> classification gate | Toggle must reliably prevent AI calls when disabled |
| KnowledgeGraphWriter -> PatternClusterEngine | Engine failure must not break existing graph pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Denial of Service | reconcile interval + post-AC reconcile | mitigate | 1-hour interval is coarse; post-AC reconcile is fire-and-forget (non-blocking); both filter by problemsFolder prefix; use metadataCache (in-memory) |
| T-11-10 | Tampering | KnowledgeGraphWriter pipeline | mitigate | patternClusterEngine is optional in deps — undefined gracefully skips classification; existing pipeline unaffected |
| T-11-11 | Information Disclosure | Settings toggles | accept | Toggle state is local to data.json; no network disclosure from toggle changes alone |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
npx vitest run tests/graph/onAccepted.aiClassification.test.ts tests/graph/onAccepted.gate.test.ts tests/graph/onAccepted.tags.test.ts --reporter=dot
npx vitest run tests/ai/lc-isolation.test.ts --reporter=dot
npx vitest run  # full suite
npm run lint  # eslint passes
npm run build  # bundle compiles
</verification>

<success_criteria>
- AI classification fires inline on AC when toggle is ON and provider configured
- Background reconcile fires (fire-and-forget) after each AC classification completes (D-07 mechanism 2)
- Classification failure does not break existing KG pipeline (frontmatter tags still written)
- Hub reconcile fires on 1-hour interval (D-07 mechanism 3) and via palette command (D-07 mechanism 4)
- Settings tab exposes both toggles with correct defaults
- All existing tests pass (no regressions from KnowledgeGraphWriter extension)
- LC-isolation test passes
- Bundle compiles cleanly
- Lint passes
</success_criteria>

<output>
Create `.planning/phases/11-ai-knowledge-graph/11-03-SUMMARY.md` when done
</output>
