---
phase: 09-ai-aced-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/buildReviewPrompt.ts
  - src/ai/mergeAIReviewSection.ts
  - src/ai/disclosure.ts
  - tests/ai/buildReviewPrompt.test.ts
  - tests/ai/mergeAIReviewSection.test.ts
  - tests/ai/disclosure.withReviewBullet.test.ts
autonomous: true
requirements: [AIREV-02, AIREV-03, AIREV-04]
must_haves:
  truths:
    - "buildReviewPrompt assembles a deterministic prompt with 3 dimension headings"
    - "mergeAIReviewSection appends ## AI Review after ## Notes on first write"
    - "mergeAIReviewSection replaces existing ## AI Review to EOF on re-write (idempotent)"
    - "withReviewBullet returns a fresh object without mutating DISCLOSURE_BASE_COPY"
  artifacts:
    - path: "src/ai/buildReviewPrompt.ts"
      provides: "Pure prompt assembler for AI review"
      exports: ["buildReviewPrompt", "BuildReviewPromptArgs"]
    - path: "src/ai/mergeAIReviewSection.ts"
      provides: "Idempotent vault-write transform for ## AI Review section"
      exports: ["mergeAIReviewSection", "AI_REVIEW_HEADING_LINE"]
    - path: "src/ai/disclosure.ts"
      provides: "withReviewBullet composition factory"
      exports: ["withReviewBullet"]
    - path: "tests/ai/buildReviewPrompt.test.ts"
      provides: "Unit tests for prompt assembly"
    - path: "tests/ai/mergeAIReviewSection.test.ts"
      provides: "Unit tests for idempotent section merge"
    - path: "tests/ai/disclosure.withReviewBullet.test.ts"
      provides: "Unit tests for disclosure composition"
  key_links:
    - from: "src/ai/mergeAIReviewSection.ts"
      to: "src/notes/NoteTemplate.ts"
      via: "import NOTES_HEADING_LINE"
      pattern: "import.*NOTES_HEADING_LINE.*from.*NoteTemplate"
    - from: "src/ai/disclosure.ts"
      to: "DISCLOSURE_BASE_COPY"
      via: "spread composition"
      pattern: "\\.\\.\\.base\\.willSend"
---

<objective>
Create the three pure helper modules that Phase 09's wiring plans depend on: (1) `buildReviewPrompt` — assembles the combined-dimensions review prompt from problem markdown + code + language; (2) `mergeAIReviewSection` — idempotent vault-write transform that inserts or replaces `## AI Review` in a note body; (3) `withReviewBullet` — disclosure copy composition factory for the review feature. All three are zero-I/O, zero-DOM pure functions with comprehensive unit tests.

Purpose: Gates Plans 03 and 04 — both VerdictModal auto-review and manual re-run call these helpers.
Output: 3 source modules + 3 test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ai-aced-review/09-CONTEXT.md
@.planning/phases/09-ai-aced-review/09-RESEARCH.md
@.planning/phases/09-ai-aced-review/09-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/ai/buildDebugPrompt.ts (analog for buildReviewPrompt):
```typescript
export interface BuildDebugPromptArgs {
  problemMd: string;
  code: string;
  language: string;
  lastVerdict?: LastVerdict;
}
export function buildDebugPrompt(args: BuildDebugPromptArgs): string { ... }
```

From src/graph/mergeTechniquesSection.ts (analog for mergeAIReviewSection):
```typescript
import { TECHNIQUES_HEADING_LINE, NOTES_HEADING_LINE, CUSTOM_TESTS_HEADING_LINE } from '../notes/NoteTemplate';
const H2 = /^## /;
export function mergeTechniquesSection(body: string, topicTags: ReadonlyArray<{ name: string; slug: string }>): string { ... }
```

From src/ai/disclosure.ts (extension target):
```typescript
export function withDebugBullet(
  base: { willSend: readonly string[]; neverSends: readonly string[] },
): { willSend: readonly string[]; neverSends: readonly string[] } {
  return {
    willSend: [...base.willSend, 'AI Debug also sends the last failing run/submit verdict...'],
    neverSends: base.neverSends,
  };
}
```

From src/notes/NoteTemplate.ts:
```typescript
export const NOTES_HEADING_LINE = '## Notes';
export const LOCKED_HEADINGS = [PROBLEM_HEADING_LINE, CODE_HEADING_LINE, TECHNIQUES_HEADING_LINE, NOTES_HEADING_LINE] as const;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildReviewPrompt pure helper + tests</name>
  <files>src/ai/buildReviewPrompt.ts, tests/ai/buildReviewPrompt.test.ts</files>
  <read_first>
    - src/ai/buildDebugPrompt.ts (analog — mirror module structure, interface pattern, array-join assembly)
    - tests/ai/buildDebugPrompt.test.ts (analog — mirror test structure, verbatim fixture assertions)
    - src/notes/NoteTemplate.ts (heading constants for reference)
  </read_first>
  <behavior>
    - Test 1: buildReviewPrompt with standard args produces output containing `### Approach`, `### Efficiency`, `### Code Style` as three distinct headings
    - Test 2: Output contains `## Problem` section with the problemMd content trimmed
    - Test 3: Output contains a fenced code block with the language tag and code content trimmed
    - Test 4: Output includes instruction "Do NOT include code for minor style tweaks" (D-04 enforcement)
    - Test 5: Output never contains the string `## Notes` (locked decision — Notes is never sent to AI)
    - Test 6: Determinism — calling buildReviewPrompt N times with same args produces byte-identical output
    - Test 7: Empty code string does not throw — produces valid prompt with empty fence
  </behavior>
  <action>
    Create `src/ai/buildReviewPrompt.ts` mirroring `buildDebugPrompt.ts` structure. Export interface `BuildReviewPromptArgs` with fields: `problemMd: string`, `code: string`, `language: string`. Export function `buildReviewPrompt(args: BuildReviewPromptArgs): string`. Implementation: join an array of strings with `\n`. Prompt content per D-01/D-02/D-04:
    - System instruction: reviewing an Accepted LeetCode solution, constructive feedback in three sections
    - `## Problem` section with `args.problemMd.trim()`
    - `## Accepted {language} solution` section with fenced code block (`args.code.trim()`)
    - `## Review instructions` section with three sub-headings explaining expectations:
      - `### Approach`: evaluate algorithm choice; include code fence ONLY when fundamentally different approach exists (per D-04)
      - `### Efficiency`: time/space complexity analysis
      - `### Code Style`: readability, naming, edge-case, idiomatic usage
    - Final instruction: "Be concise. Do not restate the problem. Do not congratulate."
    
    Zero imports (type-only if needed). No I/O, no DOM, no Obsidian deps.
    
    Create tests mirroring `tests/ai/buildDebugPrompt.test.ts` describe/it structure.
  </action>
  <verify>
    <automated>npx vitest run tests/ai/buildReviewPrompt.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/ai/buildReviewPrompt.ts` exports `buildReviewPrompt` and `BuildReviewPromptArgs`
    - Output contains exactly 3 H3 headings: `### Approach`, `### Efficiency`, `### Code Style`
    - Output contains instruction about code fence only for fundamentally different approaches
    - Output never contains `## Notes`
    - All 7 test cases pass
  </acceptance_criteria>
  <done>buildReviewPrompt is a tested, deterministic, zero-I/O pure function that assembles the 3-dimension review prompt per D-01, D-02, D-04</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: mergeAIReviewSection vault-write transform + tests</name>
  <files>src/ai/mergeAIReviewSection.ts, tests/ai/mergeAIReviewSection.test.ts</files>
  <read_first>
    - src/graph/mergeTechniquesSection.ts (analog — mirror module structure, line-split approach, splice pattern)
    - tests/graph/mergeTechniquesSection.test.ts (analog — mirror test structure, body-string-in body-string-out assertions)
    - src/notes/NoteTemplate.ts (import NOTES_HEADING_LINE constant)
  </read_first>
  <behavior>
    - Test 1: First write — body with `## Notes` section but no `## AI Review` → appends `## AI Review` after Notes content (at EOF)
    - Test 2: First write — body without `## Notes` section → appends `## AI Review` at EOF
    - Test 3: Replacement — body already has `## AI Review` → replaces from heading to EOF with new content
    - Test 4: Idempotency — merge(merge(body, content), content) === merge(body, content)
    - Test 5: Heading detection is exact literal match (`'## AI Review'`), not regex
    - Test 6: Trailing whitespace handling — output ends with single `\n`, no double-trailing-newlines
    - Test 7: Empty reviewContent does not throw — produces heading with blank body
  </behavior>
  <action>
    Create `src/ai/mergeAIReviewSection.ts`. Import `NOTES_HEADING_LINE` from `'../notes/NoteTemplate'`. Export `AI_REVIEW_HEADING_LINE = '## AI Review' as const`. Export `mergeAIReviewSection(body: string, reviewContent: string): string`.
    
    Implementation per D-17/D-18/D-20:
    - Split body into lines
    - Find line index matching exact `AI_REVIEW_HEADING_LINE` string
    - If found (replacement path per D-20): slice lines before the heading, discard from heading to EOF, reconstruct as `before + '\n\n' + AI_REVIEW_HEADING_LINE + '\n\n' + reviewContent + '\n'`
    - If not found (first write per D-17/D-18): since `## AI Review` is always last, append after all existing content. Trim trailing newlines from body, then append `'\n\n' + AI_REVIEW_HEADING_LINE + '\n\n' + reviewContent + '\n'`
    
    The function is pure, retry-safe inside `vault.process` callback. No I/O, no DOM.
    
    Create tests mirroring `tests/graph/mergeTechniquesSection.test.ts` structure with body-in/body-out assertions.
  </action>
  <verify>
    <automated>npx vitest run tests/ai/mergeAIReviewSection.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/ai/mergeAIReviewSection.ts` exports `mergeAIReviewSection` and `AI_REVIEW_HEADING_LINE`
    - First-write appends `## AI Review` after existing content (after `## Notes` if present)
    - Replacement replaces from `## AI Review` heading to EOF
    - Idempotency holds: applying merge twice with same content produces identical output
    - All 7 test cases pass
  </acceptance_criteria>
  <done>mergeAIReviewSection is a tested, idempotent, pure vault-write transform that correctly inserts or replaces ## AI Review per D-17, D-18, D-20</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: withReviewBullet disclosure composition factory + tests</name>
  <files>src/ai/disclosure.ts, tests/ai/disclosure.withReviewBullet.test.ts</files>
  <read_first>
    - src/ai/disclosure.ts (target file — add withReviewBullet alongside withDebugBullet, observe frozen DISCLOSURE_BASE_COPY)
    - tests/ai/disclosure.withDebugBullet.test.ts (analog — mirror test structure exactly)
  </read_first>
  <behavior>
    - Test 1: withReviewBullet returns a FRESH object; DISCLOSURE_BASE_COPY is unchanged after call
    - Test 2: result.willSend.length === base.willSend.length + 1
    - Test 3: result.willSend ends with the locked verbatim review bullet
    - Test 4: result.neverSends === base.neverSends (reference equality — no copy)
    - Test 5: Calling factory twice returns two independent objects (no shared state)
  </behavior>
  <action>
    Add `withReviewBullet` function to `src/ai/disclosure.ts` immediately below `withDebugBullet`. Signature mirrors `withDebugBullet` exactly:
    
    `export function withReviewBullet(base: { willSend: readonly string[]; neverSends: readonly string[] }): { willSend: readonly string[]; neverSends: readonly string[] }`
    
    Body: spread `base.willSend` into new array, append the review-specific bullet string: `'AI Review sends the problem statement and your accepted solution code'` (per D-10 — describes what Review sends). Pass `neverSends` by reference. Return fresh object.
    
    JSDoc mirrors `withDebugBullet`'s comment, referencing Phase 09 and the D-10 locked composition contract.
    
    Create tests mirroring `tests/ai/disclosure.withDebugBullet.test.ts` — dynamic import isolation per `it` block, assert frozen base unchanged, reference equality for neverSends, independent objects per call.
  </action>
  <verify>
    <automated>npx vitest run tests/ai/disclosure.withReviewBullet.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/ai/disclosure.ts` exports `withReviewBullet`
    - The bullet string is exactly `'AI Review sends the problem statement and your accepted solution code'`
    - `DISCLOSURE_BASE_COPY` remains frozen and unchanged after any number of `withReviewBullet` calls
    - `result.neverSends` is reference-equal to `base.neverSends` (no unnecessary copy)
    - All 5 test cases pass
  </acceptance_criteria>
  <done>withReviewBullet composition factory ships alongside withDebugBullet, tested for immutability and correct bullet text per D-10</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI response → vault | Untrusted AI-generated text written to user's note file |
| User problem content → AI prompt | User-visible LC data included in prompt (low risk — no secrets) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | mergeAIReviewSection | mitigate | Pure function; deterministic output from (body, reviewContent) inputs; vault.process retry-safe |
| T-09-02 | Information Disclosure | buildReviewPrompt | mitigate | Never include `## Notes` in prompt (test assertion enforces); only problem + code sent |
| T-09-03 | Tampering | disclosure.ts | mitigate | withReviewBullet spreads frozen base — never mutates DISCLOSURE_BASE_COPY |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 09 — all internal code |
</threat_model>

<verification>
npx vitest run tests/ai/buildReviewPrompt.test.ts tests/ai/mergeAIReviewSection.test.ts tests/ai/disclosure.withReviewBullet.test.ts
</verification>

<success_criteria>
- All 3 pure helpers export correct types and functions
- All ~19 unit tests pass
- Zero external dependencies added
- buildReviewPrompt never includes ## Notes content
- mergeAIReviewSection is proven idempotent via test
- withReviewBullet proven to never mutate DISCLOSURE_BASE_COPY
</success_criteria>

<output>
Create `.planning/phases/09-ai-aced-review/09-01-SUMMARY.md` when done
</output>
