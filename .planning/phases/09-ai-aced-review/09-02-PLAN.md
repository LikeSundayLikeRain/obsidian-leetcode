---
phase: 09-ai-aced-review
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/notes/NoteTemplate.ts
  - src/main/sectionLockExtension.ts
  - src/settings/SettingsStore.ts
  - src/settings/SettingsTab.ts
  - tests/ai/aiReview.settings.test.ts
autonomous: true
requirements: [AIREV-01, AIREV-02]
must_haves:
  truths:
    - "## AI Review heading is locked in the editor (cannot be deleted by user)"
    - "PluginData.autoAIReviewOnAC defaults to false and survives load/save round-trip"
    - "Settings UI shows an Auto AI review toggle in the AI section"
  artifacts:
    - path: "src/notes/NoteTemplate.ts"
      provides: "AI_REVIEW_HEADING_LINE constant + LOCKED_HEADINGS extension"
      contains: "AI_REVIEW_HEADING_LINE"
    - path: "src/main/sectionLockExtension.ts"
      provides: "ai-review HeadingKind + lock range emission"
      contains: "ai-review"
    - path: "src/settings/SettingsStore.ts"
      provides: "autoAIReviewOnAC field in PluginData + DEFAULT_DATA + shape-guard"
      contains: "autoAIReviewOnAC"
    - path: "tests/ai/aiReview.settings.test.ts"
      provides: "Unit tests for shape-guard and default"
  key_links:
    - from: "src/main/sectionLockExtension.ts"
      to: "src/notes/NoteTemplate.ts"
      via: "import AI_REVIEW_HEADING_LINE"
      pattern: "import.*AI_REVIEW_HEADING_LINE.*from.*NoteTemplate"
    - from: "src/settings/SettingsTab.ts"
      to: "src/settings/SettingsStore.ts"
      via: "getAutoAIReviewOnAC / setAutoAIReviewOnAC"
      pattern: "autoAIReviewOnAC"
---

<objective>
Extend the schema layer so Phase 09's wiring plans can function: (1) add `AI_REVIEW_HEADING_LINE` to `NoteTemplate.ts` and extend `LOCKED_HEADINGS`; (2) add `'ai-review'` to `HeadingKind` in `sectionLockExtension.ts` with heading-only lock semantics; (3) add `autoAIReviewOnAC: boolean` field to `PluginData` with shape-guard defaulting to `false`; (4) add the settings toggle to the AI section of SettingsTab.

Purpose: Enables section locking for `## AI Review` and the opt-in toggle that Plans 03/04 check before firing AI calls.
Output: Schema + lock extension + settings toggle + test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ai-aced-review/09-CONTEXT.md
@.planning/phases/09-ai-aced-review/09-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/notes/NoteTemplate.ts (lines 74-79):
```typescript
export const LOCKED_HEADINGS = [
  PROBLEM_HEADING_LINE,
  CODE_HEADING_LINE,
  TECHNIQUES_HEADING_LINE,
  NOTES_HEADING_LINE,
] as const;
```

From src/main/sectionLockExtension.ts (lines 77-79):
```typescript
type HeadingKind = 'problem' | 'code' | 'techniques' | 'notes';
interface HeadingHit { readonly kind: HeadingKind; readonly line: number; }
```

From src/main/sectionLockExtension.ts (lines 190-192 — techniques/notes case):
```typescript
} else {
  // techniques | notes — heading line only (D-03). Body editable.
  out.push(headFrom, headTo);
}
```

From src/settings/SettingsStore.ts (PluginData interface, line 50+):
```typescript
export interface PluginData {
  // ... existing fields ...
  activeAIProvider: AIProvider | null;
  providerConfigs: Record<AIProvider, ProviderConfig | BedrockProviderConfig>;
  aiCostLedger: AICostLedger;
}
```

From src/settings/SettingsStore.ts (DEFAULT_DATA, line 204+):
```typescript
const DEFAULT_DATA: PluginData = {
  // ... existing fields ...
  activeAIProvider: null,
  providerConfigs: DEFAULT_PROVIDER_CONFIGS,
  aiCostLedger: { date: new Date().toISOString().slice(0, 10), usdToday: 0 },
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LOCKED_HEADINGS extension + sectionLock HeadingKind</name>
  <files>src/notes/NoteTemplate.ts, src/main/sectionLockExtension.ts</files>
  <read_first>
    - src/notes/NoteTemplate.ts (full file — find heading constants pattern, LOCKED_HEADINGS tuple at line 74)
    - src/main/sectionLockExtension.ts (full file — find HeadingKind type at line 77, import block at lines 57-61, Pass 1 loop at ~115-128, Pass 2 per-kind switch at ~133-195)
  </read_first>
  <action>
    In `src/notes/NoteTemplate.ts`:
    1. Add constant: `export const AI_REVIEW_HEADING_LINE = '## AI Review' as const;` — place it after `NOTES_HEADING_LINE` definition, before the `LOCKED_HEADINGS` tuple.
    2. Extend `LOCKED_HEADINGS` tuple by adding `AI_REVIEW_HEADING_LINE` as the 5th element (after `NOTES_HEADING_LINE`).
    
    In `src/main/sectionLockExtension.ts`:
    1. Add `AI_REVIEW_HEADING_LINE` to the import from `'../notes/NoteTemplate'`.
    2. Extend `HeadingKind` type union: `type HeadingKind = 'problem' | 'code' | 'techniques' | 'notes' | 'ai-review';`
    3. In Pass 1 loop (after the `NOTES_HEADING_LINE` else-if branch), add: `else if (text === AI_REVIEW_HEADING_LINE) { headings.push({ kind: 'ai-review', line: i }); }`
    4. In Pass 2 per-kind switch, the existing `else` clause handles `techniques | notes` with heading-line-only lock. Change the comment to `techniques | notes | ai-review` — the else clause already emits `out.push(headFrom, headTo)` which is correct for `ai-review` (D-19: heading locked, body editable). No code change needed in the else branch logic itself.
  </action>
  <verify>
    <automated>npx vitest run tests/main/sectionLock.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `NoteTemplate.ts` exports `AI_REVIEW_HEADING_LINE` with value `'## AI Review'`
    - `LOCKED_HEADINGS` tuple contains 5 elements ending with `AI_REVIEW_HEADING_LINE`
    - `sectionLockExtension.ts` imports `AI_REVIEW_HEADING_LINE`
    - `HeadingKind` includes `'ai-review'`
    - Pass 1 detects `## AI Review` lines
    - Pass 2 locks only the heading line (body editable) per D-19
    - Existing section lock tests remain green
  </acceptance_criteria>
  <done>## AI Review heading is locked in the editor via the existing sectionLockExtension infrastructure per D-19</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PluginData.autoAIReviewOnAC + settings toggle + tests</name>
  <files>src/settings/SettingsStore.ts, src/settings/SettingsTab.ts, tests/ai/aiReview.settings.test.ts</files>
  <read_first>
    - src/settings/SettingsStore.ts (PluginData interface at line 50, DEFAULT_DATA at line 204, load() shape-guard at line 524+)
    - src/settings/SettingsTab.ts (AI section — find the existing AI heading and provider dropdown; the toggle goes after the provider configuration area)
  </read_first>
  <behavior>
    - Test 1: DEFAULT_DATA.autoAIReviewOnAC is `false`
    - Test 2: Shape-guard collapses missing/non-boolean raw value to `false`
    - Test 3: Shape-guard preserves `true` when explicitly set
    - Test 4: getAutoAIReviewOnAC() returns the stored value
    - Test 5: setAutoAIReviewOnAC(true) persists and subsequent get returns true
  </behavior>
  <action>
    In `src/settings/SettingsStore.ts`:
    1. Add field to `PluginData` interface: `/** Phase 09 AIREV-01 — opt-in auto AI review on Accepted. Default false (user must explicitly enable). Shape-guard collapses non-boolean to false. */ autoAIReviewOnAC: boolean;`
    2. Add to `DEFAULT_DATA`: `autoAIReviewOnAC: false,` (place after `aiCostLedger` line).
    3. In the `load()` shape-guard block (~line 530), add: `autoAIReviewOnAC: typeof raw.autoAIReviewOnAC === 'boolean' ? raw.autoAIReviewOnAC : DEFAULT_DATA.autoAIReviewOnAC,`
    4. Add getter: `getAutoAIReviewOnAC(): boolean { return this.data.autoAIReviewOnAC; }`
    5. Add setter: `async setAutoAIReviewOnAC(value: boolean): Promise<void> { this.data.autoAIReviewOnAC = value; await this.persist(); }`
    
    In `src/settings/SettingsTab.ts`:
    1. In the AI settings section (after the existing provider configuration area, before Knowledge graph section), add a `new Setting(containerEl)` toggle:
       - `.setName('Auto AI review on Accept')`
       - `.setDesc('When enabled, an AI review is generated automatically each time you get Accepted.')`
       - `.addToggle(toggle => toggle.setValue(this.settings.getAutoAIReviewOnAC()).onChange(async (value) => { await this.settings.setAutoAIReviewOnAC(value); }))`
    
    Create `tests/ai/aiReview.settings.test.ts` verifying shape-guard and defaults.
  </action>
  <verify>
    <automated>npx vitest run tests/ai/aiReview.settings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `PluginData` interface includes `autoAIReviewOnAC: boolean`
    - `DEFAULT_DATA.autoAIReviewOnAC` is `false`
    - Shape-guard in `load()` collapses `undefined`, `null`, `'true'`, `42` to `false`
    - Shape-guard preserves `true` as `true`
    - SettingsTab renders a toggle in the AI section with correct name and desc
    - All 5 test cases pass
  </acceptance_criteria>
  <done>PluginData extended with autoAIReviewOnAC (default false, shape-guarded), Settings UI toggle wired per D-06</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data.json → PluginData | Untrusted persisted state loaded at plugin start |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | SettingsStore shape-guard | mitigate | Non-boolean `autoAIReviewOnAC` collapses to `false` — never enables AI calls from corrupt data |
| T-09-05 | Denial of Service | sectionLockExtension | accept | Adding a 5th heading to the lock loop is O(n) per document; negligible performance impact |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
npx vitest run tests/main/sectionLock.test.ts tests/ai/aiReview.settings.test.ts
</verification>

<success_criteria>
- LOCKED_HEADINGS has 5 entries
- HeadingKind includes 'ai-review'
- Section lock tests pass (existing + new heading locked)
- PluginData shape-guard defaults to false
- Settings toggle renders in AI section
- Full test suite remains green
</success_criteria>

<output>
Create `.planning/phases/09-ai-aced-review/09-02-SUMMARY.md` when done
</output>
