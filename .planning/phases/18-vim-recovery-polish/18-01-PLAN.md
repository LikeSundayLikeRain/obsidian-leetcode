---
phase: 18-vim-recovery-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/childEditorFactory.ts
  - src/main/childEditorVimScope.ts
  - tests/main/childEditorVimScope.test.ts
autonomous: false
requirements: [VIM-INTERACTION-01]

must_haves:
  truths:
    - "PROBE FIRST. Before any source code change, the operator runs the §3.2 DevTools probe from 18-RESEARCH.md against the live vault, presses j / k / dd / yy / i / Esc / a / o / s / x / p one keystroke at a time, and pastes the captured event table. The plan's auto-coding tasks DO NOT proceed until the probe output is in this conversation. (Anti-pattern #1 from .continue-here.md: 1738 unit tests didn't catch the previous regressions; live DOM probe is the only reliable signal.)"
    - "Mechanism is conditional on probe outcome. Two hypotheses get tested by the probe: (A) keys leak to parent even with focus correctly inside child — fix is a NARROW Obsidian Scope intercept (D-32) that gates by `getCM(view).state.vim.insertMode === false` and intercepts ONLY the empirically-leaking Normal-mode keys. (B) focus is correct and keys do NOT leak but the cursor isn't fully primed for input until a second keystroke — fix is a FOCUS-TRANSITION primer (no Scope at all) that ensures CM6's contentDOM has selection set when focus arrives. The probe distinguishes these; the fix-code task's <action> body has both branches and the operator's probe output picks one before coding."
    - "Insert-mode keystrokes (i / a / o / s / I / A / O / S / Esc / `:` / visual-mode) are NEVER intercepted, regardless of which hypothesis wins. They route through CM6's `vim()` extension's input handler unchanged. The previous Phase 18 attempt's regression — Esc not exiting Insert mode, `o` creating a blank uneditable line — was caused by intercepting these. This is non-negotiable."
    - "If the chosen mechanism is the Scope (Hypothesis A), it MUST mirror the shape of `createCmdSlashScopeExtension` at src/main/childEditorFactory.ts:153-198 verbatim — push on focus (line 159-176), pop on blur (line 178-182), pop in destroy (line 188-196). The Scope handler must read the child's vim mode via `getCM(childView)` from `@replit/codemirror-vim` (verified export at node_modules/@replit/codemirror-vim/dist/index.d.ts:1086) and short-circuit (return `true` = pass through) when `getCM(childView)?.state.vim?.insertMode === true`."
    - "If the chosen mechanism is the focus primer (Hypothesis B), no new Scope is registered. Implementation is a `view.contentDOM.addEventListener('focus', ...)` in `createChildEditor` that calls `view.dispatch({ selection: view.state.selection })` to re-affirm the selection and trigger CM6's contentDOM contenteditable priming. This is a SINGLE-LINE addition; no new module file is required for Hypothesis B (the file `src/main/childEditorVimScope.ts` becomes unused — operator deletes its placeholder if Hypothesis B wins)."
    - "DO NOT register `:set nu` / `:set nonu` aliases (Decision 3). The `Vim.defineEx('set', 'se', ...)` call is FORBIDDEN in this plan. Acceptance criteria below explicitly forbid the `defineEx` symbol appearing in any source file modified by this plan."
    - "Vim navigation keys (j, k, dd, yy, p, x in Normal mode) reach the child's vim instance and execute against the child's doc. The parent doc is unmodified by these keystrokes when focus is inside `.lc-nested-editor`. This is verified by a POST-CODE live UAT — operator returns to the vault, runs the §3.2 probe again, and confirms the leaking-keys set is now empty."
    - "Insert-mode entry verified: pressing `i` in Normal mode transitions to Insert mode AND the next typed character is inserted into the child editor on the first keystroke (NOT requiring a second i/a/o/s as the previous regression). Pressing Esc returns to Normal mode. Pressing `o` creates a new line below cursor and the cursor is immediately editable (NOT requiring a second `o`/`i`/`a`)."
    - "Bundle delta < 5 KB raw. The 1.8 MB ceiling (CONTEXT D-19) is preserved."
    - "All 1713 existing unit tests remain green. New unit test `tests/main/childEditorVimScope.test.ts` is created ONLY when Hypothesis A wins; the test asserts the Scope membership rules (which keys are registered, mode-gating logic). For Hypothesis B no new dedicated test file is needed — the focus-primer behavior is asserted in the existing `tests/main/childEditorFactory.test.ts`."
    - "Preserve: CLAUDE.md Conventions section unchanged (no new userEvent strings). Plan 17 D-05 canonical write-path pattern unchanged. `'leetcode.reset.child'` does NOT get added to ECHO_PRONE_USER_EVENTS at src/main/nestedEditorExtension.ts:273-276."
  artifacts:
    - path: "src/main/childEditorFactory.ts"
      provides: "Modified factory wiring — adds the chosen vim fix mechanism (Scope intercept extension if Hypothesis A; contentDOM focus listener if Hypothesis B) into the extension array returned by `createChildEditor`."
      contains: "createChildEditorVimScope|leetcode-vim-focus-primer|createChildEditor"
    - path: "src/main/childEditorVimScope.ts"
      provides: "NEW module exporting the Scope-based intercept extension (Hypothesis A only). Mirrors `createCmdSlashScopeExtension` shape — push Scope on focus, pop on blur, pop on destroy. Gates intercept by `getCM(view).state.vim.insertMode`. Intercepts ONLY the empirically-leaking Normal-mode keys from probe output. If Hypothesis B wins, this file is NOT created."
      contains: "createChildEditorVimScope|getCM|app\\.keymap\\.pushScope"
      min_lines: 60
    - path: "tests/main/childEditorVimScope.test.ts"
      provides: "NEW unit test file (Hypothesis A only). Asserts: (1) Scope is registered with the narrow leaking-keys set from the probe, NOT i/a/o/s/Esc/`:`; (2) Scope handler short-circuits when vim is in Insert mode (mocked `getCM` returns `{ state: { vim: { insertMode: true } } }`); (3) Scope is popped on blur; (4) Scope is popped on destroy."
      contains: "childEditorVimScope|insertMode|describe.*Scope"
  key_links:
    - from: "src/main/childEditorFactory.ts createChildEditor"
      to: "src/main/childEditorVimScope.ts createChildEditorVimScope (or factory-internal focus primer)"
      via: "extension array entry near the vim() registration at line 297-299"
      pattern: "createChildEditorVimScope|focus.*primer|app\\.keymap"
    - from: "src/main/childEditorVimScope.ts (Hypothesis A only)"
      to: "@replit/codemirror-vim getCM"
      via: "import { getCM } from '@replit/codemirror-vim'"
      pattern: "getCM\\("
---

<objective>
Fix the bug where vim navigation keys (j / k / dd / yy / p / x) leak from the focused child editor to the parent editor when Obsidian's global vim mode is ON. The fix mechanism is conditional on a live DevTools probe — Hypothesis A (Scope intercept on app.keymap with narrow Normal-mode-only key set) or Hypothesis B (focus-transition primer). Insert-mode keystrokes (i / a / o / s / Esc / `:`) are NEVER intercepted; the previous over-broad Scope attempt that swallowed Esc + Insert-mode entry is forbidden by acceptance criteria.

Purpose: Closes UAT Test 17 (VIM-01) for v1.2 ship gate. Restores expected vim modal-state semantics — keystrokes execute against the focused vim instance, not the parent's.

Output: Modified `src/main/childEditorFactory.ts`; new `src/main/childEditorVimScope.ts` (Hypothesis A only); new `tests/main/childEditorVimScope.test.ts` (Hypothesis A only); 1713+ tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-vim-recovery-polish/CONTEXT.md
@.planning/phases/18-vim-recovery-polish/18-RESEARCH.md
@.planning/phases/18-vim-recovery-polish/.continue-here.md
@.planning/debug/phase-18-regressions-2026-05-25.md
@CLAUDE.md
@src/main/childEditorFactory.ts
@src/main/nestedEditorExtension.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase. -->
<!-- Use these directly — no codebase exploration needed. -->

From src/main/childEditorFactory.ts:153-198 (createCmdSlashScopeExtension — the canonical Scope reference):
```typescript
function createCmdSlashScopeExtension(app: App): Extension {
  return ViewPlugin.define((view): PluginValue => {
    let activeScope: Scope | null = null;
    const onFocus = (): void => {
      if (activeScope !== null) return;
      const { Scope } = require('obsidian') as typeof import('obsidian');
      const scope = new Scope(app.scope);
      scope.register(['Mod'], '/', (event) => { /* ... */ });
      app.keymap.pushScope(scope);
      activeScope = scope;
    };
    const onBlur = (): void => {
      if (activeScope === null) return;
      app.keymap.popScope(activeScope);
      activeScope = null;
    };
    view.contentDOM.addEventListener('focus', onFocus);
    view.contentDOM.addEventListener('blur', onBlur);
    if (document.activeElement === view.contentDOM) onFocus();
    return {
      destroy(): void {
        view.contentDOM.removeEventListener('focus', onFocus);
        view.contentDOM.removeEventListener('blur', onBlur);
        if (activeScope !== null) {
          app.keymap.popScope(activeScope);
          activeScope = null;
        }
      },
    };
  });
}
```

From src/main/childEditorFactory.ts:229-250 (createChildEditor signature + vim mount):
```typescript
export function createChildEditor(
  content: string,
  parent: HTMLElement,
  initialSlug: string,
  indentOverride: 'auto' | 2 | 4 | 8,
  app?: App,
  syncExtensions?: Extension[],
): EditorView {
  const vimEnabled =
    !!app &&
    (app as unknown as { vault: { getConfig(key: string): unknown } }).vault.getConfig('vimMode') === true;
  // ... line 297-299: extension array entry:
  //   ...(vimEnabled ? [vim({ status: true } as Parameters<typeof vim>[0])] : []),
}
```

From node_modules/@replit/codemirror-vim/dist/index.d.ts:1083-1088:
```typescript
export declare function vim(options?: { status?: boolean }): Extension;
export declare function getCM(view: EditorView): CodeMirror | null;
// CodeMirror.state.vim is the modal state — { insertMode: boolean, visualMode: boolean, ... }
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live DevTools probe — capture vim leaking-keys ground truth</name>
  <read_first>
    - .planning/phases/18-vim-recovery-polish/18-RESEARCH.md (§3.2 — the probe snippet to paste)
    - .planning/debug/phase-18-regressions-2026-05-25.md (the failure modes to verify)
    - ~/.claude/projects/-Users-moxu-projects-obsidian-leetcode/memory/vault_plugin_path.md (vault path is `obsidian-leetcode/` not `leetcode/`)
  </read_first>
  <what-built>NOT YET — this checkpoint runs BEFORE any source code change. The operator captures live DevTools probe data from their vault on the Phase 17 round-3 baseline (HEAD `692ed26`, vim regression bugs reproducible).</what-built>
  <how-to-verify>
    1. Verify the dev vault build is the Phase 17 round-3 baseline: `git log --oneline | head -3` shows `692ed26` near HEAD (or merge from gsd/v1.2-code-editor-experience).
    2. Verify Obsidian's global vim mode is ON: in Obsidian, Editor → Vim key bindings is checked.
    3. Open a LeetCode problem note (any one — Java is fine for this probe).
    4. Open Obsidian DevTools (Cmd-Opt-I on macOS / Ctrl-Shift-I elsewhere) → Console tab.
    5. Paste the §3.2 probe snippet from 18-RESEARCH.md verbatim and press Enter. Console should print "Probe installed."
    6. Click into the child editor (the `.lc-nested-editor` block). The status panel at the bottom of the child should show `--NORMAL--`.
    7. Press these keys ONE AT A TIME, slowly (wait 200ms between each): `j`, `k`, `dd` (two `d`s), `yy` (two `y`s), `i`, `Esc`, `a`, `Esc`, `o`, `Esc`, `s`, `Esc`, `x`, `p`.
    8. Now run `lcVimProbeStop()` in the console. The captured event table is printed.
    9. Copy the FULL `console.table` output AND the "Active scopes" line back into this conversation.
    10. Also capture the symptom for each Insert-mode entry key: did `i` then a typed character (e.g. `Z`) insert `Z` on the FIRST attempt, or did the typing not register until you pressed `i` again? Did `o` create a blank line that you could immediately type into, or was the blank line uneditable until you pressed `i` afterward? Record these as YES/NO for each of i / a / o / s.
  </how-to-verify>
  <resume-signal>
    Paste:
    (a) the full console.table output from `lcVimProbeStop()`,
    (b) the "Active scopes" log line,
    (c) for each of `i`, `a`, `o`, `s`: did the editor accept the next typed character on the first attempt? (yes/no per key).

    Then type "approved — proceed with Hypothesis A" (Scope intercept) OR "approved — proceed with Hypothesis B" (focus primer) based on the probe output. Decision rule:
    - If the table shows keys with `inChild: true` THAT STILL MOVED THE PARENT CURSOR (verifiable by parent gutter or doc state), Hypothesis A wins — coding the Scope intercept.
    - If `inChild: true` rows show the keystroke landing in child but the symptoms are limited to "first keystroke after focus is dropped, second works", Hypothesis B wins — coding the focus primer.
    - If unclear, type "ambiguous — pause" and Claude returns the table to the user for interpretation before locking the mechanism.
  </resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement chosen fix mechanism + unit test</name>
  <files>src/main/childEditorFactory.ts, src/main/childEditorVimScope.ts (Hypothesis A only), tests/main/childEditorVimScope.test.ts (Hypothesis A only)</files>
  <read_first>
    - src/main/childEditorFactory.ts (entire file — lines 153-198 are the canonical Scope reference; lines 229-310 are the createChildEditor body where the new extension is wired in)
    - node_modules/@replit/codemirror-vim/dist/index.d.ts (lines 1083-1088 for `vim()` and `getCM()` exports; verify the package's modal-state shape)
    - tests/main/childEditorFactory.test.ts (existing test file — patterns for mocking `app.vault.getConfig` and constructing a CM6 EditorView in tests)
    - .planning/phases/18-vim-recovery-polish/18-RESEARCH.md (§4.1 — mechanism comparison table; §3.2 probe interpretation)
    - The pasted probe output from Task 1 (in conversation context — defines the exact key set to intercept under Hypothesis A, OR confirms Hypothesis B)
  </read_first>
  <behavior>
    Hypothesis A (Scope intercept) — when the user-confirmed branch:
    - Test 1 (Scope membership): when `createChildEditorVimScope(app)` is registered and child gains focus, `app.keymap.pushScope` is called with a Scope whose registered key bindings are EXACTLY the leaking-keys set from the probe (typically a subset of {`j`, `k`, `h`, `l`, `d`, `y`, `p`, `x`, `w`, `b`, `e`, `0`, `$`, `g`, `G`} — the operator-confirmed set goes here).
    - Test 2 (Insert mode passthrough): when the Scope handler is invoked AND `getCM(childView).state.vim.insertMode === true`, the handler returns `true` (pass through) — vim's input handler receives the keystroke unchanged. Mock `getCM` to return `{ state: { vim: { insertMode: true } } }`.
    - Test 3 (Normal mode intercept): when `getCM(childView).state.vim.insertMode === false`, the handler routes the keystroke to the child's vim by dispatching against `childView` (the implementation may call `Vim.handleKey(getCM(childView), key)` if exposed; otherwise dispatch a CM6 command from `@replit/codemirror-vim`'s commands map). The handler returns `false` (consumed).
    - Test 4 (lifecycle): on blur, `app.keymap.popScope` is called; on destroy, popScope is also called if the scope is still active.

    Hypothesis B (focus primer) — when the user-confirmed branch:
    - Test 1 (focus dispatch): when child's contentDOM receives focus, the factory's added focus listener calls `view.dispatch({ selection: view.state.selection })` — re-affirming selection. Asserted via spy on `view.dispatch`.
    - Test 2 (no Scope side effect): no `app.keymap.pushScope` calls are made by this primer (distinguishes from Hypothesis A; verified via mock spy on app.keymap).
    - Test 3 (lifecycle): on view destroy, the focus listener is removed (no leak).
  </behavior>
  <action>
    BRANCH ON HYPOTHESIS RESULT FROM TASK 1.

    **If Hypothesis A (Scope intercept) — operator confirmed leaking-keys set in conversation:**

    1. Create `src/main/childEditorVimScope.ts` mirroring `createCmdSlashScopeExtension` at `src/main/childEditorFactory.ts:153-198`:
       - Imports: `import { ViewPlugin, type PluginValue, type EditorView } from '@codemirror/view'; import type { Extension } from '@codemirror/state'; import type { App, Scope } from 'obsidian'; import { getCM } from '@replit/codemirror-vim';`
       - Export signature: `export function createChildEditorVimScope(app: App): Extension`
       - Inside, return `ViewPlugin.define((view): PluginValue => { ... })`. Track `activeScope: Scope | null = null`.
       - `onFocus`: same as the Cmd-/ Scope at line 159-176 — `const { Scope } = require('obsidian')`; `const scope = new Scope(app.scope)`. Register ONLY the leaking keys reported by the probe (e.g., if probe shows `j`, `k`, `dd`, `yy`, `x`, `p` leaked, register only those). For each key, the handler reads `getCM(view)?.state.vim?.insertMode` — when truthy, return `true` (pass through). When falsy (Normal mode), dispatch against the child's vim instance: prefer `Vim.handleKey(getCM(view), event.key)` if `Vim` is exported from `@replit/codemirror-vim`; if not, dispatch the equivalent CodeMirror command from the package's exported commands map. Return `false` to consume the event.
       - `onBlur` and destroy: identical shape to lines 178-196 of createCmdSlashScopeExtension.
       - Add focus/blur listeners on `view.contentDOM`; check `document.activeElement === view.contentDOM` to handle the already-focused-at-mount case.
    2. Wire into `createChildEditor` at `src/main/childEditorFactory.ts`. After the `vim({ status: true })` extension at line 299 (which is the existing vim mount point), add `...(vimEnabled && app ? [createChildEditorVimScope(app)] : [])` so the Scope is only registered when vim is enabled AND `app` is defined. Import `createChildEditorVimScope` at the top of the file.
    3. Create `tests/main/childEditorVimScope.test.ts` modeled on `tests/main/childEditorFactory.test.ts`. Use vitest. Mock `app.keymap.pushScope` and `popScope` as `vi.fn()`. Mock `getCM` from `@replit/codemirror-vim` via `vi.mock('@replit/codemirror-vim')` returning a controllable insertMode value. Cover the four behavior assertions above.
    4. **EXPLICIT FORBIDDEN SYMBOLS — verify absent in modified files:** `Vim.defineEx`, `'set'`, `'se'`, `'nu'`, `'nonu'`, `'number'` (as `Vim.defineEx` arguments). Decision 3 forbids these.
    5. **EXPLICIT FORBIDDEN KEYS in scope.register calls** — these MUST NOT appear: `'i'`, `'a'`, `'o'`, `'s'`, `'I'`, `'A'`, `'O'`, `'S'`, `'Escape'`, `'Esc'`, `':'`. The Scope must NOT intercept Insert-mode entry keys, Esc, or the Ex command-line trigger. Verify by `grep -E "scope\\.register.*(['\\\"]i['\\\"]|['\\\"]a['\\\"]|['\\\"]o['\\\"]|['\\\"]s['\\\"]|Escape)" src/main/childEditorVimScope.ts` returns NO matches in scope.register lines.

    **If Hypothesis B (focus primer) — probe shows keys do not actually leak:**

    1. Do NOT create `src/main/childEditorVimScope.ts`. Do NOT create `tests/main/childEditorVimScope.test.ts`.
    2. In `src/main/childEditorFactory.ts:createChildEditor`, after the EditorView is constructed and BEFORE the function returns, add:
       ```
       view.contentDOM.addEventListener('focus', () => {
         // Hypothesis B fix: re-affirm selection on focus to prime contenteditable
         // for the first keystroke. Without this, the first key after focus is
         // dropped because CM6's contentDOM hasn't fully primed its caret state.
         view.dispatch({ selection: view.state.selection });
       });
       ```
       (Action is directive prose; the executor writes the actual code per this directive.)
    3. Add focus-primer test in the existing `tests/main/childEditorFactory.test.ts` — assert that focusing the contentDOM triggers a `view.dispatch` call with a `selection` payload. Use the existing JSDOM patterns from that file.
    4. The new module file path `src/main/childEditorVimScope.ts` and test file `tests/main/childEditorVimScope.test.ts` are NOT created. The frontmatter `files_modified` includes both speculatively; under Hypothesis B those files simply don't exist post-commit (acceptable per the executor — frontmatter lists files that MAY be modified, not all that MUST be).

    **Both hypotheses:**
    - Run `npx vitest run --reporter=dot` after the change. Existing 1713 tests must remain green; the new test (if any) must pass.
    - Run `npm run build`. Bundle size must stay < 1.8 MB raw (CONTEXT D-19). Capture pre-Phase-18 baseline size from a `git stash` build for delta comparison.
    - DO NOT add `'leetcode.reset.child'` to ECHO_PRONE_USER_EVENTS at src/main/nestedEditorExtension.ts:273-276.
  </action>
  <verify>
    <automated>npx vitest run --reporter=dot && npm run build && (raw=$(wc -c < main.js); test "$raw" -lt 1800000) && echo "BUNDLE OK ($raw bytes &lt; 1.8 MB)" || echo "BUNDLE EXCEEDED" && grep -E "scope\\.register.*['\\\"](\\bi\\b|\\ba\\b|\\bo\\b|\\bs\\b|Escape|Esc)['\\\"]" src/main/childEditorVimScope.ts 2>/dev/null && echo "FORBIDDEN INSERT KEY INTERCEPTED" || echo "INSERT KEYS NOT INTERCEPTED OK" && grep -E "(Vim\\.defineEx|defineEx)" src/main/childEditorVimScope.ts src/main/childEditorFactory.ts 2>/dev/null && echo "FORBIDDEN defineEx PRESENT" || echo "NO defineEx OK"</automated>
  </verify>
  <acceptance_criteria>
    - **Behavior**: If Hypothesis A — child's vim Normal-mode keys (the ones from the probe) execute against child's doc; parent doc unchanged when focus is in child; vim Insert mode entry (i/a/o/s) and Esc work without regression. If Hypothesis B — first keystroke after focus drops zero characters; subsequent keystrokes flow normally.
    - **Test command**: `npx vitest run --reporter=dot` exits 0 with 1713+ green tests (Hypothesis A: 1714+ if new test file added; Hypothesis B: 1713+ existing remain green).
    - **CLI**: `npm run build` exits 0; `wc -c < main.js` shows raw bytes < 1,800,000.
    - **Source**: `grep -E "scope\\.register.*['\\\"](\\\\bi\\\\b|\\\\ba\\\\b|\\\\bo\\\\b|\\\\bs\\\\b|Escape|Esc)['\\\"]" src/main/childEditorVimScope.ts` returns NO matches in scope.register call lines (Hypothesis A only — Insert-mode entry keys + Esc are NOT registered in the Scope).
    - **Source**: `grep -E "(Vim\\.defineEx|defineEx)" src/main/childEditorVimScope.ts src/main/childEditorFactory.ts` returns NO matches (Decision 3: `:set nu` / `:set nonu` aliases NOT implemented).
    - **Source**: `grep -n "ECHO_PRONE_USER_EVENTS" src/main/nestedEditorExtension.ts | head -3` shows the existing set is unchanged — `'leetcode.child-sync'` and `'leetcode.fence-repair'` ONLY (no `'leetcode.reset.child'` added).
    - **Source**: For Hypothesis A — `grep -n "createChildEditorVimScope" src/main/childEditorFactory.ts` returns at least 2 matches (the import + the conditional spread inside the extension array).
  </acceptance_criteria>
  <done>Source change committed. Test suite green. Bundle under ceiling. No forbidden symbols. Ready for live UAT verification (Task 3).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live UAT — verify vim fix in operator's vault</name>
  <read_first>
    - .planning/phases/17-polish-edge-cases/17-UAT.md (Test 17 VIM-01 — the specific reproduction steps the operator runs)
    - The Task 2 commit summary (in conversation context — what mechanism was chosen)
    - ~/.claude/projects/-Users-moxu-projects-obsidian-leetcode/memory/vault_plugin_path.md (deploy targets `obsidian-leetcode/`)
  </read_first>
  <what-built>The vim fix from Task 2 is committed and built. The plugin needs to be deployed to the dev vault (`~/Documents/Obsidian Vault/.obsidian/plugins/obsidian-leetcode/`) and verified live by the operator.</what-built>
  <how-to-verify>
    1. Deploy: copy `main.js`, `manifest.json`, `styles.css` to `~/Documents/Obsidian Vault/.obsidian/plugins/obsidian-leetcode/`. Verify the path with `app.plugins.manifests.leetcode.dir` in Obsidian's DevTools console.
    2. Reload Obsidian (Cmd-R or Cmd-P → "Reload app without saving").
    3. Open a LeetCode problem note. Click into the child editor — status panel = `--NORMAL--`.
    4. **Reproduce the bug from 17-UAT.md Test 17 (VIM-01)**: Press `j` 5 times. The cursor must move DOWN inside the child editor (visible by line highlight inside `.lc-nested-editor`), NOT in the parent doc. The parent doc must be UNCHANGED (its visible content is the markdown around the fence — frontmatter, `## Problem`, `## Notes`).
    5. **Reproduce more vim motions**: press `k` 5 times (cursor moves up in child); press `dd` (a child line is deleted, parent unchanged); press `yy` then `p` (a child line is duplicated below); press `x` (a child character is deleted).
    6. **Test Insert-mode entry**: in Normal mode, press `i`. Status panel = `--INSERT--`. Type a single character, e.g. `Z`. The `Z` MUST appear in the child editor on the FIRST attempt (NOT requiring you to press `i` a second time). Press `Esc` — status panel returns to `--NORMAL--`.
    7. **Test `o` / `a` / `s`**: from Normal mode press `a` and type `Q` — `Q` appears immediately. Press Esc, press `o` and type `R` — a new line is created and `R` is inserted on the first attempt. Press Esc, press `s` and type `T` — character at cursor is deleted and `T` inserted on first attempt.
    8. **Test `:` Ex command** (no longer absorbed by Scope): from Normal mode press `:`. The vim status panel command-line should appear. Type `set number` (verify `:set nu` is NOT supported per Decision 3 — typing `set nu` should produce vim's standard "unknown option" error). Press Esc to exit command-line.
    9. **Re-run the §3.2 DevTools probe** from 18-RESEARCH.md — paste the snippet, press the same keys (`j`, `k`, `dd`, `i`, `Esc`, `o`, `a`, `s`, `x`, `p`). Run `lcVimProbeStop()`. The captured table must show: every keystroke has `inChild: true`; for Normal-mode-leaking keys (j/k/dd/yy/x/p) Hypothesis A's Scope handler ran (verifiable by absence of parent-cursor movement); for Insert-mode keys vim's modal state correctly flipped between INSERT and NORMAL.
  </how-to-verify>
  <resume-signal>
    Paste:
    (a) for each of the 6 listed reproduction steps (4–9), specific YES/NO verdict + any observed regression notes;
    (b) the captured §3.2 probe table from step 9.

    Then type "approved" if every step passes, OR describe the failure mode for follow-up. Decision 3 enforcement: typing `:set nu` MUST produce vim's standard error — if it works, that's a Decision 3 violation and the plan FAILS at this checkpoint.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keystroke → Obsidian event pipeline | OS-level keydown reaches DOM, then Obsidian's keymap, then CM6 |
| Obsidian app.keymap → child CM6 EditorView | Hypothesis A's Scope intercepts BEFORE CM6 sees the keystroke |
| child CM6 → parent CM6 | child→parent sync extension (existing); not modified by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | Scope intercept registered with too many keys | mitigate | Acceptance criteria forbid registering i/a/o/s/Esc/`:` in scope.register; grep gate verifies. |
| T-18-02 | Denial-of-service | Scope swallows Esc → user trapped in Insert mode | mitigate | Forbidden-keys grep gate explicitly checks Esc is NOT registered; live UAT step 6 verifies Esc returns to Normal mode. |
| T-18-03 | Information disclosure | None | accept | No PII or credentials touched by this plan; vim is a local-only modal-state shim. |
| T-18-04 | Repudiation | None | accept | No audit trail required for keymap routing. |
</threat_model>

<verification>
- `npx vitest run --reporter=dot` — 1713+ tests green
- `npm run build` — exits 0; `main.js` < 1.8 MB raw (CONTEXT D-19)
- Operator-pasted DevTools probe output (from Task 1 BEFORE coding, Task 3 AFTER coding)
- Operator-confirmed live UAT for Test 17 (VIM-01) — all reproduction steps pass
- `grep -nE "(Vim\\.defineEx|scope\\.register.*['\\\"](Escape|Esc|\\\\bi\\\\b|\\\\bo\\\\b))" src/main/childEditorVimScope.ts src/main/childEditorFactory.ts` returns no matches
- `grep -n "ECHO_PRONE_USER_EVENTS" src/main/nestedEditorExtension.ts | head -3` confirms unchanged set
</verification>

<success_criteria>
- VIM-INTERACTION-01 satisfied: vim Normal-mode keys (j/k/dd/yy/x/p) execute in the focused child editor; parent doc unchanged when focus is in child
- Insert-mode entry (i/a/o/s) accepts the next typed character on the FIRST attempt
- Esc returns Insert mode to Normal mode (no swallow)
- Decision 3 enforced: no `Vim.defineEx`; `:set nu` is NOT a recognized alias
- Decision 4 partial: bundle under 1.8 MB; CLAUDE.md unchanged
- All 1713 existing unit tests green; new test (if Hypothesis A) green
- 17-UAT.md Test 17 (VIM-01) confirmed PASS by live operator UAT
</success_criteria>

<output>
Create `.planning/phases/18-vim-recovery-polish/18-01-SUMMARY.md` when done.
</output>
</content>
</invoke>