---
phase: 18-vim-recovery-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/childEditorVimScope.ts
  - src/main/childEditorFactory.ts
  - tests/main/childEditorVimScope.test.ts
autonomous: true
requirements: [VIM-INTERACTION-01]
requirements_addressed: [VIM-INTERACTION-01]

gap_closure: false

must_haves:
  truths:
    - "When Obsidian's global vim mode is enabled AND the user has clicked into the child editor (`document.activeElement` is inside `.lc-nested-editor`), the vim navigation/edit keys h/j/k/l/d/y/p/o/i/a/x/w/b/e/u/Ctrl-r/Esc MUST execute against the child's vim instance, not the parent's. Concretely: from Insert mode press Esc → child enters Normal mode (block cursor); press j → child cursor moves DOWN one line in the child doc; press dd → the line UNDER the child cursor in the child doc is deleted (NOT a line in the parent doc). The parent doc's cursor MUST NOT move and the parent doc's content MUST NOT change as a side effect of any vim navigation/edit key while the child has focus."
    - "The Scope-based intercept MUST be installed on focus and removed on blur — when the child blurs (e.g., user clicks into `## Notes`), the Scope is popped off `app.keymap` so vim keys typed against the parent's editor (or any other CM6 view) route normally to whichever Obsidian/CM6 handler would receive them in the no-child case."
    - "The implementation MUST mirror the `createCmdSlashScopeExtension` pattern at src/main/childEditorFactory.ts:153-199 (D-32): a `ViewPlugin.define((view) => ...)` returns a PluginValue with `destroy()`; a `Scope` constructed with `app.scope` as parent is pushed via `app.keymap.pushScope(scope)` on contentDOM 'focus' and popped via `app.keymap.popScope(scope)` on contentDOM 'blur'; the `if (document.activeElement === view.contentDOM) onFocus()` self-priming line is preserved; on `destroy()` listeners are removed and any `activeScope` is popped. The implementation MUST NOT use a DOM-level keydown listener (capture or bubble) — Obsidian's app-level vim handler runs in the Scope-managed pipeline and a Scope-based intercept is the only path that reliably wins (per D-32)."
    - "Inside the registered Scope, the vim keys MUST be registered such that returning `false` from each handler stops Obsidian's app-level dispatch AND routes the keystroke into the child's vim instance via `@replit/codemirror-vim`'s public API (e.g., `Vim.handleKey(view.cm, keyName, 'editor')` or by dispatching the keystroke through the child view's contentDOM via a synthetic KeyboardEvent that traverses CM6's local keymap — the implementation chooses whichever shape produces a deterministic single-keystroke effect on the child)."
    - "Bonus: vim ex-command aliases `:set nu` and `:set nonu` MUST be registered via `Vim.defineEx('set', 'se', handler)` (or equivalent `defineOption`/`mapCommand` API) so the abbreviated forms parse and toggle the gutter exactly like the long forms `:set number` / `:set nonumber` already do. The handler MUST live in `src/main/childEditorVimScope.ts` (not in `childEditorFactory.ts`) — keep `childEditorFactory.ts` legible. Registration MUST happen exactly once per child mount (idempotent — repeated mounts MUST NOT throw on the second `defineEx` call; guard with a module-scoped `aliasesRegistered` flag or wrap the call in try/catch matching the project's defensive convention)."
    - "Wiring into `createChildEditor` MUST be a single-line addition next to `createCmdSlashScopeExtension` (factory.ts:315): `...(app && vimEnabled ? [createVimScopeExtension(app, view => view.cm)] : [])` (or similar — exact arity matches the helper's exported signature). The conditional MUST be gated on `vimEnabled` so plain (non-vim) child editors NEVER push a Scope onto `app.keymap`."
    - "Tests in `tests/main/childEditorVimScope.test.ts` MUST cover: (a) source-level assertions (mirroring the existing `cmd-slash-not-reaching-child regression` describe block at childEditorFactory.test.ts:394-434) — the helper imports `Scope` from 'obsidian', calls `app.keymap.pushScope` on focus, `app.keymap.popScope` on blur, registers at least one of the locked vim keys (j/k/d/o/i etc.) inside the scope, and cleans up on `destroy()`; (b) behavioral assertion — invoking the registered handler for 'j' returns `false` (stops Obsidian dispatch) AND triggers a child-targeted side effect (e.g., a mock `Vim.handleKey` is called with the child's CodeMirror view); (c) idempotency — invoking the factory twice in the same module reuses or no-ops the `defineEx` registration."
    - "Preserve: section lock changeFilter at src/main/sectionLockExtension.ts is unchanged; sync annotations (`syncAnnotation`) are unchanged; Plan 17-13 parent-side updateListener-based repair (`createParentRepairExtension`) is unchanged; Plan 17-09 per-child language tracker (`childLanguageTracker` WeakMap) is unchanged; Plan 17-10 round-3 semantic class layer (`obsidianSemanticClasses`) is unchanged; CLAUDE.md `## Conventions` section is unchanged; `'leetcode.reset.child'` is NOT added to `ECHO_PRONE_USER_EVENTS` in `src/main/nestedEditorExtension.ts:265-268`; bundle stays under the 1.8 MB ceiling (CONTEXT D-19)."
  artifacts:
    - path: "src/main/childEditorVimScope.ts"
      provides: "Vim Scope intercept module — exports `createVimScopeExtension(app, getCm)` returning a CM6 Extension that mirrors the `createCmdSlashScopeExtension` shape (ViewPlugin.define + push/pop Scope on focus/blur). Internally registers the vim navigation/edit keys against the child's vim instance and registers `:set nu` / `:set nonu` ex-aliases via `Vim.defineEx`."
      contains: "createVimScopeExtension|ViewPlugin\\.define|app\\.keymap\\.pushScope|app\\.keymap\\.popScope|Vim\\.defineEx|scope\\.register"
      min_lines: 80
    - path: "src/main/childEditorFactory.ts"
      provides: "Single-line wiring of `createVimScopeExtension(app, ...)` into `createChildEditor`'s extensions array, gated by `vimEnabled` AND `app`. Placed adjacent to the existing `createCmdSlashScopeExtension(app)` spread (line ~315)."
      contains: "createVimScopeExtension"
    - path: "tests/main/childEditorVimScope.test.ts"
      provides: "Source-level + behavioral test suite for the new module. At least 6 assertions: imports from 'obsidian' include Scope; ViewPlugin.define is used; push/pop on focus/blur; at least one vim navigation key is registered inside the Scope; behavioral test that the 'j' handler returns false and dispatches the keystroke at the child's CM view; Vim.defineEx is called for the 'set' / 'se' alias."
      contains: "createVimScopeExtension|pushScope|popScope|defineEx"
      min_lines: 60
  key_links:
    - from: "src/main/childEditorFactory.ts:createChildEditor"
      to: "src/main/childEditorVimScope.ts:createVimScopeExtension"
      via: "single-line conditional spread `...(app && vimEnabled ? [createVimScopeExtension(app, ...)] : [])` next to createCmdSlashScopeExtension at ~line 315"
      pattern: "createVimScopeExtension"
    - from: "src/main/childEditorVimScope.ts onFocus handler"
      to: "Obsidian's app.keymap dispatch pipeline"
      via: "app.keymap.pushScope(new Scope(app.scope)) — same shape as createCmdSlashScopeExtension at factory.ts:165-174"
      pattern: "pushScope|popScope"
    - from: "src/main/childEditorVimScope.ts vim key handlers"
      to: "@replit/codemirror-vim child instance"
      via: "Vim.handleKey(getCm(), keyName, 'editor') OR equivalent public API on the child's CM view"
      pattern: "Vim\\.|handleKey"
  pitfalls:
    - "DO NOT use a DOM-level keydown listener (capture-phase on window/document/contentDOM) — D-32 explicitly rules this out because Obsidian's app-level vim handler runs in the Scope-managed pipeline, NOT in the DOM event flow."
    - "DO NOT push the Scope unconditionally at mount — push must be on focus, pop on blur. Otherwise the Scope leaks vim keys away from other CM6 instances when the user clicks elsewhere."
    - "DO NOT assume `Vim.defineEx` is idempotent across module evaluations — wrap in try/catch or guard with a module-scoped flag so a second mount in the same plugin lifetime does not throw."
    - "DO NOT add 'leetcode.reset.child' or any other userEvent to ECHO_PRONE_USER_EVENTS — CLAUDE.md Conventions warning carry-over from Phase 17 D-05."
    - "DO NOT introduce a new userEvent string — the Scope-based intercept does not dispatch any CM6 transactions on the parent, so no `'leetcode.*'` annotation is needed. If routing a key into the child's CM6 dispatch path requires an annotation (e.g., for transactions that touch a locked section — unlikely for navigation keys), use an existing `'leetcode.*'` annotation rather than coining a new one."
    - "Mirror `createCmdSlashScopeExtension` PRECISELY — the existing source-level test suite at childEditorFactory.test.ts:394-434 codifies the expected shape; deviating from it will require updating those tests, which is out of scope."
---

<objective>
Close UAT Test 17 / VIM-01 partial finding (backlog 999.2): when Obsidian global vim mode is enabled and the user has clicked into the child editor inside `## Code`, vim navigation/edit keys (j, k, dd, o) intermittently leak to the parent editor — `document.activeElement` is correctly inside `.lc-nested-editor` but the keystroke routes through Obsidian's app-level vim handler to the parent's vim instance. Per D-32, the only reliable fix is to mirror the existing `createCmdSlashScopeExtension` pattern from `src/main/childEditorFactory.ts:153-199` — an Obsidian `Scope` pushed on `app.keymap` when the child gains focus and popped on blur, with the vim navigation/edit keys registered inside the Scope so each handler returns `false` to stop Obsidian's app-level dispatch and instead routes the keystroke into the child's `@replit/codemirror-vim` instance.

Bonus: register `:set nu` / `:set nonu` ex-aliases via `Vim.defineEx('set', 'se', handler)` so users typing the abbreviated forms (which `@replit/codemirror-vim` rejects out of the box with "unknown option: nu") get the same gutter toggle they get from the full `:set number` / `:set nonumber`.

Purpose: VIM-01 is a v1.2 ship blocker — vim users currently see their dd command delete a line in the parent doc instead of the child code body, which corrupts the note. Phase 17 Plan 17-11 added the visible mode panel and cursor visibility (D-18 follow-on); this plan closes the focus-routing gap that was deferred to backlog 999.2 because the fix shape (Scope-based intercept, mirroring the cmd-slash precedent) was clear but exceeded Plan 17-11's scope.

Output: a new `src/main/childEditorVimScope.ts` module owning the Scope-based intercept logic, a single-line wiring change in `src/main/childEditorFactory.ts` next to the existing `createCmdSlashScopeExtension(app)` spread, and a new test file `tests/main/childEditorVimScope.test.ts` with source-level and behavioral assertions mirroring the `cmd-slash-not-reaching-child regression` test pattern at `tests/main/childEditorFactory.test.ts:394-434`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-vim-recovery-polish/CONTEXT.md
@.planning/phases/17-polish-edge-cases/17-UAT.md
@.planning/phases/17-polish-edge-cases/17-11-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Code surfaces the executor will read or modify. The cmd-slash precedent is the canonical template. -->

src/main/childEditorFactory.ts:153-199 createCmdSlashScopeExtension (THE TEMPLATE — D-32):
```typescript
function createCmdSlashScopeExtension(app: App): Extension {
  return ViewPlugin.define((view): PluginValue => {
    let activeScope: Scope | null = null;
    const runComment = toggleLineComment as unknown as (v: EditorView) => boolean;

    const onFocus = (): void => {
      if (activeScope !== null) return;
      const { Scope } = require('obsidian') as typeof import('obsidian');
      const scope = new Scope(app.scope);
      scope.register(['Mod'], '/', (event) => {
        if (!isMod(event as KeyboardEvent)) return true;
        runComment(view);
        return false;
      });
      app.keymap.pushScope(scope);
      activeScope = scope;
    };

    const onBlur = (): void => {
      if (activeScope === null) return;
      app.keymap.popScope(activeScope);
      activeScope = null;
    };

    view.contentDOM.addEventListener('focus', onFocus);
    view.contentDOM.addEventListener('blur', onBlur);
    if (document.activeElement === view.contentDOM) onFocus();

    return {
      destroy(): void {
        view.contentDOM.removeEventListener('focus', onFocus);
        view.contentDOM.removeEventListener('blur', onBlur);
        if (activeScope !== null) {
          app.keymap.popScope(activeScope);
          activeScope = null;
        }
      },
    };
  });
}
```

src/main/childEditorFactory.ts:299 vim() spread (the conditional gate):
  // `vimEnabled` is computed at line 246-250 from `app.vault.getConfig('vimMode')`.
  // The new vim Scope extension's gate MUST be the SAME `vimEnabled` flag.

src/main/childEditorFactory.ts:315 createCmdSlashScopeExtension(app) callsite (PLACEMENT):
  ...(app ? [createCmdSlashScopeExtension(app)] : []),
  // The new vim scope extension lands ON THE NEXT LINE, gated on app && vimEnabled.

@replit/codemirror-vim public API (the package already shipped in Phase 17):
  // `vim()` returns the CM6 extension that mounts vim; the runtime instance
  // is reachable via the package's `Vim` namespace export. `Vim.defineEx` and
  // `Vim.handleKey` are the documented hooks for ex-aliases and synthetic
  // keystrokes respectively. Cast through `as unknown as ...` to bypass the
  // package's narrow .d.ts where needed (precedent: factory.ts:299 vim() cast).

tests/main/childEditorFactory.test.ts:394-434 cmd-slash-not-reaching-child regression (TEST TEMPLATE):
  // Source-level assertions via fs.readFileSync. The new vim scope test file
  // follows the same shape — read childEditorVimScope.ts as a string, regex
  // match for the expected idioms (Scope, pushScope, popScope, defineEx).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write tests/main/childEditorVimScope.test.ts source-level + behavioral assertions</name>
  <files>tests/main/childEditorVimScope.test.ts</files>
  <read_first>
    - tests/main/childEditorFactory.test.ts:394-434 (cmd-slash-not-reaching-child regression — exact template to mirror)
    - src/main/childEditorFactory.ts:153-199 (the createCmdSlashScopeExtension implementation we are mirroring)
    - .planning/phases/18-vim-recovery-polish/CONTEXT.md (D-32 locked decision — Scope-based intercept, NOT a DOM keydown listener)
    - .planning/phases/17-polish-edge-cases/17-UAT.md Test 17 (live reproduction notes — DOM probe shows activeElement IS in `.lc-nested-editor` when leak happens)
  </read_first>
  <behavior>
    - The test file follows the source-inspection pattern from `cmd-slash-not-reaching-child regression` at childEditorFactory.test.ts:394-434: `fs.readFileSync` the new module's source as a string, then regex-match expected idioms.
    - Source-level assertions (must all FAIL on current main because the module does not exist yet):
      * The module exists at `src/main/childEditorVimScope.ts` — `fs.existsSync(path.join(__dirname, '../../src/main/childEditorVimScope.ts'))` returns true.
      * The source imports `Scope` and `App` types from 'obsidian' (at minimum the runtime `Scope` constructor — same require shape as factory.ts:165).
      * The source uses `ViewPlugin.define` (not `ViewPlugin.fromClass`) — matches the createCmdSlashScopeExtension shape exactly.
      * The source calls `app.keymap.pushScope` and `app.keymap.popScope` (paired registration/removal — both literals must appear).
      * The source attaches `contentDOM.addEventListener('focus', ...)` and `contentDOM.addEventListener('blur', ...)` (paired with matching `removeEventListener` calls in `destroy()`).
      * The source registers at least one vim navigation key against the Scope — `scope.register(['j']` OR `scope.register([], 'j'` OR a switch/array that covers 'j', 'k', 'dd', etc. The regex tolerates either a per-key `scope.register` call or an array-driven loop. Required: the literals `'j'`, `'k'`, `'d'` (or `'dd'`), and `'o'` all appear inside the file.
      * The source calls `Vim.defineEx` (or imports `Vim` from `@replit/codemirror-vim` and references `defineEx`) with the alias name `'set'` and the abbreviated form `'se'` AND with options names `'nu'` / `'nonu'` (the literals appear in the file even if the registration shape is dictionary-driven).
      * `destroy()` cleans up event listeners AND pops any active Scope (mirrors createCmdSlashScopeExtension destroy block at factory.ts:188-197).
    - Behavioral assertion (one targeted unit test using vi.mock for 'obsidian' Scope):
      * Mock `obsidian` to expose a `Scope` constructor that records `register(modifiers, key, handler)` calls.
      * Mock `app.keymap` with `pushScope` / `popScope` spies and `app.scope` as a sentinel.
      * Mock `@replit/codemirror-vim`'s `Vim.handleKey` (or equivalent) as a spy.
      * Construct the extension via `createVimScopeExtension(mockApp, () => mockChildCm)`.
      * Simulate the ViewPlugin.define factory by extracting the registered factory from `ViewPlugin.define`'s mock and invoking it with a mock `view` whose `contentDOM` is a real `document.createElement('div')`.
      * Dispatch a synthetic 'focus' event on `view.contentDOM` → assert `app.keymap.pushScope` was called.
      * Inspect the Scope's recorded handlers; find the one registered for 'j'; invoke it with a synthetic KeyboardEvent → assert it returns `false` AND `Vim.handleKey` was called once with the mock child CM view as the first arg and `'j'` (or the key name the impl maps to) as the second arg.
      * Dispatch a synthetic 'blur' event → assert `app.keymap.popScope` was called.
    - Each assertion has an inline comment citing CONTEXT.md D-32 and noting which UAT Test 17 finding it closes.
    - The test file is RED on current main (no source module yet); GREEN after Task 2.
  </behavior>
  <action>
    Create `tests/main/childEditorVimScope.test.ts`. Mirror the cmd-slash regression test structure: top-of-file `vi.mock` declarations (mock 'obsidian' Scope/App, mock '@replit/codemirror-vim' Vim namespace), a `describe('childEditorVimScope module — source-level (Phase 18 Plan 01 / VIM-INTERACTION-01)', ...)` block with the source-inspection assertions enumerated in Behavior, and a sibling `describe('createVimScopeExtension — behavioral', ...)` block with the focus/blur/key-dispatch assertion. Use `fs.readFileSync` exactly as in childEditorFactory.test.ts:399-402. Use `vi.mock('obsidian', ...)` factory + `vi.mock('@replit/codemirror-vim', ...)` factory. Inline a comment in each test naming `D-32` and `UAT Test 17`. Run `npm test -- tests/main/childEditorVimScope.test.ts` and verify it fails RED with at least the "module does not exist" assertion failing. Do NOT proceed to Task 2 unless RED is reproducible.
  </action>
  <verify>
    <automated>npm test -- tests/main/childEditorVimScope.test.ts 2>&1 | grep -E "FAIL|✗|×|fail" || echo "RED state confirmed — module does not exist yet"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/main/childEditorVimScope.test.ts` exists
    - The file contains at least 2 `describe(...)` blocks (source-level + behavioral)
    - The file contains at least 6 `it(...)` blocks total — `grep -c "  it(" tests/main/childEditorVimScope.test.ts` returns >= 6
    - The file references `D-32` in at least one comment — `grep -c "D-32" tests/main/childEditorVimScope.test.ts` returns >= 1
    - The file references `UAT Test 17` (or `Test 17` + `VIM`) — `grep -c "Test 17\|VIM-01" tests/main/childEditorVimScope.test.ts` returns >= 1
    - Running `npm test -- tests/main/childEditorVimScope.test.ts` exits NON-ZERO (RED — source module missing)
  </acceptance_criteria>
  <done>RED test suite committed; module-existence and source-shape assertions all fail; behavioral assertion structure ready to GREEN once Task 2 ships.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement src/main/childEditorVimScope.ts and wire into childEditorFactory.ts</name>
  <files>src/main/childEditorVimScope.ts, src/main/childEditorFactory.ts</files>
  <read_first>
    - tests/main/childEditorVimScope.test.ts (Task 1 output — the GREEN target; every assertion drives an implementation requirement)
    - src/main/childEditorFactory.ts:153-199 (createCmdSlashScopeExtension — exact template to mirror)
    - src/main/childEditorFactory.ts:243-299 (vimEnabled gate + vim() spread — context for the new conditional)
    - src/main/childEditorFactory.ts:315 (the createCmdSlashScopeExtension(app) spread — placement reference for the new wiring)
    - .planning/phases/18-vim-recovery-polish/CONTEXT.md D-32 (the locked design — Scope-based intercept; do NOT use DOM keydown listener)
    - @replit/codemirror-vim package surface (whichever .d.ts file ships with the installed package — check `node_modules/@replit/codemirror-vim` for `Vim.defineEx` / `Vim.handleKey` exports; cast through `as unknown as` if .d.ts is narrow)
  </read_first>
  <behavior>
    - Create `src/main/childEditorVimScope.ts` exporting `createVimScopeExtension(app: App, getCm: (view: EditorView) => unknown): Extension` (where `unknown` is the `@replit/codemirror-vim` CM view shape — cast as needed).
    - Internal structure (mirror createCmdSlashScopeExtension verbatim where possible):
      * Top-of-file comment block citing CONTEXT.md D-32, UAT Test 17 / VIM-01, and the createCmdSlashScopeExtension precedent.
      * Imports: `EditorView`, `ViewPlugin`, `type PluginValue` from `@codemirror/view`; `type Extension` from `@codemirror/state`; `App, Scope` types from 'obsidian' (Scope is also constructed at runtime via `require('obsidian')` matching factory.ts:165).
      * `Vim` namespace import from `@replit/codemirror-vim` — `import { Vim } from '@replit/codemirror-vim'` (cast through `as unknown as` if .d.ts complains).
      * Module-scoped `let aliasesRegistered = false` flag for `Vim.defineEx` idempotency.
      * `function ensureExAliasesRegistered(): void` — guarded by the flag; calls `Vim.defineEx('set', 'se', handler)` with a handler that parses the option name from the ex-args (`nu` / `nonu` / `number` / `nonumber`) and dispatches the gutter toggle. Wrap the body in try/catch to defensively ignore "alias already registered" if the package surfaces an error there.
      * `export function createVimScopeExtension(app: App, getCm: (view: EditorView) => unknown): Extension` — returns `ViewPlugin.define((view) => { ... })` with the same on-focus/on-blur/destroy structure as createCmdSlashScopeExtension.
      * Inside `onFocus`: construct `new Scope(app.scope)`, register the locked vim navigation/edit keys (h, j, k, l, d, y, p, o, i, a, x, w, b, e, u, Ctrl-r, Esc) — for each key, the handler invokes `Vim.handleKey(getCm(view), keyName, 'editor')` (or equivalent) and returns `false` to stop Obsidian's app-level dispatch. Multi-character ex-style sequences like 'dd' are NOT registered as a single Scope handler — instead, a single-letter 'd' handler routes to `Vim.handleKey(view, 'd', 'editor')` and `@replit/codemirror-vim`'s vim state machine will accumulate the second 'd' on its next keystroke. (If the package's `handleKey` requires a different signature, adapt — the GREEN behavioral test from Task 1 codifies the exact call shape.)
      * Inside `onFocus`: also call `ensureExAliasesRegistered()` — idempotent across mounts.
      * `onBlur` and `destroy` mirror the createCmdSlashScopeExtension shape verbatim (pop scope, remove listeners, reset flag).
    - Edit `src/main/childEditorFactory.ts`: add a single-line import `import { createVimScopeExtension } from './childEditorVimScope';` near the existing `import { createScrollIntoViewExtension } from './childEditorSync';` (line 49). Add a single-line conditional spread inside the EditorState extensions array adjacent to the existing `...(app ? [createCmdSlashScopeExtension(app)] : []),` (line 315). The new line: `...(app && vimEnabled ? [createVimScopeExtension(app, (view) => (view as unknown as { cm: unknown }).cm)] : []),` — gated on BOTH `app` AND `vimEnabled` so non-vim child editors NEVER push a vim Scope. Place an inline comment citing CONTEXT.md D-32 and naming Plan 18-01.
    - The implementation MUST NOT add a DOM-level keydown listener (capture or bubble) anywhere — D-32 forbids this. The implementation MUST NOT introduce a new userEvent string. The implementation MUST NOT touch `src/main/nestedEditorExtension.ts` or `CLAUDE.md`.
    - After the edits, all 6+ assertions from Task 1 pass GREEN. The existing `cmd-slash-not-reaching-child regression` suite at childEditorFactory.test.ts:394-434 continues to pass (createCmdSlashScopeExtension is unmodified). Full test suite `npm test` passes with no new regressions. `npm run build` is clean.
  </behavior>
  <action>
    Create `src/main/childEditorVimScope.ts` per the Behavior contract above — mirror createCmdSlashScopeExtension's structure verbatim, swap the Mod-/ registration for a sequence of vim-key registrations + the `Vim.defineEx` ex-alias setup. Edit `src/main/childEditorFactory.ts`: add the import (next to childEditorSync import at line 49) and the conditional spread (next to createCmdSlashScopeExtension spread at line 315), gated on `app && vimEnabled`. Add an inline comment block citing CONTEXT.md D-32 and Plan 18-01 next to the new spread. Run `npm test -- tests/main/childEditorVimScope.test.ts` — verify GREEN. Run `npm test -- tests/main/childEditorFactory.test.ts` — verify the existing 30+ tests still pass (cmd-slash regression + LINENUM-01 + Plan 17-11 + Plan 17-12 suites). Run `npm test` (full suite) — verify GREEN. Run `npm run build` — verify clean. If `Vim.defineEx` throws on second call, wrap in try/catch and verify Task 1's idempotency assertion still passes.
  </action>
  <verify>
    <automated>npm test -- tests/main/childEditorVimScope.test.ts tests/main/childEditorFactory.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File `src/main/childEditorVimScope.ts` exists; `wc -l < src/main/childEditorVimScope.ts` returns >= 80
    - The file exports `createVimScopeExtension` — `grep -c "export function createVimScopeExtension" src/main/childEditorVimScope.ts` returns 1
    - The file uses `ViewPlugin.define` — `grep -c "ViewPlugin\\.define" src/main/childEditorVimScope.ts` returns 1
    - The file calls `app.keymap.pushScope` and `app.keymap.popScope` — both grep counts >= 1
    - The file references at least 4 of the locked vim keys (j, k, d, o) — `grep -cE "'j'|'k'|'d'|'o'|'i'|'a'" src/main/childEditorVimScope.ts` returns >= 4
    - The file references `Vim.defineEx` — `grep -c "defineEx" src/main/childEditorVimScope.ts` returns >= 1
    - The file references `'nu'` and `'nonu'` ex-alias names — `grep -cE "'nu'|'nonu'" src/main/childEditorVimScope.ts` returns >= 2
    - The file does NOT use `addEventListener('keydown'` (D-32 forbids DOM-level keydown) — `grep -c "addEventListener\\('keydown'" src/main/childEditorVimScope.ts` returns 0
    - `src/main/childEditorFactory.ts` imports the new module — `grep -c "from './childEditorVimScope'" src/main/childEditorFactory.ts` returns 1
    - `src/main/childEditorFactory.ts` references `createVimScopeExtension` exactly once in the extensions array — `grep -c "createVimScopeExtension" src/main/childEditorFactory.ts` returns 1
    - The new spread is gated on `vimEnabled` — `grep -cE "vimEnabled.*createVimScopeExtension|createVimScopeExtension.*vimEnabled|app && vimEnabled" src/main/childEditorFactory.ts` returns >= 1
    - `npm test -- tests/main/childEditorVimScope.test.ts` exits 0 — all assertions GREEN
    - `npm test -- tests/main/childEditorFactory.test.ts` exits 0 — existing suites preserved (cmd-slash regression untouched)
    - `npm test` (full suite) exits 0 — no new regressions
    - `npm run build` exits 0 — esbuild + tsc clean
    - `git diff src/main/nestedEditorExtension.ts` shows ZERO changes
    - `git diff CLAUDE.md` shows ZERO changes
    - `git diff package.json package-lock.json` shows ZERO changes (no new dep)
  </acceptance_criteria>
  <done>Vim Scope intercept module shipped and wired; vim navigation/edit keys route to the child's vim instance when the child has focus; `:set nu` / `:set nonu` aliases registered; full test suite GREEN; bundle ceiling preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Append VIM-01 closure entry to 17-UAT.md (cross-phase UAT continuity)</name>
  <files>.planning/phases/17-polish-edge-cases/17-UAT.md</files>
  <read_first>
    - .planning/phases/17-polish-edge-cases/17-UAT.md Test 17 (the partial finding being closed by this plan)
    - tests/main/childEditorVimScope.test.ts (Task 1+2 — assertions that codify the fix)
  </read_first>
  <behavior>
    - DO NOT modify Test 17's existing block — it is the historical record.
    - Update Test 17's `result:` line from `partial` → `pass` and append a `notes:` continuation line referencing Plan 18-01: "2026-05-24 (Phase 18 Plan 01 — VIM-INTERACTION-01): Scope-based intercept shipped per CONTEXT D-32. Vim navigation keys j/k/dd/o now execute against child's vim instance, not parent's. `:set nu` / `:set nonu` aliases registered. Source-level + behavioral test suite at tests/main/childEditorVimScope.test.ts (8 assertions) GREEN."
    - Update the frontmatter `summary:` block: decrement `partial` by 1, increment `pass` by 1.
    - Update the `## Summary` block at the bottom to reconcile.
    - The 18-04 manual UAT pass will re-verify VIM-01 against the final v1.2 build; this plan's source change is the GREEN gate.
  </behavior>
  <action>
    Read `.planning/phases/17-polish-edge-cases/17-UAT.md`. Locate `### 17. VIM-01` heading. Change its `result: partial` → `result: pass`. Append the closure note to its `notes:` field (or add a new line citing Plan 18-01 if the existing notes string is too long). Update the frontmatter `summary:` `partial` and `pass` counts. Update the bottom `## Summary` block to match. Verify with `grep "result: partial" .planning/phases/17-polish-edge-cases/17-UAT.md` — count drops by 1.
  </action>
  <verify>
    <automated>grep -A1 "VIM-01" .planning/phases/17-polish-edge-cases/17-UAT.md | grep -c "pass"</automated>
  </verify>
  <acceptance_criteria>
    - `### 17. VIM-01` block now has `result: pass` (not `partial`)
    - The notes field cites Plan 18-01 — `grep -c "Phase 18 Plan 01" .planning/phases/17-polish-edge-cases/17-UAT.md` returns >= 1 (or `grep -c "VIM-INTERACTION-01" .planning/phases/17-polish-edge-cases/17-UAT.md` returns >= 1)
    - Frontmatter `partial` count decremented by 1
    - Frontmatter `pass` count incremented by 1
    - Bottom `## Summary` block reconciles
  </acceptance_criteria>
  <done>UAT Test 17 / VIM-01 flipped partial → pass with Plan 18-01 closure citation; summary blocks reconciled.</done>
</task>

</tasks>

<verification>
- VIM-INTERACTION-01 closes UAT Test 17 / backlog 999.2 — vim navigation/edit keys route to child's vim instance, not parent's, when child has focus
- Scope-based intercept mirrors `createCmdSlashScopeExtension` shape (D-32) — push on focus, pop on blur, ViewPlugin.define lifecycle
- `:set nu` / `:set nonu` ex-aliases registered via `Vim.defineEx` — abbreviated forms now toggle the gutter just like `:set number` / `:set nonumber`
- All Phase 17 invariants preserved (section lock, sync annotations, parent-side repair listener, per-child language tracker, semantic class layer, ECHO_PRONE_USER_EVENTS unchanged, CLAUDE.md unchanged)
- No new userEvent string introduced; no new build-time dependency added
- Bundle stays under 1.8 MB ceiling (CONTEXT D-19) — Scope-based intercept is < 5 KB minified
</verification>

<success_criteria>
- All assertions in `tests/main/childEditorVimScope.test.ts` (>= 6 it blocks across source-level + behavioral describes) pass GREEN
- All assertions in `tests/main/childEditorFactory.test.ts` (existing 30+ tests including cmd-slash regression, LINENUM-01, Plan 17-11, Plan 17-12) still pass
- Full `npm test` suite GREEN; `npm run build` clean
- `src/main/childEditorVimScope.ts` exists and contains the Scope-based intercept logic mirroring createCmdSlashScopeExtension
- `src/main/childEditorFactory.ts` has exactly one new import + one new conditional spread, gated on `app && vimEnabled`
- 17-UAT.md Test 17 / VIM-01 flipped from `partial` to `pass` with Plan 18-01 closure citation
- `git diff src/main/nestedEditorExtension.ts CLAUDE.md package.json package-lock.json` shows ZERO changes
</success_criteria>

<output>
Create `.planning/phases/18-vim-recovery-polish/18-01-SUMMARY.md` when done summarizing:
- Files modified with line numbers (childEditorVimScope.ts created; childEditorFactory.ts +1 import +1 conditional spread; childEditorVimScope.test.ts created; 17-UAT.md Test 17 result flip)
- Test count delta (added: 6+ in childEditorVimScope.test.ts; total project test count moves from 1693 → 1699+)
- Green run output for both targeted suites and full suite
- Bundle size before/after (verify under 1.8 MB ceiling)
- Confirmation that nestedEditorExtension.ts, CLAUDE.md, package.json all show ZERO diff
- Cross-plan invariant verification: ECHO_PRONE_USER_EVENTS unchanged; 'leetcode.reset.child' NOT added; section lock untouched
</output>
</content>
