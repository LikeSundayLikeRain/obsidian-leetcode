# Phase 18 — Continue Here

**Last paused:** 2026-05-25T16:26:55Z
**Git HEAD:** 692ed26 (Phase 17 round-3 baseline + regression debug doc)
**Tests:** 1713 passed / 6 skipped — clean baseline
**Vault deploy:** in sync (Phase 17 round-3 build at ~/Documents/Obsidian Vault/.obsidian/plugins/obsidian-leetcode/)

## What Happened

Phase 18 had three plans (18-01 vim Scope intercept, 18-02 vault.on recovery + stale-child invalidation, 18-03 relative line numbers). All three shipped passing 1738 unit tests. All three introduced major UAT regressions in production. **All three reverted.**

| Plan | Reverted in | What broke in production |
|---|---|---|
| 18-03 | dc886a1 | Toggle setting was a no-op; child editor still showed absolute line numbers |
| 18-01 | cf7cd51 | Esc no longer exited Insert mode; `i`/`a`/`o`/`s` intermittently failed to enable typing; `:set nu` aliases were no-ops |
| 18-02 | 98dec9b | **CRITICAL** — chevron switch to python3 or c blanks the Code section. checkStaleChildAndInvalidate was deleting the registry on the chevron event. |

Tree is back at Phase 17 round-3 baseline. **All three plans must still ship before v1.2 release** (user-confirmed scope).

## Critical Anti-Patterns (BLOCKING — read before resuming)

The resume gate enforces that you demonstrate understanding of these before any further Phase 18 work.

| # | Anti-pattern | Severity |
|---|---|---|
| 1 | Trusting unit tests to catch Phase 18 UI/lifecycle bugs. 1738 green tests didn't catch any of the three regressions. Live DOM probes via DevTools are the only reliable path. | blocking |
| 2 | Iterating on the existing 18-01/02/03 PLAN.md files. They contain wrong assumptions about the codebase (callsite line numbers, integration paths). Replan from scratch. | blocking |
| 3 | Assuming a simple slug normalization fix resolves the chevron-blank bug. The hotfix that normalized readActiveFenceSlug via resolveLangSlug was insufficient — user reported "still broken" with the fix in place. | blocking |
| 4 | Deploying to .obsidian/plugins/leetcode/ instead of .obsidian/plugins/obsidian-leetcode/. Wrong path silently fails. Always verify via `app.plugins.manifests.leetcode.dir`. | advisory |

## Resume Strategy

1. **Read the debug doc first:** `.planning/debug/phase-18-regressions-2026-05-25.md` has all three failure modes with confirmed root causes and recommended fix paths.

2. **Order of attack:** chevron-blank-on-python3-c (18-02 area) FIRST — it's the most damaging because it affects core feature for two language slugs. Then 18-01 (vim Scope, narrower key intercept). Then 18-03 (relative line numbers — plumbing-only).

3. **Spawn /gsd-debug** for each: capture live DevTools probe data BEFORE writing any fix code. Have the user paste the live state.

4. **Replan all three from scratch.** Don't iterate the existing PLAN.md. Capture root-cause-driven plans informed by the debug session findings.

5. **After all three pass live UAT:** run 18-04 (manual ship-readiness pass — bundle audit, heap snapshot, regression spot-checks).

## What Lives Where

- `.planning/debug/phase-18-regressions-2026-05-25.md` — primary handoff context, all three failure modes
- `.planning/phases/18-vim-recovery-polish/18-01-PLAN.md` etc — **DO NOT trust these**, replan from scratch
- `.planning/phases/18-vim-recovery-polish/CONTEXT.md` — high-level scope definition; still useful as input to replanning
- `.planning/phases/17-polish-edge-cases/17-UAT.md` — Tests 17 (VIM-01) + 23 (REPAIR-02) currently marked partial; Test 25 (LINENUM-RELATIVE-01) was added by reverted 18-03 and may need cleanup
- `~/.claude/projects/-Users-moxu-projects-obsidian-leetcode/memory/vault_plugin_path.md` — vault active-folder path memory

## Backlog Items Still Open

- 999.1 — Opinionated One Dark Pro palette (deferred to v1.3 by user 2026-05-24)
- 999.2 / 999.3 / 999.4 — entirely subsumed into Phase 18; backlog dirs at `.planning/phases/999.{2,3,4}-*` should be deleted once Phase 18 ships
