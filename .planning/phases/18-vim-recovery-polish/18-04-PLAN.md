---
phase: 18-vim-recovery-polish
plan: 04
type: execute
wave: 3
depends_on: ["18-01", "18-02", "18-03"]
files_modified:
  - .planning/phases/17-polish-edge-cases/17-UAT.md
  - .planning/phases/17-polish-edge-cases/17-BUNDLE-AUDIT.md
  - .planning/phases/17-polish-edge-cases/17-LIFE-SNAPSHOT.md
autonomous: false

requirements: []
requirements_addressed: []

gap_closure: false

must_haves:
  truths:
    - "Three deliverable docs MUST exist after this plan completes: (a) updated `.planning/phases/17-polish-edge-cases/17-UAT.md` with regression spot-check results for Tests 1, 9, 10, 12, 13, 14, 17, 18, 19, 23, 24, 25 against the FINAL v1.2 build that includes Phase 18 fixes (per CONTEXT D-31); (b) NEW `.planning/phases/17-polish-edge-cases/17-BUNDLE-AUDIT.md` with raw + gzipped main.js sizes, esbuild metafile contributor breakdown (top 10 modules by bytes), hard-gate verdict against the 1.8 MB ceiling per CONTEXT D-19; (c) NEW `.planning/phases/17-polish-edge-cases/17-LIFE-SNAPSHOT.md` documenting the heap-snapshot UAT (D-23 arm b) re-run on the FINAL v1.2 build."
    - "The 17-UAT.md regression spot-check MUST cover the 12 tests listed in CONTEXT.md §18-04 (Tests 1, 9, 10, 12, 13, 14, 17, 18, 19, 23, 24, 25). For each test the manual operator records: (a) PASS / FAIL / N/A verdict; (b) any observed regression notes; (c) the FINAL v1.2 build commit SHA used. Test 17 (VIM-01) MUST verify the Plan 18-01 fix is live (vim navigation keys execute against child's vim, not parent's). Test 23 (REPAIR-02) MUST verify the Plan 18-02 fix is live (vim-dd no longer leaves fence broken; stale-child invalidation prevents cross-language render). Test 25 (LINENUM-RELATIVE-01, added by Plan 18-03 Task 3) MUST verify the new toggle works as documented. Tests 1 / 9 / 10 / 12 / 13 / 14 / 18 / 19 are regression sanity checks against the prior PASS state — any flip to FAIL is a P0 ship blocker."
    - "The 17-BUNDLE-AUDIT.md doc MUST contain at minimum: (a) build invocation `npm run build` exit code; (b) raw `main.js` size in bytes (`wc -c main.js`); (c) gzipped `main.js` size (`gzip -c main.js | wc -c`); (d) esbuild metafile contributor breakdown — re-run with `--metafile=metafile.json` and document the top 10 modules by bytes via `npx esbuild-visualizer` or a manual JSON read; (e) hard-gate verdict against 1.8 MB ceiling (raw < 1,800,000 bytes); (f) Phase 18 bundle delta (compute by checking out the pre-18 commit, building, recording size, then comparing to the post-18 build size); (g) PASS / FAIL verdict with explicit reasoning. Phase 18 estimated delta: < 5 KB minified (Scope-based vim intercept ~2 KB; relative formatter ~< 1 KB; vault.on('modify') registration ~< 0.5 KB)."
    - "The 17-LIFE-SNAPSHOT.md doc MUST follow CONTEXT D-23 arm b — the heap-snapshot lifecycle UAT must be re-run on the FINAL v1.2 build (per CONTEXT D-31). Procedure: (a) Open Obsidian DevTools (Cmd-Shift-I); (b) Memory tab → Heap snapshot → take baseline (Snapshot 1); (c) Open and close 5 different LeetCode problem notes in sequence (NOT 20 — Phase 17 Test 22 LIFE-01 already passed at 20 cycles; this re-run uses 5 cycles to verify Phase 18 changes did not introduce a leak); (d) Force GC (DevTools Memory tab → trash-can icon); (e) Take Snapshot 2; (f) Comparison view filter `EditorView` — verify zero retained instances OR verdict explanation if minified class names obscure direct count; (g) Verdict: pass / fail / leak detected; (h) Phase 18 module-specific scrutiny: search the heap for retained Scope instances (childEditorVimScope.ts onFocus path), retained vault.on listeners (childEditorSync.ts vault.on registration), retained childLanguageTracker entries (WeakMap should auto-collect when child EditorView is GC'd)."
    - "All three docs MUST cite the final v1.2 build commit SHA at the top of each file (e.g., `## Build under test\\n\\nCommit: abc1234 (v1.2 ship gate)\\nDate: 2026-05-24`). The commit SHA is the merge commit for the FINAL Phase 18 wave (after 18-01, 18-02, 18-03 all merge)."
    - "Plan 18-04 is HUMAN-DRIVEN — `autonomous: false`. The tasks below are checkpoint-style: Claude executes the automated portions (`npm run build`, `wc -c main.js`, esbuild metafile generation, file scaffolding with template content) but PAUSES at human-required steps (open Obsidian, click into child editor, take heap snapshots, run vim keystrokes manually). The user types `approved` after each manual step's results are filled in, OR the user describes the failure and Claude pivots to a P0 follow-up plan."
    - "Each manual UAT verdict MUST be specific (not 'looks good'): for Test 17 the operator describes which vim keys were tested (j, k, dd at minimum), what the parent doc looked like before/after, and explicit confirmation that focus stayed in the child via DOM probe (`document.activeElement` is inside `.lc-nested-editor`). For Test 23 the operator describes the exact reproduction steps (vim-dd on the closer line, observe in-place repair without reload). For Test 25 the operator describes the gutter rendering before/after toggle + cursor movement re-rendering."
    - "If ANY test in the regression spot-check FAILS or any FAIL appears in the bundle audit / heap snapshot, this plan PAUSES and triggers a P0 follow-up plan (likely a new Phase 18 Plan 05 or a Phase 18.1 in CONTEXT.md). The user decides whether to ship or block. Claude MUST NOT mark the plan complete with any FAIL verdicts unrecorded."
    - "Preserve: ALL Phase 17 + Phase 18 source-level invariants are unchanged by this plan — the plan only writes documentation. Section lock, sync annotations, ECHO_PRONE_USER_EVENTS, CLAUDE.md, package.json, all source files: ZERO diff."
  artifacts:
    - path: ".planning/phases/17-polish-edge-cases/17-UAT.md"
      provides: "Existing UAT doc updated with regression spot-check results for the 12 listed tests run against the final v1.2 build. The original test entries are NOT modified — new outcome notes are appended OR a new top-level section `## Phase 18 Regression Spot-Check (final v1.2 build)` is added with per-test results."
      contains: "Phase 18|final v1.2 build|spot-check"
    - path: ".planning/phases/17-polish-edge-cases/17-BUNDLE-AUDIT.md"
      provides: "NEW doc with raw + gzipped main.js sizes, esbuild metafile top-10 module breakdown, Phase 18 bundle delta, hard-gate verdict against 1.8 MB ceiling per CONTEXT D-19."
      contains: "main\\.js|bundle|1\\.8 MB|gzipped"
      min_lines: 40
    - path: ".planning/phases/17-polish-edge-cases/17-LIFE-SNAPSHOT.md"
      provides: "NEW doc with heap-snapshot UAT procedure, snapshot1/snapshot2 deltas, retained-EditorView count, verdict against D-23 arm b. Includes Phase 18 module-specific scrutiny (Scope, vault.on listener, WeakMap)."
      contains: "heap snapshot|retained|EditorView|Phase 18|D-23"
      min_lines: 40
  key_links:
    - from: "17-UAT.md regression spot-check"
      to: "Plan 18-01 / 18-02 / 18-03 source-level fixes"
      via: "Tests 17 / 23 / 25 verdicts confirm the live fix is observable"
      pattern: "Phase 18 Plan 01|Phase 18 Plan 02|Phase 18 Plan 03"
    - from: "17-BUNDLE-AUDIT.md"
      to: "CONTEXT.md D-19 (1.8 MB ceiling)"
      via: "explicit ceiling verdict + Phase 18 delta computation"
      pattern: "1\\.8 MB|ceiling|D-19"
    - from: "17-LIFE-SNAPSHOT.md"
      to: "CONTEXT.md D-23 arm b (heap snapshot lifecycle UAT)"
      via: "5-cycle open/close + GC + Comparison view + retained-EditorView count"
      pattern: "D-23|heap snapshot"
  pitfalls:
    - "DO NOT run any of these tasks against an intermediate build (e.g., post-18-01 but pre-18-02). Per CONTEXT D-31, all three docs must be re-run on the FINAL v1.2 build that includes ALL Phase 18 fixes (18-01 + 18-02 + 18-03 merged)."
    - "DO NOT modify the existing test entries in 17-UAT.md — append a new section or per-test continuation notes. The original entries are the historical record."
    - "DO NOT mark the plan complete if any FAIL verdict is recorded — pause and surface to the user. The user may choose to ship with known issues (and update CONTEXT.md / RETROSPECTIVE.md accordingly), but Claude MUST NOT auto-advance past a FAIL."
    - "DO NOT skip the Phase 18 module-specific heap scrutiny in 17-LIFE-SNAPSHOT.md — the new Scope (Plan 18-01), vault.on listener (Plan 18-02), and the existing WeakMap (Plan 17-09 unchanged but worth re-verifying after 18-02's stale-child invalidation may force registry deletes) are all candidate leak surfaces."
    - "DO NOT estimate the bundle delta from the diff alone — actually build the pre-18 commit AND the post-18 commit and `wc -c` both to record the real number. Use `git stash` or a worktree if needed to avoid disturbing the working tree."
    - "DO NOT compute the bundle audit before all of 18-01 / 18-02 / 18-03 have merged — Wave 3 means this plan runs LAST."
    - "DO NOT introduce any source-level changes in this plan — it is documentation only. Verify with `git diff src/ tests/` showing ZERO changes after the plan completes (only `.planning/` paths in the diff)."
---

<objective>
Close the v1.2 ship-readiness gate per CONTEXT D-30 + D-31. Plan 17-06 deferred two ship-readiness deliverables (bundle audit + heap-snapshot UAT) and the regression spot-check pass to a final-build phase; CONTEXT D-31 promotes those deferrals into Plan 18-04, which runs on the FINAL v1.2 build that includes all Phase 18 fixes (18-01 vim Scope intercept + 18-02 vault.on('modify') trigger + stale-child invalidation + 18-03 relative line numbers setting).

Three deliverables, all run on the FINAL v1.2 build:

1. **17-UAT.md regression spot-checks** — re-run Tests 1, 9, 10, 12, 13, 14, 17, 18, 19, 23, 24, 25 on the final build. Tests 17 / 23 / 25 verify the new Phase 18 fixes are live; the others are regression sanity checks against prior PASS states.

2. **17-BUNDLE-AUDIT.md** — capture final main.js raw + gzipped sizes, esbuild metafile top-10 contributor breakdown, hard-gate verdict against the 1.8 MB ceiling (CONTEXT D-19). Phase 18 added bundle weight (Scope-based vim intercept + relative formatter + vault.on registration) — needs verification.

3. **17-LIFE-SNAPSHOT.md** — heap-snapshot lifecycle UAT per CONTEXT D-23 arm b. Open + close 5 problem notes; take baseline + post-cycle heap snapshots; verify retained EditorView count drops to zero; Phase 18 module-specific scrutiny (Scope, vault.on listener, WeakMap).

Plan 18-04 is HUMAN-DRIVEN (`autonomous: false`). Claude executes the automatable portions (build, size measurement, esbuild metafile generation, doc scaffolding) but PAUSES at human-required steps (open Obsidian, take heap snapshots, run vim keystrokes manually, fill in verdicts).

Purpose: this plan is the v1.2 release readiness checklist. Without these three artifacts, v1.2 cannot ship — bundle ceiling unverified, lifecycle UAT unverified against the final build, and the regression spot-check would leave Phase 18 fixes unconfirmed in production-grade UX.

Output: updated 17-UAT.md, new 17-BUNDLE-AUDIT.md, new 17-LIFE-SNAPSHOT.md. ZERO source-level changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-vim-recovery-polish/CONTEXT.md
@.planning/phases/18-vim-recovery-polish/18-01-PLAN.md
@.planning/phases/18-vim-recovery-polish/18-02-PLAN.md
@.planning/phases/18-vim-recovery-polish/18-03-PLAN.md
@.planning/phases/17-polish-edge-cases/17-UAT.md
@.planning/phases/17-polish-edge-cases/17-06-PLAN.md
@CLAUDE.md

<interfaces>
<!-- This is a docs-only plan. Source surfaces are read-only references for verifying the live state matches the plan documents. -->

.planning/phases/17-polish-edge-cases/17-06-PLAN.md (CANONICAL UAT entry shape):
// Plan 17-06 Task 4 documented the UAT entry conventions. The Phase 18
// regression spot-check appends new content following the same `expected:`
// + `result: pass|fail|partial` + `notes:` shape.

CONTEXT.md D-19 (BUNDLE CEILING):
// 1.8 MB raw main.js. Phase 17 baseline post-vim was 1,707,327 raw / 459,257
// gzipped. Phase 18 estimated delta: < 5 KB minified.

CONTEXT.md D-23 arm b (HEAP SNAPSHOT UAT):
// Procedure: Memory tab → Heap snapshot → baseline → open/close cycles → GC
// → second snapshot → Comparison view → filter EditorView → verify zero
// retained instances. Phase 17 Test 22 LIFE-01 passed at 20 cycles; this
// plan re-runs at 5 cycles for the final v1.2 build.

CONTEXT.md D-31 (FINAL BUILD):
// "17-LIFE-SNAPSHOT.md, 17-BUNDLE-AUDIT.md and 17-UAT.md regression spot-
// checks are re-run on the FINAL v1.2 build that includes Phase 18 fixes."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the FINAL v1.2 candidate; capture raw + gzipped sizes; generate esbuild metafile</name>
  <files>
    (Read-only build artifacts: main.js, metafile.json — these are not committed; only the audit doc is committed.)
  </files>
  <read_first>
    - .planning/phases/18-vim-recovery-polish/CONTEXT.md D-19, D-31
    - esbuild.config.mjs (the build script — confirm `metafile: true` is set OR add a temporary local-only override)
    - .planning/phases/17-polish-edge-cases/17-06-PLAN.md (the precedent for the prior bundle audit shape)
  </read_first>
  <behavior>
    - Confirm Wave 1 + Wave 2 plans (18-01, 18-02, 18-03) have all merged. Verify with `git log --oneline -5` showing the relevant commits.
    - Run `npm run build` and confirm exit code 0. If exit non-zero, STOP and surface to the user — the FAIL state is a ship blocker.
    - Capture raw size: `wc -c main.js` → record the byte count.
    - Capture gzipped size: `gzip -c main.js | wc -c` → record the gzipped byte count.
    - Generate esbuild metafile: if `esbuild.config.mjs` does not already write a metafile, ADD a local-only invocation flag (do NOT commit the change). Run with metafile output. Parse the JSON to compute the top 10 modules by bytes (read the `outputs[*].bytes` and `outputs[*].inputs` map; sum input contributions per source file path; sort descending; take top 10).
    - Compute the Phase 18 delta: stash any uncommitted changes; checkout the parent commit of the first Phase 18 commit (i.e., the v1.2 ship-gate baseline commit pre-Phase-18); run `npm run build`; record raw + gzipped sizes; un-stash and return to the post-Phase-18 build state. (Use a worktree if cleaner: `git worktree add /tmp/v12-pre18 <pre-18-commit>` then `cd /tmp/v12-pre18 && npm install --offline && npm run build && wc -c main.js`.)
    - Record all numbers + the FINAL build commit SHA in temporary variables for Task 2.
  </behavior>
  <action>
    Verify all Phase 18 source merges are present. Run `npm run build` (background mode acceptable for the build — the audit doc waits on its output). Capture sizes via `wc -c` + `gzip -c`. Generate the metafile and parse top-10 modules. Compute Phase 18 delta by building the pre-18 commit (worktree-based). Record all numbers. If any step FAILS (build exit non-zero, ceiling exceeded, regression in size), STOP and surface to the user.
  </action>
  <verify>
    <automated>npm run build && test -f main.js && echo "Build OK; size=$(wc -c < main.js) bytes"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 against the FINAL v1.2 build commit
    - `main.js` raw size recorded; size < 1,800,000 bytes (CONTEXT D-19 ceiling)
    - `main.js` gzipped size recorded
    - esbuild metafile top-10 modules computed
    - Phase 18 delta (raw + gzipped) computed by comparing FINAL build vs pre-18 baseline build
    - Final build commit SHA recorded
  </acceptance_criteria>
  <done>Build artifacts captured; sizes confirmed under ceiling; metafile contributors enumerated; Phase 18 delta computed.</done>
</task>

<task type="auto">
  <name>Task 2: Write 17-BUNDLE-AUDIT.md with full audit content</name>
  <files>.planning/phases/17-polish-edge-cases/17-BUNDLE-AUDIT.md</files>
  <read_first>
    - Task 1 output (sizes + metafile + delta + commit SHA)
    - .planning/phases/17-polish-edge-cases/17-06-PLAN.md (the prior bundle audit precedent)
    - .planning/phases/18-vim-recovery-polish/CONTEXT.md D-19
  </read_first>
  <behavior>
    - Create `.planning/phases/17-polish-edge-cases/17-BUNDLE-AUDIT.md`. Frontmatter: `phase: 17-polish-edge-cases`, `created: 2026-05-24`, `final_build_commit: <SHA from Task 1>`, `verdict: pass|fail`.
    - Sections (in order):
      * `## Build under test` — commit SHA, date, build invocation (`npm run build`), exit code.
      * `## Raw size` — `main.js` byte count + comparison to the 1.8 MB ceiling. PASS / FAIL verdict.
      * `## Gzipped size` — gzipped byte count.
      * `## Phase 18 bundle delta` — table comparing pre-Phase-18 build vs FINAL Phase 18 build. Columns: raw bytes, raw delta (bytes + percentage), gzipped bytes, gzipped delta (bytes + percentage). The delta MUST be < 5 KB raw (per CONTEXT estimate); record the actual number even if it differs.
      * `## Top 10 contributors (esbuild metafile)` — table with columns: rank, module/file path, bytes, % of total. Read from the parsed metafile JSON.
      * `## Phase 18 module attributions` — explicit attribution of any size delta to specific Phase 18 plans:
        - Plan 18-01 (childEditorVimScope.ts): expected ~2 KB minified.
        - Plan 18-02 (childEditorSync.ts vault.on registration extension + checkStaleChildAndInvalidate in main.ts): expected ~< 1 KB minified.
        - Plan 18-03 (relativeFormatter + new SettingsStore field + new SettingsTab toggle + lineNumbers config): expected ~< 1 KB minified.
        - Sum: < 5 KB. The actual delta is recorded in the previous section.
      * `## Verdict` — explicit PASS / FAIL with reasoning. PASS criteria: raw size < 1,800,000 bytes; Phase 18 delta < 50 KB raw (a generous ceiling — the spec says < 5 KB; > 50 KB would indicate accidental bloat). FAIL on either criterion.
    - File length: at least 40 lines (substantive content; not just headers).
  </behavior>
  <action>
    Write `.planning/phases/17-polish-edge-cases/17-BUNDLE-AUDIT.md` with the structure above. Embed the actual numbers from Task 1. The verdict is computed from the recorded sizes — if PASS, mark `verdict: pass` in frontmatter; if FAIL, mark `verdict: fail` and STOP, escalate to the user.
  </action>
  <verify>
    <automated>test -f .planning/phases/17-polish-edge-cases/17-BUNDLE-AUDIT.md && grep -c "Verdict" .planning/phases/17-polish-edge-cases/17-BUNDLE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/17-polish-edge-cases/17-BUNDLE-AUDIT.md` exists; `wc -l` >= 40
    - File contains literal headings `## Build under test`, `## Raw size`, `## Gzipped size`, `## Phase 18 bundle delta`, `## Top 10 contributors`, `## Verdict`
    - Frontmatter contains `final_build_commit:` and `verdict:` keys
    - File contains the actual byte count from Task 1 — `grep -E "[0-9]{6,7}" .planning/phases/17-polish-edge-cases/17-BUNDLE-AUDIT.md` returns at least 2 matches (raw + gzipped numbers)
    - Verdict is explicit PASS or FAIL with reasoning
  </acceptance_criteria>
  <done>17-BUNDLE-AUDIT.md committed with FINAL build sizes, top-10 contributor breakdown, Phase 18 delta, and explicit ceiling verdict.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Heap-snapshot lifecycle UAT (5-cycle open/close on FINAL v1.2 build)</name>
  <what-built>
    Phase 18 source changes are merged. Build is captured. Now verify the lifecycle UAT (D-23 arm b) on the FINAL build to confirm Phase 18 changes did not introduce a memory leak.
  </what-built>
  <how-to-verify>
    1. Start Obsidian against the dev vault with the FINAL v1.2 build deployed (the post-Phase-18 main.js).
    2. Open Obsidian DevTools (Cmd-Shift-I on macOS / Ctrl-Shift-I on Win/Linux).
    3. Switch to the Memory tab.
    4. Take a baseline heap snapshot — label it "Snapshot 1: baseline".
    5. Open and close 5 different LeetCode problem notes from the dev vault's `LeetCode/problems/` folder. Use a mix of languages (e.g., 2 Java, 2 Python, 1 C++) to exercise the language Compartment + child language tracker paths.
    6. After closing the 5th note, force GC: DevTools Memory tab → trash-can icon ("Collect garbage").
    7. Take a second heap snapshot — label it "Snapshot 2: post-cycle".
    8. In DevTools' Comparison view of Snapshot 2 vs Snapshot 1, filter the constructor list by typing `EditorView`.
    9. Record:
       (a) Count of retained EditorView instances (target: 0).
       (b) Whether any instance is "Detached" (DOM-detached but JS-reachable — a leak signal).
       (c) Filter for `Scope` (Plan 18-01) — expect 0 retained Scope instances after GC (Scope is popped on blur AND on ViewPlugin destroy).
       (d) Filter for `WeakMap` — note that childLanguageTracker is a WeakMap so its entries should auto-collect when their EditorView keys are GC'd. If any retained EditorView has a non-empty childLanguageTracker entry, that is a leak.
       (e) Verdict: pass / fail / leak detected.
    10. Specifically test Plan 18-02's vault.on('modify') registration: open a problem note, modify it (e.g., type one character in the child), close it. The vault.on listener stays registered for the plugin's lifetime (auto-cleaned on plugin unload via `this.registerEvent`), but it should NOT retain references to closed notes' EditorViews. Verify by checking that no EditorView is reachable from the vault.on handler closure after GC.
    11. Specifically test Plan 18-01's child Scope: focus the child editor, then click into `## Notes` (blur). Verify the Scope was popped (`app.keymap.popScope` traceable in the Performance tab if needed; otherwise rely on the absence of leaked Scope instances post-GC).
    12. Specifically test Plan 18-03's relativeFormatter: open a note with the new toggle ON, type to move the cursor, observe the gutter re-renders. Close the note. Verify no `relativeFormatter` references retain EditorState.
  </how-to-verify>
  <resume-signal>Type "approved" with the recorded verdict (pass / fail / leak detected) and any notes — OR describe failure and Claude pivots to a P0 follow-up plan</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Write 17-LIFE-SNAPSHOT.md from Task 3 results</name>
  <files>.planning/phases/17-polish-edge-cases/17-LIFE-SNAPSHOT.md</files>
  <read_first>
    - Task 3 results (verdict + recorded numbers)
    - .planning/phases/17-polish-edge-cases/17-UAT.md Test 22 LIFE-01 (the prior 20-cycle pass — precedent for doc shape)
    - .planning/phases/18-vim-recovery-polish/CONTEXT.md D-23 arm b
  </read_first>
  <behavior>
    - Create `.planning/phases/17-polish-edge-cases/17-LIFE-SNAPSHOT.md`. Frontmatter: `phase: 17-polish-edge-cases`, `created: 2026-05-24`, `final_build_commit: <same SHA as Task 2>`, `verdict: pass|fail|leak`.
    - Sections (in order):
      * `## Build under test` — commit SHA, date, OS + Obsidian version.
      * `## Procedure` — verbatim restatement of the steps run (5-cycle open/close + GC + Comparison view).
      * `## Findings` — 5-row table:
        - Row 1: retained EditorView count (target 0).
        - Row 2: detached EditorView count (target 0).
        - Row 3: retained Scope count (target 0 — Plan 18-01 cleanup).
        - Row 4: childLanguageTracker WeakMap entries that retain EditorView (target 0 — Plan 17-09 + Plan 18-02 invariant).
        - Row 5: vault.on('modify') listener leak signal (target none — Plan 18-02 invariant).
      * `## Phase 18 module-specific scrutiny` — explicit text confirming each Plan 18 module's lifecycle behavior was verified (Plan 18-01 Scope push/pop; Plan 18-02 vault.on listener does not retain closed notes; Plan 18-03 relativeFormatter does not retain EditorState).
      * `## Verdict` — explicit pass / fail / leak with reasoning. PASS: zero retained EditorViews + zero leaked Scopes + zero leaked childLanguageTracker entries. FAIL or LEAK: any non-zero count → escalate to a P0 follow-up plan.
    - File length: at least 40 lines.
  </behavior>
  <action>
    Write `.planning/phases/17-polish-edge-cases/17-LIFE-SNAPSHOT.md` with the structure above. Embed the actual results from Task 3. If verdict is fail/leak, STOP and escalate to the user. The user may choose to ship with a known leak (and update CONTEXT.md / RETROSPECTIVE.md), or block the v1.2 release pending a fix plan.
  </action>
  <verify>
    <automated>test -f .planning/phases/17-polish-edge-cases/17-LIFE-SNAPSHOT.md && grep -c "Verdict" .planning/phases/17-polish-edge-cases/17-LIFE-SNAPSHOT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/17-polish-edge-cases/17-LIFE-SNAPSHOT.md` exists; `wc -l` >= 40
    - File contains literal headings `## Build under test`, `## Procedure`, `## Findings`, `## Phase 18 module-specific scrutiny`, `## Verdict`
    - Frontmatter contains `final_build_commit:` and `verdict:` keys
    - File contains a 5-row findings table covering EditorView retained/detached, Scope, WeakMap, vault.on listener
    - Verdict is explicit pass / fail / leak with reasoning
  </acceptance_criteria>
  <done>17-LIFE-SNAPSHOT.md committed with FINAL build heap-snapshot results, Phase 18 module scrutiny, and explicit lifecycle verdict.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Regression spot-check pass — re-run Tests 1, 9, 10, 12, 13, 14, 17, 18, 19, 23, 24, 25 on FINAL build</name>
  <what-built>
    Phase 18 source merges + bundle audit + heap-snapshot UAT all PASS. Final regression spot-check confirms the live UX.
  </what-built>
  <how-to-verify>
    With the FINAL v1.2 build deployed, run each test below per its existing 17-UAT.md `expected:` paragraph. For each: PASS / FAIL / N/A verdict + notes.

    **Test 1 — PASTE-01:** paste from VS Code into child editor; raw text preserved.
    **Test 9 — Phase 16 sanity regressions:** chevron switch, Cmd-/ toggle, bracket auto-close, indent rules.
    **Test 10 — RESET-01:** Cmd-P → Reset code; Cmd-Z scope correct.
    **Test 12 — fm reactivity (D-13/D-14):** change lc-language frontmatter; child reflects.
    **Test 13 — THEME-01 dark theme:** keywords, strings, comments, types all colored distinctly.
    **Test 14 — THEME-02 light theme:** bidirectional theme tracking.
    **Test 17 — VIM-01 (Plan 18-01 closure):** vim mode ON; click into child; press i → INSERT; press Esc → NORMAL; press j → cursor moves DOWN IN CHILD (NOT parent); press dd → line in CHILD doc deleted (NOT parent); press `:set nu` → gutter renders. Verify DOM probe `document.activeElement` is inside `.lc-nested-editor` throughout.
    **Test 18 — VIM-02:** Tab in vim Insert mode honors customTabCommand.
    **Test 19 — VIM-03:** Cmd-/ in both vim Insert + Normal modes.
    **Test 23 — REPAIR-02 (Plan 18-02 closure):** delete the fence closer line via vim's `dd` in Normal mode; observe in-place repair without reload (the new vault.on('modify') trigger fires repair). Then reload and verify no stale-language render (the new childLanguageTracker disagreement check forces a clean re-mount).
    **Test 24 — LINENUM-01 (Plan 17-12 baseline preserved):** Obsidian showLineNumber ON → gutter renders with absolute numbers (when relative is OFF).
    **Test 25 — LINENUM-RELATIVE-01 (Plan 18-03 closure):** plugin's new toggle ON + Obsidian showLineNumber ON → gutter renders relative numbers; cursor's line shows absolute; other lines show distance; toggling cursor moves re-renders.

    For Tests 17 / 23 / 25, the operator MUST verify the Phase 18 fix is observable in the live UX — these tests are the gate for shipping Phase 18.

    For Tests 1 / 9 / 10 / 12 / 13 / 14 / 18 / 19 / 24, the operator verifies prior PASS state holds — any FAIL is a P0 regression.

    The operator records each verdict directly in `.planning/phases/17-polish-edge-cases/17-UAT.md` (append a new section `## Phase 18 Regression Spot-Check (final v1.2 build)` at the bottom of the file with per-test result rows).
  </how-to-verify>
  <resume-signal>Type "approved" with all 12 verdicts recorded — OR describe failures + Claude pivots to a P0 follow-up plan</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Append Phase 18 regression spot-check section to 17-UAT.md</name>
  <files>.planning/phases/17-polish-edge-cases/17-UAT.md</files>
  <read_first>
    - Task 5 results (12 verdicts + notes)
    - .planning/phases/17-polish-edge-cases/17-UAT.md (current file — append, do NOT modify existing entries)
  </read_first>
  <behavior>
    - DO NOT modify any existing test entries in 17-UAT.md — they are the historical record from prior phases.
    - Append a new top-level section at the END of the file: `## Phase 18 Regression Spot-Check (final v1.2 build)`. Include a top header line: `Build commit: <SHA>` and `Date: 2026-05-24`.
    - For each of the 12 tests, write a row: `### {N}. {TestId} (final v1.2 build)\n result: {pass|fail|n/a}\n notes: <operator notes from Task 5>`.
    - At the end of the section, write a `## Phase 18 Spot-Check Verdict` heading with explicit OVERALL pass / fail / partial verdict — pass requires ALL 12 verdicts pass.
    - Update the frontmatter `summary:` block to add a new key: `phase_18_spot_check: pass|fail|partial`.
  </behavior>
  <action>
    Append the new section per the Behavior contract. Embed verdicts from Task 5. If any FAIL, STOP and surface to the user — likely triggers a P0 follow-up plan (Phase 18 Plan 05 OR a new Phase 18.1).
  </action>
  <verify>
    <automated>grep -c "Phase 18 Regression Spot-Check" .planning/phases/17-polish-edge-cases/17-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - 17-UAT.md contains the new heading `## Phase 18 Regression Spot-Check (final v1.2 build)`
    - The section contains 12 result rows (one per test)
    - The section ends with an overall verdict heading
    - Frontmatter `summary:` block has a new `phase_18_spot_check` key
    - Existing test entries (1-25) are unchanged
  </acceptance_criteria>
  <done>17-UAT.md updated with Phase 18 spot-check section; v1.2 ship-readiness gate documented.</done>
</task>

</tasks>

<verification>
- v1.2 ship-readiness checklist closed: bundle audit + heap-snapshot UAT + regression spot-check all PASS against the FINAL Phase 18 build
- Plan 17-06 deferred ship-readiness work (per CONTEXT D-31) is now complete
- Phase 18 fixes (18-01 vim Scope, 18-02 vault.on('modify') + stale-child invalidation, 18-03 relative line numbers) are confirmed live in the production-grade UX via Tests 17 / 23 / 25
- Bundle stays under the 1.8 MB ceiling (CONTEXT D-19); Phase 18 delta < 5 KB raw
- Heap snapshot shows zero retained EditorViews + zero leaked Scopes + zero leaked WeakMap entries (CONTEXT D-23 arm b)
- Zero source-level changes — `git diff src/ tests/` shows ZERO; only `.planning/` paths in the plan-level diff
- All Phase 17 + Phase 18 invariants preserved (section lock, sync annotations, ECHO_PRONE_USER_EVENTS, CLAUDE.md, package.json all unchanged)
</verification>

<success_criteria>
- `.planning/phases/17-polish-edge-cases/17-BUNDLE-AUDIT.md` exists with PASS verdict (raw < 1.8 MB)
- `.planning/phases/17-polish-edge-cases/17-LIFE-SNAPSHOT.md` exists with PASS verdict (zero retained EditorViews/Scopes/WeakMap entries)
- `.planning/phases/17-polish-edge-cases/17-UAT.md` has new Phase 18 Regression Spot-Check section with 12 verdicts; overall verdict PASS
- `git diff src/ tests/ package.json package-lock.json CLAUDE.md` shows ZERO changes (docs-only plan)
- The user has confirmed all 12 spot-check verdicts pass and the heap-snapshot UAT passes — explicit `approved` signals received for both checkpoint tasks
- Plan completes with no FAIL verdicts unrecorded
</success_criteria>

<output>
Create `.planning/phases/18-vim-recovery-polish/18-04-SUMMARY.md` when done summarizing:
- Three deliverable docs created/updated (paths + line counts)
- All 12 spot-check verdicts recorded
- Bundle size raw + gzipped + Phase 18 delta
- Heap-snapshot verdict (retained counts)
- Final v1.2 build commit SHA
- Confirmation that src/ tests/ package.json CLAUDE.md all show ZERO diff
- Cross-plan invariant verification: Plan 17-* and Plan 18-* invariants all preserved
- Phase 18 ship-readiness gate: PASS / FAIL → drives the v1.2 release decision
</output>
</content>
