---
phase: 18-vim-recovery-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/childEditorSync.ts
  - src/main.ts
  - tests/main/childEditorSync.repair.test.ts
autonomous: false
requirements: [REPAIR-02-RESILIENT]

must_haves:
  truths:
    - "PROBE FIRST. Before any source code change, the operator runs the §3.1 DevTools probe from 18-RESEARCH.md against the live vault TWICE: (a) BEFORE clicking the chevron (note open at Java), (b) AFTER chevron-switching to Python3 then to C. The probe captures the per-child tracker (Plan 17-09) value, the raw fence opener slug, the alias-normalized slug, and whether `.cm-content` exists post-switch. Operator pastes both sets of probe output BEFORE the auto-coding task runs. (Anti-pattern #1: 1738 unit tests didn't catch chevron-blank.)"
    - "DROP D-34. The previous Phase 18 attempt's `checkStaleChildAndInvalidate` registry-deletion path is FORBIDDEN to re-introduce. The function does not currently exist in main.ts (reverted in 98dec9b). This plan MUST NOT add it back, and MUST NOT add any registry-invalidation behavior that compares per-child tracker against the parent fence opener tag. Per Decision 1, rely entirely on Plan 17-09 (per-child childLanguageTracker WeakMap), Plan 17-12 (line-number gating at mount), and Plan 17-13 (parent-side updateListener-based repair) for legitimate stale-child cases."
    - "STALE-RENDER-AFTER-RELOAD covered by Plan 17-09 lifecycle (positive-evidence claim, REQUIRES UAT CONFIRMATION). ROADMAP Success Criterion 3 requires that after closing+reopening Obsidian on a previously-broken-fence note where parent doc + lc-language frontmatter agree (e.g., on Java), the child editor renders Java content (NOT stale Python3 content from a prior chevron switch before close). The new theory is: Plan 17-09's per-child `childLanguageTracker` WeakMap is keyed off the live `EditorView` instance — when Obsidian closes and reopens the note, the prior child's `EditorView` is destroyed and a fresh one is constructed; the WeakMap entry for the prior view is GC-reclaimed and the new view enters with no tracker entry, forcing `buildNestedDecorations` to read the current fence opener as authoritative. Therefore D-34's registry-deletion gate is unnecessary — the lifecycle naturally cures stale-render-after-reload. THIS CLAIM MUST BE CONFIRMED BY TASK 3'S RELOAD UAT STEP. If UAT shows stale Python3 content rendering after reload (instead of Java matching `lc-language` frontmatter), the claim is FALSIFIED, the plan FAILS, and the failure must be documented as a regression that requires immediate attention — NOT silently passed."
    - "ADD vault.on('modify') trigger ONLY (D-33). The single new mechanism is a `this.app.vault.on('modify', file => { ... })` listener registered at plugin `onload`, wrapped in `this.registerEvent(...)` for auto-cleanup. The listener gates on: (1) file is a TFile and matches the active MarkdownView's file (the active LC problem note); (2) the file's `lc-slug` frontmatter is present (per D-33); (3) `findCodeFence(activeView.editor.cm.state) === null` — same gate Plan 17-13's `createParentRepairExtension` uses. When all three gates pass, the listener calls `repairFenceStructure(activeView.editor.cm, activeSlug)` where `activeSlug` is read from the parent doc's `lc-language` frontmatter (matching createParentRepairExtension's helper). The repair dispatch sets `userEvent: 'leetcode.fence-repair'` (already done inside `repairFenceStructure`) and the existing 'leetcode.fence-repair' entry in ECHO_PRONE_USER_EVENTS prevents re-mirroring."
    - "Critical mid-flight gate: the listener MUST short-circuit during `switchFenceLanguage`'s in-flight transaction. The chevron's path at src/main.ts:2451-2458 dispatches a single CM6 transaction (atomic opener + body rewrite); Step C at line 2476 is the `processFrontMatter` write that fires `vault.on('modify')`. At that moment the parent CM6 state has the fence INTACT (because Step B already wrote it). The `findCodeFence(...) !== null` gate naturally short-circuits this case — the listener fires only when CM6 actually shows a damaged fence. The probe in §3.1 must confirm `findCodeFence` returns non-null at the metadataCache.changed moment of every chevron switch including python3 and c. Acceptance criteria require this verification in the post-coding live UAT step."
    - "The repair dispatch MUST go through the parent CM6 view, NOT through `vault.process(...)`. Per CLAUDE.md Phase 17 D-05 canonical write-path pattern + 18-RESEARCH §5 invariant 8: the new vault.on('modify') trigger calls `repairFenceStructure(parentView, activeSlug)` which internally uses `parentView.dispatch`. Calling `vault.process` mid-vault-event would corrupt CM6 state. Acceptance criteria forbid `vault.process` appearing in the new repair-trigger code path."
    - "Idempotency preserved (D-33). `repairFenceStructure` is already idempotent — when fence is intact, it returns false (no dispatch). The `findCodeFence !== null` gate in the listener wrapper provides a second short-circuit. The 'leetcode.fence-repair' userEvent stays in ECHO_PRONE_USER_EVENTS at src/main/nestedEditorExtension.ts:273-276 (NO change to that set; verify in acceptance)."
    - "Reproduction case verified: vim's `dd` Normal-mode delete on the fence closer line — currently bypasses CM6 transactions because Obsidian's app-level vim handler edits via vault layer. After this plan's vault.on('modify') trigger, that path's resulting file write fires the listener, the listener detects `findCodeFence === null` on the parent CM6 state (because vim's vault-layer write reflows the CM6 doc — Obsidian's vault → CM6 sync is synchronous for active files), and `repairFenceStructure` re-inserts the closer. Operator's live UAT verifies: open a problem note, vim-mode `:1G` then navigate to the ` ``` ` closer line, press `dd`, observe the closer reappears in-place within ~100ms (no reload required)."
    - "All 1713 existing unit tests remain green. New unit tests added to `tests/main/childEditorSync.repair.test.ts` for the vault.on('modify') trigger path: (1) listener is registered at onload; (2) listener short-circuits when active file does NOT have lc-slug frontmatter; (3) listener short-circuits when `findCodeFence` returns non-null; (4) listener calls `repairFenceStructure` when `lc-slug` present + `findCodeFence` null; (5) listener uses `parentView.dispatch` (NOT `vault.process`)."
    - "Bundle delta < 0.5 KB raw. The 1.8 MB ceiling (CONTEXT D-19) is preserved."
    - "Preserve: CLAUDE.md Conventions section unchanged. ECHO_PRONE_USER_EVENTS at src/main/nestedEditorExtension.ts:273-276 unchanged (only `'leetcode.child-sync'` and `'leetcode.fence-repair'`). The existing `createParentRepairExtension` at src/main/childEditorSync.ts:385-422 is NOT removed — the new vault.on path is ADDITIVE, covering the non-CM6-transaction gap that createParentRepairExtension cannot observe. `'leetcode.reset.child'` does NOT get added to ECHO_PRONE_USER_EVENTS."
  artifacts:
    - path: "src/main/childEditorSync.ts"
      provides: "Adds NEW exported function `createVaultModifyRepairTrigger(plugin: PluginHost): EventRef-returning helper or factory` that registers the `vault.on('modify')` listener and returns the unsubscribe handle. The helper does the lc-slug + findCodeFence gating and calls repairFenceStructure on the active MarkdownView's CM6. Existing exports (`createParentRepairExtension`, `repairFenceStructure`, `wireSyncIfNeeded`, etc.) unchanged."
      contains: "createVaultModifyRepairTrigger|vault\\.on.*'modify'|repairFenceStructure"
    - path: "src/main.ts"
      provides: "In `onload()`, after the existing `metadataCache.on('changed')` registration at line 919-921, register the new vault-modify repair trigger via `this.registerEvent(this.app.vault.on('modify', ...))` calling into the new helper from childEditorSync.ts. NO new userEvent string introduced. NO `checkStaleChildAndInvalidate` function added (Decision 1)."
      contains: "vault\\.on.*'modify'|createVaultModifyRepairTrigger"
    - path: "tests/main/childEditorSync.repair.test.ts"
      provides: "Extended with vault.on('modify') trigger path tests. Existing `createParentRepairExtension` tests unchanged."
      contains: "vault\\.on|createVaultModifyRepairTrigger|describe.*vault.*modify"
      min_lines: 80
  key_links:
    - from: "src/main.ts onload()"
      to: "src/main/childEditorSync.ts createVaultModifyRepairTrigger"
      via: "this.registerEvent(this.app.vault.on('modify', ...))"
      pattern: "vault\\.on\\(['\"]modify['\"]"
    - from: "createVaultModifyRepairTrigger handler"
      to: "src/main/childEditorSync.ts repairFenceStructure"
      via: "repairFenceStructure(parentView, activeSlug) — NOT vault.process"
      pattern: "repairFenceStructure\\("
---

<objective>
Close UAT Test 23 (REPAIR-02) by adding a `vault.on('modify')` listener that triggers `repairFenceStructure` for active LC problem notes when the parent CM6 state shows a damaged fence (`findCodeFence === null`). Specifically covers the gap where vim's `dd` Normal-mode keystroke on the fence closer line edits the doc via Obsidian's vault layer, bypassing CM6 transactions that the existing `createParentRepairExtension` updateListener observes. Decision 1 forbids re-introducing the previous attempt's `checkStaleChildAndInvalidate` registry-deletion path — Plan 17-09 + 17-12 + 17-13 already cover the legitimate stale-child cases.

Purpose: Restores the v1.2 contract that fence auto-recovery fires regardless of how the closer was deleted (CM6 transaction OR vault-layer edit OR external editor save). Closes the chevron-blank-on-python3-c regression by NOT introducing the registry-invalidation gate that caused it.

Output: Modified `src/main/childEditorSync.ts` (new exported helper); modified `src/main.ts` (one `registerEvent` call in onload); extended `tests/main/childEditorSync.repair.test.ts`; 1713+ tests green; chevron switch through ALL 8 LC languages confirmed working in live UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-vim-recovery-polish/CONTEXT.md
@.planning/phases/18-vim-recovery-polish/18-RESEARCH.md
@.planning/phases/18-vim-recovery-polish/.continue-here.md
@.planning/debug/phase-18-regressions-2026-05-25.md
@.planning/debug/chevron-switch-child-body-stale.md
@CLAUDE.md
@src/main/childEditorSync.ts
@src/main/nestedEditorExtension.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase. -->

From src/main/childEditorSync.ts:385-422 (createParentRepairExtension — the canonical CM6-update reference):
```typescript
export function createParentRepairExtension(): Extension {
  return EditorView.updateListener.of((update) => {
    if (!update.docChanged) return;
    if (update.transactions.some(
      (tr) => tr.annotation(Transaction.userEvent) === 'leetcode.fence-repair',
    )) return;
    if (findCodeFence(update.state) !== null) return;
    const activeSlug = readLcLanguageFromDoc(update.state) ?? 'python3';
    repairFenceStructure(update.view, activeSlug);
  });
}
```
The new `createVaultModifyRepairTrigger` mirrors this gate sequence (lc-slug, fence-damage, fallback slug) but reads from the active MarkdownView's CM6 state at the moment vault.on('modify') fires, not from a CM6 update transaction.

From src/main/childEditorSync.ts:439-456 (readLcLanguageFromDoc — already exported within the file scope; reuse for activeSlug derivation):
```typescript
function readLcLanguageFromDoc(state: import('@codemirror/state').EditorState): string | null
```
The new helper can reuse this OR read `lc-language` from the metadataCache directly via the plugin handle.

From src/main/childEditorSync.ts:488 (repairFenceStructure — the actual repair function):
```typescript
export function repairFenceStructure(parentView: EditorView, activeSlug: string = 'python3'): boolean
```

From src/main.ts:919-921 (existing onload metadataCache registration — the precedent for adding the new vault.on registration):
```typescript
this.registerEvent(
  this.app.metadataCache.on('changed', (file, _data, cache) => {
    this.handleFmChangeForLanguageReactivity(file, cache);
  }),
);
```

From src/main.ts:2451-2458 (chevron's atomic CM6 dispatch — mid-flight context for understanding when vault.on fires):
```typescript
cm.dispatch({
  changes: [openerChange, bodyChange],
  effects: languageRefreshEffect.of(newSlug),
  userEvent: 'leetcode.lang-switch',
});
// ... line 2476 Step C — processFrontMatter (this is what fires vault.on('modify'))
```

From src/main/nestedEditorExtension.ts:273-276 (ECHO_PRONE_USER_EVENTS — DO NOT MODIFY):
```typescript
const ECHO_PRONE_USER_EVENTS = new Set([
  'leetcode.child-sync',
  'leetcode.fence-repair',
]);
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live DevTools probe — confirm chevron-blank does NOT regress; chevron path is healthy on baseline</name>
  <read_first>
    - .planning/phases/18-vim-recovery-polish/18-RESEARCH.md (§3.1 — the probe snippet)
    - .planning/debug/phase-18-regressions-2026-05-25.md (the chevron-blank failure mode)
    - ~/.claude/projects/-Users-moxu-projects-obsidian-leetcode/memory/vault_plugin_path.md (vault path is `obsidian-leetcode/`)
  </read_first>
  <what-built>NOT YET. The operator runs the §3.1 chevron probe against the Phase 17 round-3 baseline (HEAD `692ed26`) — the chevron-blank bug is REVERTED so chevron switch should work cleanly across all 8 languages including python3 and c. This baseline check is essential because the new vault.on('modify') trigger MUST NOT reintroduce a chevron-blank.</what-built>
  <how-to-verify>
    1. Verify dev vault has the Phase 17 round-3 baseline build (no Phase 18 source changes deployed yet): `app.plugins.manifests.leetcode.version` in DevTools console.
    2. Open a LeetCode problem note (Java; `lc-language: java`).
    3. Open Obsidian DevTools (Cmd-Opt-I) → Console.
    4. Paste the §3.1 probe snippet from 18-RESEARCH.md verbatim, press Enter. Capture the BEFORE-click output (9 logged lines).
    5. Click the language chevron, pick **Python3**. Wait for the language switch to settle (~500ms). Re-run the same probe snippet. Capture the AFTER-python3 output. The critical assertions: `child .cm-content : true` (NOT null), `child doc preview` shows the Python3 starter snippet (not empty).
    6. Click chevron again, pick **C**. Re-run probe. Capture AFTER-c output. Same assertions.
    7. Click chevron, pick back to **Java**. Re-run probe. Confirm the round-trip works.
    8. **Critical sanity check**: at no point should `child .cm-content` show `false` post-switch. If it does, the baseline itself is broken (revert was incomplete) — pause this plan and re-verify the baseline before proceeding.
    9. Now reproduce the OTHER bug this plan addresses: enable Obsidian's global vim mode (Editor → Vim key bindings). In a fresh problem note, click into the parent doc (NOT the child editor). Move the cursor to the fence closer line (the bare ` ``` ` at the bottom of the Code section — viewable when the child editor is unfocused, the closer line is visible in the parent's Source Mode if you toggle Cmd-E). In Source Mode, press `Esc` to enter Normal mode (parent's vim), navigate to the closer line via `j`/`k`, press `dd`. Observe: the closer is deleted, the fence is now structurally broken, and on the Phase 17 round-3 baseline NO auto-recovery fires (the fence stays broken until reload). Capture this as confirmation that the gap exists.
  </how-to-verify>
  <resume-signal>
    Paste:
    (a) the four §3.1 probe outputs (before-click, after-python3, after-c, after-java-roundtrip);
    (b) confirmation that `child .cm-content` is `true` in all four (chevron-blank does NOT reproduce on baseline);
    (c) confirmation that vim-`dd` on the closer line on baseline leaves the fence broken without auto-recovery.

    Then type "approved — proceed with vault.on('modify') trigger". If `child .cm-content` is `false` anywhere, type "baseline broken — pause" and Claude diagnoses before continuing.
  </resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement vault.on('modify') repair trigger + tests</name>
  <files>src/main/childEditorSync.ts, src/main.ts, tests/main/childEditorSync.repair.test.ts</files>
  <read_first>
    - src/main/childEditorSync.ts (entire file — pay attention to lines 385-422 createParentRepairExtension which is the gate-sequence template; lines 488-614 repairFenceStructure which is what the new helper calls into; lines 439-456 readLcLanguageFromDoc for activeSlug derivation)
    - src/main.ts (lines 295-330 onload structure; lines 857-921 existing registerEvent registrations — the pattern; lines 2386-2479 switchFenceLanguage to understand what fires vault.on('modify') during chevron; lines 2674-2705 readActiveFenceSlug for plugin-level slug reading patterns)
    - src/main/nestedEditorExtension.ts (lines 273-276 ECHO_PRONE_USER_EVENTS — verify NOT modified; lines 50-60 PluginHost type — the helper signature uses this or a narrower subset)
    - tests/main/childEditorSync.repair.test.ts (existing patterns for mocking parentView, repairFenceStructure spy, and findCodeFence)
    - tests/main/switchFenceLanguage.test.ts (chevron-path tests — extend to add a python3 + c regression test asserting child registry is NOT deleted post-switch)
    - .planning/phases/18-vim-recovery-polish/18-RESEARCH.md (§4.2 — recommended approach for vault.on listener; §6.4 — Wave 0 gaps including the chevron round-trip test)
  </read_first>
  <behavior>
    - Test 1 (lc-slug gate): given a vault.on('modify') event for a TFile whose metadataCache lacks `lc-slug` frontmatter, the new helper short-circuits — no call to `repairFenceStructure`, no call to `findCodeFence`. Mock metadataCache.getFileCache to return `{frontmatter: {}}`.
    - Test 2 (active-view gate): given a vault.on('modify') event for a TFile that is NOT the active MarkdownView's file (different file path), the helper short-circuits.
    - Test 3 (fence-healthy gate): given an active LC problem note where parent CM6 state shows `findCodeFence !== null`, the helper short-circuits even when lc-slug is present. Mock `findCodeFence` returning a valid fence object.
    - Test 4 (mid-flight chevron gate, integration): given the chevron switch path's vault.on('modify') firing at Step C (processFrontMatter write at src/main.ts:2476), `findCodeFence` returns the freshly-rewritten fence (non-null), so the helper short-circuits. This is the test that proves the new trigger does NOT cause chevron-blank. Mock the chevron switch sequence: dispatch the atomic CM6 transaction first, then fire vault.on('modify') via a triggered event, then assert no `repairFenceStructure` call.
    - Test 5 (vim-dd path, integration): given an active LC problem note where the parent CM6 state shows `findCodeFence === null` (closer missing), the helper calls `repairFenceStructure(parentView, activeSlug)` exactly once. Mock `findCodeFence` returning null; mock parentView.dispatch as a spy.
    - Test 6 (no vault.process): the helper does NOT call `app.vault.process(...)` or any vault-layer write. Repair flows through `parentView.dispatch` only (per CLAUDE.md Phase 17 D-05 + 18-RESEARCH §5 invariant 8). Verify by spy on `app.vault.process` (assert called 0 times).
    - Test 7 (cleanup): the listener is registered via `plugin.registerEvent(...)` so it auto-detaches on unload. Verify by checking the registered EventRef is added to plugin's event registry (or by spy on registerEvent).
  </behavior>
  <action>
    1. **Add new exported helper to `src/main/childEditorSync.ts`** at end of file (after `repairFenceStructure`):
       ```
       export interface VaultModifyRepairPluginHost {
         app: import('obsidian').App;
         registerEvent(eventRef: import('obsidian').EventRef): void;
       }
       export function registerVaultModifyRepairTrigger(plugin: VaultModifyRepairPluginHost): void
       ```
       (Action is directive prose; the executor writes the body per these rules.)
       Implementation rules:
       - Body calls `plugin.registerEvent(plugin.app.vault.on('modify', (file) => { ... }))`.
       - Inside the handler:
         - Cast `file` to TFile via `import('obsidian').TFile`.
         - Read `const cache = plugin.app.metadataCache.getFileCache(file); const fm = cache?.frontmatter as Record<string, unknown> | undefined; const lcSlug = fm?.['lc-slug']; if (typeof lcSlug !== 'string' || lcSlug.length === 0) return;`
         - Read `const activeView = plugin.app.workspace.getActiveViewOfType(import('obsidian').MarkdownView)`. If null, return. If `activeView.file !== file`, return.
         - Read `const cm = (activeView.editor as unknown as { cm: import('@codemirror/view').EditorView }).cm;`
         - Call `findCodeFence(cm.state)` (import the function or use the existing import in the file). If non-null, return — fence is intact, no repair needed.
         - Read `activeSlug` from frontmatter: `const lcLang = fm?.['lc-language']; const activeSlug = (typeof lcLang === 'string' && lcLang.length > 0) ? lcLang : 'python3';`
         - Call `repairFenceStructure(cm, activeSlug)`.
       - Idempotency: `repairFenceStructure` is already idempotent (returns false when fence is intact); the `findCodeFence !== null` short-circuit in the wrapper provides a second guard.
       - DO NOT call `plugin.app.vault.process(...)`. DO NOT introduce a registry-invalidation step. DO NOT compare any tracker slug against the parent fence opener.

    2. **Wire into `src/main.ts onload()`**. Find the existing `this.registerEvent(this.app.metadataCache.on('changed', ...))` block at lines 919-921. AFTER that block, add a new line:
       ```
       registerVaultModifyRepairTrigger(this);
       ```
       Add the import at the top of the file: `import { registerVaultModifyRepairTrigger } from './main/childEditorSync';`. (Existing imports from `./main/childEditorSync` likely exist — extend the import list rather than duplicating.)
       The `LeetCodePlugin` class extends `Plugin` (which provides `app` and `registerEvent`), so it satisfies `VaultModifyRepairPluginHost` without explicit conformance.

    3. **Extend `tests/main/childEditorSync.repair.test.ts`** with a new describe block `describe('registerVaultModifyRepairTrigger', () => { ... })`. Cover the seven behavior assertions above. Use vitest. Mock `plugin.app.vault.on`, `plugin.app.metadataCache.getFileCache`, `plugin.app.workspace.getActiveViewOfType`, and `findCodeFence` (via vi.mock or a stub on the imported binding). Spy on `plugin.app.vault.process` to assert NEVER called. Spy on the helper's call to `repairFenceStructure` (may need to extract the inner repair call into a parameter for testability, or import-mock).

    4. **Add chevron-regression test to `tests/main/switchFenceLanguage.test.ts`** (file already exists). Add a describe block: `describe('chevron switch through python3 + c — chevron-blank regression', () => { ... })`. Test: (a) run a chevron switch to python3 — assert that AFTER the dispatch + processFrontMatter, the child registry entry for the file path STILL exists (`registry.get(filePath) !== undefined`); (b) same for c; (c) assert no `registry.delete` calls during the chevron path (spy on `childEditorRegistry.delete`).

    5. **Verify forbidden symbols are absent**: `grep -nE "(checkStaleChildAndInvalidate|registry\\.delete)" src/main.ts src/main/childEditorSync.ts` returns NO matches in code (only test-file delete-spy assertions allowed). `grep -n "ECHO_PRONE_USER_EVENTS" src/main/nestedEditorExtension.ts` shows the set is unchanged.

    6. Run `npx vitest run --reporter=dot`. All 1713+ existing tests must remain green. The new tests must pass.

    7. Run `npm run build`. Bundle must stay < 1.8 MB raw.
  </action>
  <verify>
    <automated>npx vitest run --reporter=dot && npm run build && raw=$(wc -c < main.js) && test "$raw" -lt 1800000 && echo "BUNDLE OK ($raw bytes)" && (grep -nE "(checkStaleChildAndInvalidate)" src/main.ts src/main/childEditorSync.ts 2>/dev/null && echo "FORBIDDEN checkStaleChildAndInvalidate PRESENT" || echo "checkStaleChildAndInvalidate ABSENT OK") && (grep -n "registry\\.delete" src/main/childEditorSync.ts src/main.ts 2>/dev/null | grep -v "test\\." && echo "FORBIDDEN registry.delete in code" || echo "no registry.delete in code OK") && grep -n "registerVaultModifyRepairTrigger" src/main.ts src/main/childEditorSync.ts | wc -l | grep -E "^[2-9]" && echo "WIRED OK"</automated>
  </verify>
  <acceptance_criteria>
    - **Behavior**: vim's `dd` on the fence closer line of an active LC problem note triggers `repairFenceStructure` within ~100 ms via the new vault.on('modify') listener; chevron switch through ALL 8 LC languages (especially python3 + c) does NOT trigger `repairFenceStructure` (because findCodeFence stays non-null).
    - **Test command**: `npx vitest run --reporter=dot` exits 0 with 1713+ green tests, including the new vault.on path tests + new chevron-regression tests.
    - **CLI**: `npm run build` exits 0; `wc -c < main.js` shows raw < 1,800,000.
    - **Source**: `grep -nE "(checkStaleChildAndInvalidate|triggerRepairFromVaultModify)" src/main.ts src/main/childEditorSync.ts` returns NO matches (Decision 1 — those reverted symbols stay reverted).
    - **Source**: `grep -n "registry\\.delete" src/main/childEditorSync.ts src/main.ts` returns NO matches in non-test code (the previous chevron-blank-causing path).
    - **Source**: `grep -n "ECHO_PRONE_USER_EVENTS" src/main/nestedEditorExtension.ts` confirms set has exactly two members: `'leetcode.child-sync'` and `'leetcode.fence-repair'` (no addition of `'leetcode.reset.child'`).
    - **Source**: `grep -n "vault\\.process" src/main/childEditorSync.ts | grep -i "registerVaultModifyRepairTrigger\\|vault\\.on.*modify"` returns NO matches inside the new function body (the new repair path uses parentView.dispatch only — Phase 17 D-05).
    - **Source**: `grep -n "registerVaultModifyRepairTrigger" src/main.ts src/main/childEditorSync.ts` shows at least 2 matches (export in childEditorSync.ts + import + onload call in main.ts).
    - **Behavior**: existing `createParentRepairExtension` at src/main/childEditorSync.ts:385-422 is UNCHANGED — the new vault.on path is additive.
  </acceptance_criteria>
  <done>Source change committed. Test suite green. No forbidden symbols. Bundle under ceiling. Ready for live UAT (Task 3).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live UAT — chevron all 8 languages + vim-dd repair + reload-after-chevron stale-render check</name>
  <read_first>
    - .planning/phases/17-polish-edge-cases/17-UAT.md (Test 23 REPAIR-02 — the specific reproduction steps)
    - The Task 2 commit summary (in conversation context)
    - .planning/debug/phase-18-regressions-2026-05-25.md (the chevron-blank failure mode this plan must NOT reintroduce)
    - .planning/ROADMAP.md (Phase 18 Success Criterion 3 — the stale-render-after-reload scenario)
    - ~/.claude/projects/-Users-moxu-projects-obsidian-leetcode/memory/vault_plugin_path.md (deploy path)
  </read_first>
  <what-built>The vault.on('modify') repair trigger from Task 2 is committed and built. The plugin needs to be deployed to the dev vault and verified live by the operator across THREE scenarios: (a) chevron through all 8 languages with NO chevron-blank; (b) vim-dd on closer line auto-recovers within ~100ms; (c) ROADMAP Success Criterion 3 — close+reopen Obsidian after a chevron switch, confirm child renders the language matching `lc-language` frontmatter (NOT stale pre-close content). Scenario C tests the must_haves positive-evidence claim that Plan 17-09's per-child language tracker WeakMap lifecycle naturally cures stale-render-after-reload (D-34 dropped).</what-built>
  <how-to-verify>
    1. Deploy: copy `main.js`, `manifest.json`, `styles.css` to `~/Documents/Obsidian Vault/.obsidian/plugins/obsidian-leetcode/`. Verify path with `app.plugins.manifests.leetcode.dir`.
    2. Reload Obsidian (Cmd-R or Cmd-P → "Reload app without saving").
    3. **Scenario A — chevron does NOT regress:**
       a. Open a LeetCode problem note (any one). Note its initial language.
       b. Click the chevron, switch to **java**. Wait 500ms. Confirm: child editor renders Java starter code (NOT blank).
       c. Switch to **javascript**. Confirm Code section visible.
       d. Switch to **typescript**. Confirm.
       e. Switch to **python3** (the previously-failing case #1). Confirm Code section is NOT blank — `.cm-content` exists, Python starter visible.
       f. Switch to **c** (the previously-failing case #2). Confirm Code section is NOT blank — C starter visible.
       g. Switch to **cpp**. Confirm.
       h. Switch to **golang**. Confirm.
       i. Switch to **rust**. Confirm.
       j. Round-trip back through python3 → java → c → java to confirm no cumulative regression.
       k. Re-run §3.1 probe: confirm `trackedSlug` matches the fmLang for each switch; `child .cm-content : true` everywhere.
    4. **Scenario B — vim-dd auto-recovery:**
       a. Verify Obsidian global vim mode is ON.
       b. Open a problem note. Toggle to Source Mode (Cmd-E) so the fence ` ``` ` markers are visible in the parent doc.
       c. Click into the parent doc (NOT the child editor).
       d. Press `Esc` to enter Normal mode. Navigate via `j`/`k`/`G` to the line containing the bare ` ``` ` closer (last line of the Code fence).
       e. Press `dd`. Observe: the closer line is deleted.
       f. **Within ~100ms**, the closer is auto-restored by `repairFenceStructure` via the new vault.on('modify') trigger. Visual confirmation: the ` ``` ` line reappears on the same row.
       g. Repeat the same test BUT in Live Preview mode (Cmd-E to toggle out of Source Mode). The trigger should fire identically.
       h. Edge case: with a healthy fence already in place, type a single character in the `## Notes` section. The vault.on('modify') listener fires but should short-circuit (because findCodeFence is non-null). Verify: the Code section is unaffected, no spurious dispatches.
    5. **Scenario C — ROADMAP Success Criterion 3: stale-render-after-reload (NEW — confirms must_haves positive-evidence claim):**
       a. Open a Java LeetCode problem note (one whose `lc-language: java` frontmatter agrees with the parent doc fence opener `` ```java ``).
       b. Open DevTools → Console. Run §3.1 probe. Capture: `fmLang` = java, fence opener slug = java, `trackedSlug` = java (or null if first open), `child .cm-content : true`, `child doc preview` = Java starter.
       c. Click the chevron, switch to **Python3**. Wait ~500ms for the switch to settle. Re-run §3.1 probe. Capture: `fmLang` = python3, fence opener slug = python3, `trackedSlug` = python3, child renders Python3 starter.
       d. Now click the chevron AGAIN, switch BACK to **Java**. This is the key setup: the note ends up with `lc-language: java` + parent fence opener `` ```java `` agreeing — but the child editor has been through a Python3 mount cycle. Re-run §3.1 probe. Capture: child renders Java starter; tracker reports java.
       e. **Close Obsidian completely** (Cmd-Q on macOS, full quit — NOT just close window). Wait 5 seconds.
       f. **Reopen Obsidian.** Wait for the workspace to fully restore (the same problem note should reopen automatically via Obsidian's "Open last opened file" behavior; if not, manually open it).
       g. After the note is reopened, open DevTools → Console. Run §3.1 probe IMMEDIATELY (do NOT click into the child first — the goal is to observe the freshly-mounted child's state).
       h. **Critical assertion**: child renders the language matching `lc-language` frontmatter at reopen time. Specifically, with `lc-language: java` and parent fence opener `` ```java ``, child `.cm-content` MUST show the Java starter code (NOT stale Python3 content from step c). Capture the probe output AND a visual screenshot/text-paste of the first 5 lines of `child doc preview`.
       i. **Decision branch:**
          - If child renders **Java** (matching frontmatter): PASS. The must_haves positive-evidence claim is confirmed — Plan 17-09's per-child WeakMap lifecycle naturally cures stale-render-after-reload. D-34 dropped is validated. Document this as a positive UAT artifact.
          - If child renders **Python3 or any other stale content**: FAIL. The positive-evidence claim is FALSIFIED. Do NOT silently pass. Type the exact failure mode in the resume-signal so the operator can decide whether to (i) revive D-34 in a follow-up plan, (ii) fall back to a `processStaleChildOnReopen` mechanism in Plan 17-09's mount path, or (iii) defer the fix to a later phase. Capture the §3.1 probe output, the actual rendered language, and the observed `trackedSlug`/fmLang/fence-opener triple — these tell the diagnosis story.
       j. **Variant check** (only if step h passes): repeat steps a-h but in reverse — open a Python3 problem note, chevron-switch to Java, then Python3 again, close Obsidian, reopen, confirm child renders Python3 (matching frontmatter). This catches the symmetric direction.
    6. **DevTools console check** (listener registration): open DevTools, paste:
       ```
       (function() {
         const handlers = window.app.vault._?.['modify'] || [];
         console.log('vault modify listeners:', handlers.length);
       })();
       ```
       Confirm at least 1 listener is registered (the new one). After plugin unload+reload, count should be unchanged (no leak).
    7. **No regressions to other features**: open and close 5 different problem notes. Type in the child editor, run a build (Cmd-Enter Run/Submit) — confirm no console errors, no broken behavior.
  </how-to-verify>
  <resume-signal>
    Paste:
    (a) for each of the 8 languages in Scenario A: PASS / FAIL verdict + post-switch `child .cm-content` boolean from probe;
    (b) for Scenario B steps a–g: PASS / FAIL verdict + observed delay between dd and closer restoration;
    (c) for Scenario C — ROADMAP Success Criterion 3 stale-render-after-reload:
       - the probe output BEFORE close (step d): `fmLang`, `trackedSlug`, child-doc-preview-first-line;
       - the probe output AFTER reopen (step g): `fmLang`, `trackedSlug`, child-doc-preview-first-line;
       - explicit verdict: "Java rendered (PASS — positive-evidence claim confirmed)" OR "stale Python3 rendered (FAIL — positive-evidence claim FALSIFIED, D-34 follow-up needed)";
       - if PASS: confirm variant check (step j) also passes;
       - if FAIL: paste the §3.1 probe output triple (`trackedSlug`, fmLang, fence opener slug) to inform follow-up diagnosis;
    (d) DevTools listener count from step 6;
    (e) any console errors during step 7.

    Then type "approved" if all PASS, OR describe failures. Critical FAIL conditions:
    - any chevron switch to python3 or c shows blank Code section (= chevron-blank regression — block ship);
    - vim-dd on closer doesn't auto-restore within ~500ms (= the new trigger isn't firing);
    - any spurious dispatches (= gate is too loose);
    - **Scenario C step h shows stale content after reopen (= ROADMAP Success Criterion 3 violated — DO NOT silently pass; surface the failure as a regression for D-34 follow-up).**
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Obsidian vault layer → file modifications | vim's Ex commands and external editors edit files via vault, bypassing CM6 transactions |
| vault.on('modify') event → repair trigger | the new helper observes vault-layer writes and reacts |
| repair trigger → parent CM6 dispatch | `repairFenceStructure(parentView, ...)` writes via CM6, NOT vault.process |
| Obsidian app lifecycle (close → reopen) → child editor remount | Plan 17-09 per-child WeakMap is GC-reclaimed; new EditorView reads frontmatter as authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | Tampering | vault.on('modify') firing during chevron mid-flight could cause spurious repair dispatches | mitigate | Three gates: (1) lc-slug present; (2) active-view file matches modified file; (3) findCodeFence === null. Chevron's atomic dispatch leaves findCodeFence non-null at processFrontMatter time, so the gate short-circuits. Live UAT step 3 verifies. |
| T-18-06 | Tampering | Re-introducing checkStaleChildAndInvalidate would re-cause chevron-blank | mitigate | Forbidden-symbol grep gate in acceptance criteria; Decision 1 enforcement in must_haves; live UAT step 3 verifies all 8 languages. |
| T-18-07 | Denial-of-service | vault.on listener leaking on plugin unload | mitigate | `this.registerEvent(...)` wrapping ensures auto-cleanup; live UAT step 6 spot-checks listener count. |
| T-18-08 | Information disclosure | None | accept | No PII or credentials touched. |
| T-18-13 | Tampering | Stale-render-after-reload (ROADMAP Success Criterion 3) — D-34 dropped on positive-evidence claim that Plan 17-09 lifecycle covers it | mitigate | Live UAT Scenario C explicitly tests close+reopen after chevron and asserts child renders the frontmatter-matching language. If FAILS, surface as regression (NOT silent pass) so D-34 follow-up can be planned. |
</threat_model>

<verification>
- `npx vitest run --reporter=dot` — 1713+ green
- `npm run build` — exit 0; `main.js` < 1.8 MB raw
- `grep -nE "(checkStaleChildAndInvalidate|triggerRepairFromVaultModify)" src/main.ts src/main/childEditorSync.ts` — no matches
- `grep -n "registry\\.delete" src/main/childEditorSync.ts src/main.ts` — no matches in non-test code
- `grep -n "ECHO_PRONE_USER_EVENTS" src/main/nestedEditorExtension.ts` — set unchanged (2 members)
- Operator-pasted §3.1 probe outputs from Tasks 1 + 3 confirm no chevron-blank in any of 8 languages
- Operator-confirmed live UAT for Test 23 (REPAIR-02): vim-dd on closer line auto-recovers
- Operator-confirmed live UAT for ROADMAP Success Criterion 3 Scenario C: child renders frontmatter-matching language after Obsidian close+reopen (positive-evidence claim for dropped D-34 confirmed, OR failure documented for follow-up)
</verification>

<success_criteria>
- REPAIR-02-RESILIENT satisfied: vault.on('modify') trigger fires `repairFenceStructure` for active LC problem notes when parent CM6 fence is damaged
- Chevron switch through ALL 8 LC languages produces NO blank Code section (chevron-blank-on-python3-c regression does NOT recur)
- Decision 1 enforced: no `checkStaleChildAndInvalidate`; no registry.delete in production code
- Decision 4 partial: bundle under 1.8 MB
- Phase 17 D-05 canonical write-path pattern preserved: new repair path uses parentView.dispatch, not vault.process
- All 1713 existing tests green; new vault.on path tests + chevron-regression tests added and green
- 17-UAT.md Test 23 (REPAIR-02) confirmed PASS by live operator UAT
- ROADMAP Success Criterion 3 confirmed PASS by live operator UAT Scenario C: after Obsidian close+reopen on a previously-chevron-switched note, child renders the language matching `lc-language` frontmatter (validating must_haves positive-evidence claim that Plan 17-09 per-child WeakMap lifecycle covers stale-render-after-reload, so D-34 stays dropped). If FAILS, the failure is surfaced as a regression — NOT silently passed.
</success_criteria>

<output>
Create `.planning/phases/18-vim-recovery-polish/18-02-SUMMARY.md` when done.
</output>
</content>
