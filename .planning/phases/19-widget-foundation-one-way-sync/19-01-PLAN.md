---
phase: 19-widget-foundation-one-way-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widget/codeBlockProcessor.ts
  - src/widget/liveModeViewPlugin.ts
  - src/widget/LeetCodeFenceWidget.ts
  - src/widget/WidgetController.ts
  - src/widget/widgetRegistry.ts
  - src/widget/fenceLocator.ts
  - src/widget/fenceSerialization.ts
  - src/widget/embedDetect.ts
  - src/main.ts
  - src/settings/SettingsStore.ts
  - src/settings/SettingsTab.ts
  - tests/widget/__fixtures__/lcNoteFixtures.ts
  - tests/widget/__fixtures__/cm6Helpers.ts
  - tests/widget/widgetRegistry.test.ts
  - tests/widget/codeBlockProcessor.test.ts
  - tests/widget/WidgetController.test.ts
  - tests/widget/atomicRanges.test.ts
  - tests/widget/themeIntegration.test.ts
  - tests/widget/vimMount.test.ts
  - tests/widget/fenceLocator.test.ts
  - tests/widget/fenceSerialization.property.test.ts
  - tests/main/mutualExclusion.test.ts
autonomous: false
requirements:
  - WIDGET-01
  - WIDGET-02
  - WIDGET-05
  - WIDGET-07
  - WIDGET-08
  - VIM-01
  - VIM-04
  - THEME-01
  - THEME-02
  - THEME-03
must_haves:
  truths:
    - "D-05: With useInlineWidget=ON, opening an LC note in Reading mode renders an embedded CM6 inside the leetcode-solve fence with editable.of(false) and 8-language pack support; with useInlineWidget=OFF, neither registerMarkdownCodeBlockProcessor nor registerEditorExtension fires (hard-gate)"
    - "D-07: With useInlineWidget=ON, opening an LC note in Live Preview renders an embedded CM6 via Decoration.replace and the parent cursor cannot enter the fence range (atomicRanges); the v1.2 path remains regression-clean when useInlineWidget=OFF"
    - "D-03: Phase 19 widgets render code only — WidgetController.mount appends only the EditorView; no widgetActions.ts import, no buttons, no language chevron"
    - "Vim extension only mounts when app.vault.getConfig('vimMode') is true at mount time"
    - "D-06: Mutual-exclusion assert at top of Plugin.onload() forces useNestedEditor=false (with Notice) when useInlineWidget=true; both registrations gated behind useInlineWidget"
    - "D-01: State-map / suppression keys derive fenceIndex from ordinal-counting prior `\\`\\`\\`leetcode-solve` openers up to getSectionInfo().lineStart — never from a content hash; key shape `${file.path}::${fenceIndex}`"
    - "Stray leetcode-solve fence in non-LC note (no lc-slug) renders static <pre><code> fallback; null getSectionInfo also routes to fallback; mount never throws"
    - "D-09: extractFenceBody and rewriteFenceBody are inverses on the seed corpus (CRLF, nested triple backticks, --- lookalikes, edge whitespace, empty/single-line/mid-byte) — verified by tests/widget/fenceSerialization.property.test.ts"
    - "D-08: Settings tab shows new Experimental subsection with banner ('These features are under development and may change between releases.'), useInlineWidget toggle, and Save delay dropdown (300/400/500/1000/2000ms)"
    - "D-10: This plan is plan 1 of 4 in the vertical-slice split (minimal mount → sync → state → embed/property)"
  artifacts:
    - path: src/widget/codeBlockProcessor.ts
      provides: "Reading-mode handler registered via registerMarkdownCodeBlockProcessor('leetcode-solve', …); routes to LeetCodeWidgetRenderChild or static fallback"
      exports: ["leetCodeBlockProcessor"]
      min_lines: 60
    - path: src/widget/liveModeViewPlugin.ts
      provides: "ViewPlugin contributing both Decoration.replace per fence and EditorView.atomicRanges Facet over the same RangeSet"
      exports: ["leetCodeFenceViewPlugin"]
      min_lines: 80
    - path: src/widget/LeetCodeFenceWidget.ts
      provides: "WidgetType subclass with content-hash eq() and ignoreEvent() returning true; toDOM calls mountLeetCodeWidget"
      exports: ["LeetCodeFenceWidget"]
      min_lines: 50
    - path: src/widget/WidgetController.ts
      provides: "mountLeetCodeWidget factory + LeetCodeWidgetRenderChild MarkdownRenderChild wrapper; CM6 mount with conditional vim, theme, semantic classes, atomicRanges chrome"
      exports: ["mountLeetCodeWidget", "LeetCodeWidgetRenderChild", "WidgetController"]
      min_lines: 150
    - path: src/widget/widgetRegistry.ts
      provides: "Plugin-singleton Map<`${file.path}::${fenceIndex}`, WidgetController> with flushAll/destroyAll"
      exports: ["WidgetRegistry"]
      min_lines: 40
    - path: src/widget/fenceLocator.ts
      provides: "Pure helpers findCodeFence (lifted+widened), extractFenceBody, computeFenceIndex"
      exports: ["findCodeFence", "extractFenceBody", "computeFenceIndex"]
      min_lines: 60
    - path: src/widget/fenceSerialization.ts
      provides: "Pure string-only extractFenceBody + rewriteFenceBody for the vault.process callback path"
      exports: ["extractFenceBody", "rewriteFenceBody"]
      min_lines: 50
    - path: src/widget/embedDetect.ts
      provides: "isEmbedContext combining .markdown-embed/.internal-embed ancestor walk + ctx.sourcePath mismatch"
      exports: ["isEmbedContext"]
      min_lines: 20
    - path: src/main.ts
      provides: "useInlineWidget gating block in onload() with mutual-exclusion assert; widget registry instantiation"
      contains: "this.settings.getUseInlineWidget"
    - path: src/settings/SettingsStore.ts
      provides: "useInlineWidget + widgetSyncDebounceMs fields, defaults, shape-guards, getters/setters"
      contains: "getUseInlineWidget"
    - path: src/settings/SettingsTab.ts
      provides: "Experimental subsection with useInlineWidget toggle and Save delay dropdown"
      contains: "Experimental"
    - path: tests/widget/fenceSerialization.property.test.ts
      provides: "SHELLS x HOSTILE_BODIES round-trip property test corpus"
  key_links:
    - from: src/main.ts
      to: src/widget/codeBlockProcessor.ts
      via: registerMarkdownCodeBlockProcessor('leetcode-solve', leetCodeBlockProcessor(this))
      pattern: "registerMarkdownCodeBlockProcessor.*leetcode-solve"
    - from: src/main.ts
      to: src/widget/liveModeViewPlugin.ts
      via: registerEditorExtension([leetCodeFenceViewPlugin(this)])
      pattern: "registerEditorExtension.*leetCodeFenceViewPlugin"
    - from: src/widget/liveModeViewPlugin.ts
      to: "@codemirror/view EditorView.atomicRanges"
      via: provide hook in ViewPlugin
      pattern: "EditorView\\.atomicRanges\\.of"
    - from: src/widget/codeBlockProcessor.ts
      to: src/widget/embedDetect.ts
      via: isEmbedContext(el, ctx, file)
      pattern: "isEmbedContext"
---

<objective>
Phase 19 Plan 01 — Minimal Mount: ship a runnable inline widget for `leetcode-solve` fences, hard-gated behind `useInlineWidget=OFF`, that renders an embedded CM6 in both Reading mode and Live Preview without performing any disk writes. Establishes the foundation Plans 19-02..19-04 build on.

Purpose: Stand up the two-path mount (CONTEXT C-02), `EditorView.atomicRanges` parent-cursor exclusion (C-05/WIDGET-02), `lc-slug` gate + null-section-info fallback (C-10/WIDGET-05), 8-language pack carry-over (C-12/WIDGET-08), theme integration (C-13/THEME-01..03), conditional vim mount (C-14/VIM-01/VIM-04), Experimental settings UI with useInlineWidget toggle + debounce slider (D-08), mutual-exclusion assert (D-06), and the pure-function fence serialization corpus (D-09). No live writes — Plan 19-02 owns sync.

Output: 8 new src files in `src/widget/`, 3 modified src files (`src/main.ts`, `src/settings/SettingsStore.ts`, `src/settings/SettingsTab.ts`), 9 new test files under `tests/widget/` and `tests/main/`. Acceptance: dev-vault smoke shows widget rendering in both modes; nothing touches disk yet; `extractFenceBody`/`rewriteFenceBody` are inverses on the seed corpus.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/phases/19-widget-foundation-one-way-sync/19-CONTEXT.md
@.planning/phases/19-widget-foundation-one-way-sync/19-RESEARCH.md
@.planning/phases/19-widget-foundation-one-way-sync/19-PATTERNS.md
@.planning/phases/19-widget-foundation-one-way-sync/19-VALIDATION.md
@CLAUDE.md
@src/main/codeActionsPostProcessor.ts
@src/main/codeActionsEditorExtension.ts
@src/main/childEditorFactory.ts
@src/main/childEditorLanguage.ts
@src/main/childEditorTheme.ts
@src/main/childEditorSemanticClasses.ts
@src/main/nestedEditorExtension.ts
@src/main/childEditorRegistry.ts
@src/settings/SettingsStore.ts
@src/settings/SettingsTab.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase + RESEARCH/PATTERNS docs. -->

From src/main/childEditorLanguage.ts (verbatim carry-over — DO NOT modify):
```typescript
export const languageCompartment: Compartment;
export function buildLanguageExtensions(slug: string, indent: 'auto' | number): Extension;
// 8 packs: python/python3, java, cpp/c, javascript, typescript, golang, rust
```

From src/main/childEditorTheme.ts (verbatim carry-over):
```typescript
export function createThemedHighlight(): Extension[];
```

From src/main/childEditorSemanticClasses.ts (verbatim carry-over):
```typescript
export const obsidianSemanticClasses: ViewPlugin<unknown>;
```

From obsidian (verified in obsidian.d.ts):
```typescript
// Plugin.registerMarkdownCodeBlockProcessor (line 4848)
registerMarkdownCodeBlockProcessor(
  language: string,
  handler: (source: string, el: HTMLElement, ctx: MarkdownPostProcessorContext) => Promise<any> | void,
  sortOrder?: number,
): MarkdownPostProcessor;

// MarkdownPostProcessorContext (line 3837+)
interface MarkdownPostProcessorContext {
  sourcePath: string;
  frontmatter: any | null | undefined;  // DO NOT use; prefer metadataCache
  addChild(child: MarkdownRenderChild): void;
  getSectionInfo(el: HTMLElement): MarkdownSectionInformation | null;  // null in many circumstances
}

// MarkdownSectionInformation (line 4017+)
interface MarkdownSectionInformation { text: string; lineStart: number; lineEnd: number }

// MarkdownRenderChild (line 3970+)
class MarkdownRenderChild extends Component {
  containerEl: HTMLElement;
  constructor(containerEl: HTMLElement);
  // override onload() / onunload() in subclass
}
```

From @codemirror/view (verified in index.d.ts):
```typescript
// EditorView.atomicRanges (line 1284) — Facet of (view: EditorView) => RangeSet<any>
static atomicRanges: Facet<(view: EditorView) => RangeSet<any>, readonly ((view: EditorView) => RangeSet<any>)[]>;

// WidgetType (line ~210)
abstract class WidgetType {
  abstract toDOM(view: EditorView): HTMLElement;
  eq(other: WidgetType): boolean;        // default returns false — Phase 19 MUST override
  destroy(dom: HTMLElement): void;
  ignoreEvent(event: Event): boolean;    // default returns true — keep default for Phase 19
}

// Decoration.replace (line ~151)
static replace(spec: { widget?: WidgetType; inclusive?: boolean; ... }): Decoration;

// EditorView.editable
static editable: Facet<boolean, boolean>;
```

From @codemirror/state:
```typescript
class RangeSetBuilder<T extends RangeValue> { add(from: number, to: number, value: T): void; finish(): RangeSet<T>; }
class Compartment { of(ext: Extension): Extension; reconfigure(ext: Extension): StateEffect<unknown>; }
```

From @replit/codemirror-vim (verified):
```typescript
function vim(opts?: { status?: boolean }): Extension;
```

Lifted from src/main/codeActionsEditorExtension.ts:177-212 (verbatim into fenceLocator.ts, FENCE_RE widened):
```typescript
findCodeFence(state: EditorState): { openerLine: number; closerLine: number; kind: 'leetcode-solve' | 'legacy' } | null
```

Lifted from src/main/nestedEditorExtension.ts:168-176 (verbatim into fenceLocator.ts):
```typescript
extractFenceBody(state: EditorState, fence: { openerLine, closerLine }): string
```

New pure helpers in src/widget/fenceSerialization.ts (no analog — RESEARCH §"Specific Findings §1"):
```typescript
extractFenceBody(noteBody: string, fenceIndex: number): string | null
rewriteFenceBody(noteBody: string, fenceIndex: number, newBody: string): string
// Round-trip invariant: rewriteFenceBody(s, i, extractFenceBody(s, i) ?? '') === s
```

New helper in src/widget/fenceLocator.ts (RESEARCH §"Specific Findings §2"):
```typescript
computeFenceIndex(fileText: string, fenceLineStart0Based: number): number
// Counts prior `^\s*```leetcode-solve\b` openers up to (not including) fenceLineStart.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — fixtures + seeded test scaffolds + property test scaffolding</name>
  <files>tests/widget/__fixtures__/lcNoteFixtures.ts, tests/widget/__fixtures__/cm6Helpers.ts, tests/widget/widgetRegistry.test.ts, tests/widget/codeBlockProcessor.test.ts, tests/widget/WidgetController.test.ts, tests/widget/atomicRanges.test.ts, tests/widget/themeIntegration.test.ts, tests/widget/vimMount.test.ts, tests/widget/fenceLocator.test.ts, tests/widget/fenceSerialization.property.test.ts, tests/main/mutualExclusion.test.ts</files>
  <read_first>
    - .planning/phases/19-widget-foundation-one-way-sync/19-VALIDATION.md (Wave 0 list lines 78-103; per-task table rows 19-01-01..08)
    - .planning/phases/19-widget-foundation-one-way-sync/19-PATTERNS.md (lines 30-56 test fixtures table; lines 599-655 fixture analogs; lines 668-718 controller/atomic/theme/vim test patterns)
    - tests/main/nestedEditorExtension.test.ts (lines 66-91 CANONICAL_NOTE; lines 113-137 createMockPlugin pattern)
    - tests/main/codeActionsPostProcessor.test.ts (lines 14-72 vi.mock + makeCtx setup; lines 101-130 no-lc-slug test)
    - tests/main/childEditorRegistry.test.ts (full file — direct template for widgetRegistry.test.ts)
    - tests/main/childEditorFactory.test.ts (vim-mount conditional sections; createMockApp pattern)
    - tests/main/codeActionsEditorExtension.test.ts (lines 38-63 FULL_NOTE; CodeActionsWidget.eq currentSlug test for widgetEquality precedent)
    - tests/main/sectionLockExtension.test.ts (changeFilter cursor-blocked patterns analogous to atomicRanges shape)
    - tests/main/lifecycle.test.ts (mutual-exclusion analog — Plugin.onload assertions)
    - tests/helpers/obsidian-stub.ts (lines 258-307 makeStateForLockTests + makeFakeTransaction)
    - tests/solve/mocks/fakeWorkspace.ts (createFakePlugin + createFakeMetadataCache exports)
    - .planning/phases/19-widget-foundation-one-way-sync/19-RESEARCH.md (lines 564-606 SHELLS x HOSTILE_BODIES corpus)
  </read_first>
  <behavior>
    - All test files are RED-on-create stubs that import from src/widget/* paths the next tasks will populate. Each `describe` declares the contract; bodies use `it.todo` only where the contract is non-trivial — every test in the seeded corpus has at least one concrete assertion that will fail today against absent code.
    - lcNoteFixtures.ts exports CANONICAL_LC_NOTE (frontmatter `lc-slug: two-sum`, `lc-language: python3`, fence ` ```leetcode-solve `, body `class Solution:\n    def twoSum(self):\n        pass`), MULTI_FENCE_NOTE (two `leetcode-solve` fences), MISSING_SLUG_NOTE (no lc-slug, has fence), MISSING_LANGUAGE_NOTE (lc-slug present, no lc-language), EMBED_HOST_NOTE (host containing `![[two-sum]]`), STRAY_FENCE_NOTE (random vault note with leetcode-solve fence), CRLF_NOTE (CANONICAL_LC_NOTE with `\r\n` joins), NO_TRAILING_NEWLINE_NOTE.
    - cm6Helpers.ts exports makeFakeMarkdownPostProcessorContext({sourcePath, sectionInfo}), makeFakeApp({vimMode, showLineNumber}) (mirrors childEditorFactory.test.ts createMockApp), makeFakeUpdateForViewPlugin(state, opts).
    - widgetRegistry.test.ts: get/set/has/delete/flushAll/destroyAll cases over makeMockController returning {flushNow: vi.fn(), destroy: vi.fn()}; multi-key (`a.md::0`, `a.md::1`) coverage; flushAll calls flushNow on every entry.
    - codeBlockProcessor.test.ts: lc-slug present + valid info → ctx.addChild called with LeetCodeWidgetRenderChild; lc-slug absent → static fallback rendered (assert el.firstChild is `<pre>` with code); null getSectionInfo → static fallback; embed context (sourcePath mismatch) → readOnly RenderChild; stray fence (no lc-slug, not embed) → static fallback. Each case asserts no exceptions thrown.
    - WidgetController.test.ts: mountLeetCodeWidget called with readOnly=true → CM6 state has `EditorView.editable.of(false)`; readOnly=false → editable; vim mode true → state.facet(Prec? — verify via inspecting `vim` in extensions); vim mode false → no vim binding; container className includes both `lc-nested-editor` and `HyperMD-codeblock`; semantic classes ViewPlugin attached.
    - atomicRanges.test.ts: instantiate parent EditorState with leetCodeFenceViewPlugin extension + a doc containing one `leetcode-solve` fence; query `state.facet(EditorView.atomicRanges)` returns array containing function whose result includes the fence range; cursor selection at fence midpoint moves through the range (no assertion on rendered DOM — happy-dom cannot paint).
    - themeIntegration.test.ts: WidgetController container className contains `lc-nested-editor` AND `HyperMD-codeblock`; createThemedHighlight() output present in extensions; obsidianSemanticClasses ViewPlugin in extensions.
    - vimMount.test.ts: mounted with `app.vault.getConfig('vimMode') === true` → extensions array contains a vim() Extension (assert via instanceof or by checking presence of an extension-array entry whose name matches vim's exported function); mounted with `vimMode === false` → no vim extension.
    - fenceLocator.test.ts: findCodeFence locates `leetcode-solve` opener within `## Code` section; finds legacy ` ```python ` opener too with kind='legacy'; null when no fence; computeFenceIndex returns 0 for first leetcode-solve opener, 1 for second; CRLF-tolerant; extractFenceBody slices state.doc body bytes.
    - fenceSerialization.property.test.ts: full SHELLS x HOSTILE_BODIES matrix from RESEARCH lines 572-605 wrapped in `it.each` over `{shell, body, fenceIndex}` triples; assertion `extractFenceBody(file, i) === body` AND `rewriteFenceBody(file, i, body) === file`. NO fast-check.
    - mutualExclusion.test.ts (under tests/main/): instantiate fake plugin with both useInlineWidget=true and useNestedEditor=true persisted in data.json; call its onload(); assert `setUseNestedEditor(false)` was invoked AND `Notice` constructor was called. Also assert that registering both processors does NOT happen (registerEditorExtension call count for nested = 0).
  </behavior>
  <action>
    Create all 11 listed files. For each test file, use the pattern `vi.mock('obsidian', async () => { const actual = await import('../helpers/obsidian-stub'); return actual; })` (PATTERNS lines 1115-1122). Mirror exact `describe`/`beforeEach` skeletons from the analogs cited in `<read_first>`. Tests reference symbols from `src/widget/*` modules that do not yet exist — that is intentional; they will compile once Tasks 2-4 land. Use `tests/helpers/obsidian-stub.ts` and `tests/solve/mocks/fakeWorkspace.ts` exports verbatim — do not duplicate stubs. Embed the SHELLS × HOSTILE_BODIES corpus from 19-RESEARCH.md §"Property-test seeds" lines 572-605 verbatim into fenceSerialization.property.test.ts. Honor PATTERNS line 692 mock plugin shape (Object.assign(basePlugin, {settings: {getUseInlineWidget: vi.fn(() => true), getWidgetSyncDebounceMs: vi.fn(() => 400), …}})). DO NOT touch CLAUDE.md. DO NOT modify the v1.2 files listed in CONTEXT C-17 (childEditorSync.ts, sectionLockExtension.ts, nestedEditorExtension.ts, childEditorRegistry.ts, codeActionsEditorExtension.ts) — those stay reachable behind useNestedEditor.
  </action>
  <verify>
    <automated>npm test -- --run --reporter=dot tests/widget/ tests/main/mutualExclusion.test.ts 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - All 11 files exist on disk
    - `grep -c "describe\\|it\\|it.each" tests/widget/__fixtures__/lcNoteFixtures.ts tests/widget/__fixtures__/cm6Helpers.ts` — fixtures count ≥ 0 (they may be pure-data files); other 9 test files have at least 1 describe and at least 2 concrete assertions each
    - `npm test -- --run tests/widget/ tests/main/mutualExclusion.test.ts` produces failures referencing `Cannot find module '../../src/widget/*'` (or equivalent) — confirming the tests were authored, not absent
    - No tests pass yet (every assertion is RED until Tasks 2-4 ship)
    - Per CLAUDE.md preserve list, no edits to v1.2 files in CONTEXT C-17 list
  </acceptance_criteria>
  <done>11 test/fixture files committed; vitest discovers and reports failing-on-import for the widget/* tests; mutualExclusion.test.ts compiles and fails on missing onload integration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure helpers + WidgetType + ViewPlugin (atomicRanges contribution) + Reading-mode processor</name>
  <files>src/widget/fenceLocator.ts, src/widget/fenceSerialization.ts, src/widget/embedDetect.ts, src/widget/LeetCodeFenceWidget.ts, src/widget/liveModeViewPlugin.ts, src/widget/codeBlockProcessor.ts, src/widget/widgetRegistry.ts</files>
  <read_first>
    - src/main/codeActionsEditorExtension.ts (lines 41-67 imports; lines 145-156 CodeActionsWidget.eq; lines 158-163 ignoreEvent; lines 177-212 findCodeFence VERBATIM lift target; lines 238-299 buildDecorations shape; lines 320-394 StateField extension shape — for translating into ViewPlugin)
    - src/main/nestedEditorExtension.ts (lines 81-162 NestedEditorWidget toDOM/destroy; lines 168-176 extractFenceBody)
    - src/main/codeActionsPostProcessor.ts (lines 1-67 entire file — Reading-mode lc-slug gate template)
    - src/main/childEditorRegistry.ts (lines 1-114 entire file — direct successor template for widgetRegistry.ts)
    - .planning/phases/19-widget-foundation-one-way-sync/19-PATTERNS.md (lines 73-114 codeBlockProcessor; lines 116-194 liveModeViewPlugin; lines 411-498 selfWriteSuppression / widgetRegistry; lines 500-595 fenceLocator + fenceSerialization)
    - .planning/phases/19-widget-foundation-one-way-sync/19-RESEARCH.md (Pattern 3 lines 247-272 ViewPlugin+atomicRanges; Pitfall 19-F lines 412-422 eq() content-hash; Specific Findings §2 lines 638-657 computeFenceIndex; Specific Findings §3 lines 659-666 embed detection both-signals; Pitfall 19-D lines 391-399 embed null path)
    - .planning/phases/19-widget-foundation-one-way-sync/19-CONTEXT.md (C-02, C-05, C-10, C-15, D-01, D-04 lines 22-58)
    - tests/widget/fenceLocator.test.ts + tests/widget/fenceSerialization.property.test.ts + tests/widget/codeBlockProcessor.test.ts + tests/widget/atomicRanges.test.ts + tests/widget/widgetRegistry.test.ts (the seed tests from Task 1)
  </read_first>
  <action>
    1. `src/widget/fenceLocator.ts`: lift `findCodeFence` from `src/main/codeActionsEditorExtension.ts:177-212` verbatim; widen FENCE_RE to detect both `^\s*```leetcode-solve\b` (set kind='leetcode-solve') and `^\s*```([a-z][\w-]*)\b` (set kind='legacy', preserve langslug for Phase 21). Lift `extractFenceBody(state, fence)` from `nestedEditorExtension.ts:168-176` verbatim. Add new `computeFenceIndex(fileText, fenceLineStart0Based)` per RESEARCH §2 line 642-651: split on `/\r?\n/`, count prior `^\s*```leetcode-solve\b` matches before `fenceLineStart0Based`. All three exports must be pure (no Obsidian/CM6 imports beyond `EditorState` for state-aware overload).
    2. `src/widget/fenceSerialization.ts`: new pure string-only `extractFenceBody(noteBody: string, fenceIndex: number): string | null` and `rewriteFenceBody(noteBody, fenceIndex, newBody): string`. Walk lines, find the `fenceIndex`-th `^\s*```leetcode-solve\b` opener, find its matching closer (nearest subsequent line matching `^\s*```\s*$`), slice/splice body lines preserving line endings exactly (DO NOT normalize CRLF — RESEARCH Specific Findings §5; DO NOT trim — PITFALLS P6). Return null when fenceIndex out of range; rewriteFenceBody returns input unchanged when out of range. Round-trip invariant: `rewriteFenceBody(s, i, extractFenceBody(s, i) ?? '') === s` for every fenceIndex < count.
    3. `src/widget/embedDetect.ts`: `isEmbedContext(el, ctx, targetFile): boolean` per PATTERNS lines 480-499 — DOM ancestor walk for `markdown-embed`/`internal-embed` classes OR `ctx.sourcePath !== targetFile.path` (BOTH signals OR'd per RESEARCH Specific Findings §3). Pure boolean; no side effects.
    4. `src/widget/LeetCodeFenceWidget.ts`: `class LeetCodeFenceWidget extends WidgetType` with constructor accepting `(plugin, file, fenceIndex, sourceHash, source)`. Implement `eq(other)` returning true iff `other instanceof LeetCodeFenceWidget && other.plugin === this.plugin && other.file === this.file && other.fenceIndex === this.fenceIndex && other.sourceHash === this.sourceHash` (RESEARCH Pitfall 19-F lines 412-422 — content-hash identity prevents thrash). Override `ignoreEvent()` returning `true` (PATTERNS lines 158-163; lets embedded EditorView consume events). `toDOM(view)` calls `mountLeetCodeWidget(container, this.source, this.file, this.plugin, /*readOnly=*/false)` and returns container. `destroy(dom)` calls `controller.flushNow()` (no-op in Plan 19-01 since debouncedWriter doesn't exist yet — leave a stub method on WidgetController) then unregisters from widgetRegistry. NEVER include the WidgetController instance in eq() — must be content-based only.
    5. `src/widget/liveModeViewPlugin.ts`: export `leetCodeFenceViewPlugin(plugin)` returning `ViewPlugin.fromClass(LeetCodeLiveViewPlugin, { decorations: v => v.decorations, provide: pl => EditorView.atomicRanges.of(view => view.plugin(pl)?.ranges ?? Decoration.none) })`. Inner class scans `editorInfoField`-derived file + `metadataCache` for lc-slug (gate per PATTERNS lines 168-174); when present, locates fence via `findCodeFence(view.state)` and emits `Decoration.replace({ widget: new LeetCodeFenceWidget(...) })` over the fence range. Build the SAME RangeSet into both `decorations` and `ranges` fields (RESEARCH Pattern 3 lines 263-272; PATTERNS lines 1080-1090 — drift between the two means cursor leaks into widget). Update on `update.docChanged || update.viewportChanged` only — DO NOT add the metadataCache.on('changed') subscription (Phase 20 owns that per CONTEXT deferred ideas + PATTERNS line 192).
    6. `src/widget/codeBlockProcessor.ts`: export `leetCodeBlockProcessor(plugin)` returning the `(source, el, ctx) => void` handler. Resolve `file = plugin.app.vault.getAbstractFileByPath(ctx.sourcePath); if (!(file instanceof TFile)) return renderStaticFallback(el, source);`. Read frontmatter via `plugin.app.metadataCache.getFileCache(file)?.frontmatter as Record<string, unknown> | undefined` (NOT `ctx.frontmatter` — RESEARCH Anti-Patterns + Specific Findings §A5). Read `info = ctx.getSectionInfo(el)`; if null → `renderStaticFallback(el, source)` (RESEARCH Pitfall 19-D treats null as embed-likely). Compute `isEmbed = isEmbedContext(el, ctx, file)`. Branching: (a) no lc-slug AND not embed → static fallback (CONTEXT C-10/D-04, EMBED-04); (b) no lc-slug AND embed → readOnly RenderChild via `ctx.addChild(new LeetCodeWidgetRenderChild(el, source, ctx, plugin, file, info, /*readOnly=*/true))`; (c) lc-slug present AND embed → readOnly RenderChild; (d) lc-slug present AND not embed → editable RenderChild. `renderStaticFallback(el, source)` creates `el.createEl('pre').createEl('code', { text: source })` (CLAUDE.md: NO innerHTML). NEVER throw — every branch is graceful (WIDGET-05).
    7. `src/widget/widgetRegistry.ts`: `class WidgetRegistry` with `private map = new Map<string, WidgetController>()` keyed by `${file.path}::${fenceIndex}` (CONTEXT D-01); methods `get(key)`, `set(key, ctl)`, `delete(key)`, `has(key)`, `flushAll()` calling `ctl.flushNow()` for each entry (no-op stub in Plan 19-01 since flushNow comes online in Plan 19-02), `flushFile(filePath)` for `vault.on('rename')`, `destroyAll()` calling `ctl.destroy()` then `map.clear()`. Mirror class shape from `src/main/childEditorRegistry.ts:19-26, 75-81` (PATTERNS lines 449-477) but drop LRU eviction.
    8. DO NOT modify CLAUDE.md. DO NOT touch the v1.2 files in CONTEXT C-17. DO NOT add metadataCache subscription.
  </action>
  <verify>
    <automated>npm test -- --run tests/widget/fenceLocator.test.ts tests/widget/fenceSerialization.property.test.ts tests/widget/widgetRegistry.test.ts tests/widget/codeBlockProcessor.test.ts tests/widget/atomicRanges.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 7 src/widget/*.ts files exist
    - `npm test -- --run tests/widget/fenceLocator.test.ts` exits 0 (computeFenceIndex, findCodeFence, extractFenceBody all green)
    - `npm test -- --run tests/widget/fenceSerialization.property.test.ts` exits 0 (full SHELLS × HOSTILE_BODIES corpus passes the round-trip invariant)
    - `npm test -- --run tests/widget/widgetRegistry.test.ts` exits 0
    - `npm test -- --run tests/widget/codeBlockProcessor.test.ts` exits 0 (lc-slug gate, null section info, embed routing all green)
    - `npm test -- --run tests/widget/atomicRanges.test.ts` exits 0 (parent EditorState facet contains the leetCodeFenceViewPlugin's ranges function)
    - `grep -c "ctx\\.frontmatter" src/widget/codeBlockProcessor.ts` returns 0 (uses metadataCache instead — RESEARCH Anti-Patterns)
    - `grep -c "innerHTML" src/widget/` returns 0 (CLAUDE.md POLISH-03 carry-through)
    - `grep -c "ignoreEvent" src/widget/LeetCodeFenceWidget.ts` returns 1
  </acceptance_criteria>
  <done>Pure helpers, WidgetType, ViewPlugin, Reading-mode processor, and widgetRegistry all green against their seed tests; pure-function corpus passes 100%.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WidgetController + LeetCodeWidgetRenderChild (mount factory + lifecycle wrapper)</name>
  <files>src/widget/WidgetController.ts</files>
  <read_first>
    - src/main/childEditorFactory.ts (lines 22-52 imports; lines 252-416 createChildEditor body — primary lift target; lines 261-274 vimMode read; lines 322-323 conditional vim spread; lines 362-364 themed highlight + brackets; lines 381-395 EditorView.theme block; lines 405-413 click-to-focus listener; lines 428-481 createVimIsolationExtension lift; lines 176-222 createCmdSlashScopeExtension lift)
    - src/main/nestedEditorExtension.ts (lines 81-162 NestedEditorWidget toDOM + destroy lifecycle; line 114 container.className 'lc-nested-editor HyperMD-codeblock')
    - .planning/phases/19-widget-foundation-one-way-sync/19-PATTERNS.md (lines 196-333 WidgetController section — exact lift instructions)
    - .planning/phases/19-widget-foundation-one-way-sync/19-RESEARCH.md (Pattern 4 lines 274-308 statePersistence stub structure; Pitfall 19-F lines 412-422 eq() identity; D-02 + Pitfall 19-D lines 391-399)
    - .planning/phases/19-widget-foundation-one-way-sync/19-CONTEXT.md (C-13, C-14, D-02 lines 33-46)
    - tests/widget/WidgetController.test.ts + tests/widget/themeIntegration.test.ts + tests/widget/vimMount.test.ts (seed tests from Task 1)
  </read_first>
  <action>
    Create `src/widget/WidgetController.ts` exporting:
    1. `class LeetCodeWidgetRenderChild extends MarkdownRenderChild` — Reading-mode lifecycle wrapper. Constructor: `(host, source, ctx, plugin, file, info, readOnly)`. `onload()` calls `mountLeetCodeWidget(this.host, this.source, this.file, this.plugin, this.readOnly)` storing the returned `WidgetController` on `this.controller`. `onunload()` calls `this.controller?.flushNow()` (stub in Plan 19-01) then `this.controller?.destroy()` then `widgetRegistry.delete(\`${this.file.path}::${this.fenceIndex}\`)`. `fenceIndex` computed via `computeFenceIndex(info.text, info.lineStart)` at construction.
    2. `function mountLeetCodeWidget(host: HTMLElement, source: string, file: TFile, plugin: LeetCodePlugin, readOnly: boolean): WidgetController` — the shared factory. Builds container `div` with `className = 'lc-nested-editor HyperMD-codeblock lc-leetcode-solve'` (PATTERNS line 1101 — three classes: two carry-over + one new). Creates child `EditorView` with extensions (in this order):
       - `keymap.of([...defaultKeymap, ...historyKeymap, ...closeBracketsKeymap, indentWithTab keymap entries (PATTERNS lift instructions for childEditorFactory keymap), { key: 'Mod-/', run: toggleLineComment }])`
       - `history()`
       - `lineNumbers()` (gated on `plugin.settings.getShowRelativeLineNumbers?.()` — fall back to no-op gutter if absent)
       - `bracketMatching()`
       - `indentUnit.of('    ')` or per `plugin.settings.getIndentSizeOverride()` mirroring childEditorFactory line 357-360
       - `languageCompartment.of(buildLanguageExtensions(slug, indent))` — slug derived from frontmatter `lc-language` with Python fallback (full Notice handling lands in Plan 19-04 Task; Plan 19-01 just passes through and silently uses 'python' on miss)
       - `obsidianSemanticClasses` (verbatim import from `src/main/childEditorSemanticClasses`)
       - `...createThemedHighlight()` (verbatim import from `src/main/childEditorTheme`)
       - `EditorView.theme({...})` block lifted verbatim from childEditorFactory.ts:381-395
       - `EditorView.editable.of(!readOnly)` — gates Reading-mode read-only
       - `createVimIsolationExtension(container)` lifted verbatim from childEditorFactory.ts:428-481
       - `createCmdSlashScopeExtension()` lifted verbatim from childEditorFactory.ts:176-222
       - Conditional vim: `const vimEnabled = !!plugin.app && (plugin.app as any).vault.getConfig('vimMode') === true; if (vimEnabled) extensions.push(vim({ status: true } as any));` (PATTERNS lines 226-235; CONTEXT C-14, VIM-01)
       - DO NOT include createScrollIntoViewExtension (PATTERNS line 222 — that helper lives in soon-deleted childEditorSync.ts)
       - DO NOT accept syncExtensions parameter (PATTERNS line 223)
    3. After `view.dom` exists, attach two mousedown listeners IN THIS ORDER (CONTEXT D-02, PATTERNS lines 280-285):
       (a) `view.dom.addEventListener('mousedown', (e) => e.stopPropagation())` — first, defends against Live Preview cursor-place;
       (b) `view.dom.addEventListener('mousedown', () => window.requestAnimationFrame(() => { if (document.activeElement !== view.contentDOM) view.contentDOM.focus(); }))` — second, click-to-focus from childEditorFactory.ts:405-413.
       Order matters — stopPropagation must fire first.
    4. `class WidgetController` (returned from mountLeetCodeWidget): stores `{view: EditorView, container: HTMLElement, file: TFile, fenceIndex: number, plugin: LeetCodePlugin}`. Stub methods `flushNow(): void` (no-op in Plan 19-01 — body added in Plan 19-02), `destroy(): void` calls `view.destroy()`, `getDoc(): string` returns `view.state.doc.toString()`. Register in `plugin.widgetRegistry.set(\`${file.path}::${fenceIndex}\`, this)` if registry exists (it will exist once Task 4 wires onload).
    5. `slug` lookup helper (private): reads `plugin.app.metadataCache.getFileCache(file)?.frontmatter?.['lc-language']`, normalizes to a known pack via the existing slug-resolution from `buildLanguageExtensions`; falls back silently to 'python' in Plan 19-01 (Notice and language-fallback test surface arrives in Plan 19-04 Task 3 per VALIDATION row 19-04-03). DO NOT emit Notice in this Plan.
    6. DO NOT modify any file under `src/main/` — those stay reachable behind useNestedEditor (CONTEXT C-17). Import from `'../main/childEditorLanguage'`, `'../main/childEditorTheme'`, `'../main/childEditorSemanticClasses'`.
  </action>
  <verify>
    <automated>npm test -- --run tests/widget/WidgetController.test.ts tests/widget/themeIntegration.test.ts tests/widget/vimMount.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/widget/WidgetController.ts` exists with all three exports (`LeetCodeWidgetRenderChild`, `mountLeetCodeWidget`, `WidgetController`)
    - `npm test -- --run tests/widget/WidgetController.test.ts` exits 0 — readOnly toggle behavior, container className includes both 'lc-nested-editor' and 'HyperMD-codeblock', semantic classes attached
    - `npm test -- --run tests/widget/themeIntegration.test.ts` exits 0 — theme extensions present
    - `npm test -- --run tests/widget/vimMount.test.ts` exits 0 — conditional vim mount based on getConfig('vimMode')
    - `grep -E "from '\\./childEditorLanguage'" src/widget/WidgetController.ts` returns 0 (must use `../main/childEditorLanguage` path)
    - `grep -c "createScrollIntoViewExtension" src/widget/WidgetController.ts` returns 0 (omitted per PATTERNS line 222)
    - `grep -c "stopPropagation" src/widget/WidgetController.ts` returns 1 (D-02 mitigation present)
    - `grep -c "EditorView.editable.of" src/widget/WidgetController.ts` returns 1 (Reading-mode read-only path — WIDGET-07)
    - DO NOT touch src/main/childEditor*.ts — `git diff --name-only src/main/childEditor*.ts` returns no entries
  </acceptance_criteria>
  <done>WidgetController + factory + RenderChild wrapper green; container has correct theme classes; conditional vim verified; mousedown listener order correct (stopPropagation first, focus second); no v1.2 src/main/* files modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Settings shape + UI Experimental subsection + Plugin.onload gating + mutual-exclusion assert</name>
  <files>src/settings/SettingsStore.ts, src/settings/SettingsTab.ts, src/main.ts</files>
  <read_first>
    - src/settings/SettingsStore.ts (lines 71-79 useNestedEditor field; lines 258-260 DEFAULT_DATA; lines 664-668 indentSizeOverride strict-equality shape-guard; lines 672-677 useNestedEditor shape-guard; lines 837-847 useNestedEditor getter/setter)
    - src/settings/SettingsTab.ts (lines 222-273 Code editor section + reload-on-toggle Notice pattern)
    - src/main.ts (lines 819-839 useNestedEditor flag-gating + childEditorRegistry instantiation; lines 1027-1051 onunload cleanup; lines 752-808 in childEditorSync.ts for vault.on('modify') registration shape)
    - .planning/phases/19-widget-foundation-one-way-sync/19-PATTERNS.md (lines 749-1015 the entire MODIFIED FILES section — exact insertion-point line ranges and idiomatic comment style)
    - .planning/phases/19-widget-foundation-one-way-sync/19-CONTEXT.md (D-05, D-06, D-08; Pitfall 19-G timing constraint)
    - .planning/phases/19-widget-foundation-one-way-sync/19-RESEARCH.md (Pitfall 19-G lines 423-431 mutual-exclusion at top of onload; Specific Findings §8 lines 695-708 settings shape)
    - tests/main/mutualExclusion.test.ts (seed test from Task 1)
  </read_first>
  <action>
    1. `src/settings/SettingsStore.ts`:
       - Add interface field `useInlineWidget: boolean;` AFTER existing `useNestedEditor` at line 79 with the JSDoc comment per PATTERNS lines 845-851 (default false; D-05 hard-gate; reload-required).
       - Add interface field `widgetSyncDebounceMs: 300 | 400 | 500 | 1000 | 2000;` AFTER it with JSDoc per PATTERNS lines 853-858.
       - Add `useInlineWidget: false,` and `widgetSyncDebounceMs: 400,` to DEFAULT_DATA AFTER line 260.
       - Add shape-guards per PATTERNS lines 877-887: typeof boolean check for useInlineWidget collapsing to default; strict-equality check for widgetSyncDebounceMs against the 5-element whitelist collapsing to 400 otherwise.
       - Add getters/setters per PATTERNS lines 909-922: `getUseInlineWidget(): boolean`, `setUseInlineWidget(v): Promise<void>`, `getWidgetSyncDebounceMs(): 300|400|500|1000|2000`, `setWidgetSyncDebounceMs(v): Promise<void>`. All four mirror the existing useNestedEditor accessor signatures.
    2. `src/settings/SettingsTab.ts` AFTER line 273 (last entry in `codeEditorGroup`):
       - Add `new Setting(containerEl).setName('Experimental').setHeading();` heading.
       - Create `expGroup = containerEl.createDiv('lc-settings-group');`.
       - Append a banner via `expGroup.createEl('p', { text: 'These features are under development and may change between releases.', cls: 'setting-item-description' });` (NOT a Setting; DOM-style createEl per CLAUDE.md no-innerHTML).
       - Add `useInlineWidget` toggle Setting per PATTERNS lines 979-994: name 'Use inline widget editor (v1.3 alpha)', description includes 'Mutually exclusive with the nested code editor. Reload Obsidian to apply changes.', onChange: (a) if v && getUseNestedEditor() then `await setUseNestedEditor(false)` first (D-06), (b) `await setUseInlineWidget(v)`, (c) `new Notice('Reload Obsidian to apply', 5000)`, (d) `this.display()` to re-render dependent rows.
       - Add `widgetSyncDebounceMs` dropdown Setting per PATTERNS lines 996-1014: name 'Save delay', 5 options ('300', '400', '500', '1000', '2000'), default to 400 on parse miss.
    3. `src/main.ts` Plugin.onload() AFTER line 827 (existing `registerEditorExtension(buildCodeActionsEditorExtension(this))`):
       - Read `const useInlineWidget = this.settings.getUseInlineWidget();` (parallel to existing useNestedEditor read at line 830).
       - INSERT mutual-exclusion assert IMMEDIATELY (before either registration path fires — RESEARCH Pitfall 19-G timing): `if (useInlineWidget && useNestedEditor) { new Notice('useInlineWidget is ON — disabling useNestedEditor (mutually exclusive)', 5000); await this.settings.setUseNestedEditor(false); /* recompute or branch off the local flag */ }`. Recompute `useNestedEditor` afterwards OR mutate the local for the v1.2 branch decision (PATTERNS lines 770-779).
       - Wrap the existing `if (useNestedEditor) { this.registerEditorExtension(buildNestedEditorExtension(this)); }` block: that stays as-is for the v1.2 path.
       - Add new gated block per PATTERNS lines 781-803: `if (useInlineWidget) { this.widgetRegistry = new WidgetRegistry(); this.registerMarkdownCodeBlockProcessor('leetcode-solve', leetCodeBlockProcessor(this)); this.registerEditorExtension([leetCodeFenceViewPlugin(this)]); /* flush hooks deferred to Plan 19-02 */ }`. Plan 19-01 does NOT register vault.on('modify') or any flush-on-transition hooks — those land in Plan 19-02.
       - Plan 19-01 does NOT instantiate selfWriteSuppression or statePersistence (Plans 19-02 and 19-03 own those).
    4. `src/main.ts` Plugin.onunload() AFTER line 1050 (PATTERNS lines 822-826): add `this.widgetRegistry?.destroyAll();`. (flushAll arrives in Plan 19-02.)
    5. Add a class field `widgetRegistry?: WidgetRegistry` on the LeetCodePlugin class with the matching import.
    6. DO NOT modify CLAUDE.md. DO NOT modify any file under `src/main/childEditor*.ts` or `src/main/nestedEditorExtension.ts` or `src/main/sectionLockExtension.ts` or `src/main/codeActionsEditorExtension.ts` (CONTEXT C-17). DO NOT touch `'leetcode.*'` userEvent paragraph in CLAUDE.md (RESEARCH §7).
  </action>
  <verify>
    <automated>npm test -- --run tests/main/mutualExclusion.test.ts && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run tests/main/mutualExclusion.test.ts` exits 0
    - `npm run build` exits 0 (esbuild type-checks the new fields and the SettingsTab subsection)
    - `grep -c "getUseInlineWidget" src/settings/SettingsStore.ts` returns ≥ 2 (declaration + getter)
    - `grep -c "widgetSyncDebounceMs" src/settings/SettingsStore.ts` returns ≥ 4 (interface + DEFAULT + shape-guard + getter+setter)
    - `grep -c "Experimental" src/settings/SettingsTab.ts` returns ≥ 1
    - `grep -c "useInlineWidget && useNestedEditor" src/main.ts` returns ≥ 1 (mutual-exclusion assert present)
    - `grep -c "registerMarkdownCodeBlockProcessor.*leetcode-solve" src/main.ts` returns 1
    - `grep -c "leetCodeFenceViewPlugin" src/main.ts` returns ≥ 1
    - `git diff --name-only src/main/` returns no v1.2 files modified (CONTEXT C-17)
    - `grep -c "leetcode\\.\\*" CLAUDE.md` matches the pre-Phase-19 baseline (paragraph preserved per CLAUDE.md preserve list)
  </acceptance_criteria>
  <done>Settings shape + UI + onload gating + mutual-exclusion assert all wired; build green; mutualExclusion.test green; v1.2 path under useNestedEditor still operational; CLAUDE.md untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: UAT — Plan 19-01 dev-vault smoke</name>
  <what-built>
    A useInlineWidget=ON path that mounts an editable CM6 inside `leetcode-solve` fences in both Reading mode and Live Preview, with parent-cursor exclusion via atomicRanges, conditional vim, theme/semantic-class carry-over, lc-slug + null-section-info safe fallback, and an Experimental settings subsection. NO live writes yet (Plan 19-02 owns sync). Mutual-exclusion enforced at onload.
  </what-built>
  <files>(manual UAT — no source files modified by this task; verifies Tasks 1-4 deliverables)</files>
  <action>This is a manual user-acceptance test; the developer (and the user reviewing) executes the steps in <how-to-verify> against a real Obsidian dev vault. No code is written by this task. The task gates Plan 19-01 closure: a fail blocks Plan 19-02. See <what-built> for what is being verified, <how-to-verify> for the exact steps.</action>
    <how-to-verify>
    1. `npm run build` exits 0; copy `main.js` + `manifest.json` to `~/Documents/Obsidian Vault/.obsidian/plugins/obsidian-leetcode/`.
    2. Reload Obsidian (Cmd-R or restart). Open `Settings → Obsidian LeetCode → Experimental`. Confirm:
       - Banner text 'These features are under development and may change between releases.' visible
       - 'Use inline widget editor (v1.3 alpha)' toggle visible (default OFF)
       - 'Save delay' dropdown visible with 5 options (300/400/500/1000/2000)
    3. Toggle `useInlineWidget=ON`. Confirm Notice 'Reload Obsidian to apply' appears. Reload Obsidian.
    4. Open an existing LC note (one with `lc-slug` and `lc-language` frontmatter and a `## Code` fence — convert opener manually to ` ```leetcode-solve ` for this UAT; Phase 21 owns automatic migration). Confirm:
       - Reading mode: widget renders with read-only CM6 inside the fence (cannot type into it; syntax highlighting matches `lc-language`); container has `lc-nested-editor HyperMD-codeblock lc-leetcode-solve` classes (verify via DevTools).
       - Live Preview: same widget renders; clicking into the widget focuses the inner CM6; arrow-down from the heading above lands at the widget BUT the cursor stops at the fence boundary — try arrow-down from `## Problem`'s last line through `## Code` heading toward the widget; cursor cannot enter the widget range (atomicRanges working).
       - 8 language packs: change `lc-language` frontmatter to `java`, `cpp`, `javascript`, `typescript`, `c`, `go`, `rust` one at a time; reload note each time; widget renders with appropriate syntax highlighting (Plan 19-01 does not live-reconfigure — note reload is fine).
       - Theme: switch to a community theme (e.g., Minimal); widget visual updates to match the new theme without note reload (THEME-01..03 carry-over working).
    5. Vim test (VIM-01/VIM-04): enable Vim mode in `Settings → Editor → Vim`, reload note. Confirm widget responds to vim normal-mode keys (`hjkl` move cursor inside widget; `i` enters insert). Press `j` repeatedly — confirm parent CM6 cursor does NOT move (atomicRanges enforces VIM-04).
    6. lc-slug gate (WIDGET-05/EMBED-04 partial): create a non-LC note containing ` ```leetcode-solve ` followed by some code and a closing ` ``` `. Open in Reading mode. Confirm: static `<pre><code>` fallback renders (no editable widget; no Run/Submit which don't exist yet); no error in DevTools console; nothing crashes.
    7. Mutual-exclusion (D-06): manually edit `data.json` under `.obsidian/plugins/obsidian-leetcode/` to set BOTH `useInlineWidget: true` AND `useNestedEditor: true`. Reload Obsidian. Confirm: a Notice 'useInlineWidget is ON — disabling useNestedEditor (mutually exclusive)' appears; `data.json` now has `useNestedEditor: false`; only one CM6 instance per fence (DevTools should show one `lc-leetcode-solve` per fence, NOT two stacked editors).
    8. Hard-gate isolation (D-05): toggle `useInlineWidget=OFF` in settings, reload. Confirm: v1.2 nested-editor path mounts as before; non-LC `leetcode-solve` fences fall back to Obsidian default code-block rendering; v1.3 widget code paths are unreachable.
    9. NO disk writes yet: type into the widget (when useInlineWidget=ON, lc-slug present, Live Preview), then immediately check the underlying note file's mtime via Finder/`stat`. Confirm: mtime is unchanged (Plan 19-01 does not write — Plan 19-02 ships the debouncedWriter).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues. Specifically flag any: widget fails to render in either mode; cursor leaks into the fence range; vim leaks to parent; theme regressions; toggle UI broken; mutual-exclusion not enforced; v1.2 baseline (useInlineWidget=OFF) regressed; static fallback throws on stray fence.</resume-signal>
  <verify>(developer confirms each verification step; user replies "approved" or with issues per <resume-signal>)</verify>
  <done>Approved by user via <resume-signal>; Plan 19-01 acceptance gates met; ready for Plan 19-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keystrokes → embedded CM6 | Typing inside the widget's child EditorView; bounded by atomicRanges on the parent |
| parent CM6 → embedded widget | Cursor approach / mode flip / decoration rebuild events |
| stray `leetcode-solve` fence in non-LC note | Untrusted file content (any markdown file); must not escalate to editable widget |
| settings UI → settings store → onload | data.json may be hand-edited to set both flags simultaneously |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Elevation of Privilege | leetCodeFenceViewPlugin / codeBlockProcessor mount path | mitigate | lc-slug frontmatter gate (WIDGET-05; CONTEXT C-10) at every mount predicate; non-LC fence + non-embed → static `<pre><code>` fallback; embed (LC or non-LC) → readOnly RenderChild; never escalate to editable widget without lc-slug |
| T-19-05 | Tampering | LeetCodeFenceWidget.eq() identity | mitigate | eq() returns true iff (filePath, fenceIndex, sourceHash) all equal (RESEARCH Pitfall 19-F); never include WidgetController instance in eq(); same RangeSet shared between decorations and atomicRanges Facet to prevent drift |
| T-19-06 | Tampering | Plugin.onload double-registration when both flags ON | mitigate | Mutual-exclusion assert at TOP of onload (RESEARCH Pitfall 19-G; CONTEXT D-06) BEFORE either registerEditorExtension call; force useNestedEditor=false with Notice; SettingsTab onChange handler sets useNestedEditor=false when useInlineWidget toggled ON; tested via mutualExclusion.test.ts |
| T-19-04-fallback | Information Disclosure | renderStaticFallback DOM construction | mitigate | Use createEl/createDiv API (no innerHTML — CLAUDE.md POLISH-03 carry-through); pass `text:` option for source content so HTML/MD in body is treated as text content |
| T-19-NEW-Phase19-1 | Information Disclosure | mousedown.stopPropagation listener | accept | Empirical mitigation against Live Preview "raw source reveal" (CONTEXT D-02); state-persistence map (Plan 19-03) is the load-bearing fallback regardless; cost ~3 LOC |
| T-19-NEW-Phase19-2 | Tampering | useInlineWidget OFF default | mitigate | Hard-gate (CONTEXT D-05) — when OFF, neither registration path fires; v1.2 baseline preserved unmodified through Phase 21; bisection-clean |
</threat_model>

<verification>
**Phase-level gates for Plan 19-01:**
- `npm test -- --run tests/widget/ tests/main/mutualExclusion.test.ts` — all green (Tasks 2-4)
- `npm run build` — exits 0
- `grep -E "innerHTML" src/widget/` — returns no matches (POLISH-03 carry-through)
- `grep -E "ctx\\.frontmatter" src/widget/` — returns no matches (RESEARCH Anti-Patterns)
- `git diff --name-only src/main/` — no v1.2 files in CONTEXT C-17 list modified
- `git diff CLAUDE.md` — no changes (preserve list)
- UAT (Task 5) — all 9 verification scenarios pass
</verification>

<success_criteria>
1. Two-path mount works behind useInlineWidget=ON in both Reading mode and Live Preview (WIDGET-01, WIDGET-07)
2. Parent CM6 cursor cannot enter the widget fence range (WIDGET-02)
3. Stray fence in non-LC note renders static fallback without crashing (WIDGET-05; partial EMBED-04)
4. 8 language packs available via languageCompartment (WIDGET-08)
5. Theme + semantic classes carry over verbatim (THEME-01, THEME-02, THEME-03)
6. Vim mounts only when getConfig('vimMode') is true and is confined to widget (VIM-01, VIM-04)
7. Settings UI exposes useInlineWidget toggle and Save delay dropdown in Experimental subsection (CONTEXT D-08)
8. Mutual-exclusion assert in Plugin.onload prevents double-mount when both flags ON (CONTEXT D-06)
9. extractFenceBody/rewriteFenceBody round-trip on the SHELLS × HOSTILE_BODIES corpus (SYNC-06 pure-fn portion; CONTEXT D-09)
10. v1.2 path with useInlineWidget=OFF remains regression-clean (CONTEXT D-07; UAT step 8)
</success_criteria>

<output>
Create `.planning/phases/19-widget-foundation-one-way-sync/19-01-SUMMARY.md` when done.
</output>
