---
phase: 19-widget-foundation-one-way-sync
plan: 05
type: execute
wave: 5
depends_on: [19-01, 19-02, 19-03, 19-04]
gap_closure: true
files_modified:
  - src/widget/WidgetController.ts
  - src/main.ts
  - tests/widget/vimMount.test.ts
  - tests/widget/readOnlyMount.test.ts
  - tests/main/inlineWidgetActionGate.test.ts
autonomous: false
requirements:
  - WIDGET-07
  - VIM-01
  - THEME-01
  - THEME-02
  - THEME-03
  - EMBED-04
user_setup: []

must_haves:
  truths:
    - "In Reading mode, the widget DOM has `contenteditable=\"false\"` (or the attribute absent) and typing has no effect"
    - "In Reading mode, NO `.cm-vim-panel` (or any vim status DOM) is rendered, regardless of Obsidian's `vimMode` global setting"
    - "When `useInlineWidget=ON`, NO `.leetcode-code-actions` element renders below the widget in Reading mode OR Live Preview"
    - "When `useInlineWidget=OFF`, the v1.2 path (incl. Run/Submit/AI Debug button row in BOTH Reading mode and Live Preview) is unchanged from pre-Phase 19 behavior"
    - "Widget visual font-size matches v1.2 nested-editor mount (same `cm-content { font-size: var(--font-text-size) }` rule reaches the embedded EditorView; no white unstyled vim panel because vim never mounts in read-only)"
  artifacts:
    - path: "src/widget/WidgetController.ts"
      provides: "Read-only-aware vim gating + read-only-aware editable gate (no escape via vim's internal editable.of(true))"
      contains: "vimEnabled && !readOnly"
    - path: "src/main.ts"
      provides: "useInlineWidget-gated registration of v1.2 Run/Submit action row processors"
      contains: "if (!useInlineWidget)"
    - path: "tests/widget/readOnlyMount.test.ts"
      provides: "Behavioral assertion: when readOnly=true, buildExtensions output does NOT include vim() AND editable.of(true) never resolves to true"
    - path: "tests/main/inlineWidgetActionGate.test.ts"
      provides: "Behavioral assertion: registerCodeBlockActionProcessor + buildCodeActionsEditorExtension are NOT called when useInlineWidget=true"
  key_links:
    - from: "src/widget/WidgetController.ts:buildExtensions"
      to: "vim() extension from @replit/codemirror-vim"
      via: "conditional spread guarded by `vimEnabled && !readOnly`"
      pattern: "vimEnabled && !readOnly"
    - from: "src/main.ts:Plugin.onload"
      to: "registerCodeBlockActionProcessor + buildCodeActionsEditorExtension"
      via: "negation gate `if (!useInlineWidget)` wrapping both calls (mirrors hard-gate at main.ts:899)"
      pattern: "if \\(!useInlineWidget\\)"
---

<objective>
Phase 19 widget shipped behind `useInlineWidget` and passed the full automated suite (1906/1912 tests, build exit 0, 22/22 must-haves wired) but human UAT Test 1 surfaced THREE blockers and ONE watch item. This plan closes the three blockers without regressing the v1.2 path or the green automated suite, and adds dedicated test guards so each regression cannot silently re-emerge.

Purpose: Honor CONTEXT C-03 / WIDGET-07 (Reading mode = `editable.of(false)` — no typing, no vim panel), CONTEXT D-03 (Phase 19 widgets render code only — no Run/Submit/AI Debug buttons), and CONTEXT C-13 / THEME-01..03 (theme parity with v1.2). Preserve CONTEXT D-05 (hard-gate — when useInlineWidget=OFF, v1.2 path including its action rows is unchanged), CONTEXT C-14 / VIM-01 (vim only when `getConfig('vimMode')=true` AT mount time), CONTEXT C-17 (the 5 v1.2 files stay), CONTEXT C-16 (no new runtime deps), and the CLAUDE.md `'leetcode.*'` userEvent + Phase 17 D-05 paragraphs.

Output:
- `src/widget/WidgetController.ts` — vim mount becomes `vimEnabled && !readOnly`; comment block updated to record the gap-closure rationale.
- `src/main.ts` — `registerCodeBlockActionProcessor(this)` (currently main.ts:848) and `this.registerEditorExtension(buildCodeActionsEditorExtension(this))` (currently main.ts:859) are wrapped in `if (!useInlineWidget) { ... }` so the v1.2 button row stays for the v1.2 path and is suppressed for the v1.3 widget path.
- `tests/widget/readOnlyMount.test.ts` — NEW: extension-list assertion that when `readOnly=true` AND `vimMode=true`, the vim extension is NOT in the buildExtensions output AND `EditorView.editable.of(false)` IS present.
- `tests/main/inlineWidgetActionGate.test.ts` — NEW: assert `registerMarkdownPostProcessor` (the surface used by `registerCodeBlockActionProcessor`) and `registerEditorExtension` for the action-row StateField are NOT called for the action-row code paths when `useInlineWidget=true`. Also assert that with `useInlineWidget=false`, BOTH registrations DO fire (regression guard for D-05).
- `tests/widget/vimMount.test.ts` — EXTEND: add a case where `vimMode=true` but `readOnly=true` → vim must NOT mount.

Notable explicit non-goals (do NOT touch in this plan):
- Watch item (CM6 RangeError on multi-line `Decoration.replace`) — verify-after-fix only; do not refactor `liveModeViewPlugin.ts` to a StateField in this plan unless the verify step reproduces it.
- Phase 20 reload/conflict modal, action-row inside widget DOM, language switching reactivity — explicitly Phase 20 (CONTEXT deferred ideas).
- `'leetcode.*'` userEvent paragraph and Phase 17 D-05 paragraph in CLAUDE.md — preserve verbatim per CONTEXT C-17.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-widget-foundation-one-way-sync/19-CONTEXT.md
@.planning/phases/19-widget-foundation-one-way-sync/19-RESEARCH.md
@.planning/phases/19-widget-foundation-one-way-sync/19-PATTERNS.md
@.planning/phases/19-widget-foundation-one-way-sync/19-VERIFICATION.md
@.planning/phases/19-widget-foundation-one-way-sync/19-HUMAN-UAT.md
@.planning/phases/19-widget-foundation-one-way-sync/19-01-SUMMARY.md
@.planning/phases/19-widget-foundation-one-way-sync/19-02-SUMMARY.md
@.planning/phases/19-widget-foundation-one-way-sync/19-04-SUMMARY.md

@src/widget/WidgetController.ts
@src/widget/codeBlockProcessor.ts
@src/widget/liveModeViewPlugin.ts
@src/main/codeActionsPostProcessor.ts
@src/main/codeActionsEditorExtension.ts
@src/main/childEditorFactory.ts
@src/main/childEditorTheme.ts
@src/main.ts
@CLAUDE.md
@tests/widget/vimMount.test.ts
@tests/widget/themeIntegration.test.ts

<interfaces>
<!-- Key contracts the executor must honor. Extracted from current source. -->

From src/widget/WidgetController.ts (CURRENT — buggy state at line 251–319):
```typescript
// CURRENT — line 261-264 (read once at mount):
const getConfig = plugin.app.vault.getConfig;
const vimEnabled =
  typeof getConfig === 'function' &&
  (getConfig.call(plugin.app.vault, 'vimMode') as boolean | undefined) === true;

// CURRENT — line 270 (BUG: gate omits readOnly):
...(vimEnabled ? [vim({ status: true } as Parameters<typeof vim>[0])] : []),

// CURRENT — line 304 (correct in isolation, but vim() above silently
// re-asserts editable=true via @replit/codemirror-vim's internal editable
// extension that wins by extension order):
EditorView.editable.of(!readOnly),
```
The fix is exactly: change line 270's gate from `vimEnabled` to `vimEnabled && !readOnly`. No other lines in `buildExtensions` need to change — `EditorView.editable.of(!readOnly)` at line 304 already does the right thing once vim is no longer fighting it.

From src/main/codeActionsPostProcessor.ts:1–25 (the v1.2 Reading-mode action processor — registered at main.ts:848 unconditionally):
```typescript
export function registerCodeBlockActionProcessor(
  plugin: Plugin & CodeBlockButtonRowHost,
): void {
  plugin.registerMarkdownPostProcessor((element, ctx) => {
    // ... matches by lc-slug + first <pre> under ## Code, fence-tag-AGNOSTIC.
    // Inserts buttons under the widget's <pre> too unless we gate the call.
  });
}
```

From src/main/codeActionsEditorExtension.ts:320 (the v1.2 Live Preview action StateField — registered at main.ts:859 unconditionally):
```typescript
export function buildCodeActionsEditorExtension(
  plugin: PluginHost,
): Extension {
  // Returns a StateField<DecorationSet> producing a BLOCK widget with
  // chevron+Run+Submit row beneath the ## Code fence. lc-slug-gated.
  // Lc-slug-gated, fence-tag-AGNOSTIC — fires on any ``` fence under ## Code,
  // including the new leetcode-solve fence rendered by the widget.
}
```

From src/main.ts:843–862 (CURRENT registration site — both calls run BEFORE the useInlineWidget hard-gate at main.ts:899):
```
Step 6e (line 843–848):
  registerCodeBlockActionProcessor(this);   // v1.2 Reading-mode buttons
Step 6f (line 850–859):
  this.registerEditorExtension(             // v1.2 Live Preview action row
    buildCodeActionsEditorExtension(this),
  );
Phase 19 hard-gate (line 873–1007):
  if (useInlineWidget) {
    // Plan 19-01..04 widget registrations
  }
```
The gap-closure restructures this so the v1.2 action processors land INSIDE an `if (!useInlineWidget)` block (or, equivalently, an early-return guard before each call). Either shape is acceptable — pick the one with the smallest diff. The mutual-exclusion assert at lines 873–878 must remain BEFORE the new gate so corrupt data.json (both flags ON) still resolves to widget-only.

From src/main/childEditorFactory.ts:381–395 (v1.2 theme block — already mirrored verbatim in WidgetController.buildExtensions:287–301):
```typescript
EditorView.theme({
  '&': { background: 'var(--code-background, var(--background-secondary))', borderRadius: '4px', padding: '8px 0' },
  '.cm-content': { fontFamily: 'var(--font-monospace)', fontSize: 'var(--font-text-size)' },
  '.cm-gutters': { background: 'transparent', borderRight: 'none' },
}),
```
Theme parity is already byte-identical in source. Any remaining visual font-size delta in UAT after BLOCKER 1 + BLOCKER 2 fixes is most plausibly a transitive effect of the white unstyled vim panel raising the container's effective height/baseline metrics. If a real font-size delta persists post-fix, it is a Phase 20 polish item (THEME-04 territory) — capture with a screenshot in the next UAT and defer; do NOT modify the theme block in this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate vim mount on `vimEnabled && !readOnly` in WidgetController.buildExtensions; add test guard</name>
  <files>src/widget/WidgetController.ts, tests/widget/readOnlyMount.test.ts, tests/widget/vimMount.test.ts</files>
  <read_first>
    - src/widget/WidgetController.ts:251-319 (buildExtensions function — current vim gate at line 270 omits readOnly; editable.of at line 304 is correct in isolation but vim's internal editable wins by extension order)
    - tests/widget/vimMount.test.ts:1-160 (existing pattern for asserting against buildExtensions output; uses a `vimMode` fixture knob and inspects the returned Extension array length / shape via spy on `vim()`)
    - src/main/childEditorFactory.ts:261-323 (v1.2 mount also gates on `vimEnabled` only, but v1.2 child editors are NEVER read-only so the missing `&& !readOnly` was acceptable there. Phase 19 widgets DO have a readOnly mode (Reading-mode + embeds + stray-fence) — confirms why the parity copy from v1.2 needs to diverge here.)
    - tests/widget/themeIntegration.test.ts (existing pattern for asserting on the EditorView.theme bits; reuse the same harness style for the new readOnlyMount test)
  </read_first>
  <behavior>
    Test 1 (NEW — tests/widget/readOnlyMount.test.ts):
    - GIVEN `readOnly=true` and `vimMode=true` (Obsidian vim setting ON)
      WHEN buildExtensions runs (or mountLeetCodeWidget is invoked with readOnly=true)
      THEN the resulting Extension array MUST NOT contain the vim() return value
      AND the resulting Extension array MUST contain `EditorView.editable.of(false)`
    - GIVEN `readOnly=true` and `vimMode=false`
      WHEN buildExtensions runs
      THEN no vim() in extensions; editable.of(false) present
    - GIVEN `readOnly=false` and `vimMode=true`
      WHEN buildExtensions runs
      THEN vim() IS present in extensions; editable.of(true) present
    - GIVEN `readOnly=false` and `vimMode=false`
      WHEN buildExtensions runs
      THEN no vim(); editable.of(true) present

    Test 2 (EXTEND — tests/widget/vimMount.test.ts):
    - Add the `vimMode=true && readOnly=true` case to the existing matrix; assert vim() is NOT spied as called.

    Edge cases:
    - `getConfig` undefined (test fixture omits it) — vim never mounts regardless of readOnly. Existing behavior, preserve.
    - `getConfig` returns truthy non-`true` (e.g., string "true") — vim never mounts (strict equality `=== true` already enforces this).
  </behavior>
  <action>
    Step 1 — Edit src/widget/WidgetController.ts:
    - Locate line 270 (the conditional vim spread inside `buildExtensions`).
    - Change the spread guard from `vimEnabled` to `vimEnabled && !readOnly`.
    - Update the immediately-preceding doc comment block (lines 268-269) to record the gap-closure rationale: vim's `vim()` extension internally registers an editable behavior that wins by extension order over `EditorView.editable.of(!readOnly)` at line 304; gating vim on `!readOnly` is the only correct fix for read-only mounts (vim has no "read-only mode" of its own; even with editable.of(false) present, vim re-enables typing). Reference WIDGET-07 + CONTEXT C-03 + UAT Test 1 BLOCKER 1.
    - Do NOT change line 304 (`EditorView.editable.of(!readOnly)`); it is already correct.
    - Do NOT change anything else in `buildExtensions`. Theme parity (lines 287-301) is already byte-identical to v1.2 (childEditorFactory.ts:381-395); changing it would regress THEME-01..03.
    - Verify nothing else in WidgetController.ts (mount factory, RenderChild, controller class) needs to change. The fenceIndex / persistenceKey / debouncedWriter / hydrate paths all already correctly skip the writer when `readOnly=true` (line 395-413).

    Step 2 — Create tests/widget/readOnlyMount.test.ts:
    - Mirror the pattern from tests/widget/vimMount.test.ts. Stub `@replit/codemirror-vim`'s `vim` export with a `vi.fn(() => SENTINEL)` and assert SENTINEL appears (or does not appear) in the extension array returned by `mountLeetCodeWidget` (use the same plugin-host fixture pattern as vimMount.test.ts:80-110).
    - For the editable.of(...) assertion, the cleanest approach is to spy on `EditorView.editable.of` (already a static factory). If `@codemirror/view` is mocked in the existing fixtures, follow the existing mock pattern; otherwise mount a real EditorView and assert `view.contentDOM.contentEditable` reflects the readOnly flag. Either approach is acceptable — pick the one consistent with vimMount.test.ts.
    - Cover all four cells of the (readOnly × vimMode) matrix.
    - Add a regression note in the test file header referencing UAT Test 1 BLOCKER 1.

    Step 3 — Edit tests/widget/vimMount.test.ts:
    - Add a single test case to the existing matrix: `readOnly=true, vimMode=true → vim() NOT called`. Use the same fixture plumbing the file already has; this is purely an additive case.

    No other source files change. No new runtime deps (CONTEXT C-16). No `'leetcode.*'` userEvent paragraph touch (CLAUDE.md C-17).
  </action>
  <verify>
    <automated>npm test -- --run tests/widget/readOnlyMount.test.ts tests/widget/vimMount.test.ts tests/widget/WidgetController.test.ts tests/widget/themeIntegration.test.ts</automated>
    <human-check>
      After Task 3 completes, in dev vault with useInlineWidget=ON and Obsidian vimMode=ON:
      1. Open an LC note in Reading mode. Inspect the widget container in DevTools.
      2. Confirm `.cm-content` has `contenteditable="false"` (or no contenteditable attribute).
      3. Confirm NO `.cm-vim-panel` element anywhere inside the widget container.
      4. Click inside the widget and try to type — no character should appear.
      5. Switch to Live Preview. Confirm `.cm-content` has `contenteditable="true"` AND `.cm-vim-panel` IS rendered (vim panel only in editable mode).
    </human-check>
  </verify>
  <done>
    - Single-line gate change applied at WidgetController.ts:270 (`vimEnabled && !readOnly`).
    - tests/widget/readOnlyMount.test.ts exists with all four matrix cells passing.
    - tests/widget/vimMount.test.ts has the new readOnly=true case passing.
    - Full widget test suite (`npm test -- --run tests/widget/`) green; no test count regression vs the pre-gap baseline (182 widget tests previously per VERIFICATION.md; new file adds at minimum 4 tests).
    - Build (`npm run build`) exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate v1.2 action-row processors on `!useInlineWidget` in src/main.ts; add registration-gate test</name>
  <files>src/main.ts, tests/main/inlineWidgetActionGate.test.ts</files>
  <read_first>
    - src/main.ts:843-918 (current registration order — `registerCodeBlockActionProcessor(this)` at line 848 and `registerEditorExtension(buildCodeActionsEditorExtension(this))` at line 859 BOTH run unconditionally; the useInlineWidget hard-gate begins at line 899)
    - src/main.ts:862-878 (the `useInlineWidget` read at line 864 + mutual-exclusion assert at lines 873-878 — the new gate must come AFTER the read but its placement relative to the assert can be either side; document the choice)
    - src/main/codeActionsPostProcessor.ts:1-67 (matches by lc-slug + first `<pre>` under `## Code` heading; fence-tag-AGNOSTIC — that's WHY it picks up the new widget's code area)
    - src/main/codeActionsEditorExtension.ts:165-296 (StateField produces a BLOCK widget below the closer-fence line for any ``` fence under ## Code; lc-slug-gated; fence-tag-AGNOSTIC — same reason as the post-processor)
    - tests/main/codeActionsPostProcessor.test.ts (existing test pattern for the v1.2 path — keep regression-clean)
    - tests/main/codeActionsEditorExtension.test.ts (existing test pattern for the v1.2 LP path — keep regression-clean)
    - tests/main/mutualExclusion.test.ts:1-160 (existing pattern for asserting on plugin onload registration calls; new test follows this shape)
  </read_first>
  <behavior>
    Test 1 (NEW — tests/main/inlineWidgetActionGate.test.ts):
    - GIVEN `useInlineWidget=true` (mock settings store returns true)
      WHEN `Plugin.onload()` runs to completion
      THEN `plugin.registerMarkdownPostProcessor` MUST NOT have been called with the codeActionsPostProcessor handler
      AND `plugin.registerEditorExtension` MUST NOT have been called with the action-row StateField extension
      (other registerMarkdownPostProcessor / registerEditorExtension calls — e.g., the widget's own — are out of scope for this assertion; gate on the SPECIFIC factories.)
    - GIVEN `useInlineWidget=false` (default)
      WHEN `Plugin.onload()` runs to completion
      THEN `registerCodeBlockActionProcessor(this)` MUST have been called exactly once
      AND `this.registerEditorExtension(buildCodeActionsEditorExtension(this))` MUST have been called exactly once
      (D-05 hard-gate regression guard — v1.2 path unchanged when widget is OFF.)

    Edge cases:
    - Mutual-exclusion path (`useInlineWidget=true && useNestedEditor=true`): assert sets useNestedEditor=false (existing behavior, mutualExclusion.test.ts), THEN our new gate STILL skips the action-row registrations because useInlineWidget=true. The action row never coexists with the widget regardless of the legacy nested-editor flag.
    - Settings flip mid-session: useInlineWidget is read at onload only (CONTEXT D-05 hard-gate is reload-apply-only). No runtime re-gating required; document this in a code comment.
  </behavior>
  <action>
    Step 1 — Edit src/main.ts:
    - At lines 843-859 (Step 6e + Step 6f), wrap `registerCodeBlockActionProcessor(this);` and `this.registerEditorExtension(buildCodeActionsEditorExtension(this));` in a single `if (!this.settings.getUseInlineWidget())` block. Place the block BETWEEN the existing comments (preserve the Phase 5 / Phase 5.1 documentation prose) so future readers see why the gate exists.
    - Use `this.settings.getUseInlineWidget()` directly (NOT a captured `useInlineWidget` const), because:
        a) The existing `const useInlineWidget = this.settings.getUseInlineWidget();` is declared at line 864 — AFTER the v1.2 action-row registrations at lines 848 + 859. Reordering the const declaration to before line 848 is acceptable but increases diff surface; calling the accessor twice is cheap and keeps the diff minimal.
        b) Either approach is acceptable. Pick whichever produces the smaller, clearer diff. Document the choice in a single-line comment referencing UAT Test 1 BLOCKER 2 + CONTEXT D-03 + D-05.
    - Do NOT touch the mutual-exclusion assert at lines 873-878 — it remains BEFORE the widget hard-gate at line 899 and continues to force `useNestedEditor=false` when `useInlineWidget=true`.
    - Do NOT touch the widget hard-gate at lines 899-1007 — it remains AS-IS (Plan 19-01..04 wiring intact).
    - Add a header comment block above the new gate explaining the rationale: "Phase 19 gap-closure (UAT Test 1 BLOCKER 2 / CONTEXT D-03): the v1.2 codeActionsPostProcessor and codeActionsEditorExtension are FENCE-TAG-AGNOSTIC — they match `lc-slug + first <pre> under ## Code` and a `lc-slug + first ``` fence under ## Code` respectively. With useInlineWidget=ON, both happily fire on the new `leetcode-solve` fence and render Run/Submit/AI Debug buttons under the widget — but Phase 19 D-03 explicitly excludes the action row (Phase 20 territory). Hard-gate them off when the widget is active. When useInlineWidget=OFF, BOTH must fire (D-05 — v1.2 path unchanged)."

    Step 2 — Create tests/main/inlineWidgetActionGate.test.ts:
    - Mirror tests/main/mutualExclusion.test.ts shape: construct a fake plugin with `settings.getUseInlineWidget` toggleable; spy on `plugin.registerMarkdownPostProcessor` and `plugin.registerEditorExtension`; invoke `onload()` (or just the registration block factored into a testable helper if mutualExclusion.test.ts already does that — follow the established pattern).
    - Two top-level describes: `useInlineWidget=true` and `useInlineWidget=false`.
    - Each describe asserts the correct registrations did/didn't fire.
    - Add a third describe asserting no behavior change when `useNestedEditor` flips alongside `useInlineWidget` (mutual-exclusion compat).

    No other source files change. No new runtime deps (CONTEXT C-16).
  </action>
  <verify>
    <automated>npm test -- --run tests/main/inlineWidgetActionGate.test.ts tests/main/codeActionsPostProcessor.test.ts tests/main/codeActionsEditorExtension.test.ts tests/main/mutualExclusion.test.ts</automated>
    <human-check>
      After Task 3 completes, in dev vault:
      1. With useInlineWidget=ON, open an LC note in Reading mode. Inspect the DOM under the `## Code` heading. Confirm NO `.leetcode-code-actions` element anywhere in or below the widget container.
      2. Switch to Live Preview. Confirm NO `.leetcode-code-actions` block widget below the fence.
      3. Toggle useInlineWidget=OFF, reload Obsidian. Reopen the same note. Confirm `.leetcode-code-actions` IS present below the fence in BOTH Reading mode AND Live Preview (v1.2 path unchanged — D-05 regression guard).
    </human-check>
  </verify>
  <done>
    - main.ts wraps both v1.2 action-row registrations in `if (!useInlineWidget) { ... }`.
    - tests/main/inlineWidgetActionGate.test.ts exists with both describes (true and false branches) green.
    - Existing mutualExclusion.test.ts + codeActionsPostProcessor.test.ts + codeActionsEditorExtension.test.ts still green (no regression).
    - Full main test suite (`npm test -- --run tests/main/`) green.
    - Build (`npm run build`) exits 0.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live UAT re-run — re-test Test 1 (Reading mode + Live Preview two-path mount and edit behavior)</name>
  <files>(manual UAT — gap closure verification; no source files modified)</files>
  <what-built>
    Tasks 1+2 closed BLOCKER 1 (vim leak in read-only) + BLOCKER 2 (action row under widget). BLOCKER 3 (font/visual drift) is hypothesized to flow transitively from removing the white unstyled vim panel — to be confirmed in this UAT.
  </what-built>
  <action>Manual user-acceptance re-test of UAT Test 1 (Reading mode + Live Preview two-path mount and edit behavior). The developer executes the steps in <how-to-verify> against a real Obsidian dev vault to confirm BLOCKER 1 (read-only editable + vim leak), BLOCKER 2 (action row under widget), and BLOCKER 3 (font/visual drift) are resolved, and that the WATCH item (CM6 RangeError) does not reproduce. The previous UAT report (19-HUMAN-UAT.md Test 1, severity blocker) is the baseline.</action>
  <how-to-verify>
    Pre-conditions:
    1. Run `npm run build`. Confirm exit 0.
    2. Copy `main.js` and `manifest.json` into the dev vault plugin folder: `~/Documents/Obsidian Vault/.obsidian/plugins/obsidian-leetcode/`.
    3. In Obsidian, fully reload (Cmd-R) so onload() re-runs with the fixed gate.
    4. Confirm Settings → Experimental → useInlineWidget = ON.
    5. Confirm Settings → Editor → Vim key bindings = ON.

    Test re-run for UAT Test 1:
    1. Open an LC note (must have `lc-slug` and a `leetcode-solve` fence in `## Code`). Switch to Reading mode.
       - Expected: widget renders. DOM container has classes `lc-nested-editor HyperMD-codeblock lc-leetcode-solve`.
       - Expected: `.cm-content` has `contenteditable="false"` (or no contenteditable attribute). [BLOCKER 1 fix]
       - Expected: NO `.cm-vim-panel` element. [BLOCKER 1 fix]
       - Expected: typing has no effect (no characters appear). [BLOCKER 1 fix]
       - Expected: NO `.leetcode-code-actions` element below the widget. [BLOCKER 2 fix]
       - Expected: font size visually matches v1.2 path (compare side-by-side with useInlineWidget=OFF in another note, or screenshot the previous UAT for diff). [BLOCKER 3 — likely transitively resolved]
       - Expected: NO CM6 RangeError in DevTools console. [WATCH item — confirm non-reproduction]
    2. Switch the same note to Live Preview.
       - Expected: widget renders editable.
       - Expected: `.cm-content` has `contenteditable="true"`.
       - Expected: vim panel `.cm-vim-panel` IS rendered (vim mounts because !readOnly).
       - Expected: typing → 400ms later, fence body on disk byte-matches widget doc.
       - Expected: NO `.leetcode-code-actions` block widget below the fence. [BLOCKER 2 fix]
    3. Regression guard for D-05: toggle useInlineWidget=OFF, reload Obsidian, reopen the same note.
       - Expected: v1.2 nested editor mounts (different DOM shape).
       - Expected: `.leetcode-code-actions` row IS present below the fence in BOTH Reading mode and Live Preview (v1.2 path unchanged).

    If BLOCKER 3 (font drift) STILL reproduces after BLOCKER 1 + 2 fixes:
    - Capture a side-by-side screenshot (useInlineWidget=ON vs OFF, same note, same theme).
    - Note the delta in `resume-signal` description. Do NOT block this UAT on it; the UAT can pass with BLOCKER 3 downgraded to a Phase 20 polish item (THEME-04 territory) since C-13 / THEME-01..03 wiring is verified at the source level (theme block byte-identical to v1.2; container classes verified in DOM per the existing UAT report).

    If WATCH item (CM6 RangeError) reproduces:
    - Capture the full DevTools console stack trace.
    - Note in `resume-signal`. The fix (refactor liveModeViewPlugin.ts to a StateField source for the Decoration.replace) is non-trivial and should be a NEW gap-closure plan (19-06), not folded into this one.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - "approved" — all three BLOCKERS resolved, UAT Test 1 passes
    - "approved-with-deferred: BLOCKER 3 still reproduces (defer to Phase 20)" — Tasks 1+2 fixed BLOCKERS 1+2 cleanly, BLOCKER 3 minor visual drift persists and is acceptable for Phase 19 close-out
    - "blocked: <description>" — describe the remaining defect; trigger another gap-closure plan (19-06)
  </resume-signal>
  <verify>
    <automated>npm run build && npm test -- --run tests/widget/ tests/main/inlineWidgetActionGate.test.ts tests/main/mutualExclusion.test.ts</automated>
    <human-check>The developer follows every numbered step under <how-to-verify> in a live Obsidian dev vault with the freshly built main.js deployed; signs off via the <resume-signal> string above.</human-check>
  </verify>
  <done>
    UAT signal received from the developer. The signal is one of:
    - "approved" → BLOCKERS 1+2+3 resolved, WATCH item not reproduced; phase 19 cleared for closeout.
    - "approved-with-deferred: BLOCKER 3 still reproduces (defer to Phase 20)" → BLOCKERS 1+2 resolved, BLOCKER 3 minor visual delta deferred to THEME-04 / Phase 20; phase 19 cleared for closeout.
    - "blocked: <description>" → record the failure, open gap closure plan 19-06.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reading-mode markdown post-processor → embedded CM6 | Untrusted vault content (potentially from external vault sync, embed inclusion, or a non-LC note hosting a stray `leetcode-solve` fence) crosses into the embedded EditorView. Privilege drift if the read-only mount silently accepts edits (BLOCKER 1). |
| v1.2 action-row processors → widget DOM | The v1.2 codeActionsPostProcessor + codeActionsEditorExtension are fence-tag-AGNOSTIC — they match by `lc-slug` frontmatter, NOT by fence tag. They will surface Run/Submit/AI Debug buttons under any `<pre>`/``` fence in the `## Code` section, including the new widget's `leetcode-solve` fence. Privilege drift if the widget exposes execute-code affordances Phase 19 explicitly excludes (BLOCKER 2). |
| Plugin onload settings read | `useInlineWidget` is read from `data.json` at onload. Corrupt data.json (both flags ON) is the bisection-poisoning attack vector — the existing mutual-exclusion assert (main.ts:873-878) handles this; the new `!useInlineWidget` action-row gate must NOT regress that handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.05-01 | Elevation of Privilege | `WidgetController.buildExtensions` (vim mount) | mitigate | Task 1 — gate vim on `vimEnabled && !readOnly`. Vim's internal editable behavior wins by extension order over `EditorView.editable.of(false)`; gating vim on `!readOnly` is the only correct fix for read-only mounts. Test guard at tests/widget/readOnlyMount.test.ts asserts vim() is NOT in the extension list when readOnly=true. |
| T-19.05-02 | Information Disclosure / Privilege Drift | v1.2 `codeActionsPostProcessor` + `buildCodeActionsEditorExtension` registered against widget DOM | mitigate | Task 2 — gate both registrations on `!useInlineWidget` at onload. With useInlineWidget=ON, the widget surface contains exactly the embedded EditorView and theme/atomicRanges chrome (CONTEXT D-03). Action affordances (Run/Submit/AI Debug) only appear via the v1.2 path. Test guard at tests/main/inlineWidgetActionGate.test.ts asserts the gate enforces this in both branches. |
| T-19.05-03 | Tampering | Reading-mode mount accepting edits | mitigate | Closed by T-19.05-01 fix. Once vim cannot mount in read-only, `EditorView.editable.of(false)` at WidgetController.ts:304 holds — DOM `contenteditable=false`, keystrokes drop, no `vault.process` call path is reachable from a read-only widget (the writer is also gated on `!readOnly` at WidgetController.ts:395-413, so even if vim DID get an edit through, no write would reach disk — defense in depth). |
| T-19.05-04 | Repudiation | Mutual-exclusion data.json corruption | accept | Existing mutual-exclusion assert at main.ts:873-878 already handles `useInlineWidget=true && useNestedEditor=true` by forcing useNestedEditor=false + Notice. Task 2 must NOT reorder this assert. mutualExclusion.test.ts (8 tests) regression-protects. |
| T-19.05-SC | Tampering | npm/pip/cargo installs | accept | This plan adds NO new runtime deps (CONTEXT C-16). All test additions use the existing vitest + happy-dom + the `@codemirror/*` + `obsidian` stubs already in tests/. No package legitimacy gate required. |
</threat_model>

<verification>
## Phase-level checks after Task 3 sign-off

1. **Full test suite** (`npm test -- --run`) — must remain at or above the 1906/1912 baseline (VERIFICATION.md). Adding readOnlyMount.test.ts (4 cases) + inlineWidgetActionGate.test.ts (3+ cases) + the new vimMount.test.ts case = ~8 new tests; expect 1914+/1920+ passing.
2. **Build** (`npm run build`) — exits 0.
3. **Source-level grep regression checks:**
   - `grep -nE "vimEnabled\s*\?\s*\[vim" src/widget/WidgetController.ts` → 0 hits (the bare `vimEnabled ?` form is gone).
   - `grep -nE "vimEnabled && !readOnly" src/widget/WidgetController.ts` → 1 hit (the fixed gate).
   - `grep -nE "if\s*\(\s*!\s*(this\.settings\.getUseInlineWidget|useInlineWidget)" src/main.ts` → at least 1 hit (the new action-row gate).
   - `grep -c "leetcode\.\*" CLAUDE.md` → 1 (CLAUDE.md userEvent paragraph preserved per CONTEXT C-17).
   - `grep -c "Phase 17 D-05" CLAUDE.md` → 1 (Phase 17 D-05 paragraph preserved per CONTEXT C-17).
   - `grep -nE "EditorView\.editable\.of" src/widget/WidgetController.ts` → exactly 1 hit, at line ~304, unchanged.
4. **No new runtime deps:** `git diff main..HEAD -- package.json` is empty (CONTEXT C-16).
5. **v1.2 files preserved:** `ls src/main/childEditorSync.ts sectionLockExtension.ts nestedEditorExtension.ts childEditorRegistry.ts codeActionsEditorExtension.ts` → all 5 present (CONTEXT C-17).
6. **UAT Test 1 re-run** (Task 3 checkpoint) — three BLOCKER assertions pass. WATCH item either non-reproduces (preferred) or escalates to a NEW gap-closure plan (19-06).
</verification>

<success_criteria>
Phase 19 gap-closure plan 19-05 is complete when:

- [ ] `src/widget/WidgetController.ts:270` reads `vimEnabled && !readOnly` (single-line gate change with updated comment block).
- [ ] `src/main.ts` wraps `registerCodeBlockActionProcessor(this);` AND `this.registerEditorExtension(buildCodeActionsEditorExtension(this));` in `if (!useInlineWidget) { ... }` (with header comment referencing UAT Test 1 BLOCKER 2 / CONTEXT D-03 + D-05).
- [ ] `tests/widget/readOnlyMount.test.ts` exists; all four (readOnly × vimMode) cells pass.
- [ ] `tests/widget/vimMount.test.ts` includes the `readOnly=true && vimMode=true → no vim()` case.
- [ ] `tests/main/inlineWidgetActionGate.test.ts` exists; both branches (`useInlineWidget=true` skips, `useInlineWidget=false` registers) pass.
- [ ] `npm test -- --run` exit 0, total passing tests ≥ 1914.
- [ ] `npm run build` exit 0.
- [ ] All grep regression checks (verification §3) match.
- [ ] Task 3 UAT checkpoint received `approved` or `approved-with-deferred: BLOCKER 3 still reproduces (defer to Phase 20)`.
- [ ] `package.json` runtime deps unchanged (CONTEXT C-16).
- [ ] CLAUDE.md `'leetcode.*'` userEvent + Phase 17 D-05 paragraphs preserved (CONTEXT C-17).
</success_criteria>

<output>
Create `.planning/phases/19-widget-foundation-one-way-sync/19-05-SUMMARY.md` capturing:
- Which BLOCKER each task closed and the exact line-level change.
- Test count delta (before / after).
- Whether BLOCKER 3 (font drift) resolved transitively or was deferred to Phase 20 / THEME-04.
- Whether the WATCH item (CM6 RangeError) reproduced post-fix; if yes, recommend a 19-06 plan.
- One-line statement that the CLAUDE.md userEvent + Phase 17 D-05 paragraphs are unchanged.
</output>
