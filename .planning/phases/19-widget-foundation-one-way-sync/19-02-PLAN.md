---
phase: 19-widget-foundation-one-way-sync
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - src/widget/selfWriteSuppression.ts
  - src/widget/debouncedWriter.ts
  - src/widget/WidgetController.ts
  - src/widget/widgetRegistry.ts
  - src/main.ts
  - tests/widget/modifyEventOrdering.probe.test.ts
  - tests/widget/debouncedWriter.test.ts
  - tests/widget/selfWriteSuppression.test.ts
  - tests/widget/flushRateLimit.test.ts
  - tests/widget/flushTransitions.test.ts
  - tests/widget/postFlushDiagnostic.test.ts
  - tests/widget/fenceIndexRecompute.test.ts
autonomous: false
requirements:
  - SYNC-01
  - SYNC-02
  - SYNC-03
  - SYNC-06
  - SYNC-07
must_haves:
  truths:
    - "Typing inside the widget triggers a vault.process write to the host file ~400ms after the last keystroke (configurable per widgetSyncDebounceMs); fence body on disk is byte-identical to widget.state.doc.toString()"
    - "Self-writes do NOT cause widget reload — vault.on('modify') consults selfWriteSuppression and drops matching entries"
    - "External writes (no matching suppression entry) are observed by the modify listener and logged (Plan 19-02 owns suppression; Plan 20 owns reload)"
    - "Per-file flushes never exceed 1 per 200ms regardless of typing speed"
    - "Force-quit Obsidian within ms of typing — most-recent characters present on disk on next open (workspace.on('quit') Tasks.add path is primary; beforeunload is best-effort)"
    - "extractFenceBody/rewriteFenceBody byte-exactness verified at runtime via post-flush hash diagnostic console.warn — no false positives during normal typing"
    - "fenceIndex is recomputed at flush time (not just at mount) so multi-fence inserts above the active fence don't mis-target"
  artifacts:
    - path: src/widget/selfWriteSuppression.ts
      provides: "Per-path content-hash suppression map with 2s TTL; arm()/tryConsume()"
      exports: ["SelfWriteSuppression"]
      min_lines: 60
    - path: src/widget/debouncedWriter.ts
      provides: "Obsidian Debouncer-backed writer; rate-limit gate; arm-then-write order; post-flush hash diagnostic"
      exports: ["DebouncedWriter"]
      min_lines: 100
    - path: src/widget/WidgetController.ts
      provides: "flushNow() implementation; debouncedWriter binding via updateListener.of"
      contains: "flushNow"
    - path: src/widget/widgetRegistry.ts
      provides: "flushAll/flushAllSync/flushFile real implementations"
      contains: "flushAll"
    - path: src/main.ts
      provides: "Six flush-on-transition hooks + vault.on('modify') consumer + selfWriteSuppression instance"
      contains: "selfWriteSuppression"
  key_links:
    - from: src/widget/WidgetController.ts
      to: src/widget/debouncedWriter.ts
      via: this.writer = new DebouncedWriter(...) inside mountLeetCodeWidget
      pattern: "new DebouncedWriter"
    - from: src/widget/debouncedWriter.ts
      to: src/widget/selfWriteSuppression.ts
      via: this.suppression.arm(file.path, expectedHash)
      pattern: "suppression\\.arm"
    - from: src/main.ts
      to: src/widget/selfWriteSuppression.ts
      via: vault.on('modify') handler calls suppression.tryConsume
      pattern: "tryConsume"
    - from: src/main.ts
      to: src/widget/widgetRegistry.ts
      via: workspace.on('active-leaf-change') / workspace.on('quit') / vault.on('rename') / beforeunload → flushAll/flushFile
      pattern: "widgetRegistry\\.flush"
---

<objective>
Phase 19 Plan 02 — Debounced One-Way Sync + Suppression: ship the canonical write path. Typing in the widget produces atomic `vault.process` writes via a per-file debouncer; self-writes are suppressed via a per-path content-hash map (2s TTL); rate-limited to 1 flush per 200ms per file; flushed on six transition hooks; runtime hash diagnostic catches drift.

Purpose: SYNC-01 (debounced 400ms default with 300/500/1000/2000ms options), SYNC-02 (six flush hooks), SYNC-03 (per-path content-hash suppression — NOT a boolean — CONTEXT C-04, RESEARCH Pitfall 19-A), SYNC-06 runtime portion (post-flush hash diagnostic — D-09), SYNC-07 (rate limit). Wave 0 of this Plan is an empirical probe of the `vault.on('modify')` event-ordering assumption (RESEARCH Pitfall 19-A / Open Question 1) — the suppression map's correctness depends on it.

Output: 2 new src files (selfWriteSuppression.ts, debouncedWriter.ts), updates to WidgetController.ts and widgetRegistry.ts, additions to src/main.ts onload/onunload, 6 new test files. Acceptance: type 5 chars then Cmd-Q within 100ms — chars present on disk; fence body on disk byte-matches widget doc within 400ms of last keystroke; modify listener never reloads the widget for its own writes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-widget-foundation-one-way-sync/19-CONTEXT.md
@.planning/phases/19-widget-foundation-one-way-sync/19-RESEARCH.md
@.planning/phases/19-widget-foundation-one-way-sync/19-PATTERNS.md
@.planning/phases/19-widget-foundation-one-way-sync/19-VALIDATION.md
@.planning/phases/19-widget-foundation-one-way-sync/19-01-SUMMARY.md
@CLAUDE.md
@src/main/childEditorSync.ts
@src/graph/copyToCode.ts
@src/widget/WidgetController.ts
@src/widget/widgetRegistry.ts
@src/widget/fenceLocator.ts
@src/widget/fenceSerialization.ts
@src/main.ts

<interfaces>
<!-- Contracts from Plan 19-01 + Obsidian + new helpers in this Plan -->

From Plan 19-01 (src/widget/WidgetController.ts):
```typescript
class WidgetController {
  view: EditorView;
  file: TFile;
  fenceIndex: number;
  plugin: LeetCodePlugin;
  flushNow(): void;       // STUB in Plan 19-01 — Plan 19-02 implements
  destroy(): void;
  getDoc(): string;
}
function mountLeetCodeWidget(host, source, file, plugin, readOnly): WidgetController;
```

From src/widget/fenceSerialization.ts (Plan 19-01):
```typescript
export function extractFenceBody(noteBody: string, fenceIndex: number): string | null;
export function rewriteFenceBody(noteBody: string, fenceIndex: number, newBody: string): string;
```

From src/widget/fenceLocator.ts (Plan 19-01):
```typescript
export function computeFenceIndex(fileText: string, fenceLineStart0Based: number): number;
```

From obsidian (verified):
```typescript
function debounce<T, V>(cb: (...args: T) => V, timeout?: number, resetTimer?: boolean): Debouncer<T, V>;
interface Debouncer<T, V> { run(...args: T): V | void; cancel(): this; }

interface Vault {
  read(file: TFile): Promise<string>;
  process(file: TFile, fn: (data: string) => string, options?: any): Promise<string>;
  on(name: 'modify' | 'rename' | 'create' | 'delete', cb: (...args: any[]) => any, ctx?: any): EventRef;
}
interface Workspace {
  on(name: 'active-leaf-change', cb: (leaf: WorkspaceLeaf | null) => void): EventRef;
  on(name: 'quit', cb: (tasks: { add: (promise: Promise<any>) => void }) => void): EventRef;  // since 1.4.4
}
class Plugin {
  registerEvent(ref: EventRef): void;
  registerDomEvent(el: Window | Document | HTMLElement, type: string, cb: (e: Event) => void): void;
}
```

From PATTERNS write-path (lift verbatim contract):
```typescript
// src/graph/copyToCode.ts:72-78 — canonical vault.process callsite
await app.vault.process(file, (current) => transformBody(current, ...args));
```

New in Plan 19-02:
```typescript
// src/widget/selfWriteSuppression.ts
export class SelfWriteSuppression {
  arm(path: string, expectedHash: string): void;
  tryConsume(path: string, observedHash: string): 'consumed' | 'stale' | 'miss';
  // 2s TTL
}

// src/widget/debouncedWriter.ts
export class DebouncedWriter {
  constructor(app: App, file: TFile, getDoc: () => string, getFenceIndex: () => number, suppression: SelfWriteSuppression, delayMs: number);
  run(): void;            // schedules debounced flush; resetTimer=true
  cancel(): void;         // cancels pending timer
  forceFlush(): Promise<void>;  // cancel timer then flush synchronously
  setDelay(ms: number): void;   // for hot-reconfigure of widgetSyncDebounceMs
}
```

Hash function (RESEARCH "Don't Hand-Roll" + A7): use `crypto.subtle.digest('SHA-1', ...)` Web Crypto API in Electron renderer. Async-await is fine inside `flush()`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — empirical probe + sync test scaffolds (RED)</name>
  <files>tests/widget/modifyEventOrdering.probe.test.ts, tests/widget/debouncedWriter.test.ts, tests/widget/selfWriteSuppression.test.ts, tests/widget/flushRateLimit.test.ts, tests/widget/flushTransitions.test.ts, tests/widget/postFlushDiagnostic.test.ts, tests/widget/fenceIndexRecompute.test.ts</files>
  <read_first>
    - .planning/phases/19-widget-foundation-one-way-sync/19-VALIDATION.md (rows 19-02-01..07)
    - .planning/phases/19-widget-foundation-one-way-sync/19-PATTERNS.md (lines 33-56 test analog table; lines 729-735 modifyEventOrdering analog; lines 730-735 strayFenceFallback notes)
    - .planning/phases/19-widget-foundation-one-way-sync/19-RESEARCH.md (Pitfall 19-A lines 356-366; Pitfall 19-B lines 368-377; Pitfall 19-E lines 401-410; Pattern 2 lines 213-238)
    - .planning/phases/19-widget-foundation-one-way-sync/19-CONTEXT.md (C-04, C-06, C-07, C-08; D-09)
    - tests/main/childEditorSync.test.ts (vault.on('modify') wiring patterns; vault.process callback assertions)
    - tests/main/lifecycle.test.ts (Plugin onunload assertion patterns)
    - tests/widget/__fixtures__/cm6Helpers.ts + lcNoteFixtures.ts (from Plan 19-01)
    - tests/helpers/obsidian-stub.ts
    - tests/solve/mocks/fakeWorkspace.ts (createFakePlugin / createFakeMetadataCache)
  </read_first>
  <behavior>
    - modifyEventOrdering.probe.test.ts: build a fake vault with `process(file, fn)` that synchronously invokes its callback then schedules an async `vault.on('modify')` emit via `Promise.resolve().then(emit)`. Test 1 — `arm(path, hash)` then `await vault.process(...)` then assert listener observed entry as 'consumed'. Test 2 — same flow but emit synchronously inside `process` callback (before resolve); assert this case ALSO consumes (proving robustness either ordering). Test 3 — record the actual ordering fakeVault used and emit a console.warn if production-shape callers must use microtask arming. The probe documents the assumption; production code in Tasks 2-3 chooses the safe path.
    - selfWriteSuppression.test.ts: fresh `SelfWriteSuppression`. arm('a.md', 'h1'); tryConsume('a.md', 'h1') === 'consumed'; second tryConsume === 'miss'. arm('a.md', 'h1'); advance fake time 2001ms via vi.useFakeTimers(); tryConsume === 'stale'. Multi-file: arm('a.md', 'hA'); arm('b.md', 'hB'); tryConsume('a.md', 'hA') === 'consumed'; tryConsume('b.md', 'hB') === 'consumed' — proves no boolean flag. Hash mismatch within TTL: arm('a.md', 'hA'); tryConsume('a.md', 'hX') === 'miss'; entry deleted defensively.
    - debouncedWriter.test.ts: vi.useFakeTimers(). new DebouncedWriter with 400ms delay; call run() once, advance 399ms — vault.process not called. Advance 1ms more — process called exactly once. Run() three times in 100ms intervals — process called exactly once 400ms after the third (resetTimer behavior). cancel() — pending flush dropped. forceFlush() — bypasses timer; resolves with vault.process called. setDelay(1000) at runtime — next run() fires after 1000ms.
    - flushRateLimit.test.ts: configure rateLimitMs=200; force flushes back-to-back; second flush returns/coalesces and is delayed; assert vault.process called twice with ≥200ms gap; advance fake time deterministically.
    - flushTransitions.test.ts: register a fake plugin with widgetRegistry containing two controllers (file A, file B). Simulate `MarkdownRenderChild.onunload` — assert controller A.flushNow called once. Simulate `Plugin.onunload` → registry.flushAll → both controllers' flushNow called. Simulate `workspace.on('active-leaf-change')` → flushAll. Simulate `vault.on('rename')` → flushFile(oldPath) — only matching controller flushNow'd. Simulate `beforeunload` window event → flushAll synchronously (no await). Simulate `workspace.on('quit')` Tasks.add path — promise added; once it resolves, both controllers flushed.
    - postFlushDiagnostic.test.ts: spy on console.warn. Inject a deliberately-broken `rewriteFenceBody` mock (returns body with one extra newline). Trigger flush. Assert `console.warn` called with message matching /post-flush hash drift/ and containing the file path. With the real rewriteFenceBody no warning fires.
    - fenceIndexRecompute.test.ts: mount a controller with initial fenceIndex=0 over a single-fence file. Externally insert a leetcode-solve fence above it (mutate the disk read result for the next flush call). Trigger flush. Assert: (a) controller calls `computeFenceIndex` against the new disk content, (b) detects the shift to index 1, (c) per RESEARCH Pitfall 19-E aborts the write and surfaces a Notice 'Fence position changed; reload to continue editing.', (d) controller does NOT silently rewrite the wrong fence.
  </behavior>
  <action>
    Create all 7 test files. All seeded RED (depend on Plan 19-02 Tasks 2-4 src files). Use vitest fake timers (`vi.useFakeTimers()` / `vi.advanceTimersByTime`) for debouncer/rate-limit/TTL tests. Use the shared `tests/widget/__fixtures__/cm6Helpers.ts` `makeFakeApp` (extend it inline if needed to add `app.vault.process` and `app.vault.read` mocks). Use `vi.spyOn(console, 'warn')` for postFlushDiagnostic. For flushTransitions register a fake plugin with `registerEvent: vi.fn(ref => ref)` and inspect calls. Use `crypto.subtle.digest` if available in happy-dom; fall back to a hand-rolled deterministic stub if `crypto.subtle` is missing (RESEARCH Environment Availability §). Tests for postFlushDiagnostic must work with the eventual hash function — keep the mock hash deterministic (e.g., synchronous `(s) => 'h:' + s.length + ':' + s.slice(0, 4)` in the test fixture). modifyEventOrdering.probe is descriptive — it reports the observed ordering via `console.log`/test name suffix; if the assumption breaks, Plan 19-02 Task 2 chooses microtask arming.
  </action>
  <verify>
    <automated>npm test -- --run --reporter=dot tests/widget/modifyEventOrdering.probe.test.ts tests/widget/debouncedWriter.test.ts tests/widget/selfWriteSuppression.test.ts tests/widget/flushRateLimit.test.ts tests/widget/flushTransitions.test.ts tests/widget/postFlushDiagnostic.test.ts tests/widget/fenceIndexRecompute.test.ts 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - All 7 test files exist
    - Each declares ≥1 describe and ≥3 concrete assertions
    - vitest discovers all 7 — failures reference missing imports from `../../src/widget/selfWriteSuppression` etc.
    - The probe test runs without throwing (its job is to characterize ordering; assertions about ordering may be soft, e.g., `expect(observed).toMatch(/(consumed|microtask-required)/)`)
    - Fake timers used in debouncedWriter, selfWriteSuppression, flushRateLimit
  </acceptance_criteria>
  <done>7 RED test files committed; vitest discovers them; modifyEventOrdering probe ready to run as the gate before Task 2 ships the suppression map.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SelfWriteSuppression + DebouncedWriter (the canonical write path)</name>
  <files>src/widget/selfWriteSuppression.ts, src/widget/debouncedWriter.ts</files>
  <read_first>
    - src/main/childEditorRegistry.ts (lines 19-23, 60-69 Map shape)
    - src/main/childEditorSync.ts (lines 691-693 host typing; lines 752-808 vault.on('modify') registration shape)
    - src/graph/copyToCode.ts (lines 66-99 canonical vault.process callsite — VERBATIM CONTRACT)
    - .planning/phases/19-widget-foundation-one-way-sync/19-RESEARCH.md (lines 213-238 Pattern 2 SelfWriteSuppression sketch; Specific Findings §1 lines 612-636 hash arming order; Pattern 5 lines 311-326 flush hooks; Pitfall 19-A lines 356-366; Pitfall 19-E lines 401-410)
    - .planning/phases/19-widget-foundation-one-way-sync/19-PATTERNS.md (lines 337-407 debouncedWriter section; lines 410-441 selfWriteSuppression)
    - .planning/phases/19-widget-foundation-one-way-sync/19-CONTEXT.md (C-04, C-06, C-08, D-09)
    - tests/widget/selfWriteSuppression.test.ts + tests/widget/debouncedWriter.test.ts + tests/widget/flushRateLimit.test.ts + tests/widget/postFlushDiagnostic.test.ts + tests/widget/fenceIndexRecompute.test.ts (RED tests from Task 1)
  </read_first>
  <action>
    1. `src/widget/selfWriteSuppression.ts`: per PATTERNS lines 410-441 + RESEARCH Pattern 2.
       - `class SelfWriteSuppression` with `private map = new Map<string, { expectedHash: string; expiresAt: number }>()` and `private TTL_MS = 2000`.
       - `arm(path: string, expectedHash: string): void` — sets `{ expectedHash, expiresAt: Date.now() + TTL_MS }`.
       - `tryConsume(path: string, observedHash: string): 'consumed' | 'stale' | 'miss'` — if no entry → 'miss'; if `Date.now() > expiresAt` → delete + 'stale'; if hashes match → delete + 'consumed'; if hashes differ within TTL → delete defensively + 'miss' (RESEARCH Pattern 2 lines 232-237).
       - `clear(): void` — empties the map (called by Plugin.onunload).
       - `clearForPath(path: string): void` — for vault.on('rename') re-keying and recovery scenarios.
    2. `src/widget/debouncedWriter.ts`: per PATTERNS lines 372-405 + RESEARCH Specific Findings §1.
       - Class `DebouncedWriter` with `private deb: Debouncer<[], void>; private lastFlushAt = 0; private rateLimitMs = 200; private app: App; private file: TFile; private getDoc: () => string; private getFenceIndex: () => number; private suppression: SelfWriteSuppression; private delayMs: number`.
       - Constructor: `this.deb = debounce(() => this.flush(), delayMs, /*resetTimer=*/true)` (Obsidian built-in `debounce`; CONTEXT C-06 — DO NOT add lodash).
       - `run(): void` → `this.deb.run()`; `cancel(): void` → `this.deb.cancel()`; `forceFlush(): Promise<void>` → `this.deb.cancel(); return this.flush()`.
       - `setDelay(ms): void` → `this.delayMs = ms; this.deb = debounce(() => this.flush(), ms, true)` (rebuild the debouncer; pending timers cancelled).
       - `private async flush(): Promise<void>`:
         (a) `const now = Date.now(); if (now - this.lastFlushAt < this.rateLimitMs) { setTimeout(() => this.flush(), this.rateLimitMs - (now - this.lastFlushAt)); return; }; this.lastFlushAt = now;` (rate limit per CONTEXT C-08 / SYNC-07).
         (b) Compute future content per RESEARCH §1 lines 612-636: `const newBody = this.getDoc(); const fenceIndex = this.getFenceIndex(); const currentDisk = await this.app.vault.read(this.file);`.
         (c) Multi-fence drift detection per RESEARCH Pitfall 19-E line 410: recompute `actualFenceIndex` by counting `^\s*```leetcode-solve\b` openers in currentDisk; if `actualFenceIndex` count exceeds expected fenceIndex+1 ahead OR if the fence at fenceIndex is no longer present, abort with `new Notice('Fence position changed; reload to continue editing.', 5000)` and return WITHOUT writing.
         (d) `const futureFullText = rewriteFenceBody(currentDisk, fenceIndex, newBody); const futureFenceBody = extractFenceBody(futureFullText, fenceIndex) ?? newBody; const expectedHash = await sha1(futureFenceBody);`.
         (e) `this.suppression.arm(this.file.path, expectedHash);` BEFORE vault.process (CONTEXT C-04).
         (f) `let postWriteText = ''; await this.app.vault.process(this.file, (body) => { postWriteText = rewriteFenceBody(body, fenceIndex, newBody); return postWriteText; });`. Idempotent — if `body !== currentDisk` due to race, the suppression entry won't consume and external-edit is preserved (RESEARCH §1 fail-safe).
         (g) Post-flush hash diagnostic per CONTEXT D-09: `const observed = extractFenceBody(postWriteText, fenceIndex) ?? ''; if (await sha1(observed) !== await sha1(newBody)) { console.warn('LC widget: post-flush hash drift for ' + this.file.path); }`. ALWAYS-ON in Phase 19 (CONTEXT specifics).
       - Hash helper: implement `async function sha1(s: string): Promise<string>` via `crypto.subtle.digest('SHA-1', new TextEncoder().encode(s))` then hex-encode. Co-locate in this file or a small `src/widget/hash.ts`. RESEARCH A7 — SHA-1 acceptable for echo detection.
    3. DO NOT modify CLAUDE.md. DO NOT modify v1.2 files in CONTEXT C-17. The probe test from Task 1 dictates whether arm() needs `Promise.resolve().then(...)` micro-task wrapping — if probe shows synchronous-after-process arming works, keep the simple `arm-then-process` order; if probe shows microtask wrapping needed, wrap accordingly (default: simple order; document the empirical result in the SUMMARY).
  </action>
  <verify>
    <automated>npm test -- --run tests/widget/selfWriteSuppression.test.ts tests/widget/debouncedWriter.test.ts tests/widget/flushRateLimit.test.ts tests/widget/postFlushDiagnostic.test.ts tests/widget/modifyEventOrdering.probe.test.ts tests/widget/fenceIndexRecompute.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `src/widget/selfWriteSuppression.ts` and `src/widget/debouncedWriter.ts` exist and export the contracted shapes
    - `npm test -- --run tests/widget/selfWriteSuppression.test.ts` exits 0 — multi-file arm/consume green; TTL expiry green; hash-mismatch defensive-delete green
    - `npm test -- --run tests/widget/debouncedWriter.test.ts` exits 0 — debounce 400ms timer, resetTimer=true, cancel, forceFlush, setDelay all green
    - `npm test -- --run tests/widget/flushRateLimit.test.ts` exits 0 — coalesce + 200ms gap green
    - `npm test -- --run tests/widget/postFlushDiagnostic.test.ts` exits 0 — console.warn fires on hash drift, silent on match
    - `npm test -- --run tests/widget/fenceIndexRecompute.test.ts` exits 0 — multi-fence drift aborts with Notice
    - `grep -c "lodash" src/widget/debouncedWriter.ts` returns 0 (CONTEXT C-06)
    - `grep -c "vault\\.process" src/widget/debouncedWriter.ts` returns ≥ 1
    - `grep -c "vault\\.modify" src/widget/` returns 0 (CI grep `scripts/grep-no-vault-modify.sh`)
    - `grep -c "suppression\\.arm" src/widget/debouncedWriter.ts` returns ≥ 1 (arm before vault.process)
    - `grep -c "post-flush hash drift" src/widget/debouncedWriter.ts` returns 1 (D-09)
  </acceptance_criteria>
  <done>SelfWriteSuppression + DebouncedWriter green; rate-limit + arm-before-process + post-flush diagnostic + multi-fence drift detection all verified by Task 1 RED tests turning GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire WidgetController + WidgetRegistry to writer; add updateListener and flushNow</name>
  <files>src/widget/WidgetController.ts, src/widget/widgetRegistry.ts</files>
  <read_first>
    - src/widget/WidgetController.ts (Plan 19-01 baseline)
    - src/widget/widgetRegistry.ts (Plan 19-01 baseline)
    - src/widget/debouncedWriter.ts (Plan 19-02 Task 2)
    - .planning/phases/19-widget-foundation-one-way-sync/19-PATTERNS.md (lines 196-333 WidgetController; lines 442-496 WidgetRegistry)
    - .planning/phases/19-widget-foundation-one-way-sync/19-RESEARCH.md (Pattern 5 lines 311-326 transitions; SUMMARY primitive 4)
    - .planning/phases/19-widget-foundation-one-way-sync/19-CONTEXT.md (C-07)
    - tests/widget/WidgetController.test.ts (extend with flushNow assertions); tests/widget/flushTransitions.test.ts (RED from Task 1)
  </read_first>
  <action>
    1. `src/widget/WidgetController.ts` — extend the controller and mountLeetCodeWidget:
       - In `mountLeetCodeWidget`, after creating the EditorView and BEFORE returning the controller, instantiate `const writer = new DebouncedWriter(plugin.app, file, () => view.state.doc.toString(), () => computeFenceIndex(currentDiskOrCachedSource, fenceLineStart), plugin.selfWriteSuppression, plugin.settings.getWidgetSyncDebounceMs())`. The `getFenceIndex` callback computes from the current source captured at mount; flush time recomputes against fresh disk content per Task 2 step (c).
       - Add `EditorView.updateListener.of(update => { if (update.docChanged && !readOnly) writer.run(); })` to the extensions array. Pass it through the existing extension list before mounting. (Read-only Reading-mode widgets do NOT register the listener.)
       - On `WidgetController`: replace the stub `flushNow(): void` with `flushNow(): Promise<void> { return this.writer.forceFlush(); }`.
       - Replace the stub `destroy(): void` to first await `this.writer.forceFlush()` (synchronous via `void` chain) — but since `destroy` is sync-shaped from CM6's perspective, fire `this.writer.cancel()` and synchronously `vault.process` once via a sentinel: in practice, `flushNow` is called by the registry layer BEFORE destroy; controller.destroy() just calls `this.writer.cancel(); this.view.destroy()`.
       - In `LeetCodeWidgetRenderChild.onunload()`, call `await this.controller?.flushNow()` (note MarkdownRenderChild.onunload can be async) THEN `this.controller?.destroy()` — flush before destroy so chars-on-quit are persisted (RESEARCH Pitfall 19-B + SYNC-02).
       - Update `WidgetController.fenceIndex` to be a mutable property; flush-time recompute happens inside `DebouncedWriter.flush()` (Task 2 step c). The controller's stored `fenceIndex` is used only for registry key — if drift detected, the Notice in DebouncedWriter aborts the write and the controller stays at its current key.
    2. `src/widget/widgetRegistry.ts` — replace stub `flushAll()`:
       - `async flushAll(): Promise<void> { for (const ctl of this.map.values()) await ctl.flushNow(); }` — sequential to avoid race; ~ms total cost.
       - `flushAllSync(): void { for (const ctl of this.map.values()) { ctl.writer.cancel(); /* fire-and-forget; beforeunload best-effort */ ctl.flushNow().catch(() => {}); } }` — explicitly synchronous-issue, used only by `beforeunload` (RESEARCH Pitfall 19-B).
       - `flushFile(filePath: string): Promise<void>` — flush every controller whose key starts with `${filePath}::`. Used by `vault.on('rename', file => flushFile(oldPath))`.
       - `destroyAll()`: existing — keep, but call before-flush is the responsibility of the caller (Plugin.onunload).
    3. DO NOT touch v1.2 files in CONTEXT C-17. DO NOT modify CLAUDE.md.
  </action>
  <verify>
    <automated>npm test -- --run tests/widget/WidgetController.test.ts tests/widget/flushTransitions.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run tests/widget/WidgetController.test.ts` exits 0 — controller now has working flushNow that proxies to writer.forceFlush
    - `npm test -- --run tests/widget/flushTransitions.test.ts` exits 0 — flushAll/flushAllSync/flushFile all green
    - `grep -c "EditorView\\.updateListener" src/widget/WidgetController.ts` returns ≥ 1
    - `grep -c "writer\\.forceFlush\\|writer\\.run\\|writer\\.cancel" src/widget/WidgetController.ts` returns ≥ 3
    - `grep -c "flushAllSync" src/widget/widgetRegistry.ts` returns ≥ 1
    - Read-only widgets do NOT trigger writer.run() — assert via test that controller created with readOnly=true does not register the updateListener (or the listener early-returns on readOnly)
  </acceptance_criteria>
  <done>WidgetController binds DebouncedWriter via updateListener; flushNow real implementation in place; registry flush methods wired; flushTransitions tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: src/main.ts — six flush hooks + vault.on('modify') consumer + selfWriteSuppression instantiation</name>
  <files>src/main.ts</files>
  <read_first>
    - src/main.ts (lines 819-839 useNestedEditor + Plan 19-01 useInlineWidget block; lines 1027-1051 onunload; lines 864/900/938/966 registerEvent patterns; line 966 fence-repair hook for reference shape only)
    - src/main/childEditorSync.ts (lines 752-808 vault.on('modify') registration with TFile guard)
    - .planning/phases/19-widget-foundation-one-way-sync/19-RESEARCH.md (Pattern 5 lines 311-326; Pitfall 19-A lines 356-366; Pitfall 19-B lines 368-377; Specific Findings §4 lines 661-668 useInlineWidget gated body)
    - .planning/phases/19-widget-foundation-one-way-sync/19-PATTERNS.md (lines 749-829 onload/onunload insertion-point pattern)
    - .planning/phases/19-widget-foundation-one-way-sync/19-CONTEXT.md (C-07, C-08; D-08)
    - src/widget/selfWriteSuppression.ts + src/widget/debouncedWriter.ts + src/widget/widgetRegistry.ts
  </read_first>
  <action>
    Inside the `if (useInlineWidget) { ... }` block in Plugin.onload() (added in Plan 19-01 Task 4), add:
    1. `this.selfWriteSuppression = new SelfWriteSuppression();` after `this.widgetRegistry = new WidgetRegistry();`.
    2. Six flush-on-transition hooks per CONTEXT C-07 / RESEARCH Pattern 5:
       - `this.registerEvent(this.app.workspace.on('active-leaf-change', () => this.widgetRegistry?.flushAll()));`
       - `this.registerEvent(this.app.workspace.on('quit', (tasks: { add: (p: Promise<any>) => void }) => { tasks.add(this.widgetRegistry?.flushAll() ?? Promise.resolve()); }));` — primary shutdown path per RESEARCH Pitfall 19-B (verified obsidian.d.ts:7195). At runtime, `console.log(tasks)` once on first invocation to confirm shape (RESEARCH Open Question A8); if shape differs, fall back to no-op + beforeunload only.
       - `this.registerEvent(this.app.vault.on('rename', (file, oldPath) => { if (typeof oldPath === 'string') this.widgetRegistry?.flushFile(oldPath); }));`
       - `this.registerDomEvent(window, 'beforeunload', () => this.widgetRegistry?.flushAllSync());` — best-effort belt-and-suspenders.
       - Reading-mode `MarkdownRenderChild.onunload` is owned by the child class (Plan 19-01 Task 3) — calls controller.flushNow().
       - Plugin.onunload (existing — extend below) calls `widgetRegistry.flushAll()` THEN destroyAll.
    3. `vault.on('modify')` consumer per RESEARCH Specific Findings §4:
       ```
       this.registerEvent(this.app.vault.on('modify', async (file) => {
         if (!this.settings.getUseInlineWidget()) return;     // gated body — Specific Findings §4
         if (!(file instanceof TFile)) return;
         if (!this.selfWriteSuppression) return;
         try {
           const disk = await this.app.vault.read(file);
           // Per-fence loop: for each leetcode-solve fence in this file, compute fence body hash and tryConsume
           // Plan 19-02: just consume by file path with the fence body of fenceIndex 0 (single-fence default)
           const body = extractFenceBody(disk, 0) ?? '';
           const observedHash = await sha1(body);
           const result = this.selfWriteSuppression.tryConsume(file.path, observedHash);
           if (result === 'consumed') return; // self-write — no-op
           if (result === 'stale' || result === 'miss') {
             console.log('[LC widget] external modify observed for', file.path, '— Plan 20 reload TBD');
             // Plan 20 owns the cursor-preserving reload + conflict modal (SYNC-04, SYNC-05).
           }
         } catch (e) { /* swallow; modify is best-effort observability */ }
       }));
       ```
       Import `extractFenceBody` from `src/widget/fenceSerialization` and `sha1` from `src/widget/debouncedWriter` (or extract sha1 to `src/widget/hash.ts` for clean import).
    4. Plugin.onunload() AFTER existing line 1050 (PATTERNS lines 822-826) — extend Plan 19-01's `this.widgetRegistry?.destroyAll();` to:
       ```
       await this.widgetRegistry?.flushAll();
       this.widgetRegistry?.destroyAll();
       this.selfWriteSuppression?.clear();
       ```
       (onunload is allowed to be async per Obsidian — verify against existing onunload signature; if not, fire `flushAllSync` instead for the synchronous path.)
    5. Add class fields `selfWriteSuppression?: SelfWriteSuppression;` on the LeetCodePlugin class (next to widgetRegistry from Plan 19-01).
    6. SettingsTab onChange for `widgetSyncDebounceMs` (Plan 19-01 Task 4) — extend to call `this.plugin.widgetRegistry?.applyDelay(val)` where `applyDelay(ms)` iterates controllers and calls `ctl.writer.setDelay(ms)`. Add this method to `WidgetRegistry` (Plan 19-02 Task 3 expansion). Live application without note reload.
    7. DO NOT modify CLAUDE.md. DO NOT touch v1.2 files in CONTEXT C-17. DO NOT delete or modify the `'leetcode.*'` userEvent paragraph or Phase 17 D-05 paragraph in CLAUDE.md (RESEARCH §7 — those stay through Phase 21).
  </action>
  <verify>
    <automated>npm test -- --run tests/widget/ tests/main/mutualExclusion.test.ts && npm run build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - All previously-green widget tests still pass
    - `npm run build` exits 0
    - `grep -c "workspace\\.on('quit'" src/main.ts` returns ≥ 1 (RESEARCH Pitfall 19-B)
    - `grep -c "vault\\.on('rename'" src/main.ts` returns ≥ 1
    - `grep -c "registerDomEvent.*beforeunload" src/main.ts` returns ≥ 1
    - `grep -c "vault\\.on('modify'" src/main.ts` increases by ≥ 1 vs Plan 19-01 baseline (the new useInlineWidget-gated listener)
    - `grep -c "tryConsume" src/main.ts` returns ≥ 1
    - `grep -c "selfWriteSuppression" src/main.ts` returns ≥ 3 (instantiation + onunload clear + modify consumer)
    - `git diff CLAUDE.md` returns no changes (preserve list)
    - `git diff --name-only src/main/` returns no v1.2 files modified
  </acceptance_criteria>
  <done>Six flush hooks wired; modify-listener consumes self-writes; build green; all widget tests still green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: UAT — Plan 19-02 dev-vault sync smoke (force-quit + iCloud + rate-limit)</name>
  <what-built>
    A debounced one-way write path. Typing in the widget produces atomic vault.process writes ~400ms after the last keystroke (configurable). Self-writes do NOT cause widget reload. Force-quit within ms of typing preserves the most-recent characters. Rate-limited to 1/200ms per file. Six flush hooks fire on transitions. Post-flush hash drift produces a console.warn diagnostic.
  </what-built>
  <files>(manual UAT — no source files modified)</files>
  <action>Manual user-acceptance test. The developer executes the steps in <how-to-verify> against a real Obsidian dev vault. Force-quit, suppression, rate-limit, and post-flush diagnostic are the critical gates. A fail blocks Plan 19-03.</action>
    <how-to-verify>
    1. `npm run build` exits 0; deploy to dev vault.
    2. Reload Obsidian. With useInlineWidget=ON and an LC note open in Live Preview:
       - Type 'X' inside the widget. Watch via DevTools → Network/Performance or `tail -f` on the note file's mtime. Within ~400ms, mtime updates and the disk content shows 'X' at the correct position. Open the note in a separate text editor (BBEdit/VS Code) — the file content matches the widget's doc byte-for-byte (`---` lines, triple backticks, trailing whitespace all preserved).
       - Type 5 chars in 100ms intervals. Within 400ms of the LAST char, exactly ONE flush fires (verify by mtime increment count and DevTools console — `vault.process` should be called once).
       - DevTools console: NO 'post-flush hash drift' warnings during normal typing. If you see drift, that's a real bug — flag immediately.
    3. **Force-quit test (SYNC-02 critical)**: type 'TESTSENTINEL_FORCEQUIT' inside widget. Within 100ms, press Cmd-Q (or right-click Obsidian dock icon → Quit). Reopen Obsidian; reopen the note. Confirm 'TESTSENTINEL_FORCEQUIT' is on disk in the fence body. (workspace.on('quit') Tasks.add path is doing the work; beforeunload is best-effort fallback.)
    4. **Self-write echo suppression (SYNC-03 critical)**: type 'A' inside widget. Watch DevTools console. After the flush, you should see NO log message about 'external modify observed' for this file (the widget consumed its own write via the suppression map).
    5. **External edit detection**: with the widget open, edit the note's `## Problem` section in another editor (vim/VS Code) — anything outside the fence. Save. The widget's `vault.on('modify')` listener fires; DevTools console shows '[LC widget] external modify observed for ... — Plan 20 reload TBD'. The widget itself does NOT reload (Plan 20 owns reload).
    6. **Multi-pane (informational; full handling is Plan 20/v1.4 deferred)**: open the same LC note in two panes. Type in pane A. Pane B's widget DOES NOT auto-reload (Plan 19 single-pane scope per CONTEXT — multi-pane is v1.4 deferred per REQUIREMENTS Out of Scope).
    7. **Rate-limit (SYNC-07)**: type continuously for 5 seconds. mtime should increment at most ~25 times (5s / 200ms = 25), not at 400ms intervals (which would be 12). Use Activity Monitor or `stat -f %m` checks at 100ms intervals to verify no two flushes within 200ms.
    8. **Save delay configurability (SYNC-01)**: in Settings → Experimental, change Save delay to 1000ms. Type a single char. Wait 999ms. mtime unchanged. At ~1000ms, mtime ticks. Change back to 400ms.
    9. **Multi-fence drift (Pitfall 19-E)**: edit the note in another editor to ADD a second `leetcode-solve` fence ABOVE the first one. Save. In Obsidian (with widget still open from before), type one char. Confirm a Notice appears: 'Fence position changed; reload to continue editing.' AND the widget did NOT silently rewrite the wrong fence. Reload note.
    10. **Property test corpus + post-flush diagnostic**: artificially introduce drift by hand-editing the note's `\n` line endings via `tr` to CRLF, save, type one char. Confirm post-flush diagnostic in DevTools console (or no diagnostic if rewriteFenceBody preserves CRLF correctly per Plan 19-01 Task 2).
    11. **iCloud / Obsidian Sync interaction (informational, optional)**: if dev vault is in iCloud, type for 60s continuously. No sync conflicts; no 'failed to sync' notifications.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues. Critical failures (block Plan 19-03): force-quit loses chars; widget reloads on every flush (suppression broken); post-flush diagnostic warns during normal typing (round-trip broken); multi-fence drift silently writes wrong fence; rate-limit not honored.</resume-signal>
  <verify>(developer confirms each step; user replies "approved" or with issues)</verify>
  <done>Approved by user; Plan 19-02 acceptance gates met; ready for Plan 19-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| widget → disk via vault.process | Plugin write authority |
| disk → vault.on('modify') → widget | External writes (Sync, iCloud, CLI, other panes) |
| selfWriteSuppression Map | Plugin-singleton; no cross-process exposure |
| beforeunload / quit | Renderer process shutdown — best-effort |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Denial of Service / Data Loss | DebouncedWriter.flush — debounced 400ms | mitigate | Six flush-on-transition hooks (SYNC-02; CONTEXT C-07): MarkdownRenderChild.onunload, Plugin.onunload, workspace.on('active-leaf-change'), vault.on('rename'), workspace.on('quit') Tasks.add, beforeunload synchronous best-effort. RESEARCH Pitfall 19-B: workspace.on('quit') is primary; beforeunload is fallback. UAT step 3 verifies force-quit preservation. |
| T-19-02 | Tampering | SelfWriteSuppression — boolean flag race | mitigate | Per-path content-hash map with 2s TTL (CONTEXT C-04, RESEARCH Pattern 2; PITFALLS P1). Hashes the post-write fence body computed via the SAME rewriteFenceBody function (RESEARCH §1) — race between vault.read and vault.process resolves to 'miss' (defensive delete) which is safe (treats race as external — never swallows external writes). Multi-file isolation tested in selfWriteSuppression.test.ts. |
| T-19-03 | Tampering / Data Corruption | rewriteFenceBody/extractFenceBody round-trip | mitigate | (a) Property tests over SHELLS × HOSTILE_BODIES corpus (Plan 19-01 Task 1; CONTEXT D-09 + RESEARCH §"Property Tests"); (b) runtime post-flush hash diagnostic (D-09 — console.warn on drift, ALWAYS-ON in Phase 19 per CONTEXT specifics); (c) UAT step 2 confirms no spurious diagnostics during normal typing. Fail-loud (throw) was rejected — could brick a session mid-typing per CONTEXT D-09 rationale. |
| T-19-07 | Resource Growth (Denial of Service) | SelfWriteSuppression Map unbounded | mitigate | 2s TTL bounds entry lifetime; tryConsume always deletes on success/stale/mismatch; clear() on Plugin.onunload drains the map; clearForPath() on vault.on('rename'). Memory cap is O(open files × 2s flush rate) — bounded ~tens of entries. |
| T-19-NEW-Phase19-A | Tampering | vault.on('modify') ordering vs. arm() | mitigate | Wave 0 empirical probe (Plan 19-02 Task 1 modifyEventOrdering.probe.test.ts; RESEARCH Pitfall 19-A) confirms ordering before suppression ships; if probe fails, switch arm() to Promise.resolve().then() microtask wrapping; if probe passes, simple arm-then-process order is safe. Defensive guard: even if arm misses the modify event, defensive delete on hash mismatch within TTL preserves external writes. |
| T-19-NEW-Phase19-E | Tampering / Confused Deputy | fenceIndex stale across mid-flight inserts above | mitigate | DebouncedWriter.flush() recomputes fenceIndex from fresh disk content (RESEARCH Pitfall 19-E line 410); on drift detection, abort write and surface Notice 'Fence position changed; reload to continue editing.'; never silently rewrite wrong fence. UAT step 9 verifies. |
| T-19-NEW-Phase19-Rate | Denial of Service via file-watcher storm (Sync/iCloud/Git) | Per-file flush rate | mitigate | Per-file rate limit max 1 flush per 200ms (SYNC-07; CONTEXT C-08; PITFALLS P12). Coalesce over-rate calls. UAT step 7 verifies via mtime sampling. |
</threat_model>

<verification>
**Phase-level gates for Plan 19-02:**
- `npm test -- --run tests/widget/` — all 19 widget tests green (incl. fenceSerialization.property.test.ts retained from Plan 19-01)
- `npm test -- --run tests/main/mutualExclusion.test.ts` — green
- `npm run build` exits 0
- `grep -c "vault\\.modify\\b" src/widget/` returns 0 (CI grep enforced)
- `grep -c "lodash" src/widget/` returns 0 (CONTEXT C-06)
- `git diff CLAUDE.md` returns no changes
- `git diff --name-only src/main/` returns no v1.2 files modified
- UAT (Task 5) — all 11 verification scenarios pass; force-quit + suppression + rate-limit are critical
</verification>

<success_criteria>
1. SYNC-01: debounced 400ms write via vault.process; configurable 300/500/1000/2000ms via Experimental settings; setDelay applies live without note reload
2. SYNC-02: six flush hooks fire (unmount, plugin-unload, leaf-change, rename, quit, beforeunload); force-quit preserves most-recent chars
3. SYNC-03: per-path content-hash suppression with 2s TTL; multi-file isolation; defensive delete on hash mismatch
4. SYNC-06 runtime portion: post-flush hash diagnostic in console.warn; no false positives during normal typing
5. SYNC-07: per-file rate limit 1/200ms; coalesce
6. RESEARCH Pitfall 19-A: modifyEventOrdering empirical probe runs; arm()-before-process order chosen based on probe result
7. RESEARCH Pitfall 19-E: fenceIndex recomputed at flush time; Notice on drift; never writes wrong fence
8. v1.2 path with useInlineWidget=OFF still regression-clean (UAT)
</success_criteria>

<output>
Create `.planning/phases/19-widget-foundation-one-way-sync/19-02-SUMMARY.md` when done. Document the empirical probe result (whether modify event fires synchronously or via microtask) so Plan 19-04 / Phase 20 can reference it.
</output>
