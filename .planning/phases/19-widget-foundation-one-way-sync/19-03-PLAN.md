---
phase: 19-widget-foundation-one-way-sync
plan: 03
type: execute
wave: 3
depends_on:
  - 19-02
files_modified:
  - src/widget/statePersistence.ts
  - src/widget/WidgetController.ts
  - src/main.ts
  - tests/widget/statePersistence.test.ts
  - tests/widget/historyRoundTrip.test.ts
  - tests/widget/livePreviewUnmount.test.ts
autonomous: false
requirements:
  - WIDGET-03
  - WIDGET-04
must_haves:
  truths:
    - "Closing an LC note and reopening within 30 seconds restores cursor position, scroll offset, and undo history at the leetcode-solve fence"
    - "Live Preview cursor approach that triggers an unmount/remount preserves widget state via the persistence map (D-02 belt) and the mousedown.stopPropagation listener (D-02 suspenders)"
    - "Widget mounts on fence tag (`leetcode-solve`) — embeds and stray fences render correctly without depending on lc-slug for mount detection (WIDGET-03)"
    - "Persistence-map entries expire after 30s TTL; sweep on plugin load / onunload"
    - "CM6 history round-trip via state.toJSON({history}) + EditorState.fromJSON(json, config, {history}) preserves the undo stack"
  artifacts:
    - path: src/widget/statePersistence.ts
      provides: "Plugin-level state map keyed by `${file.path}::${fenceIndex}` with 30s TTL; captureState/hydrateState"
      exports: ["StatePersistenceMap"]
      min_lines: 60
    - path: src/widget/WidgetController.ts
      provides: "captureState on unmount; hydrateState on mount; CM6 history field round-trip; mousedown.stopPropagation listener (already in Plan 19-01 — verify still present)"
      contains: "captureState"
    - path: src/main.ts
      provides: "StatePersistenceMap instantiation in onload; sweep in onunload"
      contains: "statePersistence"
  key_links:
    - from: src/widget/WidgetController.ts
      to: src/widget/statePersistence.ts
      via: plugin.statePersistence.captureState(key, view) on onunload; .hydrateState(key, view) on mount
      pattern: "statePersistence\\.(capture|hydrate)State"
    - from: src/main.ts
      to: src/widget/statePersistence.ts
      via: this.statePersistence = new StatePersistenceMap()
      pattern: "new StatePersistenceMap"
---

<objective>
Phase 19 Plan 03 — State Persistence + P3 Mitigation: ship widget state survival across unmount/remount within a 30-second TTL, covering the Live Preview cursor-approach raw-source-reveal scenario (PITFALLS P3) plus viewport scroll, mode-switch, and theme-change unmounts.

Purpose: WIDGET-04 (state persistence map keyed by `${file.path}::${fenceIndex}` with 30s TTL — CONTEXT C-09 / D-01), WIDGET-03 (mount-on-fence-tag semantics; persistence ensures embeds and stray fences ALSO survive remount), the belt-and-suspenders D-02 mitigation (RESEARCH Pitfall 19-C history round-trip + Pattern 4). Plan 19-01 already shipped the `mousedown.stopPropagation` listener; this Plan adds the persistence map.

Output: 1 new src file (statePersistence.ts), updates to WidgetController.ts (capture/hydrate hooks + history JSON round-trip), small additions to src/main.ts (instantiation + sweep), 3 new test files. Acceptance: type → undo three times → close note → reopen within 30s → undo three times succeeds and restores original state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-widget-foundation-one-way-sync/19-CONTEXT.md
@.planning/phases/19-widget-foundation-one-way-sync/19-RESEARCH.md
@.planning/phases/19-widget-foundation-one-way-sync/19-PATTERNS.md
@.planning/phases/19-widget-foundation-one-way-sync/19-VALIDATION.md
@.planning/phases/19-widget-foundation-one-way-sync/19-02-SUMMARY.md
@CLAUDE.md
@src/widget/WidgetController.ts
@src/widget/widgetRegistry.ts
@src/widget/fenceLocator.ts
@src/main.ts
@src/main/childEditorRegistry.ts

<interfaces>
<!-- New helper in Plan 19-03 -->

```typescript
// src/widget/statePersistence.ts
export interface ChildEditorState {
  cursor: number;        // selection.main.head
  scrollTop: number;     // scrollDOM.scrollTop
  historyJSON: unknown;  // state.toJSON({history: historyField}).history
}

export class StatePersistenceMap {
  // Key: `${file.path}::${fenceIndex}`
  // TTL: 30_000ms
  captureState(key: string, view: EditorView): void;
  hydrateState(key: string, view: EditorView): boolean;  // returns true if entry hydrated
  sweepExpired(): void;       // run on plugin load and on Plugin.onunload
  clear(): void;              // empty map (onunload)
  clearForPath(path: string): void;
}
```

CM6 history serialization (RESEARCH Pitfall 19-C lines 380-388):
```typescript
import { historyField } from '@codemirror/commands';

// Capture
const json = view.state.toJSON({ history: historyField });
// json.history is the serialized history payload

// Hydrate (must build state via fromJSON, NOT via setValue + dispatch)
const newState = EditorState.fromJSON(
  json,
  { extensions: [...allExtensions], doc: docString },
  { history: historyField },
);
view.setState(newState);
```

From src/widget/fenceLocator.ts (Plan 19-01):
```typescript
export function computeFenceIndex(fileText: string, fenceLineStart0Based: number): number;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — RED tests for state persistence + history round-trip + Live Preview unmount</name>
  <files>tests/widget/statePersistence.test.ts, tests/widget/historyRoundTrip.test.ts, tests/widget/livePreviewUnmount.test.ts</files>
  <read_first>
    - .planning/phases/19-widget-foundation-one-way-sync/19-VALIDATION.md (rows 19-03-01..03)
    - .planning/phases/19-widget-foundation-one-way-sync/19-PATTERNS.md (lines 50-56 statePersistence/historyRoundTrip/livePreviewUnmount analog table; lines 1132-1140 no-analog rationale)
    - .planning/phases/19-widget-foundation-one-way-sync/19-RESEARCH.md (Pattern 4 lines 274-308; Pitfall 19-C lines 380-388; Pitfall 19-F lines 412-422; Open Question 3 lines 757-761)
    - .planning/phases/19-widget-foundation-one-way-sync/19-CONTEXT.md (C-09, D-01, D-02; specifics line 169)
    - tests/main/childEditorRegistry.test.ts (TTL/destruction patterns)
    - tests/widget/__fixtures__/cm6Helpers.ts (Plan 19-01)
    - tests/widget/__fixtures__/lcNoteFixtures.ts (Plan 19-01)
  </read_first>
  <behavior>
    - statePersistence.test.ts: vi.useFakeTimers(). new StatePersistenceMap. captureState('a.md::0', mockView) writes entry. hydrateState('a.md::0', otherMockView) returns true and dispatches selection + scrollTop. Entry deleted on hydrate. Hydrate after 30001ms — returns false; entry expired and deleted. sweepExpired removes stale entries. Multi-key isolation: captureState('a.md::0') doesn't affect 'a.md::1'. Cursor clamping: stored cursor=100, hydrating into doc of length 50 → cursor clamped to 50.
    - historyRoundTrip.test.ts: build a real CM6 EditorState with history extension. Type three changes (insert 'A', 'B', 'C'). Capture state via state.toJSON({history: historyField}). Build a fresh EditorState via EditorState.fromJSON(json, {extensions: [history(), ...]}, {history: historyField}). Mount into a new view via view.setState(newState). Dispatch undo three times. Assert doc reflects empty after three undos (matching the original empty buffer). DOES NOT use setValue + dispatch (that path loses history per RESEARCH Pitfall 19-C).
    - livePreviewUnmount.test.ts: simulate the unmount/remount cycle. Mount widget controller A. captureState fires on unmount with synthesized cursor/scroll/history. Wait <30s, mount widget controller B at the same key — hydrateState returns true; controller B's view selection.head matches controller A's pre-unmount cursor. Wait 30001ms, mount controller C — hydrateState returns false; fresh state. Test the mousedown.stopPropagation listener is present on widget root (Plan 19-01 already attached it; verify it still exists by querying the widget container's event listeners count or via the patched-mock approach in cm6Helpers).
  </behavior>
  <action>
    Create the 3 test files RED. Use real CM6 imports from `@codemirror/state`, `@codemirror/commands`, `@codemirror/view` for the historyRoundTrip test (no mocking — that test needs real CM6 history serialization). Use mocked EditorView (via cm6Helpers makeFakeEditorView) for the statePersistence and livePreviewUnmount tests where DOM behavior is needed but full CM6 mount overhead isn't. Use vi.useFakeTimers() for TTL tests; advance via vi.advanceTimersByTime. The livePreviewUnmount test mounts a real lightweight controller through the existing `mountLeetCodeWidget` factory — but with happy-dom limitations, query controller.view.state.selection.main.head rather than visual cursor.
  </action>
  <verify>
    <automated>npm test -- --run --reporter=dot tests/widget/statePersistence.test.ts tests/widget/historyRoundTrip.test.ts tests/widget/livePreviewUnmount.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - All 3 test files exist with ≥3 concrete assertions each
    - vitest discovers all; failures reference missing `src/widget/statePersistence` and missing capture/hydrate methods on WidgetController
    - historyRoundTrip uses `EditorState.fromJSON` (not setValue) — `grep -c "fromJSON" tests/widget/historyRoundTrip.test.ts` returns ≥ 1
  </acceptance_criteria>
  <done>3 RED test files committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StatePersistenceMap (capture/hydrate with CM6 history JSON round-trip)</name>
  <files>src/widget/statePersistence.ts</files>
  <read_first>
    - .planning/phases/19-widget-foundation-one-way-sync/19-RESEARCH.md (Pattern 4 lines 274-308 — full sketch; Pitfall 19-C lines 380-388 — fromJSON requirement)
    - .planning/phases/19-widget-foundation-one-way-sync/19-CONTEXT.md (C-09, D-01)
    - src/main/childEditorRegistry.ts (Map TTL pattern)
    - tests/widget/statePersistence.test.ts + tests/widget/historyRoundTrip.test.ts (RED)
  </read_first>
  <action>
    Implement `src/widget/statePersistence.ts`:
    1. `interface ChildEditorState { cursor: number; scrollTop: number; historyJSON: unknown }`
    2. `class StatePersistenceMap` with `private map = new Map<string, { state: ChildEditorState; expiresAt: number }>()` and `private TTL_MS = 30_000`.
    3. `captureState(key: string, view: EditorView): void` — capture per RESEARCH Pattern 4 lines 285-292:
       ```
       const cursor = view.state.selection.main.head;
       const scrollTop = view.scrollDOM.scrollTop;
       const json = view.state.toJSON({ history: historyField });
       const historyJSON = (json as Record<string, unknown>)?.history ?? null;
       this.map.set(key, { state: { cursor, scrollTop, historyJSON }, expiresAt: Date.now() + this.TTL_MS });
       ```
    4. `hydrateState(key: string, view: EditorView): boolean` — if entry missing or expired → delete + return false. Otherwise:
       (a) Clamp `head = Math.min(entry.state.cursor, view.state.doc.length)`.
       (b) Dispatch selection + scroll: `view.dispatch({ selection: EditorSelection.cursor(head) }); view.scrollDOM.scrollTop = entry.state.scrollTop;`.
       (c) **History rehydration is the tricky part (Pitfall 19-C)**: Phase 19's pragmatic approach — capture historyJSON; on hydrate, reuse the existing extensions and dispatch a `historyField`-targeted state-update annotated to rebuild the history. **If `EditorState.fromJSON` round-trip works empirically (Plan 19-03 Task 1's historyRoundTrip.test.ts), prefer that path**: rebuild the state via `EditorState.fromJSON(captured.json, {extensions: existingExtensions, doc: view.state.doc.toString()}, {history: historyField})` then `view.setState(newState)`. **If the empirical test fails, ship cursor + scroll only WITHOUT history** (RESEARCH Open Question 3 fallback) and document the limitation in WidgetController.ts comment + 19-03 SUMMARY.
       (d) Delete the entry after hydration.
       (e) Return true.
    5. `sweepExpired(): void` — iterate map, delete every entry with `Date.now() > expiresAt`. Called on Plugin.onunload AND optionally from a `setInterval` registered via `plugin.registerInterval(setInterval(sweep, 60_000))` (60s sweep is fine — TTL is 30s).
    6. `clear(): void` — `this.map.clear()`.
    7. `clearForPath(path: string): void` — iterate keys; delete every key starting with `${path}::`.
    8. Use `EditorSelection` from `@codemirror/state` and `historyField` from `@codemirror/commands`.
  </action>
  <verify>
    <automated>npm test -- --run tests/widget/statePersistence.test.ts tests/widget/historyRoundTrip.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/widget/statePersistence.ts` exists with `StatePersistenceMap` class export and `ChildEditorState` interface
    - `npm test -- --run tests/widget/statePersistence.test.ts` exits 0
    - `npm test -- --run tests/widget/historyRoundTrip.test.ts` exits 0 OR (if empirical test reveals fromJSON limitation) the test is updated to assert cursor+scroll only AND `WidgetController.ts` carries an explicit comment + 19-03 SUMMARY documents the residual limitation per RESEARCH Open Question 3
    - `grep -c "expiresAt" src/widget/statePersistence.ts` returns ≥ 2
    - `grep -c "historyField" src/widget/statePersistence.ts` returns ≥ 1
    - `grep -c "Math\\.min" src/widget/statePersistence.ts` returns ≥ 1 (cursor clamping per RESEARCH Pattern 4 line 301)
  </acceptance_criteria>
  <done>StatePersistenceMap green; capture/hydrate works; CM6 history round-trip works (or documented residual limitation noted).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WidgetController capture-on-unmount / hydrate-on-mount + Plugin onload integration</name>
  <files>src/widget/WidgetController.ts, src/main.ts</files>
  <read_first>
    - src/widget/WidgetController.ts (Plan 19-02 baseline)
    - src/widget/statePersistence.ts (Plan 19-03 Task 2)
    - src/main.ts (Plan 19-02 useInlineWidget block)
    - .planning/phases/19-widget-foundation-one-way-sync/19-PATTERNS.md (lines 287-333 destroy lifecycle)
    - .planning/phases/19-widget-foundation-one-way-sync/19-RESEARCH.md (Pattern 4 + Pitfall 19-C)
    - tests/widget/livePreviewUnmount.test.ts (RED)
  </read_first>
  <action>
    1. `src/widget/WidgetController.ts`:
       - Add private fields `private persistenceKey: string` (computed at mount as `${file.path}::${fenceIndex}`).
       - In `mountLeetCodeWidget`, AFTER constructing the EditorView and BEFORE attaching the updateListener (Plan 19-02), call `plugin.statePersistence?.hydrateState(persistenceKey, view)` — if it returns true, the cursor and scroll are restored.
       - In `LeetCodeWidgetRenderChild.onunload()`, BEFORE `controller.flushNow()` and `controller.destroy()`, call `plugin.statePersistence?.captureState(controller.persistenceKey, controller.view)` so state is captured before destruction.
       - Same in `LeetCodeFenceWidget.destroy(dom)` for Live Preview unmount path: capture BEFORE flushing.
       - Confirm Plan 19-01's mousedown.stopPropagation listener is still attached (do NOT remove it; `grep -c "stopPropagation" src/widget/WidgetController.ts` should remain 1). Per CONTEXT D-02 belt-and-suspenders, both mitigations ship together.
       - Sequence on unmount: capture → flushNow → destroy. Sequence on mount: create view → hydrate → attach updateListener.
    2. `src/main.ts`:
       - In the `if (useInlineWidget) { ... }` block, after `this.selfWriteSuppression = new SelfWriteSuppression()`, add `this.statePersistence = new StatePersistenceMap();`.
       - Optionally register a sweep interval: `this.registerInterval(window.setInterval(() => this.statePersistence?.sweepExpired(), 60_000));` (60s sweep beats 30s TTL).
       - In Plugin.onunload (after Plan 19-02's flushAll/destroyAll), add `this.statePersistence?.clear();`.
       - Add class field `statePersistence?: StatePersistenceMap;`.
    3. DO NOT modify CLAUDE.md. DO NOT modify v1.2 files in CONTEXT C-17.
  </action>
  <verify>
    <automated>npm test -- --run tests/widget/ tests/main/mutualExclusion.test.ts && npm run build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run tests/widget/livePreviewUnmount.test.ts` exits 0
    - All previously-green widget tests still pass
    - `npm run build` exits 0
    - `grep -c "captureState" src/widget/WidgetController.ts` returns ≥ 2 (RenderChild.onunload + LeetCodeFenceWidget.destroy)
    - `grep -c "hydrateState" src/widget/WidgetController.ts` returns ≥ 1
    - `grep -c "statePersistence" src/main.ts` returns ≥ 3 (instantiation + sweep + clear)
    - `grep -c "stopPropagation" src/widget/WidgetController.ts` returns 1 (Plan 19-01's listener untouched — D-02 belt)
    - `git diff CLAUDE.md` returns no changes
  </acceptance_criteria>
  <done>State persistence wired into mount/unmount; tests green; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT — Plan 19-03 dev-vault state persistence smoke</name>
  <what-built>
    State map keyed by `${file.path}::${fenceIndex}` with 30s TTL; capture cursor/scroll/undo on unmount, hydrate on mount; CM6 history JSON round-trip preserves undo stack across remount; combined with Plan 19-01's `mousedown.stopPropagation` listener (D-02), all major Live Preview unmount paths preserve state.
  </what-built>
  <files>(manual UAT — no source files modified)</files>
  <action>Manual user-acceptance test. The developer executes the steps in <how-to-verify> against a real Obsidian dev vault. Undo stack preservation, 30s TTL expiry, and multi-fence isolation are the critical gates. A fail blocks Plan 19-04.</action>
    <how-to-verify>
    1. `npm run build` exits 0; deploy to dev vault. Reload Obsidian.
    2. With useInlineWidget=ON, open an LC note in Live Preview.
    3. **Cursor + scroll preservation (WIDGET-04)**:
       - Type a long body (~50 lines). Scroll the widget down to mid-body. Place cursor at line 30, column 5.
       - Close the note (Cmd-W). Within 5 seconds, reopen the same note (Cmd-O / quick switcher).
       - Confirm: cursor at line 30 column 5; scroll position preserved; widget renders without flash.
    4. **Undo stack preservation (Pitfall 19-C)**:
       - Type 'A', 'B', 'C' as separate edits (small pause between to ensure separate history entries — typically a press-release per char is enough).
       - Close the note. Reopen within 5 seconds.
       - Press Cmd-Z three times. Each undo removes one of A/B/C. After three undos, doc returns to pre-A state. (If history is dropped on unmount, only one undo would happen and Cmd-Z would do nothing afterwards — that's the test fail.)
    5. **30s TTL expiry**:
       - Type a unique sentinel string. Close note. Wait 35 seconds (use a phone timer). Reopen the note.
       - Confirm: cursor at position 0 (default), NOT at the prior cursor location. State entry expired and was swept. (Test that sweep ran by checking DevTools — `setInterval` registered at 60s; 60s sweep + 30s TTL means entries are swept within 90s — ours wait of 35s confirms expiry on the NEXT mount even if sweep hasn't fired yet.)
    6. **Live Preview cursor approach (D-02 belt-and-suspenders)**:
       - Place parent cursor (use Source mode briefly OR click in the markdown above the widget) just above the `## Code` heading.
       - In Live Preview, arrow-down repeatedly toward the widget. The cursor should jump OVER the widget (atomicRanges from Plan 19-01) without unmounting it.
       - Click directly on the widget's CM6 area. The mousedown.stopPropagation listener prevents the parent's "raw source reveal" — widget should NOT flash to raw fence; cursor lands inside the widget. (If the empirical test reveals stopPropagation doesn't fully prevent reveal, the persistence map kicks in: even if widget remounts, cursor + scroll restored within 30s.)
    7. **Mode switch (Cmd-E) preservation**:
       - Place cursor at line 20 col 3 in widget (Live Preview). Press Cmd-E to switch to Source mode (raw fence visible). Press Cmd-E to return to Live Preview.
       - Confirm: cursor at line 20 col 3 in widget. Scroll position preserved. Undo stack preserved (Cmd-Z undoes prior typing not the mode switch).
    8. **Theme change preservation**:
       - Open Settings → Appearance → switch theme. The widget remounts (theme classes re-render). Confirm cursor position and undo stack survive the remount.
    9. **Multi-fence persistence isolation (CONTEXT D-01)**:
       - In a test note with TWO `leetcode-solve` fences: edit fence 0 (cursor at line 5 col 2), then edit fence 1 (cursor at line 8 col 4). Close note. Reopen. Confirm: fence 0's widget cursor at line 5 col 2; fence 1's widget cursor at line 8 col 4. Keys are isolated by `::fenceIndex`.
    10. **No state leakage to wrong file**:
       - Edit note A, position cursor; close. Edit note B (different lc-slug). State map should NOT hydrate note B's widget with note A's cursor (key includes file.path).
    11. **NO regression of Plan 19-02 force-quit / sync behaviors** — repeat Plan 19-02 UAT step 3 (force-quit) — sentinel string still preserved; persistence + sync interact correctly.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues. Critical: undo stack lost on remount (history round-trip broken — RESEARCH Pitfall 19-C); cursor jumps to 0 on every Live Preview interaction; state map leaks across files; widget flashes raw source on click despite stopPropagation+persistence (escalate — Phase 22 may need to revisit).</resume-signal>
  <verify>(developer confirms each step; user replies "approved" or with issues)</verify>
  <done>Approved by user; Plan 19-03 acceptance gates met; ready for Plan 19-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StatePersistenceMap | Plugin-singleton; in-memory only; never serialized to disk |
| view.state.toJSON({history}) | CM6-internal serialization; no user input crosses boundary |
| 30s TTL window | Bounds entry lifetime; sweep on plugin load + interval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-07 | Resource Growth | StatePersistenceMap unbounded | mitigate | 30s TTL bounds entry lifetime; sweepExpired called on Plugin.onload AND every 60s via registerInterval; clear() on Plugin.onunload drains. Memory cap O(open notes × 30s session window) — bounded ~tens of entries. |
| T-19-NEW-Phase19-C | Tampering / Data Loss | CM6 history serialization round-trip | mitigate | EditorState.fromJSON(json, {extensions, doc}, {history: historyField}) per RESEARCH Pitfall 19-C — DO NOT use setValue + dispatch. Plan 19-03 historyRoundTrip.test.ts verifies undo stack preservation; if empirical test fails, fall back to cursor+scroll only and document residual limitation (RESEARCH Open Question 3). |
| T-19-NEW-Phase19-3 | Tampering | Cursor clamping on hydrate | mitigate | hydrateState uses Math.min(cursor, view.state.doc.length) per RESEARCH Pattern 4 line 301; if doc shrunk between unmount and remount, cursor lands at end-of-doc rather than throwing or landing past EOF. |
| T-19-NEW-Phase19-Persist1 | Information Disclosure | Cross-file state leakage | accept | Map keys include file.path; clearForPath on rename. Same-vault same-user context — no cross-tenant concern. UAT step 10 verifies isolation. |
| T-19-04 (carry) | Elevation of Privilege | Persistence map applied to non-LC fences | mitigate | persistenceKey computed only inside mountLeetCodeWidget which is gated behind useInlineWidget=ON + lc-slug + non-stray fence (Plan 19-01). Stray fence and embed paths use readOnly RenderChild which still hydrates state (legitimate UX) but never writes to disk (readOnly=true means no debouncedWriter run). |
</threat_model>

<verification>
**Phase-level gates for Plan 19-03:**
- `npm test -- --run tests/widget/` — all tests green (now 22 widget files including 3 new)
- `npm test -- --run tests/main/mutualExclusion.test.ts` — green
- `npm run build` exits 0
- `grep -c "fromJSON" src/widget/statePersistence.ts` returns ≥ 1 (Pitfall 19-C requirement)
- `git diff CLAUDE.md` returns no changes
- `git diff --name-only src/main/` returns no v1.2 files modified
- UAT (Task 4) — undo stack preservation + 30s TTL + multi-fence isolation all pass
</verification>

<success_criteria>
1. WIDGET-04: State map keyed by ${file.path}::${fenceIndex} with 30s TTL; capture on unmount; hydrate on mount; sweep on plugin load
2. WIDGET-03: Mount on fence-tag semantics — embeds and stray fences also benefit from the persistence map (state survives remount even for read-only widgets)
3. CONTEXT D-02 belt-and-suspenders: stopPropagation listener (Plan 19-01) + state-persistence map (Plan 19-03) both ship; combined coverage of Live Preview unmount scenarios
4. CM6 undo stack survives unmount/remount via state.toJSON({history}) + EditorState.fromJSON (RESEARCH Pitfall 19-C); OR cursor+scroll-only fallback documented
5. Multi-fence isolation: ::fenceIndex keys prevent cross-fence state leak (CONTEXT D-01)
6. v1.2 path with useInlineWidget=OFF still regression-clean (UAT)
</success_criteria>

<output>
Create `.planning/phases/19-widget-foundation-one-way-sync/19-03-SUMMARY.md` when done. If history round-trip via fromJSON fails empirically, document the residual limitation explicitly (Plan 19-04 may revisit).
</output>
