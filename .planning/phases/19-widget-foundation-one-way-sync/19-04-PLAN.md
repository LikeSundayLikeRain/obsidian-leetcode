---
phase: 19-widget-foundation-one-way-sync
plan: 04
type: execute
wave: 4
depends_on:
  - 19-03
files_modified:
  - src/widget/codeBlockProcessor.ts
  - src/widget/embedDetect.ts
  - src/widget/WidgetController.ts
  - src/widget/LeetCodeFenceWidget.ts
  - src/widget/fenceSerialization.ts
  - tests/widget/embedDetection.test.ts
  - tests/widget/strayFenceFallback.test.ts
  - tests/widget/languageFallback.test.ts
  - tests/widget/widgetEquality.test.ts
  - tests/widget/fenceSerialization.property.test.ts
autonomous: false
requirements:
  - WIDGET-06
  - EMBED-01
  - EMBED-02
  - EMBED-03
  - EMBED-04
must_haves:
  truths:
    - "![[lc-note]] embed renders read-only widget — no editing, no buttons (no buttons exist in Phase 19 anyway per CONTEXT D-03)"
    - "![[lc-note#Code]] section embed renders read-only widget"
    - "Stray ```leetcode-solve fence in non-LC note (no lc-slug) renders read-only widget OR static <pre><code> fallback — never crashes, never offers Run/Submit"
    - "Embed detection uses BOTH .markdown-embed/.internal-embed ancestor walk AND ctx.sourcePath !== file.path mismatch (per RESEARCH §3 — either signal sufficient)"
    - "Missing or unrecognized lc-language frontmatter falls back to Python with a Notice issued exactly once per mount (WIDGET-06)"
    - "Property test corpus expanded to cover CRLF, nested triple backticks, edge whitespace, frontmatter-like lines, empty body, trailing whitespace, mid-character truncation, multi-fence within blockquote — full RESEARCH §5 expansion"
    - "WidgetType.eq() identity is content-hash-based (filePath + fenceIndex + sourceHash); CM6 reuses widget DOM across rebuilds for unchanged fences (RESEARCH Pitfall 19-F)"
  artifacts:
    - path: src/widget/codeBlockProcessor.ts
      provides: "Read-only widget routing for embeds and stray-fence-in-LC-note variants; embed detection both-signals integration"
      contains: "isEmbedContext"
    - path: src/widget/embedDetect.ts
      provides: "Refined isEmbedContext combining DOM ancestor walk + sourcePath mismatch (Plan 19-01 baseline + Plan 19-04 hardening)"
      contains: "markdown-embed"
    - path: src/widget/WidgetController.ts
      provides: "Notice-once-per-mount language fallback path"
      contains: "Notice"
    - path: src/widget/LeetCodeFenceWidget.ts
      provides: "eq() returns true iff (filePath, fenceIndex, sourceHash) all equal — content-hash identity"
      contains: "sourceHash"
    - path: src/widget/fenceSerialization.ts
      provides: "Hardening for CRLF/nested-backtick/blockquote/no-trailing-newline edge cases — same exports, broader correctness"
      contains: "rewriteFenceBody"
    - path: tests/widget/fenceSerialization.property.test.ts
      provides: "Expanded SHELLS corpus per RESEARCH §5"
  key_links:
    - from: src/widget/codeBlockProcessor.ts
      to: src/widget/embedDetect.ts
      via: isEmbedContext routing decisions
      pattern: "isEmbedContext"
    - from: src/widget/LeetCodeFenceWidget.ts
      to: src/widget/fenceSerialization.ts
      via: sourceHash computed from extracted fence body content
      pattern: "sourceHash"
---

<objective>
Phase 19 Plan 04 — Embed + Stray Fence + Property-Test Hardening: complete the edge-case surface. Embeds (both `![[lc-note]]` and `![[lc-note#Code]]`) render read-only via dual-signal detection; stray `leetcode-solve` fences in non-LC notes degrade safely; missing or unrecognized `lc-language` falls back to Python with a Notice; expanded property test corpus catches the long-tail round-trip bugs; `WidgetType.eq()` identity uses a content hash to prevent CM6 widget thrash on no-op rebuilds.

Purpose: WIDGET-06 (language fallback + Notice), EMBED-01..04 (read-only widget for `![[…]]` embeds; section embeds; dual-signal detection; stray-fence safe handling), expanded SYNC-06 corpus (RESEARCH §5), Pitfall 19-F mitigation (eq() identity prevents unnecessary remount per RESEARCH lines 412-422), Pitfall 19-D treatment (null getSectionInfo as embed-likely signal, refining Plan 19-01's static fallback).

Output: Updates to 5 src files, 1 expanded test file, 4 new test files. Acceptance: hub note containing `![[lc-note]]` shows read-only widget; non-LC note with stray `leetcode-solve` fence shows safe fallback; missing `lc-language` triggers exactly one Notice on mount; property test corpus passes all expanded shells.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-widget-foundation-one-way-sync/19-CONTEXT.md
@.planning/phases/19-widget-foundation-one-way-sync/19-RESEARCH.md
@.planning/phases/19-widget-foundation-one-way-sync/19-PATTERNS.md
@.planning/phases/19-widget-foundation-one-way-sync/19-VALIDATION.md
@.planning/phases/19-widget-foundation-one-way-sync/19-03-SUMMARY.md
@CLAUDE.md
@src/widget/codeBlockProcessor.ts
@src/widget/embedDetect.ts
@src/widget/WidgetController.ts
@src/widget/LeetCodeFenceWidget.ts
@src/widget/fenceSerialization.ts
@src/main/childEditorLanguage.ts

<interfaces>
<!-- Refined / extended exports for Plan 19-04 -->

```typescript
// src/widget/embedDetect.ts (Plan 19-01 baseline; refined here)
export function isEmbedContext(
  el: HTMLElement,
  ctx: MarkdownPostProcessorContext,
  targetFile: TFile,
): boolean;
// Plan 19-04 refinement: also returns true when getSectionInfo previously returned null
// (treat null section info as embed-likely per RESEARCH Pitfall 19-D).

// src/widget/LeetCodeFenceWidget.ts — eq() identity uses content hash
class LeetCodeFenceWidget extends WidgetType {
  constructor(
    public plugin: LeetCodePlugin,
    public file: TFile,
    public fenceIndex: number,
    public sourceHash: string,    // sha1 of fence body (synchronous? — see action)
    public source: string,
  );
  eq(other: WidgetType): boolean;  // true iff filePath + fenceIndex + sourceHash all equal
  ignoreEvent(): boolean;          // true (carry-over from Plan 19-01)
}
```

Existing slug→pack mapping (carry-over):
```typescript
// src/main/childEditorLanguage.ts
export function buildLanguageExtensions(slug: string, indent: 'auto' | number): Extension;
// Internally maps: python|python3 → Python; java → Java; cpp|c → C++; javascript|js → JS;
//                  typescript|ts → TS; c → C-only; golang|go → Go; rust → Rust.
// Phase 19-04 adds explicit fallback for missing/unrecognized: → Python + Notice.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — RED tests for embed detection, stray-fence safety, language fallback, widget equality, expanded property corpus</name>
  <files>tests/widget/embedDetection.test.ts, tests/widget/strayFenceFallback.test.ts, tests/widget/languageFallback.test.ts, tests/widget/widgetEquality.test.ts, tests/widget/fenceSerialization.property.test.ts</files>
  <read_first>
    - .planning/phases/19-widget-foundation-one-way-sync/19-VALIDATION.md (rows 19-04-01..05)
    - .planning/phases/19-widget-foundation-one-way-sync/19-PATTERNS.md (lines 51-55 embed/stray/language/equality test analog table; lines 731-739 strayFenceFallback exact analog)
    - .planning/phases/19-widget-foundation-one-way-sync/19-RESEARCH.md (Pitfall 19-D lines 391-399 embed null section info; Pitfall 19-F lines 412-422 eq() content-hash; Specific Findings §3 lines 659-666 dual-signal embed; §5 lines 670-677 corpus expansion; Code Examples §"Embed detection (BOTH signals)" lines 480-499)
    - .planning/phases/19-widget-foundation-one-way-sync/19-CONTEXT.md (C-11, C-15; D-04)
    - tests/main/codeActionsPostProcessor.test.ts (lines 101-130 no-lc-slug analog)
    - tests/main/childEditorLanguage.test.ts (D-04 unknown-slug Python fallback analog)
    - tests/main/codeActionsEditorExtension.test.ts (CodeActionsWidget.eq currentSlug test)
    - tests/widget/fenceSerialization.property.test.ts (Plan 19-01 — Plan 19-04 expands)
    - tests/widget/__fixtures__/lcNoteFixtures.ts + cm6Helpers.ts (Plan 19-01 — extend with embed/stray fixtures)
  </read_first>
  <behavior>
    - embedDetection.test.ts: cases over makeFakeMarkdownPostProcessorContext + makeFakeFile.
      - DOM ancestor with `markdown-embed` class on parent → true
      - DOM ancestor with `internal-embed` class three levels up → true
      - No embed class anywhere AND ctx.sourcePath === file.path → false
      - No embed class AND ctx.sourcePath !== file.path → true (signal 2)
      - Both signals true → true (idempotent)
      - Plan 19-04 addition: also true when getSectionInfo returned null at the call site that consults isEmbedContext (Pitfall 19-D treatment integrated in codeBlockProcessor flow — assert via codeBlockProcessor branch test rather than embedDetect itself)
    - strayFenceFallback.test.ts: register codeBlockProcessor handler; render against:
      (a) non-LC note (no lc-slug, no embed context) with leetcode-solve fence → static `<pre><code>` rendered; el.firstChild instanceof HTMLPreElement; no addChild called for editable RenderChild; no exception thrown.
      (b) non-LC note inside an embed context → readOnly RenderChild added (still safer than nothing — user gets read-only view of the fence content); EMBED-04 satisfied because still no Run/Submit (nothing exists in Phase 19).
      (c) null getSectionInfo path (already covered by Plan 19-01 test, but assert here too): static fallback renders; never throws.
      (d) malformed fence (opener present but no closer) → static fallback; never throws.
    - languageFallback.test.ts: WIDGET-06.
      - Mount with frontmatter `lc-slug: foo, lc-language: python3` → languageCompartment configured for Python; NO Notice emitted.
      - Mount with frontmatter `lc-slug: foo, lc-language: kotlin` (unsupported) → languageCompartment configured for Python; Notice emitted exactly once per mount with text including 'kotlin' and 'Python'.
      - Mount with frontmatter `lc-slug: foo` (no lc-language) → Python + Notice emitted exactly once per mount with text including 'lc-language' missing context.
      - Notice instance counted via `vi.spyOn(globalThis as any, 'Notice')` or via the Notice class export from tests/helpers/obsidian-stub.ts (recommended): `expect(Notice).toHaveBeenCalledTimes(1)`.
      - Re-mount the SAME widget (simulate remount) → Notice still emits once per mount (not deduplicated across mounts) — design decision: per-mount Notice is acceptable per VALIDATION row 19-04-03 ('exactly once per mount').
    - widgetEquality.test.ts: RESEARCH Pitfall 19-F.
      - Two LeetCodeFenceWidget instances with identical (plugin, file, fenceIndex, sourceHash) — eq() returns true.
      - Same file, same fenceIndex, DIFFERENT sourceHash → eq returns false (body changed; CM6 should remount).
      - Same file, DIFFERENT fenceIndex, same sourceHash → eq returns false.
      - DIFFERENT plugin (e.g., another instance) → eq returns false.
      - Self-equality: w.eq(w) returns true.
      - Order independence in object construction does not affect eq.
      - eq() does NOT include the WidgetController instance — proven by constructing two widgets that wrap different controllers but have same identity tuple → eq returns true (RESEARCH lines 419-421).
    - fenceSerialization.property.test.ts EXPANSION (RESEARCH §5):
      - Add SHELLS variants: mixed Windows/Unix line endings within the same file (e.g., `## Code\r\n` opener with body using LF only); shells with non-`leetcode-solve` fences before/after the target (test fenceIndex shifts); shells with `\`\`\`leetcode-solve` opener inside a blockquote prefix (`> \`\`\`leetcode-solve`); shells with the closer on the last line (no trailing newline); body containing the literal string `## Code` (false-heading defense); body containing the SHELL's exact frontmatter `---` lookalike at column 0.
      - Each new SHELL × HOSTILE_BODIES combination must satisfy `rewriteFenceBody(file, idx, extractFenceBody(file, idx) ?? '') === file`.
  </behavior>
  <action>
    Create the 4 new test files + extend the existing fenceSerialization.property.test.ts. Use `vi.mock('obsidian', ...)` shape from PATTERNS lines 1115-1122. Use `Notice` class as exported from tests/helpers/obsidian-stub.ts (verify the stub exports it; if not, extend the stub to track Notice instantiations). Use realistic fixtures that include actual triple-backtick bodies, frontmatter-like lines, CRLF endings — these are the long-tail cases that catch round-trip bugs. Mark tests RED-on-create (they fail until Tasks 2-3 update src). DO NOT touch CLAUDE.md. DO NOT modify v1.2 files in CONTEXT C-17.
  </action>
  <verify>
    <automated>npm test -- --run --reporter=dot tests/widget/embedDetection.test.ts tests/widget/strayFenceFallback.test.ts tests/widget/languageFallback.test.ts tests/widget/widgetEquality.test.ts tests/widget/fenceSerialization.property.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 5 test files exist with ≥3 concrete assertions each (fenceSerialization grew from Plan 19-01 baseline)
    - vitest discovers all
    - The 4 new files are RED today; fenceSerialization.property.test.ts may pass partially if rewriteFenceBody already handles edge cases — failing cases are documented for Task 3
  </acceptance_criteria>
  <done>5 RED/expanded test files committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Embed routing refinement + stray-fence safety + language fallback Notice + WidgetType eq() content hash</name>
  <files>src/widget/codeBlockProcessor.ts, src/widget/embedDetect.ts, src/widget/WidgetController.ts, src/widget/LeetCodeFenceWidget.ts</files>
  <read_first>
    - src/widget/codeBlockProcessor.ts (Plan 19-01 baseline)
    - src/widget/embedDetect.ts (Plan 19-01 baseline)
    - src/widget/WidgetController.ts (Plan 19-03 state)
    - src/widget/LeetCodeFenceWidget.ts (Plan 19-01 baseline)
    - src/main/childEditorLanguage.ts (8-pack registry; KNOWN_SLUGS list)
    - .planning/phases/19-widget-foundation-one-way-sync/19-RESEARCH.md (Pitfall 19-D lines 391-399; Pitfall 19-F lines 412-422; §3 lines 659-666; Code Examples lines 480-499)
    - .planning/phases/19-widget-foundation-one-way-sync/19-PATTERNS.md (lines 1027-1042 lc-slug + getSectionInfo gate)
    - .planning/phases/19-widget-foundation-one-way-sync/19-CONTEXT.md (C-11, C-15; D-04)
    - tests/widget/embedDetection.test.ts + strayFenceFallback.test.ts + languageFallback.test.ts + widgetEquality.test.ts (RED from Task 1)
  </read_first>
  <action>
    1. `src/widget/embedDetect.ts` — Plan 19-04 refinement on top of Plan 19-01 baseline:
       - Keep the DOM ancestor walk (`markdown-embed`/`internal-embed`) AND the `ctx.sourcePath !== targetFile.path` check.
       - Add an OPTIONAL third parameter `info: MarkdownSectionInformation | null`; if `info === null`, return `true` (Pitfall 19-D treatment — null section info is embed-likely).
       - Documenting comment notes that section embeds (`![[lc-note#Code]]`) typically present as `sourcePath !== file.path` AND null section info both, so either signal alone catches them.
    2. `src/widget/codeBlockProcessor.ts` — Plan 19-04 routing refinement:
       - Refactor the branching from Plan 19-01 to handle the four corners cleanly:
         (a) `lc-slug` present AND not embed AND non-null info → editable RenderChild (the LC-note primary path).
         (b) `lc-slug` present AND (embed OR null info) → readOnly RenderChild (EMBED-01, EMBED-02, EMBED-03 — both `![[lc-note]]` whole-note embeds and `![[lc-note#Code]]` section embeds satisfied).
         (c) No `lc-slug` AND not embed → static `<pre><code>` fallback (CONTEXT D-04 stray fence in non-LC note; EMBED-04).
         (d) No `lc-slug` AND embed → readOnly RenderChild OR static fallback (CONTEXT C-15 says "stray ```leetcode-solve fence in non-LC note (no lc-slug) renders read-only widget — never crashes, never offers Run/Submit"; this Plan ships read-only widget for embeds even on non-LC source files because it's safer UX, but if the resolved targetFile has no lc-slug AND we're in a host's embed → static fallback is also acceptable. Pick one consistently; recommended: readOnly widget for any embed regardless of lc-slug because it's more useful and Phase 19 has no Run/Submit anyway).
       - Pass `isEmbedContext(el, ctx, file, info)` (with the new info param) at every check.
       - When mounting, compute `sourceHash` via `await sha1(source)` and pass it to `LeetCodeFenceWidget` constructor (used by Live Preview path; Reading-mode path uses LeetCodeWidgetRenderChild which doesn't need WidgetType.eq but does need to surface the same identity tuple to the registry key construction).
    3. `src/widget/WidgetController.ts` — language fallback Notice (WIDGET-06):
       - In `mountLeetCodeWidget`, after extracting `lc-language` from frontmatter:
         ```
         const KNOWN_SLUGS = ['python','python3','java','cpp','c','javascript','js','typescript','ts','golang','go','rust'];
         const rawLang = (fm?.['lc-language'] as string | undefined)?.toLowerCase();
         let slug = 'python';
         let needNotice = false;
         let noticeReason: 'missing' | 'unknown' = 'missing';
         if (typeof rawLang === 'string' && rawLang.length > 0) {
           if (KNOWN_SLUGS.includes(rawLang)) {
             slug = rawLang;
           } else {
             needNotice = true;
             noticeReason = 'unknown';
           }
         } else {
           needNotice = true;
           noticeReason = 'missing';
         }
         if (needNotice) {
           const msg = noticeReason === 'unknown'
             ? `LeetCode widget: lc-language '${rawLang}' not supported; falling back to Python.`
             : `LeetCode widget: lc-language frontmatter missing; falling back to Python.`;
           new Notice(msg, 5000);
         }
         ```
       - Notice fires exactly once per mount call. DO NOT add cross-mount deduplication (per VALIDATION row 19-04-03 contract).
    4. `src/widget/LeetCodeFenceWidget.ts` — content-hash eq() per RESEARCH Pitfall 19-F:
       - Constructor signature `constructor(public plugin: LeetCodePlugin, public file: TFile, public fenceIndex: number, public sourceHash: string, public source: string)`.
       - `eq(other: WidgetType): boolean { return other instanceof LeetCodeFenceWidget && other.plugin === this.plugin && other.file === this.file && other.fenceIndex === this.fenceIndex && other.sourceHash === this.sourceHash; }`. NEVER include the WidgetController instance in the comparison.
       - The Live Preview ViewPlugin (Plan 19-01 Task 2) must compute sourceHash from the fence body BEFORE constructing the widget. **Concern**: sha1 is async (`crypto.subtle.digest`), but ViewPlugin update() is synchronous. Resolution: use a synchronous hash for eq() identity ONLY (a small DJB2 or a 30-LOC sha1-sync fallback in `src/widget/hash.ts`). Hash for self-write suppression remains SHA-1 via crypto.subtle (where async-await is fine inside DebouncedWriter.flush()). The two hashes serve different purposes (eq identity = synchronous-fast-non-cryptographic-OK; suppression = collision-resistant). DO NOT conflate them; document the distinction in `src/widget/hash.ts`.
       - Update `liveModeViewPlugin.ts` to compute the synchronous hash inline when constructing the widget per fence — extract the fence body via `extractFenceBody(view.state.doc.toString(), fenceIndex)` and hash via `djb2(body)` (or chosen sync hash).
    5. DO NOT modify CLAUDE.md. DO NOT modify v1.2 files in CONTEXT C-17.
  </action>
  <verify>
    <automated>npm test -- --run tests/widget/embedDetection.test.ts tests/widget/strayFenceFallback.test.ts tests/widget/languageFallback.test.ts tests/widget/widgetEquality.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 4 named test files green
    - All previously-green widget tests still pass: `npm test -- --run tests/widget/` exits 0
    - `grep -c "lc-language" src/widget/WidgetController.ts` returns ≥ 1
    - `grep -c "new Notice" src/widget/WidgetController.ts` returns ≥ 2 (missing + unknown branches)
    - `grep -c "sourceHash" src/widget/LeetCodeFenceWidget.ts` returns ≥ 2 (constructor + eq)
    - `grep -c "instanceof LeetCodeFenceWidget" src/widget/LeetCodeFenceWidget.ts` returns ≥ 1 (eq guard)
    - `grep -c "WidgetController" src/widget/LeetCodeFenceWidget.ts` returns 0 in eq() body (verified by inspection — never include controller in identity)
    - `grep -c "innerHTML" src/widget/` returns 0 (CLAUDE.md preserve)
  </acceptance_criteria>
  <done>Embed routing + language fallback + content-hash eq() all green; static fallback never crashes; Notice fires correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: fenceSerialization hardening for the expanded SHELL × BODY corpus (CRLF / blockquote / no-trailing-newline / multi-fence-with-non-LC-around / mid-byte)</name>
  <files>src/widget/fenceSerialization.ts</files>
  <read_first>
    - src/widget/fenceSerialization.ts (Plan 19-01 baseline)
    - .planning/phases/19-widget-foundation-one-way-sync/19-RESEARCH.md (Specific Findings §5 lines 670-677; PITFALLS P6 lines 204-246 in research/PITFALLS.md context)
    - .planning/phases/19-widget-foundation-one-way-sync/19-PATTERNS.md (lines 562-595 fenceSerialization role-match)
    - tests/widget/fenceSerialization.property.test.ts (Plan 19-04 Task 1 expansion)
  </read_first>
  <action>
    Update `src/widget/fenceSerialization.ts` to handle the long-tail cases that the expanded corpus surfaces:
    1. **CRLF preservation**: when splitting and joining, preserve the file's existing line endings exactly. Use a regex split that retains the actual newline characters (e.g., `match` instead of `split`, OR detect file-level `\r\n` vs `\n` once and use it for re-joins). RESEARCH Specific Findings §5: do NOT normalize CRLF.
    2. **No trailing newline**: when the closer line is the last line in the file with no trailing `\n`, preserve that — `rewriteFenceBody` should not append a trailing newline. Detect via `noteBody.endsWith('\n')` before write; replicate the trailing state.
    3. **Blockquote prefix**: a `\`\`\`leetcode-solve` opener inside `> > leetcode-solve` blockquote — Phase 19 acceptable behavior is to NOT match the fence (`/^\s*\`\`\`leetcode-solve\b/` already permits leading whitespace; if blockquote `>` characters appear, the regex won't match). Document this in a code comment: blockquoted fences are out of scope for Phase 19; they fall through to default markdown rendering. (Consistent with WIDGET-03's mount-on-fence-tag semantics — only top-level openers count.)
    4. **Body containing the literal string `## Code` or `---` at column 0**: extractFenceBody/rewriteFenceBody must NOT be confused — they operate ONLY on `\`\`\`leetcode-solve` openers and the nearest subsequent `\`\`\`` closer line. `## Code` and `---` inside a fence body are pure content. The current line-by-line scan already handles this correctly; verify via test.
    5. **Mixed Windows/Unix line endings within the same file**: rare but real (file edited on multiple platforms with no .gitattributes). Resolution: walk line endings on the first split; preserve each line's terminator individually. Implement via a tokenizing scan that splits into `(lineContent, lineTerminator)` pairs where lineTerminator is `'\n'` | `'\r\n'` | `''` (last line without terminator). Re-join preserves each pair's terminator.
    6. **Multi-fence with non-leetcode-solve fences interleaved**: confirm `computeFenceIndex` (already in fenceLocator.ts) only counts `leetcode-solve` openers. extractFenceBody at index N skips non-leetcode-solve fences entirely. Verified by Plan 19-04 corpus test cases that include a `\`\`\`python` fence between two `\`\`\`leetcode-solve` fences.
    7. **Round-trip invariant remains**: `rewriteFenceBody(s, i, extractFenceBody(s, i) ?? '') === s` for every well-formed shell in the corpus.
    8. DO NOT modify CLAUDE.md. DO NOT modify v1.2 files in CONTEXT C-17.
  </action>
  <verify>
    <automated>npm test -- --run tests/widget/fenceSerialization.property.test.ts && npm run build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run tests/widget/fenceSerialization.property.test.ts` exits 0 across the FULL expanded SHELL × HOSTILE_BODIES matrix
    - `grep -c "trim\\|normalize" src/widget/fenceSerialization.ts` returns 0 (PITFALLS P6 — never normalize)
    - `grep -c "\\\\r\\\\n\\|CRLF\\|line endings" src/widget/fenceSerialization.ts` returns ≥ 1 (acknowledges CRLF handling)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Expanded property test corpus passes; fenceSerialization handles CRLF, no-trailing-newline, mixed line endings, multi-fence interleaved correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: UAT — Plan 19-04 + Phase 19 closeout dev-vault smoke (full Phase 19 acceptance)</name>
  <what-built>
    Embed read-only routing (both `![[lc-note]]` and `![[lc-note#Code]]`); stray-fence safe fallback in non-LC notes; missing/unknown lc-language Python fallback with Notice; expanded property test corpus passing; WidgetType.eq() content-hash identity preventing CM6 widget thrash. Combined with Plans 19-01..19-03, this completes the Phase 19 acceptance surface.
  </what-built>
  <files>(manual UAT — Phase 19 closeout; no source files modified)</files>
  <action>Manual user-acceptance test for Plan 19-04 PLUS Phase 19 closeout. The developer executes the steps in <how-to-verify> against a real Obsidian dev vault. Embed read-only routing, language fallback Notice, eq() identity, expanded property corpus, and the full Phase 19 acceptance gates from CONTEXT §"Success criteria" all verified here.</action>
    <how-to-verify>
    1. `npm run build` exits 0; deploy to dev vault. Reload Obsidian.
    2. **EMBED-01 — `![[lc-note]]` whole-note embed**:
       - Create a hub note: `## My LeetCode Practice\n\n![[two-sum]]\n\n![[reverse-list]]`. Open in Reading mode and Live Preview.
       - Confirm: each embed renders the LC widget READ-ONLY (cannot type into the inner CM6); container has `lc-nested-editor HyperMD-codeblock lc-leetcode-solve` classes; no Run/Submit (none exist in Phase 19); no console errors.
    3. **EMBED-02 — `![[lc-note#Code]]` section embed**:
       - Create a hub note containing `![[two-sum#Code]]`. Open.
       - Confirm: only the `## Code` section embeds; widget renders read-only with the fence body. `getSectionInfo` likely returned null for this transclusion (Pitfall 19-D); the Plan 19-04 routing refinement caught it via the null-info → embed-likely path.
    4. **EMBED-03 — dual-signal detection**:
       - Open the hub note from step 2 → DevTools → inspect the embed widget's container; confirm a `markdown-embed` ancestor exists (signal 1 caught it).
       - Direct test of signal 2: open `two-sum.md` directly. ctx.sourcePath === file.path; embed detection returns false; widget mounts editable. Confirm via typing in the widget — it accepts input.
    5. **EMBED-04 — stray fence in non-LC note**:
       - Create a non-LC note: `# My Notes\n\n\`\`\`leetcode-solve\nrandom code\n\`\`\``. (No frontmatter, no lc-slug.) Open in Reading mode.
       - Confirm: static `<pre><code>` rendered showing 'random code'; no editable widget; no Run/Submit; no console errors. Open in Live Preview — same fallback (no widget mount because lc-slug gate fails AND not in embed context).
    6. **WIDGET-06 — language fallback**:
       - Open an LC note. Manually edit frontmatter to remove `lc-language` entirely. Reload note. Confirm: a Notice appears with text 'LeetCode widget: lc-language frontmatter missing; falling back to Python.' Widget renders with Python syntax highlighting.
       - Edit frontmatter to set `lc-language: kotlin`. Reload note. Confirm: Notice 'LeetCode widget: lc-language 'kotlin' not supported; falling back to Python.' appears. Widget renders Python.
       - Edit frontmatter to set `lc-language: python3`. Reload note. Confirm: NO Notice; widget renders Python.
       - Notice fires exactly once per mount (not on every keystroke, not on remount within 30s — re-mount IS one Notice though, per VALIDATION row 19-04-03).
    7. **WidgetType.eq() content-hash (Pitfall 19-F)**:
       - Type continuously in Live Preview for 10s. Watch DevTools → Performance/Profiling: confirm CM6 does NOT call `LeetCodeFenceWidget.toDOM()` per keystroke (i.e., the inner widget DOM is reused). If toDOM fires per keystroke, eq() identity is broken — flag.
       - Edit the file body OUTSIDE the fence (e.g., type in `## Problem` body via Source mode). Confirm: the widget DOES NOT remount (its sourceHash hasn't changed because fence body unchanged).
       - Type INSIDE the fence body. Now the widget MAY remount (sourceHash changed) — but state-persistence map (Plan 19-03) restores cursor + scroll + undo, so user-visible behavior is seamless.
    8. **Round-trip property tests in CI**:
       - `npm test -- --run tests/widget/fenceSerialization.property.test.ts` exits 0 — every shell × body combination passes.
       - DevTools console: NO 'post-flush hash drift' warnings during normal typing in any of the test scenarios in steps 2-7.
    9. **Phase 19 full-suite acceptance (the gates from CONTEXT §"Success criteria")**:
       (1) Reading + Live Preview render identical CM6; Reading is editable.of(false) — VERIFIED step 4 (signal 2 path) + step 7. ✓
       (2) Type → 400ms later disk reflects byte-for-byte — VERIFIED Plan 19-02 UAT. ✓
       (3) Force-quit within ms preserves chars — VERIFIED Plan 19-02 UAT step 3. ✓
       (4) `![[lc-note]]` embed read-only; stray fence safe — VERIFIED steps 2, 5. ✓
       (5) Parent cursor cannot enter fence; close+reopen within 30s restores cursor/scroll/undo — VERIFIED Plan 19-01 + Plan 19-03 UAT. ✓
       (6) useInlineWidget=OFF default; v1.2 path regression-clean — VERIFIED Plan 19-01 UAT step 8. Re-confirm now: toggle OFF, reload, open existing v1.2 LC note, confirm v1.2 nested-editor mounts and works as before.
    10. **CLAUDE.md preserve check**: `git diff CLAUDE.md` returns no changes. The `'leetcode.*'` userEvent paragraph and Phase 17 D-05 paragraph still present in the file (verify via `grep` — RESEARCH §7).
    11. **v1.2 files preserve check**: `git diff --name-only src/main/childEditorSync.ts src/main/sectionLockExtension.ts src/main/nestedEditorExtension.ts src/main/childEditorRegistry.ts src/main/codeActionsEditorExtension.ts` returns no entries (CONTEXT C-17 preserved).
    12. **Theme matrix sample (Phase 22 owns full regression — Phase 19 baseline check)**: switch through 2-3 community themes (Minimal, Catppuccin) — confirm widget renders without obvious visual breakage. Full theme regression gate is Phase 22 THEME-05.
    13. **Bundle size sanity**: `npm run build` reports bundle size. Phase 19 net delta should be additive (~+600 LOC widget code; v1.2 code still present); compare against Phase 18 baseline (1.71 MB raw / 459 KB gzipped). Acceptable: ≤ +50 KB raw delta. Phase 22 owns the net-negative cutover.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 19, or describe issues. Critical: any embed renders editable; stray fence crashes; language Notice missing/spammy; eq() identity broken (toDOM per keystroke); CLAUDE.md or v1.2 files modified; v1.2 baseline regressed when useInlineWidget=OFF.</resume-signal>
  <verify>(developer confirms each step; user replies "approved" or with issues)</verify>
  <done>Approved by user; Plan 19-04 + Phase 19 acceptance gates met; ready for Phase 20 planning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Embed host note → embedded fence DOM | DOM may be in a different markdown-rendered tree; never offer write authority |
| Stray `leetcode-solve` fence in non-LC note | Untrusted file content — never escalate to editable widget |
| `lc-language` frontmatter value | User input (or external Sync) — must validate against KNOWN_SLUGS |
| WidgetType.eq() identity tuple | CM6 internal — must not include mutable controller refs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 (final) | Elevation of Privilege | codeBlockProcessor non-LC stray fence | mitigate | Four-corners gating in Plan 19-04 Task 2: lc-slug + non-embed → editable; lc-slug + embed/null-info → readOnly; no-lc-slug + non-embed → static fallback (D-04, EMBED-04); no-lc-slug + embed → readOnly (still no write authority because controller's debouncedWriter is gated on readOnly=false). Stray fence cannot reach vault.process. |
| T-19-04-language | Tampering | lc-language frontmatter injection | accept | KNOWN_SLUGS allowlist enforces fallback to Python on miss. Notice surfaces the fallback. No path injection or eval — slug strictly maps to one of the 8 LanguageSupport configs from buildLanguageExtensions (verbatim carry-over from v1.2). |
| T-19-05 (final) | Tampering | LeetCodeFenceWidget.eq identity drift | mitigate | Plan 19-04 Task 2 step 4: eq() compares (plugin, file, fenceIndex, sourceHash); never controller; sourceHash uses synchronous DJB2/inline-sha1 for eq path (separate from cryptographic SHA-1 used by suppression). Test widgetEquality.test.ts verifies. UAT step 7 verifies CM6 DOM reuse. |
| T-19-03 (final) | Tampering / Data Corruption | rewriteFenceBody round-trip across long-tail | mitigate | Plan 19-04 Task 1 expanded corpus + Task 3 hardening: CRLF preservation, no-trailing-newline, mixed line endings, multi-fence interleaved with non-LC fences, blockquote prefix scoped out (documented). Property test suite ~100+ generated cases per RESEARCH §5. |
| T-19-NEW-Phase19-D | Information Disclosure | getSectionInfo returning null in embed | mitigate | Embed detection signal 3: pass `info: MarkdownSectionInformation | null` to isEmbedContext; null → embed-likely → readOnly path. RESEARCH Pitfall 19-D treatment integrated. UAT step 3 verifies section embed `![[lc-note#Code]]`. |
</threat_model>

<verification>
**Phase-level gates for Plan 19-04 (and Phase 19 closeout):**
- `npm test -- --run tests/widget/ tests/main/mutualExclusion.test.ts` — all 24+ tests green
- `npm test -- --run` — full suite green (regression check across all 196+ test files; Phase 18 baseline: 1,713 v1.2 tests)
- `npm run build` exits 0
- `grep -c "innerHTML" src/widget/` returns 0 (CLAUDE.md POLISH-03 carry-through)
- `git diff CLAUDE.md` returns no changes (preserve list)
- `git diff --name-only src/main/childEditorSync.ts src/main/sectionLockExtension.ts src/main/nestedEditorExtension.ts src/main/childEditorRegistry.ts src/main/codeActionsEditorExtension.ts` returns no entries (CONTEXT C-17)
- UAT (Task 4) — all 13 verification scenarios pass; embeds render read-only; language fallback Notice correct; eq() identity verified via DevTools profiling
</verification>

<success_criteria>
1. WIDGET-06: Missing or unknown lc-language → Python fallback + Notice exactly once per mount
2. EMBED-01: `![[lc-note]]` renders read-only widget
3. EMBED-02: `![[lc-note#Code]]` section embed renders read-only widget (null section info routed correctly per Pitfall 19-D)
4. EMBED-03: Embed detection uses BOTH .markdown-embed/.internal-embed ancestor AND ctx.sourcePath mismatch
5. EMBED-04: Stray ```leetcode-solve in non-LC note renders safe fallback (static `<pre><code>` for non-embed; readOnly widget for embed); never crashes; never offers Run/Submit
6. SYNC-06 corpus expansion: CRLF, mixed line endings, no-trailing-newline, multi-fence interleaved, body containing ## Code/--- all preserved byte-exact (RESEARCH §5)
7. RESEARCH Pitfall 19-F: WidgetType.eq() returns true iff (filePath, fenceIndex, sourceHash) all equal; CM6 reuses widget DOM for unchanged fences
8. CLAUDE.md preserve list honored ('leetcode.*' userEvent + Phase 17 D-05 paragraphs untouched)
9. CONTEXT C-17 v1.2 files untouched
10. **Phase 19 acceptance**: all 6 success-criteria observable behaviors from CONTEXT §"Success criteria" verified across Plans 19-01..19-04 UATs
</success_criteria>

<output>
Create `.planning/phases/19-widget-foundation-one-way-sync/19-04-SUMMARY.md` AND `.planning/phases/19-widget-foundation-one-way-sync/19-PHASE-SUMMARY.md` when done. Phase summary aggregates Plans 19-01..19-04 acceptance, the empirical probe results from Plan 19-02 (modify event ordering), the history round-trip status from Plan 19-03, and the bundle-size delta vs Phase 18.
</output>
