---
phase: 20-reconciliation-ux-action-row-section-protection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/sectionProtectionExtension.ts
  - src/main.ts
  - src/widget/WidgetController.ts
  - src/widget/widgetRegistry.ts
  - src/widget/vimMode.ts
  - tests/main/sectionProtectionExtension.test.ts
  - tests/widget/vimReconfigure.test.ts
autonomous: true
requirements:
  - PROTECT-01
  - PROTECT-02
  - VIM-02
tags:
  - section-protection
  - vim-compartment
  - foundation
must_haves:
  truths:
    - "When useInlineWidget=ON, the new sectionProtectionExtension protects ## Problem body, ## Code heading + blank-line pocket, ## Techniques heading — and ONLY those three regions"
    - "When useInlineWidget=ON, the v1.2 sectionLockExtension is NOT registered (mutually exclusive); when useInlineWidget=OFF, sectionLockExtension is registered exactly as before"
    - "Fence opener line and fence closer line are no longer protected by changeFilter — they are editable in source mode (atomicRanges from Phase 19's ViewPlugin keeps the parent cursor out in Live Preview)"
    - "The 'leetcode.*' userEvent bypass is preserved verbatim per D-protect-02 (CLAUDE.md convention stays alive through Phase 21; PROTECT-03 is Phase 22 work)"
    - "The v1.0 boundary fix (UAT 2026-05-13: expanded.push(Math.max(0, from-1))), blank-line pocket logic, malformed-note path, mergeLockedRanges helper, computeSnapTarget helper, transactionFilter selection-snap, and Decoration.line dimming are preserved verbatim"
    - "Each widget owns a per-widget vimCompartment; vimCompartment.reconfigure(vim() / []) swaps vim mode without rebuilding the EditorView (cursor + scroll + undo preserved)"
    - "A single workspace.on('layout-change') listener iterates widgetRegistry.values() and calls reconfigureVim() on each widget when getConfig('vimMode') changes from the cached value"
    - "The dev-vault probe outcome (pass / glitchy-pass=fail / fail) is recorded in 20-01-SUMMARY.md so Phase 22 knows whether to ship VIM-03 banner"
  artifacts:
    - path: "src/main/sectionProtectionExtension.ts"
      provides: "Forked + narrowed protection extension; ~370 LOC after surgical fence-opener/closer body deletion from 527 LOC source"
      exports: ["buildSectionProtectionExtension", "computeProtectedRanges", "mergeLockedRanges", "computeSnapTarget"]
      contains: "tr.annotation(Transaction.userEvent).startsWith('leetcode.')"
    - path: "src/main.ts"
      provides: "Mutually-exclusive registration of sectionProtectionExtension (useInlineWidget=ON) vs. sectionLockExtension (useInlineWidget=OFF); workspace.on('layout-change') vim-toggle dispatcher"
      contains: "buildSectionProtectionExtension"
    - path: "src/widget/WidgetController.ts"
      provides: "Per-widget vimCompartment + reconfigureVim(enabled) method; vimCompartment.of(...) wrapping in buildExtensions"
      exports: ["WidgetController"]
    - path: "src/widget/widgetRegistry.ts"
      provides: "*values() iterator accessor for plugin-side fan-out (also consumed by Plan 20-04)"
    - path: "tests/main/sectionProtectionExtension.test.ts"
      provides: "Forked v1.0 Phase 5.5 UAT regression cases; fence-opener/closer cases removed; remaining cases pass"
    - path: "tests/widget/vimReconfigure.test.ts"
      provides: "vimCompartment.reconfigure round-trip unit test"
  key_links:
    - from: "src/main.ts:1029 (current buildSectionLockExtension call)"
      to: "src/main/sectionProtectionExtension.ts:buildSectionProtectionExtension"
      via: "if (useInlineWidget) { registerEditorExtension(buildSectionProtectionExtension(this)) } else { registerEditorExtension(buildSectionLockExtension(this)) }"
      pattern: "buildSectionProtectionExtension\\(this\\)"
    - from: "src/main.ts (new layout-change listener)"
      to: "WidgetController.reconfigureVim"
      via: "this.registerEvent(this.app.workspace.on('layout-change', () => { for (const ctl of this.widgetRegistry.values()) ctl.reconfigureVim(newVim) }))"
      pattern: "workspace\\.on\\('layout-change'"
    - from: "WidgetController.buildExtensions (vim wrap)"
      to: "vimCompartment.of(this.mountedVimMode ? vim({status: true}) : [])"
      via: "Compartment per-instance, replacing the unconditional vim() injection from Phase 19"
      pattern: "vimCompartment\\.of\\("
---

<objective>
Fork `src/main/sectionLockExtension.ts` (527 LOC) → `src/main/sectionProtectionExtension.ts` (~370 LOC after surgical removal), narrow the protection scope to exactly the three regions still required under the v1.3 widget architecture (`## Problem` body + `## Code` heading + blank-line pocket + `## Techniques` heading), and wire mutually-exclusive registration with `sectionLockExtension` based on `useInlineWidget` (per CONTEXT D-protect-03). In the same plan, ship VIM-02 live-reconfigure: introduce a per-widget `vimCompartment` and a single `workspace.on('layout-change')` plugin-side dispatcher that swaps `vim() ↔ []` without rebuilding the EditorView. Run the v1.0 Phase 5.5 section-lock UAT regression cases against the new extension as the acceptance gate.

Purpose: Make the v1.3 protection surface consistent with the widget-owned fence range (the widget already enforces atomicRanges via Phase 19's ViewPlugin — locking the fence opener/closer body in the parent CM6 is now redundant and blocks legitimate source-mode editing) AND give vim-mode users live-toggle UX without note reload (Q2 / VIM-02). Both changes ship together because they're the "Foundation" dogfood checkpoint (D-plan-02): the v1.3 widget keeps existing keystroke gating correct AND vim toggles live before any UX (action row) or sync (conflict modal) work lands.

Output: A new fork-source file `sectionProtectionExtension.ts` with surgical deletions only, mutually-exclusive plugin registration, a per-widget `vimCompartment` extending Phase 19's `WidgetController`, a single plugin-side layout-change dispatcher, and two new test files (forked regression test + vim-reconfigure unit test). The dev-vault probe outcome (pass / fail / glitchy-pass) is recorded in `20-01-SUMMARY.md` so Phase 22 knows whether VIM-03 banner is needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-reconciliation-ux-action-row-section-protection/20-CONTEXT.md
@.planning/phases/20-reconciliation-ux-action-row-section-protection/20-RESEARCH.md
@.planning/phases/20-reconciliation-ux-action-row-section-protection/20-PATTERNS.md
@.planning/phases/20-reconciliation-ux-action-row-section-protection/20-UI-SPEC.md
@.planning/phases/20-reconciliation-ux-action-row-section-protection/20-VALIDATION.md
@.planning/phases/19-widget-foundation-one-way-sync/19-PHASE-SUMMARY.md
@CLAUDE.md
@src/main/sectionLockExtension.ts
@src/widget/WidgetController.ts
@src/widget/widgetRegistry.ts
@tests/main/sectionLockExtension.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fork sectionLockExtension → sectionProtectionExtension with surgical narrowing + register mutually-exclusively</name>
  <files>src/main/sectionProtectionExtension.ts, src/main.ts, tests/main/sectionProtectionExtension.test.ts</files>
  <read_first>
    - src/main/sectionLockExtension.ts (entire file, 527 LOC — fork source)
    - tests/main/sectionLockExtension.test.ts (entire file — fork source for the regression test)
    - src/main.ts lines 120-135 (existing `import { buildSectionLockExtension }` line) and lines 870-1030 (existing `useInlineWidget` gate block + line 1029 current registration)
    - .planning/phases/20-reconciliation-ux-action-row-section-protection/20-RESEARCH.md §"Pattern 1: Section Protection Narrowing — Forking Strategy" (lines 286-342) and §"Code Examples — Forking sectionLockExtension.ts" (lines 998-1041)
    - .planning/phases/20-reconciliation-ux-action-row-section-protection/20-PATTERNS.md §1 `src/main/sectionProtectionExtension.ts` (lines 41-153) — the byte-level preserve/delete contract
    - .planning/phases/20-reconciliation-ux-action-row-section-protection/20-CONTEXT.md decisions D-protect-01..D-protect-04 (lines 60-68) and L5, L6, L9
    - CLAUDE.md §Conventions paragraph 1 (`'leetcode.*'` userEvent) — verbatim preservation requirement
  </read_first>
  <behavior>
    - Test 1 (forked from sectionLockExtension.test.ts): On a canonical lc-slug note, computeProtectedRanges emits ranges spanning `## Problem` heading-line.from through next-heading boundary (PROBLEM body lock). PASS.
    - Test 2: computeProtectedRanges emits a range from `## Code` heading-line.from through opener-line top boundary (heading + blank-line pocket lock). PASS.
    - Test 3: computeProtectedRanges does NOT emit a range covering fence opener-line content or fence closer line or fence body (per D-protect-01 surgical deletion). PASS.
    - Test 4: computeProtectedRanges emits a single-line range for `## Techniques` heading. PASS.
    - Test 5: changeFilter passes through transactions whose userEvent starts with 'leetcode.' (verbatim bypass per D-protect-02 / L6). PASS.
    - Test 6: changeFilter rejects an `input.type` change inside the `## Problem` body. PASS (regression case from v1.0 Phase 5.5).
    - Test 7: changeFilter accepts an `input.type` change inside the fence body (no longer protected — opposite of v1.2 behavior). PASS.
    - Test 8: When `findCodeFence` returns null (malformed note), only the `## Code` heading line is locked (malformed-note path preserved). PASS.
    - Test 9: Boundary fix preserved — pure insertion at lockFrom is still suppressed (the `expanded.push(Math.max(0, from - 1))` extension at lines 416-430 of source). PASS.
    - Test 10: Plugin onload selects exactly one of buildSectionProtectionExtension / buildSectionLockExtension based on `settings.getUseInlineWidget()`; both are never registered simultaneously.
  </behavior>
  <action>
    Step 1 — Literal copy: `cp src/main/sectionLockExtension.ts src/main/sectionProtectionExtension.ts`. Do NOT rewrite from scratch — fork preserves every UAT-hardened scar (boundary fix, blank-line pocket, malformed-note path, transactionFilter snap logic, Decoration.line dimming) per D-protect-04. Confirm via `wc -l src/main/sectionProtectionExtension.ts` returns 527 before edits begin.

    Step 2 — File-header rename: replace the v1.0 Phase 05.5 banner comment with: "Phase 20 — Section Protection (forked from sectionLockExtension at v1.3 per D-protect-04). Drops fence-opener and fence-closer body lock; preserves Problem body / Code heading + blank-line pocket / Techniques heading + 'leetcode.*' userEvent bypass + boundary fix + malformed-note path."

    Step 3 — Symbol renames (search/replace inside the new file ONLY): `buildSectionLockExtension` → `buildSectionProtectionExtension`. Adjust JSDoc strings that reference "section lock" to "section protection" where the public API name is mentioned. Internal helpers (`mergeLockedRanges`, `computeSnapTarget`, `LOCKED_HEADINGS`, the `LockedRange` type) keep their names — they are pure shape utilities reused verbatim by the regression test fork. Export `computeLockedRanges` ALSO under a new name `computeProtectedRanges` (re-export alias `export { computeLockedRanges as computeProtectedRanges }`) so the new test file can import the canonical name without forcing a wider rename in production code.

    Step 4 — Surgical deletion of the `'code'` branch's fence-closer + inter-fence body lock per 20-PATTERNS §1 lines 140-145 and 20-RESEARCH lines 314-341. In the `computeLockedRanges` function: KEEP the `out.push(headFrom, openerTo)` push (heading + blank-line pocket + opener-line lock — the opener line itself becomes editable in source mode but `atomicRanges` from Phase 19's ViewPlugin keeps the parent cursor out in Live Preview); DELETE entirely the `out.push(closer.from, closerLockTo)` block AND the variable computations preceding it (`nextHeadingLine`, `closerLockTo`); KEEP the `else { out.push(headFrom, headTo); }` malformed-note fallback verbatim. Net deletion: ~30-50 LOC from the `'code'` branch. Do NOT touch any other branch (`'problem'`, `'techniques'`).

    Step 5 — Verify preservation of every other UAT-hardened scar by literal `diff src/main/sectionLockExtension.ts src/main/sectionProtectionExtension.ts` review: the boundary fix (lines 415-431 of source), the `'leetcode.*'` userEvent bypass (lines 384-391 of source — D-protect-02 / L6 — DO NOT REMOVE), the Gate 0 isUserInput check (lines 374-381), the file + lc-slug gate (lines 393-407), the `mergeLockedRanges` helper (228-243), the `computeSnapTarget` helper (263-285), the transactionFilter selection-snap logic (453-520), the `EditorView.decorations.of` `.leetcode-locked-heading-line` line decoration (522-525), the `tr.startState` Pitfall-2 read discipline (33-35). Lines that change: only the file-header banner, the export/symbol renames from Step 3, and the surgical `'code'` branch deletion from Step 4.

    Step 6 — Plugin registration in `src/main.ts`: at line 123 add `import { buildSectionProtectionExtension } from './main/sectionProtectionExtension';` ALONGSIDE the existing `import { buildSectionLockExtension }` (do not delete the v1.2 import — both alive through Phase 21 per L9). At line 1029 (the existing `this.registerEditorExtension(buildSectionLockExtension(this));` call), wrap in the mutual-exclusion gate from 20-PATTERNS §8 lines 533-540: `if (this.settings.getUseInlineWidget()) { this.registerEditorExtension(buildSectionProtectionExtension(this)); } else { this.registerEditorExtension(buildSectionLockExtension(this)); }`. Read `useInlineWidget` ONCE at onload (matches the convention at lines 851-852); do not re-read inside the changeFilter. The mutual-exclusion assert at lines 885-890 (Phase 19 D-06) already guarantees `useInlineWidget && useNestedEditor` cannot both fire.

    Step 7 — Fork the regression test file. `cp tests/main/sectionLockExtension.test.ts tests/main/sectionProtectionExtension.test.ts`. Inside the new file: replace import path `../../src/main/sectionLockExtension` → `../../src/main/sectionProtectionExtension`; rename `buildSectionLockExtension` → `buildSectionProtectionExtension` in the call sites; DELETE every test case that asserts the fence-closer-line lock OR the inter-fence body lock (those ranges are no longer protected by the changeFilter — they are owned by the widget's atomicRanges in Live Preview); PRESERVE every other case verbatim (Problem body lock, Code heading + opener-line + blank-line-pocket lock, Techniques heading lock, malformed-fence fallback, `'leetcode.*'` userEvent bypass, boundary fix, mergeLockedRanges shape, computeSnapTarget decision tree). The remaining cases ARE the v1.0 Phase 5.5 UAT regression rerun gate (per D-protect-04). Add ONE new positive test: `input.type` inside the fence body now passes through the changeFilter (assert via `extractChangeFilterCallback` returning `true` for an in-fence-body change with userEvent `'input.type'`).

    Step 8 — Do NOT delete `src/main/sectionLockExtension.ts` or `tests/main/sectionLockExtension.test.ts`. Both stay alive through Phase 21 for the `useInlineWidget=OFF` path (L9 / DELETE-02 is Phase 22 work).

    Step 9 — Run `npx tsc --noEmit` and `npx vitest run tests/main/sectionProtectionExtension.test.ts tests/main/sectionLockExtension.test.ts` to confirm both extensions still pass their respective gates (per D-protect-03 mutual exclusion verified in unit tests via two separate plugin construction paths).

    Step 10 — **Manual UAT (recorded in 20-01-SUMMARY.md per VALIDATION.md §Manual-Only line 74):** in dev vault (`~/Documents/Obsidian Vault` per project skills MEMORY.md), exercise the four cursor-edge cases — (a) up-arrow into closer line; (b) right-arrow at end of `## Code` heading line; (c) backspace at fence-opener line; (d) type into fence body — and record PASS/FAIL outcomes per case. These cases validate the runtime CM6 `atomicRanges` cursor-skip behavior installed by Phase 19's ViewPlugin and cannot be exercised under vitest (per VALIDATION.md classification — atomicRanges is a runtime-only behavior).

    Per D-protect-01 / D-protect-02 / D-protect-03 / D-protect-04 / L5 / L6 / L9 / PROTECT-01 / PROTECT-02. CRITICAL — DO NOT remove the `'leetcode.*'` userEvent bypass (PROTECT-03 / DELETE-08 are Phase 22 work; CLAUDE.md §Conventions paragraph 1 stays verbatim through Phase 21).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/main/sectionProtectionExtension.test.ts tests/main/sectionLockExtension.test.ts --reporter=basic</automated>
  </verify>
  <done>
    `src/main/sectionProtectionExtension.ts` exists; `wc -l` returns ≤400 (target ~370); `grep -n "'leetcode\\.\\*\\|leetcode\\\\\\.'" src/main/sectionProtectionExtension.ts` returns the verbatim bypass block (Step 5); `grep -n "closer\\.from" src/main/sectionProtectionExtension.ts` returns ZERO hits inside the `'code'` branch (Step 4 surgical deletion verified); `tests/main/sectionProtectionExtension.test.ts` exists and ALL forked-and-kept cases pass; `tests/main/sectionLockExtension.test.ts` continues to pass unchanged; `src/main.ts` line ~1029 contains both `buildSectionProtectionExtension(this)` and `buildSectionLockExtension(this)` inside an `if/else` keyed on `getUseInlineWidget()`; `npx tsc --noEmit` exits 0; the v1.0 Phase 5.5 acceptance cases (Problem body / Code heading + pocket / Techniques heading / boundary fix / `'leetcode.*'` bypass / malformed-note path) continue to pass byte-for-byte against the new extension.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-widget vimCompartment + plugin-side layout-change dispatcher + dev-vault probe outcome capture</name>
  <files>src/widget/WidgetController.ts, src/widget/widgetRegistry.ts, src/main.ts, tests/widget/vimReconfigure.test.ts</files>
  <read_first>
    - src/widget/WidgetController.ts (entire file, 485 LOC — Phase 19 baseline; especially the `buildExtensions` factory and constructor where `vim()` is currently injected unconditionally)
    - src/widget/widgetRegistry.ts (entire file, 124 LOC — needs `*values()` iterator addition matching the existing `flushAll` / `flushAllSync` / `destroyAll` shape at lines 67-119)
    - src/main.ts lines 870-1030 (where Phase 19's six event-listener registrations live; new `workspace.on('layout-change')` listener registers in the same block under `if (useInlineWidget) { ... }`)
    - .planning/phases/20-reconciliation-ux-action-row-section-protection/20-RESEARCH.md §"Pattern 6: Vim Live-Reconfigure (VIM-02) — Probe Procedure" (lines 531-611) — the full skeleton, the pass/fail criteria, and the layout-change recommendation
    - .planning/phases/20-reconciliation-ux-action-row-section-protection/20-PATTERNS.md §9 (WidgetController vimCompartment extension lines 568-588) and §5 (multiPaneCoordinator analog for the same `*values()` accessor pattern)
    - .planning/phases/20-reconciliation-ux-action-row-section-protection/20-CONTEXT.md L4 (vim live-reconfigure pre-accepted fallback) and "Claude's Discretion" (vim glitchy-pass = fail classification)
    - node_modules/obsidian/obsidian.d.ts line 7119 (`Workspace.on('layout-change')` since 0.9.20)
  </read_first>
  <behavior>
    - Test 1: Constructing a WidgetController with `getConfig('vimMode') === false` produces an EditorView whose `vimCompartment.get(view.state)` resolves to the empty extension array `[]`. PASS.
    - Test 2: Constructing a WidgetController with `getConfig('vimMode') === true` produces an EditorView whose extensions include `vim({status: true})` via the compartment. PASS.
    - Test 3: `ctl.reconfigureVim(true)` on a vim=OFF widget dispatches a Compartment.reconfigure effect AND mutates `mountedVimMode` to `true`. PASS.
    - Test 4: `ctl.reconfigureVim(false)` on a vim=ON widget dispatches a Compartment.reconfigure effect with the empty payload AND mutates `mountedVimMode` to `false`. PASS.
    - Test 5: `ctl.reconfigureVim(currentValue)` (no-op) does NOT dispatch — early return when `mountedVimMode === enabled`. PASS.
    - Test 6: After `reconfigureVim(true)`, the cursor head, scroll position, and history JSON of the view are unchanged from before the dispatch (Compartment.reconfigure preserves state — no rebuild). PASS.
    - Test 7: `widgetRegistry.values()` (new iterator) yields every registered controller; given two widgets registered, iteration produces both. PASS.
    - Test 8: Plugin-side `workspace.on('layout-change')` listener, when fired with a new `getConfig('vimMode')` value, calls `ctl.reconfigureVim(newValue)` on every controller in the registry. PASS (use a stub `app` with mock `vault.getConfig`).
  </behavior>
  <action>
    Step 1 — Add `*values()` iterator to `src/widget/widgetRegistry.ts`. Place it next to the existing `flushAll` / `flushAllSync` / `destroyAll` accessors. Signature: `*values(): IterableIterator<WidgetControllerLike>` yielding `this.map.values()`. Matches the existing iteration shape exactly; one-line change (per 20-PATTERNS §4 lines 304-312). DO NOT change the private `map` field name or visibility.

    Step 2 — Per-widget `vimCompartment` in `src/widget/WidgetController.ts`. Add a private field `private readonly vimCompartment = new Compartment();` (NOT module-singleton — each widget owns its own EditorView and Compartments are identity-keyed; module-singleton would dispatch to all widgets simultaneously which is correct for `languageCompartment` but NOT what we want for vim because the registry walk in Step 3 is the one fan-out). Add a private mutable field `private mountedVimMode: boolean;` initialized in the constructor by reading `getConfig('vimMode')` once via the existing pattern at WidgetController:264 (the v1.2 cast `(getConfig.call(plugin.app.vault, 'vimMode') as boolean | undefined) === true`). Wrap the existing `vim({status: true})` extension injection in `buildExtensions` with `this.vimCompartment.of(this.mountedVimMode ? vim({status: true}) : [])`. The previous unconditional `vim()` injection from Phase 19 becomes the conditional payload of the compartment.

    Step 3 — Add public method `reconfigureVim(enabled: boolean): void` to WidgetController per 20-RESEARCH lines 595-601: early-return when `this.mountedVimMode === enabled`; otherwise mutate `this.mountedVimMode = enabled` THEN dispatch `this.view.dispatch({ effects: this.vimCompartment.reconfigure(enabled ? vim({status: true}) : []) })`. No `userEvent` annotation needed — the widget's own EditorView has no section-protection extension installed; only the parent does.

    Step 4 — Plugin-side dispatcher in `src/main.ts`. **First**, extract a small helper to eliminate duplicate `(vault as { getConfig?: ... }).getConfig?.('vimMode')` cast sites: create `src/widget/vimMode.ts` (NEW, ~10 LOC) with single export `export function readVimModeFromVault(plugin: Plugin): boolean { return ((plugin.app.vault as { getConfig?: (k: string) => unknown }).getConfig?.('vimMode') as boolean | undefined) === true; }`. Update `WidgetController.ts:264` (the existing constructor read site) to call `readVimModeFromVault(this.plugin)` instead of duplicating the cast inline. **Then**, inside the existing `if (useInlineWidget) { ... }` block (lines 911-1019), add (placed alongside the existing `vault.on('modify')` etc. registerEvent calls): `this.registerEvent(this.app.workspace.on('layout-change', () => { const newVim = readVimModeFromVault(this); for (const ctl of this.widgetRegistry.values()) { ctl.reconfigureVim(newVim); } }));`. Cache the previous `vimMode` value at module/plugin scope IFF the per-controller early-return at Step 3 isn't sufficient (it IS sufficient — `reconfigureVim` no-ops when value unchanged — so the plugin-side cache is unnecessary). Wrap with try/catch only if `getConfig` is unavailable (test fixtures); otherwise let it bubble. **Type discipline:** the single helper file is the canonical cast site; future call sites MUST import it rather than re-cast.

    Step 5 — Dev-vault probe procedure (manual, recorded in 20-01-SUMMARY.md). Per 20-RESEARCH §"Probe Procedure" lines 540-556 + CONTEXT "Claude's Discretion" lines 88-90: open a real LeetCode note in the configured dev vault (`~/Documents/Obsidian Vault` per project skills MEMORY.md) with `useInlineWidget=ON` and `vimMode=OFF`; type some text; toggle Settings → Editor → Vim key bindings → ON; without reloading, position cursor in widget, press Esc; verify (a) cursor changes to block, (b) `j/k/l/h` move without inserting, (c) press `i`, verify insert mode (line cursor + characters insert), (d) toggle vim OFF, verify `j` inserts the character. Pass criteria: all six steps work without note reload AND without insert-mode glitches AND without losing cursor/scroll/undo across the dispatch. Fail criteria: ANY of (Esc doesn't enter normal mode / cursor stays line-shape / OFF doesn't release bindings / glitchy pass requires no-op keystroke / state lost on dispatch). Record the verbatim outcome in 20-01-SUMMARY.md as `## Probe Outcome: PASS` or `## Probe Outcome: FAIL — VIM-03 banner required at Phase 22`.

    Step 6 — Test file `tests/widget/vimReconfigure.test.ts`. Mock `obsidian` via `tests/helpers/obsidian-stub` (existing project pattern). Construct fake plugin + WidgetController with `getConfig('vimMode')` stub. Assert all 8 behavior cases above. For Test 8 (plugin-side dispatcher), construct two controllers, register both in a stub registry exposing `values()`, fire a synthetic `workspace.on('layout-change')` callback with mutated `getConfig` return value, assert `reconfigureVim` was called on both. Use `vi.spyOn` on the controller method.

    Step 7 — Existing `tests/widget/vimMount.test.ts` (Phase 19) must continue to pass — it asserts mount-time vim injection. Verify after the Compartment wrap in Step 2.

    Per L4, VIM-02, Q2 (REQUIREMENTS.md), CONTEXT "Claude's Discretion" vim glitchy-pass classification.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/widget/vimReconfigure.test.ts tests/widget/vimMount.test.ts tests/widget/widgetRegistry.test.ts --reporter=basic</automated>
  </verify>
  <done>
    `src/widget/WidgetController.ts` exposes a public `reconfigureVim(enabled: boolean): void` method; `vim({status: true})` injection in `buildExtensions` is wrapped in `vimCompartment.of(...)`; `src/widget/widgetRegistry.ts` exposes `*values(): IterableIterator<WidgetControllerLike>`; `src/main.ts` registers exactly one new `workspace.on('layout-change')` listener inside the `if (useInlineWidget)` block that iterates `widgetRegistry.values()` and calls `reconfigureVim`; `tests/widget/vimReconfigure.test.ts` exists and all 8 cases pass; the 20-01-SUMMARY.md probe-outcome section records PASS or FAIL verbatim per the dev-vault probe; `npx tsc --noEmit` exits 0; full test suite green via `npm test`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin → CM6 transactionFilter | Untrusted user keystrokes cross from CM6 into the changeFilter; the gate decides which transactions land in the doc |
| Plugin → Obsidian Settings (vimMode) | The vim mode flag is read from `getConfig('vimMode')` (undocumented Obsidian internal API) |
| Plugin → Plugin (`'leetcode.*'` userEvent bypass) | Plugin-internal CM6 dispatches with userEvent starting `leetcode.` bypass the protection extension's changeFilter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01-01 | Spoofing | `'leetcode.*'` userEvent bypass | accept | Documented carry-forward limitation (Phase 17 known): a malicious community plugin running on the same parent CM6 could dispatch a transaction with userEvent `'leetcode.evil'` to bypass the changeFilter. Phase 22 (PROTECT-03 / DELETE-08) removes this bypass entirely together with the v1.2 path. Phase 20 preserves it verbatim per CONTEXT D-protect-02 / L6 because the v1.2 path's chevron switch + Reset child dispatch still depend on it through Phase 21. |
| T-20-01-02 | Tampering | sectionProtectionExtension protected-ranges builder | mitigate | Forking strategy (D-protect-04) preserves every UAT-hardened scar verbatim. The acceptance gate is the v1.0 Phase 5.5 UAT regression rerun (Step 7) — 100+ existing test cases continue to pass against the new extension; new positive case asserts fence body is now editable (the explicit narrowing). |
| T-20-01-03 | Tampering | Mutually-exclusive registration | mitigate | The Phase 19 D-06 mutual-exclusion assert (`src/main.ts:885-890`) ensures `useInlineWidget` and `useNestedEditor` cannot both be ON simultaneously; if data.json is corrupt with both flags, useNestedEditor is forced OFF and a Notice fires. Plan 20-01 inherits this guard — only one of `buildSectionProtectionExtension` / `buildSectionLockExtension` can register. Read `useInlineWidget` ONCE at onload (matches lines 851-852 convention) so the gate cannot flip mid-session. |
| T-20-01-04 | Repudiation | Vim probe outcome | mitigate | The PASS/FAIL outcome is recorded in 20-01-SUMMARY.md verbatim under `## Probe Outcome:` so Phase 22 can mechanically decide whether to ship VIM-03 banner. No silent failures — if the probe fails, FAIL is documented and Phase 22 work increases. |
| T-20-01-05 | Information Disclosure | `getConfig('vimMode')` undocumented API | accept | The cast `(getConfig.call(plugin.app.vault, 'vimMode') as boolean \| undefined)` is the v1.2 carry-forward pattern; documented at WidgetController.ts:264 + childEditorFactory.ts:268. No new info disclosure; the value is a boolean flag, not user data. |
| T-20-01-06 | Denial of Service | layout-change listener fires for every Obsidian layout change (not just vim toggle) | mitigate | The `reconfigureVim` early-return at `mountedVimMode === enabled` is the per-controller no-op gate — fires only when the value actually changed. layout-change fires on EVERY pane focus / settings save; without the no-op gate, every layout change would dispatch a redundant Compartment.reconfigure to every widget (cheap but wasteful). Documented in Step 3 action. |
| T-20-01-07 | Elevation of Privilege | Compartment.reconfigure leaking vim internal state on toggle-OFF | accept | A6 (RESEARCH §Assumptions) — vim Compartment.reconfigure is empirically untested in `@replit/codemirror-vim@6.3.0`. If state leaks (e.g., key handler retained), the failure mode is "vim bindings still active when expected to be OFF" — captured as the probe FAIL criterion (Step 5) → Phase 22 VIM-03 banner. CONTEXT L4 pre-accepted the fallback. |
| T-20-01-SC | Tampering | npm/pip/cargo installs | mitigate | NONE — Phase 20 introduces ZERO new package dependencies. RESEARCH §"Package Legitimacy Audit" verified no new installs proposed. |
</threat_model>

<verification>
## Phase-level checks for Plan 20-01

1. `wc -l src/main/sectionProtectionExtension.ts` returns 350-400 (target ~370 LOC after surgical deletion).
2. `grep -c "userEvent\\b.*leetcode\\." src/main/sectionProtectionExtension.ts` returns ≥1 (verbatim bypass preserved per L6).
3. `grep -nE "closer\\.from|closerLockTo" src/main/sectionProtectionExtension.ts` returns no hits inside `computeLockedRanges`'s `'code'` branch (surgical deletion verified).
4. `grep -nE "expanded\\.push\\(Math\\.max\\(0," src/main/sectionProtectionExtension.ts` returns the boundary-fix block (UAT 2026-05-13 preservation).
5. Both `tests/main/sectionLockExtension.test.ts` AND `tests/main/sectionProtectionExtension.test.ts` pass via `npx vitest run`.
6. The remaining v1.0 Phase 5.5 cases (Problem body / Code heading + pocket / Techniques heading / boundary fix / `'leetcode.*'` bypass / malformed-note path) all pass against the new extension.
7. `src/widget/WidgetController.ts` `reconfigureVim` method exists and dispatches `vimCompartment.reconfigure(...)` per the locked skeleton.
8. `src/widget/widgetRegistry.ts` exports an iterator `*values()`.
9. `src/main.ts` registers the layout-change listener inside the `if (useInlineWidget)` block; lint clean.
10. `npx tsc --noEmit` exits 0; `npm test` exits 0 with full suite green (1906 → 1906+N where N = new test cases).
11. The dev-vault probe outcome (PASS / FAIL) is recorded in 20-01-SUMMARY.md.
</verification>

<success_criteria>
- New `sectionProtectionExtension.ts` exists; protects exactly `## Problem` body + `## Code` heading + blank-line pocket + `## Techniques` heading; fence opener-line content + closer + body all editable in source mode.
- v1.0 Phase 5.5 UAT regression cases pass byte-for-byte against the new extension.
- The `'leetcode.*'` userEvent bypass is preserved verbatim per L6 / D-protect-02; CLAUDE.md §Conventions paragraph 1 untouched.
- Mutually-exclusive registration: `useInlineWidget=ON` → `buildSectionProtectionExtension`; `useInlineWidget=OFF` → `buildSectionLockExtension` (unchanged v1.2 path).
- Per-widget `vimCompartment` swaps `vim() ↔ []` via `reconfigureVim` without rebuilding the EditorView; cursor + scroll + undo preserved across dispatch.
- Single plugin-side `workspace.on('layout-change')` listener iterates `widgetRegistry.values()` and calls `reconfigureVim` per widget.
- Dev-vault probe outcome documented in 20-01-SUMMARY.md (Phase 22 decides whether to ship VIM-03 banner based on this).
- Full test suite passes: `npm test` exits 0.
</success_criteria>

<output>
Create `.planning/phases/20-reconciliation-ux-action-row-section-protection/20-01-SUMMARY.md` when done with:
- File deltas (new + modified, with LOC counts)
- The verbatim probe outcome (`## Probe Outcome: PASS` or `## Probe Outcome: FAIL — VIM-03 banner required at Phase 22`)
- Test results (cases added, full-suite pass count delta)
- Carry-forward notes for Plan 20-02 (any test-file references the next plan needs)
</output>
</content>
</invoke>