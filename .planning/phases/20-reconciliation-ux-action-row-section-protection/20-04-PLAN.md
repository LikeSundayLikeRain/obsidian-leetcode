---
phase: 20-reconciliation-ux-action-row-section-protection
plan: 04
type: execute
wave: 4
depends_on:
  - 20-03
files_modified:
  - src/widget/themeListener.ts
  - src/widget/multiPaneCoordinator.ts
  - src/widget/WidgetController.ts
  - src/widget/LeetCodeFenceWidget.ts
  - src/widget/widgetRegistry.ts
  - src/main.ts
  - styles.css
  - tests/widget/themeListener.test.ts
  - tests/widget/multiPaneCoordinator.test.ts
autonomous: true
requirements:
  - THEME-04
tags:
  - theme-retheme
  - multi-pane
  - polish
must_haves:
  truths:
    - "A single global `app.workspace.on('css-change')` listener (verified at obsidian.d.ts:7137 in 1.12.3) iterates `widgetRegistry.values()` and calls `ctl.cssRetheme()` per widget — fan-out from one event to N widgets without remount"
    - "`cssRetheme()` calls `view.requestMeasure()` (CM6 reflow on next animation frame) — does NOT rebuild the EditorView; cursor + scroll + undo state preserved (the cascading CSS classes lc-nested-editor + HyperMD-codeblock + childEditorSemanticClasses already pick up the new theme's vars)"
    - "Single global `app.workspace.on('active-leaf-change')` listener walks the registry, finds peer widgets on the same file path, and calls `ctl.setGreyedOut(boolean)` based on whether each widget's containing pane is the active pane"
    - "`setGreyedOut(true)` sets the container attribute `data-pane-state='inactive'` (CSS reactively shows the overlay); `setGreyedOut(false)` sets `data-pane-state='active'` (overlay hidden)"
    - "The greyed-out overlay is a child of the widget container (NOT a CM6 child) with classes `.lc-takeover-overlay` and `.lc-takeover-cta`; click on the overlay or CTA promotes that pane and demotes the previously-active pane in the same animation frame"
    - "Multi-pane state changes are reversible — clicking the CTA on pane B greys pane A and activates pane B; clicking the CTA on pane A swaps back; the single-active-per-file invariant (L10) holds across all transitions"
    - "Multi-pane coordinator does NOT itself dispatch widget writes — it only flips a CSS attribute. Phase 19's debouncedWriter remains the single write channel; multi-pane is purely a focus / affordance layer"
    - "MutationObserver fallback for css-change is documented but NOT shipped (event verified to exist in 1.12.3 per RESEARCH §Pattern 7)"
  artifacts:
    - path: "src/widget/themeListener.ts"
      provides: "Single export `registerThemeListener(plugin)`; subscribes to `app.workspace.on('css-change')`; iterates `widgetRegistry.values()` and calls `ctl.cssRetheme()` per widget"
      exports: ["registerThemeListener"]
      min_lines: 15
    - path: "src/widget/multiPaneCoordinator.ts"
      provides: "Single export `registerMultiPaneCoordinator(plugin)`; subscribes to `app.workspace.on('active-leaf-change')`; walks registry to determine peer widgets on same file path; flips greyedOut state"
      exports: ["registerMultiPaneCoordinator"]
    - path: "src/widget/WidgetController.ts"
      provides: "cssRetheme(): void method; setGreyedOut(b: boolean): void method; greyedOut state field"
    - path: "src/widget/LeetCodeFenceWidget.ts"
      provides: "toDOM extension: conditionally append `.lc-takeover-overlay` child when `data-pane-state='inactive'`; click handler routes to coordinator promotion"
    - path: "src/main.ts"
      provides: "Calls registerThemeListener + registerMultiPaneCoordinator inside the `if (useInlineWidget)` block"
    - path: "styles.css"
      provides: "lc-takeover-overlay + lc-takeover-cta classes per UI-SPEC §3 (data-pane-state attribute selectors; opacity 0.55; hover ring; reduced-motion gating)"
    - path: "tests/widget/themeListener.test.ts"
      provides: "css-change event fires → view.requestMeasure called per registered widget"
    - path: "tests/widget/multiPaneCoordinator.test.ts"
      provides: "Two widgets on same file path; active-leaf-change → setGreyedOut(true) on inactive-pane widget; click on overlay → swap"
  key_links:
    - from: "app.workspace.on('css-change')"
      to: "WidgetController.cssRetheme → view.requestMeasure"
      via: "registerThemeListener iterates widgetRegistry.values()"
      pattern: "css-change.*requestMeasure"
    - from: "app.workspace.on('active-leaf-change')"
      to: "WidgetController.setGreyedOut"
      via: "registerMultiPaneCoordinator walks registry, finds peer widgets on same file path, flips state based on each widget's containing pane vs. active pane"
      pattern: "setGreyedOut|data-pane-state"
    - from: "Click on .lc-takeover-overlay or .lc-takeover-cta"
      to: "WidgetController.setGreyedOut(false) on this widget; setGreyedOut(true) on peer widget"
      via: "click handler installed inside toDOM (or mountLeetCodeWidget) when greyedOut=true"
      pattern: "lc-takeover-overlay|lc-takeover-cta"
---

<objective>
Land THEME-04 (live theme retheme on light/dark toggle / community theme swap) and the v1.3-baseline multi-pane "Take over" affordance (per CONTEXT L10 single-active-per-file). Theme retheme: a single global `app.workspace.on('css-change')` listener (verified at `obsidian.d.ts:7137`) iterates `widgetRegistry.values()` and calls `ctl.cssRetheme() → view.requestMeasure()` per widget — no EditorView rebuild; cursor + scroll + undo preserved (the cascading CSS classes already pick up the new theme's CSS variables). Multi-pane "Take over": a single global `app.workspace.on('active-leaf-change')` listener walks the registry, finds peer widgets on the same file path, flips each widget's `data-pane-state` attribute on its container — the active pane's widget is editable; peer panes' widgets show a greyed-out overlay + "Click to take over" CTA per UI-SPEC §3. Click on the overlay promotes that pane and demotes the previously-active pane in the same animation frame.

Purpose: The Day 4 dogfood checkpoint per D-plan-02 — visual + multi-pane edge cases handled cleanly. Closes the v1.3 widget UX loop so Phase 22 can flip `useInlineWidget` to default ON without UX regressions.

Output: New `src/widget/themeListener.ts` (~20 LOC), new `src/widget/multiPaneCoordinator.ts` (~80 LOC), extended `WidgetController` (`cssRetheme`, `setGreyedOut`), extended `LeetCodeFenceWidget` toDOM with overlay child, plugin onload registrations, styles.css multi-pane block per UI-SPEC §3, and two new test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-reconciliation-ux-action-row-section-protection/20-CONTEXT.md
@.planning/phases/20-reconciliation-ux-action-row-section-protection/20-RESEARCH.md
@.planning/phases/20-reconciliation-ux-action-row-section-protection/20-PATTERNS.md
@.planning/phases/20-reconciliation-ux-action-row-section-protection/20-UI-SPEC.md
@.planning/phases/20-reconciliation-ux-action-row-section-protection/20-VALIDATION.md
@.planning/phases/19-widget-foundation-one-way-sync/19-PHASE-SUMMARY.md
@.planning/phases/20-reconciliation-ux-action-row-section-protection/20-03-SUMMARY.md
@CLAUDE.md
@src/widget/widgetRegistry.ts
@src/widget/WidgetController.ts
@src/widget/LeetCodeFenceWidget.ts
@src/main/childEditorTheme.ts
@src/main/childEditorSemanticClasses.ts
@src/solve/ephemeralTabStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Theme listener + WidgetController.cssRetheme</name>
  <files>src/widget/themeListener.ts, src/widget/WidgetController.ts, src/main.ts, tests/widget/themeListener.test.ts</files>
  <read_first>
    - .planning/phases/20-reconciliation-ux-action-row-section-protection/20-RESEARCH.md §"Pattern 7: Live Theme Retheme (THEME-04)" (lines 614-649) — verified `app.workspace.on('css-change')` at obsidian.d.ts:7137 + verbatim listener skeleton + MutationObserver fallback (NOT shipped)
    - .planning/phases/20-reconciliation-ux-action-row-section-protection/20-PATTERNS.md §4 (`src/widget/themeListener.ts` analog at lines 263-317) — registerEvent + iterate-registry shape; the `*values()` accessor on WidgetRegistry was added in Plan 20-01 Task 2
    - .planning/phases/20-reconciliation-ux-action-row-section-protection/20-UI-SPEC.md §"Visual Layout Contracts §4" (lines 333-352) — manual smoke-test contract for the 4 theme cases (T1 light/dark; T2 Minimal swap; T3 Things swap; T4 reduced-motion); §"Definitions of Done — Surface 4" lines 480-483
    - .planning/phases/20-reconciliation-ux-action-row-section-protection/20-CONTEXT.md "Claude's Discretion" lines 88 (theme detection probe — css-change exists; MutationObserver fallback documented but not shipped)
    - src/main/childEditorTheme.ts (verifies cascading class discipline that makes requestMeasure sufficient)
    - src/main/childEditorSemanticClasses.ts (Lezer→CSS class mapping that cascades)
    - src/widget/widgetRegistry.ts (the `*values()` iterator added in Plan 20-01)
    - node_modules/obsidian/obsidian.d.ts line 7137 (`Workspace.on('css-change', cb): EventRef`)
  </read_first>
  <behavior>
    - Test 1: `registerThemeListener(plugin)` calls `plugin.registerEvent` with the result of `plugin.app.workspace.on('css-change', cb)` — verified by spy on `registerEvent`.
    - Test 2: When the registered callback fires, `widgetRegistry.values()` is iterated and `ctl.cssRetheme()` is called on every controller.
    - Test 3: `WidgetController.cssRetheme()` calls `this.view.requestMeasure()` exactly once and does NOT call any other view method (no rebuild).
    - Test 4: Two registered widgets both receive a single `cssRetheme()` call when one css-change event fires.
    - Test 5: Plugin onload (under `useInlineWidget=ON`) calls `registerThemeListener(this)` exactly once; under `useInlineWidget=OFF` does NOT (no widgets to retheme; the v1.2 path uses Obsidian's default).
  </behavior>
  <action>
    Step 1 — Create `src/widget/themeListener.ts` per 20-RESEARCH §"Pattern 7" lines 626-637 verbatim. Single export `registerThemeListener(plugin: Plugin & { widgetRegistry: WidgetRegistry }): void`. Body: `plugin.registerEvent(plugin.app.workspace.on('css-change', () => { for (const ctl of plugin.widgetRegistry.values()) { ctl.cssRetheme(); } }));`. Imports: `import { type Plugin } from 'obsidian';` `import type { WidgetRegistry } from './widgetRegistry';`. Target ~15-20 LOC including JSDoc. Add a JSDoc note documenting the MutationObserver fallback per RESEARCH lines 642-649 (NOT shipped — `css-change` is verified to exist in 1.12.3; defense-in-depth shim left in JSDoc as future-proofing reference).

    Step 2 — Add `cssRetheme(): void` method to `WidgetController` per 20-RESEARCH lines 631-635 + 20-PATTERNS §9 lines 600-605: body is exactly `this.view.requestMeasure();`. No rebuild, no DOM mutation, no extension swap. Document in JSDoc that the method relies on the cascading CSS classes (`lc-nested-editor` + `HyperMD-codeblock` + `childEditorSemanticClasses` Lezer→CSS-class outputs — all carried over from v1.2 per Phase 19 THEME-01..THEME-03) automatically picking up the new theme's `var(--code-keyword)` etc.

    Step 3 — Plugin onload registration in `src/main.ts`. Inside the existing `if (useInlineWidget) { ... }` block (lines 911-1019 — the same block where Plan 20-01 added the layout-change listener), add `registerThemeListener(this);` placed alongside the other registerEvent calls. Imports: `import { registerThemeListener } from './widget/themeListener';`.

    Step 4 — Test file `tests/widget/themeListener.test.ts`. Mock `obsidian` via existing test stub. Construct fake plugin with `widgetRegistry` exposing `values()`. Spy on `plugin.app.workspace.on` AND `plugin.registerEvent`. Call `registerThemeListener(plugin)`. Assert `workspace.on` called with `'css-change'` and the returned EventRef is passed to `registerEvent`. Extract the callback (mock the `on` to capture the cb) and invoke it; assert `cssRetheme` called per registered controller. For Test 3 (cssRetheme isolation), construct a real WidgetController stub with a mocked `view.requestMeasure` spy; assert no other view methods called.

    Step 5 — Manual UAT documentation in `20-04-SUMMARY.md` per UI-SPEC §"Visual Layout Contracts §4" lines 343-352. Test cases T1-T4: open dev-vault LeetCode note; toggle Light/Dark; verify widget body background flips within ~16ms; cursor stays at same line:col; scroll position unchanged; undo stack intact (Cmd-Z reverts last typed character, not the theme change). Repeat for community theme swap (Minimal → Things). Verify `prefers-reduced-motion` does not animate.

    Per THEME-04, RESEARCH §"Pattern 7", UI-SPEC §"Visual Layout Contracts §4".
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/widget/themeListener.test.ts --reporter=basic</automated>
  </verify>
  <done>
    `src/widget/themeListener.ts` exists; single export `registerThemeListener`; subscribes via `plugin.registerEvent(plugin.app.workspace.on('css-change', ...))`. `WidgetController.cssRetheme()` calls only `view.requestMeasure()` — no rebuild. `src/main.ts` registers the listener inside the `if (useInlineWidget)` block. `tests/widget/themeListener.test.ts` passes all 5 cases. Manual UAT cases T1-T4 documented in 20-04-SUMMARY.md as PASS. `npx tsc --noEmit` exits 0; full test suite green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multi-pane coordinator + setGreyedOut + LeetCodeFenceWidget overlay + styles.css</name>
  <files>src/widget/multiPaneCoordinator.ts, src/widget/WidgetController.ts, src/widget/LeetCodeFenceWidget.ts, src/widget/widgetRegistry.ts, src/main.ts, styles.css, tests/widget/multiPaneCoordinator.test.ts</files>
  <read_first>
    - .planning/phases/20-reconciliation-ux-action-row-section-protection/20-RESEARCH.md §"Pattern 8: Multi-Pane 'Take Over' Affordance" (lines 651-665) — three options enumerated; greyed-out + CTA is the recommendation locked
    - .planning/phases/20-reconciliation-ux-action-row-section-protection/20-PATTERNS.md §5 (`src/widget/multiPaneCoordinator.ts` lines 320-379) — analog from `src/solve/ephemeralTabStore.ts` + registry-walk shape + container `data-pane-state` toggle pattern
    - .planning/phases/20-reconciliation-ux-action-row-section-protection/20-PATTERNS.md §10 (LeetCodeFenceWidget toDOM extension lines 615-633 — conditional overlay child)
    - .planning/phases/20-reconciliation-ux-action-row-section-protection/20-PATTERNS.md §12 (styles.css lc-takeover-* additions lines 705-726)
    - .planning/phases/20-reconciliation-ux-action-row-section-protection/20-UI-SPEC.md §"Visual Layout Contracts §3" (lines 254-331) — locked greyed-out + CTA contract; DOM mount; CSS variables; state transition + race window; §"Copywriting Contract" line 131-132 ("Click to take over" + title attribute) ; §"Definitions of Done — Surface 3" lines 474-478
    - .planning/phases/20-reconciliation-ux-action-row-section-protection/20-CONTEXT.md L10 (single-active-per-file baseline); "Claude's Discretion" lines 90 (multi-pane Take Over UX — option 1 greyed-out + CTA recommended; options 2/3 rejected)
    - src/solve/ephemeralTabStore.ts (active-leaf-change reconcile pattern — registers two events, walks state, mutates per-item)
    - src/widget/widgetRegistry.ts (the `*values()` iterator added in Plan 20-01; consider adding `getAllByFilePath(path: string): WidgetController[]` accessor or filter inline)
    - src/widget/LeetCodeFenceWidget.ts (toDOM body — sibling-append discipline)
    - node_modules/obsidian/obsidian.d.ts (`Workspace.on('active-leaf-change')` + `Workspace.on('layout-change')`)
  </read_first>
  <behavior>
    - Coord Test 1: `registerMultiPaneCoordinator(plugin)` registers `workspace.on('active-leaf-change', cb)` via `plugin.registerEvent`.
    - Coord Test 2: When two WidgetControllers exist for the SAME file path (different containers in different `.workspace-leaf` ancestors), and active-leaf-change fires with leaf A as the active leaf: `setGreyedOut(false)` is called on the controller whose container is inside leaf A; `setGreyedOut(true)` on the controller in leaf B.
    - Coord Test 3: When a single WidgetController exists for a file path (no peer), `setGreyedOut(false)` is called on it (no peer = no greying).
    - Coord Test 4: When the active leaf doesn't contain ANY widget for the file path (e.g., user focused a non-LC note), no widget state changes (the listener is a no-op for this case — accept-the-no-op per UI-SPEC §3 line 322).
    - setGreyedOut Test 1: `setGreyedOut(true)` sets `container.setAttribute('data-pane-state', 'inactive')`.
    - setGreyedOut Test 2: `setGreyedOut(false)` sets `container.setAttribute('data-pane-state', 'active')`.
    - setGreyedOut Test 3: When transitioning from inactive→active, the overlay child is removed (or hidden via CSS — the attribute toggle alone is sufficient because CSS has `[data-pane-state="inactive"] > .lc-takeover-overlay` selector; the overlay child's display is controlled by attribute).
    - Overlay Test 1: When `data-pane-state="inactive"` AND the overlay child is mounted, click on the overlay invokes the coordinator's `promote(thisWidget)` method which calls `setGreyedOut(false)` on this widget AND `setGreyedOut(true)` on every other widget for the same file path.
    - styles.css: `.lc-nested-editor[data-pane-state="inactive"] > .lc-takeover-overlay` rule exists with `position: absolute; inset: 0; opacity: 0.55; pointer-events: auto`.
  </behavior>
  <action>
    Step 1 — Create `src/widget/multiPaneCoordinator.ts` per 20-PATTERNS §5 lines 348-376 + 20-RESEARCH §"Pattern 8". Single export `registerMultiPaneCoordinator(plugin: Plugin & { widgetRegistry: WidgetRegistry }): void` PLUS a sibling export `promote(plugin, widget)` (or attach `promote` to the coordinator's closure scope). Body for the active-leaf listener: import `MarkdownView` from obsidian; `plugin.registerEvent(plugin.app.workspace.on('active-leaf-change', () => { reconcileFocus(plugin); }));` AND ALSO register `workspace.on('layout-change')` for pane creation/teardown (per `src/solve/ephemeralTabStore.ts` precedent which registers BOTH events). The `reconcileFocus` body resolves `active = plugin.app.workspace.getActiveViewOfType(MarkdownView)`; if `active?.file == null`, every widget gets `setGreyedOut(false)` (no active LC pane = no contention); otherwise iterate `widgetRegistry.values()`: for each widget where `ctl.file.path === active.file.path`, determine pane affiliation via `pane = ctl.container.closest('.workspace-leaf')` AND `activePaneEl = active.containerEl.closest('.workspace-leaf')`; set `ctl.setGreyedOut(pane !== activePaneEl)`. For widgets on different file paths, `setGreyedOut(false)` (they never have peer contention).

    Step 2 — Add `setGreyedOut(b: boolean): void` to WidgetController per 20-PATTERNS §9 lines 605-610. Body: `this.greyedOut = b; this.container.setAttribute('data-pane-state', b ? 'inactive' : 'active');`. Add private/public field `greyedOut: boolean = false;` initialized to false. The CSS selector at Step 6 reactively shows/hides the overlay based on the attribute — no DOM mount/unmount required HERE if the overlay is always rendered (pointer-events controls interactivity). Decision: ALWAYS mount the overlay child as a sibling of `.cm-editor` inside `mountLeetCodeWidget`, but with CSS `display: none` when data-pane-state="active" — simpler than dynamic mount/unmount and matches UI-SPEC §3 lines 286-307. Container default attribute is `data-pane-state="active"` (set at mount time by `mountLeetCodeWidget` per Phase 19 baseline).

    Step 3 — Mount the overlay child inside `mountLeetCodeWidget` (in `src/widget/WidgetController.ts`). After the action row mount (Plan 20-02 Step 2), append: `const overlay = container.createDiv({ cls: 'lc-takeover-overlay', attr: { role: 'button', tabindex: '0' } });` `const cta = overlay.createEl('button', { cls: 'lc-takeover-cta', text: 'Click to take over' });` `cta.setAttribute('title', 'This file is being edited in another pane. Click to take over and edit here.');` `overlay.addEventListener('click', (e) => { e.preventDefault(); e.stopPropagation(); promoteWidget(plugin, ctl); });` Where `promoteWidget(plugin, ctl)` is exported from `multiPaneCoordinator.ts` (Step 1) and: (a) iterates `widgetRegistry.values()` filtering by `c.file.path === ctl.file.path`; (b) calls `setGreyedOut(c !== ctl)` per widget; (c) optionally focuses the editor view via `ctl.view.focus()`. Note: the overlay div is mounted UNCONDITIONALLY at widget creation; the CSS at Step 6 hides it when `data-pane-state="active"` (default). Skip overlay mount entirely for embed-context widgets (per Phase 19 embedDetect, same gate as action row).

    Step 4 — `LeetCodeFenceWidget.toDOM` does NOT need direct edit IF the overlay is mounted in `mountLeetCodeWidget` (which `toDOM` already calls). Verify the call graph and ensure the overlay arrives "for free" via the factory — same approach as Plan 20-02's action row.

    Step 5 — `widgetRegistry.ts`: the `*values()` accessor was added in Plan 20-01. If a `getAllByFilePath(path: string): WidgetController[]` accessor would simplify the coordinator, add it as a one-line helper: `getAllByFilePath(path: string): WidgetController[] { return Array.from(this.map.values()).filter(c => c.file.path === path); }`. Optional — the inline filter via `*values()` is also acceptable.

    Step 6 — styles.css block per 20-PATTERNS §12 lines 711-726 + UI-SPEC §3 lines 286-319 verbatim. Add to existing styles.css (do NOT collide with Plan 20-03's `lc-conflict-*` block):
      ```
      .lc-nested-editor { position: relative; }
      .lc-nested-editor[data-pane-state="active"] > .lc-takeover-overlay { display: none; }
      .lc-nested-editor[data-pane-state="inactive"] > .lc-takeover-overlay {
        position: absolute; inset: 0;
        background: var(--background-secondary); opacity: 0.55;
        display: flex; align-items: center; justify-content: center;
        cursor: pointer; border-radius: 4px; pointer-events: auto; z-index: 5;
      }
      .lc-nested-editor[data-pane-state="inactive"] > .lc-takeover-overlay:hover {
        border: 1px solid var(--interactive-accent);
      }
      .lc-takeover-cta {
        padding: 12px 16px; border-radius: 6px;
        background: var(--background-primary); color: var(--text-normal);
        border: 1px solid var(--background-modifier-border);
        font-size: 13px; font-weight: 500;
      }
      .lc-takeover-cta:hover { border-color: var(--interactive-accent); }
      ```
      Use only Obsidian CSS variables (no raw hex).

    Step 7 — Plugin onload registration in `src/main.ts`. Inside the existing `if (useInlineWidget)` block, add `registerMultiPaneCoordinator(this);` placed alongside other registerEvent calls. Imports: `import { registerMultiPaneCoordinator } from './widget/multiPaneCoordinator';`.

    Step 8 — Test file `tests/widget/multiPaneCoordinator.test.ts`. Construct fake plugin with widget registry; construct two stub WidgetControllers with containers in different fake `.workspace-leaf` ancestors (use `document.createElement('div')` with class manipulation). Mock `getActiveViewOfType` to return a stub MarkdownView whose `containerEl.closest('.workspace-leaf')` returns leaf A. Fire the registered callback; assert `setGreyedOut(false)` on widget-A and `setGreyedOut(true)` on widget-B. Then mock active leaf to leaf B; fire callback again; assert reverse. Test the click → promote path: simulate click on widget-B's overlay (assuming it's mounted via the regular construction path — may need to mock `mountLeetCodeWidget` or test the overlay mount separately). Cover Coord Tests 1-4 + setGreyedOut Tests 1-3 + Overlay Test 1.

    Step 9 — Manual UAT documentation in `20-04-SUMMARY.md` per UI-SPEC §"Definitions of Done — Surface 3" + VALIDATION §"Manual-Only Verifications" line 73. Open same LeetCode note in two split panes; verify pane B widget greys + shows "Click to take over" CTA when pane A is active; click the CTA; verify pane A demotes (greys with CTA) and pane B promotes (editable).

    Per THEME-04 (theme listener) + L10 (multi-pane single-active baseline) + UI-SPEC §3.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/widget/multiPaneCoordinator.test.ts tests/widget/themeListener.test.ts --reporter=basic</automated>
  </verify>
  <done>
    `src/widget/multiPaneCoordinator.ts` exists; subscribes to `active-leaf-change` (and `layout-change` for pane create/teardown); walks `widgetRegistry.values()`; flips `setGreyedOut` based on pane affiliation. `WidgetController.setGreyedOut` toggles `data-pane-state` attribute. `mountLeetCodeWidget` mounts the overlay child (hidden by default via CSS) and wires its click handler to the coordinator's `promoteWidget`. styles.css `lc-takeover-*` block uses Obsidian CSS variables only. Plugin onload registers both `registerThemeListener` AND `registerMultiPaneCoordinator` inside the `if (useInlineWidget)` block. `tests/widget/multiPaneCoordinator.test.ts` passes; manual UAT for two-pane case PASS recorded in 20-04-SUMMARY.md. Full test suite green via `npm test`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workspace event → Plugin (focus tracking) | Active-leaf-change fires on every pane focus; multi-pane coordinator must serialize correctly |
| Click event → Plugin (promote pane) | User click on overlay CTA dispatches the promote logic |
| Plugin → Plugin (multi-pane race) | Two panes on same file simultaneously dispatching writes — plugin must enforce single-active baseline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04-01 | Tampering | Multi-pane race: two widgets dispatching writes simultaneously | mitigate | L10 single-active-per-file baseline. The coordinator ensures only ONE widget has `data-pane-state="active"` at any moment. The greyed-out widget's overlay has `pointer-events: auto` capturing all clicks BEFORE they reach the underlying CM6 editor — keystrokes cannot reach an inactive widget's view. The Phase 19 debouncedWriter remains the single write channel; the coordinator only flips visual state, never directly dispatches writes. |
| T-20-04-02 | Denial of Service | active-leaf-change fires on every focus event; coordinator runs on each fire | mitigate | The reconcileFocus body is O(N) where N = widget count (typically 1-3 widgets in a vault session). The setGreyedOut method is a no-op when state already matches (early-return on `this.greyedOut === b`). Bounded cost. |
| T-20-04-03 | DOM XSS | Overlay CTA + title text rendered | mitigate | All text via `createEl(..., { text })` / `setText` / `textContent` per CLAUDE.md no-innerHTML rule. The CTA copy is hardcoded ("Click to take over") — not user-controlled. |
| T-20-04-04 | Spoofing | Hostile theme could render fake "Click to take over" overlay | accept | Inherited limitation — community theme review prevents arbitrary DOM injection. The overlay class names (`lc-takeover-overlay`, `lc-takeover-cta`) are scoped under `.lc-nested-editor[data-pane-state="inactive"]` so a theme cannot manufacture them outside this context. |
| T-20-04-05 | Tampering | css-change event fires for non-theme reasons (e.g., Obsidian sidebar drawer toggle) | accept | Per RESEARCH §"Pattern 7" lines 638-640: `requestMeasure` is idempotent — calling it on a non-theme css-change is benign (CM6 schedules a remeasure on next animation frame and the layout is unchanged so no visible flicker). |
| T-20-04-06 | Information Disclosure | Greyed-out overlay shows underlying widget content at 55% opacity | accept | The user owns this content — same data they would see in any other pane. No new disclosure surface. |
| T-20-04-07 | Tampering | Click race between overlay click and active-leaf-change | mitigate | The promoteWidget call is synchronous (mutates state in same animation frame per UI-SPEC §3 line 329); workspace.on('active-leaf-change') fires after the focus shift — by the time the listener runs, promoteWidget has already set the state correctly so the listener is a no-op (early-return on matching state). Race window is ~16ms; acceptable per RESEARCH Open Question 4. |
| T-20-04-SC | Tampering | npm/pip/cargo installs | mitigate | NONE — Phase 20 introduces ZERO new package dependencies. |
</threat_model>

<verification>
## Phase-level checks for Plan 20-04

1. `wc -l src/widget/themeListener.ts` returns 15-30 (target ~20).
2. `wc -l src/widget/multiPaneCoordinator.ts` returns 50-100 (target ~80).
3. `grep -nE "css-change" src/widget/themeListener.ts` returns the verified event subscription.
4. `grep -nE "active-leaf-change" src/widget/multiPaneCoordinator.ts` returns the listener registration.
5. `grep -nE "data-pane-state" src/widget/WidgetController.ts styles.css` returns the attribute toggle + CSS selectors.
6. `grep -nE "innerHTML" src/widget/multiPaneCoordinator.ts src/widget/themeListener.ts` returns 0 hits.
7. `tests/widget/themeListener.test.ts` + `tests/widget/multiPaneCoordinator.test.ts` pass via `npx vitest run`.
8. Full test suite green via `npm test` (1906+N passing).
9. Manual UAT cases documented in 20-04-SUMMARY.md: T1-T4 theme retheme PASS; multi-pane two-pane CTA promotion PASS.
10. styles.css uses only Obsidian CSS variables — no raw hex except documented existing exceptions.
11. `npx tsc --noEmit` exits 0; `npm run build` exits 0; bundle size delta <10KB raw.
</verification>

<success_criteria>
- Theme retheme: light/dark toggle + community theme swap retheme widget colors live within ~16ms; cursor + scroll + undo state preserved (no EditorView rebuild).
- Multi-pane: opening same file in two panes shows pane A editable + pane B greyed-out with "Click to take over" CTA; clicking the CTA promotes pane B in the same animation frame; reversible.
- Single-active-per-file invariant (L10) holds across all transitions.
- Both new event listeners registered exclusively under `useInlineWidget=ON` (the v1.2 path is unaffected).
- All DOM text via `textContent` / `createEl(..., { text })` — zero `innerHTML` usages.
- styles.css uses only Obsidian CSS variables; reduced-motion gating documented.
- Full test suite green; new test files cover THEME-04 + multi-pane coordinator.
</success_criteria>

<output>
Create `.planning/phases/20-reconciliation-ux-action-row-section-protection/20-04-SUMMARY.md` when done with:
- File deltas (new + modified, with LOC counts)
- Manual UAT cases T1-T4 (theme retheme) PASS/FAIL outcomes
- Manual UAT for two-pane "Take over" CTA PASS/FAIL outcome
- Test results (cases added, full-suite pass count delta)
- Bundle size delta from `npm run build` — verify within milestone headroom (~92KB remaining)
- Phase 20 closeout pointer: this is the final plan in Phase 20; the next /gsd-execute-phase action after 20-04 lands is to run /gsd-verify-work to gate on the full Phase 20 acceptance criteria from CONTEXT/ROADMAP.
</output>
</content>
</invoke>