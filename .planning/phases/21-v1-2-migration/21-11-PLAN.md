---
phase: 21-v1-2-migration
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widget/liveModeViewPlugin.ts
  - src/widget/liveModeBannerStateField.ts
  - tests/widget/liveModeBannerStateField.test.ts
  - tests/widget/livePreviewUnmount.test.ts
autonomous: false
gap_closure: true
requirements: [MIGRATE-BANNER-LP-01]
tags: [migration, banner, live-preview, statefield, cm6, gap-closure, phase-21]
must_haves:
  truths:
    - "Opening a v1.2-shaped note in Live Preview with `useInlineWidget=ON` AND `autoMigrateOnOpen=OFF` mounts the migration banner WITHOUT throwing the CodeMirror 6 RangeError 'Decorations that replace line breaks may not be specified via plugins'."
    - "Opening a v1.2-shaped note in Live Preview with `useInlineWidget=ON` AND `autoMigrateOnOpen=ON` mounts the AutoMigratingBannerWidget WITHOUT throwing the CM6 RangeError, and the banner unmounts on the post-migration update cycle when the v1.3 widget takes over."
    - "Existing v1.3 widget mount path (`fence.kind === 'leetcode-solve'`) still functions — the LeetCodeFenceWidget at lines 213-232 either (a) is verified safe-as-is and unchanged, or (b) is migrated to a SEPARATE permanent v1.3 StateField that does NOT carry the PHASE_22_DELETE_WITH_V1_2_PATH marker."
    - "Existing `EditorView.atomicRanges` Facet contribution still works (parent cursor cannot enter the widget range)."
    - "No console error or warning emitted in either mode during legacy-fence open."
    - "The legacy banner StateField is tagged with the PHASE_22_DELETE_WITH_V1_2_PATH header marker so Phase 22 cleanup can mechanically retire the v1.2 path without touching the v1.3 widget StateField."
  artifacts:
    - path: "src/widget/liveModeBannerStateField.ts"
      provides: "StateField-hosted Decoration.replace for legacy fences (and conditionally a SEPARATE StateField for the v1.3 widget if Task 1 scopes 'fix both')"
      contains: "legacyBannerStateField"
    - path: "src/widget/liveModeViewPlugin.ts"
      provides: "ViewPlugin restricted to v1.3 widget; legacy decoration moved to StateField"
      contains: "leetCodeFenceViewPlugin"
    - path: ".planning/phases/21-v1-2-migration/21-11-INVESTIGATION.md"
      provides: "Investigation results for LeetCodeFenceWidget susceptibility"
      contains: "LeetCodeFenceWidget"
  key_links:
    - from: "leetCodeFenceViewPlugin Extension array"
      to: "legacyBannerStateField"
      via: "Combined Extension export"
      pattern: "legacyBannerStateField"
---

<objective>
Close UAT Gap 4 (Test 4b, severity=major): "Live Preview emits CM6 RangeError 'Decorations that replace line breaks may not be specified via plugins' when migrating a v1.2 note via the command palette." Per 21-HUMAN-UAT.md lines 140-174 (root cause B), `src/widget/liveModeViewPlugin.ts:160-208` builds `Decoration.replace` at lines 176-182 spanning the FULL multi-line legacy fence (`legacyFrom = view.state.doc.line(fence.openerLine).from` to `legacyTo = view.state.doc.line(fence.closerLine).to`). The decoration is supplied via the ViewPlugin's `decorations: v => v.decorations` field at line 386, NOT a StateField. CM6's contract: line-break-spanning `Decoration.replace` MUST come from a StateField (transaction-time), not a ViewPlugin (build-time). The error text matches verbatim.

Cross-check from 21-HUMAN-UAT.md lines 154 + diagnosis_hint line 60-69: `LeetCodeFenceWidget` mount path at lines 213-232 has the SAME construct shape (Decoration.replace spanning multi-line fence inside the same ViewPlugin) but works in production. Plan MUST investigate whether the existing v1.3 widget construct is ALSO susceptible (and only escapes the error because of some pre-fold or intermediate-state difference) — if so, the StateField migration applies to BOTH widget paths.

Purpose: Migrate the AutoMigratingBannerWidget Decoration.replace from the ViewPlugin's `decorations` field into a StateField (transaction-time), so the line-break-spanning decoration is provided by an Extension shape that CM6's contract permits. Investigate v1.3 widget susceptibility BEFORE implementing; scope the fix to either just the legacy banner OR both widget paths based on findings.

Output: New module `src/widget/liveModeBannerStateField.ts` exporting one or two CM6 StateField extensions (legacy banner; conditionally a sibling permanent v1.3 StateField when Task 1 reports "fix both"). Existing `leetCodeFenceViewPlugin` modified to ship as a combined Extension (ViewPlugin + new StateField(s), in the correct order). Investigation document `21-11-INVESTIGATION.md` records the v1.3 susceptibility findings.

PHASE 22 RETIREMENT MARKER: When Task 1's scope decision is "fix both", the legacy-banner StateField and the permanent v1.3 widget StateField MUST be authored as TWO SEPARATE StateField definitions (NOT a single StateField that branches on `fence.kind`). This separation lets Phase 22 cleanup grep for the `PHASE_22_DELETE_WITH_V1_2_PATH` marker on the legacy StateField and delete it mechanically without surgical edits to the v1.3 path. See Task 2 action for the exact split shape and marker text.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-v1-2-migration/21-HUMAN-UAT.md
@.planning/phases/21-v1-2-migration/21-CONTEXT.md
@.planning/phases/21-v1-2-migration/21-PATTERNS.md
@src/widget/liveModeViewPlugin.ts
@src/widget/legacyFenceBanner.ts
@src/widget/LeetCodeFenceWidget.ts
@src/widget/fenceLocator.ts
@src/widget/fenceMigrator.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate LeetCodeFenceWidget susceptibility — does the v1.3 widget at liveModeViewPlugin.ts:213-232 also violate the CM6 line-break Decoration.replace + ViewPlugin contract?</name>
  <files>.planning/phases/21-v1-2-migration/21-11-INVESTIGATION.md</files>
  <action>
    Investigate WHY the existing LeetCodeFenceWidget construct at liveModeViewPlugin.ts:213-232 builds a Decoration.replace spanning multi-line ranges (`from = doc.line(fence.openerLine).from`, `to = doc.line(fence.closerLine).to`) inside a ViewPlugin's `decorations` field WITHOUT throwing the same RangeError that the legacy banner triggers.

    Investigation steps:
    (1) Construct a minimal repro in a test file: build a multi-line ` ```leetcode-solve ` fence in jsdom + a real CM6 EditorState, run `leetCodeFenceViewPlugin` against it, and observe whether the RangeError fires. Use the same EditorView fixture pattern as `tests/widget/atomicRanges.test.ts` or `tests/widget/livePreviewUnmount.test.ts`.
    (2) If the RangeError fires for `LeetCodeFenceWidget` too: this means the existing v1.3 path is latently broken — it either escapes in production via undocumented timing (Obsidian's editor pre-folds the leetcode-solve fence before the ViewPlugin sees it as multi-line) or is silently swallowed by Obsidian's plugin error boundary. Document this and scope the fix to BOTH widget paths.
    (3) If the RangeError does NOT fire for `LeetCodeFenceWidget`: identify the difference. Hypotheses to test:
        - (h1) The `replaceWith` widget property differs — banner uses `Decoration.replace({widget})` while v1.3 also uses `Decoration.replace({widget})`. CHECK both signatures byte-for-byte.
        - (h2) The fence-locator pre-folds the v1.3 fence's lines somehow (Obsidian's editor folding extension may interact with `\`\`\`leetcode-solve` because the tag is registered as a code-block processor). When the lines are folded, the "line-break-spanning" condition does not apply.
        - (h3) Some Obsidian internal Markdown extension treats `leetcode-solve` as block-level whereas `java`/`python` are inline-eligible — the rendering pipeline differs.
        - (h4) The order of extensions in the registered Extension array matters — if leetcode-solve is processed by Obsidian's CommonMark BEFORE the ViewPlugin runs, the multi-line range is collapsed to a single block-line by the time decorations are built.
    (4) Document findings in `.planning/phases/21-v1-2-migration/21-11-INVESTIGATION.md` with: (a) repro outcome, (b) confirmed cause if v1.3 escapes, (c) recommendation: "fix legacy only" OR "fix both".
    (5) Record the SCOPE DECISION at the end of the investigation document. This decision drives Task 2's scope.

    Threat-model + write-path hygiene: this is a read-only investigation. NO write paths.

    The investigation document must contain:
    - Repro test code snippet (or path to a temporary test file).
    - Observed error messages + stack traces.
    - For each hypothesis: tested = yes/no, result = supported/refuted/inconclusive.
    - Conclusion: "v1.3 IS / IS NOT also susceptible".
    - Scope decision: "Task 2 fixes legacy banner only" OR "Task 2 fixes both legacy banner AND v1.3 widget".
    - Rationale referencing CM6 documentation or Obsidian source.
  </action>
  <verify>
    <automated>test -f .planning/phases/21-v1-2-migration/21-11-INVESTIGATION.md && grep -E '^Scope decision:' .planning/phases/21-v1-2-migration/21-11-INVESTIGATION.md</automated>
  </verify>
  <done>
    Investigation document exists. Each of the four hypotheses is marked tested + result. A clear scope decision is recorded ("legacy only" or "both"). Task 2 has unambiguous direction.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate AutoMigratingBannerWidget Decoration.replace into a StateField (TDD: failing CM6-RangeError-absent test first); under "fix both" scope, separate v1.3 into a SECOND StateField (no marker) so Phase 22 deletes only the v1.2 StateField</name>
  <files>src/widget/liveModeBannerStateField.ts, src/widget/liveModeViewPlugin.ts, tests/widget/liveModeBannerStateField.test.ts, tests/widget/livePreviewUnmount.test.ts</files>
  <behavior>
    - Test "Multi-line legacy fence: StateField-provided Decoration.replace mounts WITHOUT CM6 RangeError": construct a multi-line ` ```java ... ``` ` fence inside a `## Code` section in a CM6 EditorState. Wire the new `legacyBannerStateField` extension with the plugin host. Build the EditorView. Assert: NO RangeError thrown during construction. Assert: the AutoMigratingBannerWidget DOM is mounted at the fence range. (NB: this is the test that exactly reproduces the bug from Test 4b and proves it closed.)
    - Test "Multi-line legacy fence: under autoMigrateOnOpen=OFF the manual-prompt banner mounts WITHOUT RangeError": same as above but with autoMigrateOnOpen=false; assert the manual-prompt banner DOM mounts. (Live Preview parallel of Plan 21-10's Reading-mode coverage.)
    - Test "v1.3 fence still mounts via the existing path": construct a leetcode-solve fence; assert LeetCodeFenceWidget DOM mounts (no regression).
    - Test "atomicRanges contribution still applies to the leetcode-solve range": cursor positioned mid-fence; verify `EditorView.atomicRanges` excludes the inner range (Phase 19 C-05 invariant).
    - Test "atomicRanges contribution applies to the legacy fence range too": same as above but for a legacy fence — the new StateField must contribute its RangeSet to the SAME atomicRanges Facet so the parent cursor cannot enter the legacy banner range.
    - Test "StateField recomputes on docChanged": migrate the fence (rewrite opener to ```leetcode-solve); assert the StateField's RangeSet updates and the widget DOM transitions from banner to v1.3 widget.
    - Test "StateField rebuilds on lc-slug toggle (metadataCache change indirectly via doc replace)": replace the entire doc with a non-LC note (no lc-slug); assert the StateField's RangeSet collapses to empty.
    - If Task 1 scoped to "fix both": add parallel tests for the v1.3 widget path moved into a SECOND StateField (`leetCodeWidgetStateField`).
    - Header-marker lock test (only when scope = "fix both"): read `src/widget/liveModeBannerStateField.ts` source and assert (a) the file's first non-empty line does NOT contain `PHASE_22_DELETE_WITH_V1_2_PATH` (the file as a whole is mixed v1.2/v1.3 and must NOT carry a file-wide marker); (b) the line block IMMEDIATELY preceding `export const legacyBannerStateField` contains `PHASE_22_DELETE_WITH_V1_2_PATH`; (c) the line block immediately preceding `export const leetCodeWidgetStateField` does NOT contain `PHASE_22_DELETE_WITH_V1_2_PATH`. This locks the marker placement so Phase 22 grep finds only the v1.2 StateField.
    - Header-marker lock test (only when scope = "legacy only"): assert the file's first non-empty line contains `PHASE_22_DELETE_WITH_V1_2_PATH` (the entire module is v1.2 scaffolding when the v1.3 path stays in the ViewPlugin, so the file-wide marker is appropriate).

    Implementation:
    - New module `src/widget/liveModeBannerStateField.ts`:
      ```
      export const legacyBannerStateField = StateField.define<DecorationSet>({
        create(state) { return buildLegacyBannerDecorations(state, plugin); },
        update(value, tr) {
          if (!tr.docChanged) return value.map(tr.changes);
          return buildLegacyBannerDecorations(tr.state, plugin);
        },
        provide: (f) => [
          EditorView.decorations.from(f),
          EditorView.atomicRanges.of((view) => view.state.field(f, false) ?? Decoration.none),
        ],
      });
      ```
      where `buildLegacyBannerDecorations(state, plugin)` is extracted from the legacy-kind branch of the existing `buildLeetCodeFenceRanges` (lines 160-208). It returns a DecorationSet covering only legacy fences (kind === 'legacy'); when no legacy fence is present, returns `Decoration.none`.
    - Plugin host is captured via a factory: `export function makeLegacyBannerExtension(plugin: PluginHost): Extension { return legacyBannerStateField }` — but StateField.define captures `plugin` by closure inside `create` and `update`. Use the standard CM6 pattern (close over plugin in the factory, NOT pass it via Facet). Reference Phase 19 PATTERNS.md lines 116-194 (template ViewPlugin shape) and existing `selfWriteSuppression` extension factories for closure idiom.

    Modify `src/widget/liveModeViewPlugin.ts`:
      - REMOVE the legacy-kind branch from `buildLeetCodeFenceRanges` (lines 160-208 — keep the migration trigger fire-and-forget logic but move its decoration emission out).
      - Branch on Task 1's scope decision (see action for the exact split shape):
        - Scope = "legacy only": keep the leetcode-solve branch (lines 213-232) IN the ViewPlugin unchanged.
        - Scope = "fix both": ALSO move the leetcode-solve branch into a SECOND, SEPARATE StateField (`leetCodeWidgetStateField`) authored alongside `legacyBannerStateField` in the same module, but WITHOUT the PHASE_22_DELETE_WITH_V1_2_PATH header marker. The two StateFields each provide their own decoration set; both are added to the combined Extension array. Do NOT collapse them into a single StateField that branches on `fence.kind` — separation is what makes Phase 22 cleanup mechanical.
      - The migration fire-and-forget block (lines 184-205) MUST stay reachable. Place it in the ViewPlugin (still synchronous, still observable in update()) — it does NOT emit a decoration; it only triggers `migrateLegacyFenceIfNeeded`. The banner decoration is now emitted by the StateField via `buildLegacyBannerDecorations`.
      - The `leetCodeFenceViewPlugin` factory must now return a combined Extension. Order matters — the StateField(s) must be provided BEFORE or in-array-with the ViewPlugin so CM6's transaction processing applies StateField updates first.
        - Scope = "legacy only": `[legacyBannerStateField, ViewPlugin.define(...)]`.
        - Scope = "fix both": `[legacyBannerStateField, leetCodeWidgetStateField, ViewPlugin.define(...)]`. The ViewPlugin retains the migration fire-and-forget logic but emits NO decorations.
    - autoMigrateOnOpen=OFF + isMigrationCandidate(noteText, fm) → `buildLegacyBannerDecorations` emits the manual-prompt banner widget. autoMigrateOnOpen=ON + legacy fence → emits the AutoMigratingBannerWidget. Both branches produce a StateField-hosted decoration that does NOT trigger the RangeError.

    Threat-model + write-path hygiene: NO new write paths. The migration write path remains the same (`migrateLegacyFenceIfNeeded` from the ViewPlugin's update fire-and-forget block, banner click handler from `mountLegacyFenceBanner`). The Phase 17 D-05 canonical write-path pattern is preserved (banner click → migrator → `app.vault.process` + `processFrontMatter` — both vault-layer primitives that bypass CM6 by design). Phase 05.5 userEvent annotations: NO new CM6 dispatches in this plan.

    Tests in `tests/widget/liveModeBannerStateField.test.ts` (new file) + extend `tests/widget/livePreviewUnmount.test.ts` if regression checks belong there. Use jsdom + real `@codemirror/state` + `@codemirror/view` (these are external in production esbuild; in test they are real npm deps). Reference `tests/widget/atomicRanges.test.ts` for the test fixture shape.
  </behavior>
  <action>
    Author the StateField module and apply the conditional split based on Task 1's scope decision.

    PHASE 22 HEADER MARKER PLACEMENT (MANDATORY — varies by Task 1 scope):

    **(a) When Task 1 scope = "legacy only"** — module hosts ONLY the legacy banner StateField:
    The file `src/widget/liveModeBannerStateField.ts` MUST begin (line 1) with the verbatim block comment, before any imports:

      ```
      // PHASE_22_DELETE_WITH_V1_2_PATH — this module is part of the v1.2 → v1.3 migration scaffolding and must be deleted mechanically when Phase 22 retires the v1.2 read path.
      ```

    The `buildLegacyBannerDecorations` helper, the `legacyBannerStateField` export, and the entire module are deletable as a unit. The ViewPlugin in `src/widget/liveModeViewPlugin.ts` retains the leetcode-solve branch (v1.3 widget) AS-IS — no marker needed there because the v1.3 path is permanent.

    Combined Extension shape: `export function leetCodeFenceViewPlugin(plugin) { return [legacyBannerStateField, ViewPlugin.define(...)]; }`.

    **(b) When Task 1 scope = "fix both"** — module hosts TWO separate StateFields:
    The file `src/widget/liveModeBannerStateField.ts` MUST NOT carry a file-wide PHASE_22_DELETE_WITH_V1_2_PATH header (the file mixes v1.2 scaffolding and the permanent v1.3 path).

    Author TWO SEPARATE StateField definitions in the same module file. Place the marker comment IMMEDIATELY ABOVE the legacy export only:

      ```
      // PHASE_22_DELETE_WITH_V1_2_PATH — legacyBannerStateField is v1.2 scaffolding for the migration banner. Phase 22 must delete this StateField, its build helper, and the entry in the combined Extension array below. The sibling leetCodeWidgetStateField is the permanent v1.3 path and MUST be preserved.
      export const legacyBannerStateField = StateField.define<DecorationSet>({
        create(state) { return buildLegacyBannerDecorations(state, plugin); },
        update(value, tr) {
          if (!tr.docChanged) return value.map(tr.changes);
          return buildLegacyBannerDecorations(tr.state, plugin);
        },
        provide: (f) => [
          EditorView.decorations.from(f),
          EditorView.atomicRanges.of((view) => view.state.field(f, false) ?? Decoration.none),
        ],
      });

      // (No marker — permanent v1.3 path.)
      export const leetCodeWidgetStateField = StateField.define<DecorationSet>({
        create(state) { return buildLeetCodeWidgetDecorations(state, plugin); },
        update(value, tr) {
          if (!tr.docChanged) return value.map(tr.changes);
          return buildLeetCodeWidgetDecorations(tr.state, plugin);
        },
        provide: (f) => [
          EditorView.decorations.from(f),
          EditorView.atomicRanges.of((view) => view.state.field(f, false) ?? Decoration.none),
        ],
      });
      ```

    Each StateField contributes its OWN DecorationSet to `EditorView.decorations` (CM6 unions them via Facet) AND its OWN RangeSet to `EditorView.atomicRanges`. The legacy StateField returns `Decoration.none` for any non-legacy fence; the v1.3 StateField returns `Decoration.none` for any non-leetcode-solve fence. The two StateFields are MUTUALLY EXCLUSIVE per fence (no range overlap possible — a single fence is either kind === 'legacy' or kind === 'leetcode-solve', never both).

    Combined Extension shape: `export function leetCodeFenceViewPlugin(plugin) { return [legacyBannerStateField, leetCodeWidgetStateField, ViewPlugin.define(...)]; }`. The ViewPlugin's `decorations` field is REMOVED (or set to a no-op returning `Decoration.none`); the ViewPlugin retains ONLY the migration fire-and-forget logic in `update()`.

    Phase 22 cleanup procedure (informational — guides why the split matters): `grep PHASE_22_DELETE_WITH_V1_2_PATH src/widget/liveModeBannerStateField.ts` lands directly on the `legacyBannerStateField` export block. The cleanup script deletes from the marker comment through the `});` of the StateField definition, plus the `legacyBannerStateField,` entry in the combined Extension array, plus the `buildLegacyBannerDecorations` helper. The `leetCodeWidgetStateField` is untouched. If the two were instead a single StateField branching on `fence.kind`, Phase 22 would need surgical edits inside one StateField's `create`/`update` bodies — the exact error-prone scenario the checker called out.

    Common to both scopes:
    - Build helpers (`buildLegacyBannerDecorations`, conditionally `buildLeetCodeWidgetDecorations`) are extracted from the existing `buildLeetCodeFenceRanges` body in `src/widget/liveModeViewPlugin.ts` (lines 160-208 for legacy; 213-232 for leetcode-solve under "fix both").
    - Plugin host is captured by closure inside the factory; no Facet is used.
    - The fire-and-forget migration trigger stays in the ViewPlugin's `update()` method.
    - Add tests in `tests/widget/liveModeBannerStateField.test.ts` + extend `tests/widget/livePreviewUnmount.test.ts` per the behavior block above.
    - Add the header-marker lock test described in the behavior block (its assertions branch on Task 1's scope).

    Threat-model + write-path hygiene: same as the behavior block — no new write paths; banner click + migration write paths are unchanged from Plan 21-01.
  </action>
  <verify>
    <automated>npm test -- tests/widget/liveModeBannerStateField.test.ts tests/widget/livePreviewUnmount.test.ts tests/widget/atomicRanges.test.ts --run && npm run build && grep -c PHASE_22_DELETE_WITH_V1_2_PATH src/widget/liveModeBannerStateField.ts</automated>
  </verify>
  <done>
    All new + existing CM6 tests pass. Build is clean. The CM6 RangeError "Decorations that replace line breaks may not be specified via plugins" no longer fires for legacy fences in Live Preview. Under scope = "legacy only", the v1.3 widget path remains in the ViewPlugin unchanged and the StateField module begins with the file-wide PHASE_22_DELETE_WITH_V1_2_PATH header. Under scope = "fix both", the module exports TWO separate StateFields — `legacyBannerStateField` (PHASE_22_DELETE_WITH_V1_2_PATH marker block-commented immediately above its export) and `leetCodeWidgetStateField` (no marker, permanent v1.3 path) — and the combined Extension array places both StateFields before the ViewPlugin. The atomicRanges Facet contribution still excludes the parent cursor from BOTH legacy banner range AND v1.3 widget range. The header-marker lock test passes the scope-appropriate placement assertion.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live UAT regression — Live Preview banner + migrate command without console RangeError</name>
  <what-built>
    Plan 21-11 Task 2 migrated the AutoMigratingBannerWidget Decoration.replace from the ViewPlugin into a StateField. Live Preview banner mount on a v1.2 fence should no longer emit the CM6 RangeError.
  </what-built>
  <how-to-verify>
    1. Settings: `useInlineWidget=ON`, `autoMigrateOnOpen=OFF`. Open the dev console (Cmd-Option-I) BEFORE doing anything.
    2. Open a v1.2-shaped fixture (`9-palindrome-number.md` per UAT Test 4b) in Live Preview mode.
    3. Expected: legacy migration banner mounts (manual-prompt copy + [Migrate now] button). NO console error/warning. NO RangeError.
    4. Run command palette → "LeetCode: Migrate current note". Expected: file rewritten on disk to ` ```leetcode-solve `; v1.3 widget mounts; NO RangeError.
    5. Toggle settings → `autoMigrateOnOpen=ON`. Re-open a fresh v1.2 fixture. Expected: AutoMigratingBannerWidget mounts briefly (~10–50ms), migration completes, v1.3 widget takes over. NO RangeError.
    6. Type into the v1.3 widget after migration. Expected: cursor stays inside; no parent-cursor leak (atomicRanges still functions).
    7. (If Task 1 scoped to "fix both") open an already-v1.3 note. Expected: v1.3 widget mounts; NO RangeError. (This regression-tests the v1.3 path migration.)
    8. Console verification: scroll back through entire console output. Expected: ZERO occurrences of the substring "Decorations that replace line breaks".
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 expectations match (including console clean of the RangeError text). If any fail, describe the divergence and any console-error stack traces.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CM6 transaction → StateField | StateField update is transaction-time; CM6 contract permits line-break-spanning Decoration.replace |
| StateField → EditorView.decorations Facet | provide hook publishes the DecorationSet |
| StateField → EditorView.atomicRanges Facet | provide hook publishes the same RangeSet to atomicRanges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-11-01 | Tampering | Decoration ordering between StateField + ViewPlugin (or between two sibling StateFields under "fix both") | mitigate | Combined Extension array places StateField(s) first; CM6 documented contract: when multiple Extensions publish to EditorView.decorations, all are merged via Facet aggregation. The legacy banner StateField publishes only legacy-fence decorations; the v1.3 StateField (under "fix both") OR the ViewPlugin (under "legacy only") publishes only v1.3-widget decorations. NO range overlap possible (legacy and v1.3 are mutually exclusive per fence.kind). |
| T-21-11-02 | Denial of Service | StateField rebuilds on every docChanged | accept | Same cost profile as the existing ViewPlugin's update() — buildLegacyBannerDecorations is the same function body, now invoked from a different host. No additional CPU. Under "fix both", the cost doubles (two StateFields rebuild per docChanged) but each StateField returns Decoration.none early when its fence.kind is absent — empirically negligible. |
| T-21-11-03 | Tampering | atomicRanges drift between StateField + ViewPlugin (or between two sibling StateFields) | mitigate | Each StateField's `provide` hook contributes to atomicRanges via `EditorView.atomicRanges.of((view) => view.state.field(f, false) ?? Decoration.none)`. The ViewPlugin's atomicRanges contribution remains intact under "legacy only". Same RangeSet shape (DecorationSet) on every contributor; CM6 unions them. |
| T-21-11-04 | Information Disclosure | Investigation document leaks Obsidian-internal details | accept | Investigation findings reference CM6 + Obsidian public APIs only. No private data. |
| T-21-11-05 | Tampering | Phase 22 cleanup misses the legacy StateField | mitigate | Under "fix both", the legacy StateField carries the PHASE_22_DELETE_WITH_V1_2_PATH marker block-commented immediately above its export; under "legacy only" the entire module file carries the file-wide marker on line 1. The header-marker lock test asserts the marker placement matches Task 1's scope decision so a future edit cannot silently drop it. The sibling v1.3 StateField (under "fix both") explicitly does NOT carry the marker, ensuring Phase 22's grep returns ONLY the v1.2 path. |
</threat_model>

<verification>
- `npm test -- tests/widget/liveModeBannerStateField.test.ts tests/widget/livePreviewUnmount.test.ts tests/widget/atomicRanges.test.ts --run` exits 0.
- `npm run build` exits 0.
- `grep -c PHASE_22_DELETE_WITH_V1_2_PATH src/widget/liveModeBannerStateField.ts` returns ≥ 1 under either scope decision.
- Live UAT regression (Task 3 checkpoint): all 8 expectations satisfied.
- 21-HUMAN-UAT.md Test 4b `result: issue` resolves to `result: pass` (no RangeError on migrate-command path).
- Investigation document `21-11-INVESTIGATION.md` exists and records the v1.3 susceptibility finding.
</verification>

<success_criteria>
- The CM6 RangeError "Decorations that replace line breaks may not be specified via plugins" no longer fires under any combination of (Reading mode, Live Preview) × (autoMigrateOnOpen ON/OFF) × (v1.2 fence, v1.3 fence).
- The legacy banner mounts in Live Preview when autoMigrateOnOpen=OFF on a v1.2 fixture (parallels Plan 21-10's Reading-mode coverage).
- The AutoMigratingBannerWidget mounts briefly under autoMigrateOnOpen=ON and gracefully unmounts when the v1.3 widget takes over post-migration.
- The atomicRanges Facet contribution still applies to BOTH the legacy fence range AND the v1.3 widget range.
- The investigation document records WHY the v1.3 widget either escaped the bug in production or was equally susceptible.
- Phase 22 retirement marker is correctly placed: under scope = "fix both", the module exports two distinct StateFields with the v1.2-only one tagged with `PHASE_22_DELETE_WITH_V1_2_PATH` immediately above its export and the v1.3 StateField untagged; under scope = "legacy only", the module's first non-empty line carries the file-wide marker.
- Plan 21-11 is wave-parallel with Plans 21-08, 21-10 (Wave 1) and runs before Plan 21-09 (Wave 2). Plan 21-09 was REVISED (per checker fix) to declare `depends_on: ["21-11"]` so 21-11's restructuring of `src/widget/liveModeViewPlugin.ts` lands first; 21-09 then layers its `repairInFlight` guard atop the leetcode-solve branch of the restructured file. This avoids a three-way merge hazard.
</success_criteria>

<output>
Create `.planning/phases/21-v1-2-migration/21-11-SUMMARY.md` when done.
</output>
