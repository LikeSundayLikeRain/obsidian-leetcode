---
phase: 08.1-streaming-transport-fix-bedrock-provider
verified: 2026-05-17T01:15:00Z
status: human_needed
score: 17/17
overrides_applied: 0
human_verification:
  - test: "Real streaming UAT against Anthropic or Ollama in live Obsidian"
    expected: "AI Debug modal shows tokens arriving progressively (not a single 'Thinking...' block) — TIER 1 native window.fetch() is live"
    why_human: "Cannot start Obsidian in CI; TIER 1 requires a real renderer + real AI provider endpoint responding with SSE"
  - test: "Bedrock default-chain UAT — configure AWS Bedrock in Settings, select 'Default credential chain', run Test Connection"
    expected: "Settings sub-form shows Region + Model ID + Auth dropdown (no secret rows in default-chain mode); Test Connection probe returns ok:true with a valid AWS environment present"
    why_human: "Requires real AWS credentials in environment, real Bedrock regional endpoint, and live Obsidian Settings UI"
  - test: "Bedrock explicit auth method UAT — switch to 'Explicit access keys', enter key/secret, verify mode-switch back to 'default-chain' does NOT clear the entered keys"
    expected: "Switching auth mode only changes which rows render; previously entered secrets survive the mode-switch (Pitfall 10 invariant)"
    why_human: "UI state invariant across dropdown changes cannot be verified by grep; requires live Settings interaction"
  - test: "Disclosure modal shows correct Bedrock region URL"
    expected: "First AI Debug call with provider=bedrock shows 'Active provider: AWS Bedrock — https://bedrock-runtime.{user-region}.amazonaws.com' with the configured region substituted"
    why_human: "Modal rendering requires a live Obsidian renderer and a real first-call trigger"
---

# Phase 08.1: Streaming Transport Fix + Bedrock Provider — Verification Report

**Phase Goal:** Close two URGENT carry-overs from Phase 08 dogfood — (1) restore real token-by-token streaming to AI Debug (current eager-probe always falls through to buffered because `electron.net.fetch` is unavailable in Obsidian 1.10+ renderer), and (2) add AWS Bedrock as a first-class AI provider so the user can run AI Debug / Review against Bedrock-hosted Anthropic models without a litellm/BAG proxy.

**Verified:** 2026-05-17T01:15:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | AIClient.invokeStream tries native window.fetch FIRST with credentials:'omit', then obsidianFetch('stream'), then buffered (3-tier ladder) | VERIFIED | `src/ai/AIClient.ts:277-311` — TIER 1 block (`window.fetch` + `credentials:'omit'` at line 286-287), TIER 2 block (obsidianFetch('stream') at line 302), TIER 3 buffered (obsidianFetch('request') at line 315) |
| 2 | AbortController cascade preserved across the new tier | VERIFIED | `src/ai/AIClient.ts:243-249` — abortController minted before tier ladder; `tests/ai/AIClient.invokeStream.abort.test.ts:72,107` — two real `it()` tests cover pre-abort and post-return cascade |
| 3 | scripts/check-no-fetch-in-lc.sh exists, passes; wired into npm prelint | VERIFIED | File exists at `scripts/check-no-fetch-in-lc.sh`; `bash scripts/check-no-fetch-in-lc.sh` exits 0; `package.json:9` `"prelint": "npm run check:lc-isolation"` chains to `"check:lc-isolation": "bash scripts/check-no-obsidianfetch-in-lc.sh && bash scripts/check-no-fetch-in-lc.sh"` |
| 4 | tests/ai/lc-no-fetch-leakage.test.ts exists with real it() (not it.todo) | VERIFIED | `tests/ai/lc-no-fetch-leakage.test.ts:63` — `it('No file under LC-side directories invokes native fetch()')` — zero `it.todo` |
| 5 | src/ai/providers/bedrock.ts exports all 4 helpers (createBedrockModel, probeBedrock, streamBedrock, invokeBedrockBuffered) | VERIFIED | Lines 46, 86, 114, 133 in `src/ai/providers/bedrock.ts` |
| 6 | resolveAdapter dispatches case 'bedrock'; Bedrock helpers re-exported from providers/index.ts | VERIFIED | `src/ai/providers/index.ts:205` — `case 'bedrock'`; lines 45-48 and 70-73 re-export all 4 helpers; `case 'bedrock'` returns `{ probe: probeBedrock, streamInvoke: streamBedrock, bufferedInvoke: invokeBedrockBuffered }` |
| 7 | AIProvider union widened with 'bedrock'; prettyName extended; BedrockProviderConfig type defined | VERIFIED | `src/ai/types.ts:23` — union includes `'bedrock'`; line 148 — `case 'bedrock': return 'AWS Bedrock'`; `export interface BedrockProviderConfig` confirmed (grep count=1) |
| 8 | SettingsStore extended: DEFAULT_PROVIDER_CONFIGS.bedrock; sanitizeBedrockProviderConfig; VALID_AI_PROVIDERS includes 'bedrock' | VERIFIED | `src/settings/SettingsStore.ts:188` (bedrock entry), `:393-400` (VALID_AI_PROVIDERS includes 'bedrock'), `:458` (sanitizeBedrockProviderConfig function), `:593-598` (load() hydration) |
| 9 | SettingsTab renderAIProviderForm has 'bedrock' branch with Region + Model ID + 4-mode auth dropdown + conditional secret fields; active-provider dropdown gains 'AWS Bedrock' | VERIFIED | `src/settings/SettingsTab.ts:400` — `case 'bedrock'`; `:259` — `addOption('bedrock', 'AWS Bedrock')`; grep confirms all 4 addOption auth mode calls; 3 `bcfg.authMethod ===` conditional guards for non-default modes; `this.display()` called on auth-mode change |
| 10 | awsCredentials.ts INI parser exists; uses nodeRequire shim, never literal require() | VERIFIED | `src/ai/awsCredentials.ts` exists; exports `resolveAwsCredentials`; grep for bare `require(` at non-comment, non-nodeRequire lines returns empty; comment at line 10-13 confirms nodeRequire shim pattern mirroring BrowserWindowLogin.ts:12-22 |
| 11 | displayBaseUrl.ts substitutes region into Bedrock base URL | VERIFIED | `src/ai/displayBaseUrl.ts:34` — `return \`https://bedrock-runtime.${region}.amazonaws.com\`` inside `case 'bedrock'` |
| 12 | disclosure.ts uses getDisplayBaseUrl helper (no DISCLOSURE_BASE_COPY mutation) | VERIFIED | `src/ai/disclosure.ts:34` imports getDisplayBaseUrl; line 168 uses `getDisplayBaseUrl(this.provider, this.cfg)`; grep for `DISCLOSURE_BASE_COPY.willSend.push` and `DISCLOSURE_BASE_COPY.willSend[` both return zero lines |
| 13 | Logger REDACT regex + SECRET_VALUE_PATTERN extended for AWS/Bedrock secret fields | VERIFIED | `src/shared/logger.ts:29` — REDACT includes `accesskeyid|secretaccesskey|aws_access_key_id|aws_secret_access_key|aws_session_token|aws_bearer_token_bedrock|bedrockapikey|sso_?profile`; line 89 — SECRET_VALUE_PATTERN includes same field names |
| 14 | package.json pins @ai-sdk/amazon-bedrock@4.0.107 EXACT; does NOT contain @aws-sdk/credential-providers | VERIFIED | `package.json:34` — `"@ai-sdk/amazon-bedrock": "4.0.107"` (no caret); node -e confirm: bedrock=4.0.107, credential-providers=ABSENT |
| 15 | README ## Network usage section enumerates Bedrock regional endpoint | VERIFIED | `README.md:75` — `**AWS Bedrock**: \`https://bedrock-runtime.{region}.amazonaws.com\`` |
| 16 | Bundle stays under 1.2 MB ceiling | VERIFIED | `npm run check:bundle-size` — main.js 1124196 bytes (1097.8 KB); BUNDLE CHECK OK (soft warn at >1080000, hard gate at 1.2 MB = 1228800 bytes; 104 KB headroom) |
| 17 | npm test (full suite) green | VERIFIED | 155 passed / 1 skipped (156 files); 1159 passed / 3 skipped (1162 tests); exit 0 |

**Score:** 17/17 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/ai/AIClient.ts` | invokeStream with TIER 1 native-fetch primary + credentials:'omit' | VERIFIED | Lines 277-298 TIER 1 block; line 286 credentials:'omit' |
| `scripts/check-no-fetch-in-lc.sh` | Shell grep gate; exits 0 on current tree | VERIFIED | File exists, executable, contains correct pattern, exits 0 |
| `tests/ai/AIClient.invokeStream.fetch-primary.test.ts` | Real it() tests for TIER 1 success | VERIFIED | 2 real `it()` tests; zero `it.todo` |
| `tests/ai/AIClient.invokeStream.cors-fallback.test.ts` | Real it() tests for TIER 1->2, TIER 1+2->3 | VERIFIED | 2 real `it()` tests; zero `it.todo` |
| `tests/ai/AIClient.invokeStream.abort.test.ts` | Real it() tests for abort cascade | VERIFIED | 2 real `it()` tests; zero `it.todo` |
| `tests/ai/lc-no-fetch-leakage.test.ts` | Runtime fs-walk isolation regression | VERIFIED | Real `it()` at line 63; zero `it.todo` |
| `src/ai/providers/bedrock.ts` | 4 helpers; all 4 auth cases; probeBedrock never-throws | VERIFIED | 4 exports; lines 52/56/61/68 cover all auth cases; catch block returns `{ok:false, errorMessage}` |
| `src/ai/awsCredentials.ts` | INI parser with nodeRequire shim | VERIFIED | Exports `resolveAwsCredentials`; nodeRequire pattern; no literal require() |
| `src/ai/displayBaseUrl.ts` | getDisplayBaseUrl with Bedrock region substitution | VERIFIED | Exports `getDisplayBaseUrl`; line 34 returns bedrock-runtime URL |
| `src/ai/providers/index.ts` | case 'bedrock' + barrel re-exports | VERIFIED | Line 205 case; lines 45-48 and 70-73 re-exports |
| `src/ai/types.ts` | AIProvider union + prettyName + BedrockProviderConfig | VERIFIED | Line 23 union; line 148 prettyName; BedrockProviderConfig interface exported |
| `src/settings/SettingsStore.ts` | DEFAULT_PROVIDER_CONFIGS.bedrock; sanitizeBedrockProviderConfig; VALID_AI_PROVIDERS | VERIFIED | All 3 present at expected lines |
| `src/settings/SettingsTab.ts` | case 'bedrock' branch; addOption('bedrock') | VERIFIED | Lines 400 and 259 respectively |
| `src/ai/disclosure.ts` | getDisplayBaseUrl import + usage; no DISCLOSURE_BASE_COPY mutation | VERIFIED | Line 34 import; line 168 usage; S6 invariant holds |
| `src/shared/logger.ts` | REDACT + SECRET_VALUE_PATTERN extended | VERIFIED | Line 29 REDACT; line 89 SECRET_VALUE_PATTERN |
| `package.json` | @ai-sdk/amazon-bedrock@4.0.107 exact | VERIFIED | Line 34; no caret |
| `README.md` | ## Network usage Bedrock entry | VERIFIED | Line 75 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/ai/AIClient.ts` | `window.fetch` | TIER 1 try block with credentials:'omit' | VERIFIED | Line 287 `return window.fetch(input, safeInit)` with credentials:'omit' at line 286 |
| `src/ai/AIClient.ts` | `obsidianFetch('stream')` | TIER 2 fall-through | VERIFIED | Line 302 `const fetcher = obsidianFetch('stream')` |
| `src/ai/AIClient.ts` | `obsidianFetch('request')` | TIER 3 buffered floor | VERIFIED | Line 315 `const fetcher = obsidianFetch('request')` |
| `scripts/check-no-fetch-in-lc.sh` | LC directories | grep over 7 dirs | VERIFIED | `grep -rnE "$PATTERN" src/api/ src/auth/ src/browse/ src/notes/ src/solve/ src/graph/ src/preview/` |
| `src/ai/providers/index.ts` | `src/ai/providers/bedrock.ts` | case 'bedrock' switch branch | VERIFIED | Line 205-211; returns probe/streamInvoke/bufferedInvoke |
| `src/ai/providers/bedrock.ts` | `src/ai/awsCredentials.ts` | default-chain + sso-profile auth modes | VERIFIED | Line 53 default-chain; line 64 sso-profile call |
| `src/ai/disclosure.ts` | `src/ai/displayBaseUrl.ts` | getDisplayBaseUrl call for baseUrl rendering | VERIFIED | Line 34 import; line 168 usage |
| `src/shared/logger.ts` | Bedrock secret field names | REDACT + SECRET_VALUE_PATTERN alternation | VERIFIED | Both regexes include all 6+ Bedrock field name patterns |
| `src/settings/SettingsStore.ts` | BedrockProviderConfig invariant | sanitizeBedrockProviderConfig shape-guard | VERIFIED | Function at line 458; preserves all 4 secret fields per Pitfall 10 contract |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `src/ai/AIClient.ts:invokeStream` | `result` (StreamTextResult) | `adapter.streamInvoke(req.prompt, abortController.signal)` → `streamBedrock` / `streamAnthropic` → `@ai-sdk` `streamText()` | Yes — real streaming text from provider | FLOWING |
| `src/settings/SettingsTab.ts` Bedrock branch | `bcfg` (BedrockProviderConfig) | `this.plugin.settings.getProviderConfig('bedrock')` → `sanitizeBedrockProviderConfig` → hydrated from `data.json` | Yes — real user config | FLOWING |
| `src/ai/disclosure.ts` | `baseUrlText` | `getDisplayBaseUrl(this.provider, this.cfg)` → `cfg.region` from BedrockProviderConfig | Yes — real region string from settings | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| TIER 1 native fetch in invokeStream | `grep -n 'window\.fetch' src/ai/AIClient.ts` | line 287: `return window.fetch(input, safeInit)` | PASS |
| credentials:'omit' on TIER 1 | `grep -n "credentials: 'omit'" src/ai/AIClient.ts` | line 286 | PASS |
| Shell gate exits 0 | `bash scripts/check-no-fetch-in-lc.sh; echo $?` | 0 | PASS |
| Native fetch not in LC paths | `grep -rn 'window\.fetch\|globalThis\.fetch' src/ \| grep -v 'src/ai/'` | empty | PASS |
| Bedrock dep exact pin | `node -e "console.log(require('./package.json').dependencies['@ai-sdk/amazon-bedrock'])"` | 4.0.107 | PASS |
| @aws-sdk/credential-providers absent | node check | ABSENT | PASS |
| Full test suite | `npm test -- --run` | 1159 passed / 1162 total; exit 0 | PASS |
| Build clean | `npm run build` | exit 0; no type errors | PASS |
| Bundle under 1.2 MB ceiling | `npm run check:bundle-size` | 1097.8 KB; BUNDLE CHECK OK | PASS |
| Lint (including prelint gates) | `npm run lint` | 0 errors, 1 pre-existing warning (`DetailCacheEntry` unused in main.ts — not introduced by this phase) | PASS |

### Probe Execution

| Probe | Command | Result | Status |
|-------|---------|--------|--------|
| `scripts/check-no-fetch-in-lc.sh` | `bash scripts/check-no-fetch-in-lc.sh` | exit 0 | PASS |
| `scripts/check-no-obsidianfetch-in-lc.sh` | run via `npm run lint` prelint chain | exit 0 (both gates green) | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| AIPROV-04 | 08.1-01, 08.1-02 | Carry-over: streaming transport — restore real token-by-token streaming | SATISFIED | TIER 1 native window.fetch() in AIClient.invokeStream:277; all invokeStream tests pass |
| AIPROV-05 | 08.1-01 | Preserved: native fetch confined to src/ai/ via prelint+fs-walk gates | SATISFIED | check-no-fetch-in-lc.sh wired into prelint via check:lc-isolation; lc-no-fetch-leakage.test.ts passes; grep of src/ confirms native fetch only in src/ai/AIClient.ts |
| AIPROV-02 | 08.1-02 | Extended: Bedrock secret fields use masked inputs + logger redaction | SATISFIED | SettingsTab type='password' inputs for access-keys/sso-profile/api-key modes; logger REDACT and SECRET_VALUE_PATTERN extended with all Bedrock field names |
| AIPROV-FUT-01 | 08.1-02 | Forward-port: native Bedrock via @ai-sdk/amazon-bedrock | SATISFIED | bedrock.ts adapter exists; resolveAdapter dispatches 'bedrock'; @ai-sdk/amazon-bedrock@4.0.107 installed (no SigV4 hand-roll); Settings UI complete |

Note: AIPROV-04 and AIPROV-05 are marked Complete in REQUIREMENTS.md (Phase 07) — Phase 08.1 extends/repairs their implementation without changing the requirement status. AIPROV-FUT-01 was listed under "Future Requirements" (deferred from v1.1); this phase forward-ports it into delivery.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/main.ts` | 27 | `'DetailCacheEntry' is defined but never used` (lint warning) | Info | Pre-existing warning; not introduced by Phase 08.1; no `TBD/FIXME/XXX` debt markers found in any phase-modified file |

No `TBD`, `FIXME`, or `XXX` markers found in files modified by this phase.

### Human Verification Required

#### 1. Real Token-by-Token Streaming UAT

**Test:** In a real Obsidian instance with a live AI provider (Anthropic API key or local Ollama), trigger AI Debug on a problem note. Watch the modal.

**Expected:** Tokens appear progressively as they arrive — the modal's content area fills incrementally rather than showing a "Thinking..." indicator followed by a single block of text. This confirms TIER 1 native window.fetch() is the active transport.

**Why human:** Cannot launch Obsidian renderer in CI. TIER 1 requires a real Electron renderer, a real AI provider endpoint responding with SSE, and the specific contextIsolation:true environment where TIER 2 (electron.net.fetch) is blocked.

#### 2. Bedrock Default-Chain Connection Test

**Test:** Open Settings > AI Provider, select "AWS Bedrock" from the dropdown. Verify: (a) sub-form shows Region (default us-east-1), Model ID, Auth method dropdown, and a helper text row (no secret fields in default-chain mode). (b) Set a valid region. (c) With AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY in the environment (or ~/.aws/credentials [default]), click "Test connection".

**Expected:** ok:true notice with the model responding. The modal for the first-call disclosure should show "AWS Bedrock — https://bedrock-runtime.{region}.amazonaws.com" with the configured region.

**Why human:** Requires real AWS credentials, a live Bedrock endpoint, and Obsidian Settings UI rendered in a real instance.

#### 3. Bedrock Auth-Mode Switch Preserves Secrets (Pitfall 10)

**Test:** Select "Explicit access keys", enter a fake accessKeyId and secretAccessKey. Switch auth dropdown to "Default credential chain". Switch back to "Explicit access keys".

**Expected:** The previously entered accessKeyId and secretAccessKey values are still present in the fields (not cleared by the mode switch). Only the rendered rows changed, not the stored values.

**Why human:** UI state persistence across React-style re-renders (`this.display()` calls) cannot be verified by static analysis; requires live Settings interaction.

#### 4. Disclosure Modal Bedrock URL Substitution

**Test:** Configure AWS Bedrock with region set to "us-west-2". Trigger AI Debug for the first time (or reset disclosures via command palette). The disclosure modal opens.

**Expected:** Modal body contains "https://bedrock-runtime.us-west-2.amazonaws.com" — the configured region is substituted, not the default "us-east-1".

**Why human:** Modal DOM rendering requires a live Obsidian instance; cannot be verified by grep or static analysis.

---

## Gaps Summary

No automated gaps found. All 17 must-have truths are verified against the actual codebase. The 4 human verification items above are UAT checks that require a live Obsidian environment and/or real AWS/AI provider credentials — they are not implementation gaps but runtime behavior checks that cannot be exercised in CI.

---

_Verified: 2026-05-17T01:15:00Z_
_Verifier: Claude (gsd-verifier)_
