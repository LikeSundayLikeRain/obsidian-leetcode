---
phase: 14-bidirectional-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/childEditorSync.ts
autonomous: true
requirements: [INDENT-01, INDENT-02, ENTER-01]
must_haves:
  truths:
    - "Typing in child editor updates parent document fence body in real-time"
    - "External writes (vault.process) update child editor without corruption"
    - "No echo loop: child->parent does not trigger parent->child back"
    - "Editing outside the fence does not corrupt child content"
    - "Broken fence structure auto-recovers and sync resumes"
  artifacts:
    - path: "src/main/childEditorSync.ts"
      provides: "Core sync module — child->parent listener, parent->child detection, echo prevention, fence repair"
      exports: ["syncAnnotation", "createChildSyncExtension", "detectAndPropagateExternalChange", "wireSyncIfNeeded"]
  key_links:
    - from: "src/main/childEditorSync.ts"
      to: "src/main/codeActionsEditorExtension.ts"
      via: "imports findCodeFence"
      pattern: "findCodeFence"
    - from: "src/main/childEditorSync.ts"
      to: "src/main/nestedEditorExtension.ts"
      via: "imports extractFenceBody"
      pattern: "extractFenceBody"
    - from: "src/main/childEditorSync.ts"
      to: "src/main/childEditorRegistry.ts"
      via: "imports ChildEditorRegistry"
      pattern: "ChildEditorRegistry"
---

<objective>
Create the core bidirectional sync module (`childEditorSync.ts`) that implements the CM6 split-view pattern for child-parent synchronization.

Purpose: This is the foundational sync logic that enables the nested child editor to function as a real editor — keystrokes propagate to the parent document and external writes propagate back, with no infinite loops or data corruption.

Output: `src/main/childEditorSync.ts` — fully self-contained sync module exporting all sync primitives needed by the wiring plan (Plan 02).
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-bidirectional-sync/14-CONTEXT.md
@.planning/phases/14-bidirectional-sync/14-RESEARCH.md
@.planning/phases/14-bidirectional-sync/14-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/main/codeActionsEditorExtension.ts:
```typescript
export function findCodeFence(
  state: EditorState,
): { openerLine: number; closerLine: number } | null;
```

From src/main/nestedEditorExtension.ts:
```typescript
export function extractFenceBody(
  state: EditorState,
  fence: { openerLine: number; closerLine: number },
): string;

type PluginHost = Plugin & {
  childEditorRegistry: ChildEditorRegistry;
  app: {
    metadataCache: {
      getFileCache(file: { path: string }): { frontmatter?: Record<string, unknown> } | null;
    };
  };
};
```

From src/main/childEditorRegistry.ts:
```typescript
export class ChildEditorRegistry {
  get(key: string): EditorView | undefined;
  has(key: string): boolean;
  set(key: string, view: EditorView): void;
  delete(key: string): void;
  destroyAll(): void;
  readonly size: number;
}
```

From src/main/sectionLockExtension.ts (Gate 0 — child-sync must pass):
```typescript
// Gate 0: only fires for user-input categories
const isUserInput = typeof ev === 'string' &&
  (ev.startsWith('input.') || ev.startsWith('delete.') || ev === 'undo' || ev === 'redo');
if (!isUserInput) return true; // 'leetcode.child-sync' exits HERE
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create childEditorSync.ts — sync annotation, child->parent listener, parent->child detection, fence repair</name>
  <files>src/main/childEditorSync.ts</files>
  <read_first>
    - src/main/codeActionsEditorExtension.ts (findCodeFence at line 177, eslint-disable pattern at line 41-55, languageRefreshEffect at line 112)
    - src/main/nestedEditorExtension.ts (extractFenceBody at line 120, PluginHost type at line 44, StateField fast-path at line 208-210)
    - src/main/childEditorRegistry.ts (ChildEditorRegistry class interface)
    - src/main/sectionLockExtension.ts (Gate 0 userEvent check at lines 375-381, Gate 1 at line 389)
    - src/main.ts (switchFenceLanguage dispatch pattern at lines 799-805 — userEvent convention)
    - .planning/phases/14-bidirectional-sync/14-PATTERNS.md (all analog patterns and conventions)
    - .planning/phases/14-bidirectional-sync/14-RESEARCH.md (full architecture)
  </read_first>
  <behavior>
    - syncAnnotation: Annotation.define&lt;boolean&gt;() exported for parent->child echo prevention
    - createChildSyncExtension(parentView, filePath, registry): returns EditorView.updateListener Extension that fires on child docChanged, skips if transaction has syncAnnotation (from parent), remaps child changes to parent offsets via findCodeFence, dispatches to parentView with Transaction.userEvent.of('leetcode.child-sync')
    - detectAndPropagateExternalChange(tr, plugin, registry): given a parent transaction, checks if docChanged overlaps fence body range (using iterChangedRanges), if so extracts new fence content via extractFenceBody and dispatches full replacement to child with syncAnnotation.of(true); skips if transaction userEvent starts with 'leetcode.'
    - wireSyncIfNeeded(parentView, childView, filePath, registry): checks module-level Set&lt;string&gt; of wired paths, if not wired creates sync extension and reconfigures child; idempotent
    - repairFenceStructure(parentView): when findCodeFence returns null, scans for ## Code heading, determines missing opener/closer, re-inserts via parent dispatch with userEvent 'leetcode.fence-repair'; returns boolean success
    - Offset derivation: bodyStart = parentView.state.doc.line(fence.openerLine).to + 1, bodyEnd = parentView.state.doc.line(fence.closerLine).from — NEVER cached per D-10
    - Defensive try/catch around all dispatch calls (widget may be in teardown)
    - All @codemirror/* imports have eslint-disable comment per project convention
  </behavior>
  <action>
    Create src/main/childEditorSync.ts implementing the CM6 split-view sync pattern per D-01 through D-10.

    Exports:
    1. `syncAnnotation` — `Annotation.define&lt;boolean&gt;()` (per D-09, used by parent->child dispatches to prevent echo back to parent)
    2. `createChildSyncExtension(parentView: EditorView, filePath: string, registry: ChildEditorRegistry): Extension` — returns `EditorView.updateListener.of(...)` that:
       - Early-returns if `!update.docChanged`
       - Early-returns if any transaction carries `syncAnnotation` (echo check per D-09)
       - Calls `findCodeFence(parentView.state)` — if null, calls `repairFenceStructure` then retries once
       - Derives `bodyStart` and `bodyEnd` per D-10 offset convention
       - Validates `bodyStart < bodyEnd` and both within doc bounds (defensive guard)
       - Iterates `update.changes` via `iterChanges((fromA, toA, fromB, toB, inserted) => {...})` remapping each by adding `bodyStart`
       - Dispatches to `parentView` with `annotations: Transaction.userEvent.of('leetcode.child-sync')` per D-02
       - All dispatch wrapped in try/catch (teardown safety per codeActionsEditorExtension.ts:352-363 pattern)
    3. `detectAndPropagateExternalChange(tr: Transaction, plugin: PluginHost, registry: ChildEditorRegistry): void` — called from parent StateField update when `tr.docChanged && !userEvent.startsWith('leetcode.')`:
       - Gets file via `editorInfoField`, checks lc-slug frontmatter gate (same pattern as nestedEditorExtension.ts:153-159)
       - Calls `findCodeFence(tr.state)` — null guard returns early
       - Computes fence body range [bodyStart, bodyEnd]
       - Uses `tr.changes.iterChangedRanges((fromA, toA, fromB, toB) => {...})` to check overlap with old fence range (pre-change positions mapped via `tr.changes.mapPos`)
       - If overlap detected: extracts new fence body via `extractFenceBody(tr.state, fence)`, gets child from registry, dispatches full replacement `{ changes: { from: 0, to: childDoc.length, insert: newContent }, annotations: syncAnnotation.of(true) }` per D-03
    4. `wireSyncIfNeeded(parentView: EditorView, childView: EditorView, filePath: string, registry: ChildEditorRegistry): void`:
       - Module-level `const wiredPaths = new Set&lt;string&gt;()` tracks already-wired children
       - If `wiredPaths.has(filePath)` → return (idempotent)
       - Creates sync extension via `createChildSyncExtension(parentView, filePath, registry)`
       - Reconfigures child: `childView.dispatch({ effects: StateEffect.appendConfig.of(syncExt) })`
       - Adds filePath to `wiredPaths`
       - Exports `unwireSync(filePath: string)` for cleanup on registry eviction
    5. `repairFenceStructure(parentView: EditorView): boolean`:
       - Scans parent doc for `## Code` heading (regex `/^\s*##\s+Code\s*$/`)
       - Finds section end (next `## ` heading or EOF) per D-07
       - Within that range, scans for fence opener (`/^\s*```/`) and closer
       - If opener missing: inserts opener line at start of code section body (line after heading)
       - If closer missing: inserts closer line at end of code section (before next heading or EOF)
       - All repair dispatches use `annotations: Transaction.userEvent.of('leetcode.fence-repair')` per D-05
       - Returns true if repair succeeded, false if ## Code heading not found

    Import `editorInfoField` from 'obsidian'. Import `findCodeFence` from './codeActionsEditorExtension'. Import `extractFenceBody` from './nestedEditorExtension'. Import `ChildEditorRegistry` from './childEditorRegistry'. Use eslint-disable comments on all @codemirror/* imports per project convention.

    The PluginHost type definition can be imported from nestedEditorExtension or redefined locally (Claude's discretion — avoid circular deps if needed by redefining the structural type locally).
  </action>
  <verify>
    <automated>cd /Users/moxu/projects/obsidian-leetcode && npx tsc --noEmit 2>&1 | head -30 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/main/childEditorSync.ts exists and exports: syncAnnotation, createChildSyncExtension, detectAndPropagateExternalChange, wireSyncIfNeeded, unwireSync, repairFenceStructure
    - All @codemirror/* imports preceded by eslint-disable comment matching pattern in codeActionsEditorExtension.ts
    - createChildSyncExtension returns EditorView.updateListener.of(...) Extension
    - syncAnnotation is Annotation.define&lt;boolean&gt;()
    - Child->parent dispatches include annotations: Transaction.userEvent.of('leetcode.child-sync')
    - Parent->child dispatches include annotations: syncAnnotation.of(true)
    - Fence repair dispatches include annotations: Transaction.userEvent.of('leetcode.fence-repair')
    - findCodeFence called with parentView.state (never cached state) per D-10
    - All dispatch calls wrapped in try/catch
    - `tsc --noEmit` reports 0 errors
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>Core sync module compiles, exports all required sync primitives, follows all project conventions (eslint-disable, try/catch dispatch, userEvent annotations), and the build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| child EditorView -> parent document | User input from child crosses into vault document via dispatch |
| vault.process -> child EditorView | External writes cross into child editor state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | child->parent offset remap | mitigate | Always re-derive offsets via findCodeFence(parentView.state) per D-10; validate bodyStart < bodyEnd before dispatch |
| T-14-02 | Denial of Service | echo loop | mitigate | syncAnnotation + userEvent check prevents infinite dispatch loops; defensive depth counter as failsafe |
| T-14-03 | Information Disclosure | fence body content | accept | All content is local vault data owned by the user; no network transmission |
| T-14-04 | Tampering | fence repair injection | mitigate | Repair bounded to ## Code section only; uses known fence syntax; dispatches are undo-able |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in Phase 14; all imports are existing transitive peers |
</threat_model>

<verification>
- `tsc --noEmit` passes with 0 errors
- `npm run build` produces main.js without errors
- `grep -c "syncAnnotation\|createChildSyncExtension\|detectAndPropagateExternalChange\|wireSyncIfNeeded\|repairFenceStructure" src/main/childEditorSync.ts` returns >= 5 (all exports present)
- `grep -c "leetcode.child-sync" src/main/childEditorSync.ts` returns >= 1
- `grep -c "leetcode.fence-repair" src/main/childEditorSync.ts` returns >= 1
</verification>

<success_criteria>
The sync module exists, compiles, and exports all primitives needed by Plan 02 (wiring) and Plan 03 (tests). No new npm packages required.
</success_criteria>

<output>
Create `.planning/phases/14-bidirectional-sync/14-01-SUMMARY.md` when done
</output>
