---
phase: 16-language-packs-switching
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/settings/SettingsStore.ts
  - src/settings/SettingsTab.ts
  - tests/settings/settingsStore.indentSizeOverride.test.ts
autonomous: true
requirements: [INDENT-04]
must_haves:
  truths:
    - "PluginData has new field `indentSizeOverride: 'auto' | 2 | 4 | 8` with default `'auto'` (D-06)"
    - "SettingsStore.load shape-guard collapses any non-matching value (typo, wrong type, missing) to `'auto'`"
    - "SettingsStore exposes `getIndentSizeOverride(): 'auto' | 2 | 4 | 8` and `setIndentSizeOverride(v): Promise<void>`"
    - "Settings UI has a new 'Code editor' section heading containing one dropdown with options Auto / 2 / 4 / 8"
    - "Dropdown reads/writes through the getter/setter; values are coerced from string back to `'auto' | 2 | 4 | 8` in onChange"
    - "Existing settings (auth, problemsFolder, defaultLanguage, AI, contest, knowledge graph) untouched"
    - "Shape-guard test covers the four invalid-input → 'auto' fallback shapes (missing field, wrong type, string '4', invalid number 3)"
  artifacts:
    - path: "src/settings/SettingsStore.ts"
      provides: "PluginData.indentSizeOverride field, DEFAULT_DATA entry, load shape-guard, getter, setter"
      contains: "indentSizeOverride"
    - path: "src/settings/SettingsTab.ts"
      provides: "'Code editor' heading + indent-size dropdown UI bound to settings store"
      contains: "Code editor"
    - path: "tests/settings/settingsStore.indentSizeOverride.test.ts"
      provides: "Unit tests for PluginData.indentSizeOverride load + shape-guard + getter/setter"
  key_links:
    - from: "src/settings/SettingsTab.ts"
      to: "this.plugin.settings.getIndentSizeOverride / setIndentSizeOverride"
      via: "addDropdown onChange handler"
      pattern: "setIndentSizeOverride|getIndentSizeOverride"
    - from: "src/settings/SettingsStore.ts load()"
      to: "DEFAULT_DATA.indentSizeOverride"
      via: "shape-guard fallback path"
      pattern: "raw.indentSizeOverride === 2"
---

<objective>
Extend `SettingsStore` (PluginData + DEFAULT_DATA + load shape-guard + getter + setter) with the `indentSizeOverride: 'auto' | 2 | 4 | 8` field per D-06, and add a 'Code editor' settings section to `SettingsTab` containing a single dropdown bound to the new field. Default is `'auto'`. The setting is consumed in 16-03 (factory init) and 16-04 (chevron reconfigure) — but those plans do NOT block on this one because their tasks read the getter at execution time, and any race during the parallel Wave 1 development is impossible (Wave 2 waits for both Wave 1 plans).

Purpose: User-visible knob to override per-language defaults (e.g., a Java user who prefers 2 spaces). Go is exempt from the override per D-06. The settings persistence pattern follows the strict shape-guard posture established in Phase 06 (`previewClickBehavior`) and Phase 09 (`autoAIReviewOnAC`) — anything not literally matching the locked enum collapses to `'auto'`.

Output: One new field on `PluginData`, one new `Code editor` section in `SettingsTab`, one new test file covering the four shape-guard cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-language-packs-switching/16-CONTEXT.md
@.planning/phases/16-language-packs-switching/16-RESEARCH.md
@CLAUDE.md
@src/settings/SettingsStore.ts
@src/settings/SettingsTab.ts

<interfaces>
<!-- Locked patterns and types from existing code. -->

PluginData interface (src/settings/SettingsStore.ts:51-140) — extend with one new field:
```
indentSizeOverride: 'auto' | 2 | 4 | 8;
```

DEFAULT_DATA constant (src/settings/SettingsStore.ts:226-255) — add:
```
indentSizeOverride: 'auto',
```

Load shape-guard pattern (RESEARCH §7) — placed in `SettingsStore.load`:
```
indentSizeOverride: (raw.indentSizeOverride === 2 ||
                     raw.indentSizeOverride === 4 ||
                     raw.indentSizeOverride === 8)
  ? raw.indentSizeOverride
  : 'auto',
```
Note: `'auto'` is the catchall — string `'auto'` is the default value, but ANY non-numeric input (missing field, wrong type, string `'4'`, etc.) collapses to `'auto'`. Numeric `2/4/8` literals are accepted; everything else falls through.

Getter/setter pattern (existing examples in same file lines 803-812 for previewClickBehavior):
```
getIndentSizeOverride(): 'auto' | 2 | 4 | 8 { return this.data.indentSizeOverride; }
async setIndentSizeOverride(v: 'auto' | 2 | 4 | 8): Promise<void> {
  this.data.indentSizeOverride = v;
  await this.persist();
}
```

Settings UI pattern (SettingsTab.ts lines 175-199 for 'Notes' section, lines 210-222 for 'Preview' section). Use `setHeading()` for the section header and `addDropdown` with `addOption(value, label)` chain (NOT `addOptions` Record literal — locked precedent for dropdowns with explicit ordering, per RESEARCH §7 reference).

Section placement: After the existing 'Notes' section (line ~199 close) and BEFORE the existing 'Preview' section (line 210). Insert as a new `Code editor` heading + one dropdown row.

Dropdown values are strings in Obsidian's API; coerce in onChange:
```
onChange(async (v) => {
  const val: 'auto' | 2 | 4 | 8 =
    v === '2' ? 2 :
    v === '4' ? 4 :
    v === '8' ? 8 :
    'auto';
  await this.plugin.settings.setIndentSizeOverride(val);
})
```
.setValue takes the current value coerced to string: `.setValue(String(this.plugin.settings.getIndentSizeOverride()))`.

Strict-true posture (cf. `disclosureAcknowledged === true` precedent, line 479 of SettingsStore.ts): don't accept truthy strings; only literal numbers 2/4/8. This matches the shape-guard rigour applied to AI fields and prevents a corrupt data.json (e.g., `"4"` as a string) from silently switching the user's indent away from the language default.

Test pattern to follow:
- Mirror `tests/settings/` if it exists; otherwise place under `tests/settings/settingsStore.indentSizeOverride.test.ts`. Use the standard vitest + happy-dom pattern. Mock `Plugin.loadData()` to return crafted raw inputs; assert `SettingsStore.load(plugin).getIndentSizeOverride()` returns the expected resolved value.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add indentSizeOverride field to PluginData, DEFAULT_DATA, load shape-guard, getter, and setter</name>
  <files>src/settings/SettingsStore.ts</files>
  <read_first>
    - src/settings/SettingsStore.ts (full file — note PluginData interface lines 51-140, DEFAULT_DATA lines 226-255, load() lines 613-743 including the previewClickBehavior shape-guard at line 657, getter/setter pairs lines 803-812)
    - .planning/phases/16-language-packs-switching/16-CONTEXT.md D-06
    - .planning/phases/16-language-packs-switching/16-RESEARCH.md §7 (Settings UI for indentSizeOverride — full shape-guard, getter, setter snippets)
  </read_first>
  <behavior>
    - PluginData has `indentSizeOverride: 'auto' | 2 | 4 | 8` between existing fields
    - DEFAULT_DATA includes `indentSizeOverride: 'auto'`
    - Fresh data.json (no field) loads as `'auto'`
    - `loadData()` returning `{ indentSizeOverride: 4 }` loads as `4`
    - `loadData()` returning `{ indentSizeOverride: 3 }` loads as `'auto'` (3 is not in the locked set)
    - `loadData()` returning `{ indentSizeOverride: '4' }` (string) loads as `'auto'` (strict-true posture)
    - `loadData()` returning `{ indentSizeOverride: null }` loads as `'auto'`
    - `getIndentSizeOverride()` returns the current value
    - `setIndentSizeOverride(2)` persists via plugin.saveData and updates the in-memory data
  </behavior>
  <action>
    Add a new field `indentSizeOverride: 'auto' | 2 | 4 | 8` to the `PluginData` interface (place near other Phase-16-related concerns; placement under existing `defaultLanguage` field is reasonable since they relate). Add `indentSizeOverride: 'auto'` to `DEFAULT_DATA` (matching field order). In the `SettingsStore.load` body, add the shape-guard line per RESEARCH §7 — the strict equality check accepts only numeric literals 2/4/8; anything else falls through to `'auto'` (per D-06). Add `getIndentSizeOverride(): 'auto' | 2 | 4 | 8` and `async setIndentSizeOverride(v: 'auto' | 2 | 4 | 8): Promise<void>` methods near the existing related getters/setters (e.g., after `setDefaultLanguage`). Per D-06 commentary, add a JSDoc comment explaining "Go always uses tab regardless of this override (gofmt-non-negotiable)" — this is a setting-on-data, not setting-on-language; the consumer (`childEditorLanguage.ts:effectiveIndent`) already enforces the Go exception.

    Do NOT touch any other field, getter, setter, or DEFAULT_DATA entry. Do NOT modify the existing test files. The shape-guard MUST be a strict equality check (`raw.indentSizeOverride === 2 || === 4 || === 8`); do NOT use loose equality, `parseInt`, or string coercion.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10 && grep -c "indentSizeOverride" src/settings/SettingsStore.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "indentSizeOverride" src/settings/SettingsStore.ts` returns >= 5 (interface field, DEFAULT_DATA, load shape-guard, getter, setter)
    - `npm run build` exits 0
    - Grep `grep "indentSizeOverride: 'auto' | 2 | 4 | 8"` matches at least once (interface declaration)
    - Grep `grep "raw.indentSizeOverride === 2"` matches once (shape-guard)
    - Grep `grep "this.data.indentSizeOverride"` matches in both getter and setter (>= 2 hits)
    - No other Settings fields modified (diff scope check)
  </acceptance_criteria>
  <done>
    PluginData carries the new field with default 'auto'; load is strict; getter and setter follow existing patterns.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Code editor section + indent-size dropdown to SettingsTab</name>
  <files>src/settings/SettingsTab.ts</files>
  <read_first>
    - src/settings/SettingsTab.ts (full file — note 'Notes' section lines 175-199, 'Preview' section lines 210-222, addDropdown patterns at 193 and 215)
    - .planning/phases/16-language-packs-switching/16-RESEARCH.md §7 (full snippet of the Code editor dropdown + onChange coercion)
    - CLAUDE.md (eslint-plugin-obsidianmd sentence-case rule for setting names)
  </read_first>
  <behavior>
    - A new 'Code editor' heading appears between 'Notes' and 'Preview' sections
    - Below it, one dropdown labeled 'Indent size' with description from RESEARCH §7
    - Dropdown options: 'Auto (language default)', '2 spaces', '4 spaces', '8 spaces'
    - Selecting an option persists via `setIndentSizeOverride` and the next `getIndentSizeOverride` returns the chosen value
    - On reload, the dropdown displays the persisted value
  </behavior>
  <action>
    Insert a new Settings section between the existing 'Notes' close (line ~199) and the existing 'Preview' open (line 210). Use `new Setting(containerEl).setName('Code editor').setHeading();` for the header. Then `new Setting(containerEl).setName('Indent size').setDesc(...)` with the description from RESEARCH §7 ("Number of spaces per indent level in the code editor. \"Auto\" uses the language default (4 for Java/Python/C++, 2 for JS/TS, tab for Go)."). Use `.addDropdown(d => d.addOption('auto', 'Auto (language default)').addOption('2', '2 spaces').addOption('4', '4 spaces').addOption('8', '8 spaces').setValue(String(this.plugin.settings.getIndentSizeOverride())).onChange(async (v) => { ... }))`. The onChange coerces string `v` back to `'auto' | 2 | 4 | 8`: `v === '2' ? 2 : v === '4' ? 4 : v === '8' ? 8 : 'auto'`. Pass the coerced value to `setIndentSizeOverride`. Use `addOption` chain (NOT `addOptions` Record) to match the locked precedent for explicit-order dropdowns. No `setCta()` (not a primary action). No `mod-cta` class. Do NOT touch any other section.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10 && grep -n "Code editor\|Indent size\|indentSizeOverride" src/settings/SettingsTab.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Code editor" src/settings/SettingsTab.ts` returns 1
    - `grep -c "Indent size" src/settings/SettingsTab.ts` returns 1
    - `grep -c "setIndentSizeOverride\|getIndentSizeOverride" src/settings/SettingsTab.ts` returns >= 2 (one read, one write)
    - The Code editor heading appears AFTER the Notes section line numbers and BEFORE the Preview section line numbers (verify with `grep -n "setName('Notes'\\|setName('Code editor'\\|setName('Preview')" src/settings/SettingsTab.ts | sort -n` showing Notes < Code editor < Preview)
    - All four addOption calls present: 'auto', '2', '4', '8'
    - `npm run build` exits 0
    - `npm run lint` exits 0 (no eslint-plugin-obsidianmd violations on the new section)
  </acceptance_criteria>
  <done>
    User-visible Code editor section with indent-size dropdown wired to the new SettingsStore methods; lint clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for indentSizeOverride shape-guard + getter/setter</name>
  <files>tests/settings/settingsStore.indentSizeOverride.test.ts</files>
  <read_first>
    - src/settings/SettingsStore.ts (the implementation just modified)
    - Existing settings tests (`ls tests/settings/` — if pattern files exist, mirror them; otherwise mirror `tests/main/childEditorFactory.test.ts` Plugin mock pattern at the top)
  </read_first>
  <behavior>
    - `SettingsStore.load` with raw `{}` → getIndentSizeOverride() === 'auto'
    - `SettingsStore.load` with raw `{ indentSizeOverride: 2 }` → getIndentSizeOverride() === 2
    - `SettingsStore.load` with raw `{ indentSizeOverride: 4 }` → 4
    - `SettingsStore.load` with raw `{ indentSizeOverride: 8 }` → 8
    - `SettingsStore.load` with raw `{ indentSizeOverride: 3 }` → 'auto' (3 is not in the locked set)
    - `SettingsStore.load` with raw `{ indentSizeOverride: '4' }` (string) → 'auto'
    - `SettingsStore.load` with raw `{ indentSizeOverride: null }` → 'auto'
    - `SettingsStore.load` with raw `{ indentSizeOverride: 'auto' }` → 'auto' (the literal string is treated as the catchall fallback path; valid)
    - After load, calling `setIndentSizeOverride(2)` then `getIndentSizeOverride()` returns 2 AND `plugin.saveData` was called once with the updated data
  </behavior>
  <action>
    Create `tests/settings/settingsStore.indentSizeOverride.test.ts`. Build a mock `Plugin` using `vi.fn()` for `loadData` and `saveData`. For each scenario above, set up `loadData` to resolve to the crafted raw object, call `await SettingsStore.load(mockPlugin)`, then assert on `getIndentSizeOverride()`. For the setter test, after load with `{}`, call `await store.setIndentSizeOverride(2)`, assert `mockPlugin.saveData` was called with an object whose `indentSizeOverride === 2`. Use `it.each` table-driven tests for the 7 load scenarios where appropriate. Do not test other settings fields in this file — keep the test file scoped.
  </action>
  <verify>
    <automated>npm test -- tests/settings/settingsStore.indentSizeOverride.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists
    - `npm test -- tests/settings/settingsStore.indentSizeOverride.test.ts` exits 0
    - Test report shows >= 8 passing tests
    - Tests explicitly cover string `'4'` → 'auto' (strict-true assertion)
    - Tests cover the round-trip: load → set → saveData received correct value
  </acceptance_criteria>
  <done>
    All 7 shape-guard scenarios green; setter persistence verified; npm test clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user data.json (untrusted) → SettingsStore.load → PluginData | Per the existing pattern (T-07-01, T-09-04), every new field gets a per-direction guard with safe-default fallback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-01 | Tampering | data.json `indentSizeOverride` poisoned with truthy string | mitigate | Strict-equality shape-guard (`=== 2 || === 4 || === 8`) — string `'4'` and number `3` both collapse to `'auto'`. Test 3 explicitly covers this. |
| T-16-02-02 | Tampering | data.json missing field on first load after upgrade | mitigate | Catchall `'auto'` fallback in load. No exception, no Notice — silent default per existing pattern (cf. previewClickBehavior). |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm run lint` exits 0
- `npm test -- tests/settings/settingsStore.indentSizeOverride.test.ts` green
- `npm test` (full suite) green — must not regress existing settings tests
- Manual UAT: Open the Settings tab in dev vault → verify the new "Code editor" section appears between Notes and Preview, with the 4-option dropdown. Selecting an option and reopening Settings shows the persisted value. (UAT scripted in 16-05.)
- Bundle size delta: negligible (settings code is plain TS; no new external dep introduced by this plan)
</verification>

<success_criteria>
- PluginData has indentSizeOverride field with default 'auto'
- SettingsStore.load is strict and falls back to 'auto' on any non-numeric input
- Settings UI exposes the new dropdown in a new 'Code editor' section
- Tests cover the 7 shape-guard scenarios and one round-trip
- No regressions in existing tests
</success_criteria>

<output>
Create `.planning/phases/16-language-packs-switching/16-02-SUMMARY.md` documenting: PluginData field added, UI section placement, test coverage matrix, any deviations from RESEARCH §7.
</output>
