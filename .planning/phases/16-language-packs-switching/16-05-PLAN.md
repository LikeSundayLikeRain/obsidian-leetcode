---
phase: 16-language-packs-switching
plan: 05
type: execute
wave: 3
depends_on: ["16-03", "16-04"]
files_modified:
  - tests/main/childEditorLanguage.behavioral.test.ts
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [ENTER-02, ENTER-03, ENTER-04, BRACKET-01, BRACKET-02, BRACKET-03, BRACKET-04, BRACKET-05, COMMENT-01, INDENT-04, HIGHLIGHT-01]
must_haves:
  truths:
    - "Behavioral tests with LIVE EditorState (no CM6 mocks) verify BRACKET-01 (auto-close), BRACKET-03 (overtype), BRACKET-04 (Backspace pair-delete), BRACKET-02 (no markdown pair in child — regression test for D-10), COMMENT-01 (Python # / Java // toggle), and INDENT-04 (per-language indent unit) — RESEARCH §10 behavioral matrix"
    - "Behavioral tests cover ENTER-02 (Lezer indent on { for Java/JS/C++/Rust), ENTER-03 (Python : indent), ENTER-04 (matched-brace split) per RESEARCH §4 — these are emergent from Lezer LanguageSupport + closeBrackets and need only fixture verification"
    - "Bundle-size delta is measured via `node scripts/check-bundle-size.mjs` and recorded in the SUMMARY (RESEARCH §9 estimate: ~25-40 KB gzipped)"
    - "REQUIREMENTS.md BRACKET-05 status flipped from Pending → Deferred (D-09) and added to Future Requirements section"
    - "Manual UAT checkpoint covers: chevron switch from Java → Python on a real lc-slug note; bracket auto-close in 8 langs; Cmd-/ in Python (#) and Java/JS/Go/Rust (//); HIGHLIGHT-01 visual at cursor on a {; no scroll jump on switch; no regressions in Phase 13-15 (focus, undo, cursor, sync, section lock)"
    - "Pitfall E (Go commentTokens) gap is verified or remediated: if Cmd-/ no-ops on a Go fence, add `languageData.of({ commentTokens: { line: '//' } })` to the Go LanguageSupport in 16-01's builder (follow-up edit) before phase completion"
  artifacts:
    - path: "tests/main/childEditorLanguage.behavioral.test.ts"
      provides: "Live-EditorState behavioral tests for BRACKET, COMMENT, INDENT, ENTER requirements"
    - path: ".planning/REQUIREMENTS.md"
      provides: "BRACKET-05 traceability row updated to Deferred; Future Requirements section augmented"
      contains: "BRACKET-05"
  key_links:
    - from: "tests/main/childEditorLanguage.behavioral.test.ts"
      to: "src/main/childEditorLanguage.ts (buildLanguageExtensions)"
      via: "import { buildLanguageExtensions } from '../../src/main/childEditorLanguage'"
      pattern: "from.*childEditorLanguage"
    - from: "tests/main/childEditorLanguage.behavioral.test.ts"
      to: "@codemirror/state EditorState (live, not mocked)"
      via: "import { EditorState } from '@codemirror/state'"
      pattern: "import.*EditorState.*from '@codemirror/state'"
---

<objective>
Add behavioral test coverage for the language-aware behaviors that emerge from the Lezer packs + closeBrackets + toggleLineComment combination wired by 16-01..16-04. Use live `EditorState` (no mocks) to verify ENTER-02..04, BRACKET-01..04, COMMENT-01, and INDENT-04 across the 8 LC slugs. Mark BRACKET-05 as Deferred in REQUIREMENTS.md per D-09. Measure bundle-size delta. Run a final manual UAT verifying the end-to-end chevron-switch experience plus regression-coverage of Phase 13-15 invariants.

Purpose: This is the phase's verification gate. Plans 16-01..16-04 are structural (builder, settings, factory, dispatch); their tests are mock-shape assertions. The user-facing requirements (ENTER, BRACKET, COMMENT, INDENT) are emergent from the live integration of LanguageSupport + closeBrackets + the Compartment. Without behavioral tests against real EditorState, the phase ships untested at the requirement level. Live tests also catch the load-bearing assumption A1 (closeBrackets reads per-language data) and A3 (Go StreamLanguage exposes commentTokens) — both flagged MEDIUM in RESEARCH §Assumptions Log.

Output: One new behavioral test file (~150-250 lines), one REQUIREMENTS.md mutation, one human-verify checkpoint, one bundle-size measurement recorded in SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-language-packs-switching/16-CONTEXT.md
@.planning/phases/16-language-packs-switching/16-RESEARCH.md
@.planning/phases/16-language-packs-switching/16-01-PLAN.md
@.planning/phases/16-language-packs-switching/16-03-PLAN.md
@.planning/phases/16-language-packs-switching/16-04-PLAN.md
@CLAUDE.md
@src/main/childEditorLanguage.ts
@scripts/check-bundle-size.mjs

<interfaces>
<!-- Behavioral test patterns + mutation targets -->

Live EditorState behavioral test pattern (RESEARCH §10):
- vitest 4.1.5 + happy-dom environment (already configured)
- Import LIVE `@codemirror/state` (`EditorState`, `Transaction`, etc.) — vitest resolves esbuild-externals from node_modules normally for tests
- Import `buildLanguageExtensions` from the SUT
- Construct a state: `EditorState.create({ doc: '...', extensions: buildLanguageExtensions(slug, override) })`
- Simulate user input via `state.update({ changes: { from, to, insert: '{' } })` then assert resulting `doc.toString()` contains `{}` (auto-close)
- For keymap-driven behavior (closeBracketsKeymap Backspace, Cmd-/), invoke the command directly: locate the binding in the keymap array and call `binding.run({ state, dispatch: () => {} })` — this is the standard CM6 unit-test pattern when you don't have a live view

Reference: `tests/main/sectionLockExtension.test.ts` already uses live EditorState; mirror its setup (no `vi.mock` on CM6 modules).

Test matrix (RESEARCH §10 + §11 — Wave 0 gaps):

| Test | Requirement | Approach |
|------|------------|----------|
| BRACKET-01 Python `{` → `{}` | BRACKET-01 | live state.update with insert '{' |
| BRACKET-01 Java `[` → `[]` | BRACKET-01 | live state.update |
| BRACKET-01 JS `(` → `()` | BRACKET-01 | live state.update |
| BRACKET-01 JS `"` → `""` | BRACKET-01 | live state.update |
| BRACKET-01 Python `'` → `''` | BRACKET-01 | live state.update |
| BRACKET-03 overtype `}` when next char is `}` | BRACKET-03 | position cursor before `}`, type `}`, assert no double `}}` |
| BRACKET-04 Backspace between `{}` deletes both | BRACKET-04 | run closeBracketsKeymap Backspace handler from cursor between pair |
| BRACKET-02 Python `*` does NOT auto-pair (no markdown) | BRACKET-02 | live state.update with `*`; assert doc has single `*`, not `**` |
| BRACKET-02 Python `_` does NOT auto-pair | BRACKET-02 | same |
| BRACKET-02 Python backtick does NOT auto-pair as a markdown thing | BRACKET-02 | same |
| COMMENT-01 Python `toggleLineComment` adds `# ` prefix | COMMENT-01 | run command, assert doc updated |
| COMMENT-01 Java `toggleLineComment` adds `// ` prefix | COMMENT-01 | same |
| COMMENT-01 JS `toggleLineComment` adds `// ` prefix | COMMENT-01 | same |
| COMMENT-01 Rust `toggleLineComment` adds `// ` prefix | COMMENT-01 | same |
| COMMENT-01 Go `toggleLineComment` adds `// ` prefix | COMMENT-01 | live test — IF this fails, see Pitfall E remediation in Task 4 |
| INDENT-04 `getIndentUnit` for python3 | INDENT-04 | call `getIndentUnit(state)` from `@codemirror/language` — assert `'    '` |
| INDENT-04 `getIndentUnit` for javascript | INDENT-04 | assert `'  '` |
| INDENT-04 `getIndentUnit` for golang | INDENT-04 | assert `'\t'` |
| INDENT-04 `getIndentUnit` for typescript | INDENT-04 | assert `'  '` |
| INDENT-04 `getIndentUnit` for golang with override 4 | INDENT-04 | assert `'\t'` (Go non-negotiable; D-06) |
| ENTER-02 Java `{` then Enter indents the new line one level | ENTER-02 | run `insertNewlineAndIndent` after `{`; assert new line indent = previous + indentUnit |
| ENTER-03 Python `:` then Enter indents | ENTER-03 | same |
| ENTER-04 Java between `{|}` Enter splits 3 lines (open / indented blank / dedented close) | ENTER-04 | same |
| HIGHLIGHT-01 — `bracketMatching()` is in the extensions array | HIGHLIGHT-01 | already verified in 16-03 factory test (regression guard); ALSO add a behavioral assertion that the buildLanguageExtensions output does NOT include bracketMatching (it's at top-level outside the Compartment, per D-15) |

REQUIREMENTS.md mutation targets (RESEARCH §8):
- Line 31-32 (BRACKET-05 checkbox `- [ ] **BRACKET-05**: ...`) — leave the checkbox row OR move it under the Future Requirements section (per RESEARCH §8 the load-bearing change is the traceability table at line 83)
- Line 83: change `| BRACKET-05 | Phase 16: Language Packs & Switching           | Pending |` to `| BRACKET-05 | Phase 16: Language Packs & Switching           | Deferred |`
- Add to Future Requirements section (after FUTURE-04, around line 50): `- **BRACKET-05**: Triple-backtick template literal auto-close in JS/TS (CM6 stock closeBrackets does not cover triple-quote sequences; deferred to v1.3 if user-reported).`

Bundle measurement:
- `node scripts/check-bundle-size.mjs` exists per package.json line 12 (`"check:bundle-size"`)
- Run `npm run build` first (production esbuild), then run the script. Record `main.js` size pre/post via git: pre-Phase-16 main.js comes from the last v1.2 commit; post comes from current build. Document delta in SUMMARY.

Pitfall E remediation (RESEARCH §12):
- IF the Go COMMENT-01 behavioral test FAILS (toggleLineComment produces no output on a Go state), the fix is in `src/main/childEditorLanguage.ts` (16-01's file): wrap the Go LanguageSupport: `new LanguageSupport(StreamLanguage.define(go), [languageData.of({ commentTokens: { line: '//' } })])`. Imports needed: `LanguageSupport, languageData` from `@codemirror/language`. This edit lands in this plan as Task 4 (conditional).

Manual UAT requirements (RESEARCH §11 + project convention):
- Open dev vault, navigate to an existing lc-slug note (Java or Python solution)
- Click chevron → switch to each of the other 7 langs in turn; observe:
  - Fence opener tag flips immediately (no flicker)
  - Starter code body swaps (existing parent-side behavior)
  - Indent unit visibly correct (Tab in Java/Python = 4 sp, JS = 2 sp, Go = real tab character)
  - `{` typed → `{}` auto-closes (BRACKET-01)
  - Cmd-/ on a line of code adds the right comment prefix (COMMENT-01)
  - Cursor adjacent to `{` shows match highlight (HIGHLIGHT-01)
  - No note re-open, no scroll jump (LANG-01)
- Smoke-test Phase 13-15 invariants:
  - Click into child → focus stays in child; click into ## Notes → focus returns to parent
  - Tab inside child → indents (not focus-navigation)
  - Cmd-Z → undoes last child edit only
  - External vault.process write (e.g. trigger Copy to Code from a past submission) → child updates without echo
  - ## Problem section still locked (try editing → no-op)
- If any step fails, capture observation in checkpoint resume-signal and remediate before phase close.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create behavioral test file with live EditorState coverage of BRACKET, COMMENT, INDENT, ENTER requirements</name>
  <files>tests/main/childEditorLanguage.behavioral.test.ts</files>
  <read_first>
    - src/main/childEditorLanguage.ts (the SUT — buildLanguageExtensions exports)
    - tests/main/sectionLockExtension.test.ts (live EditorState pattern reference)
    - .planning/phases/16-language-packs-switching/16-RESEARCH.md §10 (Behavioral test matrix)
    - .planning/phases/16-language-packs-switching/16-RESEARCH.md §11 (Validation Architecture — file path)
    - .planning/phases/16-language-packs-switching/16-RESEARCH.md §12 Pitfall E (Go commentTokens contingency)
    - vitest.config.ts (confirm happy-dom environment)
    - package.json (confirm vitest 4.1.5)
  </read_first>
  <behavior>
    Test file structure (RESEARCH §10 + §11 matrix):

    `describe('Behavioral: BRACKET requirements')`:
    - 5+ `it.each` cases covering BRACKET-01 across slugs and bracket characters
    - 1 case for BRACKET-03 overtype
    - 1 case for BRACKET-04 Backspace pair-delete
    - 3+ cases for BRACKET-02 (markdown chars do NOT auto-pair) — D-10 regression

    `describe('Behavioral: COMMENT-01 — toggleLineComment per language')`:
    - 5 `it.each` cases: python3 (#), java (//), javascript (//), rust (//), golang (//)
    - For each: build a live state with the language extensions, position cursor on a code line, run toggleLineComment, assert the line is now prefixed correctly

    `describe('Behavioral: INDENT-04 — getIndentUnit per language and override')`:
    - 5+ cases: python3-auto → '    ', javascript-auto → '  ', typescript-auto → '  ', golang-auto → '\t', java-auto → '    ', rust-auto → '    '
    - 2 override cases: python3 with override=2 → '  ', golang with override=4 → '\t' (D-06 regression)

    `describe('Behavioral: ENTER-02/03/04 — Lezer indent emergent from LanguageSupport')`:
    - ENTER-02 Java: `class Foo {` → press Enter → new line is indented by indentUnit
    - ENTER-03 Python: `def foo():` → press Enter → new line is indented
    - ENTER-04 Java: cursor between `{|}` → press Enter → result is `{\n    \n}` (or equivalent 3-line shape with indent + dedent)
    - These tests run `insertNewlineAndIndent` from `@codemirror/commands` against the live state — IF Lezer indent does not produce the exact expected output for some langs (especially golang StreamLanguage), record the observation in the test name as `it.skip` with a comment referencing assumption A4 in RESEARCH §Assumptions Log; this is acceptable since LANG-01's primary deliverable is the Compartment switch, and ENTER-02..04 are emergent properties of the upstream LanguageSupport packs.
  </behavior>
  <action>
    Create `tests/main/childEditorLanguage.behavioral.test.ts` with no `vi.mock(...)` calls for `@codemirror/*` packages — let vitest resolve them live from node_modules. Imports: `EditorState` from `@codemirror/state`, `getIndentUnit` from `@codemirror/language`, `toggleLineComment, insertNewlineAndIndent` from `@codemirror/commands`, `buildLanguageExtensions` from `../../src/main/childEditorLanguage`. Use `it.each(...)` for table-driven coverage where appropriate. Helper function: `function makeState(slug, override, doc) { return EditorState.create({ doc, extensions: buildLanguageExtensions(slug, override) }) }`. For keymap-driven commands (closeBracketsKeymap Backspace), import `closeBracketsKeymap` from `@codemirror/autocomplete`, find the Backspace binding by `b.key === 'Backspace'`, call `binding.run({ state, dispatch: tr => state = state.update(tr).state })`.

    For BRACKET-01 auto-close, the closeBrackets behavior is triggered by an input event in CM6. Use `closeBrackets()` then simulate by invoking the input handler from the closeBrackets package's input rule — the canonical pattern uses `EditorView.inputHandler` or direct dispatch of the auto-close prefix transaction. If the simpler approach is to assert that `closeBrackets()` extension is present and that closeBracketsKeymap contains the expected bindings (a structural assertion), use that as a fallback when full input simulation proves brittle in happy-dom — note the chosen approach in the test file's docstring. RESEARCH §10 explicitly accepts "live EditorState (no mock) with happy-dom" so the simpler structural assertion is acceptable IF input simulation does not work cleanly; ground-truth behavior verification then lands in the manual UAT (Task 3).

    Per RESEARCH §10 caveat: "CM6 packages work in happy-dom for state-level tests (no rendering)." This means input-event simulation may not work; the test should verify state-level behaviors (getIndentUnit, toggleLineComment command effects, closeBracketsKeymap binding lookups). Document any test that falls back to structural assertion.
  </action>
  <verify>
    <automated>npm test -- tests/main/childEditorLanguage.behavioral.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/main/childEditorLanguage.behavioral.test.ts` exists
    - File does NOT contain `vi.mock('@codemirror/` (live imports)
    - `npm test -- tests/main/childEditorLanguage.behavioral.test.ts` exits 0
    - Test report shows >= 20 passing tests across the four describe blocks (or >= 15 if any ENTER tests skip per RESEARCH §Assumption A4 — must be explicit `.skip` with documented reason)
    - At least 5 BRACKET tests, 5 COMMENT-01 tests (one per language), 5 INDENT-04 tests
    - Go COMMENT-01 test exists and either passes OR triggers Task 4 (Pitfall E remediation)
    - At least one BRACKET-02 test (markdown char does not auto-pair) per D-10
    - `npm test` (full suite) green
  </acceptance_criteria>
  <done>
    Behavioral coverage of all 11 in-scope requirements; >= 15 tests green; Go COMMENT-01 either green or flagged for Task 4.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mutate REQUIREMENTS.md — flip BRACKET-05 to Deferred and add to Future Requirements</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file)
    - .planning/phases/16-language-packs-switching/16-CONTEXT.md D-09 (BRACKET-05 deferred rationale)
    - .planning/phases/16-language-packs-switching/16-RESEARCH.md §8 (exact mutation target)
  </read_first>
  <behavior>
    - The traceability table row at line 83 changes from `Pending` to `Deferred`
    - The Future Requirements section grows by one bullet for BRACKET-05 with the rationale text from RESEARCH §8
    - The Bracket & Pair Handling list (lines 27-32) shows BRACKET-05 either struck through, moved, or annotated as deferred — the load-bearing change is the traceability row + Future Requirements bullet (RESEARCH §8 line 391-393)
    - All other requirements rows unchanged
  </behavior>
  <action>
    Edit `.planning/REQUIREMENTS.md`:

    1. In the traceability table (line 83), change the BRACKET-05 row's Status column from `Pending` to `Deferred`. Preserve the rest of the row's formatting/spacing.

    2. In the `## Future Requirements (Deferred)` section (after FUTURE-04, around line 50), append a new bullet:
       ```
       - **BRACKET-05**: Triple-backtick template literal auto-close in JS/TS (CM6 stock closeBrackets does not cover triple-quote sequences; deferred to v1.3 if user-reported).
       ```

    3. (Optional but recommended for clarity) In the `### Bracket & Pair Handling` list (lines 27-32), append a `(Deferred — see Future Requirements)` annotation to the BRACKET-05 line, OR leave it as-is and rely on the traceability table as the canonical status indicator. The locked rule from RESEARCH §8 line 391 says the traceability table change is "load-bearing"; the checkbox/list is informational.

    Do NOT modify any other requirement IDs, statuses, or sections. Do NOT remove BRACKET-05 from the file (it remains as a Deferred item with traceability).
  </action>
  <verify>
    <automated>grep "| BRACKET-05" .planning/REQUIREMENTS.md && grep "BRACKET-05.*Deferred" .planning/REQUIREMENTS.md && grep -c "BRACKET-05" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "BRACKET-05.*Deferred" .planning/REQUIREMENTS.md` matches at least once (traceability table OR Future Requirements bullet OR both)
    - `grep "BRACKET-05.*Pending" .planning/REQUIREMENTS.md` returns NO matches
    - The Future Requirements section contains a new BRACKET-05 bullet referencing template literals and v1.3
    - `git diff .planning/REQUIREMENTS.md` shows ONLY the BRACKET-05 mutations (no other rows touched)
  </acceptance_criteria>
  <done>
    BRACKET-05 traceability flipped to Deferred; Future Requirements augmented; no other rows touched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bundle-size measurement — record current main.js size and Phase-16 delta</name>
  <files>(no files modified — measurement-only; recorded in SUMMARY)</files>
  <read_first>
    - scripts/check-bundle-size.mjs (the verification script)
    - package.json (line 12 — `check:bundle-size` script)
    - .planning/phases/16-language-packs-switching/16-RESEARCH.md §9 (estimate ~25-40 KB gzipped delta)
    - CLAUDE.md (1.5 MB ceiling)
  </read_first>
  <behavior>
    - `npm run build` produces `main.js` cleanly
    - `node scripts/check-bundle-size.mjs` reports a size value
    - The delta vs. the pre-Phase-16 main.js (last v1.2 commit, e.g., HEAD~N where N is Phase 16's commit count) is within the budget per RESEARCH §9 + CLAUDE.md ceiling
    - Recorded in SUMMARY (16-05-SUMMARY.md): pre-size, post-size, delta KB, gzipped delta, budget headroom
  </behavior>
  <action>
    Run sequentially:
    1. `npm run build` (production esbuild — outputs main.js)
    2. `node scripts/check-bundle-size.mjs` — capture stdout + exit code (must exit 0 to pass the bundle gate)
    3. To compute pre-Phase-16 baseline: stash any uncommitted Phase 16 work, check out the last commit before Phase 16's first commit (`git log --oneline | head -20` to find the boundary), `npm run build`, capture `wc -c main.js`. Restore Phase 16 work via `git stash pop`. (Alternative if stashing is awkward: use `git show {last-pre-phase-16-commit}:main.js | wc -c` if main.js is committed — verify with `git ls-files main.js`.)
    4. Compute delta: post - pre. Report both raw KB and the gzipped estimate using `gzip -c main.js | wc -c` (gzip-compressed size).

    Document all four numbers (pre-raw, post-raw, pre-gz, post-gz) in 16-05-SUMMARY.md. If the script exits non-zero (over budget), DO NOT proceed to Task 4; instead, surface the failure to the user and ask whether to revisit the bundle ceiling — RESEARCH §9 estimates the delta well within budget, so any failure indicates an unexpected bundling regression.

    Use `run_in_background` for the build if it takes > 30s; poll with `wait` semantics or accept the inline 1-2 minute time cost.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && node scripts/check-bundle-size.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `node scripts/check-bundle-size.mjs` exits 0 (bundle within budget)
    - SUMMARY.md (created in <output> step) contains a section "Bundle Size Delta" with at least four numeric values (pre-raw, post-raw, delta-raw, delta-gz)
    - Delta-gz is within RESEARCH §9 estimate of ~25-40 KB OR a deviation explanation is documented
  </acceptance_criteria>
  <done>
    Bundle stays within budget; pre/post numbers captured for SUMMARY.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4 (CONDITIONAL): Pitfall E remediation — Go commentTokens languageData</name>
  <files>src/main/childEditorLanguage.ts</files>
  <read_first>
    - src/main/childEditorLanguage.ts (16-01's file — Go branch in buildLanguageExtensions)
    - .planning/phases/16-language-packs-switching/16-RESEARCH.md §12 Pitfall E (the wrap pattern)
    - tests/main/childEditorLanguage.behavioral.test.ts (the Go COMMENT-01 result from Task 1)
  </read_first>
  <behavior>
    - ONLY EXECUTE THIS TASK IF Task 1's Go `toggleLineComment` test failed.
    - If it passed: skip this task entirely, document "Pitfall E not encountered — StreamLanguage(go) exposes commentTokens correctly" in SUMMARY.
    - If it failed: the Go LanguageSupport branch in `buildLanguageExtensions` is wrapped: `new LanguageSupport(StreamLanguage.define(go), [languageData.of({ commentTokens: { line: '//' } })])` — added imports: `LanguageSupport, languageData` from `@codemirror/language`. After the edit, the Go COMMENT-01 behavioral test passes.
  </behavior>
  <action>
    Conditional on Task 1's Go COMMENT-01 test result:

    - If green: SKIP this task. Add a one-line note in 16-05-SUMMARY.md: "Pitfall E not encountered — StreamLanguage(go) exposes commentTokens for `//`."

    - If red: edit `src/main/childEditorLanguage.ts` Go branch in `buildLanguageExtensions`. Replace the bare `StreamLanguage.define(go)` call with `new LanguageSupport(StreamLanguage.define(go), [languageData.of({ commentTokens: { line: '//' } })])`. Add `LanguageSupport, languageData` to the existing `@codemirror/language` import. Re-run the Go COMMENT-01 test → must turn green. Do NOT modify any other slug branch.
  </action>
  <verify>
    <automated>npm test -- tests/main/childEditorLanguage.behavioral.test.ts --reporter=dot --testNamePattern="COMMENT-01.*[Gg]o"</automated>
  </verify>
  <acceptance_criteria>
    - Either: this task is skipped and SUMMARY documents Pitfall E was not encountered
    - Or: file `src/main/childEditorLanguage.ts` Go branch contains `new LanguageSupport(StreamLanguage.define(go), [languageData.of({ commentTokens: { line: '//' } })])` and the Go COMMENT-01 behavioral test passes
    - `npm test` (full suite) green
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    Either skipped (Go works out of the box) or wrapped (Go now declares commentTokens explicitly); Go COMMENT-01 test green either way.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Manual UAT — chevron switch end-to-end and Phase 13-15 regression</name>
  <what-built>
    Plans 16-01..16-04 wire the Compartment-based language switch. Plan 16-05 Tasks 1-3 cover automated behavioral tests, REQUIREMENTS.md status flip, and bundle measurement. This checkpoint is the human verification of the chevron-switch user experience and Phase 13-15 invariants.
  </what-built>
  <how-to-verify>
    Open the dev vault in Obsidian (Hot Reload plugin if installed; otherwise full Obsidian reload after rebuild).

    Pre-flight:
    1. Confirm `npm run build` outputs the latest main.js into the dev vault's plugin folder (or symlinked).
    2. Reload the plugin (Cmd-P → "Reload app without saving" or click hot-reload).
    3. Open an existing lc-slug Java problem note (e.g. "two-sum" if previously solved in Java; otherwise create one via the chevron from Python).

    Chevron switch UX (LANG-01):
    4. Click the chevron in the fence opener. Switch from current language to Python.
       - [ ] Fence opener flips immediately to ```python (or whatever the new tag is)
       - [ ] Body content swaps to Python starter code (existing parent-side behavior; not a regression)
       - [ ] Indent unit visibly correct: 4 spaces in the new fence (Python default)
       - [ ] Cursor position preserved (or sensibly placed at body start)
       - [ ] No visible scroll jump
       - [ ] No note re-open (the title bar / leaf does not flicker)

    5. Repeat the chevron switch through all 8 languages in sequence: python3 → java → cpp → c → javascript → typescript → golang → rust → python3
       - [ ] Each switch is visually instantaneous
       - [ ] Indent units update correctly per D-05: 4-space for Java/Python/C++/C/Rust, 2-space for JS/TS, real tab for Go
       - [ ] No console errors in DevTools

    Per-language behaviors (BRACKET, COMMENT, HIGHLIGHT):
    6. In a Java fence: type `{` — observe `{}` auto-closes (BRACKET-01). Position cursor between, press Backspace — both deleted (BRACKET-04). Type `(`, then `)` over the auto-inserted closer — observe single `)`, no `))` (BRACKET-03).
    7. In a Python fence: type `*` — observe NO auto-pair (BRACKET-02 / D-10).
    8. In a Python fence: place cursor on a code line, press Cmd-/ (Mac) or Ctrl-/ (Win/Linux) — line gets `# ` prefix (COMMENT-01). Press again — prefix removed.
    9. In a Java fence: same Cmd-/ → `// ` prefix.
    10. In a Go fence: same Cmd-/ → `// ` prefix (Pitfall E gate — if this fails, return to Task 4 remediation BEFORE proceeding).
    11. Position cursor adjacent to a `{` in any fence — observe both the `{` and its matching `}` are visually highlighted (HIGHLIGHT-01 / D-15).

    Per-language Enter-key indent behavior (ENTER-02 / ENTER-03 / ENTER-04):
    12. In a Java fence: on a fresh empty line, type `if (x) {` then press Enter — observe the new line is indented one level deeper than the `if` line (ENTER-02). Acceptable: 4 spaces beyond the previous line's indent.
    13. In a Python fence: on a fresh empty line, type `def foo():` then press Enter — observe the new line is indented one level deeper than the `def` line (ENTER-03). Acceptable: 4 spaces beyond.
    14. In a Java fence: type `{` (observe `{}` auto-pair from BRACKET-01), confirm cursor sits between the pair (`{|}`), then press Enter — observe a 3-line split: `{` on line N, an indented blank line on line N+1 with the cursor on it, `}` on line N+2 aligned with the original `{` line's indent (ENTER-04). If the test infrastructure cannot produce this exact shape, the manual observation here IS the gate; record any deviation.

    Phase 13-15 regression (must still pass):
    15. Click into the child editor → focus stays in child (Phase 15)
    16. Click into ## Notes → focus returns to parent (Phase 15)
    17. Tab inside child → indents (NOT focus-navigation) (Phase 15)
    18. Cmd-Z → undoes last child edit only; ## Notes content unchanged (Phase 15)
    19. Trigger Copy to Code from a past submission → child updates without echo loop (Phase 14)
    20. Try editing ## Problem section → no-op (section lock; Phase 5.5)
    21. Run/Submit buttons in the action row still work (Phase 5.4)

    Report any FAIL observations explicitly so they can be remediated before phase close.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks passed, or list the specific check numbers that failed with observations.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input in child editor → BRACKET-01 auto-close | Already covered structurally in 16-01; behavioral test confirms emergent property |
| Bundle build artifact size → user disk usage | Bundle ceiling is a soft contract with the user (CLAUDE.md 1.5 MB); regression here would be a UX failure, not a security issue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05-01 | DoS | Bundle size regression beyond 1.5 MB ceiling | mitigate | Task 3 measures + the check:bundle-size script gates; over-budget aborts before phase close |
| T-16-05-02 | Tampering | Behavioral tests pass but real-world UX fails (happy-dom vs Electron divergence) | mitigate | Task 5 manual UAT in real Obsidian — a blocking human checkpoint |
| T-16-05-03 | Information Disclosure | REQUIREMENTS.md mutation accidentally flips other rows | mitigate | Acceptance criterion in Task 2 includes `git diff` scope check |
</threat_model>

<verification>
- `npm run build` exits 0
- `npm run lint` exits 0
- `npm test` (full suite) green — at least 20 new behavioral tests on top of 16-01..16-04 unit tests
- `node scripts/check-bundle-size.mjs` exits 0
- Manual UAT (Task 5) signed off — all 21 checks green or remediated
- BRACKET-05 traceability row flipped to Deferred
- No regressions in Phase 13-15 invariants (focus, undo, cursor, sync, section lock, Run/Submit)
</verification>

<success_criteria>
- 11 Phase-16 requirements verified end-to-end (10 active + BRACKET-05 deferred via docs mutation)
- Behavioral tests cover BRACKET, COMMENT, INDENT, ENTER live with EditorState
- BRACKET-05 documented as Deferred in REQUIREMENTS.md
- Bundle delta within RESEARCH §9 estimate
- Manual UAT confirms LANG-01 visual atomicity, BRACKET-01..04 behavior, COMMENT-01 across 5+ langs, HIGHLIGHT-01, AND Phase 13-15 regressions all green
- Pitfall E either not encountered (Go works out of the box) or remediated
</success_criteria>

<output>
Create `.planning/phases/16-language-packs-switching/16-05-SUMMARY.md` documenting:
- Behavioral test coverage matrix with green/skip status per requirement
- Bundle-size delta (pre-raw, post-raw, pre-gz, post-gz, headroom vs 1.5 MB ceiling)
- REQUIREMENTS.md mutation evidence (git diff snippet)
- Pitfall E disposition (encountered + remediated, or not encountered)
- Manual UAT result for the 21 checks
- Phase 13-15 regression status: PASS/FAIL with notes
- Next steps (Phase 17 polish hand-off — D-14 frontmatter reactivity, D-16 theme-aware highlighting, BRACKET-05 v1.3 reconsideration)
</output>
