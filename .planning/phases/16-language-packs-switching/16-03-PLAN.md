---
phase: 16-language-packs-switching
plan: 03
type: execute
wave: 2
depends_on: ["16-01", "16-02"]
files_modified:
  - src/main/childEditorFactory.ts
  - src/main/nestedEditorExtension.ts
  - tests/main/childEditorFactory.test.ts
autonomous: true
requirements: [INDENT-04, BRACKET-01, BRACKET-02, BRACKET-03, BRACKET-04, COMMENT-01, HIGHLIGHT-01]
must_haves:
  truths:
    - "createChildEditor accepts an `initialSlug` and `indentOverride`, then wraps language-dependent extensions with `languageCompartment.of(buildLanguageExtensions(initialSlug, indentOverride))`"
    - "Hardcoded `python()` import + call removed from childEditorFactory.ts (D-11)"
    - "Hardcoded `indentUnit.of('    ')` call removed from childEditorFactory.ts (D-07)"
    - "`closeBracketsKeymap` from `@codemirror/autocomplete` is added to the extensions array at TOP LEVEL (outside the Compartment), placed BEFORE `keymap.of([indentWithTab, ...defaultKeymap, ...historyKeymap])` so its Backspace handler wins (Pitfall D)"
    - "`bracketMatching()` remains in the extensions array (HIGHLIGHT-01 from Phase 13 — verified, NOT removed; D-15)"
    - "Cmd-/ → toggleLineComment binding is reachable via the Compartment's keymap entry (built by 16-01); no separate top-level binding"
    - "Existing extension order preserved per RESEARCH §Architecture Patterns: language Compartment → closeBracketsKeymap → syntaxHighlighting → bracketMatching → history → drawSelection → highlightActiveLine → main keymap → lineWrapping → theme → scrollIntoView → syncExtensions"
    - "Caller in nestedEditorExtension.ts derives initialSlug from lc-language frontmatter (resolveLangSlug fallback to 'python3') and reads indentOverride via settings store"
    - "Section lock and child↔parent sync remain unaffected: factory build is structural; no transactions dispatched"
  artifacts:
    - path: "src/main/childEditorFactory.ts"
      provides: "Updated child editor factory using languageCompartment + closeBracketsKeymap"
      contains: "languageCompartment"
    - path: "src/main/nestedEditorExtension.ts"
      provides: "Updated call site passing initialSlug + indentOverride to createChildEditor"
      contains: "createChildEditor"
    - path: "tests/main/childEditorFactory.test.ts"
      provides: "Updated unit tests asserting Compartment-based shape, with new mocks for './childEditorLanguage' and '@codemirror/autocomplete'"
  key_links:
    - from: "src/main/childEditorFactory.ts"
      to: "src/main/childEditorLanguage.ts"
      via: "import { languageCompartment, buildLanguageExtensions }"
      pattern: "from './childEditorLanguage'"
    - from: "src/main/childEditorFactory.ts"
      to: "@codemirror/autocomplete"
      via: "import { closeBracketsKeymap }"
      pattern: "from '@codemirror/autocomplete'"
    - from: "createChildEditor signature"
      to: "callsite in nestedEditorExtension.ts"
      via: "new params: initialSlug, indentOverride"
      pattern: "createChildEditor\\("
---

<objective>
Replace the hardcoded `python()` LanguageSupport and `indentUnit.of('    ')` in `src/main/childEditorFactory.ts` with `languageCompartment.of(buildLanguageExtensions(initialSlug, indentOverride))` (from 16-01). Add `keymap.of(closeBracketsKeymap)` at the top level (outside the Compartment) BEFORE the existing main keymap so its Backspace handler wins (Pitfall D). Verify `bracketMatching()` (HIGHLIGHT-01) remains in place. Extend the factory signature so callers pass the initial language slug and the user's current `indentSizeOverride`. Update the existing factory tests to assert the new Compartment-based shape, and update the nested editor extension's call site to derive these new arguments from the parent file's frontmatter and settings store.

Purpose: After this plan, opening any child editor uses the correct LanguageSupport for the active fence's slug from the moment of creation — no more "always Python" debt from Phase 13. The Compartment is also now in place so 16-04's chevron reconfigure dispatch will land on a real Compartment key (no remount, no widget rebuild). All seven Phase-16 requirements that depend on the factory becoming language-aware (BRACKET-01..04, COMMENT-01, HIGHLIGHT-01 verification, INDENT-04 active language) are unlocked here. ENTER-02..04 also activate via the Lezer LanguageSupport packs once the Compartment is wired (verified behaviorally in 16-05).

Output: A factory that accepts `(content, parent, initialSlug, indentOverride, syncExtensions?)` (signature change) and produces a per-language-aware EditorView; updated nested editor extension call site; updated tests that mock the new builder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-language-packs-switching/16-CONTEXT.md
@.planning/phases/16-language-packs-switching/16-RESEARCH.md
@.planning/phases/16-language-packs-switching/16-01-PLAN.md
@.planning/phases/16-language-packs-switching/16-02-PLAN.md
@.planning/research/NESTED-EDITOR-PITFALLS.md
@CLAUDE.md
@src/main/childEditorFactory.ts
@src/main/childEditorRegistry.ts
@src/main/childEditorSync.ts
@src/main/childEditorLanguage.ts
@src/solve/languages.ts

<interfaces>
<!-- Locked patterns and types extracted from existing code + RESEARCH §Architecture Patterns. -->

Current `createChildEditor` signature (src/main/childEditorFactory.ts:35-39):
```
export function createChildEditor(
  content: string,
  parent: HTMLElement,
  syncExtensions?: Extension[],
): EditorView
```

Target signature (this plan):
```
export function createChildEditor(
  content: string,
  parent: HTMLElement,
  initialSlug: string,
  indentOverride: 'auto' | 2 | 4 | 8,
  syncExtensions?: Extension[],
): EditorView
```

NOTE on call-site updates: `createChildEditor` callers live in `src/main/nestedEditorExtension.ts` (Phase 13 mount path). This plan adds two new positional parameters BEFORE the existing optional `syncExtensions` parameter. Callers MUST be updated. Search command: `grep -rn "createChildEditor(" src/`. At the call site, derive `initialSlug` from the parent file's `lc-language` frontmatter via the existing `resolveLangSlug` helper from `src/solve/languages.ts` (fallback `'python3'`). Read `indentOverride` via `plugin.settings.getIndentSizeOverride()`. The plugin instance is reachable via the existing closure pattern Phase 13 used for the nested editor extension.

Target extensions array order (RESEARCH §Architecture Patterns lines 549-564):
1. `languageCompartment.of(buildLanguageExtensions(initialSlug, indentOverride))`
2. `keymap.of(closeBracketsKeymap)` — top-level, outside the Compartment, placed BEFORE main keymap (Pitfall D — Backspace priority)
3. `syntaxHighlighting(defaultHighlightStyle)`
4. `bracketMatching()` — HIGHLIGHT-01, unchanged from Phase 13 (D-15)
5. `history()`
6. `drawSelection()`
7. `highlightActiveLine()`
8. `keymap.of([indentWithTab, ...defaultKeymap, ...historyKeymap])` — main keymap
9. `EditorView.lineWrapping`
10. `EditorView.theme({...})`
11. `createScrollIntoViewExtension()`
12. `...(syncExtensions ?? [])`

Compared to current order (childEditorFactory.ts:42-69):
- Line 43 `python(),` → REMOVED (now lives in the Compartment value built by buildLanguageExtensions)
- Line 49 `indentUnit.of("    "),` → REMOVED (now lives in the Compartment value)
- After `python` import removal: also remove `import { python } from '@codemirror/lang-python';`
- After `indentUnit.of(...)` line removal: REMOVE the `indentUnit` symbol from the `@codemirror/language` import line (no other usage remains in this file). Verify with `grep -n "indentUnit" src/main/childEditorFactory.ts`.
- ADD: `import { languageCompartment, buildLanguageExtensions } from './childEditorLanguage';`
- ADD: `import { closeBracketsKeymap } from '@codemirror/autocomplete';` — package root only (Pitfall B)

Pitfall D (RESEARCH §12): `closeBracketsKeymap` must precede `defaultKeymap` in the extensions array so its Backspace handler is consulted first. Place at position 2 (right after the language Compartment).

Pitfall B (RESEARCH §12): Always import `closeBracketsKeymap` from `'@codemirror/autocomplete'` package root. NEVER from a deep subpath.

HIGHLIGHT-01 verification (D-15): The `bracketMatching()` call MUST remain. Don't remove or duplicate it.

Existing test file (`tests/main/childEditorFactory.test.ts`) currently asserts (lines 49-51):
- `python` mocked from `@codemirror/lang-python` returning `'mock-python-extension'`
- `indentUnit.of` mocked from `@codemirror/language`

After this plan, these assertions are REMOVED (the factory no longer calls `python()` or `indentUnit.of` directly). Replace them with mocks for `'./childEditorLanguage'` (mocking `languageCompartment`, `buildLanguageExtensions`) and `'@codemirror/autocomplete'` (mocking `closeBracketsKeymap`). Assertions become: `buildLanguageExtensions` was called with the expected slug + override; `languageCompartment.of` was called with the result.

resolveLangSlug helper (src/solve/languages.ts:67-77):
```
export function resolveLangSlug(fenceTag: string | null | undefined, fallback: string): string
```
At the call site, `lc-language` frontmatter value (string) is the canonical LC slug. Pass it directly when present; fall back to `'python3'` when absent.

Plugin host wiring at the call site:
- `src/main/nestedEditorExtension.ts` (Phase 13) — the StateField factory function takes `plugin` as a closure argument and uses it to access the registry, sync, etc. Add `plugin.settings.getIndentSizeOverride()` and a frontmatter read at the createChildEditor call site. The frontmatter read pattern is established in `childEditorSync.ts:195-199` (`plugin.app.metadataCache.getFileCache(file)?.frontmatter as Record<string, unknown>`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor childEditorFactory.ts to use languageCompartment + closeBracketsKeymap; signature gains initialSlug + indentOverride</name>
  <files>src/main/childEditorFactory.ts</files>
  <read_first>
    - src/main/childEditorFactory.ts (current full file — 73 lines)
    - src/main/childEditorLanguage.ts (16-01's exports)
    - src/settings/SettingsStore.ts (16-02's getIndentSizeOverride contract — informational)
    - .planning/phases/16-language-packs-switching/16-RESEARCH.md §Architecture Patterns + §12 Pitfall D + §12 Pitfall B + §6 Sync Compatibility
    - .planning/phases/16-language-packs-switching/16-CONTEXT.md D-07, D-11, D-15
    - CLAUDE.md (eslint-disable-next-line import/no-extraneous-dependencies pattern)
  </read_first>
  <behavior>
    - `createChildEditor(content, parent, initialSlug, indentOverride, syncExtensions?)` returns an EditorView with the new extension order
    - Calling `createChildEditor('print("hi")', parentEl, 'python3', 'auto')` produces an editor where `buildLanguageExtensions` was invoked with `('python3', 'auto')` and `languageCompartment.of` was invoked with the result
    - Calling with `'golang'` produces an editor where `buildLanguageExtensions` was invoked with `('golang', ...)` (the actual indent unit lookup is delegated to that builder, tested in 16-01)
    - The existing `bracketMatching()` HIGHLIGHT-01 wiring is preserved (verifiable via mock-call assertion on the unchanged import)
    - `closeBracketsKeymap` is included in the extensions array at the top level (verifiable via test mock assertion that `keymap.of` was called with the closeBracketsKeymap sentinel)
    - The `python` import is no longer present in the file
    - The hardcoded `indentUnit.of('    ')` call is no longer present in the file
    - The `indentUnit` symbol is no longer in the `@codemirror/language` import (no other usage remains)
  </behavior>
  <action>
    Edit `src/main/childEditorFactory.ts`:

    1. Imports: REMOVE `import { python } from '@codemirror/lang-python';` (line 23). REMOVE the `indentUnit` symbol from the `@codemirror/language` import (line 19) — keep `syntaxHighlighting`, `defaultHighlightStyle`, `bracketMatching` (verify via grep that indentUnit has no other usage). ADD `import { languageCompartment, buildLanguageExtensions } from './childEditorLanguage';` (no eslint-disable needed — sibling module). ADD `import { closeBracketsKeymap } from '@codemirror/autocomplete';` with the matching `// eslint-disable-next-line import/no-extraneous-dependencies -- transitive peer of obsidian; external in esbuild` comment per the existing convention. KEEP `bracketMatching` import (D-15 — HIGHLIGHT-01 unchanged).

    2. Update the function signature to: `export function createChildEditor(content: string, parent: HTMLElement, initialSlug: string, indentOverride: 'auto' | 2 | 4 | 8, syncExtensions?: Extension[]): EditorView`

    3. Replace the extensions array contents per RESEARCH §Architecture Patterns ordering. Specifically: REMOVE the existing `python(),` line and the existing `indentUnit.of("    "),` line. ADD `languageCompartment.of(buildLanguageExtensions(initialSlug, indentOverride)),` as the FIRST extension. ADD `keymap.of(closeBracketsKeymap),` as the SECOND extension (top-level, before all subsequent extensions including the main keymap — Pitfall D Backspace priority). Preserve the rest in their existing relative order: syntaxHighlighting → bracketMatching → history → drawSelection → highlightActiveLine → main keymap → lineWrapping → theme → createScrollIntoViewExtension → spread syncExtensions.

    4. Update the JSDoc on the function to mention Phase 16, name the new parameters, and reference D-11 + D-07. Do NOT remove the existing Phase 13 attribution comment block at the file head — append a Phase 16 note.

    Do NOT modify section-lock, sync, or registry code in this plan. Do NOT add any cm.dispatch calls — the factory is structural construction only. Do NOT promote `closeBrackets()` to top level — it MUST live inside the Compartment per D-11 so it picks up per-language pairs from languageData on every reconfigure.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10 && grep -c "languageCompartment\|buildLanguageExtensions\|closeBracketsKeymap" src/main/childEditorFactory.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "languageCompartment" src/main/childEditorFactory.ts` returns >= 2 (import + usage)
    - `grep -c "buildLanguageExtensions" src/main/childEditorFactory.ts` returns >= 2
    - `grep -c "closeBracketsKeymap" src/main/childEditorFactory.ts` returns >= 2
    - `grep -c "from '@codemirror/lang-python'" src/main/childEditorFactory.ts` returns 0 (import removed)
    - `grep -c "indentUnit.of(\"    \")" src/main/childEditorFactory.ts` returns 0 (literal removed)
    - `grep -c "indentUnit" src/main/childEditorFactory.ts` returns 0 (symbol no longer used)
    - `grep -c "bracketMatching()" src/main/childEditorFactory.ts` returns 1 (HIGHLIGHT-01 preserved, D-15)
    - The signature `function createChildEditor(content: string, parent: HTMLElement, initialSlug: string, indentOverride:` exists (grep)
    - `npm run build` exits 0 — note: build will FAIL until Task 2 updates the call site, so verify Task 1 passes type-check by running `tsc -noEmit -skipLibCheck` and accepting the call-site error there as expected; the FULL `npm run build` is verified at end of Task 2
  </acceptance_criteria>
  <done>
    Factory uses Compartment-based language wiring; closeBracketsKeymap at top level; HIGHLIGHT-01 preserved; signature widened. Call sites broken until Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update nestedEditorExtension.ts call site to pass initialSlug + indentOverride</name>
  <files>src/main/nestedEditorExtension.ts</files>
  <read_first>
    - src/main/nestedEditorExtension.ts (full file — focus on the createChildEditor invocation site)
    - src/main/childEditorFactory.ts (the new signature from Task 1)
    - src/solve/languages.ts (resolveLangSlug helper — lines 67-77)
    - src/main/childEditorSync.ts:195-199 (frontmatter read pattern via plugin.app.metadataCache.getFileCache)
    - src/settings/SettingsStore.ts (getIndentSizeOverride from 16-02)
  </read_first>
  <behavior>
    - All `createChildEditor(...)` invocations in `src/main/nestedEditorExtension.ts` pass the new 5-arg shape (content, parent, initialSlug, indentOverride, syncExtensions?)
    - `initialSlug` is read from the parent file's `lc-language` frontmatter via `plugin.app.metadataCache.getFileCache(file)?.frontmatter['lc-language']`; if absent or non-string, fallback to `'python3'`
    - `indentOverride` is read via `plugin.settings.getIndentSizeOverride()`
    - The plugin reference is already in scope via the existing Phase 13 closure pattern; no new wiring required
    - If `file` is not available at the call site (rare race — registry hit before editorInfo populated), fall back to slug `'python3'` and override `'auto'`
  </behavior>
  <action>
    Search for `createChildEditor(` in `src/main/nestedEditorExtension.ts` (use grep). At each call site:

    1. Before the call, read the active file from the parent state's `editorInfoField` (same pattern Phase 13/14 already use — see `childEditorSync.ts:192` for the pattern). If `file` is null, default `initialSlug = 'python3'` and `indentOverride = 'auto'`.

    2. If `file` is present, read `plugin.app.metadataCache.getFileCache(file)?.frontmatter as Record<string, unknown> | undefined`, then `const lcLang = fm?.['lc-language']; const initialSlug = typeof lcLang === 'string' ? lcLang : 'python3';`. Use `resolveLangSlug` from `src/solve/languages.ts` if a fence-tag-shaped value reaches here (defensive — primary source is the canonical lc-language frontmatter, which already holds the canonical slug; resolveLangSlug is identity for canonical slugs).

    3. Read `const indentOverride = plugin.settings.getIndentSizeOverride();`.

    4. Pass both as the new 3rd + 4th positional arguments to `createChildEditor`. The optional `syncExtensions` (if present at the call site) shifts to the 5th position.

    Do NOT modify any other behavior in nestedEditorExtension.ts. Do NOT alter section-lock, transaction filter, or widget decoration logic. The change is strictly: read two values + pass them to the factory.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "createChildEditor(" src/main/nestedEditorExtension.ts` shows each call has at least 4 positional arguments before the optional 5th
    - `grep -n "getIndentSizeOverride\|lc-language" src/main/nestedEditorExtension.ts` shows the new reads inserted near each createChildEditor call
    - `npm run build` exits 0 (full TS strict + esbuild production — no type errors anywhere)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>
    Call site passes new args; full build clean; type-check passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update childEditorFactory.test.ts to assert Compartment-based shape</name>
  <files>tests/main/childEditorFactory.test.ts</files>
  <read_first>
    - tests/main/childEditorFactory.test.ts (full file — note current mocks at lines 8-49 and the test bodies that assert python() and indentUnit.of('    '))
    - src/main/childEditorFactory.ts (the file just modified in Task 1)
    - src/main/childEditorLanguage.ts (the new module to mock)
    - .planning/phases/16-language-packs-switching/16-RESEARCH.md §10 ("Update tests/main/childEditorFactory.test.ts" subsection)
  </read_first>
  <behavior>
    - REMOVE: any test asserting `python()` was called directly (line ~99 in current test file — moved into the builder, no longer factory's responsibility)
    - REMOVE: any test asserting `indentUnit.of('    ')` was called directly (line ~173 — moved into the builder)
    - REMOVE: the `vi.mock('@codemirror/lang-python', ...)` factory (line 49 in current test file) — no longer imported by the SUT
    - REMOVE: the `indentUnit` mock from the `@codemirror/language` mock factory (line 38) — no longer imported
    - ADD: `vi.mock('./childEditorLanguage', () => ({ languageCompartment: { of: vi.fn().mockReturnValue('mock-lang-compartment') }, buildLanguageExtensions: vi.fn().mockReturnValue(['mock-lang-extensions']) }))`
    - ADD: `vi.mock('@codemirror/autocomplete', () => ({ closeBracketsKeymap: [{ key: 'mock-cb-key' }] }))`
    - UPDATE: existing test invocations of `createChildEditor(content, parent)` to `createChildEditor(content, parent, 'python3', 'auto')` (or other slugs in dedicated tests)
    - ADD: a new test asserting `buildLanguageExtensions` was called with the slug + override passed to the factory
    - ADD: a new test asserting `languageCompartment.of` was called with the buildLanguageExtensions return value
    - ADD: a new test asserting `keymap.of` was called with `closeBracketsKeymap` (verifiable via mock invocation captures or by inspecting the EditorState.create extensions array)
    - VERIFY (existing test, retained): `bracketMatching()` was called once (HIGHLIGHT-01 D-15 — explicit regression guard)
  </behavior>
  <action>
    Edit `tests/main/childEditorFactory.test.ts`. At the top of the file, REPLACE the `@codemirror/lang-python` mock with the new mocks for `'./childEditorLanguage'` and `'@codemirror/autocomplete'` per the behavior specification above. In the existing `@codemirror/language` mock factory, REMOVE the `indentUnit` entry. In each `it(...)` body, update the `createChildEditor(...)` call signature to pass `'python3', 'auto'` (or test-specific slug/override). REMOVE the assertions that `python()` was called directly and that `indentUnit.of('    ')` was called directly.

    ADD a new `describe('language Compartment wiring', () => { ... })` block with three tests:
    1. `it('passes initialSlug and indentOverride to buildLanguageExtensions')` — call factory with `'java', 4`, assert `buildLanguageExtensions` mock was called with `('java', 4)`
    2. `it('wraps the builder result in languageCompartment.of')` — assert `languageCompartment.of` was called with the result of buildLanguageExtensions (use mock return value sentinel)
    3. `it('includes closeBracketsKeymap via keymap.of at top level')` — inspect the extensions array passed to `EditorState.create` and assert it contains `'mock-keymap'` (the keymap.of return value sentinel) — exact assertion depends on the existing test fixture style. Cross-check with the closeBracketsKeymap mock's recorded `keymap.of` invocation.

    ADD a regression test `it('still calls bracketMatching() — HIGHLIGHT-01 / D-15 preserved')` that asserts the bracketMatching mock was called.

    Do NOT add behavioral / live-EditorState tests in this file — those land in 16-05.
  </action>
  <verify>
    <automated>npm test -- tests/main/childEditorFactory.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/main/childEditorFactory.test.ts` no longer contains `vi.mock('@codemirror/lang-python'`
    - File no longer contains `indentUnit:` inside the `@codemirror/language` mock factory
    - File contains `vi.mock('./childEditorLanguage'`
    - File contains `vi.mock('@codemirror/autocomplete'`
    - File contains test asserting `buildLanguageExtensions` was called with the expected slug + override
    - File contains test asserting `bracketMatching` mock was called (HIGHLIGHT-01 regression)
    - `npm test -- tests/main/childEditorFactory.test.ts` exits 0
    - All tests in this file pass
    - `npm test` (full suite) green — no other test file regresses
  </acceptance_criteria>
  <done>
    Factory tests cover the new Compartment-based shape; HIGHLIGHT-01 explicitly regression-tested; full suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| childEditorFactory build call → Compartment.of(buildLanguageExtensions(...)) | Pure construction; no untrusted input flows through this boundary |
| nestedEditorExtension call site → frontmatter `lc-language` value | Frontmatter is user-editable; resolveLangSlug + 'python3' fallback handle malformed values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | Tampering | User edits `lc-language` frontmatter to a non-canonical value (e.g., 'klingon') | mitigate | `buildLanguageExtensions` falls back to `python()` for unknown slugs (16-01 default branch); no exception thrown; behavior degrades gracefully to Python highlighting |
| T-16-03-02 | DoS | Backspace handler ordering (defaultKeymap consumes Backspace before closeBracketsKeymap can pair-delete) | mitigate | Pitfall D — closeBracketsKeymap registered BEFORE main keymap in the extensions array. Acceptance criterion in Task 1 verifies position. |
| T-16-03-03 | Tampering | HIGHLIGHT-01 regression: someone removes `bracketMatching()` during refactor | mitigate | Explicit regression test in Task 3 asserts the mock was called; D-15 noted in JSDoc |
</threat_model>

<verification>
- `npm run build` exits 0 (TS strict + esbuild production)
- `npm run lint` exits 0
- `npm test -- tests/main/childEditorFactory.test.ts` green
- `npm test` (full suite) green — no regressions in Phase 13/14/15 tests
- Manual UAT (deferred to 16-05): Open an lc-slug note in dev vault → fence renders with appropriate language coloring (Python for python3, Java braces for java, etc.). Bracket auto-close fires on `{`. Cmd-/ toggles `#` (Python) or `//` (Java).
- Bundle size delta documented in 16-05 SUMMARY (this plan's net effect is the new builder import — actual delta from new lang-rust + legacy-modes bundles in 16-05 measurement)
- Regression check: Phase 13/14/15 behaviors preserved — focus, undo, cursor, sync, section lock unchanged. Verified by full test suite green and visual UAT in 16-05.
</verification>

<success_criteria>
- Factory uses Compartment + builder (no hardcoded python or indentUnit literal)
- closeBracketsKeymap at top level, before main keymap
- bracketMatching() preserved (HIGHLIGHT-01)
- Call site in nestedEditorExtension.ts derives initialSlug + indentOverride correctly
- Tests updated; full suite green
- No regressions in Phase 13/14/15
</success_criteria>

<output>
Create `.planning/phases/16-language-packs-switching/16-03-SUMMARY.md` documenting: factory signature change, extension order, call-site updates, test changes, regression evidence, any deviations from RESEARCH §Architecture Patterns.
</output>
