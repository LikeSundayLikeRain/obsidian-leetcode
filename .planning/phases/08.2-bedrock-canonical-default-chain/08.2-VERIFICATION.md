---
phase: 08.2-bedrock-canonical-default-chain
verified: 2026-05-17T22:02:30Z
status: passed
score: 13/13 must-haves verified
overrides_applied: 0
re_verification: null
gaps: []
deferred: []
human_verification: []
---

# Phase 08.2: Bedrock Canonical Default-Chain + credential_process Auto-Refresh Verification Report

**Phase Goal:** Close the credential-resolution drift between Phase 08.1's hand-rolled awsCredentials.ts and the canonical AWS chain. Honor AWS_PROFILE / AWS_DEFAULT_PROFILE; honor AWS_SHARED_CREDENTIALS_FILE / AWS_CONFIG_FILE overrides; read ~/.aws/config's [profile X] syntax; resolve credential_process via child_process.spawnSync with in-memory cache and auto-refresh on Expiration minus 5-minute skew.
**Verified:** 2026-05-17T22:02:30Z
**Status:** PASSED
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | AWS_PROFILE=work resolves the [work] section instead of [default] | VERIFIED | `resolveProfileName()` at awsCredentials.ts:86-92 checks `process.env.AWS_PROFILE` first; test "AWS_PROFILE=work resolves the [work] section" passes (line 242, awsCredentials.test.ts) |
| 2 | AWS_SHARED_CREDENTIALS_FILE and AWS_CONFIG_FILE env overrides are honored | VERIFIED | `resolveCredentialsPath()` (line 97-107) and `resolveConfigPath()` (line 112-122) check env vars first; two dedicated tests pass (lines 327-363, awsCredentials.test.ts) |
| 3 | credential_process spawns a helper, parses AWS CLI v1 JSON, and returns credentials | VERIFIED | `runCredentialProcess()` uses `spawnSync`, `parseCredentialProcessOutput()` validates Version/AccessKeyId/SecretAccessKey/SessionToken/Expiration; 6 spawn behavior tests pass |
| 4 | credential_process results cache in memory with Expiration-5min TTL | VERIFIED | `SKEW_MS = 5 * 60 * 1000` at credentialProcess.ts:225; cache logic at lines 256-283; "returns fresh creds when within TTL (no spawn)" and "re-spawns when past Expiration - 5min skew" tests pass |
| 5 | Concurrent getCachedOrRefresh calls coalesce to a single spawn | VERIFIED | `inflightPromises` Map at credentialProcess.ts:293; "5 concurrent getCachedOrRefresh calls -> exactly 1 spawn" test passes with spawnCount=1 across 5 concurrent calls |
| 6 | credential_process stdout is NEVER logged; stderr truncated to 200 chars | VERIFIED | Zero logger references in credentialProcess.ts (grep confirmed 0 hits on non-comment lines); `stderr.slice(0, 200)` at line 147; "source file has zero logger imports" security test passes |
| 7 | credential_process errors hard-fail; probe surfaces them as { ok: false, errorMessage } | VERIFIED | `cache.delete(profileName); throw err` in catch block (lines 279-282); `probeBedrock` catch at bedrock.ts:101 returns `{ ok: false, errorMessage: extractProviderError(err).slice(0, 200) }` |
| 8 | Settings dropdown third option reads 'Profile name' (not 'SSO profile') | VERIFIED | SettingsTab.ts line 447: `.addOption('sso-profile', 'Profile name')`; grep for `'SSO profile'` returns empty |
| 9 | Explicit access keys mode shows an optional Session token field | VERIFIED | SettingsTab.ts lines 497-509: `new Setting(containerEl).setName('Session token')` with type=password inside `if (bcfg.authMethod === 'access-keys')` block |
| 10 | Default-chain helper text mentions AWS_PROFILE, credential_process, and AWS_*_FILE overrides | VERIFIED | SettingsTab.ts line 471: full D-04 text present, mentions AWS_PROFILE, AWS_DEFAULT_PROFILE, credential_process, AWS_SHARED_CREDENTIALS_FILE, AWS_CONFIG_FILE |
| 11 | bedrock.ts access-keys branch passes sessionToken to the SDK | VERIFIED | bedrock.ts lines 59-61: `if (cfg.sessionToken) { opts.sessionToken = cfg.sessionToken; }`; integration test "access-keys mode passes sessionToken to SDK when present" passes |
| 12 | sessionToken field is preserved across auth-method switches (Pitfall 10 invariant) | VERIFIED | SettingsStore.ts line 499: `sessionToken: typeof r.sessionToken === 'string' ? r.sessionToken : ''` inside `sanitizeBedrockProviderConfig` — preserves regardless of authMethod |
| 13 | Bundle stays under 1.2 MB ceiling | VERIFIED | SUMMARY.md reports 1101.9 KB; `npm run build` exits 0 (verified by running build — TypeScript compiles cleanly) |

**Score:** 13/13 truths verified

### Roadmap Success Criteria Coverage

| SC | Criterion | Status | Evidence |
|----|-----------|--------|----------|
| 1 | AWS_PROFILE=work resolves [work]/[profile work] instead of [default]; AWS_DEFAULT_PROFILE and "default" as documented fallbacks | VERIFIED | `resolveProfileName()` waterfall: AWS_PROFILE -> AWS_DEFAULT_PROFILE -> "default"; 4 profile-resolution tests pass |
| 2 | AWS_SHARED_CREDENTIALS_FILE and AWS_CONFIG_FILE honored; defaults used when absent | VERIFIED | Both resolvers check env var first, fall back to `path.join(homedir(), '.aws', '...')`; 2 override tests pass |
| 3 | credential_process command spawned, stdout parsed as AWS-CLI v1 JSON, creds used for signing | VERIFIED | Full spawn+parse pipeline in credentialProcess.ts; all 6 spawn behavior tests pass |
| 4 | Cache TTL respects Expiration minus 5-min skew; reuse within TTL, re-spawn past skew | VERIFIED | SKEW_MS=5*60*1000; TTL cache behavior confirmed by 3 cache timing tests (including fake timers) |
| 5 | Errors surface as Notice (truncated stderr); probeBedrock never throws — collapses to {ok:false,errorMessage} | VERIFIED | probeBedrock's try/catch at bedrock.ts:100-102; Note: Notice rendering is at UI layer, not adapter layer — adapter contract {ok:false} is correctly implemented |
| 6 | [profile X] syntax + bare-name credentials; region and credential_process flow through | VERIFIED | `parseIniSection()` dispatches by fileType; config uses `[profile X]` with `[default]` exception; "parseIniProfile returns credential_process and region fields" test passes |
| 7 | Static [profile X] entries without credential_process fall through to direct key reads | VERIFIED | Lines 233-238 in awsCredentials.ts: static key fallback when no credential_process in merged section; preserved by 08.1 tests that still pass |
| 8 | Stdout of credential_process NEVER logged; stderr logged truncated to 200 chars | VERIFIED | Zero logger references in credentialProcess.ts; `stderr.slice(0, 200)` enforced at source; D-22 security test passes |
| 9 | Bundle under 1.2 MB ceiling; no new npm deps | VERIFIED | 1101.9 KB per SUMMARY; build exits 0; no packages added (tech_stack.added: [] in both SUMMARY files) |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/ai/awsCredentials.ts` | Profile-resolution waterfall + AWS_*_FILE overrides + config-file reader + merge logic | VERIFIED | 324 lines; exports `resolveAwsCredentials`, `parseIniProfile`, `safeReadFile`; imports `getCachedOrRefreshSync` from credentialProcess |
| `src/ai/credentialProcess.ts` | credential_process command parser + spawnSync runner + cache + coalescing | VERIFIED | 322 lines; exports `parseCommandLine`, `getCachedOrRefreshSync`, `getCachedOrRefresh`, `clearCredentialProcessCache`, `ResolvedCredentials` |
| `tests/ai/awsCredentials.test.ts` | Unit tests for profile waterfall, AWS_*_FILE overrides, config-file [profile X] syntax, merge logic | VERIFIED | 700 lines; 27 tests; all pass |
| `tests/ai/credentialProcess.test.ts` | Unit tests for parseCommandLine, spawnSync runner, cache TTL, concurrent coalescing, error paths | VERIFIED | 649 lines; 27 tests (exceeds plan's 20-test minimum); all pass |
| `src/ai/types.ts` | BedrockProviderConfig with sessionToken?: string field | VERIFIED | Line 74: `sessionToken?: string` with JSDoc present |
| `src/ai/providers/bedrock.ts` | sessionToken passthrough in access-keys branch | VERIFIED | Lines 59-61: conditional `opts.sessionToken = cfg.sessionToken` |
| `src/settings/SettingsStore.ts` | sanitizeBedrockProviderConfig with sessionToken shape-guard | VERIFIED | Line 499: `sessionToken: typeof r.sessionToken === 'string' ? r.sessionToken : ''` |
| `src/settings/SettingsTab.ts` | Renamed dropdown label + sessionToken input + updated helper text | VERIFIED | Line 447: 'Profile name'; Lines 497-509: Session token input; Line 471: D-04 helper text |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/ai/awsCredentials.ts` | `src/ai/credentialProcess.ts` | `getCachedOrRefreshSync(profileName, ...)` | WIRED | Line 27: `import { getCachedOrRefreshSync } from './credentialProcess'`; called at line 225 with profileName and merged.credential_process |
| `src/ai/providers/bedrock.ts` | `src/ai/awsCredentials.ts` | `resolveAwsCredentials(...)` | WIRED | Line 32: `import { resolveAwsCredentials } from '../awsCredentials'`; called at lines 53 and 67 (both dispatch branches) |
| `src/ai/providers/bedrock.ts` | `src/ai/types.ts` | `cfg.sessionToken` field | WIRED | bedrock.ts line 59 accesses `cfg.sessionToken`; field defined in types.ts line 74 |
| `src/settings/SettingsStore.ts` | `src/ai/types.ts` | `typeof r.sessionToken` shape-guard | WIRED | SettingsStore.ts line 499; field defined in types.ts line 74 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `credentialProcess.ts` getCachedOrRefreshSync | `creds` (ResolvedCredentials) | `runCredentialProcess` -> `spawnSync` -> `parseCredentialProcessOutput` | Yes — stdout from child process parsed as JSON | FLOWING |
| `awsCredentials.ts` resolveAwsCredentials | merged section | `safeReadFile(credPath)` + `safeReadFile(cfgPath)` -> `parseIniSection` | Yes — real fs reads via nodeRequire('fs') | FLOWING |
| `bedrock.ts` createBedrockModel | `opts.sessionToken` | `cfg.sessionToken` from `BedrockProviderConfig` | Yes — conditional passthrough (only when truthy) | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 27 awsCredentials tests pass | `npx vitest run tests/ai/awsCredentials.test.ts` | 27 passed in 280ms | PASS |
| All 27 credentialProcess tests pass | `npx vitest run tests/ai/credentialProcess.test.ts` | 27 passed in 40ms | PASS |
| Total 54 tests across both files | Combined run | 54 passed (2 test files) | PASS |
| TypeScript compiles cleanly | `npm run build` | Exit 0 — no type errors | PASS |

### Probe Execution

No `probe-*.sh` files declared or present for Phase 08.2. Spot-checks above serve as the behavioral verification layer.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| AIPROV-FUT-01 | 08.2-01-PLAN.md, 08.2-02-PLAN.md | Native @ai-sdk/amazon-bedrock integration with canonical AWS chain (extends — static + credential_process modes) | SATISFIED | Full D-17 waterfall implemented; AWS_PROFILE/DEFAULT_PROFILE/file overrides/[profile X] syntax/credential_process all functional. Note: AIPROV-FUT-01 is listed as a Future Requirement in REQUIREMENTS.md with no phase mapping in the traceability table — Phase 08.2 partially implements this future requirement (static + credential_process modes; SSO/assume-role remain deferred per roadmap out-of-scope). |
| AIPROV-02 | 08.2-01-PLAN.md, 08.2-02-PLAN.md | credential_process stdout never logged; stderr truncated to 200 chars; 30-second timeout (preserved) | SATISFIED | D-22: zero logger refs in credentialProcess.ts (verified by grep + test); D-08: `stderr.slice(0, 200)` at source; D-20: `timeout: 30_000` in spawnSync options |

**Note on AIPROV-FUT-01 traceability:** REQUIREMENTS.md lists this requirement under "Future Requirements" (line 83) with no entry in the traceability table (AIPROV-02 is mapped to Phase 07, not 08.2). The ROADMAP Phase 08.2 section explicitly states "AIPROV-FUT-01 (extends — canonical AWS credential chain parity for static + credential_process modes)" — meaning Phase 08.2 partially fulfills this future requirement. This is consistent intent, not a gap.

**Note on AIPROV-02 traceability:** AIPROV-02 is formally Complete at Phase 07 (REQUIREMENTS.md line 142). Phase 08.2's use of AIPROV-02 is a preservation constraint (credential_process security posture must not regress), not a new implementation. This is consistent with the ROADMAP annotation "(preserved)".

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | — | — | No TBD/FIXME/XXX markers found in any modified file |

Scanned files: `src/ai/awsCredentials.ts`, `src/ai/credentialProcess.ts`, `src/ai/providers/bedrock.ts`, `src/ai/types.ts`, `src/settings/SettingsStore.ts`, `src/settings/SettingsTab.ts`. Zero debt markers found.

Notable code quality: `return null` at awsCredentials.ts:79 (`readEnvCredentials` returns null when env vars absent) — this is an intentional design choice, not a stub; caller handles the null case correctly at line 200-201.

### Human Verification Required

None. All observable truths are programmatically verifiable. The phase is infrastructure/security-behavior focused (credential resolution, caching, wiring) with no new UI flows requiring human interaction testing.

### Gaps Summary

No gaps found. All 13 must-have truths are VERIFIED with direct codebase evidence:

- The D-17 waterfall is fully implemented and tested (54 tests, all passing).
- Key links are wired: awsCredentials.ts imports and calls getCachedOrRefreshSync from credentialProcess.ts; bedrock.ts calls resolveAwsCredentials from awsCredentials.ts.
- Security invariants D-08, D-20, D-22 are enforced in code and verified by dedicated tests.
- Settings UI changes (renamed label, sessionToken input, updated helper text) are present and verified.
- TypeScript build compiles cleanly with no new dependencies added.

---

_Verified: 2026-05-17T22:02:30Z_
_Verifier: Claude (gsd-verifier)_
