---
phase: 08.2-bedrock-canonical-default-chain
plan: 02
subsystem: ai-providers/bedrock
tags: [bedrock, settings-ui, sessionToken, credential-chain]
dependency_graph:
  requires: [08.2-01]
  provides: [sessionToken-field, profile-name-label, expanded-helper-text]
  affects: [src/ai/types.ts, src/ai/providers/bedrock.ts, src/settings/SettingsStore.ts, src/settings/SettingsTab.ts]
tech_stack:
  added: []
  patterns: [Pitfall-10-preserve-on-mode-switch, conditional-SDK-passthrough, eslint-disable-sentence-case]
key_files:
  created: []
  modified:
    - src/ai/types.ts
    - src/ai/providers/bedrock.ts
    - src/settings/SettingsStore.ts
    - src/settings/SettingsTab.ts
    - tests/ai/awsCredentials.test.ts
decisions:
  - "sessionToken conditional guard (if cfg.sessionToken) prevents empty string from overriding SDK env-var fallback"
  - "eslint-disable-next-line for STS acronym and AWS env-var literals in helper text (same pattern as Phase 08.1 Task 3)"
  - "Integration test uses vi.doMock/doUnmock to isolate SDK spy in the awsCredentials test file"
metrics:
  duration: "3m 46s"
  completed: "2026-05-18T01:55:01Z"
  tasks: 2
  files: 5
---

# Phase 08.2 Plan 02: Settings UI + sessionToken wiring Summary

**One-liner:** sessionToken passthrough in Bedrock adapter with renamed "Profile name" dropdown and expanded helper text reflecting credential_process + AWS_*_FILE support.

## What Was Done

### Task 1: Add sessionToken to types + adapter + shape-guard
- Added `sessionToken?: string` to `BedrockProviderConfig` interface
- Added conditional passthrough in bedrock.ts access-keys branch: `if (cfg.sessionToken) { opts.sessionToken = cfg.sessionToken; }`
- Added shape-guard in `sanitizeBedrockProviderConfig` to preserve sessionToken across mode switches
- Added `sessionToken: ''` default to `DEFAULT_PROVIDER_CONFIGS.bedrock`
- Verified logger redaction already covers sessionToken via `session` substring in REDACT regex

### Task 2: Rename dropdown label + add sessionToken UI input + update helper text
- Renamed dropdown option from `'SSO profile'` to `'Profile name'` (enum value unchanged — zero migration)
- Added Session token input under access-keys mode (type=password, lc-ai-input class, async onChange)
- Updated default-chain helper text (D-04) mentioning AWS_PROFILE, credential_process, AWS_*_FILE overrides
- Updated profile-name helper text (D-02) mentioning credential_process support and aws-vault/awsume
- Added eslint-disable-next-line markers for sentence-case rule on technical literals
- Added 2 integration tests verifying sessionToken round-trips through bedrock adapter (present + empty)

## Commits

| Task | Commit  | Message |
|------|---------|---------|
| 1    | 1fbd264 | feat(08.2-02): add sessionToken to BedrockProviderConfig + adapter + shape-guard |
| 2    | e061c37 | feat(08.2-02): rename dropdown label, add sessionToken UI input, update helper text |

## Verification Results

- `npm run build` — PASS (TypeScript compiles cleanly)
- `npm run check:bundle-size` — PASS (1101.9 KB, under 1.2 MB ceiling)
- `npm run lint` on `src/settings/SettingsTab.ts` — PASS (no violations)
- 51 tests across 3 test files — ALL PASS
  - tests/ai/awsCredentials.test.ts (27 tests)
  - tests/settings/SettingsStore.bedrock.test.ts (15 tests)
  - tests/ai/providers/bedrock.auth.test.ts (9 tests)

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None. All fields are wired end-to-end (types -> adapter -> settings store -> settings UI -> tests).

## Success Criteria Checklist

- [x] BedrockProviderConfig.sessionToken field exists and is typed as optional string
- [x] bedrock.ts access-keys branch passes sessionToken to SDK when present
- [x] sanitizeBedrockProviderConfig preserves sessionToken across mode switches
- [x] Settings dropdown shows "Profile name" (not "SSO profile") for third option
- [x] Settings shows optional Session token input under explicit-access-keys mode
- [x] Default-chain helper text mentions AWS_PROFILE + credential_process + AWS_*_FILE overrides
- [x] Profile-name helper text mentions credential_process support
- [x] Logger redaction covers sessionToken (pre-existing — verified)
- [x] Bundle under 1.2 MB ceiling (1101.9 KB)
- [x] npm run lint passes on modified source files
- [x] Full test suite green (51 tests)
