---
phase: 17-polish-edge-cases
plan: 08
type: execute
wave: 5
depends_on: ["17-01"]
files_modified:
  - src/main.ts
  - src/solve/resetCodeWithConfirm.ts
  - tests/main/resetCommand.childDispatch.test.ts
autonomous: true
requirements: [LANG-01, INDENT-04, ENTER-02, ENTER-03, ENTER-04, COMMENT-01, BRACKET-01, BRACKET-02, BRACKET-03, BRACKET-04]
requirements_addressed: [LANG-01, INDENT-04, ENTER-02, ENTER-03, ENTER-04, COMMENT-01, BRACKET-01, BRACKET-02, BRACKET-03, BRACKET-04]

must_haves:
  truths:
    - "Reset code on a Java problem note (lc-language frontmatter == 'java' AND fence opener tag == '```java') writes the JAVA starter snippet from detail.codeSnippets, regardless of settings.getDefaultLanguage() (which may be 'python3' globally)"
    - "The Phase 16 D-06 language priority chain (lc-language frontmatter > active fence opener tag > getDefaultLanguage) is honored at the new Phase 17 D-03 child-CM6 dispatch site — the chain that the Phase 16 reset-code-language-regression debug doc restored is NOT lost by the dispatch path swap"
    - "When neither frontmatter nor fence opener yields a slug (truly empty / new note), the helper falls back to settings.getDefaultLanguage() — preserving the legacy fallback path for non-LC notes"
    - "When detail.codeSnippets has no entry matching the resolved langSlug (e.g., LC API hasn't backfilled that language for this slug), the starter is empty string — same defensive behavior as the legacy code path; the Notice still fires"
    - "Phase 15 D-05 cm-z scope isolation invariant from Plan 17-01 is preserved — Reset still dispatches on the child with userEvent 'leetcode.reset.child' and no addToHistory.of(false); the existing parent-side mirror in childEditorSync.ts:108-114 carries addToHistory.of(false) on the parent"
  artifacts:
    - path: "src/solve/resetCodeWithConfirm.ts"
      provides: "Reset helper consults a caller-supplied resolveActiveLangSlug seam BEFORE falling back to settings.getDefaultLanguage(); priority chain documented in JSDoc"
      contains: "resolveActiveLangSlug"
    - path: "src/main.ts"
      provides: "resetCode wrapper supplies resolveActiveLangSlug that reads lc-language frontmatter first, falls back to active fence opener tag (parsed from the parent CM6 via findCodeFence), then to settings.getDefaultLanguage() — restores the Phase 16 D-06 priority chain at the post-D-03 call site"
      contains: "resolveActiveLangSlug"
    - path: "tests/main/resetCommand.childDispatch.test.ts"
      provides: "Two new it(...) blocks asserting the priority chain: (1) Reset on a Java note with lc-language: java AND fence opener ```java AND default 'python3' writes the Java starter; (2) Reset with lc-language unset, fence opener ```cpp, default 'python3' writes the C++ starter — both fail on current main and pass after fix"
      min_lines: 40
  key_links:
    - from: "src/solve/resetCodeWithConfirm.ts resetCodeWithConfirm body"
      to: "deps.resolveActiveLangSlug?.(deps.file)"
      via: "consult resolver BEFORE settings.getDefaultLanguage(); when resolver returns a non-empty slug, use it; when it returns null/undefined, fall back to default"
      pattern: "resolveActiveLangSlug"
    - from: "src/main.ts resetCode wrapper"
      to: "this.app.metadataCache.getFileCache(file).frontmatter['lc-language'] AND findCodeFence(parent CM6 state)"
      via: "frontmatter read first; fence opener tag parse second; default language third"
      pattern: "lc-language|findCodeFence"
    - from: "tests/main/resetCommand.childDispatch.test.ts"
      to: "the priority chain"
      via: "exercises resolveActiveLangSlug directly via the deps fixture; asserts the langSlug passed to forceInjectCodeSection equals the highest-priority resolved slug"
      pattern: "lc-language|java starter|cpp starter"
---

<objective>
Restore the Phase 16 D-06 language priority chain (`lc-language frontmatter > active fence opener tag > getDefaultLanguage`) at the Phase 17 D-03 child-CM6 dispatch site for Reset code. 17-UAT.md Test 10 (RESET-01) reported the regression: Reset wrote a Python starter into a Java problem note where chevron AND `lc-language` frontmatter both still said `java`. Source inspection of `src/solve/resetCodeWithConfirm.ts:138` confirms the helper unconditionally calls `deps.settings.getDefaultLanguage()` (which is `python3` in the user's settings) — the Phase 16 reset-code-language-regression fix's resolver chain was not preserved through the Phase 17 D-03 refactor (Plan 17-01).

Fix shape: Add an optional `resolveActiveLangSlug?: (file: TFile) => string | undefined` seam on `ResetCodeWithConfirmDeps`. The helper consults it FIRST and falls back to `settings.getDefaultLanguage()` only when the resolver returns null/undefined. The production caller in `src/main.ts:resetCode` supplies the resolver implementing the canonical chain: read `lc-language` from `metadataCache.getFileCache(file).frontmatter` first; if absent, parse the active fence opener tag via `findCodeFence(parent CM6 state)` and the same regex used by `readActiveFenceSlug` at `src/main.ts:2613-2641`; if still absent, return undefined (helper falls through to default).

Purpose: Closes 17-UAT.md Issue 2 (Test 10) — Reset writing wrong-language starter when default differs from the note's language. Does NOT regress Plan 17-01's child-CM6 dispatch + cm-z scope isolation work — those invariants are orthogonal to language resolution and preserved verbatim.

Output: Patched `resetCodeWithConfirm` with the new resolver seam, patched `resetCode` wrapper in `src/main.ts` supplying the chain, regression test that fails on current main and passes after fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-polish-edge-cases/17-CONTEXT.md
@.planning/phases/17-polish-edge-cases/17-UAT.md
@.planning/phases/17-polish-edge-cases/17-01-PLAN.md
@.planning/phases/17-polish-edge-cases/17-01-SUMMARY.md
@.planning/debug/reset-code-language-regression.md
@CLAUDE.md

<interfaces>
<!-- Source-of-truth shapes the executor must reuse verbatim. -->

src/solve/resetCodeWithConfirm.ts:130-160 (current helper body — the regression site):
  export async function resetCodeWithConfirm(deps: ResetCodeWithConfirmDeps): Promise<void> {
    const currentBody = await readCurrentBody(deps.app, deps.file);
    const detail = deps.settings.getProblemDetail(deps.slug);
    const langSlug = deps.settings.getDefaultLanguage();   // <-- BUG: ignores fm + fence opener
    const starter = detail?.codeSnippets?.find((s) => s.langSlug === langSlug)?.code ?? '';
    // ...handle / vault.process branches; both already use langSlug...
  }

ResetCodeWithConfirmDeps current shape (src/solve/resetCodeWithConfirm.ts:88-128):
  export interface ResetCodeWithConfirmDeps {
    app: App;
    file: TFile;
    slug: string;
    settings: {
      getProblemDetail(slug: string): DetailCacheEntry | null;
      getDefaultLanguage(): string;
    };
    confirm: () => Promise<boolean>;
    notify: (message: string) => void;
    getDispatchHandle?: (file: TFile) => ResetCodeDispatchHandle | null;
  }

src/main.ts:2916-2977 (resetCode wrapper — the production caller; this plan extends it):
  private async resetCode(file: TFile, slug: string): Promise<void> {
    await resetCodeWithConfirm({
      app: this.app, file, slug, settings: this.settings,
      confirm: () => ..., notify: (msg) => ...,
      getDispatchHandle: (targetFile) => { ... child-first lookup ... },
      // <-- resolveActiveLangSlug must be supplied here.
    });
  }

src/main.ts:2613-2641 (readActiveFenceSlug — Plan 17-04 helper; PATTERN to mirror, NOT call directly because that helper falls back to metadataCache lc-language as its own internal fallback. The new resolveActiveLangSlug seam needs to differentiate "fm explicitly set" from "fence opener falls back to fm" so the chain can express explicit-fm-priority correctly):
  private readActiveFenceSlug(file: { path: string }): string | undefined {
    // 1. Active MarkdownView CM6 state -> findCodeFence -> opener regex
    // 2. Fallback: metadataCache.lc-language
    // 3. Return undefined
  }

Phase 16 reset-code-language-regression fix (canonical chain — must be reproduced):
  Priority order:
    1. metadataCache.getFileCache(file).frontmatter['lc-language']  -- highest
    2. fence opener tag from parent CM6 state (parsed via findCodeFence + opener-line regex)
    3. settings.getDefaultLanguage()                                -- lowest

Opener regex (must match readActiveFenceSlug's pattern at src/main.ts:2622):
  /^\s*```\s*(\S+)\s*$/

src/main.ts existing imports already include MarkdownView, TFile, findCodeFence (verified — used by readActiveFenceSlug). No new imports required.

Plan 17-01 invariants (DO NOT modify):
  - Reset dispatch on child with userEvent 'leetcode.reset.child' (no addToHistory.of(false))
  - vault.process fallback when no child registered (D-04)
  - extractFenceBodyFromFullNote helper unchanged
  - CLAUDE.md ## Conventions block — no new userEvent introduced; no update needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing regression test for Reset language priority chain (17-UAT.md Issue 2 / Test 10)</name>
  <files>tests/main/resetCommand.childDispatch.test.ts</files>
  <read_first>
    - tests/main/resetCommand.childDispatch.test.ts (full file — Plan 17-01 created it; this task EXTENDS the existing describe block with two new it(...) blocks)
    - tests/main/resetCommand.test.ts (full file — pattern source for fixture wiring; existing tests do NOT exercise resolveActiveLangSlug because it didn't exist pre-Phase-17)
    - src/solve/resetCodeWithConfirm.ts (lines 88-160 — current helper signature + body; identifies the langSlug derivation site at line 138)
    - tests/helpers/obsidian-stub.ts (full file — confirms vi.mock('obsidian') + makeStateForLockTests + Transaction stubs; this task uses the same fixture shape as Plan 17-01's test)
    - 17-UAT.md (Test 10 RESET-01 — the bug report; the test asserts the post-fix behavior described there)
    - .planning/debug/reset-code-language-regression.md (Phase 16 fix — the canonical chain this plan restores)
  </read_first>
  <behavior>
    - Test 5 (NEW — adjust the number to follow the existing block; current file has Tests 1-4 from Plan 17-01) ("Reset honors lc-language frontmatter over default language — Java fm + Python default → Java starter"):
      - Build deps with: `slug: 'two-sum'`, `settings.getProblemDetail('two-sum')` returns a detail with codeSnippets containing both `{ langSlug: 'java', code: 'class Solution { /* JAVA */ }' }` and `{ langSlug: 'python3', code: 'class Solution: # PYTHON' }`, `settings.getDefaultLanguage()` returns `'python3'`.
      - Supply `resolveActiveLangSlug: (file) => 'java'` (simulates the production caller's chain returning the fm value).
      - Supply `getDispatchHandle` returning a child handle whose `replaceFullBody` captures the `next` argument.
      - Call `resetCodeWithConfirm(deps)`.
      - Assert: the captured `next` string contains `class Solution { /* JAVA */ }` AND does NOT contain `# PYTHON`. This proves the langSlug used inside `forceInjectCodeSection` was `'java'`, not the default `'python3'`.
    - Test 6 (NEW) ("Reset honors fence opener tag when frontmatter is unset — fence ```cpp + Python default → C++ starter"):
      - Build deps with detail having `cpp` and `python3` codeSnippets; default `python3`.
      - Supply `resolveActiveLangSlug: (file) => 'cpp'` (simulates fm absent → fence opener resolution).
      - Supply `getDispatchHandle` returning a child handle that captures `next`.
      - Call `resetCodeWithConfirm(deps)`.
      - Assert: captured `next` contains the C++ starter, not the Python one.
    - Test 7 (NEW) ("Reset falls back to getDefaultLanguage when resolver returns undefined"):
      - Build deps with detail having `python3` codeSnippet; default `python3`.
      - Supply `resolveActiveLangSlug: (file) => undefined`.
      - Supply `getDispatchHandle` returning a child handle.
      - Call `resetCodeWithConfirm(deps)`.
      - Assert: captured `next` contains the Python starter (fallback to default preserved).
    - Test 8 (NEW) ("Reset works WITHOUT resolveActiveLangSlug supplied — backward compat"):
      - Same setup as Test 7 but OMIT `resolveActiveLangSlug` entirely (undefined field).
      - Assert: captured `next` contains the Python starter (legacy default-only path preserved when caller does not supply the seam — protects existing tests/resetCommand.test.ts cases).
    - The four new tests MUST FAIL on current main (`npm test -- tests/main/resetCommand.childDispatch.test.ts`) — Tests 5-7 fail because the helper hardcodes `settings.getDefaultLanguage()`; Test 8 passes coincidentally because the bug IS the default-only behavior. Tests 5-6-7 prove the chain works after fix; Test 8 is a regression guard for backward compatibility.
  </behavior>
  <action>
    Edit `tests/main/resetCommand.childDispatch.test.ts`. Inside the existing top-level `describe(...)` block (created by Plan 17-01), append four new `it(...)` blocks per the Behavior contract. For each: build a deps object from the existing fixture pattern in the file (re-use `makeMockChildView`, `makeMockRegistry` if defined; otherwise inline them following `tests/main/childEditorSync.test.ts:87-109`). Pass a custom `settings` object with both `getProblemDetail` (returning a `DetailCacheEntry`-shaped object — minimal: `{ codeSnippets: [{ langSlug: 'java', code: '...JAVA...' }, ...] }`) and `getDefaultLanguage` (returning `'python3'`). Pass `resolveActiveLangSlug` per the test case. Use `vi.fn()` for `confirm` and `notify`; resolve `confirm` to `true` so the helper proceeds to write. Capture `next` via the dispatch handle's `replaceFullBody` spy. Run `npm test -- tests/main/resetCommand.childDispatch.test.ts` and confirm Tests 5-7 are RED before commit.
  </action>
  <verify>
    <automated>npm test -- tests/main/resetCommand.childDispatch.test.ts 2>&1 | grep -E "FAIL|✗|×" ; test ${PIPESTATUS[1]} -eq 0 || echo "RED state confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/main/resetCommand.childDispatch.test.ts` contains at least 8 `it(...)` blocks total (4 from Plan 17-01 + 4 new)
    - File contains the literal strings `resolveActiveLangSlug`, `class Solution { /* JAVA */ }` (or equivalent Java starter marker), and `python3`
    - `npm test -- tests/main/resetCommand.childDispatch.test.ts` exits non-zero on current main (Tests 5-7 RED)
    - Test 5 explicitly captures the dispatched `next` string and asserts it contains the Java marker AND does NOT contain the Python marker (proves langSlug switched correctly)
    - `grep -c "resolveActiveLangSlug" tests/main/resetCommand.childDispatch.test.ts` returns >= 4 (one per new test)
  </acceptance_criteria>
  <done>Four regression tests added in RED state covering the priority chain + backward-compat. Will turn GREEN after Tasks 2-3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add resolveActiveLangSlug seam to resetCodeWithConfirm — preserves backward compat (Phase 16 D-06 chain)</name>
  <files>src/solve/resetCodeWithConfirm.ts</files>
  <read_first>
    - src/solve/resetCodeWithConfirm.ts (full file — current ResetCodeWithConfirmDeps interface lines 88-128; helper body lines 130-160; the `langSlug` derivation at line 138 is the regression site)
    - tests/main/resetCommand.childDispatch.test.ts (Task 1 output — assertions to satisfy)
    - tests/main/resetCommand.test.ts (full file — confirm existing tests do not pass `resolveActiveLangSlug`; the new field MUST be optional so these legacy tests stay GREEN)
    - .planning/debug/reset-code-language-regression.md (Phase 16 chain rationale)
  </read_first>
  <behavior>
    - Add a new optional field on `ResetCodeWithConfirmDeps`:
      `resolveActiveLangSlug?: (file: TFile) => string | undefined;`
      placed after `getDispatchHandle?` to keep the diff small and the related D-03/D-06 contract co-located.
    - Update JSDoc on the new field documenting:
      - The Phase 16 D-06 canonical priority chain (lc-language frontmatter > fence opener tag > getDefaultLanguage).
      - Returning `undefined` falls back to `settings.getDefaultLanguage()`.
      - Returning a non-empty slug skips the default fallback.
      - Reference: `.planning/debug/reset-code-language-regression.md`, 17-UAT.md Test 10 (Issue 2), CONTEXT D-06.
    - Replace the langSlug derivation at line 138:
      `const langSlug = deps.settings.getDefaultLanguage();`
      with:
      ```
      // Phase 17 gap-closure (17-08 — restores Phase 16 D-06 priority chain at
      // the Phase 17 D-03 child-dispatch site). Caller supplies the resolver
      // implementing: lc-language fm > fence opener tag > default. When the
      // resolver returns undefined or null, fall back to the legacy default.
      const resolved = deps.resolveActiveLangSlug?.(deps.file);
      const langSlug =
        typeof resolved === 'string' && resolved.length > 0
          ? resolved
          : deps.settings.getDefaultLanguage();
      ```
    - DO NOT modify any other line of the helper. The starter lookup, forceInjectCodeSection call, dispatch-handle branching, and vault.process fallback all consume `langSlug` already — this fix is a one-line derivation change.
    - Existing `tests/main/resetCommand.test.ts` continues to pass (those tests don't supply `resolveActiveLangSlug` → backward-compat default-only path).
    - Plan 17-01 invariants preserved: child-CM6 dispatch path, userEvent `'leetcode.reset.child'`, no `addToHistory.of(false)`.
    - Tests 5-7 from Task 1 turn GREEN. Test 8 (backward-compat) stays GREEN.
  </behavior>
  <action>
    Edit `src/solve/resetCodeWithConfirm.ts`. In the `ResetCodeWithConfirmDeps` interface (lines 88-128), append the new `resolveActiveLangSlug?` field with full JSDoc per Behavior. In the helper body at line ~138, replace the single-line `langSlug` derivation with the four-line pattern from Behavior. DO NOT touch the imports, the `extractFenceBodyFromFullNote` export, the `ResetCodeDispatchHandle` interface, or any other helper logic. Run `npm test -- tests/main/resetCommand.childDispatch.test.ts tests/main/resetCommand.test.ts` and confirm both files GREEN. Run `npm run build`.
  </action>
  <verify>
    <automated>npm test -- tests/main/resetCommand.childDispatch.test.ts tests/main/resetCommand.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/main/resetCommand.childDispatch.test.ts` exits 0 (8/8 tests pass)
    - `npm test -- tests/main/resetCommand.test.ts` exits 0 (existing legacy tests preserved)
    - `npm run build` exits 0
    - `grep -c "resolveActiveLangSlug" src/solve/resetCodeWithConfirm.ts` returns >= 3 (interface field + JSDoc + helper-body call)
    - The exact string `deps.resolveActiveLangSlug?.(deps.file)` appears in the file (verifiable via `grep -F "deps.resolveActiveLangSlug?.(deps.file)" src/solve/resetCodeWithConfirm.ts` returning >= 1)
    - The legacy line `const langSlug = deps.settings.getDefaultLanguage();` is NOT present at the helper's body (verifiable via `grep -c "const langSlug = deps.settings.getDefaultLanguage();" src/solve/resetCodeWithConfirm.ts` returning 0)
    - The line `deps.settings.getDefaultLanguage()` is still referenced (the fallback path) — `grep -c "deps.settings.getDefaultLanguage()" src/solve/resetCodeWithConfirm.ts` returns >= 1
  </acceptance_criteria>
  <done>Helper consults caller-supplied resolver; backward compat preserved when caller omits the seam; tests 5-7 GREEN; tests 1-4 + 8 stay GREEN; build clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire resolveActiveLangSlug at the resetCode caller in src/main.ts — frontmatter > fence opener > default</name>
  <files>src/main.ts</files>
  <read_first>
    - src/main.ts (lines 2916-2977 — the resetCode wrapper; supplies confirm, notify, getDispatchHandle today; this task adds resolveActiveLangSlug to the same deps object)
    - src/main.ts (lines 2613-2641 — readActiveFenceSlug; the existing helper is the structural template for the fence-opener-parse logic; do NOT call it directly because its own metadataCache fallback short-circuits the explicit-fm priority)
    - src/main.ts (lines 80-130 — confirm imports include MarkdownView, TFile, findCodeFence, EditorView; verified all already imported in current file)
    - src/main/codeActionsEditorExtension.ts (full file — confirms findCodeFence export shape; specifically the openerLine field used by readActiveFenceSlug)
    - tests/main/resetCommand.childDispatch.test.ts (Task 1 output — Tests 5-7 must be GREEN after this task)
    - .planning/debug/reset-code-language-regression.md (Phase 16 chain rationale)
  </read_first>
  <behavior>
    - In the `resetCode` wrapper at `src/main.ts:2916-2977`, add a new field on the deps object passed to `resetCodeWithConfirm`:
      ```
      resolveActiveLangSlug: (targetFile: TFile): string | undefined => {
        // Phase 17 gap-closure (17-08, 17-UAT.md Issue 2) — restore Phase 16 D-06
        // canonical priority chain: lc-language frontmatter > fence opener tag >
        // settings.getDefaultLanguage(). The Phase 17 D-03 dispatch path swap
        // (Plan 17-01) inadvertently dropped this chain because the helper
        // hardcoded settings.getDefaultLanguage() — see
        // .planning/debug/reset-code-language-regression.md for the original fix.
        try {
          // Priority 1 — lc-language frontmatter (canonical).
          const fm = this.app.metadataCache.getFileCache(targetFile)
            ?.frontmatter as Record<string, unknown> | undefined;
          const fmLang = fm?.['lc-language'];
          if (typeof fmLang === 'string' && fmLang.length > 0) return fmLang;

          // Priority 2 — active fence opener tag. Active MarkdownView only.
          const view = this.app.workspace.getActiveViewOfType(MarkdownView);
          if (view && view.file && view.file.path === targetFile.path) {
            const cm = (view.editor as unknown as { cm: EditorView }).cm;
            const fence = findCodeFence(cm.state);
            if (fence) {
              const openerText = cm.state.doc.line(fence.openerLine).text;
              const m = /^\s*```\s*(\S+)\s*$/.exec(openerText);
              if (m && m[1]) return m[1];
            }
          }
        } catch {
          // Defensive — fall through to undefined → helper uses default.
        }
        // Priority 3 — let the helper fall back to settings.getDefaultLanguage().
        return undefined;
      },
      ```
    - DO NOT call the existing `this.readActiveFenceSlug(...)` helper directly — that helper's internal metadataCache fallback (line 2632-2640) collapses the priority distinction by treating fence-opener-fallback and fm-fallback as the same source. The new resolver's Priority 1 must be EXPLICIT-fm-only so unset fm correctly drops to fence opener.
    - The resolver MUST consult `metadataCache.getFileCache` for the FRONTMATTER (Priority 1), NOT the cached body. This matches the Phase 16 reset-code-language-regression fix's source-of-truth.
    - DO NOT change the `getDispatchHandle` block, the `confirm` callback, the `notify` callback, the structure of the `resetCodeWithConfirm` call, or any unrelated wiring. The diff is purely additive: one new field on the deps object.
    - Tests 5-7 from Task 1 turn GREEN. Plan 17-01's Tests 1-4 stay GREEN. `tests/main/resetCommand.test.ts` legacy tests stay GREEN.
  </behavior>
  <action>
    Edit `src/main.ts:2916-2977`. Inside the `resetCodeWithConfirm({...})` call's deps object, add the `resolveActiveLangSlug` field per Behavior. Place it AFTER `getDispatchHandle` to keep the D-03/D-06 contract co-located. The closure captures `this` for `this.app` access — confirm `resetCode` is a regular method (not an arrow function inside a closure that loses `this`) by inspecting the surrounding `private async resetCode(file: TFile, slug: string): Promise<void>` declaration. Run `npm test -- tests/main/resetCommand.childDispatch.test.ts` and confirm 8/8 GREEN. Run `npm test` (full suite) and confirm zero new failures (pre-existing bundle-size test failures from 17-04-SUMMARY.md "Deferred Issues" remain). Run `npm run build`.
  </action>
  <verify>
    <automated>npm test -- tests/main/resetCommand.childDispatch.test.ts && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- tests/main/resetCommand.childDispatch.test.ts` exits 0 (8/8 GREEN)
    - `npm test` (full suite) exits 0 (no NEW regressions; pre-existing bundle-size foundation tests are out of scope per 17-04-SUMMARY.md)
    - `npm run build` exits 0
    - `grep -c "resolveActiveLangSlug" src/main.ts` returns >= 1 (the new field on the deps object — note the field NAME may also appear in JSDoc on the helper imports if the executor adds doc; minimum is the deps-object field)
    - `grep -c "metadataCache.getFileCache" src/main.ts` increments by at least 1 vs baseline (the new resolver calls it for Priority 1)
    - `grep -c "findCodeFence" src/main.ts` is unchanged or increments by 1 (the resolver may reuse the import; either count is acceptable)
    - The new resolver's body contains the literal strings `'lc-language'` and `findCodeFence` (Priority 1 + Priority 2 sources)
    - `grep -c "leetcode.reset.child" src/main.ts` is unchanged from Plan 17-01 baseline (Reset dispatch userEvent is preserved)
  </acceptance_criteria>
  <done>resetCode wrapper supplies the priority-chain resolver; Tests 5-7 GREEN; Plan 17-01 invariants preserved; full suite GREEN minus pre-existing bundle-size failures; build clean.</done>
</task>

</tasks>

<verification>
- The fix is purely additive at the helper's `langSlug` derivation site and at the production caller's deps object — Plan 17-01's child-CM6 dispatch + cm-z scope isolation work is preserved verbatim.
- No new userEvent string is introduced (CLAUDE.md ## Conventions block does NOT need an update for this plan).
- Backward compatibility for `tests/main/resetCommand.test.ts` (legacy fixture without `resolveActiveLangSlug`) is preserved by making the new field OPTIONAL — the `?? deps.settings.getDefaultLanguage()` fallback fires exactly as before.
- The Phase 16 D-06 chain is restored: fm > fence opener > default. Test 5 covers the fm-priority case (Java fm + Python default → Java); Test 6 covers the fence-opener fallback (no fm + cpp opener → cpp); Test 7 covers the default fallback (no fm, no fence → python3); Test 8 covers backward-compat (omit resolver entirely → default).
- 17-UAT.md Test 10 (RESET-01) will be re-run by the user after this plan lands; the Java-fm + Python-default case must produce a Java starter post-fix.
</verification>

<success_criteria>
- `resetCodeWithConfirm` consults `deps.resolveActiveLangSlug?.(deps.file)` BEFORE falling back to `deps.settings.getDefaultLanguage()`
- `src/main.ts:resetCode` supplies the resolver implementing the canonical Phase 16 D-06 chain (fm > fence opener > default)
- Reset on a Java problem note with `lc-language: java` writes the Java starter even when default is `python3`
- Plan 17-01's child-CM6 dispatch + cm-z scope isolation invariants preserved (8/8 GREEN in tests/main/resetCommand.childDispatch.test.ts)
- Backward compat for legacy callers preserved (tests/main/resetCommand.test.ts GREEN)
- Build clean; lint clean
</success_criteria>

<output>
Create `.planning/phases/17-polish-edge-cases/17-08-SUMMARY.md` when done summarizing:
- Files modified with line numbers (resetCodeWithConfirm.ts:~88-160, src/main.ts:~2916-2977)
- Test count delta (4 new it(...) blocks in resetCommand.childDispatch.test.ts; 8/8 GREEN total)
- Priority chain restored: fm > fence opener > default
- Confirmation that Plan 17-01 child-CM6 dispatch + cm-z invariants are preserved
- Manual UAT reference (17-UAT.md Test 10 to be re-verified post-merge)
</output>
</content>
</invoke>