---
phase: 08-ai-debug
plan: 04
type: execute
wave: 4
depends_on:
  - 08-01
  - 08-02
  - 08-03
files_modified:
  - src/main/codeBlockButtonRow.ts
  - src/main.ts
  - src/notes/HeadingRegion.ts
  - tests/main/codeBlockButtonRow.test.ts
  - tests/main/aiDebugCommand.test.ts
autonomous: true
requirements:
  - AIDBG-01
must_haves:
  truths:
    - "Code-fence button row has 3 children with no chevron-prefix (was 2)"
    - "Code-fence button row has 4 children with chevron-prefix (was 3)"
    - "Edit Mode CM6 widget AND Reading Mode post-processor BOTH render the AI Debug button"
    - "Palette command 'ai-debug' is registered with editorCheckCallback that returns false for non-lc-slug notes"
    - "openAIDebug(slug) is the single entrypoint; resolves problem MD + ## Code + language + verdict + opens AIStreamModal"
    - "LeetCodePlugin instantiates LastVerdictStore in onload and registers onVerdict callback on submissionOrchestrator"
  artifacts:
    - path: src/main/codeBlockButtonRow.ts
      provides: "3rd button (AI: Debug) appended after Submit; CodeBlockButtonRowHost gains aiDebugFromActive()"
      contains: "leetcode-code-action-ai-debug"
    - path: src/main.ts
      provides: "openAIDebug(slug) method + LastVerdictStore field + ai-debug palette command + orchestrator onVerdict registration"
      contains: "openAIDebug"
    - path: tests/main/aiDebugCommand.test.ts
      provides: "Tests for palette command frontmatter guard + invocation routing"
      min_lines: 40
  key_links:
    - from: "src/main/codeBlockButtonRow.ts"
      to: "plugin.aiDebugFromActive()"
      via: "click event handler on the 3rd button"
      pattern: "aiDebugFromActive"
    - from: "src/main.ts:openAIDebug"
      to: "new AIStreamModal(app, { provider, prompt, aiClient, disclosureCopy: withDebugBullet(DISCLOSURE_BASE_COPY) })"
      via: "single-entrypoint method called from 3 surfaces"
      pattern: "AIStreamModal"
    - from: "src/main.ts:onload"
      to: "this.lastVerdictStore = new LastVerdictStore()"
      via: "field instantiated alongside EphemeralTabStore"
      pattern: "new LastVerdictStore"
---

<objective>
Wire the user-visible AI Debug entrypoints from two of the three surfaces: (1) the 3rd button in the `## Code` fence button row (Edit Mode CM6 widget AND Reading Mode post-processor — both inherit via `buildCodeBlockButtonRow`), and (2) the `ai-debug` palette command guarded by `lc-slug` frontmatter. Both delegate to a SINGLE host method `LeetCodePlugin.openAIDebug(slug)` that assembles the prompt, opens AIStreamModal, and threads in the composed disclosure copy.

Purpose: This plan ships AIDBG-01 to the user. The fence-row button is the discovery surface ("Where do I click?"), the palette command is the muscle-memory surface ("Cmd-P → ai-debug"). Both routes go through `openAIDebug(slug)` — the single source of truth that resolves problem markdown, reads `## Code` content, fetches the last verdict from LastVerdictStore, and opens AIStreamModal. The plugin onload also instantiates LastVerdictStore and registers the orchestrator's onVerdict callback (Plan 08-01 deps + Plan 08-03 modal both ship; this plan glues them together).

Output: 3rd button in `buildCodeBlockButtonRow` + palette command + `openAIDebug(slug)` + `aiDebugFromActive()` host method + LastVerdictStore field + onVerdict callback registration + extended fence-row test (2→3 / 3→4) + new palette-command tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ai-debug/08-CONTEXT.md
@.planning/phases/08-ai-debug/08-RESEARCH.md
@.planning/phases/08-ai-debug/08-PATTERNS.md
@.planning/phases/08-ai-debug/08-VALIDATION.md
@.planning/phases/08-ai-debug/08-UI-SPEC.md
@.planning/phases/08-ai-debug/08-01-SUMMARY.md
@.planning/phases/08-ai-debug/08-02-SUMMARY.md
@.planning/phases/08-ai-debug/08-03-SUMMARY.md
@CLAUDE.md

@src/main/codeBlockButtonRow.ts
@src/main/codeActionsEditorExtension.ts
@src/main/codeActionsPostProcessor.ts
@src/main.ts
@src/ai/AIClient.ts
@src/ai/AIStreamModal.ts
@src/ai/disclosure.ts
@src/ai/buildDebugPrompt.ts
@src/ai/types.ts
@src/solve/lastVerdictStore.ts
@src/solve/codeExtractor.ts
@src/notes/HeadingRegion.ts
@src/notes/NoteTemplate.ts
@tests/main/codeBlockButtonRow.test.ts

<interfaces>
<!-- Key contracts the executor needs. -->

From src/main/codeBlockButtonRow.ts:29-66 (CURRENT — 2-button row to extend):
```typescript
export interface CodeBlockButtonRowHost {
  runFromActive(): void | Promise<void>;
  submitFromActive(): void | Promise<void>;
}
export function buildCodeBlockButtonRow(
  doc: Document,
  plugin: Plugin & CodeBlockButtonRowHost,
  opts: CodeBlockButtonRowOptions = {},
): HTMLDivElement;
```

From src/main.ts:152-310 (LeetCodePlugin.onload — relevant fields and instantiation order):
- Line 162: `aiClient!: AIClient;` field declaration
- Line 256: `this.ephemeralTabs = new EphemeralTabStore(this);` (LastVerdictStore goes near here)
- Line 270-273: `this.aiClient = new AIClient(this.settings, (provider, cfg) => this.requireAIDisclosure(provider, cfg));`
- Line 425-436: `addCommand({ id: 'submit', name: 'Submit', editorCheckCallback })` — the palette-command precedent

From src/main.ts:425-436 (Submit palette-command shape — verbatim mirror for ai-debug):
```typescript
this.addCommand({
  id: 'submit',
  name: 'Submit',
  editorCheckCallback: (checking, _editor, view) => {
    const file = view.file;
    if (!file) return false;
    const fm = this.app.metadataCache.getFileCache(file)?.frontmatter as Record<string, unknown> | undefined;
    if (!isValidSlug(fm?.['lc-slug'])) return false;
    if (!checking) { void this.submitFromActive(); }
    return true;
  },
});
```

From src/solve/codeExtractor.ts (extractFirstFencedBlock — used to read ## Code):
```typescript
export function extractFirstFencedBlock(noteBody: string): { lang: string; code: string } | null;
```

From src/ai/AIStreamModal.ts (Plan 08-03):
```typescript
export interface AIStreamModalArgs {
  provider: AIProvider;
  prompt: string;
  aiClient: AIClient;
  disclosureCopy?: { willSend: readonly string[]; neverSends: readonly string[] };
}
export class AIStreamModal extends Modal {
  constructor(app: App, args: AIStreamModalArgs);
}
```

From src/ai/disclosure.ts (Plan 08-03):
```typescript
export const DISCLOSURE_BASE_COPY: { willSend: readonly string[]; neverSends: readonly string[] };
export function withDebugBullet(base): { willSend: readonly string[]; neverSends: readonly string[] };
```

From src/ai/buildDebugPrompt.ts (Plan 08-03):
```typescript
export function buildDebugPrompt(args: {
  problemMd: string;
  code: string;
  language: string;
  lastVerdict?: LastVerdict;
}): string;
```

From src/solve/lastVerdictStore.ts (Plan 08-01):
```typescript
export class LastVerdictStore {
  set(slug: string, v: LastVerdict): void;
  get(slug: string): LastVerdict | undefined;
  clear(): void;
  dispose(): void;
}
```

From tests/main/codeBlockButtonRow.test.ts (CURRENT — assertions to bump):
```typescript
// no-prefix: row.children.length === 2 (Run + Submit)
// chevron-prefix: row.children.length === 3 (chevron + Run + Submit)
```
After Plan 08-04: no-prefix → 3 (Run + Submit + AI: Debug), chevron-prefix → 4.

Locked verbatim (UI-SPEC §"Copywriting Contract"):
- Button label: `AI: Debug`
- Button class: `leetcode-code-action-ai-debug`
- Palette command ID: `ai-debug`
- Palette command label: `AI: Debug current code`
- DOM order: `[chevron-prefix?] [Run] [Submit] [AI: Debug]` — AI button is ALWAYS last child.
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend buildCodeBlockButtonRow with the 3rd AI Debug button + bump fence-row tests</name>
  <files>src/main/codeBlockButtonRow.ts, tests/main/codeBlockButtonRow.test.ts</files>
  <read_first>
    - src/main/codeBlockButtonRow.ts (lines 29-66 — current 2-button factory; CodeBlockButtonRowHost interface to extend)
    - src/main/codeActionsEditorExtension.ts (Edit Mode CM6 widget — calls buildCodeBlockButtonRow with chevron-prefix factory; verify Phase 08 inherits the 3rd button automatically)
    - src/main/codeActionsPostProcessor.ts (Reading Mode post-processor — calls buildCodeBlockButtonRow without prefix; verify same)
    - tests/main/codeBlockButtonRow.test.ts (lines 29-67 no-prefix path + 69-80 chevron-prefix path — assertions to bump 2→3 and 3→4)
    - .planning/phases/08-ai-debug/08-PATTERNS.md (§"src/main/codeBlockButtonRow.ts" — locked extension shape)
    - .planning/phases/08-ai-debug/08-UI-SPEC.md (§"Surface 1: Code-fence button row" + §"Bundle Constraint" — locked DOM order, class, label)
    - .planning/phases/08-ai-debug/08-CONTEXT.md (decision C — 3 surfaces; fence-row contract)
    - styles.css (lines 971-988 — `.leetcode-code-action-run, .leetcode-code-action-submit` token rule; AI button class joins this comma group)
  </read_first>
  <behavior>
    - `CodeBlockButtonRowHost` interface gains a 3rd method: `aiDebugFromActive(): void | Promise<void>`. Optional inline-doc references the host method on LeetCodePlugin.
    - `buildCodeBlockButtonRow` appends a 3rd button after the Submit button. Class `leetcode-code-action-ai-debug`. Text content `AI: Debug`. Click handler: `e.preventDefault(); e.stopPropagation(); void plugin.aiDebugFromActive();` (mirrors Run/Submit verbatim — no event leakage to underlying CM6 doc).
    - DOM order locked: `[prefix?][Run][Submit][AI: Debug]`. AI button is ALWAYS the LAST child, regardless of whether prefix is present.
    - Test contract: no-prefix path bumps `row.children.length === 2` → `=== 3` AND adds 3rd-child class assertion (`leetcode-code-action-ai-debug`). Chevron-prefix path bumps 3 → 4 AND adds 4th-child assertion at index 3.
    - `withHostMethods()` test helper extends to include `aiDebugFromActive: vi.fn()` field.
    - Click on the AI Debug button calls `plugin.aiDebugFromActive` exactly once (asserted via vi.fn().mock.calls.length).
    - Both Edit Mode CM6 widget AND Reading Mode post-processor surface the button automatically — no separate wiring needed (single factory consumed by both).
    - The CSS class `.leetcode-code-action-ai-debug` extends the existing comma-grouped rule at styles.css:971-988 (Plan 08-03 already added this CSS — Plan 08-04 just emits the className on the new button element).
  </behavior>
  <action>
Edit `src/main/codeBlockButtonRow.ts`. Extend `CodeBlockButtonRowHost` interface to include `aiDebugFromActive(): void | Promise<void>`. After the Submit button block (currently the last child append), insert the 3rd button block (mirrors Run/Submit shape verbatim — `doc.createElement('button')` + className + textContent + click listener with preventDefault/stopPropagation/void plugin.method() + appendChild). Class: `leetcode-code-action-ai-debug`. Text: `AI: Debug` (verbatim).

Edit `tests/main/codeBlockButtonRow.test.ts`:
- In `withHostMethods()` (lines 17-27 area), add `aiDebugFromActive: overrides?.aiDebugFromActive ?? vi.fn()` to the plugin shape. Update the overrides interface if it has typed fields.
- In the no-prefix test (lines 29-38 area), bump `expect(row.children.length).toBe(2)` to `.toBe(3)`. Add a new assertion: `expect(row.children[2]!.classList.contains('leetcode-code-action-ai-debug')).toBe(true)`.
- In the chevron-prefix test (lines 69-80 area), bump `expect(row.children.length).toBe(3)` to `.toBe(4)`. Add a new assertion: `expect(row.children[3]!.classList.contains('leetcode-code-action-ai-debug')).toBe(true)`.
- Add a new test case "AI Debug button click invokes plugin.aiDebugFromActive once" — construct row with a vi.fn(); dispatch click on `row.querySelector('.leetcode-code-action-ai-debug')`; assert mock called exactly once.
- Add a new test case "AI Debug button click does NOT propagate to CM6 doc" — same as Run/Submit precedent: dispatch a click event with a recorded propagation flag; assert preventDefault + stopPropagation behavior (mirror existing Run/Submit assertions if the test file has them).
- Add a final assertion to the no-prefix test: "no-prefix invariant survives" — comment block at top of the test asserting that `row.children[0]` is `.leetcode-code-action-run` (the FIRST child is still Run, not chevron) — this is the D-09 lock from Phase 5.3.
  </action>
  <verify>
    <automated>npx vitest run tests/main/codeBlockButtonRow.test.ts -- --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n 'aiDebugFromActive' src/main/codeBlockButtonRow.ts` returns at least 2 hits (one in CodeBlockButtonRowHost interface, one in click handler).
    - Source: `grep -n 'leetcode-code-action-ai-debug' src/main/codeBlockButtonRow.ts` returns at least 1 hit.
    - Source: `grep -nE "textContent\\s*=\\s*'AI: Debug'" src/main/codeBlockButtonRow.ts` returns at least 1 hit (verbatim label).
    - Source: the new button block uses the SAME shape as Run/Submit — `e.preventDefault()` + `e.stopPropagation()` + `void plugin.aiDebugFromActive()` (grep `grep -A 2 'leetcode-code-action-ai-debug' src/main/codeBlockButtonRow.ts` should show the matching click handler shape).
    - Source: `tests/main/codeBlockButtonRow.test.ts` no-prefix path expects 3 children, not 2 (`grep -nE 'children.length.*toBe\\(3\\)' tests/main/codeBlockButtonRow.test.ts` returns at least 1 hit).
    - Source: the chevron-prefix path expects 4 children (`grep -nE 'children.length.*toBe\\(4\\)' tests/main/codeBlockButtonRow.test.ts` returns at least 1 hit).
    - Behavior: `npx vitest run tests/main/codeBlockButtonRow.test.ts` exits 0 with at least 4 passing assertions (no-prefix 3-children, chevron 4-children, click invocation, no-prefix invariant).
    - Behavior: `npx tsc --noEmit` exits 0 (the host interface change must propagate type-cleanly to the existing call sites — a compile error here means the LeetCodePlugin class needs the host method, which is added in Task 2).
    - Behavior: existing Edit Mode + Reading Mode tests (`tests/main/codeActionsEditorExtension.test.ts`, `tests/main/codeActionsPostProcessor.test.ts` if present) continue to pass — both surfaces inherit the 3rd button automatically.
  </acceptance_criteria>
  <done>buildCodeBlockButtonRow renders 3 buttons (or 4 with chevron). Test contract bumped 2→3 / 3→4 with no-prefix invariant preserved. Edit Mode + Reading Mode both surface the button via the shared factory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire openAIDebug + LastVerdictStore + palette command + orchestrator callback in main.ts</name>
  <files>src/main.ts, src/notes/HeadingRegion.ts, tests/main/aiDebugCommand.test.ts</files>
  <read_first>
    - src/main.ts (lines 152-310 for onload — instantiation order; lines 425-436 for Submit palette command pattern; line 162 for aiClient field declaration; line 256 for EphemeralTabStore precedent)
    - src/main.ts:854-895 (requireAIDisclosure factory — already injected into AIClient at onload)
    - src/ai/AIStreamModal.ts (Plan 08-03 — AIStreamModalArgs shape)
    - src/ai/disclosure.ts (Plan 08-03 — DISCLOSURE_BASE_COPY + withDebugBullet)
    - src/ai/buildDebugPrompt.ts (Plan 08-03 — buildDebugPrompt signature)
    - src/solve/lastVerdictStore.ts (Plan 08-01 — LastVerdictStore class)
    - src/solve/submissionOrchestrator.ts (Plan 08-01 — SubmissionOrchestratorDeps with onVerdict?)
    - src/solve/codeExtractor.ts (extractFirstFencedBlock — read ## Code body)
    - src/notes/HeadingRegion.ts (helpers for reading note structure; may need a small extension or may not — read first)
    - src/api/* or wherever getProblemDetail lives (problem markdown via DetailCache or fresh fetch — same path Preview uses)
    - .planning/phases/08-ai-debug/08-PATTERNS.md (§"src/main.ts" — locked instantiation order + palette command + openAIDebug skeleton)
    - .planning/phases/08-ai-debug/08-RESEARCH.md (§"Architectural Responsibility Map" — single openAIDebug entrypoint; Pitfall 9 — extractFirstFencedBlock for ## Code)
    - .planning/phases/08-ai-debug/08-UI-SPEC.md (§"Open path" — full sequence)
  </read_first>
  <behavior>
    - LeetCodePlugin gains TWO new fields: `lastVerdictStore!: LastVerdictStore;` (definite-assignment, mirrors aiClient line 162). Plus host method `aiDebugFromActive(): Promise<void>` matching CodeBlockButtonRowHost interface.
    - In `onload()`: AFTER `EphemeralTabStore` instantiation AND AFTER `AIClient` construction, instantiate `this.lastVerdictStore = new LastVerdictStore();` (no Plugin arg per Plan 08-01).
    - In `onload()`: when constructing the SubmissionOrchestrator (or the orchestrator factory wherever submissionOrchestrator is wired with deps), pass `onVerdict: (slug, verdict) => this.lastVerdictStore.set(slug, verdict)`. If pollingOrchestrator owns its own deps shape, do the same on its construction site. The orchestrator stays pure — it does NOT import LastVerdictStore.
    - In `onunload()` (or where existing dispose calls happen — mirror the EphemeralTabStore.dispose call site), call `this.lastVerdictStore.dispose()`.
    - New host method `async openAIDebug(slug: string): Promise<void>`:
      1. Resolve problem markdown via `getProblemDetail(slug)` (DetailCache or fresh fetch — same path Preview uses; reuse the existing Preview code path, do NOT duplicate). If detail missing, Notice "Problem not found in cache; fetching…" and proceed to fetch.
      2. Read active note body — use the existing `getActiveNoteBody()` precedent from `submissionOrchestrator.ts:getCurrentBody` (read main.ts to find the existing helper or extract one).
      3. `extractFirstFencedBlock(body)` → `{ lang, code }`. If null, Notice "No `## Code` block found" and return.
      4. `const lastVerdict = this.lastVerdictStore.get(slug);` (may be undefined — buildDebugPrompt handles).
      5. `const problemMd = htmlToMarkdown(detail.contentHtml);` (same conversion Preview uses).
      6. `const prompt = buildDebugPrompt({ problemMd, code: code, language: lang || 'plaintext', lastVerdict });`
      7. `const provider = this.settings.getActiveAIProvider(); if (provider === null) { Notice "No AI provider configured. Open Settings → AI."; return; }`
      8. `new AIStreamModal(this.app, { provider, prompt, aiClient: this.aiClient, disclosureCopy: withDebugBullet(DISCLOSURE_BASE_COPY) }).open();`
    - New host method `async aiDebugFromActive(): Promise<void>`:
      1. Get active MarkdownView via `this.app.workspace.getActiveViewOfType(MarkdownView)`. If null, Notice "Open a LeetCode problem note first." and return.
      2. Read frontmatter `lc-slug` via `this.app.metadataCache.getFileCache(view.file)`. Validate via `isValidSlug`. If invalid, Notice "Active note has no `lc-slug` frontmatter." and return.
      3. Call `this.openAIDebug(slug)`.
    - New palette command `ai-debug` registered in onload via `this.addCommand({ id: 'ai-debug', name: 'AI: Debug current code', editorCheckCallback: ... })`. The `editorCheckCallback` shape is VERBATIM mirror of the Submit command at lines 425-436: read view.file, read frontmatter, validate `lc-slug` via `isValidSlug`, return `false` for non-LC notes (hides the command from palette in that context). On dispatch, call `this.openAIDebug(slug)`. NO default hotkey (project rule).
    - Command ID is `ai-debug` (clean, no `obsidian-leetcode:` prefix per FOUND-03).
    - The `disclosureCopy: withDebugBullet(DISCLOSURE_BASE_COPY)` is computed on each openAIDebug call (cheap; the factory returns a fresh object per call). AIClient.invokeStream gate inherits this disclosureCopy — IF AIClient currently does NOT accept disclosureCopy in its API, this plan's openAIDebug threads it through ARGS (AIStreamModal accepts `disclosureCopy` per Plan 08-03 modal args).
  </behavior>
  <action>
Edit `src/main.ts`:

1. Add field declaration `lastVerdictStore!: LastVerdictStore;` near the existing `aiClient!: AIClient;` field (line 162 area). Import `LastVerdictStore` from `'./solve/lastVerdictStore'`.

2. Import additions in main.ts: `import { AIStreamModal } from './ai/AIStreamModal';`, `import { buildDebugPrompt } from './ai/buildDebugPrompt';`, `import { DISCLOSURE_BASE_COPY, withDebugBullet } from './ai/disclosure';`, `import { extractFirstFencedBlock } from './solve/codeExtractor';`. Confirm `htmlToMarkdown` is already imported or imported from the same source Preview uses.

3. In `onload()`, AFTER the EphemeralTabStore instantiation (line 256 area) AND AFTER the AIClient construction (line 270-273 area), insert: `this.lastVerdictStore = new LastVerdictStore();`. Order: ephemeralTabs → aiClient → lastVerdictStore.

4. Find the SubmissionOrchestrator construction in main.ts (search for `new SubmissionOrchestrator` or wherever its deps object is built). Add `onVerdict: (slug, verdict) => this.lastVerdictStore.set(slug, verdict)` to the deps object. If pollingOrchestrator has a separate construction site with its own deps, repeat there.

5. In `onunload()` (search for `this.ephemeralTabs.dispose` or similar), add `this.lastVerdictStore?.dispose();`.

6. Add new method `async openAIDebug(slug: string): Promise<void>` on the LeetCodePlugin class. Implementation per `<behavior>`. Place it near the other AI-related methods (testActiveAIConnection at lines 854+ area). Use the existing `htmlToMarkdown` helper consumed by Preview (read Preview's import to find the source). Use Notice for the 3 failure paths (no detail, no fence, no provider).

7. Add new method `async aiDebugFromActive(): Promise<void>`. Resolves the active MarkdownView's frontmatter slug then delegates to openAIDebug(slug). Uses `this.app.workspace.getActiveViewOfType(MarkdownView)` (project rule — never `workspace.activeLeaf` direct access). 3 Notice paths: no view, no slug, success delegates.

8. Register the palette command in onload (insert near the existing Submit command at lines 425-436): `this.addCommand({ id: 'ai-debug', name: 'AI: Debug current code', editorCheckCallback: ... })` with the locked editorCheckCallback shape from `<behavior>`. NO default hotkey.

Create `tests/main/aiDebugCommand.test.ts`. Cases:
- "addCommand fires with id='ai-debug' and name='AI: Debug current code'" — spy on this.addCommand; assert command registered with verbatim ID + name.
- "editorCheckCallback returns false when view.file is null" — invoke checkCallback with `{ file: null }` view; assert false.
- "editorCheckCallback returns false when frontmatter has no lc-slug" — mock metadataCache to return `{ frontmatter: {} }`; assert false.
- "editorCheckCallback returns false when frontmatter lc-slug is invalid" — mock metadataCache `{ frontmatter: { 'lc-slug': '' } }`; assert false (isValidSlug rejects empty).
- "editorCheckCallback returns true when frontmatter has valid lc-slug" — mock `{ 'lc-slug': 'two-sum' }`; assert true.
- "editorCheckCallback fires openAIDebug with the slug when checking=false" — assert spy called with `'two-sum'`.
- "editorCheckCallback does NOT fire openAIDebug when checking=true" — assert spy NOT called when in palette-list-render mode.
- "openAIDebug shows Notice when active provider is null" — mock settings.getActiveAIProvider to return null; assert Notice with substring "No AI provider configured".
- "openAIDebug opens AIStreamModal with disclosureCopy from withDebugBullet(DISCLOSURE_BASE_COPY)" — spy on AIStreamModal constructor; assert the args.disclosureCopy.willSend.length === DISCLOSURE_BASE_COPY.willSend.length + 1 AND ends with the locked AI Debug bullet.

The test file should mock `obsidian` (Modal/MarkdownView/Notice) using the existing `tests/helpers/obsidian-stub.ts` precedent. Mock `getProblemDetail` if it lives outside main.ts. Use vi.mock for sub-modules that side-effect on import.
  </action>
  <verify>
    <automated>npx vitest run tests/main/aiDebugCommand.test.ts tests/main/codeBlockButtonRow.test.ts -- --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n 'lastVerdictStore!: LastVerdictStore' src/main.ts` returns at least 1 hit.
    - Source: `grep -n 'new LastVerdictStore()' src/main.ts` returns at least 1 hit (no Plugin arg).
    - Source: `grep -n 'lastVerdictStore.dispose' src/main.ts` returns at least 1 hit (onunload cleanup).
    - Source: `grep -nE "onVerdict.*lastVerdictStore\\.set" src/main.ts` returns at least 1 hit (orchestrator callback registered).
    - Source: `grep -n "addCommand" src/main.ts | grep -c "ai-debug"` returns at least 1 (palette command registered).
    - Source: `grep -nE "id: 'ai-debug'" src/main.ts` returns exactly 1 hit (no duplicate command IDs; clean ID per FOUND-03).
    - Source: `grep -n "name: 'AI: Debug current code'" src/main.ts` returns at least 1 hit (verbatim label).
    - Source: `grep -n "openAIDebug" src/main.ts` returns at least 3 hits (declaration + palette command + aiDebugFromActive).
    - Source: `grep -n "aiDebugFromActive" src/main.ts` returns at least 2 hits (declaration + body).
    - Source: `grep -n "withDebugBullet(DISCLOSURE_BASE_COPY)" src/main.ts` returns at least 1 hit.
    - Source: `grep -n "new AIStreamModal" src/main.ts` returns at least 1 hit.
    - Source: `grep -n "obsidian-leetcode:" src/main.ts | grep "ai-debug"` returns 0 hits (no plugin-id prefix on command ID — FOUND-03).
    - Source: `grep -n "hotkeys:\\s*\\[\\s*\\{" src/main.ts | grep -B1 "ai-debug"` returns 0 hits (no default hotkey on ai-debug command — project rule).
    - Source: orchestrator file does NOT import LastVerdictStore — `grep -n "LastVerdictStore" src/solve/submissionOrchestrator.ts` returns 0 hits (orchestrator stays pure; only main.ts wires the callback).
    - Behavior: `npx vitest run tests/main/aiDebugCommand.test.ts` exits 0 with all 9 cases passing.
    - Behavior: `npx vitest run tests/main/codeBlockButtonRow.test.ts` exits 0 (Task 1 still green).
    - Behavior: `npx tsc --noEmit` exits 0.
    - Behavior: `npm run lint` exits 0 (eslint-plugin-obsidianmd: clean command IDs, no plugin-as-component, no innerHTML, no workspace.activeLeaf direct access).
    - Behavior: `tests/ai/lc-isolation.test.ts` continues to pass (main.ts adds new code but no `obsidianFetch` import in src/api/, src/auth/, src/browse/, src/notes/, src/solve/, src/graph/, src/preview/).
  </acceptance_criteria>
  <done>LeetCodePlugin onload instantiates LastVerdictStore + registers onVerdict callback. openAIDebug + aiDebugFromActive methods exist. ai-debug palette command registers with editorCheckCallback frontmatter guard. 9 test cases pass. tsc + lint + lc-isolation all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → openAIDebug | Trusted user invocation via fence-row button or palette command |
| frontmatter slug → AI prompt | `lc-slug` value is treated as opaque problem identifier; never executed; threaded through AIClient → adapter |
| ## Code body → AI prompt | User's own code; sent to AI provider after disclosure ack |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-EoP-cmd | Elevation of Privilege | Palette command bypasses lc-slug guard | mitigate | editorCheckCallback returns false for non-LC notes — hides the command from palette. Asserted by 3 test cases (no file, no fm, invalid slug). Mirrors Submit command guard (Phase 03 precedent). |
| T-08-04-T-host | Tampering | Multiple aiDebugFromActive call sites diverge | mitigate | Single openAIDebug(slug) entrypoint; aiDebugFromActive resolves slug then delegates. Test asserts both fence-row click AND palette command call openAIDebug with the same slug. |
| T-08-04-IL-prompt | Information Disclosure | ## Code body sent to AI provider | mitigate | Disclosure modal fires on first call (Phase 07 gate). User explicitly acknowledges. AI Debug bullet on willSend documents the verdict shape. |
| T-08-04-IL-notes | Information Disclosure | ## Notes leak via prompt | mitigate | buildDebugPrompt (Plan 08-03) NEVER includes ## Notes — locked decision A; Plan 08-03 test asserts. openAIDebug threads only the extractFirstFencedBlock output (## Code only) into buildDebugPrompt. |
| T-08-04-D-noprovider | Denial of Service | openAIDebug crashes when no provider | mitigate | Notice path "No AI provider configured" + early return. Asserted by 1 test. |
| T-08-04-EoP-CMD-ID | Elevation of Privilege | Plugin-id prefix in command ID | mitigate | Locked clean ID `ai-debug` per FOUND-03 + eslint-plugin-obsidianmd `commands/no-command-in-command-id` + `no-plugin-id-in-command-id`. Lint gate green. |
| T-08-04-IL-cookie | Information Disclosure | obsidianFetch leaks cookies | mitigate | Phase 07 T-07-02 enforces `credentials: 'omit'` in obsidianFetch.ts. Plan 08-04 does NOT modify obsidianFetch. lc-isolation.test.ts gate prevents src/main/ or src/notes/ from gaining obsidianFetch imports. |
| T-08-04-T-orch | Tampering | Orchestrator-LastVerdictStore coupling | mitigate | Orchestrator stays pure (no LastVerdictStore import). main.ts injects the callback at construction. Asserted by grep on submissionOrchestrator.ts. |
| T-08-04-SC | Tampering | npm/pip/cargo installs | mitigate | NO new packages. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run tests/main/aiDebugCommand.test.ts tests/main/codeBlockButtonRow.test.ts -- --reporter=basic` exits 0.
- `npx vitest run tests/solve/submissionOrchestrator.test.ts tests/solve/submissionOrchestrator.onVerdict.test.ts` exits 0 (Plan 08-01's tests still green; the orchestrator's new `onVerdict` callback is now actually wired by main.ts, end-to-end test for the integration path may be added).
- `tests/ai/lc-isolation.test.ts` continues to pass.
- `npm run lint` exits 0 (clean command IDs + no-plugin-as-component compliance).
- `npm run check:bundle-size` reports the bundle remains under 1 MB. RESEARCH baseline expects ~0.7 KB delta from this plan (palette command + openAIDebug + aiDebugFromActive + LastVerdictStore field).
- Manual smoke (operator should run after `/gsd:execute-phase`): open a problem note → click `AI: Debug` under `## Code` → AIStreamModal opens → Continue past disclosure → response streams → Cancel works → Copy works → Close. Then Cmd-P → "AI: Debug current code" → same flow.
</verification>

<success_criteria>
- 3rd "AI: Debug" button appears under `## Code` in BOTH Edit Mode and Reading Mode (single factory consumed by both surfaces).
- `ai-debug` palette command registers with the verbatim label "AI: Debug current code"; editorCheckCallback returns false for non-LC notes (hides command from palette in that context).
- `openAIDebug(slug)` is the single entrypoint; called by fence-row button AND palette command.
- `LeetCodePlugin.lastVerdictStore` is instantiated in onload; orchestrator onVerdict callback is registered to populate it; dispose called on onunload.
- `tests/main/codeBlockButtonRow.test.ts` updated 2→3 / 3→4 with no-prefix invariant preserved.
- `tests/main/aiDebugCommand.test.ts` covers all 9 palette-command behaviors.
- tsc + lint + lc-isolation + bundle-size all green.
- Plan 08-05 may now wire the verdict-modal-footer button to call `LeetCodePlugin.openAIDebug(slug)` (same single entrypoint).
</success_criteria>

<output>
Create `.planning/phases/08-ai-debug/08-04-SUMMARY.md` when done. Document the bundle-size delta and confirm both fence-row surfaces (Edit + Reading) inherit the 3rd button automatically.
</output>
