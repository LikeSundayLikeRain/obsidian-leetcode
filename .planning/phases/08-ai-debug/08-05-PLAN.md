---
phase: 08-ai-debug
plan: 05
type: execute
wave: 4
depends_on:
  - 08-01
  - 08-02
  - 08-03
files_modified:
  - src/solve/verdictModalRenderer.ts
  - src/solve/VerdictModal.ts
  - src/solve/RunModal.ts
  - src/main.ts
  - tests/solve/VerdictModal.aiDebugButton.test.ts
  - tests/solve/RunModal.aiDebugButton.test.ts
autonomous: true
requirements:
  - AIDBG-01
must_haves:
  truths:
    - "Verdict modal footer shows AI: Debug button when verdict kind ∈ {wa, tle, mle, re, ce}"
    - "Verdict modal footer does NOT show AI: Debug button on Accepted (kind === 'ac')"
    - "Verdict modal footer does NOT show AI: Debug button on ole/ie/unknown/unknown-lc"
    - "RunModal failure footer shows AI: Debug button when compare_result has at least one '0'"
    - "Click on AI Debug button in verdict modal closes modal first then opens AIStreamModal"
    - "Both verdict surfaces delegate to the SAME LeetCodePlugin.openAIDebug(slug) — single entrypoint"
  artifacts:
    - path: src/solve/verdictModalRenderer.ts
      provides: "Conditional AI Debug button in renderSubmitVerdict footer; visibility union {wa,tle,mle,re,ce}"
      contains: "leetcode-ai-debug-action"
    - path: src/solve/VerdictModal.ts
      provides: "onOpenAIDebug callback in VerdictModalArgs; close-then-fire plumbing"
      contains: "onOpenAIDebug"
    - path: tests/solve/VerdictModal.aiDebugButton.test.ts
      provides: "Tests for visibility union (5 kinds present, 4 kinds absent) + click closes modal first"
      min_lines: 80
  key_links:
    - from: "src/solve/verdictModalRenderer.ts"
      to: "args.onOpenAIDebug?.()"
      via: "click handler on conditional button"
      pattern: "onOpenAIDebug"
    - from: "src/solve/VerdictModal.ts"
      to: "this.close() then this.args.onOpenAIDebug?.()"
      via: "lambda in renderVerdict opts.onOpenAIDebug"
      pattern: "this\\.close\\(\\)"
    - from: "src/main.ts:VerdictModalArgs construction"
      to: "() => this.openAIDebug(slug)"
      via: "callback wired at modal-open site for non-Accepted verdicts"
      pattern: "openAIDebug\\("
---

<objective>
Add the AI Debug button to the third surface — the Run/Verdict modal footer, conditional on a non-Accepted verdict (`kind ∈ {wa, tle, mle, re, ce}`). Click closes the verdict modal first, then opens AIStreamModal via the same `LeetCodePlugin.openAIDebug(slug)` entrypoint Plan 08-04 already wired for the fence-row + palette surfaces.

Purpose: This is the zero-friction "I just saw the failure → ask AI" flow. User runs Submit → judge returns WA → verdict modal shows the failure → user clicks AI Debug right there without leaving the modal. Plan 08-04 already shipped the openAIDebug entrypoint; this plan only wires the new click site. The visibility union `{wa, tle, mle, re, ce}` is locked by RESEARCH Pitfall 7 — Accepted is Phase 09's territory; OLE/IE/unknown/unknown-lc have no actionable failing case.

Output: Conditional AI Debug button in `verdictModalRenderer.renderSubmitVerdict` footer + `onOpenAIDebug` arg plumbing in VerdictModal + RunModal failure footer button (if Surface 3 ships per planner discovery — see <action>) + main.ts wires the callback at the modal-open site + 2 new test files for visibility union + click semantics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ai-debug/08-CONTEXT.md
@.planning/phases/08-ai-debug/08-RESEARCH.md
@.planning/phases/08-ai-debug/08-PATTERNS.md
@.planning/phases/08-ai-debug/08-VALIDATION.md
@.planning/phases/08-ai-debug/08-UI-SPEC.md
@.planning/phases/08-ai-debug/08-01-SUMMARY.md
@.planning/phases/08-ai-debug/08-02-SUMMARY.md
@.planning/phases/08-ai-debug/08-03-SUMMARY.md
@.planning/phases/08-ai-debug/08-04-SUMMARY.md
@CLAUDE.md

@src/solve/VerdictModal.ts
@src/solve/verdictModalRenderer.ts
@src/solve/RunModal.ts
@src/solve/statusMap.ts
@src/main.ts
@src/ai/AIStreamModal.ts
@tests/solve/verdictModalRenderer.test.ts

<interfaces>
<!-- Key contracts the executor needs. -->

From src/solve/VerdictModal.ts:27-33 (CURRENT VerdictModalArgs to extend):
```typescript
export interface VerdictModalArgs {
  problemTitle: string;
  onCancel: () => void;
  onCopyFailingInput?: (input: string) => void;
}
```
After Plan 08-05: add `onOpenAIDebug?: () => void;` next to `onCopyFailingInput?`.

From src/solve/VerdictModal.ts:111-122 (renderVerdict plumbing precedent — onCopyFailingInput closes modal then fires):
```typescript
renderVerdict({
  // ...
  onCopyFailingInput: (input) => {
    this.args.onCopyFailingInput?.(input);
    this.close();
  },
});
```
After Plan 08-05: add a parallel `onOpenAIDebug` lambda but with REVERSED order — close FIRST, then fire callback (because the callback opens a new modal that should NOT stack on top of the closing verdict modal):
```typescript
onOpenAIDebug: () => {
  this.close();
  this.args.onOpenAIDebug?.();
},
```

From src/solve/verdictModalRenderer.ts:483-505 (existing footer construction with conditional Copy buttons — locked precedent):
```typescript
const footer = appendEl(contentEl, 'div', 'leetcode-verdict-footer leetcode-verdict-action-row');
const failingInput = firstNonEmpty(res.input, res.last_testcase);
if (kind === 'wa' || kind === 'tle' || kind === 're') {
  if (onCopyFailingInput && failingInput.length > 0) {
    const copyBtn = appendEl(footer, 'button');
    setText(copyBtn, 'Copy failing testcase to custom input');
    copyBtn.addEventListener('click', () => onCopyFailingInput(failingInput));
  }
} else if (kind === 'ce') {
  // Copy error button block ...
}
const closeBtn = appendEl(footer, 'button', 'mod-cta');
setText(closeBtn, 'Close');
```

After Plan 08-05: INSERT the AI Debug button block BEFORE the closeBtn block (so the DOM order in the footer becomes `[Copy failing testcase?][Copy error?][AI: Debug?][Close]`). Locked union: `kind === 'wa' || kind === 'tle' || kind === 'mle' || kind === 're' || kind === 'ce'`.

From src/solve/statusMap.ts:40 (classifyStatus — 'kind' values that gate visibility):
```typescript
type VerdictKind = 'ac' | 'wa' | 'tle' | 'mle' | 're' | 'ce' | 'ole' | 'ie' | 'unknown' | 'unknown-lc';
```
- AI Debug button visible: `wa, tle, mle, re, ce` (5 kinds)
- AI Debug button absent: `ac` (Phase 09), `ole, ie, unknown, unknown-lc` (no actionable failing case)

Locked verbatim copy (UI-SPEC §"Copywriting Contract"):
- Button label: `AI: Debug`

Verdict modal construction sites in main.ts: search for `new VerdictModal(...)` to find every place a VerdictModalArgs is built. Each one needs the `onOpenAIDebug: () => this.openAIDebug(slug)` field added.

RunModal Surface 3 discovery (RESEARCH §Pitfall — UI-SPEC §"Surface 3"): "Verify during Plan 08-05 whether RunModal directly renders failure UI or whether the orchestrator delegates to VerdictModal in all failure paths. If the latter, Surface 3 collapses into Surface 2 and the AI Debug button is wired only in Surface 2." The planner has documented this as a TASK 1 SUB-STEP — read RunModal.ts end-to-end + the orchestrator's run-failure path to make this call.
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add conditional AI Debug button to verdictModalRenderer + plumb onOpenAIDebug through VerdictModal + decide RunModal Surface 3 fate</name>
  <files>src/solve/verdictModalRenderer.ts, src/solve/VerdictModal.ts, src/solve/RunModal.ts, src/main.ts, tests/solve/VerdictModal.aiDebugButton.test.ts, tests/solve/RunModal.aiDebugButton.test.ts</files>
  <read_first>
    - src/solve/verdictModalRenderer.ts (lines 483-505 — existing conditional Copy button block; full file end-to-end to find renderSubmitVerdict + renderRunVerdict if both exist)
    - src/solve/VerdictModal.ts (lines 27-33 for VerdictModalArgs; lines 111-122 for renderVerdict plumbing — note that onCopyFailingInput fires-then-closes, but onOpenAIDebug must close-then-fire to avoid double-modal stacking)
    - src/solve/RunModal.ts (READ END-TO-END to determine if RunModal renders its own failure UI or delegates to VerdictModal — this answers the "Surface 3 collapse" question per UI-SPEC §Surface 3)
    - src/solve/statusMap.ts (classifyStatus — verify the exact 'kind' values returned for status_code 11/12/13/14/15/20)
    - src/solve/runArity.ts and run resolve path (`tests/solve/RunModal.test.ts` if exists — find compare_result mask check pattern)
    - tests/solve/verdictModalRenderer.test.ts (existing test scaffolding — DOM stubs, fixture replay, footer assertions; mirror for new tests)
    - tests/solve/VerdictModal.test.ts (existing — DO NOT regress)
    - .planning/phases/08-ai-debug/08-PATTERNS.md (§"src/solve/verdictModalRenderer.ts" + §"src/solve/VerdictModal.ts" + §"src/solve/RunModal.ts")
    - .planning/phases/08-ai-debug/08-RESEARCH.md (§Pitfall 7 — visibility union locked; §"Code Examples" Example 5 — exact button-block shape)
    - .planning/phases/08-ai-debug/08-UI-SPEC.md (§"Surface 2" + §"Surface 3" — DOM order, conditional rendering)
    - .planning/phases/08-ai-debug/08-CONTEXT.md (decision C — three surfaces; CONTEXT decision A — non-Accepted gate)
  </read_first>
  <behavior>
    - `VerdictModalArgs` gains `onOpenAIDebug?: () => void` next to `onCopyFailingInput?`. Optional preserves backward compat with existing test fixtures that don't supply it.
    - `verdictModalRenderer.renderSubmitVerdict` (or whatever the exact function name is — read first) accepts `onOpenAIDebug?: () => void` arg alongside `onCopyFailingInput?`.
    - In the footer construction block (after the Copy-failing-input/Copy-error block, BEFORE the Close button), conditionally render the AI Debug button. Visibility union LOCKED: `kind === 'wa' || kind === 'tle' || kind === 'mle' || kind === 're' || kind === 'ce'`. ABSENT for `ac/ole/ie/unknown/unknown-lc`.
    - Button class: neutral (no `.mod-cta`); label `AI: Debug` (verbatim — UI-SPEC §"Copywriting Contract"); click handler invokes `onOpenAIDebug?.()`.
    - In `VerdictModal.ts`, the lambda passed to renderVerdict for `onOpenAIDebug` does `this.close()` FIRST then calls `this.args.onOpenAIDebug?.()` (REVERSED from onCopyFailingInput's fire-then-close — because openAIDebug opens a NEW modal that must not stack on top of the closing verdict modal).
    - RunModal Surface 3 decision (LOCKED OUTCOME below): If reading `RunModal.ts` reveals it delegates ALL failure UI to VerdictModal (i.e., RunModal only shows pre-judge sample-test-running state), Surface 3 COLLAPSES into Surface 2 and is NOT wired in this plan. Document the discovery in 08-05-SUMMARY.md. If RunModal DOES directly render failure UI with its own footer, mirror the verdictModalRenderer button block onto that footer (gated on `compare_result` containing at least one '0') and add `tests/solve/RunModal.aiDebugButton.test.ts`. Either way, ZERO surfaces are silently skipped — the discovery is recorded.
    - `tests/solve/VerdictModal.aiDebugButton.test.ts` covers the visibility union: button present for status_code 11 (wa), 12 (mle), 13 (tle), 14 (tle alt), 15 (re), 20 (ce); button absent for 10 (ac), 16 (ole), and the synthetic ie/unknown/unknown-lc kinds.
    - Tests assert button click invokes the callback exactly once; assert close-then-fire ordering when wired through VerdictModal (close called BEFORE callback).
  </behavior>
  <action>
**Sub-step 1 — RunModal Surface 3 discovery (mandatory):**
Read `src/solve/RunModal.ts` end-to-end. Determine:
- (a) Does RunModal render its own failure-state footer (with the failing-case rendered inline)?
- (b) Or does the orchestrator close RunModal and open VerdictModal for failure surfaces, leaving RunModal as a pre-judge "running tests…" indicator only?

Search the orchestrator for `new VerdictModal(...)` vs `new RunModal(...)` post-resolve paths. Read `src/solve/submissionOrchestrator.ts` and `src/solve/pollingOrchestrator.ts` for the run-resolve dispatch.

If outcome (a): Surface 3 SHIPS — wire the AI Debug button into RunModal's failure footer; add `tests/solve/RunModal.aiDebugButton.test.ts`.
If outcome (b): Surface 3 COLLAPSES — Surface 2 covers all failure paths; SKIP RunModal edits. Document the collapse in 08-05-SUMMARY.md with grep evidence.

Either way, write a 1-paragraph "Surface 3 outcome" note at the top of 08-05-SUMMARY.md explaining the choice with file:line references.

**Sub-step 2 — Verdict modal renderer:**
Edit `src/solve/verdictModalRenderer.ts`. Add `onOpenAIDebug?: () => void` to the args type for renderSubmitVerdict (or whatever the exact entry-function shape is — read first). In the footer construction block, AFTER the existing Copy-failing-input / Copy-error conditional and BEFORE the Close button block, INSERT (locked from RESEARCH §"Code Examples" Example 5):
```text
const showAIDebugButton =
  kind === 'wa' || kind === 'tle' || kind === 'mle' || kind === 're' || kind === 'ce';
if (showAIDebugButton && onOpenAIDebug) {
  const aiBtn = appendEl(footer, 'button');
  setText(aiBtn, 'AI: Debug');
  aiBtn.addEventListener('click', () => onOpenAIDebug());
}
```
The button uses neutral class (NO `.mod-cta` per UI-SPEC §"Color"). DOM order in footer: `[Copy failing testcase?][Copy error?][AI: Debug?][Close]`. The Close button stays the LAST child.

**Sub-step 3 — VerdictModal plumbing:**
Edit `src/solve/VerdictModal.ts`. Add `onOpenAIDebug?: () => void` to `VerdictModalArgs` next to `onCopyFailingInput?`. In `renderVerdict()` (around lines 111-122), pass a new lambda for `onOpenAIDebug`:
```text
onOpenAIDebug: () => {
  this.close();
  this.args.onOpenAIDebug?.();
},
```
NOTE the close-FIRST, fire-AFTER ordering. This is REVERSED from `onCopyFailingInput` (which fires-then-closes). The reason: openAIDebug opens AIStreamModal; if we fire callback first then close, AIStreamModal stacks ON TOP of a closing modal which causes z-index flicker. Closing first ensures the verdict modal's onClose runs cleanly before AIStreamModal's onOpen paints.

**Sub-step 4 — main.ts wiring:**
Edit `src/main.ts`. Find every `new VerdictModal(...)` construction site (likely 1-2 places — submissionOrchestrator wiring, possibly pollingOrchestrator wiring). Add `onOpenAIDebug: () => this.openAIDebug(slug)` to the args object alongside `onCopyFailingInput` and `onCancel`. The slug captured in the closure is the active problem's slug — read the surrounding context to find where slug is bound.

**Sub-step 5 — Tests:**
Create `tests/solve/VerdictModal.aiDebugButton.test.ts` (or extend `tests/solve/verdictModalRenderer.test.ts` per VALIDATION.md Wave 0 list). Use existing DOM-stub pattern from `tests/solve/verdictModalRenderer.test.ts`. Cases:
- "AI Debug button present for status_code=11 (WA)" — render verdict with status_code=11, status_msg='Wrong Answer'; assert button with `textContent === 'AI: Debug'` exists in the footer.
- "AI Debug button present for status_code=14 (TLE)"
- "AI Debug button present for status_code=12 (MLE)" — note: actual MLE status_code may differ; check statusMap.ts for the exact value, kind === 'mle' is the gate.
- "AI Debug button present for status_code=15 (RE)" — kind === 're'.
- "AI Debug button present for status_code=20 (CE)" — kind === 'ce'.
- "AI Debug button ABSENT for status_code=10 (Accepted)" — Phase 09 territory.
- "AI Debug button ABSENT for status_code=16 (OLE)" — kind === 'ole', no actionable failing case.
- "AI Debug button ABSENT when callback is undefined (defensive)" — render with onOpenAIDebug undefined; button must not appear even with kind='wa'.
- "AI Debug button click invokes callback exactly once" — vi.fn() callback; dispatch click; assert mock.calls.length === 1.
- "footer DOM order is [Copy failing?][Copy error?][AI: Debug?][Close]" — render with kind='wa' AND onCopyFailingInput AND onOpenAIDebug; assert footer.children indices match expected: index 0 = Copy failing, index 1 = AI: Debug, index 2 = Close (3 children); for kind='ce' AND callbacks: Copy error, AI: Debug, Close.

Also add to `tests/solve/VerdictModal.test.ts` (or new file) a case asserting close-then-fire ordering: spy on `this.close` AND `this.args.onOpenAIDebug`; assert `close` called BEFORE `onOpenAIDebug`. This guards against accidental refactor that swaps the order and reintroduces the modal-stack flicker.

If Sub-step 1 outcome is (a) [RunModal renders failure UI], create `tests/solve/RunModal.aiDebugButton.test.ts` with mirroring cases for the run-failure footer (button present iff `compare_result` contains at least one '0', absent on all-pass).
  </action>
  <verify>
    <automated>npx vitest run tests/solve/VerdictModal.aiDebugButton.test.ts tests/solve/verdictModalRenderer.test.ts tests/solve/VerdictModal.test.ts -- --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n "onOpenAIDebug" src/solve/VerdictModal.ts` returns at least 3 hits (interface field, lambda in renderVerdict, JSDoc or default).
    - Source: `grep -n "onOpenAIDebug" src/solve/verdictModalRenderer.ts` returns at least 3 hits (arg type, conditional check, click handler).
    - Source: `grep -nE "kind === 'wa' \\|\\| kind === 'tle' \\|\\| kind === 'mle' \\|\\| kind === 're' \\|\\| kind === 'ce'" src/solve/verdictModalRenderer.ts` returns at least 1 hit (LOCKED visibility union — verbatim).
    - Source: `grep -nE "setText\\(\\w+, 'AI: Debug'\\)" src/solve/verdictModalRenderer.ts` returns at least 1 hit (verbatim label).
    - Source: in `src/solve/VerdictModal.ts`, the lambda for onOpenAIDebug calls `this.close()` BEFORE `this.args.onOpenAIDebug?.()` — verify by reading the code; a brittle but useful grep: `grep -A 3 "onOpenAIDebug:" src/solve/VerdictModal.ts | head -5` should show `this.close()` before `this.args.onOpenAIDebug?.()`.
    - Source: `grep -n "onOpenAIDebug.*openAIDebug" src/main.ts` returns at least 1 hit (callback wired at modal construction site to `() => this.openAIDebug(slug)`).
    - Source: `grep -nE "mod-cta" src/solve/verdictModalRenderer.ts | grep -c "ai" || true` returns 0 (AI Debug button does NOT carry .mod-cta — UI-SPEC §"Color" lock).
    - Behavior: `npx vitest run tests/solve/VerdictModal.aiDebugButton.test.ts` exits 0 with at least 10 cases passing (5 kinds present, 2 kinds absent, 1 callback-undefined, 1 click invocation, 1 DOM order).
    - Behavior: `npx vitest run tests/solve/verdictModalRenderer.test.ts` exits 0 — existing tests still pass; the optional `onOpenAIDebug?` field is backward compatible.
    - Behavior: `npx vitest run tests/solve/VerdictModal.test.ts` exits 0 — close-then-fire ordering test passes (if added to this file vs the new file).
    - Behavior: `npx tsc --noEmit` exits 0.
    - Behavior: `npm run lint` exits 0.
    - Documentation: `08-05-SUMMARY.md` includes a "Surface 3 outcome" paragraph stating either "RunModal Surface 3 ships" with file:line references AND `tests/solve/RunModal.aiDebugButton.test.ts` exists OR "RunModal Surface 3 collapses into Surface 2" with grep evidence (e.g., `grep -n 'new VerdictModal' src/solve/submissionOrchestrator.ts` showing the orchestrator routes failures to VerdictModal not RunModal).
  </acceptance_criteria>
  <done>Verdict modal footer renders AI Debug button conditionally per the locked union. VerdictModal closes-then-fires the callback to avoid double-modal stacking. main.ts wires the callback to openAIDebug(slug). RunModal Surface 3 outcome explicitly documented (collapsed or shipped). 10+ test cases pass. tsc + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click → openAIDebug | Trusted user invocation via verdict modal footer |
| verdict modal → AIStreamModal | Single entrypoint; close-then-fire ordering prevents stacking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-EoP-ac | Elevation of Privilege | AI Debug button visible on Accepted | mitigate | Visibility union LOCKED to `{wa,tle,mle,re,ce}` — explicitly excludes `ac`. Asserted by 1 test case. Phase 09 owns AI Review on Accepted. |
| T-08-05-EoP-non-actionable | Elevation of Privilege | AI Debug visible on OLE/IE/unknown | mitigate | Visibility union excludes these. Asserted by 1+ test cases. These verdicts have no actionable failing case (LC-bug or system error). |
| T-08-05-T-stack | Tampering / UX | Modal stacking on top of closing modal | mitigate | onOpenAIDebug lambda calls `this.close()` BEFORE firing callback. Locked test asserts ordering. Reverses the `onCopyFailingInput` posture (which fires-then-closes for clipboard reasons). |
| T-08-05-D-callback-undef | Denial of Service | Click crashes when callback undefined | mitigate | Conditional render gates on `if (showAIDebugButton && onOpenAIDebug)` — button is never rendered without a callback. Asserted by 1 defensive test case. |
| T-08-05-T-host | Tampering | Multiple openAIDebug call sites diverge | mitigate | Single host method `LeetCodePlugin.openAIDebug(slug)` — all 3 surfaces (fence-row from Plan 08-04, palette from Plan 08-04, verdict modal from Plan 08-05) call into it. |
| T-08-05-IL-cookie | Information Disclosure | obsidianFetch leaks cookies | mitigate | Phase 07 T-07-02 enforces credentials: 'omit'. lc-isolation test gate prevents src/solve/ from gaining obsidianFetch imports. Plan 08-05 only adds DOM/render code in src/solve/. |
| T-08-05-SC | Tampering | npm/pip/cargo installs | mitigate | NO new packages. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run tests/solve/VerdictModal.aiDebugButton.test.ts tests/solve/verdictModalRenderer.test.ts tests/solve/VerdictModal.test.ts -- --reporter=basic` exits 0.
- If Surface 3 ships: `npx vitest run tests/solve/RunModal.aiDebugButton.test.ts tests/solve/RunModal.test.ts` exits 0.
- `npm test` (full suite) exits 0 — full Phase 08 plus all prior phases stay green.
- `tests/ai/lc-isolation.test.ts` continues to pass (no obsidianFetch imports added under src/solve/).
- `npm run lint` exits 0.
- `npm run check:bundle-size` reports the bundle remains under 1 MB.
- Manual smoke (Phase 08 closeout — operator runs after all 5 plans): submit a wrong solution → verdict modal opens with WA → AI: Debug button appears in footer → click → verdict modal closes → AIStreamModal opens → disclosure fires (or skips if already acknowledged) → response streams.
</verification>

<success_criteria>
- AI Debug button appears in verdict modal footer for `kind ∈ {wa, tle, mle, re, ce}` and is absent for `ac/ole/ie/unknown/unknown-lc`.
- Click closes verdict modal first, then opens AIStreamModal via `LeetCodePlugin.openAIDebug(slug)` (single entrypoint shared with fence-row + palette).
- VerdictModalArgs.onOpenAIDebug is optional (backward-compatible with existing test fixtures).
- main.ts wires the callback at every VerdictModal construction site.
- RunModal Surface 3 outcome explicitly documented (ships OR collapses into Surface 2 with grep evidence).
- 10+ new test cases cover visibility union + click semantics + close-then-fire ordering + callback-undef defense.
- All 3 AI Debug surfaces (fence-row, palette, verdict modal) now operational; AIDBG-01 user-visible.
- tsc + lint + lc-isolation + bundle-size + full-suite all green.
</success_criteria>

<output>
Create `.planning/phases/08-ai-debug/08-05-SUMMARY.md` when done. MUST document:
- Surface 3 outcome (RunModal ships OR collapses) with grep evidence.
- Bundle-size delta.
- Manual UAT smoke result (operator hits all 3 surfaces and confirms each opens AIStreamModal correctly).
</output>
