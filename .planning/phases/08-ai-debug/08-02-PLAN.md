---
phase: 08-ai-debug
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/ai/AIClient.ts
  - src/ai/providers/index.ts
  - src/ai/providers/anthropic.ts
  - src/ai/providers/openai.ts
  - src/ai/providers/openaiCompatible.ts
  - src/ai/providers/ollama.ts
  - tests/ai/AIClient.invokeStream.test.ts
  - tests/ai/providers/abortSignal.test.ts
  - tests/ai/electronNet.signal.test.ts
  - tests/ai/no-prompt-logging.test.ts
  - tests/helpers/electronNetStub.ts
autonomous: true
requirements:
  - AIDBG-02
  - AIDBG-03
must_haves:
  truths:
    - "AIClient.invokeStream returns a discriminated tuple { kind: 'stream' | 'buffered' } that the modal can consume"
    - "Stream path routes through obsidianFetch('stream') → electron.net.fetch with AbortSignal honoring"
    - "Fallback path (loadElectronNet throws) uses obsidianFetch('request') + generateText"
    - "AbortController.abort() propagates to all 4 provider adapters via streamText({ abortSignal })"
    - "Provider stubs throwing 'AIClient.invoke: Phase 08 wires the real call' are GONE from src/ai/providers/index.ts"
    - "No logger call site in src/ai/ logs a full prompt or response body (metadata-only)"
  artifacts:
    - path: src/ai/AIClient.ts
      provides: "invokeStream(req): Promise<{ kind: 'stream' | 'buffered', ... }> with disclosure-gate prologue mirrored from invoke()"
      exports: ["AIClient"]
    - path: src/ai/providers/anthropic.ts
      provides: "streamAnthropic + invokeAnthropicBuffered using streamText/generateText from 'ai'"
      contains: "streamText"
    - path: src/ai/providers/index.ts
      provides: "ResolvedAdapter with streamInvoke + bufferedInvoke wired for all 5 cases"
      contains: "streamInvoke"
    - path: tests/helpers/electronNetStub.ts
      provides: "Mock electron.net.fetch with signal-aware abort behavior; reused by 08-02/08-03 tests"
      min_lines: 40
  key_links:
    - from: "src/ai/providers/anthropic.ts:streamAnthropic"
      to: "streamText({ abortSignal })"
      via: "import from 'ai' (core, not @ai-sdk/anthropic)"
      pattern: "from 'ai'"
    - from: "src/ai/AIClient.ts:invokeStream"
      to: "obsidianFetch('stream')"
      via: "fetcher injection on createAnthropic({ fetch })"
      pattern: "obsidianFetch\\('stream'\\)"
---

<objective>
Add `AIClient.invokeStream(req)` returning a discriminated tuple, replace the 5 per-provider stub `invoke` methods (currently throwing `'AIClient.invoke: Phase 08 wires the real call'`) with real `streamText` + `generateText` calls, and add per-adapter abort propagation tests.

Purpose: This plan is the streaming transport plumbing — the first live customer of `obsidianFetch('stream')` Phase 07 wired. It validates the AbortController → streamText → provider fetch.init.signal chain across all 4 adapters. Disclosure gate fires automatically because invokeStream mirrors invoke's prologue verbatim. The fallback path (`loadElectronNet` throws → buffered `generateText`) is locked here so Plan 08-03's modal can switch body strategy on `kind`.

Output: AIClient.invokeStream method + per-provider streaming adapters + Wave 0 tests for AbortSignal across providers + electron.net.fetch signal-honoring mock + grep-locked logger discipline test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ai-debug/08-CONTEXT.md
@.planning/phases/08-ai-debug/08-RESEARCH.md
@.planning/phases/08-ai-debug/08-PATTERNS.md
@.planning/phases/08-ai-debug/08-VALIDATION.md
@.planning/phases/08-ai-debug/08-01-SUMMARY.md
@CLAUDE.md

@src/ai/AIClient.ts
@src/ai/types.ts
@src/ai/obsidianFetch.ts
@src/ai/providers/index.ts
@src/ai/providers/anthropic.ts
@src/ai/providers/openai.ts
@src/ai/providers/openaiCompatible.ts
@src/ai/providers/ollama.ts
@src/ai/disclosure.ts
@tests/ai/aiClient.test.ts

<interfaces>
<!-- Key contracts the executor needs. -->

From src/ai/AIClient.ts:130-160 (invoke prologue — verbatim mirror in invokeStream):
```typescript
async invoke(req: AIRequest): Promise<AIResponse> {
  const provider = this.settings.getActiveAIProvider();
  if (provider === null) throw new Error('No AI provider configured');
  let cfg = this.settings.getProviderConfig(provider);
  if (!cfg.disclosureAcknowledged) {
    const ack = await this.requireDisclosure(provider, cfg);
    if (!ack) throw new Error('AI call cancelled');
    await this.settings.setProviderConfig(provider, { ...cfg, disclosureAcknowledged: true });
    cfg = this.settings.getProviderConfig(provider);
  }
  const wantStream = req.stream === true;  // Plan 08-01 cast removal
  const fetcher = obsidianFetch(wantStream ? 'stream' : 'request');
  const adapter = resolveAdapter(provider, cfg, fetcher);
  return await adapter.invoke(req);
}
```

From src/ai/providers/index.ts:84-126 (CURRENT — stubs to replace):
```typescript
export function resolveAdapter(provider, cfg, fetcher): ResolvedAdapter {
  switch (provider) {
    case 'anthropic':
      return {
        probe: () => probeAnthropic(cfg, fetcher),
        invoke: () => { throw new Error('AIClient.invoke: Phase 08 wires the real call'); },
      };
    // ... 4 more cases identical shape ...
  }
}
```

From src/ai/providers/anthropic.ts:21-46 (createAnthropicModel + probeAnthropic — new streamAnthropic mirrors):
```typescript
export function createAnthropicModel(cfg, fetcher) {
  const provider = createAnthropic({ apiKey: cfg.apiKey, baseURL: cfg.baseUrl, fetch: fetcher });
  return provider(cfg.model || 'claude-haiku-4-5');
}
```

From src/ai/obsidianFetch.ts:77-119 (both branches — read for behavior contract):
- `obsidianFetch('stream')` → tries `loadElectronNet()` then `electron.net.fetch`; throws on Node-require failure.
- `obsidianFetch('request')` → uses Obsidian's `requestUrl`; does NOT forward `init.signal`.

From node_modules/ai/dist/index.d.ts:2735 (streamText signature):
```typescript
export function streamText<TOOLS, OUTPUT>(options: {
  model: LanguageModelV3;
  prompt?: string;
  abortSignal?: AbortSignal;
  onFinish?: (event: { totalUsage: LanguageModelUsage }) => void | Promise<void>;
  onAbort?: (event: { steps: StepResult<TOOLS>[] }) => void;
  onError?: (event: { error: Error }) => void;
}): StreamTextResult<TOOLS, OUTPUT>;
```
- `result.textStream: AsyncIterableStream<string>`
- `result.usage: PromiseLike<LanguageModelUsage>` (rejects on abort)

Discriminated tuple return shape (RESEARCH §"F-Refinement"):
```typescript
type InvokeStreamResult =
  | { kind: 'stream'; result: StreamTextResult<{}, {}>; abortController: AbortController }
  | { kind: 'buffered'; text: Promise<string>; abortController: AbortController };
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace per-provider invoke stubs with real streamText + generateText calls</name>
  <files>src/ai/providers/index.ts, src/ai/providers/anthropic.ts, src/ai/providers/openai.ts, src/ai/providers/openaiCompatible.ts, src/ai/providers/ollama.ts, tests/ai/no-prompt-logging.test.ts, tests/helpers/electronNetStub.ts</files>
  <read_first>
    - src/ai/providers/index.ts (lines 84-126 for resolveAdapter switch — all 5 stubs)
    - src/ai/providers/anthropic.ts (lines 21-46 for createAnthropicModel + probeAnthropic — SDK-call posture template)
    - src/ai/providers/openai.ts (lines 22-45 for createOpenAIModel + probeOpenAI — same pattern)
    - src/ai/providers/openaiCompatible.ts (lines 29-117 for probeCustom / probeOpenRouter / probeViaOneTokenChat)
    - src/ai/providers/ollama.ts (lines 23-53 for probeOllama)
    - src/ai/obsidianFetch.ts:77-119 (FetchFn type + both branches; signal forwarding)
    - .planning/phases/08-ai-debug/08-PATTERNS.md (Pattern Assignments §"src/ai/providers/anthropic.ts" + §"src/ai/providers/index.ts" — locked SDK-call posture)
    - .planning/phases/08-ai-debug/08-RESEARCH.md (§"Code Examples" Examples 1, 2 — verified streamText shape; §"Pitfall 10" — logger discipline)
    - tests/ai/aiClient.test.ts (MockSettings factory pattern; resolveAdapter mock pattern)
  </read_first>
  <behavior>
    - Each provider file gains TWO new exports: `stream{Provider}(cfg, fetcher, prompt, signal): StreamTextResult<{}, {}>` and `invoke{Provider}Buffered(cfg, fetcher, prompt, signal): Promise<{ text, usage? }>`.
    - `streamText` and `generateText` imported from `'ai'` (core), NOT from `@ai-sdk/*`.
    - `fetch: fetcher` field ALWAYS set on `createAnthropic`/`createOpenAI`/`createOpenAICompatible` calls (CORS-bypass non-negotiable).
    - `abortSignal: signal` propagated in BOTH stream + buffered paths.
    - For `openaiCompatible.ts`: separate factories for `openrouter`, `ollama`, `custom` cases — each takes a different baseUrl default but reuses the same `createOpenAICompatible` provider (mirror existing probe shape).
    - `src/ai/providers/index.ts` resolveAdapter switch updated: each of 5 cases returns `{ probe, streamInvoke, bufferedInvoke }`. The OLD `invoke: () => { throw new Error('AIClient.invoke: Phase 08 wires the real call'); }` line REMOVED from all 5 branches.
    - `ResolvedAdapter` type updated: drops `invoke`, adds `streamInvoke(prompt, signal): StreamTextResult<{}, {}>` and `bufferedInvoke(prompt, signal): Promise<{ text, usage? }>`.
    - The string `'AIClient.invoke: Phase 08 wires the real call'` is GONE from the entire repo (grep gate).
    - Logger discipline: NO call site in `src/ai/` passes a `prompt` or `responseText` field to `logger.{debug,info,warn,error}`. Metadata-only.
  </behavior>
  <action>
Edit `src/ai/providers/anthropic.ts` to add:
- `import { streamText, generateText } from 'ai';` (core import, NOT from @ai-sdk/anthropic).
- Export `streamAnthropic(cfg: ProviderConfig, fetcher: FetchFn, prompt: string, signal: AbortSignal): StreamTextResult<{}, {}>` — calls `streamText({ model: createAnthropicModel(cfg, fetcher), prompt, abortSignal: signal })`.
- Export `invokeAnthropicBuffered(cfg: ProviderConfig, fetcher: FetchFn, prompt: string, signal: AbortSignal): Promise<{ text: string; usage?: { inputTokens?: number; outputTokens?: number } }>` — calls `generateText({ model, prompt, abortSignal: signal })` then maps `result.text` + `result.usage`.

Apply the SAME shape to `src/ai/providers/openai.ts` (export `streamOpenAI` + `invokeOpenAIBuffered`), `src/ai/providers/ollama.ts` (export `streamOllama` + `invokeOllamaBuffered`), and `src/ai/providers/openaiCompatible.ts` (export `streamOpenAICompatible` + `invokeOpenAICompatibleBuffered` — generic over baseUrl). The `openaiCompatible` exports must accept the cfg object directly (cfg.baseUrl carries openrouter/custom/ollama URL — same pattern as probeCustom).

Edit `src/ai/providers/index.ts` resolveAdapter switch:
- Update `ResolvedAdapter` type: replace `invoke: () => Promise<AIResponse>` with `streamInvoke: (prompt: string, signal: AbortSignal) => StreamTextResult<{}, {}>` and `bufferedInvoke: (prompt: string, signal: AbortSignal) => Promise<{ text: string; usage?: ... }>`.
- For each of 5 cases (`anthropic`, `openai`, `openrouter`, `ollama`, `custom`), REPLACE the throwing `invoke: () => { throw new Error('AIClient.invoke: Phase 08 wires the real call'); }` with the real wiring: `streamInvoke: (prompt, signal) => stream{Provider}(cfg, fetcher, prompt, signal)` and `bufferedInvoke: (prompt, signal) => invoke{Provider}Buffered(cfg, fetcher, prompt, signal)`. For openrouter/ollama/custom, all three route to the openaiCompatible streaming functions but with the right baseUrl in cfg.
- Keep exhaustive switch (no default branch) — the existing posture stays.
- Verify the comment block at lines 81-94 referencing `Phase 07 invoke stub` is updated or removed (Phase 08 now owns the call site).

Create `tests/helpers/electronNetStub.ts`. Pure mock helper that returns a fake `electron.net.fetch` honoring `init.signal`:
```text
export interface MockElectronNetOptions {
  responseChunks?: string[];   // Stream of text chunks
  bufferedText?: string;       // Single-shot fallback
  delayMs?: number;             // Per-chunk delay
}
export function createMockElectronNet(opts: MockElectronNetOptions): { fetch: typeof fetch };
```
The mock fetch:
- Reads `init.signal`. If aborted at start, rejects with `AbortError`.
- Returns a Response whose body is a `ReadableStream` that emits chunks one-by-one. If signal aborts mid-stream, the ReadableStream's controller errors.
- For buffered mode (no `responseChunks`), returns a Response with `text()` resolving to `bufferedText` after `delayMs`.

Add `tests/ai/no-prompt-logging.test.ts` per RESEARCH §Pitfall 10:
- Read all `.ts` files under `src/ai/` recursively (use Node `fs` + a glob walk; mirror the helper in `tests/ai/lc-isolation.test.ts`).
- Assert no source file contains a regex match for `logger\.(debug|info|warn|error)\([^)]*\b(prompt|responseText)\b`. The match should fail for any code passing a `prompt` or `responseText` field to a logger call. Allow `// metadata-only` comment lines + tests.
  </action>
  <verify>
    <automated>npx vitest run tests/ai/no-prompt-logging.test.ts -- --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -rn 'AIClient.invoke: Phase 08 wires the real call' src/` returns 0 hits (the stub string is GONE — locked grep gate).
    - Source: `grep -nE "import \{ streamText" src/ai/providers/anthropic.ts src/ai/providers/openai.ts src/ai/providers/ollama.ts src/ai/providers/openaiCompatible.ts` returns at least 4 hits — one per provider file.
    - Source: `grep -n "from 'ai'" src/ai/providers/anthropic.ts` matches (core import, not @ai-sdk).
    - Source: `grep -nE 'fetch:\s*fetcher' src/ai/providers/anthropic.ts src/ai/providers/openai.ts src/ai/providers/openaiCompatible.ts src/ai/providers/ollama.ts` returns at least 4 hits across the four provider files (each provider's create*Model + the buffered/stream helpers).
    - Source: `grep -nE 'abortSignal:\s*signal' src/ai/providers/` returns at least 8 hits (4 streamX + 4 invokeXBuffered call sites).
    - Source: `grep -nE 'export (function|async function) (stream|invoke).+Buffered' src/ai/providers/` returns at least 8 hits.
    - Source: `grep -nE 'streamInvoke|bufferedInvoke' src/ai/providers/index.ts` returns at least 11 hits (5 streamInvoke + 5 bufferedInvoke + ResolvedAdapter type).
    - Source: `grep -n 'logger\\.\\(debug\\|info\\|warn\\|error\\).*\\bprompt\\b' src/ai/` (run with `grep -rE`) returns 0 hits.
    - Behavior: `npx vitest run tests/ai/no-prompt-logging.test.ts` exits 0.
    - Behavior: `npx tsc --noEmit` exits 0 (the ResolvedAdapter shape change must compile cleanly).
    - Behavior: existing `npx vitest run tests/ai/aiClient.test.ts` passes (Plan 07 tests must still go green; if they reference the old `adapter.invoke`, the AIClient.invoke method updates in Task 2 below cover this).
  </acceptance_criteria>
  <done>All 5 provider stubs replaced with real streamText/generateText calls. resolveAdapter exports streamInvoke + bufferedInvoke. The Phase 07 closing-log marker string is gone. No-prompt-logging test passes. tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add AIClient.invokeStream + Wave 0 tests for stream/fallback/abort across providers</name>
  <files>src/ai/AIClient.ts, tests/ai/AIClient.invokeStream.test.ts, tests/ai/providers/abortSignal.test.ts, tests/ai/electronNet.signal.test.ts</files>
  <read_first>
    - src/ai/AIClient.ts (lines 130-160 for invoke — copy disclosure-gate prologue verbatim)
    - src/ai/disclosure.ts (Phase 07 disclosure gate behavior — `requireDisclosure` returns Promise<boolean>)
    - src/ai/obsidianFetch.ts (loadElectronNet + branch behavior — must catch loadElectronNet throw on stream path)
    - src/ai/providers/index.ts (post-Task-1 — ResolvedAdapter has streamInvoke + bufferedInvoke)
    - .planning/phases/08-ai-debug/08-PATTERNS.md (§"src/ai/AIClient.ts" — disclosure gate mirror; §Shared Patterns "AbortController + Signal-Aborted Distinguish")
    - .planning/phases/08-ai-debug/08-RESEARCH.md (§"F-Refinement" discriminated tuple; §"Open Questions" #5 — explicit generateText on fallback; §Pitfall 6 — cost on cancel)
    - tests/ai/aiClient.test.ts (MockSettings factory at lines 12-63; resolveAdapter mock at lines 12-16)
    - tests/helpers/electronNetStub.ts (Task 1 — mock fetch with signal honoring)
  </read_first>
  <behavior>
    - AIClient gains async `invokeStream(req: AIRequest): Promise<InvokeStreamResult>` where `InvokeStreamResult = { kind: 'stream'; result: StreamTextResult<{}, {}>; abortController: AbortController } | { kind: 'buffered'; text: Promise<string>; abortController: AbortController }`.
    - Disclosure-gate prologue MIRRORS invoke() lines 130-146 verbatim: read provider, throw 'No AI provider configured' if null, read cfg, fire requireDisclosure if !disclosureAcknowledged, throw 'AI call cancelled' on cancel, persist `disclosureAcknowledged: true`, RE-READ cfg.
    - The 'AI call cancelled' string is locked verbatim (Plan 07-05 contract).
    - The 'No AI provider configured' string is locked verbatim.
    - Stream-first path: `try { fetcher = obsidianFetch('stream'); resolveAdapter; result = adapter.streamInvoke(req.prompt, abortController.signal); return { kind: 'stream', result, abortController }; } catch (electronError) { /* fall through to buffered */ }`.
    - Fallback path: `fetcher = obsidianFetch('request'); resolveAdapter; const promise = adapter.bufferedInvoke(prompt, signal).then(r => r.text); return { kind: 'buffered', text: promise, abortController }`.
    - The AbortController is owned by AIClient.invokeStream (NOT the modal). Caller can abort via `result.abortController.abort()`.
    - If `req.signal` is provided, AIClient.invokeStream's internal abortController forwards: `req.signal.addEventListener('abort', () => abortController.abort())`. This lets the modal pass its own AbortController in via req.signal AND get the inner controller for direct abort.
    - When `req.stream === false` (or undefined), invokeStream still works but returns `{ kind: 'buffered', ... }` directly without trying the stream path.
    - AIClient.invoke (existing method) is preserved unchanged — both methods inherit the same disclosure gate semantics.
  </behavior>
  <action>
Edit `src/ai/AIClient.ts`. Add `invokeStream(req: AIRequest): Promise<InvokeStreamResult>` method. The method MUST:
1. Mirror invoke()'s disclosure-gate prologue lines 131-146 VERBATIM (same calls in same order: getActiveAIProvider null check, getProviderConfig, disclosureAcknowledged check, requireDisclosure call, throw 'AI call cancelled' on cancel, setProviderConfig persist, re-read cfg).
2. Create internal `abortController = new AbortController()`. If `req.signal` provided, register `req.signal.addEventListener('abort', () => abortController.abort())` so the caller's signal cascades.
3. Try the stream path first when `req.stream === true`:
   - `const fetcher = obsidianFetch('stream');` (this throws on `loadElectronNet` failure — caught at the next layer)
   - `const adapter = resolveAdapter(provider, cfg, fetcher);`
   - `const result = adapter.streamInvoke(req.prompt, abortController.signal);` (synchronous call — streamText returns the result object immediately)
   - return `{ kind: 'stream', result, abortController }`
4. On either `obsidianFetch('stream')` throwing OR `req.stream !== true`, fall through to buffered:
   - `const fetcher = obsidianFetch('request');`
   - `const adapter = resolveAdapter(provider, cfg, fetcher);`
   - `const text = adapter.bufferedInvoke(req.prompt, abortController.signal).then(r => r.text);`
   - return `{ kind: 'buffered', text, abortController }`
5. Export `InvokeStreamResult` type from `src/ai/AIClient.ts` (or `src/ai/types.ts` — choose AIClient.ts to keep the type co-located with the producer).

Update `AIClient.invoke` if necessary: after Task 1 the per-provider `invoke` method is gone (replaced by streamInvoke/bufferedInvoke). The existing AIClient.invoke method needs to call `bufferedInvoke` instead. Replace `return await adapter.invoke(req);` with `const r = await adapter.bufferedInvoke(req.prompt, req.signal ?? new AbortController().signal); return { text: r.text, usdCost: 0, usage: r.usage };` (cost is added by the caller now, not the AIClient — preserves Phase 07 testActiveAIConnection's posture which doesn't call addCost).

Create `tests/ai/AIClient.invokeStream.test.ts`. Mirror MockSettings factory + vi.mock('../../src/ai/providers') from `tests/ai/aiClient.test.ts:12-63`. resolveAdapter mock returns `{ probe, streamInvoke: vi.fn(), bufferedInvoke: vi.fn() }`. Cases:
- "throws 'No AI provider configured' when active provider is null"
- "fires disclosure gate when disclosureAcknowledged === false; persists ack on Continue"
- "throws 'AI call cancelled' verbatim when disclosure returns false"
- "stream path returns { kind: 'stream', result, abortController } when req.stream=true and electron.net is available"
- "fallback path returns { kind: 'buffered', text: Promise, abortController } when obsidianFetch('stream') throws (mock loadElectronNet failure)"
- "fallback path returns buffered when req.stream is undefined / false"
- "external req.signal cascades to internal abortController on abort"
- "abortController.abort() invocation does NOT throw"

Create `tests/ai/providers/abortSignal.test.ts`. For each of the 4 provider files (anthropic, openai, openaiCompatible, ollama), test that:
- `stream{Provider}` passes `abortSignal: signal` to `streamText` (mock the `'ai'` module's `streamText` and assert its arg).
- `invoke{Provider}Buffered` passes `abortSignal: signal` to `generateText`.
- Aborting the signal causes the internal stream/promise to reject with an AbortError-shaped error (use `tests/helpers/electronNetStub.ts` to wire a signal-aware fetch — the chain should propagate).

Create `tests/ai/electronNet.signal.test.ts`. Standalone test against `tests/helpers/electronNetStub.ts` that:
- Builds a mock electron.net.fetch with 5 chunks and 50ms delay.
- Awaits the response, iterates the body's ReadableStream.
- After receiving 2 chunks, calls `controller.abort()`.
- Asserts the iterator's next call rejects with an AbortError-named error AND the underlying ReadableStream's reader is closed.
- Validates the helper itself (Assumption A1 enforcement before plan 08-03 consumes it).
  </action>
  <verify>
    <automated>npx vitest run tests/ai/AIClient.invokeStream.test.ts tests/ai/providers/abortSignal.test.ts tests/ai/electronNet.signal.test.ts tests/ai/aiClient.test.ts -- --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -nE 'async invokeStream\(' src/ai/AIClient.ts` returns at least 1 hit.
    - Source: AIClient.invokeStream contains the disclosure-gate prologue (grep for `requireDisclosure` + `'AI call cancelled'` + `'No AI provider configured'` + `setProviderConfig` + `disclosureAcknowledged: true` ALL within the invokeStream method body).
    - Source: `grep -nE "kind: 'stream'|kind: 'buffered'" src/ai/AIClient.ts` returns at least 2 hits.
    - Source: `grep -n "obsidianFetch('stream')" src/ai/AIClient.ts` returns at least 1 hit.
    - Source: `grep -n "obsidianFetch('request')" src/ai/AIClient.ts` returns at least 1 hit (fallback branch).
    - Source: `grep -nE 'addEventListener\([^)]*abort' src/ai/AIClient.ts` returns at least 1 hit (req.signal cascade).
    - Behavior: `npx vitest run tests/ai/AIClient.invokeStream.test.ts` exits 0 with all 8 cases.
    - Behavior: `npx vitest run tests/ai/providers/abortSignal.test.ts` exits 0 with at least 8 cases (2 per provider × 4).
    - Behavior: `npx vitest run tests/ai/electronNet.signal.test.ts` exits 0.
    - Behavior: `npx vitest run tests/ai/aiClient.test.ts` exits 0 — existing Phase 07 tests still pass after AIClient.invoke's adapter.invoke → adapter.bufferedInvoke migration.
    - Behavior: `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>AIClient.invokeStream returns the discriminated tuple, fires disclosure gate, falls back to buffered when electron.net unavailable. AbortSignal propagation tests pass for all 4 providers. electron.net.fetch signal-honoring is mock-validated (Assumption A1 enforced). Phase 07 aiClient.test.ts stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → AI provider host | AIClient.invokeStream initiates HTTP to anthropic.com / api.openai.com / openrouter.ai / ollama / custom; cookies stripped per Phase 07 T-07-02 |
| AbortController → fetch.init.signal | Cancel UX depends on signal propagation through streamText → SDK adapter → injected fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-IL-cookie | Information Disclosure | obsidianFetch('stream') | mitigate | Phase 07 T-07-02 already enforces `credentials: 'omit'` in BOTH branches of obsidianFetch.ts. Plan 08-02 does NOT modify obsidianFetch — the discipline is inherited. Locked by `tests/ai/lc-isolation.test.ts` (no obsidianFetch import in src/api/, src/auth/, etc.). |
| T-08-02-IL-prompt | Information Disclosure | logger calls in src/ai/ | mitigate | RESEARCH §Pitfall 10 + Pattern: no-prompt-logging.test.ts asserts no `logger.{debug,info,warn,error}` call site in src/ai/ passes a `prompt` or `responseText` field. Metadata-only logging (provider, model, char-count, duration). |
| T-08-02-T-disclosure | Tampering | Disclosure gate bypass via direct resolveAdapter import | accept | Documented in Phase 07 AIClient JSDoc + 07-RESEARCH §Security threat #5. Phase 08 only imports AIClient — never resolveAdapter directly. Community-plugin review is the human safety net. |
| T-08-02-D-zombie | Denial of Service / Repudiation | Cancel doesn't propagate to network | mitigate | Plan 08-02 Task 2 includes per-provider abortSignal tests + electronNet.signal.test.ts. AbortController propagation is asserted across all 4 providers (Assumption A4 enforced). |
| T-08-02-T-fallback | Tampering | Fallback path misroutes to streaming endpoint | mitigate | Discriminated tuple `{ kind: 'stream' | 'buffered' }` makes the path explicit at the modal layer. The fallback uses `obsidianFetch('request')` (not 'stream') — locked by grep on AIClient.ts. |
| T-08-02-EoP | Elevation of Privilege | invokeStream bypasses disclosure | mitigate | invokeStream's disclosure-gate prologue MIRRORS invoke() verbatim — every test case for invoke()'s gate (Phase 07 Plan 07 + 07-08) has an analog test in invokeStream. Asserted by 3 test cases ("fires gate", "throws on cancel", "persists ack"). |
| T-08-02-SC | Tampering | npm/pip/cargo installs | mitigate | NO new packages. Phase 07 Package Legitimacy Audit covers all 4 AI packages; Phase 08 is glue only. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run tests/ai/ -- --reporter=basic` exits 0 (all AI tests including the new invokeStream + provider abort + electronNet stub).
- `tests/ai/lc-isolation.test.ts` continues to pass (this plan does NOT add `obsidianFetch` imports outside `src/ai/`).
- `npm run check:bundle-size` reports the bundle still under 1 MB (post-Task-1 + Task-2 the AI SDK call sites land on the bundle graph for the first time — RESEARCH baseline expects ~7 KB delta).
- `npm run lint` exits 0 (no eslint-disable additions; logger discipline test catches violations early).
</verification>

<success_criteria>
- `'AIClient.invoke: Phase 08 wires the real call'` is GONE from `src/`.
- All 5 provider adapters (anthropic, openai, openrouter, ollama, custom) wire real `streamText` + `generateText` calls with `abortSignal` propagation and `fetch: fetcher` injection.
- `AIClient.invokeStream(req)` returns the discriminated tuple; disclosure gate fires verbatim; fallback path uses `obsidianFetch('request')` + `generateText`.
- Per-provider abort propagation tests pass (4 providers × 2 cases each + electron.net.fetch mock validation).
- Logger discipline test asserts no `prompt` or `responseText` in any logger call in `src/ai/`.
- `tsc --noEmit` clean. `npm run lint` clean. `tests/ai/lc-isolation.test.ts` still passes.
- Plan 08-03 (AIStreamModal) can now consume `aiClient.invokeStream(req)` and switch on `kind`.
</success_criteria>

<output>
Create `.planning/phases/08-ai-debug/08-02-SUMMARY.md` when done. Include the bundle-size delta measurement.
</output>
