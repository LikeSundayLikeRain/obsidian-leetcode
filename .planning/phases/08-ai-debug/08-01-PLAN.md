---
phase: 08-ai-debug
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/types.ts
  - src/ai/AIClient.ts
  - src/solve/lastVerdictStore.ts
  - src/solve/submissionOrchestrator.ts
  - src/solve/pollingOrchestrator.ts
  - src/solve/runArity.ts
  - tests/solve/lastVerdictStore.test.ts
  - tests/solve/submissionOrchestrator.onVerdict.test.ts
  - tests/ai/types.test.ts
autonomous: true
requirements:
  - AIDBG-01
must_haves:
  truths:
    - "AIRequest carries prompt, maxTokens, stream, signal fields with no eslint-disable suppressions"
    - "AIResponse carries text, usdCost, usage fields with no eslint-disable suppressions"
    - "LastVerdictStore.set/get/clear works per-slug; in-memory only; no data.json field"
    - "submissionOrchestrator fires onVerdict callback for non-Accepted submit verdicts (kind != 'ac', 'unknown', 'unknown-lc')"
    - "Run-failure path captures the FIRST failing case input/expected/actual via splitInput/splitOutput"
    - "Accepted submissions do NOT fire onVerdict (Phase 09's territory)"
  artifacts:
    - path: src/solve/lastVerdictStore.ts
      provides: "LastVerdict interface + LastVerdictStore class (set/get/clear/dispose)"
      exports: ["LastVerdict", "LastVerdictStore"]
    - path: src/ai/types.ts
      provides: "AIRequest + AIResponse with real fields; eslint-disable comments removed"
      contains: "interface AIRequest"
    - path: tests/solve/lastVerdictStore.test.ts
      provides: "Tests for set/get/clear, per-slug isolation, capture filter contract"
      min_lines: 60
    - path: tests/solve/submissionOrchestrator.onVerdict.test.ts
      provides: "Tests for onVerdict callback firing on non-AC verdicts only"
      min_lines: 60
  key_links:
    - from: "src/solve/submissionOrchestrator.ts"
      to: "deps.onVerdict?.(slug, LastVerdict)"
      via: "post-pollSubmission resolve callback"
      pattern: "onVerdict\\?\\."
    - from: "src/ai/AIClient.ts:147"
      to: "req.stream (typed boolean)"
      via: "AIRequest type carries stream field"
      pattern: "req\\.stream"
---

<objective>
Expand the empty Phase 07 AI request/response interfaces with real fields, build the in-memory `LastVerdictStore` (per-slug Map on the LeetCodePlugin instance), and wire the orchestrator post-resolve callback so non-Accepted Run/Submit verdicts populate the store.

Purpose: This plan ships the data-layer foundation Phase 08 plans 02-05 all consume. AIRequest/AIResponse type-clean the existing `(req as { stream?: boolean }).stream` cast in AIClient.invoke. LastVerdictStore is the verdict source-of-truth for buildDebugPrompt (Plan 08-03). Orchestrator hooks make capture happen at the resolve point without polluting the orchestrator with plugin-instance side effects.

Output: Real AIRequest/AIResponse types + new LastVerdictStore module + onVerdict callback wired into submissionOrchestrator and run-failure capture in pollingOrchestrator + comprehensive Wave 0 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ai-debug/08-CONTEXT.md
@.planning/phases/08-ai-debug/08-RESEARCH.md
@.planning/phases/08-ai-debug/08-PATTERNS.md
@.planning/phases/08-ai-debug/08-VALIDATION.md
@CLAUDE.md

@src/ai/types.ts
@src/ai/AIClient.ts
@src/solve/ephemeralTabStore.ts
@src/solve/submissionOrchestrator.ts
@src/solve/pollingOrchestrator.ts
@src/solve/types.ts
@src/solve/statusMap.ts
@src/solve/runArity.ts
@src/solve/verdictModalRenderer.ts
@tests/solve/ephemeralTabStore.test.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->
<!-- Use these directly — no codebase exploration needed. -->

From src/ai/types.ts:60-72 (CURRENT — to be replaced):
```typescript
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface AIRequest {}
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface AIResponse {}
```

From src/ai/AIClient.ts:130-160 (CURRENT — uses cast on line 147):
```typescript
async invoke(req: AIRequest): Promise<AIResponse> {
  // ...
  const wantStream = (req as { stream?: boolean }).stream === true;
  const fetcher = obsidianFetch(wantStream ? 'stream' : 'request');
  const adapter = resolveAdapter(provider, cfg, fetcher);
  return await adapter.invoke(req);
}
```

From src/solve/types.ts:71-117 (RunCheckResponse + SubmitCheckResponse — LastVerdict source of truth):
- `status_code: number`
- `status_msg: string`
- `status_runtime?: string` (e.g. '120 ms')
- `status_memory?: string` (e.g. '14.5 MB')
- `compare_result?: string` (run-side, '110' mask)
- `code_answer?: string[]` (run-side actual outputs)
- `expected_code_answer?: string[]` (run-side expected outputs)
- `code_output?: string | string[]` (submit-side actual)
- `std_output?: string` (submit-side actual)
- `expected_output?: string` (submit-side expected)
- `input?: string` (submit-side failing input)
- `last_testcase?: string` (submit-side fallback failing input)
- `runtime_error?: string` / `full_runtime_error?: string`
- `compile_error?: string` / `full_compile_error?: string`

From src/solve/statusMap.ts:40 (verdict classifier — REUSE, no new helper):
```typescript
export function classifyStatus(code: number, msg?: string): { kind: 'ac' | 'wa' | 'tle' | 'mle' | 're' | 'ce' | 'ole' | 'ie' | 'unknown' | 'unknown-lc'; displayName: string };
```

From src/solve/ephemeralTabStore.ts:35-48, 151-154 (analog — Map-on-Plugin store; LastVerdictStore strips the Plugin arg + reconcile loop):
```typescript
export class EphemeralTabStore {
  private readonly state = new Map<string, string[]>();
  constructor(private readonly plugin: Plugin) {
    plugin.registerEvent(plugin.app.workspace.on('layout-change', () => this.reconcile()));
  }
  dispose(): void { this.state.clear(); }
}
```

From src/solve/runArity.ts (helpers used to identify failing run case):
- `splitOutput(joined: string | string[], arity: number): string[]`
- `splitInput(joinedDataInput: string, linesPerCase: number): string[]`

From src/solve/verdictModalRenderer.ts:170-179 (existing pass-mask computation pattern to reuse for run-failure index):
```typescript
const passMask: boolean[] = (compare_result ?? '').split('').map(c => c === '1');
const firstFailIdx = passMask.findIndex(p => !p);
```

From src/solve/submissionOrchestrator.ts:51-74 (DI shape — extend with onVerdict?):
```typescript
export interface SubmissionOrchestratorDeps {
  fetcher: Fetcher;
  settings: OrchestratorSettings;
  slug: string | null;
  getCurrentBody: () => string;
  login?: () => void | Promise<void>;
}
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill AIRequest/AIResponse + add LastVerdictStore module</name>
  <files>src/ai/types.ts, src/ai/AIClient.ts, src/solve/lastVerdictStore.ts, tests/solve/lastVerdictStore.test.ts, tests/ai/types.test.ts</files>
  <read_first>
    - src/ai/types.ts (current empty interfaces at lines 60-72; ProviderConfig at 21-26 is field-shape precedent)
    - src/ai/AIClient.ts (line 147 cast `(req as { stream?: boolean }).stream` — must be removable after this task)
    - src/solve/ephemeralTabStore.ts (analog — class scaffold, dispose pattern; LastVerdictStore strips plugin arg + reconcile loop)
    - src/solve/types.ts:71-117 (RunCheckResponse + SubmitCheckResponse — every LastVerdict field maps here)
    - .planning/phases/08-ai-debug/08-PATTERNS.md (Pattern Assignments §"src/solve/lastVerdictStore.ts" + §"src/ai/types.ts")
    - .planning/phases/08-ai-debug/08-RESEARCH.md (§"E. AIRequest / AIResponse expansion" + §"Pattern 2: LastVerdictStore" — locked field shapes)
    - .planning/phases/08-ai-debug/08-CONTEXT.md (decision E + decision B)
    - tests/solve/ephemeralTabStore.test.ts (test scaffolding analog)
  </read_first>
  <behavior>
    - AIRequest carries: prompt: string; maxTokens?: number; stream?: boolean; signal?: AbortSignal.
    - AIResponse carries: text: string; usdCost: number; usage?: { inputTokens?: number; outputTokens?: number }.
    - Both eslint-disable comments at lines 60-72 of src/ai/types.ts are REMOVED (verify via grep).
    - AIClient.invoke line 147 cast is removed (req.stream is type-clean now).
    - LastVerdict interface fields exactly: kind ('run-failure' | 'submit-failure'), capturedAt (number), verdictText (string), failingInput?, expectedOutput?, actualOutput?, runtimeMs?, memoryMb?, errorMessage? (all optional strings except kind/capturedAt/verdictText).
    - LastVerdictStore class: private state = new Map<string, LastVerdict>(); set(slug, v); get(slug); clear(); dispose().
    - LastVerdictStore takes NO Plugin constructor arg (deviation from EphemeralTabStore — see PATTERNS.md "Pattern Assignments §lastVerdictStore" "Apply to LastVerdictStore — strip the plugin constructor arg AND the layout-change reconcile loop").
    - Per-slug isolation: set(s1, v1) + set(s2, v2) → both retrievable independently.
    - Re-set overwrites; no per-slug history.
    - clear() + dispose() empty the Map; idempotent.
    - get('unknown') returns undefined.
  </behavior>
  <action>
Edit `src/ai/types.ts` to replace the two empty `AIRequest` / `AIResponse` interfaces at lines 60-72 with real shapes per CONTEXT decision E (verbatim — see 08-RESEARCH "E. AIRequest / AIResponse expansion"). Remove BOTH `// eslint-disable-next-line @typescript-eslint/no-empty-object-type` lines. Field set: AIRequest = { prompt: string; maxTokens?: number; stream?: boolean; signal?: AbortSignal }; AIResponse = { text: string; usdCost: number; usage?: { inputTokens?: number; outputTokens?: number } }.

Edit `src/ai/AIClient.ts` line 147: remove the `(req as { stream?: boolean }).stream === true` cast — write `const wantStream = req.stream === true;` directly. The TypeScript checker must accept this with no errors after the types.ts edit.

Create new file `src/solve/lastVerdictStore.ts` exporting:
- `LastVerdict` interface (per CONTEXT decision B + 08-PATTERNS.md "src/solve/lastVerdictStore.ts" Apply pattern; do NOT include `language` or `slug` — those are passed elsewhere).
- `LastVerdictStore` class with private `state = new Map<string, LastVerdict>()`; methods `set(slug: string, v: LastVerdict): void`, `get(slug: string): LastVerdict | undefined`, `clear(): void`, `dispose(): void`. NO Plugin constructor arg (locked deviation from EphemeralTabStore — verdicts have no tab lifecycle, no reconcile loop). NO imports beyond local types.

Create new file `tests/solve/lastVerdictStore.test.ts`. Mirror the test scaffolding shape from `tests/solve/ephemeralTabStore.test.ts` (vitest describe/it/expect). Cases (one `it` block each):
- "set/get round-trip returns byte-identical verdict"
- "per-slug isolation: two slugs hold independent verdicts"
- "re-set overwrites prior verdict for the same slug"
- "get returns undefined for unknown slug"
- "clear empties the store; subsequent get returns undefined"
- "dispose is idempotent + equivalent to clear"
- "no Plugin constructor arg required (instantiate via `new LastVerdictStore()`)"

Create new file `tests/ai/types.test.ts` (or extend existing — Read first to confirm). Two cases:
- "AIRequest carries the four locked fields and accepts an AbortSignal"
- "AIResponse carries text/usdCost/usage with usage optional"
Use TypeScript compile-time satisfaction (`const _: AIRequest = { prompt: 'x', stream: true, signal: new AbortController().signal }`). Add a runtime test that the eslint-disable comments are NOT present in src/ai/types.ts (read file as string, assert `!fileContent.includes('@typescript-eslint/no-empty-object-type')`).
  </action>
  <verify>
    <automated>npx vitest run tests/solve/lastVerdictStore.test.ts tests/ai/types.test.ts -- --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n 'eslint-disable-next-line @typescript-eslint/no-empty-object-type' src/ai/types.ts` returns 0 hits (the comments are gone).
    - Source: `grep -nE 'export interface AIRequest \{' src/ai/types.ts` matches; the next 5 lines contain `prompt`, `maxTokens?`, `stream?`, `signal?`.
    - Source: `grep -nE 'export interface AIResponse \{' src/ai/types.ts` matches; the next 5 lines contain `text`, `usdCost`, `usage?`.
    - Source: `grep -n '(req as { stream' src/ai/AIClient.ts` returns 0 hits (cast removed).
    - Source: `grep -n 'req.stream === true' src/ai/AIClient.ts` returns at least 1 hit.
    - Source: `src/solve/lastVerdictStore.ts` exists; exports `LastVerdict` interface + `LastVerdictStore` class.
    - Source: `grep -n 'import.*Plugin' src/solve/lastVerdictStore.ts` returns 0 hits (NO Plugin import — deviation from EphemeralTabStore is locked).
    - Source: `grep -nE 'workspace\.on|layout-change|active-leaf-change' src/solve/lastVerdictStore.ts` returns 0 hits (no reconcile loop).
    - Behavior: `npx vitest run tests/solve/lastVerdictStore.test.ts` exits 0 with at least 7 passing assertions.
    - Behavior: `npx vitest run tests/ai/types.test.ts` exits 0.
    - Behavior: `npx tsc --noEmit` exits 0 (the type expansion + cast removal must compile cleanly).
  </acceptance_criteria>
  <done>AIRequest/AIResponse carry real fields with no lint suppressions. LastVerdictStore exists with full set/get/clear/dispose semantics + 7+ tests. AIClient.invoke compiles without the boolean cast. tsc and the two new test files pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire onVerdict callback into submissionOrchestrator + run-failure capture in pollingOrchestrator</name>
  <files>src/solve/submissionOrchestrator.ts, src/solve/pollingOrchestrator.ts, src/solve/runArity.ts, tests/solve/submissionOrchestrator.onVerdict.test.ts</files>
  <read_first>
    - src/solve/submissionOrchestrator.ts (lines 51-74 for SubmissionOrchestratorDeps; resolve point where pollSubmission terminates)
    - src/solve/pollingOrchestrator.ts (run-side resolve point — find where compare_result is computed and the first failing case is identified)
    - src/solve/statusMap.ts:40 (classifyStatus — reused as-is; no modification)
    - src/solve/runArity.ts (splitOutput, splitInput — used to slice the failing case; read both signatures)
    - src/solve/verdictModalRenderer.ts:170-179 (pass-mask computation pattern — REUSE the same `compare_result.split('').map(c => c === '1')` shape)
    - src/solve/lastVerdictStore.ts (the LastVerdict shape — created in Task 1)
    - .planning/phases/08-ai-debug/08-RESEARCH.md (§"Code Examples" Example 6 — submit-side capture; §"Pitfall 8" — run-side per-case identification)
    - .planning/phases/08-ai-debug/08-PATTERNS.md (§"src/solve/submissionOrchestrator.ts + pollingOrchestrator.ts")
    - tests/solve/submissionOrchestrator.test.ts (existing test patterns — DI shape, fixture replay; DO NOT break existing tests)
  </read_first>
  <behavior>
    - SubmissionOrchestratorDeps gains optional `onVerdict?: (slug: string, verdict: LastVerdict) => void`. Optional preserves Wave-0 backward compat.
    - After `pollSubmission` resolves with terminal, classify via `classifyStatus(terminal.status_code, terminal.status_msg)`.
    - Capture filter (locked, verbatim from RESEARCH §"Code Examples" Example 6): `info.kind !== 'ac' && info.kind !== 'unknown' && info.kind !== 'unknown-lc'`. ONLY when filter passes, fire `deps.onVerdict?.(slug, verdict)` with `kind: 'submit-failure'`.
    - Submit-side LastVerdict population (verbatim): failingInput = `terminal.input || terminal.last_testcase`; expectedOutput = `terminal.expected_output ?? asString(terminal.expected_code_answer)`; actualOutput = `terminal.std_output ?? asString(terminal.code_output)`; runtimeMs = `terminal.status_runtime`; memoryMb = `terminal.status_memory`; errorMessage = first non-empty of `[full_compile_error, compile_error, full_runtime_error, runtime_error]`.
    - For run-side (pollingOrchestrator OR wherever `interpret_solution` resolves), if `compare_result` contains at least one '0', fire onVerdict with `kind: 'run-failure'`. Use `passMask = compare_result.split('').map(c => c === '1')`; `firstFailIdx = passMask.findIndex(p => !p)`. failingInput = `splitInput(joinedDataInput, linesPerCase)[firstFailIdx]`; expectedOutput = `splitOutput(expected_code_answer, arity)[firstFailIdx]`; actualOutput = `splitOutput(code_answer, arity)[firstFailIdx]`; errorMessage = first non-empty of run-side error fields.
    - Existing submissionOrchestrator tests still pass (callback is optional; tests that don't supply it must work).
    - Acceptance test: passes a fake `onVerdict` mock; for status_code=10 (Accepted), mock is NOT called. For status_code=11 (WA), mock IS called with `kind: 'submit-failure'`. For Run with `compare_result='110'`, mock IS called with `kind: 'run-failure'` and `failingInput` = the 3rd input chunk (index 2).
  </behavior>
  <action>
Extend `SubmissionOrchestratorDeps` in `src/solve/submissionOrchestrator.ts:51-74`: add `onVerdict?: (slug: string, verdict: LastVerdict) => void` (optional). Import `LastVerdict` from `'./lastVerdictStore'`.

In the submit resolve path (find where `pollSubmission` terminates and the orchestrator returns/resolves), insert verdict capture per CONTEXT B + RESEARCH Example 6. Filter: `classifyStatus(terminal.status_code, terminal.status_msg).kind` NOT IN `{'ac', 'unknown', 'unknown-lc'}`. Build the LastVerdict from the terminal response fields per the locked mapping in `<behavior>`. Helper `firstNonEmpty(...candidates: (string | undefined)[]): string | undefined` may need creating in this file (or read submissionOrchestrator.ts for an existing one — reuse if present); the helper returns the first candidate whose trimmed length > 0, else undefined.

For run-failure capture, read `src/solve/pollingOrchestrator.ts` to locate the run-resolve point (interpret_solution terminal). If pollingOrchestrator exposes the result back to submissionOrchestrator's caller, the onVerdict wiring lives in submissionOrchestrator's run path; otherwise extend pollingOrchestrator's deps similarly. Match the verdict-classification posture: `info.kind !== 'ac' && info.kind !== 'unknown' && info.kind !== 'unknown-lc'` (reuse the same filter — for run, all-pass means kind === 'ac' so the filter is consistent).

Add a small extracted helper in `src/solve/runArity.ts` (or a new file `src/solve/extractRunFailureForVerdictStore.ts` per RESEARCH §Pitfall 8 recommendation) named `extractRunFailureForVerdictStore(res: RunCheckResponse, joinedDataInput: string, metaData: { arity: number; linesPerCase: number }): { failingInput?: string; expectedOutput?: string; actualOutput?: string; errorMessage?: string }`. Pure function. Reuses `splitOutput` and `splitInput` (export them if not already exported). Returns undefined fields when arity/mask is malformed.

Create `tests/solve/submissionOrchestrator.onVerdict.test.ts`. Fixture replay against canned `SubmitCheckResponse` payloads. Cases:
- "Accepted (status_code=10) does NOT fire onVerdict"
- "WA (status_code=11) fires onVerdict with kind='submit-failure' and full LastVerdict shape"
- "TLE (status_code=14) fires onVerdict with runtimeMs populated"
- "MLE (status_code=12) fires onVerdict with memoryMb populated"
- "RE (status_code=15) fires onVerdict with errorMessage from runtime_error"
- "CE (status_code=20) fires onVerdict with errorMessage from compile_error"
- "Unknown status (status_code=999) does NOT fire onVerdict"
- "Run failure with compare_result='110' fires onVerdict with kind='run-failure' and failingInput=splitInput()[2]"
- "Run all-pass (compare_result='111') does NOT fire onVerdict"
- "deps.onVerdict undefined: orchestrator does not throw"
  </action>
  <verify>
    <automated>npx vitest run tests/solve/submissionOrchestrator.onVerdict.test.ts tests/solve/submissionOrchestrator.test.ts -- --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n 'onVerdict?' src/solve/submissionOrchestrator.ts` returns at least 2 hits (one in deps, one at the call site).
    - Source: `grep -nE "kind !== 'ac'.*kind !== 'unknown'.*kind !== 'unknown-lc'" src/solve/submissionOrchestrator.ts` returns at least 1 hit.
    - Source: `grep -n "kind: 'submit-failure'" src/solve/submissionOrchestrator.ts` returns at least 1 hit.
    - Source: `grep -nE 'extractRunFailureForVerdictStore|splitInput|splitOutput' src/solve/` returns hits in the run-side capture path.
    - Behavior: `npx vitest run tests/solve/submissionOrchestrator.onVerdict.test.ts` exits 0 with all 10 cases passing.
    - Behavior: `npx vitest run tests/solve/submissionOrchestrator.test.ts` (existing suite) exits 0 — no regression from optional callback addition.
    - Behavior: `npx tsc --noEmit` exits 0.
    - Source: orchestrator file does NOT import `LastVerdictStore` directly (only `LastVerdict` type). Grep: `grep -n 'LastVerdictStore' src/solve/submissionOrchestrator.ts` returns 0 hits; `grep -n 'LastVerdict[^S]' src/solve/submissionOrchestrator.ts` returns at least 1 hit.
  </acceptance_criteria>
  <done>SubmissionOrchestrator fires onVerdict for non-AC submit verdicts; pollingOrchestrator/run path fires onVerdict for run failures with the right per-case slice. Existing tests stay green. The orchestrators stay pure (LastVerdictStore not imported there).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user note → orchestrator | Note frontmatter + `## Code` body crosses into the orchestrator on Run/Submit; verdict capture reads orchestrator's terminal Response (not user-supplied) |
| orchestrator → LastVerdictStore | Capture callback writes a structured LastVerdict; no user-supplied free-form path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-IL | Information Disclosure | LastVerdictStore (transient verdict cache) | mitigate | In-memory only — no `data.json` field, no logger calls log full verdict bodies. Verdict text contains user code outputs but never crosses a network boundary in this plan. Asserted by grep: no `loadData/saveData` writes from this plan; no `logger.*` calls in `src/solve/lastVerdictStore.ts`. |
| T-08-01-T | Tampering | LastVerdict shape mismatch | mitigate | Field-by-field mapping locked against `src/solve/types.ts:71-117` per RESEARCH Pattern 2. Test fixtures replay real LC payload shapes. |
| T-08-01-R | Repudiation | Verdict capture filter | mitigate | Capture filter `kind !== 'ac' && kind !== 'unknown' && kind !== 'unknown-lc'` is asserted by 10 test cases covering each kind. The filter is the contract that says "Phase 09 owns Accepted handling, not Phase 08". |
| T-08-01-D | Denial of Service | Map unbounded growth | accept | Map grows once per slug per session; cleared on plugin unload. Even at 10k slugs the Map cost is negligible (~1 MB worst case for verbose error messages). No expiry policy needed (rejected in CONTEXT decision B). |
| T-08-01-EoP | Elevation of Privilege | onVerdict callback abuse | mitigate | onVerdict is OPTIONAL on SubmissionOrchestratorDeps; orchestrator never imports LastVerdictStore directly. Callback is injected by `main.ts` only, keeping the orchestrator pure. |
| T-08-01-SC | Tampering | npm/pip/cargo installs | mitigate | NO new packages. Phase 08 reuses Phase 07 lockfile. RESEARCH "Package Legitimacy Audit" already vetted all 4 AI packages. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (type expansion + cast removal compile cleanly).
- `npx vitest run tests/solve/lastVerdictStore.test.ts tests/ai/types.test.ts tests/solve/submissionOrchestrator.onVerdict.test.ts tests/solve/submissionOrchestrator.test.ts -- --reporter=basic` exits 0.
- `npm run lint` exits 0 (the eslint-disable removal must not regress).
- `tests/ai/lc-isolation.test.ts` continues to pass (this plan does NOT add `obsidianFetch` imports under `src/solve/`).
</verification>

<success_criteria>
- AIRequest carries `{ prompt, maxTokens?, stream?, signal? }` and AIResponse carries `{ text, usdCost, usage? }`. Both eslint-disable suppressions removed. `req.stream` is type-clean in AIClient.
- LastVerdictStore module exists with set/get/clear/dispose semantics; in-memory only; no Plugin arg; no reconcile loop. 7+ unit tests pass.
- submissionOrchestrator fires `onVerdict` callback for non-Accepted submit verdicts (kind ∈ {wa, tle, mle, re, ce, ole, ie}); does NOT fire for ac/unknown/unknown-lc.
- Run-side capture (pollingOrchestrator or run path) fires `onVerdict` with `kind: 'run-failure'` and the right per-case `failingInput`/`expectedOutput`/`actualOutput` for the FIRST failing case (passMask first '0').
- Existing submission/run orchestrator tests stay green (optional callback preserves Wave-0 compat).
- Plan 08-02 may now consume AIRequest.signal cleanly. Plan 08-03 may consume LastVerdictStore + LastVerdict shape. Plan 08-04/05 may consume the populated store.
</success_criteria>

<output>
Create `.planning/phases/08-ai-debug/08-01-SUMMARY.md` when done.
</output>
