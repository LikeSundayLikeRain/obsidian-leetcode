---
phase: 08-ai-debug
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - src/ai/AIStreamModal.ts
  - src/ai/buildDebugPrompt.ts
  - src/ai/disclosure.ts
  - styles.css
  - tests/ai/AIStreamModal.streaming.test.ts
  - tests/ai/AIStreamModal.fallback.test.ts
  - tests/ai/AIStreamModal.cancel.test.ts
  - tests/ai/AIStreamModal.fallback.cancel.test.ts
  - tests/ai/AIStreamModal.debounce.test.ts
  - tests/ai/buildDebugPrompt.test.ts
  - tests/ai/disclosure.withDebugBullet.test.ts
autonomous: false
requirements:
  - AIDBG-01
  - AIDBG-02
  - AIDBG-03
must_haves:
  truths:
    - "AIStreamModal renders a Markdown body live (debounced 100ms) when streaming is available"
    - "AIStreamModal renders Thinking… + mm:ss counter on the buffered fallback path"
    - "Cancel button click aborts mid-stream; partial output preserved; cost ledger gets 0"
    - "onClose during in-flight stream calls abortController.abort() (anti-zombie per AIDBG-03)"
    - "Fallback Cancel closes the modal immediately; pending response Promise is .catch'd to swallow"
    - "buildDebugPrompt produces the locked prompt string structure including 'No verdict yet' empty-store path"
    - "withDebugBullet does NOT mutate DISCLOSURE_BASE_COPY; returns a fresh object"
    - "## Notes content NEVER appears in buildDebugPrompt output"
    - "Manual UAT: live render of a 2000-token Anthropic response shows no flicker / no scroll-jump (verification gate)"
  artifacts:
    - path: src/ai/AIStreamModal.ts
      provides: "Modal subclass with stream consumption + debounced render + counter + Cancel + Copy"
      exports: ["AIStreamModal", "AIStreamModalArgs"]
      min_lines: 200
    - path: src/ai/buildDebugPrompt.ts
      provides: "Pure function assembling prompt from problemMd + code + language + lastVerdict?"
      exports: ["buildDebugPrompt"]
      min_lines: 40
    - path: src/ai/disclosure.ts
      provides: "withDebugBullet factory composed onto frozen DISCLOSURE_BASE_COPY"
      contains: "withDebugBullet"
    - path: styles.css
      provides: "5 new selectors under .leetcode-ai-stream-* targeting < 1 KB total"
      contains: ".leetcode-ai-stream-thinking"
  key_links:
    - from: "src/ai/AIStreamModal.ts:onOpen"
      to: "aiClient.invokeStream({ prompt, stream: true, signal })"
      via: "constructor arg AIClient + AbortController owned by modal"
      pattern: "invokeStream"
    - from: "src/ai/AIStreamModal.ts:flushRender"
      to: "MarkdownRenderer.render(this.app, buffer, bodyEl, '', this)"
      via: "Modal extends Component (satisfies obsidianmd/no-plugin-as-component)"
      pattern: "MarkdownRenderer\\.render"
    - from: "src/ai/AIStreamModal.ts:scheduleRender"
      to: "setWindowTimeout(flushRender, 100)"
      via: "100ms debounce ring buffer (Pitfall 1 Tier 1)"
      pattern: "RENDER_DEBOUNCE_MS"
---

<objective>
Build `AIStreamModal` (the modal that opens immediately, consumes the stream live with a 100ms debounced MarkdownRenderer.render, owns the AbortController, and exposes Cancel + Copy + Close), plus the pure `buildDebugPrompt` helper that assembles the prompt from problem markdown + `## Code` + last verdict + language, plus the `withDebugBullet` composition factory that extends the frozen DISCLOSURE_BASE_COPY for AI Debug.

Purpose: This is the user-facing centerpiece of Phase 08. The modal validates RESEARCH Pitfall 1 (live re-render flicker) via the manual UAT step — single highest-uncertainty decision in the phase. Cancel UX (AIDBG-03) lives entirely in this plan: stream-time abort, modal-dismiss anti-zombie, fallback swallowed-promise. The disclosure factory is the composition contract Phase 09 + Phase 11 will mirror.

Output: AIStreamModal.ts (~200 lines) + buildDebugPrompt.ts (pure transform) + withDebugBullet factory + 7 new test files covering streaming/fallback/cancel/debounce/prompt-assembly/disclosure-immutability + 5 new CSS selectors + a manual UAT verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ai-debug/08-CONTEXT.md
@.planning/phases/08-ai-debug/08-RESEARCH.md
@.planning/phases/08-ai-debug/08-PATTERNS.md
@.planning/phases/08-ai-debug/08-VALIDATION.md
@.planning/phases/08-ai-debug/08-UI-SPEC.md
@.planning/phases/08-ai-debug/08-01-SUMMARY.md
@.planning/phases/08-ai-debug/08-02-SUMMARY.md
@CLAUDE.md

@src/ai/AIClient.ts
@src/ai/disclosure.ts
@src/ai/types.ts
@src/solve/lastVerdictStore.ts
@src/solve/VerdictModal.ts
@src/solve/codeExtractor.ts
@src/preview/ProblemPreviewView.ts
@src/shared/timers.ts
@src/shared/logger.ts
@styles.css
@tests/ai/disclosure.test.ts

<interfaces>
<!-- Key contracts the executor needs. -->

From src/ai/AIClient.ts (post-Plan-08-02):
```typescript
async invokeStream(req: AIRequest): Promise<
  | { kind: 'stream'; result: StreamTextResult<{}, {}>; abortController: AbortController }
  | { kind: 'buffered'; text: Promise<string>; abortController: AbortController }
>;
async addCost(usd: number): Promise<void>;
```

From src/ai/disclosure.ts:55-73 (DISCLOSURE_BASE_COPY — frozen at module load):
```typescript
export const DISCLOSURE_BASE_COPY = {
  willSend: Object.freeze([
    'Problem text (statement, examples, constraints)',
    'Your `## Code` content',
    'The last run/submit verdict and failing test (if any)',
    'Optionally your `## Notes` (only if you opt in per feature)',
  ]),
  neverSends: Object.freeze([...]),
};
Object.freeze(DISCLOSURE_BASE_COPY);
```

From src/solve/lastVerdictStore.ts (Plan 08-01):
```typescript
export interface LastVerdict {
  kind: 'run-failure' | 'submit-failure';
  capturedAt: number;
  verdictText: string;
  failingInput?: string;
  expectedOutput?: string;
  actualOutput?: string;
  runtimeMs?: string;
  memoryMb?: string;
  errorMessage?: string;
}
```

From src/ai/types.ts (post-Plan-08-01) — prettyName for modal title:
```typescript
export function prettyName(provider: AIProvider): string;  // 'Anthropic' | 'OpenAI' | 'OpenRouter' | 'Ollama' | 'Custom'
```

From src/preview/ProblemPreviewView.ts:474-481 (MarkdownRenderer.render in non-MarkdownView precedent):
```typescript
const body = root.createDiv({ cls: 'leetcode-preview__body markdown-rendered' });
void MarkdownRenderer.render(this.app, md, body, '', this);
```

From src/shared/timers.ts (counter + debounce primitives):
```typescript
export type TimerHandle;
export function setWindowTimeout(cb: () => void, ms: number): TimerHandle;
export function clearWindowTimeout(handle: TimerHandle): void;
```

From src/solve/VerdictModal.ts:49-63 (onClose anti-zombie pattern — the exact analog for AIDBG-03):
```typescript
onClose(): void {
  if (this.isPending) {
    this.isPending = false;
    try { this.args.onCancel(); } catch { /* ignore */ }
  }
  const { contentEl } = this;
  contentEl.empty();
}
```
The Phase 08 mirror replaces `onCancel()` with `abortController.abort()` and adds `clearWindowTimeout` for both renderTimer + counterTimer.

Locked CSS additions (from 08-UI-SPEC §"Bundle Constraint"):
- `.leetcode-ai-stream` — empty rule, scoping anchor
- `.leetcode-ai-stream-body` — `padding: 0`
- `.leetcode-ai-stream-thinking` — `text-align: center; padding: 16px 0; color: var(--text-muted); font-size: 13px; font-variant-numeric: tabular-nums`
- `.leetcode-ai-stream-cancelled` — `color: var(--text-muted); font-style: italic; font-size: 13px; margin: 0 0 8px 0`
- `.leetcode-ai-stream-error` — `color: var(--text-error); font-size: 13px; margin: 8px 0 0 0`
- Comma-extend `.leetcode-verdict-footer` rule at lines 627-633 to include `.leetcode-ai-stream-footer`

Locked verbatim copy (from 08-UI-SPEC §"Copywriting Contract"):
- Modal title: `AI Debug — {prettyName(provider)}`
- Body initial: `Thinking…` (U+2026 — single ellipsis, NOT three dots)
- Counter format: `Thinking… {mm}:{ss}` with NBSP between word and digits, padded `00:03`, freezes at `99:59+`
- Cancelled indicator: `Cancelled — partial response below.`
- Disclosure-cancelled body: `AI call cancelled.`
- Error heading: `Couldn't reach {prettyName(provider)}.` (apostrophe via U+2019)
- Cancel button: `Cancel`
- Copy button: `Copy response`
- Close button: `Close`
- Notice on copy success: `AI response copied.` (2000ms)
- Notice on clipboard unavailable: `Clipboard unavailable.` (4000ms)
- Disclosure bullet text (verbatim): `AI Debug also sends the last failing run/submit verdict for this problem (input, expected output, your output, error message)`

Locked debounce constant: `RENDER_DEBOUNCE_MS = 100`
Locked counter tick: `COUNTER_TICK_MS = 1000`
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add buildDebugPrompt pure helper + withDebugBullet disclosure factory</name>
  <files>src/ai/buildDebugPrompt.ts, src/ai/disclosure.ts, tests/ai/buildDebugPrompt.test.ts, tests/ai/disclosure.withDebugBullet.test.ts</files>
  <read_first>
    - src/ai/disclosure.ts (lines 55-73 for DISCLOSURE_BASE_COPY frozen baseline)
    - src/solve/lastVerdictStore.ts (LastVerdict interface — Plan 08-01)
    - src/solve/codeExtractor.ts (pure-string-transform analog — no imports beyond types, no I/O, no captured state)
    - .planning/phases/08-ai-debug/08-PATTERNS.md (§"src/ai/buildDebugPrompt.ts" + §"src/ai/disclosure.ts" — locked patterns + acceptance criteria)
    - .planning/phases/08-ai-debug/08-RESEARCH.md (§"Pattern 3: Prompt Assembly via buildDebugPrompt" + §"Pattern 5: Disclosure Composition Factory")
    - .planning/phases/08-ai-debug/08-CONTEXT.md (decision A — prompt scope; decision D-Disclosure copy extension — locked bullet text)
    - .planning/phases/08-ai-debug/08-UI-SPEC.md (§"Disclosure copy extension" — verbatim bullet)
    - tests/ai/disclosure.test.ts (existing test scaffolding to extend)
  </read_first>
  <behavior>
    - `buildDebugPrompt(args: { problemMd: string; code: string; language: string; lastVerdict?: LastVerdict }): string`. Pure: same args → byte-identical output.
    - Empty-store path (lastVerdict undefined): output contains literal `'No verdict yet — review the code as-is.'` line where the verdict block would be.
    - The output structure (locked, see RESEARCH §Pattern 3): "You are debugging a LeetCode solution..." preamble → `## Problem\n{problemMd}` → `## My {language} solution\n{fence}` → `## What happened on my last run\n{verdictBlock}` → `## Tasks\n{3 numbered tasks}`.
    - `formatVerdictBlock(v)` helper inside the same file produces lines: `Verdict: {verdictText}`, optional Input/Expected output/My output/Runtime/Memory/Error blocks. Error wrapped in fence.
    - `## Notes` content NEVER appears (test fixture: pass `## Notes\nMy hypothesis is…` as part of `code` string and assert no occurrence in output — though `## Notes` shouldn't be in the `code` arg shape anyway, the test is defense-in-depth against regression).
    - `withDebugBullet(base): { willSend, neverSends }` returns FRESH object with `willSend = [...base.willSend, '<verbatim bullet>']` and `neverSends = base.neverSends` (reference equality preserved — both frozen).
    - DISCLOSURE_BASE_COPY is unchanged after factory call (Object.isFrozen + length check).
    - Bullet text VERBATIM: `AI Debug also sends the last failing run/submit verdict for this problem (input, expected output, your output, error message)`.
  </behavior>
  <action>
Create new file `src/ai/buildDebugPrompt.ts`. Pure module — only import `import type { LastVerdict } from '../solve/lastVerdictStore';`. Export `buildDebugPrompt(args)` matching the signature in `<behavior>`. Internal helper `formatVerdictBlock(v: LastVerdict): string`. Use the joined-array idiom (`return [line1, line2, ...].join('\n');`) per RESEARCH §Pattern 3. The empty-store path uses the literal string `'No verdict yet — review the code as-is.'` (verbatim from CONTEXT decision A). Numbered Tasks block: "1. Tell me what is wrong.", "2. Suggest the smallest fix that makes the failing case pass.", "3. Show the corrected code in a fenced block. Do not rewrite unchanged sections; show the full corrected solution only if a partial diff would be confusing." (verbatim from RESEARCH §Pattern 3).

Edit `src/ai/disclosure.ts` to add the `withDebugBullet` exported function per UI-SPEC §"Disclosure copy extension" (the locked composition pattern in `<interfaces>` above). Place it near `DISCLOSURE_BASE_COPY` so subsequent phases (`withReviewBullet`, `withKgBullet`) live alongside as siblings. Function signature: `export function withDebugBullet(base: { willSend: readonly string[]; neverSends: readonly string[] }): { willSend: readonly string[]; neverSends: readonly string[] }`.

Create `tests/ai/buildDebugPrompt.test.ts`. Cases:
- "verbatim fixture round-trip with full LastVerdict" — assert exact prompt output against a hardcoded expected string for a fixed args input.
- "empty-store path emits literal 'No verdict yet — review the code as-is.'" — assert the substring is present and the verdictBlock placeholder is gone.
- "## Notes content NEVER appears in output" — pass code containing `## Notes\nMy hypothesis...` (defensive even though `code` shouldn't carry this); assert `'## Notes'` does NOT appear in the prompt output.
- "all 4 mandatory inputs present" — assert problem text, code, language, and (when provided) verdict block all appear.
- "TLE verdict produces Runtime: line" — fixture with `runtimeMs: '1500 ms'` produces `Runtime: 1500 ms`.
- "MLE verdict produces Memory: line" — fixture with `memoryMb: '256 MB'` produces `Memory: 256 MB`.
- "RE/CE verdict produces Error: fenced block" — fixture with errorMessage produces `Error:\n```\n{msg}\n```` block.
- "determinism: same args → byte-identical output across 100 calls".

Create `tests/ai/disclosure.withDebugBullet.test.ts` (or extend `tests/ai/disclosure.test.ts` per VALIDATION.md Wave 0 list). Cases:
- "withDebugBullet returns fresh object; base unchanged" — assert `Object.isFrozen(DISCLOSURE_BASE_COPY)` AND `Object.isFrozen(DISCLOSURE_BASE_COPY.willSend)` AND `DISCLOSURE_BASE_COPY.willSend.length` is unchanged after the call.
- "result.willSend.length === base.willSend.length + 1"
- "result.willSend ends with the locked verbatim bullet"
- "result.neverSends === base.neverSends" (reference equality — both frozen).
- "calling withDebugBullet twice returns two independent objects with the SAME bullet appended each time"
  </action>
  <verify>
    <automated>npx vitest run tests/ai/buildDebugPrompt.test.ts tests/ai/disclosure.withDebugBullet.test.ts tests/ai/disclosure.test.ts -- --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/ai/buildDebugPrompt.ts` exists; only import is the LastVerdict type; no I/O, no Plugin/App/Vault references (grep returns 0 hits for `obsidian|Plugin|App|Vault` in this file).
    - Source: `grep -n "No verdict yet — review the code as-is\\." src/ai/buildDebugPrompt.ts` returns at least 1 hit (verbatim string locked).
    - Source: `grep -n "withDebugBullet" src/ai/disclosure.ts` returns at least 2 hits (declaration + body).
    - Source: `grep -n "AI Debug also sends the last failing run/submit verdict for this problem" src/ai/disclosure.ts` returns at least 1 hit (verbatim bullet).
    - Source: the withDebugBullet function body uses spread (`...base.willSend`), NOT `.push()` — `grep -nE 'willSend\\.push' src/ai/disclosure.ts` returns 0 hits.
    - Behavior: `npx vitest run tests/ai/buildDebugPrompt.test.ts` exits 0 with all 8 cases passing.
    - Behavior: `npx vitest run tests/ai/disclosure.withDebugBullet.test.ts` exits 0 with all 5 cases passing.
    - Behavior: existing `tests/ai/disclosure.test.ts` continues to pass (no regressions to Phase 07 disclosure tests).
  </acceptance_criteria>
  <done>buildDebugPrompt is pure and deterministic; empty-store path emits the locked literal; ## Notes never appears. withDebugBullet composes onto frozen DISCLOSURE_BASE_COPY without mutation. 13+ tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build AIStreamModal with live render + counter + Cancel + Copy + 5 CSS selectors</name>
  <files>src/ai/AIStreamModal.ts, styles.css, tests/ai/AIStreamModal.streaming.test.ts, tests/ai/AIStreamModal.fallback.test.ts, tests/ai/AIStreamModal.cancel.test.ts, tests/ai/AIStreamModal.fallback.cancel.test.ts, tests/ai/AIStreamModal.debounce.test.ts</files>
  <read_first>
    - src/ai/AIClient.ts (post-Plan-08-02; invokeStream return shape; addCost method)
    - src/ai/types.ts (prettyName function for modal title)
    - src/solve/VerdictModal.ts (lines 27-130 — Modal scaffold, args interface, footer construction, default-focus, onClose anti-zombie pattern, clipboard Notice pattern)
    - src/solve/VerdictModal.ts:198-208 (clipboard Notice precedent — `'Payload copied.'` → mirror as `'AI response copied.'`)
    - src/preview/ProblemPreviewView.ts:474-481, 524-535 (MarkdownRenderer.render call shape; setWindowTimeout counter precedent)
    - src/shared/timers.ts (setWindowTimeout, clearWindowTimeout, TimerHandle type)
    - src/shared/logger.ts (logger.debug shape — log metadata only, not full prompts)
    - styles.css (lines 212-214, 627-633, 741-748, 749-751, 971-988, 1346-1348 — all the CSS analogs)
    - tests/ai/disclosure.test.ts (Modal mock pattern at lines 40-72 — REUSE for AIStreamModal tests)
    - .planning/phases/08-ai-debug/08-PATTERNS.md (§"src/ai/AIStreamModal.ts" — full implementation skeleton; §"styles.css" — locked selector set)
    - .planning/phases/08-ai-debug/08-RESEARCH.md (§"Pattern 4: AIStreamModal" — full skeleton with debounce + counter + cancel; §Pitfalls 1, 2, 3, 4, 6 — every pitfall must be mitigated)
    - .planning/phases/08-ai-debug/08-UI-SPEC.md (§"Surface 4: AIStreamModal layout"; §"Behavior Contract"; §"Copywriting Contract"; §"Bundle Constraint")
    - .planning/phases/08-ai-debug/08-CONTEXT.md (decision D — modal UX; decision D-Disclosure modal interaction; cost ledger posture)
    - tests/helpers/electronNetStub.ts (Plan 08-02 — signal-aware fetch mock)
  </read_first>
  <behavior>
    - `class AIStreamModal extends Modal`. Constructor `(app: App, args: AIStreamModalArgs)` where args = `{ provider: AIProvider; prompt: string; aiClient: AIClient; disclosureCopy?: DisclosureCopy }`.
    - `RENDER_DEBOUNCE_MS = 100`. `COUNTER_TICK_MS = 1000`. Both grep-locked constants.
    - `onOpen()`: title `AI Debug — {prettyName(provider)}`; body shows `Thinking…` placeholder + `aria-live="polite"`; footer `[Cancel]` button default-focused; mm:ss counter started via `setWindowTimeout(tick, 1000)`; `aiClient.invokeStream(req)` invoked.
    - On result.kind === 'stream': for-await consumes `result.textStream`. First chunk → `body.empty()`, `stopCounter()`, append text to internal `buffer`, `scheduleRender()`. Each subsequent chunk: `buffer += chunk; scheduleRender()`. Zero-length chunk guard: `if (chunk.length === 0) continue;` (Assumption A3).
    - `scheduleRender()`: if `renderTimer != null`, return (already scheduled). Else `renderTimer = setWindowTimeout(() => { renderTimer = null; void flushRender(); }, 100)`.
    - `flushRender()`: `body.empty(); MarkdownRenderer.render(this.app, this.buffer, this.bodyEl, '', this)`. The `this` 5th arg is the Modal (Modal extends Component; satisfies obsidianmd/no-plugin-as-component).
    - Stream-end (for-await loop exits naturally): `completed = true; await flushRender(); usage = await result.usage; cost = estimateUsd(provider, usage); await aiClient.addCost(cost); swapToCompletionFooter();`. (estimateUsd is a small inline helper that consults Phase 07 pricing table; if no pricing table is exposed, the helper returns 0 — graceful degradation).
    - Stream-error catch: `if (this.abortController.signal.aborted) { cancelled = true; await aiClient.addCost(0); swapToCancelledFooter(); return; } else { handleError(err); }`. The signal-aborted check fires BEFORE inspecting err.name (Pitfall 2 — SDK 6.x may wrap AbortError as APICallError).
    - On result.kind === 'buffered' (fallback): body keeps Thinking… + counter ticking; `await result.text` resolves; counter stops; body cleared; MarkdownRenderer.render runs ONCE on the full text; footer swaps to `[Copy] [Close]`. cost ledger gets either the usage (if exposed) or 0.
    - Cancel button click → `abortController.abort()`; the for-await catch handles the rest.
    - Modal `onClose()`: if `!completed && !cancelled`, `abortController.abort()` (anti-zombie AIDBG-03 + Pitfall 4). `clearWindowTimeout(renderTimer)` if non-null. `clearWindowTimeout(counterTimer)` if non-null. `contentEl.empty()`.
    - Counter tick: pads `mm` and `ss` with leading zero; uses NBSP ` ` between `Thinking…` and digits; format `Thinking… 00:03`. Freeze at `99:59+`.
    - Copy button click: `await navigator.clipboard.writeText(this.buffer); new Notice('AI response copied.', 2000)`. Catch clipboard error → `new Notice('Clipboard unavailable.', 4000)`. Mirrors VerdictModal.ts:198-208 verbatim except for Notice text.
    - Disclosure-cancelled handling: AIClient.invokeStream throws `Error('AI call cancelled')` from the disclosure gate. AIStreamModal catches → body shows `AI call cancelled.` + footer becomes `[Close]` only (no Copy — there's nothing to copy).
    - Network error handling: catch in onOpen → body shows `<h3>Couldn't reach {prettyName(provider)}.</h3><p class="leetcode-ai-stream-error">{vendor message — truncated to 200 chars}</p>` + footer becomes `[Close]` only. Apostrophe is U+2019.
    - Fallback Cancel: when result.kind === 'buffered' AND user clicks Cancel before resolution → `this.close()`; the pending `result.text` Promise lands in a `.catch(() => { /* swallowed - modal closed */ })` so no unhandled rejection (Pitfall 3).
  </behavior>
  <action>
Create `src/ai/AIStreamModal.ts`. Use the implementation skeleton in PATTERNS.md §"src/ai/AIStreamModal.ts" + RESEARCH §"Pattern 4" as the reference. Imports: `Modal, MarkdownRenderer, Notice, type App` from `'obsidian'`; `setWindowTimeout, clearWindowTimeout, type TimerHandle` from `'../shared/timers'`; `logger` from `'../shared/logger'`; `AIClient` (type) and `prettyName` from `./AIClient` and `./types` respectively; `AIProvider` from `./types`. Constants `RENDER_DEBOUNCE_MS = 100;` and `COUNTER_TICK_MS = 1000;` at module scope (grep-locked).

Modal class fields: `private buffer = ''`, `private bodyEl!: HTMLElement`, `private footerEl!: HTMLElement`, `private cancelBtn?: HTMLButtonElement`, `private renderTimer: TimerHandle | null = null`, `private counterTimer: TimerHandle | null = null`, `private elapsedSec = 0`, `private abortController = new AbortController()`, `private completed = false`, `private cancelled = false`.

Implement: `onOpen()` (async — paints DOM, default-focuses Cancel, starts counter, awaits aiClient.invokeStream, dispatches on result.kind); `consumeStream(result)` (for-await + signal-aborted distinguish + cost-on-finish); `renderBuffered(textPromise)` (await the Promise, render once, handle cancel-before-resolve via `.catch(() => {})`); `scheduleRender()`; `flushRender()`; `startCounter()`; `tick()`; `stopCounter()`; `swapToCompletionFooter()` (replaces Cancel with Copy + Close); `swapToCancelledFooter()` (prepends "Cancelled — partial response below." line, swaps to Copy + Close); `swapToErrorFooter()` (Close only); `handleCancel()` (abortController.abort()); `handleError(err)` (renders Couldn't reach + truncated vendor message); `onClose()` (anti-zombie abort + clear both timers + empty contentEl).

Inside `consumeStream(result)`: log metadata-only on entry — `logger.debug('ai-stream.start', { provider: this.args.provider, model: cfg?.model, promptCharCount: this.args.prompt.length });` (NEVER log the prompt body itself per Pitfall 10). On natural finish, `logger.debug('ai-stream.finish', { duration, ok: true })`. On abort, `logger.debug('ai-stream.abort', { duration })`. NEVER log `this.buffer` or any chunk content.

Append to `styles.css` (target < 1 KB total):
- `.leetcode-ai-stream { /* scoping anchor */ }` (empty rule)
- `.leetcode-ai-stream-body { padding: 0; }`
- `.leetcode-ai-stream-thinking { text-align: center; padding: 16px 0; color: var(--text-muted); font-size: 13px; font-variant-numeric: tabular-nums; }`
- `.leetcode-ai-stream-cancelled { color: var(--text-muted); font-style: italic; font-size: 13px; margin: 0 0 8px 0; }`
- `.leetcode-ai-stream-error { color: var(--text-error); font-size: 13px; margin: 8px 0 0 0; }`
- Comma-extend the existing `.leetcode-verdict-footer, .leetcode-verdict-action-row` rule at lines 627-633 to include `.leetcode-ai-stream-footer` (ZERO net new styling — just adds a 3rd selector to the comma group).

Create the 5 test files (test names from VALIDATION.md + RESEARCH §"Wave 0 Gaps"). Use the Modal mock from `tests/ai/disclosure.test.ts:40-72` and the electronNetStub from Plan 08-02:

`tests/ai/AIStreamModal.streaming.test.ts`:
- "first chunk replaces Thinking… placeholder; counter stops" — drive a controlled AsyncIterable<string>, assert body content after first chunk yields.
- "MarkdownRenderer.render called via flushRender after debounce window" — mock MarkdownRenderer.render and count calls.
- "stream-end fires onFinish-style cost-ledger update" — assert addCost called with non-zero on natural finish.
- "buffer accumulates across chunks; final flush has full content" — feed 3 chunks, assert final buffer = concatenation.

`tests/ai/AIStreamModal.fallback.test.ts`:
- "kind: 'buffered' shows Thinking… + counter; counter ticks every 1000ms" — vi.useFakeTimers(); advance 1000, 2000, 3000 ms; assert counter renders `00:01`, `00:02`, `00:03` with NBSP separator.
- "single MarkdownRenderer.render on text Promise resolution" — mock; assert exactly 1 call after resolve.
- "counter stops on text resolution" — assert counterTimer is null after resolve.
- "counter clamps at 99:59+ defensively" — fast-forward 6000+ seconds; assert format becomes `99:59+` and counter stops.

`tests/ai/AIStreamModal.cancel.test.ts`:
- "Cancel button click calls abortController.abort()" — spy on abort.
- "after Cancel, signal.aborted === true; for-await catch fires; addCost called with 0" — Pitfall 6 lock.
- "after Cancel, footer swaps to Copy + Close; 'Cancelled — partial response below.' line appears" — DOM inspection.
- "modal onClose during in-flight stream calls abortController.abort()" — anti-zombie AIDBG-03.
- "modal onClose clears renderTimer + counterTimer" — assert both are null after onClose; no setWindowTimeout leak.
- "after natural completion, onClose does NOT abort again (idempotent on completed flag)" — the `!completed && !cancelled` guard.

`tests/ai/AIStreamModal.fallback.cancel.test.ts`:
- "fallback Cancel calls this.close() immediately" — modal closes synchronously even though text Promise pending.
- "pending text Promise lands in .catch (no unhandled rejection)" — Pitfall 3 lock; vitest exits clean.

`tests/ai/AIStreamModal.debounce.test.ts`:
- "5 chunks within 100ms produce exactly 1 flushRender call" — vi.useFakeTimers; mock MarkdownRenderer; count calls.
- "chunks 100ms apart produce 2 flushRender calls" — assert frequency.
- "renderTimer is null after each flush" — invariant: only ONE outstanding scheduled render at a time.

The disclosure interaction (modal opens FIRST showing Thinking… → disclosure stacks on top → on Continue stream begins → on Cancel body shows 'AI call cancelled.') is covered by an integration-style test in `AIStreamModal.cancel.test.ts` per UI-SPEC §Surface 5 + RESEARCH Pitfall 5 ("acceptable per CONTEXT D-Disclosure modal interaction" — no plan change).
  </action>
  <verify>
    <automated>npx vitest run tests/ai/AIStreamModal.streaming.test.ts tests/ai/AIStreamModal.fallback.test.ts tests/ai/AIStreamModal.cancel.test.ts tests/ai/AIStreamModal.fallback.cancel.test.ts tests/ai/AIStreamModal.debounce.test.ts -- --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/ai/AIStreamModal.ts` exists; exports `AIStreamModal` class extending `Modal` and `AIStreamModalArgs` interface.
    - Source: `grep -n 'RENDER_DEBOUNCE_MS = 100' src/ai/AIStreamModal.ts` returns at least 1 hit.
    - Source: `grep -n 'COUNTER_TICK_MS = 1000' src/ai/AIStreamModal.ts` returns at least 1 hit.
    - Source: `grep -n 'this.abortController.signal.aborted' src/ai/AIStreamModal.ts` returns at least 1 hit (Pitfall 2 — signal-aborted distinguish).
    - Source: `grep -n 'addCost(0)' src/ai/AIStreamModal.ts` returns at least 1 hit (Pitfall 6 — cancel cost = 0).
    - Source: `grep -nE 'MarkdownRenderer\\.render\\(this\\.app' src/ai/AIStreamModal.ts` returns at least 1 hit (correct call shape with `this` as 5th arg implicit).
    - Source: `grep -n "AI response copied\\." src/ai/AIStreamModal.ts` returns at least 1 hit (verbatim copy locked).
    - Source: `grep -n "Clipboard unavailable\\." src/ai/AIStreamModal.ts` returns at least 1 hit.
    - Source: `grep -n "Cancelled — partial response below\\." src/ai/AIStreamModal.ts` returns at least 1 hit.
    - Source: `grep -n "AI call cancelled\\." src/ai/AIStreamModal.ts` returns at least 1 hit.
    - Source: `grep -nE 'Couldn\\u2019t reach|Couldn.t reach' src/ai/AIStreamModal.ts` returns at least 1 hit (apostrophe is U+2019 per UI-SPEC).
    - Source: `grep -n "leetcode-ai-stream-thinking" styles.css` returns at least 1 hit.
    - Source: `grep -n "leetcode-ai-stream-cancelled" styles.css` returns at least 1 hit.
    - Source: `grep -n "leetcode-ai-stream-error" styles.css` returns at least 1 hit.
    - Source: `grep -n "leetcode-ai-stream-footer" styles.css` returns at least 1 hit (must appear in comma-grouped rule with `.leetcode-verdict-footer`).
    - Source: `grep -nE 'logger\\.(debug|info|warn|error)\\([^)]*\\bprompt\\b' src/ai/AIStreamModal.ts` returns 0 hits (Pitfall 10 — metadata-only logging).
    - Behavior: `npx vitest run tests/ai/AIStreamModal.*.test.ts` exits 0 with all 5 files passing.
    - Behavior: `npx tsc --noEmit` exits 0.
    - Behavior: `npm run lint` exits 0 (modal must satisfy obsidianmd/no-plugin-as-component via `this` arg in MarkdownRenderer.render).
    - Behavior: `tests/ai/no-prompt-logging.test.ts` (from Plan 08-02) continues to pass — logger calls in AIStreamModal are metadata-only.
    - Behavior: bundle size remains under 1 MB (`npm run check:bundle-size`).
  </acceptance_criteria>
  <done>AIStreamModal class fully wired with stream + fallback + cancel + copy paths. 5 CSS selectors added. 5 test files cover streaming/fallback/cancel/fallback-cancel/debounce. tsc + lint + bundle-size + no-prompt-logging all green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT — Live MarkdownRenderer.render flicker check (Pitfall 1 verification gate)</name>
  <files>src/ai/AIStreamModal.ts</files>
  <what-built>
AIStreamModal with 100ms debounced MarkdownRenderer.render per chunk on the stream path (RESEARCH Pitfall 1 Tier 1 default). Body uses `markdown-rendered` co-class to inherit Obsidian reading-mode CSS cascade. Counter freezes on first chunk. Footer swaps Cancel → Copy + Close on completion.

The single highest-uncertainty decision in Phase 08 is whether 100ms-debounced MarkdownRenderer.render visibly stutters on a long Anthropic streaming response. RESEARCH Pitfall 1 says ship Tier 1 (debounced) first; flip to Tier 2 (append-text + final swap) if dogfood reveals jank. This UAT is the verification gate that decides.
  </what-built>
  <how-to-verify>
1. **Configure a real Anthropic provider:**
   - Open AI Settings; select Anthropic; paste a valid `sk-ant-…` key; pick `claude-haiku-4-5` model; click Test connection (should fire success Notice).

2. **Open a problem note with a long-ish solution:**
   - Pick any Medium-difficulty problem you've solved (e.g. Two Sum, Add Two Numbers, Longest Substring) and open the note. Make sure `## Code` has actual code in it.

3. **Trigger AI Debug:**
   - Click `AI: Debug` under `## Code` (Edit Mode) — wait, this comes in Plan 08-04. For UAT during Plan 08-03, use the palette command IF Plan 08-04 has shipped, OR construct a manual entrypoint test by calling `new AIStreamModal(this.app, args).open()` from the developer console with a constructed `args` object pointing at the AIClient instance.

4. **Observe live render:**
   - Disclosure modal opens first → click Continue.
   - AIStreamModal body fills with streamed Markdown content as ~2000 tokens flow in over ~30-60 seconds.
   - Watch carefully for: visible flicker on each render (should NOT happen with 100ms debounce); scroll-jump as DOM rebuilds (should NOT — body is short-list of paragraphs/fences); broken half-fences while mid-stream (should be brief — < 100ms — because debounce window batches 3-5 chunks).

5. **Verify console:**
   - Open Obsidian devtools (Cmd-Opt-I) → Console tab. During the stream, look for `[Violation] 'requestAnimationFrame' handler took XXms` warnings. Occasional 50-80ms is fine; sustained >100ms warnings indicate Tier 1 is failing.

6. **Stream-end:**
   - Footer swaps to `[Copy response] [Close]`. Click Copy → Notice "AI response copied." fires. Paste somewhere — verify full content.

7. **Cancel mid-stream:**
   - Trigger AI Debug again on the same note. While the stream is mid-flow (~2-3 seconds in), click `Cancel`.
   - Verify: body freezes at current point; "Cancelled — partial response below." appears above the existing rendered Markdown; footer becomes `[Copy response] [Close]`; modal stays open.
   - Open devtools Network tab — confirm no further chunks arrive after the abort.

8. **Decision:**
   - If render is smooth + console clean → APPROVE, Tier 1 (100ms debounce) ships as default. Document UAT outcome in 08-03-SUMMARY.md.
   - If render flickers / scroll-jumps / sustained paint warnings → REPORT to operator. Operator approves switching to Tier 2 (append-text mode during stream + final Markdown swap on completion). RESEARCH §Pitfall 1 documents the Tier 2 approach. Tier 2 trade-off: user sees raw Markdown chars (` ```python `, asterisks) during stream — uglier mid-stream but no flicker.

Approve or report findings.
  </how-to-verify>
  <resume-signal>Type "approved" to confirm Tier 1 (100ms debounce) is shipping, OR describe the flicker/jank observed AND request "Tier 2 swap" if Tier 1 fails the UAT.</resume-signal>
  <action>
This task is a manual UAT checkpoint — no Claude action required during the checkpoint itself. Claude implemented the AIStreamModal in Task 2 with `RENDER_DEBOUNCE_MS = 100` (Tier 1). The operator runs the verification steps in `<how-to-verify>` against a live Anthropic provider on a real 2000-token streaming response. If the live render flickers / scroll-jumps / triggers sustained paint warnings, the operator approves a Tier 2 swap (append-text mode + final Markdown render on stream-end) — RESEARCH §Pitfall 1 documents the exact swap.
  </action>
  <verify>
    <human-check>Operator runs steps 1–8 in &lt;how-to-verify&gt; against a real Anthropic provider and reports back "approved" (Tier 1 ships) OR "Tier 2 swap" (with observed flicker/jank evidence).</human-check>
  </verify>
  <done>Operator has approved Tier 1 (100ms debounce ships as default) OR explicitly requested Tier 2 (append-text-during-stream) and the swap has been performed against the AIStreamModal source. Outcome documented in 08-03-SUMMARY.md "Manual UAT outcome" section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI provider response → MarkdownRenderer | Streamed content rendered into modal body via Obsidian's parser (no innerHTML) |
| AbortController → modal lifecycle | onClose, Cancel button, and disclosure-cancel all ride the same controller |
| Modal → clipboard | Copy button writes the full response text to system clipboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-T-XSS | Tampering | AI response renders as HTML | mitigate | `MarkdownRenderer.render(this.app, md, el, '', this)` only — never `innerHTML`. Asserted by `grep -nE 'innerHTML' src/ai/AIStreamModal.ts` returns 0 hits. Phase 06 PreviewView precedent. |
| T-08-03-IL-prompt | Information Disclosure | Logger leakage of prompt/response | mitigate | Pitfall 10 lock + Plan 08-02 Task 1 added `tests/ai/no-prompt-logging.test.ts` which scans src/ai/ for `logger.{...}.*\\bprompt\\b` matches. AIStreamModal logs ONLY metadata (provider, model, promptCharCount, duration). |
| T-08-03-D-zombie | Denial of Service / Repudiation | Network call survives modal close | mitigate | onClose anti-zombie pattern (Pitfall 4) — `if (!completed && !cancelled) abortController.abort()`. 1 test asserts this. |
| T-08-03-T-cost | Tampering / Repudiation | Cancel inflates cost ledger | mitigate | Pitfall 6 lock — `addCost(0)` on cancel branch; never `await result.usage` in catch (rejects on abort). Asserted by 1 test. |
| T-08-03-IL-clipboard | Information Disclosure | AI response on system clipboard | accept | User-initiated copy is the explicit happy path. Mirrors VerdictModal's Payload Copy precedent. README ## Network usage already discloses what AI sees. No mitigation needed. |
| T-08-03-T-disclosure | Tampering | Disclosure copy mutated downstream | mitigate | `withDebugBullet` factory returns FRESH object; DISCLOSURE_BASE_COPY is `Object.freeze`'d at module load; `.push` would throw in strict mode. Test asserts Object.isFrozen + length unchanged after factory call. |
| T-08-03-D-flicker | Denial of Service (UX) | Live MarkdownRenderer flicker | mitigate | 100ms debounce (Tier 1) + Tier 2 escape hatch (append-text) documented. Manual UAT in Task 3 is the verification gate; if Tier 1 fails, planner flips to Tier 2 with single grep-locked constant change. |
| T-08-03-IL-disclosure-stack | Information Disclosure | Disclosure modal stacking confusion | accept | Pitfall 5 — AIStreamModal opens with empty Thinking… body; disclosure modal stacks ON TOP via Obsidian z-index. UI-SPEC §Surface 5 documents the expected behavior. No fix needed. |
| T-08-03-SC | Tampering | npm/pip/cargo installs | mitigate | NO new packages. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run tests/ai/AIStreamModal.*.test.ts tests/ai/buildDebugPrompt.test.ts tests/ai/disclosure.withDebugBullet.test.ts -- --reporter=basic` exits 0 (5 modal tests + buildDebugPrompt + disclosure factory).
- `tests/ai/no-prompt-logging.test.ts` (Plan 08-02) continues to pass with AIStreamModal added to scan scope.
- `tests/ai/lc-isolation.test.ts` continues to pass (no obsidianFetch leakage outside src/ai/).
- `npm run check:bundle-size` reports the bundle remains under 1 MB. RESEARCH baseline expects ~7 KB total Phase 08 delta; Plan 08-03 contributes the AIStreamModal class (~3 KB minified) + buildDebugPrompt (~0.5 KB) + withDebugBullet (~0.2 KB) + CSS (~< 1 KB).
- `npm run lint` exits 0 (no new eslint-disable; obsidianmd rules pass for the modal).
- Manual UAT (Task 3) approved.
</verification>

<success_criteria>
- AIStreamModal class implements live debounced render + Thinking…/counter fallback + Cancel + Copy + Close + anti-zombie onClose.
- buildDebugPrompt is pure, deterministic, emits "No verdict yet — review the code as-is." on empty store, never includes ## Notes.
- withDebugBullet factory composes onto frozen DISCLOSURE_BASE_COPY (no mutation, fresh object, verbatim bullet text).
- All 7 new test files (5 modal + 1 prompt + 1 disclosure) pass.
- 5 new CSS selectors added (< 1 KB total); `.leetcode-ai-stream-footer` comma-grouped into existing footer rule.
- Manual UAT gates the live-render decision: Tier 1 (100ms debounce) approved OR Tier 2 escape hatch selected by operator.
- Plan 08-04 + 08-05 may now consume `new AIStreamModal(app, args).open()` and `withDebugBullet(DISCLOSURE_BASE_COPY)`.
</success_criteria>

<output>
Create `.planning/phases/08-ai-debug/08-03-SUMMARY.md` when done. MUST document:
- Manual UAT outcome (Tier 1 approved OR Tier 2 swap performed).
- Bundle size delta from this plan.
- Any deviation from the locked CSS selector set.
</output>
