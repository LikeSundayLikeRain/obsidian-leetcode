---
phase: 22-v1-2-path-removal-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - src/widget/WidgetController.ts
  - styles.css
  - .planning/phases/22-v1-2-path-removal-polish/22-VERIFICATION.md
autonomous: false
requirements: []  # carry-over polish items from Phase 20 UAT (ROADMAP §22 success criteria 7-9) + 22-02-04 read-mode font-size cosmetic surfaced during 22-01-B dogfood; no REQUIREMENT IDs from REQUIREMENTS.md own these
must_haves:
  truths:
    - "When the user enters vim Insert mode (`i`) and presses Tab inside the widget, EITHER the block-cursor marker tracks the inserted character AND the indent applies (probe success → 5-LOC fix shipped) OR the cosmetic mismatch is documented in `22-VERIFICATION.md` with a tracked GitHub issue and a README note (probe failure → defer to v1.3.x backlog per D-polish-01 fallback)."
    - "Hovering the mouse over the v1.3 widget surface paints no border (Obsidian default `:hover` outline suppressed via the `.cm-editor .lc-nested-editor:hover` override)."
    - "Cursor-marker, focus-ring, and selection-highlight visual states inside the widget are unchanged from pre-fix behavior (the hover override does not nuke styles at lines 2014-2035)."
    - "The widget's action row chevron + buttons render in the user's text font (`var(--font-text)`), not the inherited `.cm-editor` monospace."
    - "When an LC note is viewed in Reading mode, the v1.3 widget's code content renders at the SAME font-size as it does in Live Preview (empirically 14px) — not the 16px prose default inherited from `.markdown-rendered`. Live Preview rendering remains unchanged."
  artifacts:
    - path: "styles.css"
      provides: "Two new CSS rules — hover-border suppression (D-polish-02) and action-row text-font override (D-polish-03)"
      contains: "leetcode-code-actions"
    - path: "src/widget/WidgetController.ts"
      provides: "Tab handler with vim-aware indent routing (D-polish-01) — only modified IF the dev-vault probe succeeds; otherwise file is unchanged and outcome is documented in 22-VERIFICATION.md"
      contains: "key: 'Tab'"
    - path: ".planning/phases/22-v1-2-path-removal-polish/22-VERIFICATION.md"
      provides: "D-polish-01 probe outcome log (PROBE PASS → fix shipped + commit hash; PROBE FAIL → defer + GitHub issue link)"
      contains: "D-polish-01"
  key_links:
    - from: "src/widget/WidgetController.ts Tab handler"
      to: "@replit/codemirror-vim Vim.handleKey"
      via: "Vim.handleKey(cm5, '<Tab>', 'mapping') input pipeline"
      pattern: "Vim\\.handleKey"
    - from: "styles.css hover override"
      to: "src/widget/WidgetController.ts mounted DOM (`.lc-nested-editor` + `.leetcode-widget-codeblock`)"
      via: "CSS specificity"
      pattern: "\\.lc-nested-editor.*:hover"
    - from: "styles.css action-row font override"
      to: "src/widget/widgetActions.ts mounted DOM (`.leetcode-code-actions`)"
      via: "CSS cascade override of inherited `.cm-editor` monospace"
      pattern: "leetcode-code-actions.*font-family"
    - from: "styles.css read-mode font-size override"
      to: "src/widget/codeBlockProcessor.ts read-mode mount path (`.markdown-rendered .lc-nested-editor`)"
      via: "CSS cascade override of inherited prose font-size in `.markdown-rendered`"
      pattern: "markdown-rendered.*lc-nested-editor.*font-size"
---

<objective>
Plan 22-02 closes three carry-over polish items from Phase 20 UAT (ROADMAP §22 success criteria 7, 8, 9) plus one read-mode font-size cosmetic surfaced during 22-01-B dogfood:

- **D-polish-01 (sc-7)** — vim-Tab cursor-marker desync. The Phase 20-10 hotfix Tab handler at `src/widget/WidgetController.ts:1071-1095` routes Tab through `insertTab` directly, bypassing vim's input pipeline. The CM5-style block-cursor marker doesn't update because vim never observes the keystroke. Time-boxed 30-min dev-vault probe of `Vim.handleKey(cm5, '<Tab>', 'mapping')`; if probe succeeds, ship a 5-LOC fix; if not, defer to v1.3.x backlog with GitHub issue + README note.
- **D-polish-02 (sc-8)** — widget hover border. Hovering the v1.3 widget surface paints an Obsidian-default `:hover` outline. Add a defensive override scoped to `.cm-editor .lc-nested-editor:hover` (and the inner `.leetcode-widget-codeblock:hover` wrapper) suppressing the border. Must NOT nuke the cursor-marker / focus-ring rules at lines 2014-2035.
- **D-polish-03 (sc-9)** — action row uses normal font, not monospace. The chevron + buttons inherit `var(--font-monospace)` from the surrounding `.cm-editor` cascade. Add `.leetcode-code-actions { font-family: var(--font-text); }` (and descendants) so the controls read as UI chrome.
- **D-polish-04 (read-mode font size)** — surfaced during 22-01-B dogfood. In Reading mode the widget's code content renders at the prose font-size (inherited from `.markdown-rendered`) instead of the smaller code/monospace size used by adjacent rendered code blocks, so the widget visibly outsizes other code on the same note. Add a CSS override scoped to `.markdown-rendered .lc-nested-editor .cm-content` (and the equivalent reading-mode wrapper) setting `font-size: var(--code-size)` (with `var(--font-monospace-size)` fallback). Live (Edit) mode must remain unchanged.

Purpose: deliver four independent UI fixes that won't conflict with the 22-01 deletions. ROADMAP §22 frames these as cosmetic — they don't block ship, but they're the user-visible polish that distinguishes a "delete v1.2" milestone from a "delete v1.2 + the rough edges" milestone.

Output: 1-4 commits (one per polish item that ships; D-polish-01 may produce 0 code commits if the probe defers). All commits land on the same branch as 22-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-v1-2-path-removal-polish/22-CONTEXT.md
@.planning/phases/22-v1-2-path-removal-polish/22-RESEARCH.md
@.planning/phases/22-v1-2-path-removal-polish/22-PATTERNS.md
@.planning/phases/22-v1-2-path-removal-polish/22-VALIDATION.md
@.planning/phases/22-v1-2-path-removal-polish/22-01-SUMMARY.md
@CLAUDE.md
</context>

## Phase Goal (Plan 22-02 contribution)

Three carry-over polish items: vim-Tab marker investigation + fix-or-defer; widget hover-border CSS suppression; action-row font CSS override. Each is independently shippable; D-polish-01 may defer per the time-boxed probe outcome.

<artifacts_this_phase_produces>

- **Three new CSS rules** in `styles.css`:
  - **D-polish-02 hover suppression**: `.cm-editor .lc-nested-editor:hover, .cm-editor .lc-nested-editor .leetcode-widget-codeblock:hover { border: none; outline: none; }` inserted near line 1955 (alongside the existing `.lc-nested-editor` rules at 1950-1955).
  - **D-polish-03 action-row font**: `.leetcode-code-actions, .leetcode-code-actions * { font-family: var(--font-text); }` inserted near line 956 or 971 (alongside the existing `.leetcode-code-actions` rules at 943-971).
  - **D-polish-04 read-mode font-size**: a CSS rule scoped under `.markdown-rendered .lc-nested-editor` that forces the widget's code content to the same font-size used in Live Preview (empirically 14px). Implementation pattern (verify selectors against the actual read-mode DOM at execution time): `.markdown-rendered .lc-nested-editor .cm-editor, .markdown-rendered .lc-nested-editor .cm-content, .markdown-rendered .lc-nested-editor .cm-line { font-size: 14px; }`. Targets only `.markdown-rendered` ancestors so live/edit mode is untouched. Prefer matching whatever CSS variable Live Preview already uses (resolved at execution time via DOM inspection in dev vault) over hardcoding `14px`; fall back to `14px` literal only if the source variable can't be identified cleanly.
- **Conditional 5-LOC fix in `src/widget/WidgetController.ts`** (D-polish-01) — produced ONLY if the dev-vault probe succeeds:
  - Adds `import { Vim } from '@replit/codemirror-vim';` if not already imported.
  - Inserts a vim-aware fast-path inside the existing Tab handler at lines ~1075-1090: `if (cm5 && cm5.state.vim?.insertMode) { const handled = Vim.handleKey(cm5, '<Tab>', 'mapping'); if (handled === true) return true; }` immediately before the existing `insertTab(target)` fallthrough.
- **Probe outcome log** in `22-VERIFICATION.md` under `## 22-02-01 Vim-Tab Probe` heading documenting PASS/FAIL and the resulting decision (ship-fix or defer).
- **Commit messages** (1-3 commits per Pattern S-01):
  - If D-polish-01 ships: `fix(22-02): vim-Tab cursor-marker sync via Vim.handleKey route (ROADMAP sc-7)`
  - If D-polish-01 defers: no commit; outcome documented in 22-VERIFICATION.md + GitHub issue filed.
  - `style(22-02): suppress widget hover border (ROADMAP sc-8)`
  - `style(22-02): action row uses text font, not monospace (ROADMAP sc-9)`
- If D-polish-01 defers: a 1-2 line note in `README.md` documenting the cosmetic + GitHub issue link. (The bulk of the README v1.3 update lands in Plan 22-03 D-readme-01; this is just the deferral footnote.)

</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 22-02-01: Vim-Tab cursor-marker probe + fix-or-defer (time-boxed 30 min)</name>
  <files>src/widget/WidgetController.ts (modified IF probe succeeds), .planning/phases/22-v1-2-path-removal-polish/22-VERIFICATION.md, README.md (deferral footnote IF probe fails)</files>
  <read_first>
    - src/widget/WidgetController.ts (lines 1071-1095 — existing Tab handler shape per RESEARCH §4)
    - node_modules/@replit/codemirror-vim/dist/index.d.ts (lines ~11 — `Vim.handleKey(cm, key, origin)` API surface verified)
    - .planning/phases/22-v1-2-path-removal-polish/22-CONTEXT.md (D-polish-01 — 30-min time-box; defer-on-empirical-failure)
    - .planning/phases/22-v1-2-path-removal-polish/22-RESEARCH.md (§4 probe shape + predicted failure mode + alternative `cm5.execCommand('insertTab')` route; Pitfall 4: probe-expanded-into-spike)
    - .planning/phases/22-v1-2-path-removal-polish/22-PATTERNS.md (D-polish-01 5-LOC change template)
    - $HOME/.claude/projects/-Users-moxu-projects-obsidian-leetcode/memory/MEMORY.md (dev-vault path)
  </read_first>
  <action>
    Pre-probe: existing Tab handler at `src/widget/WidgetController.ts:1071-1095` routes vim-Insert-mode Tab through `insertTab(target)` directly. Vim's CM5-style block-cursor marker doesn't update because the keystroke bypasses `Vim.handleKey`. The cosmetic mismatch is visible to vim users (block cursor renders one column behind the actual caret after Tab).

    Post-probe (one of two states):
    - **PASS state**: 5-LOC fix shipped to `WidgetController.ts` adding `Vim.handleKey(cm5, '<Tab>', 'mapping')` route before the existing `insertTab` fallthrough. Block-cursor marker now tracks the actual caret.
    - **FAIL state**: no code change; outcome documented in `22-VERIFICATION.md`; GitHub issue filed; README footnote acknowledges the cosmetic.
  </action>
  <how-to-verify>
    **Time-box: 30 minutes wall-clock.** Per CONTEXT D-polish-01 + RESEARCH Pitfall 4, do NOT expand into a spike. If a definitive answer doesn't emerge in 30 minutes, defer.

    **Setup**:
    1. `npm run build` from repo root.
    2. Copy `main.js`, `manifest.json`, `styles.css` into `~/Documents/Obsidian Vault/.obsidian/plugins/obsidian-leetcode/` (dev-vault path per `MEMORY.md`).
    3. Reload Obsidian (Cmd-R).
    4. In Obsidian Settings → Editor → Vim Mode, verify Vim is ON.

    **Baseline observation (5 min)**:
    1. Open an LC note with widget mounted.
    2. Click into the widget (cursor enters CM6).
    3. Press `i` to enter Insert mode (vim block cursor visible).
    4. Press Tab. Observe: text inserts; the **vim block cursor marker stays at the column it was before Tab** (visually wrong by one indent).
    5. Note this as "PRE-PROBE BASELINE: marker desync confirmed" in `22-VERIFICATION.md` `## 22-02-01 Vim-Tab Probe` (create the heading if missing).

    **Probe (15 min)**:
    1. Edit `src/widget/WidgetController.ts` Tab handler. Per RESEARCH §4 + PATTERNS D-polish-01 template, insert this block immediately after the `if (cm5 && !cm5.state.vim?.insertMode) return false;` line and before the existing `const sel = view.state.selection.main;` line:
       ```typescript
       if (cm5 && cm5.state.vim?.insertMode) {
         const handled = Vim.handleKey(cm5, '<Tab>', 'mapping');
         if (handled === true) return true;
       }
       ```
       Add `import { Vim } from '@replit/codemirror-vim';` to the existing `getCM` import line at the top of the file (or merge into the existing import if `Vim` is already named there).
    2. `npm run build`; redeploy to dev vault; reload Obsidian.
    3. Repeat the baseline observation: enter Insert mode (`i`), press Tab.
    4. Observe two things:
       - **Indent applied**: did Tab insert an indent (4 spaces or `\t` per language)? Y/N.
       - **Marker tracked**: does the vim block cursor visually move forward by the indent width? Y/N.

    **Decision branch (10 min)**:

    - **PROBE PASS** (both observations Y): the fix works; commit it. Run `npm test -- WidgetController` to confirm the existing Tab tests still pass; if they assert on the old behavior, update the assertions to expect the vim-aware route. Document outcome in `22-VERIFICATION.md`: `## 22-02-01 Vim-Tab Probe → PASS — Vim.handleKey routes the keystroke and the block cursor tracks the indent. Fix shipped in commit {hash}.` Commit subject: `fix(22-02): vim-Tab cursor-marker sync via Vim.handleKey route (ROADMAP sc-7)`.

    - **PROBE PARTIAL** (indent Y, marker N): vim's `handleKey` returned `undefined` (or didn't observe Tab). Per RESEARCH §4 predicted failure mode, the marker isn't updated by vim's pre-handler when no command exists for `<Tab>`. **Per CONTEXT D-polish-01 + Pitfall 4: do NOT expand the probe into a spike.** Roll back the WidgetController change (`git restore src/widget/WidgetController.ts`); document outcome as PARTIAL FAIL in `22-VERIFICATION.md`; file a GitHub issue titled `vim-Tab cursor-marker desync (D-polish-01 deferred to v1.3.x)` with the probe results; add a 1-2 line note to `README.md` "Known cosmetic issues" section (or create that section if missing); reply `deferred: vim Vim.handleKey did not update marker — issue {URL}`.

    - **PROBE FAIL** (indent N): the probe broke indent. Roll back; document FAIL; defer; same GitHub issue + README note as PARTIAL.

    **DO NOT** invest beyond 30 min. If the 30 min runs out without a definitive PASS/PARTIAL/FAIL, treat as defer.

    **Resume signal**: reply `approved` (PASS — fix shipped + committed) or `deferred: {one-line reason}` (FAIL/PARTIAL/timeout — outcome documented + issue filed + README footnote in place). Either resume signal advances to Task 22-02-02.
  </how-to-verify>
  <resume-signal>Type `approved` (probe PASS — `fix(22-02): vim-Tab cursor-marker sync via Vim.handleKey route` committed) or `deferred: {reason}` (probe FAIL/PARTIAL/timeout — outcome documented in `22-VERIFICATION.md`; GitHub issue filed; README "Known cosmetic issues" footnote added).</resume-signal>
  <verify>
    <human-check>Resume signal received: `approved` (probe PASS — fix shipped + committed) or `deferred: {one-line reason}` (probe PARTIAL/FAIL/timeout — outcome documented, GitHub issue filed, README footnote added). The 30-minute time-box was not exceeded (per CONTEXT D-polish-01 + RESEARCH Pitfall 4).</human-check>
  </verify>
  <done>
    - `.planning/phases/22-v1-2-path-removal-polish/22-VERIFICATION.md` contains a `## 22-02-01 Vim-Tab Probe` section recording: PROBE PASS / PARTIAL / FAIL / TIMEOUT, observed indent behavior, observed marker behavior, decision (ship-fix vs defer), and wall-clock duration (≤ 30 min).
    - **PASS branch**: `src/widget/WidgetController.ts` Tab handler updated with `Vim.handleKey(cm5, '<Tab>', 'mapping')` route; `import { Vim }` from `@replit/codemirror-vim` present; `npm test -- WidgetController` passes; commit subject is `fix(22-02): vim-Tab cursor-marker sync via Vim.handleKey route (ROADMAP sc-7)`.
    - **DEFER branch**: `src/widget/WidgetController.ts` is unchanged from pre-probe state (`git status` clean for that file); a tracked GitHub issue exists with title `vim-Tab cursor-marker desync (D-polish-01 deferred to v1.3.x)`; `README.md` contains a "Known cosmetic issues" footnote referencing the issue.
    - User typed `approved` or `deferred: ...` to advance to Task 22-02-02.
  </done>

</task>

<task type="auto" tdd="false">
  <name>Task 22-02-02: Suppress widget hover border CSS</name>
  <files>styles.css</files>
  <read_first>
    - styles.css (lines 943-971 for `.leetcode-code-actions` rules; lines 1950-1955 for `.lc-nested-editor` outer/inner wrapper rules; lines 2014-2035 for cursor-marker rules — MUST NOT BREAK these)
    - .planning/phases/22-v1-2-path-removal-polish/22-RESEARCH.md (§5 D-polish-02 + D-polish-03 CSS surface; existing rule shapes; recommended new rule patterns)
    - .planning/phases/22-v1-2-path-removal-polish/22-PATTERNS.md (D-polish-02 self-analog template)
    - .planning/phases/22-v1-2-path-removal-polish/22-CONTEXT.md (D-polish-02 — must NOT nuke cursor-marker / focus-ring styles)
  </read_first>
  <action>
    Per D-polish-02 + RESEARCH §5 + PATTERNS D-polish-02 template, in a single atomic commit:

    1. Open `styles.css`. Locate the existing `.cm-editor .lc-nested-editor { ... }` outer-wrapper rule at lines 1950-1952 and the inner `.cm-editor .lc-nested-editor .leetcode-widget-codeblock { ... }` rule at lines 1953-1955.
    2. Insert immediately after line 1955 (i.e., after the inner-wrapper rule, before the cursor-marker rules at 2014-2035 begin) the new hover-suppression rule per RESEARCH §5 / PATTERNS template:
       ```css
       /* Phase 22 D-polish-02 — suppress Obsidian's default :hover border bleed
        * on the widget surface. Scoped to outer wrapper + inner wrapper to leave
        * cursor-marker / focus-ring rules at lines 2014-2035 unaffected. */
       .cm-editor .lc-nested-editor:hover,
       .cm-editor .lc-nested-editor .leetcode-widget-codeblock:hover {
         border: none;
         outline: none;
       }
       ```
    3. **DO NOT** set `background-color`, `box-shadow`, or any other property in the override — those would defeat the hover-aware grey paint at lines 1953-1955 OR break the focus-ring/cursor-marker visual contract. Limit the override strictly to `border: none; outline: none;`.
    4. Build (`npm run build`) — CSS is bundled into `styles.css` directly by Obsidian's plugin loader, not via esbuild; the build is mostly a no-op for CSS-only changes but verify clean.
    5. Deploy to dev vault and reload Obsidian. Open an LC note; mouse over the widget surface — confirm no border appears. Click into the widget (focus state) — confirm focus ring still renders normally. Place the cursor — confirm the cursor marker still renders. Document outcome in `22-VERIFICATION.md` under `## 22-02-02 Widget Hover Border` heading: `[PASS] hover border absent; focus ring + cursor marker unchanged.` (Or describe the deviation if observed.)
    6. **Commit**: `style(22-02): suppress widget hover border (ROADMAP sc-8)`. Body: 2-3 lines explaining the cascade source (Obsidian default `.cm-editor:hover`) and the scoped suppression strategy.

    **Visual verification is human-driven** — the dev-vault check is the gate. The automated `<verify>` confirms only that the rule exists in `styles.css` and the file passes basic syntax checks.
  </action>
  <verify>
    <automated>grep -c '\.cm-editor \.lc-nested-editor:hover' styles.css | grep -q '^1$' &amp;&amp; grep -c '\.leetcode-widget-codeblock:hover' styles.css | grep -q '^1$' &amp;&amp; npm run build &amp;&amp; git log -1 --format=%s | grep -q '^style(22-02): suppress widget hover border'</automated>
  </verify>
  <done>
    - `styles.css` contains a new rule selector matching `.cm-editor .lc-nested-editor:hover, .cm-editor .lc-nested-editor .leetcode-widget-codeblock:hover` with body `border: none; outline: none;`.
    - The new rule is positioned after line 1955 and before line 2014 (between the inner-wrapper rule and the cursor-marker rules) per the comment scoping intent.
    - `npm run build` clean.
    - Dev-vault visual check: hover border absent; focus ring + cursor marker visually unchanged from pre-fix state.
    - `22-VERIFICATION.md` contains a `## 22-02-02 Widget Hover Border` PASS entry.
    - Commit subject: `style(22-02): suppress widget hover border (ROADMAP sc-8)`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 22-02-03: Action row uses text font, not monospace</name>
  <files>styles.css</files>
  <read_first>
    - styles.css (lines 943-956 for `.leetcode-code-actions` base rule; lines 961-971 for the `!important` `.cm-editor .cm-content > .leetcode-code-actions` cascade-override rule; lines 943-971 entire surface for context)
    - .planning/phases/22-v1-2-path-removal-polish/22-RESEARCH.md (§5 D-polish-03 — recommended rule pattern + variable convention `var(--font-text)`)
    - .planning/phases/22-v1-2-path-removal-polish/22-PATTERNS.md (D-polish-03 self-analog template)
    - .planning/phases/22-v1-2-path-removal-polish/22-CONTEXT.md (D-polish-03 — chevron + buttons read as UI chrome, not code)
  </read_first>
  <action>
    Per D-polish-03 + RESEARCH §5 + PATTERNS D-polish-03 template, in a single atomic commit:

    1. Open `styles.css`. Locate the existing `.leetcode-code-actions { display: flex; ... }` rule at lines 943-956 and the `!important` cascade-override rule at lines 961-971.
    2. Insert immediately after line 971 (after the `!important` cascade-override block — same position so the font override sits inside the same logical CSS section) the new font rule per RESEARCH §5 / PATTERNS template:
       ```css
       /* Phase 22 D-polish-03 — action row chrome reads as UI, not as code.
        * Override inherited monospace from .cm-editor / .cm-content. */
       .leetcode-code-actions,
       .leetcode-code-actions * {
         font-family: var(--font-text);
       }
       ```
       The descendant selector (`.leetcode-code-actions *`) covers chevron text, button labels, and the language-name display.
    3. **Variable choice**: use `var(--font-text)` (canonical token verified by RESEARCH §5; precedent in many existing rules in the file). Do NOT hardcode a font family or use `var(--font-monospace)`.
    4. **Specificity check**: the existing `.cm-editor .cm-content > .leetcode-code-actions` rule at lines 961-971 uses `!important` for `display: flex`. The new font rule at base-class specificity should win against `.cm-content`'s monospace cascade because `.cm-content` doesn't set `font-family` with `!important` (verify by `grep -n 'font-family' styles.css | grep -v 'leetcode-code-actions'` — if any `.cm-content { font-family: ...; }` rule with `!important` appears, the new rule needs `!important` too). Per RESEARCH §5 the base-class specificity is sufficient for non-`!important` `.cm-content` cascades; if the dev-vault check shows monospace still wins, add `!important`.
    5. `npm run build`. Deploy to dev vault, reload Obsidian, open an LC note with widget. Visually inspect the action row chevron + buttons — they should now render in the user's text font (not monospace). Document outcome in `22-VERIFICATION.md` under `## 22-02-03 Action Row Font` heading: `[PASS] chevron + buttons render in var(--font-text); monospace gone.` (Or describe deviation.)
    6. **Commit**: `style(22-02): action row uses text font, not monospace (ROADMAP sc-9)`. Body: 2-3 lines explaining the inherited-monospace source (`.cm-editor` cascade) and the override scope.

    **Visual verification is human-driven**. The automated `<verify>` confirms only that the rule exists and `npm run build` passes.
  </action>
  <verify>
    <automated>grep -c '\.leetcode-code-actions \*' styles.css | grep -q '^1$' &amp;&amp; grep -A 1 '\.leetcode-code-actions \*' styles.css | grep -q 'font-family: var(--font-text)' &amp;&amp; npm run build &amp;&amp; git log -1 --format=%s | grep -q '^style(22-02): action row uses text font'</automated>
  </verify>
  <done>
    - `styles.css` contains a new rule selector `.leetcode-code-actions, .leetcode-code-actions *` with body `font-family: var(--font-text);`.
    - The new rule is positioned after the existing `.leetcode-code-actions` rules (line 971+) per scoping intent.
    - `npm run build` clean.
    - Dev-vault visual check: action row chevron + buttons render in text font; monospace bleed-through gone.
    - `22-VERIFICATION.md` contains a `## 22-02-03 Action Row Font` PASS entry.
    - Commit subject: `style(22-02): action row uses text font, not monospace (ROADMAP sc-9)`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 22-02-04: Read-mode widget font-size matches Live Preview (14px)</name>
  <files>styles.css</files>
  <read_first>
    - styles.css (lines ~1950-2035 — `.lc-nested-editor` outer/inner wrapper rules and adjacent widget styles; search for any existing `.markdown-rendered .lc-nested-editor` rules to understand the read-mode scoping convention already in use)
    - src/widget/codeBlockProcessor.ts (the read-mode mount path — confirms which DOM ancestor classes are present when the widget renders inside `.markdown-rendered`)
    - .planning/phases/22-v1-2-path-removal-polish/22-CONTEXT.md (D-polish ratio + "carry-over polish" framing)
  </read_first>
  <action>
    Empirical baseline (reported by user during 22-01-B dogfood):
    - Live Preview mode: widget code content renders at **14px**.
    - Reading mode: widget code content renders at **16px** (inherited from `.markdown-rendered`'s prose font-size).
    - Goal: read-mode widget renders at the SAME 14px as Live Preview. Live Preview behavior remains untouched.

    In a single atomic commit:

    1. **DOM inspection in dev vault first.** Before editing CSS, open an LC note in Reading mode in the dev vault, open DevTools (Cmd-Option-I), and inspect the widget's code element. Record:
       - The full class chain from `.markdown-rendered` down to the rendered glyph (`.markdown-rendered → ... → .lc-nested-editor → ... → .cm-content / .cm-line / span`).
       - Which CSS rule is currently winning the `font-size` cascade (DevTools "Computed" tab → `font-size: 16px` → click to see the source rule).
       - Whether Live Preview uses a CSS variable (`var(--font-monospace-size)`, `var(--code-size)`, etc.) or a literal — inspect the same widget in Live Preview and check the source of its `14px`.
       - Document findings inline in `22-VERIFICATION.md` under `## 22-02-04 Read-Mode Font-Size` heading.
    2. **Author the override** in `styles.css`. Insert near the existing `.lc-nested-editor` rules (around line 1955+, near D-polish-02's hover rule) with this comment:
       ```css
       /* Phase 22 D-polish-04 — read-mode widget code matches Live Preview size.
        * Reading mode (.markdown-rendered) inherits prose 16px; force it back to
        * the 14px Live Preview uses. Live (Edit) mode is untouched because the
        * .markdown-rendered ancestor isn't present there. */
       .markdown-rendered .lc-nested-editor .cm-editor,
       .markdown-rendered .lc-nested-editor .cm-content,
       .markdown-rendered .lc-nested-editor .cm-line {
         font-size: 14px;
       }
       ```
       **Variable preference rule**: if the DOM-inspection step (1) shows Live Preview's 14px comes from a CSS variable (e.g. `var(--font-monospace-size)`), prefer the variable form over the literal:
       ```css
       font-size: var(--font-monospace-size); /* or whatever variable Live Preview uses */
       ```
       Only fall back to `14px` literal if the source can't be identified cleanly. Document the choice in the inline comment.
    3. **Specificity check.** The `.markdown-rendered` ancestor is already 2-class deep, and the descendant selector adds `.lc-nested-editor .cm-*`, so the specificity is sufficient against the cascade-default `.markdown-rendered { font-size: 16px; }` (which is 1-class). If dev-vault verification shows the override doesn't win, add `!important` to each declaration; document the need for `!important` in the inline comment.
    4. **DO NOT** modify any rule that runs in Live Preview. The `.markdown-rendered` selector ancestor is the safety contract — Live Preview's `.markdown-source-view` ancestor is NEVER affected.
    5. `npm run build`. Deploy to dev vault, reload Obsidian, open the same LC note in BOTH modes:
       - **Reading mode (Cmd-E off)**: widget code is now 14px. Adjacent rendered code blocks (if any) keep whatever size they had — only the widget changes.
       - **Live Preview mode (Cmd-E on)**: widget code is still 14px (unchanged from baseline).
       Document outcome in `22-VERIFICATION.md` `## 22-02-04 Read-Mode Font-Size`: pre-fix vs. post-fix font-size in each mode (use DevTools Computed tab values).
    6. **Commit**: `style(22-02): read-mode widget font-size matches Live Preview (14px)`. Body: 2-3 lines explaining the inherited-prose-16px source + the `.markdown-rendered`-scoped override.

    **Visual verification is human-driven** — the dev-vault check is the gate. The automated `<verify>` confirms only that the new rule exists in `styles.css` and the file passes basic syntax checks.
  </action>
  <verify>
    <automated>grep -c '\.markdown-rendered \.lc-nested-editor' styles.css | grep -qE '^[1-9][0-9]*$' &amp;&amp; npm run build &amp;&amp; git log -1 --format=%s | grep -q '^style(22-02): read-mode widget font-size'</automated>
  </verify>
  <done>
    - `styles.css` contains a new rule selector matching `.markdown-rendered .lc-nested-editor ...` with `font-size` declaration (variable form preferred, `14px` literal fallback acceptable).
    - The new rule is positioned alongside the other widget rules around lines 1950-2035.
    - `npm run build` clean.
    - Dev-vault visual check: Reading-mode widget code renders at 14px (matching Live Preview); Live Preview rendering unchanged.
    - DevTools Computed-tab font-size values recorded in `22-VERIFICATION.md` `## 22-02-04 Read-Mode Font-Size` for both modes (pre-fix and post-fix).
    - Commit subject: `style(22-02): read-mode widget font-size matches Live Preview (14px)`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin code → vim CM5 adapter | D-polish-01 calls `Vim.handleKey(cm5, '<Tab>', 'mapping')` from `@replit/codemirror-vim`; the adapter's behavior is library-defined. |
| Plugin CSS → Obsidian theme cascade | D-polish-02 + D-polish-03 add CSS overrides; relies on standard CSS cascade rules; no JS execution surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02-01 | Tampering | `Vim.handleKey` route mis-dispatches Tab into a vim normal-mode command | mitigate | The probe gates the change: only ship if PASS observation confirms indent applied AND marker tracked. PARTIAL/FAIL → roll back; defer. |
| T-22-02-02 | Information disclosure | CSS overrides break cursor visibility (cursor-marker rules at lines 2014-2035 broken) | mitigate | D-polish-02 explicitly limits override to `border: none; outline: none;` (no `background-color`, no `box-shadow`); dev-vault visual check confirms cursor still renders. |
| T-22-02-03 | Denial of service | The font override breaks readability for users with theme that overrides `--font-text` | accept | `var(--font-text)` is the canonical Obsidian token; if a community theme defines it pathologically, that's a theme issue, not a plugin issue. |
| T-22-02-04 | Tampering | Read-mode font-size override leaks into Live Preview (`.markdown-source-view` ancestor) | mitigate | Selector is anchored on `.markdown-rendered` ancestor — never present in Live Preview. Dev-vault verification confirms Live Preview rendering is unchanged before commit. |
| T-22-02-SC | Tampering | npm/pip/cargo installs | n/a | No new packages. |
</threat_model>

<verification>
**Aggregate plan verification**:

- `npm run build` clean (CSS-only changes for 22-02-02/03/04; conditional WidgetController.ts change for 22-02-01 if probe passes).
- `npm test` green (unchanged from 22-01 close).
- `npm run lint` clean.
- `styles.css` contains all three new rules (D-polish-02 hover + D-polish-03 action-row font + D-polish-04 read-mode font-size).
- Dev-vault visual check entries in `22-VERIFICATION.md` for all four polish tasks (PASS for shipped items; defer + GitHub issue for D-polish-01 if probe failed).
- 2-4 commits land on the working branch (2 if D-polish-01 deferred — three CSS commits minus the vim fix; 4 if D-polish-01 shipped + all three CSS commits).
</verification>

<success_criteria>
- ROADMAP §22 success criterion 7 (vim-Tab cursor-marker sync): EITHER the fix is shipped AND visually confirmed in dev vault OR the cosmetic is documented + tracked + acknowledged in README.
- ROADMAP §22 success criterion 8 (widget hover border): the override CSS rule is in `styles.css`; dev-vault visual check confirms hover border absent + cursor/focus styles intact.
- ROADMAP §22 success criterion 9 (action row font): the override CSS rule is in `styles.css`; dev-vault visual check confirms text font, not monospace.
- D-polish-04 read-mode font-size: the override CSS rule is in `styles.css`; dev-vault visual check confirms Reading-mode widget renders at 14px (matching Live Preview); Live Preview rendering unchanged.
- All commits follow Pattern S-01 atomic-commit-per-concern.
</success_criteria>

<output>
Create `.planning/phases/22-v1-2-path-removal-polish/22-02-SUMMARY.md` when done. The SUMMARY must include:
- D-polish-01 probe outcome (PASS / PARTIAL / FAIL / TIMEOUT) and resulting decision (ship / defer).
- If shipped: commit hash for the WidgetController fix.
- If deferred: GitHub issue URL + README footnote location.
- Commit hashes for the three CSS commits (D-polish-02, D-polish-03, D-polish-04).
- Dev-vault visual-check status for each of the four polish items (PASS / FAIL with notes).
- For 22-02-04: pre-fix vs. post-fix font-size values in both Live Preview and Reading mode (DevTools Computed-tab values).
- Wall-clock duration for the D-polish-01 probe (must be ≤ 30 min per CONTEXT D-polish-01 + RESEARCH Pitfall 4).
</output>
