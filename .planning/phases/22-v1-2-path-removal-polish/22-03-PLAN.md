---
phase: 22-v1-2-path-removal-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 22-01
  - 22-02
files_modified:
  - scripts/check-bundle-size.mjs
  - manifest.json
  - package.json
  - README.md
  - CLAUDE.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/22-v1-2-path-removal-polish/22-VERIFICATION.md
autonomous: false
requirements:
  - POLISH-02
  - POLISH-03
  - POLISH-04
  - POLISH-05
  - POLISH-06
  - THEME-05
  - VIM-03
must_haves:
  truths:
    - "`scripts/check-bundle-size.mjs` HARD_LIMIT is 1,706,000 bytes (v1.2 baseline) and SOFT_WARN is 1,500,000 bytes; running `node scripts/check-bundle-size.mjs` after `npm run build` exits 0 (post-22-01 + 22-02 deletions land below 1.5 MB)."
    - "`npm run lint` passes with `eslint-plugin-obsidianmd`; `grep -rn 'innerHTML' src/widget/` returns zero active assignments (only comments)."
    - "5 community themes (Minimal, Things, Catppuccin, Anuppuccin, Atom) render the widget without visual regression vs. the v1.2 ship-commit baseline; checklist execution + screenshot pairs documented in `22-VERIFICATION.md`."
    - "`README.md` includes a v1.3 architecture overview, migration documentation (autoMigrateOnOpen, backup retention), sync interaction notes (~400ms vault.process, conflict modal, external-edit reload), per-widget Cmd-Z scoping, focus-scoped Cmd-F."
    - "`CLAUDE.md` `## Architecture` section is filled with a 5-10 line v1.3 sketch describing widget-only path, vault.process mutation primitive, lc-language frontmatter SSoT, sectionProtectionExtension narrow scope, migration infrastructure retention."
    - "`manifest.json` and `package.json` versions are bumped to `1.3.0-beta.1` (BRAT alpha) then `1.3.0` (GA); BRAT release artifacts (main.js, manifest.json, styles.css) attach automatically via the existing release workflow."
    - "REQUIREMENTS.md traceability table marks VIM-03 as 'Resolved by Phase 20 live-reconfigure (no banner shipped)' per CONTEXT L7."
    - "BRAT alpha runs for 7 calendar days; no P0/P1 GitHub Issues filed; plugin-store re-review submission filed on existing community-plugins.json entry (version-bump trigger)."
  artifacts:
    - path: "scripts/check-bundle-size.mjs"
      provides: "Bundle-size CI gate with v1.2-baseline hard cap (1,706,000 B) and 88% soft warn (1,500,000 B)"
      contains: "HARD_LIMIT = 1_706_000"
    - path: "manifest.json"
      provides: "Plugin manifest with version bumped to 1.3.0; isDesktopOnly: true preserved; description ≤ 250 chars; id unchanged"
      contains: "\"version\": \"1.3.0\""
    - path: "package.json"
      provides: "npm manifest with version bumped to 1.3.0 in lockstep with manifest.json"
      contains: "\"version\": \"1.3.0\""
    - path: "README.md"
      provides: "User-facing v1.3 release notes covering architecture overview, migration, sync, Cmd-Z scoping, Cmd-F scoping (POLISH-04)"
      contains: "v1.3"
    - path: "CLAUDE.md"
      provides: "Future-Claude-session architecture sketch under `## Architecture` (D-claude-02; ~5-10 lines)"
      contains: "vault.process"
    - path: ".planning/phases/22-v1-2-path-removal-polish/22-VERIFICATION.md"
      provides: "THEME-05 manual checklist execution log + BRAT 7-day dogfood log + plugin-store re-review submission record"
      contains: "THEME-05"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table updated: VIM-03 marked Resolved by Phase 20; POLISH-01..06, PROTECT-03, THEME-05 marked Resolved by Phase 22"
      contains: "Resolved by Phase 20 live-reconfigure"
  key_links:
    - from: "scripts/check-bundle-size.mjs HARD_LIMIT"
      to: ".github/workflows/ci.yml `- run: npm run check:bundle-size` step"
      via: "exit code 1 fails CI"
      pattern: "HARD_LIMIT = 1_706_000"
    - from: "manifest.json version field"
      to: ".github/workflows/release.yml release-tag auto-patch"
      via: "jq --arg v $TAG '.version = $v'"
      pattern: "\"version\": \"1\\.3\\.0\""
    - from: "BRAT release tag 1.3.0-beta.1"
      to: "GitHub release artifacts (main.js, manifest.json, styles.css) auto-attached"
      via: ".github/workflows/release.yml lines 32-72"
      pattern: "1\\.3\\.0-beta\\.1"
---

<objective>
Plan 22-03 closes the v1.3 milestone with five sequential release gates and the BRAT alpha + plugin-store re-review filing:

- **D-gate-01 (POLISH-02) — Bundle-size CI gate**: lower `HARD_LIMIT` in `scripts/check-bundle-size.mjs` from 1,800,000 B to 1,706,000 B (v1.2 baseline) and `SOFT_WARN` from 1,710,000 to 1,500,000 (~88% of HARD bites earlier). Verify post-22-01+22-02 bundle lands well under the new ceiling.
- **D-gate-02 (POLISH-03) — eslint clean run + innerHTML scan**: confirm `npm run lint` passes; confirm `grep -rn 'innerHTML' src/widget/` returns zero active assignments (per RESEARCH §7 baseline already passing).
- **D-gate-03 (THEME-05) — Manual theme regression checklist**: 5 themes (Minimal / Things / Catppuccin / Anuppuccin / Atom) × 3 representative views (problem-open, solved note, AC'd note with `## AI Review`) screenshot-compared against the v1.2 ship-commit baseline. Baseline is regenerated by `git checkout 2411f8e` (v1.2 ship commit per RESEARCH §8) + same screenshot capture procedure.
- **D-readme-01 (POLISH-04) — README v1.3 update**: add architecture overview, migration docs, sync interaction notes, Cmd-Z per-widget undo scoping, focus-scoped Cmd-F.
- **D-claude-02 (POLISH-04) — CLAUDE.md `## Architecture` fill**: 5-10 line sketch describing post-Phase-22 surface.
- **D-manifest-01 (POLISH-04, POLISH-06) — Version bump**: `manifest.json` + `package.json` lockstep to `1.3.0-beta.1` (BRAT) then `1.3.0` (GA). Note RESEARCH §9 anomaly: `manifest.json` HEAD currently says `"1.0.1"` (stale; auto-patched at release time); the in-tree bump is for dev-vault consistency.
- **D-gate-04 (POLISH-06) — BRAT alpha + plugin-store re-review**: tag `1.3.0-beta.1`, push, dogfood 7 calendar days, watch GitHub Issues. On no-P0/P1: tag `1.3.0`, file plugin-store re-review (version-bump trigger on existing community-plugins.json entry).
- **VIM-03 traceability resolution (CONTEXT L7)**: update `.planning/REQUIREMENTS.md` to mark VIM-03 as `Resolved by Phase 20 live-reconfigure (no banner shipped)` — the reload-on-toggle banner is NOT shipped because Phase 20's live-reconfigure works empirically.

Purpose: deliver POLISH-02..06, THEME-05, and the VIM-03 traceability resolution. Close the v1.3 milestone.

Output: 6-7 commits (one per artifact: bundle gate, README, CLAUDE.md, manifest+package version bump for beta, version bump for GA, traceability update). Plus two release tags (`1.3.0-beta.1`, `1.3.0`) pushed to GitHub. Plus the THEME-05 + BRAT log entries in `22-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-v1-2-path-removal-polish/22-CONTEXT.md
@.planning/phases/22-v1-2-path-removal-polish/22-RESEARCH.md
@.planning/phases/22-v1-2-path-removal-polish/22-PATTERNS.md
@.planning/phases/22-v1-2-path-removal-polish/22-VALIDATION.md
@.planning/phases/22-v1-2-path-removal-polish/22-01-SUMMARY.md
@.planning/phases/22-v1-2-path-removal-polish/22-02-SUMMARY.md
@CLAUDE.md
</context>

## Phase Goal (Plan 22-03 contribution)

Five release gates + BRAT alpha + plugin-store re-review filing + traceability cleanup. The 7-day BRAT window is the wall-clock pacer for the milestone close.

<artifacts_this_phase_produces>

- **Lowered constants** in `scripts/check-bundle-size.mjs`: `HARD_LIMIT = 1_706_000`, `SOFT_WARN = 1_500_000` (replacing the Phase 17 vim-ceiling values 1,800,000 and 1,710,000). Comment block updated to reflect Phase 22 v1.2-baseline rationale.
- **`README.md` v1.3 sections** (POLISH-04 / D-readme-01):
  - Version section callout: "v1.3 inline-widget architecture".
  - Migration docs subsection: backup-sidecar location, 30-day retention, autoMigrateOnOpen toggle behavior.
  - Sync interaction notes: ~400ms vault.process debounce, conflict modal, external-edit reload.
  - Cmd-Z scoping: undo is per-widget.
  - Cmd-F scoping: find is focus-scoped.
- **`CLAUDE.md` `## Architecture` content** (D-claude-02): 5-10 line sketch verbatim from RESEARCH §11 recommendation — widget-only path, `vault.process` mutation primitive, `lc-language` frontmatter SSoT for Run/Submit/AI dispatch, `sectionProtectionExtension` narrow scope, migration infrastructure retention.
- **`manifest.json` + `package.json` version bumps**: `1.0.1` → `1.3.0-beta.1` → `1.3.0` (manifest); `1.2.0` → `1.3.0-beta.1` → `1.3.0` (package).
- **`.planning/REQUIREMENTS.md` traceability updates**:
  - VIM-03 row: `Resolved by Phase 20 live-reconfigure (no banner shipped — CONTEXT L7)`.
  - DELETE-01..08, POLISH-01..06, PROTECT-03, THEME-05 rows: `Resolved by Phase 22`.
- **`22-VERIFICATION.md` sections** (existing file updated):
  - `## 22-03-03 THEME-05 Manual Checklist` with 5×3=15 screenshot pair entries (or pass/fail summaries per theme).
  - `## 22-03-07 BRAT 7-Day Dogfood` log with daily entries + GitHub Issues monitoring summary.
  - `## 22-03-07 Plugin-Store Re-Review Submission` with submission timestamp + community-plugins.json status.
- **Release tags** pushed to GitHub: `1.3.0-beta.1` (pre-release), `1.3.0` (GA).
- **Commit messages** (6-7 commits per Pattern S-01):
  - `chore(22-03): lower bundle-size HARD_LIMIT to v1.2 baseline (POLISH-02)`
  - `docs(22-03): README v1.3 architecture, migration, sync, Cmd-Z/Cmd-F scoping (POLISH-04)`
  - `docs(22-03): CLAUDE.md ## Architecture v1.3 sketch (D-claude-02)`
  - `chore(22-03): bump version to 1.3.0-beta.1 (POLISH-06 BRAT alpha)`
  - `chore(22-03): bump version to 1.3.0 (POLISH-06 GA)`
  - `docs(22-03): mark VIM-03 Resolved by Phase 20 + Phase 22 traceability close`

</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 22-03-01: Lower bundle-size CI gate to v1.2 baseline (POLISH-02)</name>
  <files>scripts/check-bundle-size.mjs</files>
  <read_first>
    - scripts/check-bundle-size.mjs (full file — verify the constants `HARD_LIMIT` and `SOFT_WARN` at top, confirm exit-code logic at lines ~117-131 unchanged)
    - .planning/phases/22-v1-2-path-removal-polish/22-RESEARCH.md (§6 bundle-size measurement infrastructure; v1.2 baseline 1,707,327 B per STATE.md:65; Pitfall 5 ordering invariant)
    - .planning/phases/22-v1-2-path-removal-polish/22-PATTERNS.md (D-gate-01 — edit constants, no structural change)
    - .planning/phases/22-v1-2-path-removal-polish/22-CONTEXT.md (D-gate-01 + L3 — hard cap at v1.2 baseline 1,706,000)
    - .planning/phases/22-v1-2-path-removal-polish/22-01-SUMMARY.md (post-22-01 bundle size — must be < 1,706,000 before lowering threshold)
  </read_first>
  <action>
    Per D-gate-01 + RESEARCH §6 + Pitfall 5 ordering invariant, in a single atomic commit:

    1. **Verify ordering invariant FIRST** (Pitfall 5): confirm Plan 22-01 + 22-02 are merged onto current branch (`git log --oneline -20 | grep -E '(22-01|22-02)'` shows commits). Run `npm run build && wc -c main.js`. The post-deletion `main.js` size MUST be `< 1_706_000` bytes. If it exceeds, **STOP** and audit what didn't get deleted (likely an orphan import or zombie code path) — don't lower the threshold below the actual size or CI fails immediately.
    2. **Edit `scripts/check-bundle-size.mjs`**: locate the existing constants block at lines ~12-15 (`HARD_LIMIT = 1_800_000`, `SOFT_WARN = 1_710_000`). Replace with:
       ```javascript
       // Phase 22 D-gate-01 — v1.2 baseline cap (POLISH-02).
       // v1.2 ship size = 1,707,327 B (STATE.md:65); HARD_LIMIT set 1,327 B
       // below ship size so any post-Phase-22 regression past v1.2 fails CI.
       // Net deletion in Phase 22 = ~−2,400 LOC; expected post-Phase-22 ~1.5 MB.
       const HARD_LIMIT = 1_706_000;     // FAILS CI on regression past v1.2 baseline.
       const SOFT_WARN = 1_500_000;      // ~88% of HARD; bites earlier than 95% so drift is caught while there's headroom.
       ```
    3. Update the surrounding comment block at lines ~5-30 — rewrite the Phase 06 / Phase 07 / Phase 17 ceiling-bump narrative to add a Phase 22 stanza. Preserve the historical context but add: `// Phase 22 (2026-06-XX): post-v1.2-deletion baseline. The v1.2 ship's vim+language-pack additions pushed the bundle to 1,707,327 B; Phase 22 deletes 5 v1.2 source files (~2,246 LOC) + ~800 LOC main.ts wiring, recovering well below v1.2 baseline. HARD lowered to 1,706,000 to enforce the recovery as a permanent ratchet — any future feature must net negative bytes against the v1.2 ship size.`
    4. **DO NOT** change the script's exit-code logic at lines ~117-131; the existing `process.exit(size > HARD_LIMIT ? 1 : 0)` + stderr WARN on `size > SOFT_WARN` already implements D-gate-01's requirement.
    5. **DO NOT** add a gzipped check (CONTEXT D-gate-01 explicitly rejected option C: "marginal upside since raw correlates").
    6. Run `node scripts/check-bundle-size.mjs` — MUST exit 0. The stderr should print the post-22-01+22-02 size and confirm it's under the new HARD.
    7. Run `npm run ci` (the lint + test + build + check:bundle-size chain per RESEARCH §6) — MUST be green end-to-end.
    8. **Commit**: `chore(22-03): lower bundle-size HARD_LIMIT to v1.2 baseline (POLISH-02)`. Body: 3-4 lines explaining the v1.2 baseline rationale (1,707,327 → 1,706,000 ratchet) and the soft-warn 88% bite point.
  </action>
  <verify>
    <automated>grep -c 'HARD_LIMIT = 1_706_000' scripts/check-bundle-size.mjs | grep -q '^1$' &amp;&amp; grep -c 'SOFT_WARN = 1_500_000' scripts/check-bundle-size.mjs | grep -q '^1$' &amp;&amp; node scripts/check-bundle-size.mjs &amp;&amp; npm run ci &amp;&amp; git log -1 --format=%s | grep -q '^chore(22-03): lower bundle-size HARD_LIMIT'</automated>
  </verify>
  <done>
    - `scripts/check-bundle-size.mjs` HARD_LIMIT is `1_706_000`; SOFT_WARN is `1_500_000`.
    - `node scripts/check-bundle-size.mjs` exits 0 against the post-22-01+22-02 build.
    - `npm run ci` chain passes.
    - Commit subject: `chore(22-03): lower bundle-size HARD_LIMIT to v1.2 baseline (POLISH-02)`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 22-03-02: eslint clean run + innerHTML scan verification (POLISH-03)</name>
  <files>.planning/phases/22-v1-2-path-removal-polish/22-VERIFICATION.md (entry only — no source-file edits expected unless eslint surfaces a regression)</files>
  <read_first>
    - .planning/phases/22-v1-2-path-removal-polish/22-RESEARCH.md (§7 innerHTML scan — D-gate-02 baseline already passing; 5 hits all comments)
    - .planning/phases/22-v1-2-path-removal-polish/22-CONTEXT.md (D-gate-02)
    - package.json (verify `npm run lint` script exists and uses `eslint-plugin-obsidianmd`)
  </read_first>
  <action>
    Per D-gate-02 + RESEARCH §7 (baseline already passing per Phase 21.1 close), this task is a verification gate, not a code-edit task. No commit expected unless eslint surfaces a regression.

    1. Run `npm run lint`. MUST exit 0 cleanly. If failures, the eslint plugin is reporting drift — fix every issue in this task (likely converting `innerHTML` to `createEl` / `setText` / `appendChild` per CLAUDE.md no-`innerHTML` rule). Per RESEARCH §7 the baseline is clean as of Phase 21.1 close, so this should be a no-op.
    2. Run `grep -rn 'innerHTML' src/widget/`. MUST return only comment lines (per RESEARCH §7's enumerated 5 hits, all in comments). Active assignments (a line matching `\.innerHTML\s*=` or `\.innerHTML\(`) MUST be zero.
    3. Run `grep -rn 'innerHTML' src/` (broader scope). Confirm no active assignments anywhere in `src/` — only comments. RESEARCH §7 confirms this baseline.
    4. Document outcome in `22-VERIFICATION.md` under `## 22-03-02 ESLint + innerHTML Scan` heading: `[PASS] npm run lint clean; grep -rn 'innerHTML' src/widget/ returned N comment-only matches; zero active assignments. Date: {YYYY-MM-DD}.`
    5. **DO NOT** commit any code change (this task is purely verification). If eslint surfaces a regression, fix in this task and produce a commit `fix(22-03): resolve eslint regression {brief description}`.
  </action>
  <verify>
    <automated>npm run lint &amp;&amp; ! grep -rn 'innerHTML' src/widget/ | grep -vE '^\s*[A-Za-z0-9_./-]+:[0-9]+:\s*(\*|\/\/|\/\*)' &amp;&amp; grep -c '## 22-03-02 ESLint' .planning/phases/22-v1-2-path-removal-polish/22-VERIFICATION.md | grep -q '^1$'</automated>
  </verify>
  <done>
    - `npm run lint` exits 0.
    - No active `innerHTML` assignments in `src/widget/` (the only matches are inside comment lines starting with `//`, `*`, or `/*`).
    - `22-VERIFICATION.md` contains `## 22-03-02 ESLint + innerHTML Scan` PASS entry.
    - No commit unless an eslint regression was found and fixed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 22-03-03: THEME-05 manual theme regression checklist (5 themes × 3 views)</name>
  <files>.planning/phases/22-v1-2-path-removal-polish/22-VERIFICATION.md, .planning/phases/22-v1-2-path-removal-polish/baseline-screenshots/ (created), .planning/phases/22-v1-2-path-removal-polish/comparison-screenshots/ (created)</files>
  <read_first>
    - .planning/phases/22-v1-2-path-removal-polish/22-RESEARCH.md (§8 THEME-05 baseline regeneration — `git checkout 2411f8e` v1.2 ship commit; 5 themes by community-store name; recommended zoom + window-size convention)
    - .planning/phases/22-v1-2-path-removal-polish/22-CONTEXT.md (D-gate-03 + L4)
    - .planning/phases/22-v1-2-path-removal-polish/22-PATTERNS.md (D-gate-03 — procedural checklist; no code analog)
    - $HOME/.claude/projects/-Users-moxu-projects-obsidian-leetcode/memory/MEMORY.md (dev-vault path)
  </read_first>
  <action>
    Pre-checklist: Phase 22-01 + 22-02 land in tree; v1.3 widget is the only mount path; CSS polish (D-polish-02 hover + D-polish-03 font) is in `styles.css`. The widget visual surface MAY have drifted vs. v1.2 baseline due to deletion of `codeActionsEditorExtension.ts` + the action row reflow + the new CSS overrides.

    Post-checklist: 5 themes × 3 views = 15 screenshot pairs in `.planning/phases/22-v1-2-path-removal-polish/{baseline,comparison}-screenshots/` with PASS/FAIL annotation per theme in `22-VERIFICATION.md`. Any FAIL blocks ship until rooted out.
  </action>
  <how-to-verify>
    Per D-gate-03 + RESEARCH §8 baseline-regen procedure:

    **Step 1 — Capture v1.2 baseline (~30 min)**:
    1. `git stash push -m "phase-22-pre-baseline-regen"` (preserve in-progress work).
    2. `git checkout 2411f8e` (v1.2 ship commit per RESEARCH §8 — `docs: changelog entry for 1.2.0-alpha.4`).
    3. `npm ci && npm run build` (rebuild v1.2 main.js).
    4. `cp main.js manifest.json styles.css ~/Documents/Obsidian\ Vault/.obsidian/plugins/obsidian-leetcode/`.
    5. Reload Obsidian (Cmd-R or Settings → Restart).
    6. Standardize Obsidian window: Cmd-0 (default zoom), 1200×900 window (per RESEARCH §8 recommendation).
    7. **For each of 5 themes**:
       - **Minimal** by `kepano`
       - **Things** by `colineckert`
       - **Catppuccin** by `catppuccin`
       - **Anuppuccin** by `BasicMan-1`
       - **Atom** by `kognise`
    8. For each theme: Settings → Appearance → Theme → install + activate. Then capture **3 representative views** as screenshots:
       - **View A — problem-open**: open a problem from the LeetCode browser command palette → fresh note. Screenshot the full Obsidian window. Save to `.planning/phases/22-v1-2-path-removal-polish/baseline-screenshots/{theme-slug}-A-problem-open.png`.
       - **View B — solved note**: open an existing solved note (any from your vault). Screenshot. Save to `{theme-slug}-B-solved.png`.
       - **View C — AC'd note with `## AI Review`**: open a note that has an `## AI Review` section. Screenshot. Save to `{theme-slug}-C-ai-review.png`.
       - 5 themes × 3 views = 15 baseline screenshots.
    9. `git checkout gsd/v1.3-inline-widget-architecture` (or whichever branch holds Phase 22 work).
    10. `git stash pop` (restore in-progress work).

    **Step 2 — Capture Phase 22 comparison set (~30 min)**:
    1. `npm run build`. `cp main.js manifest.json styles.css ~/Documents/Obsidian\ Vault/.obsidian/plugins/obsidian-leetcode/`.
    2. Reload Obsidian. Same Cmd-0 + 1200×900 window standard.
    3. For each of 5 themes × 3 views, capture the same 15 screenshots into `comparison-screenshots/{theme-slug}-{A,B,C}-*.png` (matching baseline filenames).

    **Step 3 — Side-by-side comparison + documentation (~20 min)**:
    1. For each theme, open the 3 baseline + 3 comparison images side by side. Visually compare.
    2. Look for: widget background color match, action row layout match, chevron position match, code text rendering match, syntax highlighting match, hover/focus state match, problem-section background match.
    3. Document each theme's outcome in `22-VERIFICATION.md` under `## 22-03-03 THEME-05 Manual Checklist`:
       - `### Minimal: [PASS|FAIL]\n- View A: {observation}\n- View B: {observation}\n- View C: {observation}`
       - Repeat for the other 4 themes.
       - Embed the screenshot pairs as Markdown image links: `![Minimal v1.2 baseline View A](./baseline-screenshots/minimal-A-problem-open.png)` and `![Minimal v1.3 comparison View A](./comparison-screenshots/minimal-A-problem-open.png)`.
    4. **If ANY theme shows a regression** (e.g., action row clipped on Atom; widget background mismatched on Catppuccin), reply `failed: theme={name} view={A|B|C} {one-line description}` so the orchestrator can route a fix BEFORE proceeding to the BRAT tag (D-gate-04). Per CONTEXT L4 / D-gate-03: theme regression blocks ship.
    5. **If all 15 pairs PASS**, reply `approved` and proceed to Task 22-03-04.

    **Time estimate**: ~80 min total. This is the most labor-intensive task in Plan 22-03; budget accordingly.

    **Discretion** (CONTEXT): if v1.2 baseline screenshots in `baseline-screenshots/` already exist from a prior run, reuse them — only capture comparison set. RESEARCH §8 confirms baseline-regen is a one-time procedure if persisted.
  </how-to-verify>
  <resume-signal>Type `approved` (all 5 themes × 3 views PASS) or `failed: theme={name} view={A|B|C} {observation}` (regression — fix BEFORE proceeding to BRAT tag).</resume-signal>
  <verify>
    <human-check>Resume signal received: `approved` (all 5 themes × 3 views PASS) or `failed: theme={name} view={A|B|C} {observation}` (regression found — fix before BRAT tag). All 15 baseline screenshots and 15 comparison screenshots exist on disk under `.planning/phases/22-v1-2-path-removal-polish/{baseline,comparison}-screenshots/`. `22-VERIFICATION.md` has the `## 22-03-03 THEME-05 Manual Checklist` section with per-theme PASS/FAIL annotations and embedded screenshot pair references.</human-check>
  </verify>
  <done>
    - `.planning/phases/22-v1-2-path-removal-polish/baseline-screenshots/` contains 15 screenshots (5 themes × 3 views A/B/C) captured against the v1.2 ship commit `2411f8e`.
    - `.planning/phases/22-v1-2-path-removal-polish/comparison-screenshots/` contains 15 matching-named screenshots captured against the post-22-01+22-02 build.
    - `22-VERIFICATION.md` `## 22-03-03 THEME-05 Manual Checklist` section has per-theme `[PASS]` (or `[FAIL]` with description) lines for all 5 themes (Minimal, Things, Catppuccin, Anuppuccin, Atom).
    - All 15 pairs are PASS, OR any FAIL was rooted out (commit hash referenced in the FAIL line) before proceeding to Task 22-03-04.
    - User typed `approved` or `failed: ...` to advance.
  </done>

</task>

<task type="auto" tdd="false">
  <name>Task 22-03-04: README v1.3 update (POLISH-04 / D-readme-01)</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — locate version section, feature sections, existing migration/sync/keyboard documentation if any)
    - .planning/phases/22-v1-2-path-removal-polish/22-CONTEXT.md (D-readme-01 — required content items a–e)
    - .planning/phases/22-v1-2-path-removal-polish/22-RESEARCH.md (Specific Findings → README D-readme-01 spec; v1.3 architecture overview content)
    - .planning/phases/22-v1-2-path-removal-polish/22-PATTERNS.md (D-readme-01 — self-analog existing version + feature sections)
    - .planning/phases/22-v1-2-path-removal-polish/22-02-SUMMARY.md (to check if a vim-Tab known-issue footnote was already added in Plan 22-02; if so, leave it; if not, the footnote may need to land here)
    - CLAUDE.md (community-plugin requirements — README must explain network use, ≤250 char manifest description)
  </read_first>
  <action>
    Per D-readme-01 + CONTEXT items (a)–(e), in a single atomic commit:

    1. Open `README.md`. Locate the version section (likely near the top, e.g., `## What's New` or a similar header). If absent, create a `## v1.3 Inline Widget Architecture` section at an appropriate position (after the intro / project tagline, before installation).
    2. Add the **5 required content items** per CONTEXT D-readme-01:

       **(a) v1.3 callout** — one-line: `**v1.3** introduces an inline widget architecture: every \`leetcode-solve\` fence renders as a self-contained CodeMirror editor inside your note, with edits flowing to disk via debounced \`vault.process\` writes.`

       **(b) Migration docs subsection** — add a `### Migration from v1.0 / v1.1 / v1.2` heading with content covering:
       - Lazy-on-open migration: existing v1.0/v1.1/v1.2 notes auto-migrate to the `leetcode-solve` fence tag on first open in 1.3.x.
       - Backup sidecar location: `.obsidian/plugins/obsidian-leetcode/migration-backup-{slug}-{ISO}/`.
       - Backup retention: backups auto-delete after 30 days.
       - `autoMigrateOnOpen` setting: default ON; toggle OFF for manual migration via the "Migrate this note" command palette entry.
       - One-line reassurance: "Your existing notes are not modified until you open them; the migration is a single atomic write."

       **(c) Sync interaction notes subsection** — add a `### How sync works` heading with:
       - Widget edits write to disk via debounced `vault.process` (~400ms default, configurable 300/500/1000/2000 ms via settings).
       - External edits (other panes, Obsidian Sync, CLI `git pull`) reload the widget with cursor preserved.
       - On collision (external edit during local in-flight typing), a conflict modal appears: "Keep mine / Keep external / View diff."

       **(d) Cmd-Z scoping** — add a `### Keyboard scoping` heading (or fold under the existing keyboard section). Content: "Undo is per-widget. Press Cmd-Z (Ctrl-Z on Windows/Linux) inside the widget to undo widget edits; Cmd-Z outside the widget undoes parent-doc edits. The two undo stacks are independent."

       **(e) Cmd-F scoping** — under the same keyboard heading: "Find is focus-scoped. Press Cmd-F (Ctrl-F on Windows/Linux) inside the widget to search widget content; outside, Cmd-F searches the parent doc."

    3. **Vim-Tab footnote handling**: if Plan 22-02's D-polish-01 deferred (per `22-02-SUMMARY.md`), confirm a "Known cosmetic issues" subsection exists with the vim-Tab note + GitHub issue link. If it doesn't exist, add it as a `### Known cosmetic issues` subsection: `- **Vim Tab cursor marker**: when entering Insert mode and pressing Tab, the block cursor marker may visually lag the actual cursor by one indent. Cosmetic only — typing lands at the correct offset. Tracked in {GitHub issue URL}.` If Plan 22-02 shipped the fix (probe PASS), no footnote needed.

    4. **Network-use disclosure** (CLAUDE.md community-plugin requirement): verify the README contains a sentence like "This plugin communicates with leetcode.com to fetch problems and submit solutions." If absent, add one near the top.

    5. Run `npm run build` (no source change but defensive) — CSS/JS unaffected, README is documentation.

    6. **Commit**: `docs(22-03): README v1.3 architecture, migration, sync, Cmd-Z/Cmd-F scoping (POLISH-04)`. Body: list the 5 (a)–(e) sections added.
  </action>
  <verify>
    <automated>grep -c 'v1\.3' README.md | awk '$1 &gt;= 1' | grep -q . &amp;&amp; grep -c 'autoMigrateOnOpen' README.md | awk '$1 &gt;= 1' | grep -q . &amp;&amp; grep -c 'vault\.process' README.md | awk '$1 &gt;= 1' | grep -q . &amp;&amp; grep -c 'Cmd-Z' README.md | awk '$1 &gt;= 1' | grep -q . &amp;&amp; grep -c 'Cmd-F' README.md | awk '$1 &gt;= 1' | grep -q . &amp;&amp; git log -1 --format=%s | grep -q '^docs(22-03): README v1.3'</automated>
  </verify>
  <done>
    - `README.md` contains all 5 required content items: v1.3 callout (a), Migration subsection (b), Sync interaction notes (c), Cmd-Z scoping (d), Cmd-F scoping (e).
    - Network-use disclosure sentence is present (CLAUDE.md community-plugin requirement).
    - Vim-Tab footnote present IF Plan 22-02 deferred D-polish-01; absent IF shipped.
    - Commit subject: `docs(22-03): README v1.3 architecture, migration, sync, Cmd-Z/Cmd-F scoping (POLISH-04)`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 22-03-05: CLAUDE.md ## Architecture v1.3 sketch (D-claude-02)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (full file — locate `## Architecture` section; verify Plan 22-01 sub-step E successfully removed `## Conventions` paragraphs)
    - .planning/phases/22-v1-2-path-removal-polish/22-CONTEXT.md (D-claude-02 — 5-10 line content spec)
    - .planning/phases/22-v1-2-path-removal-polish/22-RESEARCH.md (§11 D-claude-02 — verbatim recommended fill content)
    - .planning/phases/22-v1-2-path-removal-polish/22-PATTERNS.md (D-claude-02 — self-analog existing empty placeholder)
  </read_first>
  <action>
    Per D-claude-02 + RESEARCH §11 verbatim recommendation, in a single atomic commit:

    1. Open `CLAUDE.md`. Locate the `## Architecture` section. Per Phase 22-01 sub-step E, the obsolete `## Conventions` paragraphs are already deleted; the `## Architecture` section currently reads `Architecture not yet mapped. Follow existing patterns found in the codebase.` (verified by RESEARCH §11).
    2. Replace the placeholder line with the 5-10 line v1.3 architecture sketch per RESEARCH §11 verbatim recommendation (adjust slightly to taste, but keep the essential elements):

       ```markdown
       **v1.3 inline-widget architecture (post-Phase-22).**

       The plugin's editing model is a single `registerMarkdownCodeBlockProcessor('leetcode-solve', ...)` + `registerEditorExtension(leetCodeFenceViewPlugin)` pair (Reading mode + Live Preview, both calling `mountLeetCodeWidget`). The widget owns its own CM6 EditorView; widget edits flow through `app.vault.process(file, fn)` — the only mutation primitive in the plugin. `lc-language` frontmatter is the single source of truth for Run/Submit/AI dispatch (read via `extractFirstFencedBlock(noteBody, frontmatter)` in `src/solve/codeExtractor.ts`).

       `src/main/sectionProtectionExtension.ts` (narrow scope: `## Problem` body + `## Techniques` heading) is the only protection extension. Section locking on the fence opener / closer is moot — the widget owns the fence range via `EditorView.atomicRanges`.

       Migration infrastructure (`src/widget/fenceMigrator.ts`, `src/widget/legacyFenceBanner.ts`, `src/widget/migrationBackupGc.ts`, `autoMigrateOnOpen` setting) stays in tree for users upgrading from 1.2.x → 1.3.x late. 30-day backup sidecar at `.obsidian/plugins/obsidian-leetcode/migration-backup-{slug}-{ISO}/`.
       ```

    3. **Verify the file**: confirm `## Conventions` heading is GONE (Plan 22-01 sub-step E deleted it); confirm `## Architecture` is now populated; confirm no references to `'leetcode.*'` userEvent or "Canonical plugin write-path" remain.
    4. **Commit**: `docs(22-03): CLAUDE.md ## Architecture v1.3 sketch (D-claude-02)`. Body: 1-2 lines summarizing the fill.
  </action>
  <verify>
    <automated>grep -c '^## Architecture$' CLAUDE.md | grep -q '^1$' &amp;&amp; grep -c 'vault\.process' CLAUDE.md | awk '$1 &gt;= 1' | grep -q . &amp;&amp; grep -c 'sectionProtectionExtension' CLAUDE.md | awk '$1 &gt;= 1' | grep -q . &amp;&amp; grep -c 'lc-language' CLAUDE.md | awk '$1 &gt;= 1' | grep -q . &amp;&amp; ! grep -q 'Architecture not yet mapped' CLAUDE.md &amp;&amp; ! grep -q "'leetcode\.\*'" CLAUDE.md &amp;&amp; git log -1 --format=%s | grep -q '^docs(22-03): CLAUDE.md'</automated>
  </verify>
  <done>
    - `CLAUDE.md` `## Architecture` section is populated with the v1.3 sketch (mentions widget mount strategy, `vault.process`, `lc-language` SSoT, `sectionProtectionExtension` narrow scope, migration infrastructure retention).
    - The placeholder string `Architecture not yet mapped` is gone.
    - The obsolete `## Conventions` paragraphs are still gone (Plan 22-01 sub-step E deletion preserved).
    - Commit subject: `docs(22-03): CLAUDE.md ## Architecture v1.3 sketch (D-claude-02)`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 22-03-06: Bump manifest.json + package.json to 1.3.0-beta.1 (BRAT alpha) + push tag</name>
  <files>manifest.json, package.json</files>
  <read_first>
    - manifest.json (current version `1.0.1` per RESEARCH §9 anomaly; isDesktopOnly: true, description ≤ 250 chars)
    - package.json (current version `1.2.0` per RESEARCH §9)
    - .github/workflows/release.yml (lines 32-72 per RESEARCH §9 — auto-detects pre-release on `-` separator; auto-patches manifest from tag; auto-attaches main.js + manifest.json + styles.css)
    - .planning/phases/22-v1-2-path-removal-polish/22-CONTEXT.md (D-manifest-01 + D-gate-04 + L8)
    - .planning/phases/22-v1-2-path-removal-polish/22-RESEARCH.md (§9 BRAT release artifacts; tag-command sequence; description ≤ 250 char check)
    - .planning/phases/22-v1-2-path-removal-polish/22-PATTERNS.md (D-manifest-01 — single-field JSON edit; lockstep)
  </read_first>
  <action>
    Per D-manifest-01 + D-gate-04 + Pitfall 6 (lockstep enforcement), in a single atomic commit + tag push:

    1. Edit `manifest.json`: change `"version": "1.0.1"` → `"version": "1.3.0-beta.1"`. Preserve all other fields verbatim:
       - `"id": "leetcode"` — unchanged (community-plugins.json registration key).
       - `"name": "LeetCode"` — unchanged.
       - `"minAppVersion": "1.10.0"` — unchanged.
       - `"description": "Browse, solve, and note LeetCode problems inside your vault."` — unchanged (60 chars; well under 250 limit; ends with `.` per CLAUDE.md community-plugin requirement).
       - `"author": "Mo Xu"` — unchanged.
       - `"authorUrl": "https://github.com/LikeSundayLikeRain"` — unchanged.
       - `"isDesktopOnly": true` — unchanged (community-plugin requirement).
    2. Edit `package.json`: change `"version": "1.2.0"` → `"version": "1.3.0-beta.1"`. Lockstep with manifest.json per Pitfall 6.
    3. Run `npm install` (no dep changes; updates `package-lock.json` version reference if present).
    4. Run `npm run ci` (lint + test + build + check:bundle-size) — MUST be green.
    5. **Commit**: `chore(22-03): bump version to 1.3.0-beta.1 (POLISH-06 BRAT alpha)`. Body: 2-3 lines summarizing the lockstep bump and the BRAT-alpha trigger.
    6. **Push tag**: `git tag -a 1.3.0-beta.1 -m "v1.3.0-beta.1 — BRAT alpha (POLISH-06)"` then `git push origin 1.3.0-beta.1`. The release workflow at `.github/workflows/release.yml:32-72` auto-detects the `-` separator → flags as pre-release; auto-patches `manifest.json` from the tag (no-op since in-tree already matches); auto-attaches `main.js`, `manifest.json`, `styles.css`.
    7. After ~5 min, verify the GitHub release page (`https://github.com/{owner}/{repo}/releases/tag/1.3.0-beta.1`) shows the pre-release with all 3 artifacts attached.
    8. **DO NOT** push the `1.3.0` GA tag yet — Task 22-03-07's BRAT 7-day dogfood gate must pass first.
  </action>
  <verify>
    <automated>jq -r .version manifest.json | grep -q '^1\.3\.0-beta\.1$' &amp;&amp; jq -r .version package.json | grep -q '^1\.3\.0-beta\.1$' &amp;&amp; jq -r .isDesktopOnly manifest.json | grep -q '^true$' &amp;&amp; npm run ci &amp;&amp; git tag -l '1.3.0-beta.1' | grep -q '^1\.3\.0-beta\.1$' &amp;&amp; git log -1 --format=%s | grep -q '^chore(22-03): bump version to 1.3.0-beta.1'</automated>
  </verify>
  <done>
    - `manifest.json` version is `1.3.0-beta.1`; `isDesktopOnly: true`; description unchanged at 60 chars.
    - `package.json` version is `1.3.0-beta.1` (lockstep).
    - `npm run ci` chain passes.
    - Local tag `1.3.0-beta.1` exists; remote tag pushed; GitHub release page shows pre-release with `main.js`, `manifest.json`, `styles.css` attached.
    - Commit subject: `chore(22-03): bump version to 1.3.0-beta.1 (POLISH-06 BRAT alpha)`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 22-03-07: BRAT 7-day dogfood gate + plugin-store re-review submission (POLISH-06)</name>
  <files>.planning/phases/22-v1-2-path-removal-polish/22-VERIFICATION.md, manifest.json, package.json</files>
  <read_first>
    - .planning/phases/22-v1-2-path-removal-polish/22-CONTEXT.md (D-gate-04 — 7-day window; pass criteria: no P0/P1; plugin-store re-review trigger)
    - .planning/phases/22-v1-2-path-removal-polish/22-RESEARCH.md (§9 BRAT release artifacts; tag command sequence; community-plugins.json status)
    - .planning/phases/22-v1-2-path-removal-polish/22-PATTERNS.md (D-gate-04 + D-manifest-01)
    - .github/workflows/release.yml (release pipeline)
    - $HOME/.claude/projects/-Users-moxu-projects-obsidian-leetcode/memory/MEMORY.md (dev-vault path)
  </read_first>
  <action>
    Pre-window: Task 22-03-06 pushed the `1.3.0-beta.1` tag; release workflow attached `main.js`, `manifest.json`, `styles.css`. The plugin is now BRAT-installable.

    Post-window (PASS state): 7 calendar days of author dogfood + GitHub Issues monitoring with no P0/P1 surfaces. Author then bumps `manifest.json` + `package.json` to `1.3.0`, commits, tags `1.3.0`, pushes — release workflow runs as final release. Plugin-store re-review submission filed via existing community-plugins.json entry (version-bump trigger; no PR needed unless `repo`/`name` changed).

    Post-window (FAIL state): a P0/P1 surface during the BRAT window — fix lands in a hotfix commit (e.g., `1.3.0-beta.2` re-tag), the 7-day window restarts, OR a mini-phase 22.1 is opened per CONTEXT Deferred Idea "Plugin-store auto-rejection escape hatch."
  </action>
  <how-to-verify>
    Per D-gate-04 + L5:

    **Step 1 — BRAT install + initial verification (~30 min)**:
    1. In dev vault, install the BRAT plugin (`obsidian-bridge` → BRAT) if not already installed.
    2. In BRAT settings, add the GitHub repo URL (or specific tag `1.3.0-beta.1`). BRAT downloads the release artifacts.
    3. Reload Obsidian. Verify the plugin shows version `1.3.0-beta.1` in Settings → Community plugins.
    4. Document install in `22-VERIFICATION.md` under `## 22-03-07 BRAT 7-Day Dogfood` with `**Tag**: 1.3.0-beta.1` and `**Installed**: {date}`.

    **Step 2 — 7-day dogfood window (7 calendar days)**:
    - Use the plugin daily on real LC notes. Mix of fresh-create, solving, AC'ing, AI debug, language switching, vim mode toggling, theme swapping.
    - Daily entry in `22-VERIFICATION.md` under `## 22-03-07 BRAT 7-Day Dogfood` → `### Day {N} ({date})`: `- {workflow used}: {observation}` per workflow.
    - Monitor `https://github.com/{owner}/{repo}/issues` for P0/P1 reports tagged with `BRAT` or filed against `1.3.0-beta.1`. Note any in the daily log.
    - **Pass criteria** (per CONTEXT D-gate-04 + L5): no P0/P1 issues filed in the 7-day window AND no P0/P1 surfaces in author dogfood.
    - **Fail criteria**: any P0/P1. Fix → re-tag `1.3.0-beta.2` → 7-day window restarts. Or open mini-phase 22.1 if non-trivial.

    **Step 3 — On PASS: GA tag + plugin-store submission (~30 min)**:
    1. Edit `manifest.json` version `1.3.0-beta.1` → `1.3.0`. Edit `package.json` version `1.3.0-beta.1` → `1.3.0` (lockstep). Run `npm install`. Run `npm run ci` — MUST be green.
    2. **Commit**: `chore(22-03): bump version to 1.3.0 (POLISH-06 GA)`. Body: 2-3 lines summarizing the GA bump + the BRAT 7-day pass.
    3. `git tag -a 1.3.0 -m "v1.3.0 — Inline Widget Architecture (milestone close)"` then `git push origin 1.3.0`. Release workflow auto-detects the absence of `-` → flags as final release (NOT pre-release); auto-patches manifest; auto-attaches artifacts.
    4. Verify GitHub release page shows `1.3.0` as a final release with all 3 artifacts attached.
    5. **Plugin-store re-review submission** (per RESEARCH §9 + CONTEXT Discretion): the community-plugins.json entry exists from v1.0; v1.3 ship is a version-bump trigger re-review (no new PR needed unless `repo`/`name` changed). Verify the entry at `https://github.com/obsidianmd/obsidian-releases/blob/master/community-plugins.json` for the `leetcode` ID. If the entry's `repo` or `name` matches current state, no PR needed — just confirm the new tag is visible to the plugin store. If a field changed, file a one-line PR to `obsidianmd/obsidian-releases`.
    6. Document outcome in `22-VERIFICATION.md` under `## 22-03-07 Plugin-Store Re-Review Submission` with submission timestamp + community-plugins.json status.
    7. Reply `approved` (BRAT 7-day pass + GA tag pushed + plugin-store re-review filed).

    **Step 3 — On FAIL: hotfix loop or mini-phase escalation**:
    - Reply `failed: {one-line P0/P1 description}` and route to a hotfix branch. The orchestrator opens mini-phase 22.1 if the fix is non-trivial.

    **Wall-clock note**: this task is the longest in the plan (~7 days + 30-30 min overhead). All other 22-03 tasks complete in days 1-2; the BRAT window is the milestone-close pacer.
  </how-to-verify>
  <resume-signal>Type `approved` (BRAT 7-day pass; `1.3.0` tag pushed; plugin-store re-review submission filed) or `failed: {P0/P1 description}` (BRAT surfaced regression; hotfix lane or mini-phase 22.1 escalation).</resume-signal>
  <verify>
    <human-check>Resume signal received: `approved` (BRAT 7-day pass; `1.3.0` tag pushed; plugin-store re-review submission filed) or `failed: {P0/P1 description}` (BRAT surfaced a regression; hotfix lane or mini-phase 22.1 escalation). 7 calendar days have elapsed since the `1.3.0-beta.1` tag was pushed (Task 22-03-06). `manifest.json` and `package.json` versions are bumped to `1.3.0` lockstep on the PASS branch.</human-check>
  </verify>
  <done>
    - `.planning/phases/22-v1-2-path-removal-polish/22-VERIFICATION.md` contains a `## 22-03-07 BRAT 7-Day Dogfood` section with: BRAT install date, daily entries for 7 days, GitHub Issues monitoring summary (P0/P1 count must be 0 for GA).
    - `22-VERIFICATION.md` contains a `## 22-03-07 Plugin-Store Re-Review Submission` section with: GA tag push timestamp, community-plugins.json entry status (verified unchanged or one-line PR filed), plugin-store reviewer feedback (if any received).
    - `manifest.json` version is `1.3.0`; `package.json` version is `1.3.0`; commit subject is `chore(22-03): bump version to 1.3.0 (POLISH-06 GA)`.
    - Local + remote tag `1.3.0` exists; GitHub release page shows `1.3.0` as a final release with `main.js`, `manifest.json`, `styles.css` attached.
    - User typed `approved` (or `failed: ...` with hotfix branch routed).
  </done>

</task>

<task type="auto" tdd="false">
  <name>Task 22-03-08: REQUIREMENTS.md traceability — VIM-03 + Phase 22 close</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (Traceability table at lines ~149-213; locate VIM-03 row at line 186 and Phase 22 rows at lines 199-212)
    - .planning/phases/22-v1-2-path-removal-polish/22-CONTEXT.md (L7 — VIM-03 NOT shipped; mark Resolved by Phase 20)
    - .planning/phases/22-v1-2-path-removal-polish/22-RESEARCH.md (Phase Requirements table — VIM-03 disposition)
  </read_first>
  <action>
    Per CONTEXT L7 + Phase 22 close, in a single atomic commit:

    1. Open `.planning/REQUIREMENTS.md`. Locate the Traceability table (lines ~149-213).
    2. **VIM-03 row** (line 186): change `| VIM-03 | Phase 22 | Pending |` to `| VIM-03 | Phase 22 | Resolved by Phase 20 live-reconfigure (no banner shipped — CONTEXT L7) |`.
    3. **Phase 22 rows** (lines 199-212; DELETE-01..08, POLISH-01..06, PROTECT-03, THEME-05): for each, change the Status column from `Pending` to `Resolved by Phase 22`. The table covers:
       - DELETE-01..08 (8 rows)
       - POLISH-01..06 (6 rows)
       - PROTECT-03 (1 row)
       - THEME-05 (1 row)
       - 16 rows total updated to `Resolved by Phase 22`.
    4. Update the Coverage summary at the bottom of REQUIREMENTS.md (lines ~214-222): `Mapped to phases: 57` → confirm count; `Resolved` → `57 v1.3 requirements all resolved`. Add a final line: `*Phase 22 closed: {YYYY-MM-DD}*` (use today's date).
    5. Verify by `grep -c 'Resolved by Phase 22' .planning/REQUIREMENTS.md` — should return at least 16.
    6. Verify by `grep -c 'Resolved by Phase 20 live-reconfigure' .planning/REQUIREMENTS.md` — should return 1 (the VIM-03 row).
    7. **Commit**: `docs(22-03): mark VIM-03 Resolved by Phase 20 + Phase 22 traceability close`. Body: 2-3 lines listing the 16 resolved + the VIM-03 disposition.
  </action>
  <verify>
    <automated>grep -c 'Resolved by Phase 22' .planning/REQUIREMENTS.md | awk '$1 &gt;= 16' | grep -q . &amp;&amp; grep -c 'Resolved by Phase 20 live-reconfigure' .planning/REQUIREMENTS.md | grep -q '^1$' &amp;&amp; ! grep -q '| VIM-03 | Phase 22 | Pending' .planning/REQUIREMENTS.md &amp;&amp; ! grep -q '| DELETE-01 | Phase 22 | Pending' .planning/REQUIREMENTS.md &amp;&amp; git log -1 --format=%s | grep -q '^docs(22-03): mark VIM-03 Resolved'</automated>
  </verify>
  <done>
    - VIM-03 row in REQUIREMENTS.md marked `Resolved by Phase 20 live-reconfigure (no banner shipped — CONTEXT L7)`.
    - All 16 Phase 22 requirement rows (DELETE-01..08, POLISH-01..06, PROTECT-03, THEME-05) marked `Resolved by Phase 22`.
    - Coverage summary line at bottom updated with `Phase 22 closed: {date}`.
    - Commit subject: `docs(22-03): mark VIM-03 Resolved by Phase 20 + Phase 22 traceability close`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → GitHub release pipeline | Tag push triggers `release.yml`; auto-patches manifest; auto-attaches artifacts. Pipeline is owned by repo and runs in GitHub Actions. |
| BRAT user vault → plugin code | BRAT auto-installs the release artifact; users in the alpha period run actual plugin code. |
| Plugin-store reviewer → community-plugins.json | Version-bump triggers re-review against the existing entry; reviewer checklist enforces P13 (no innerHTML, isDesktopOnly, no telemetry, no remote eval). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03-01 | Tampering | Bundle-size threshold lowered below current bundle → CI immediately fails | mitigate | Pitfall 5 ordering invariant: Plan 22-01 + 22-02 land FIRST; `npm run build && wc -c main.js` confirms `< 1_706_000` BEFORE the threshold drop. |
| T-22-03-02 | Repudiation | Theme regression slips past manual checklist | mitigate | D-gate-03 5×3 = 15 pair check is fine-grained; per-theme PASS/FAIL recorded; baseline + comparison screenshots persist for forensic audit. |
| T-22-03-03 | Tampering | Plugin-store reviewer flags an `innerHTML` the eslint plugin missed | mitigate | RESEARCH §7 confirms zero active `innerHTML` assignments; D-gate-02 codifies the gate. CONTEXT Deferred Idea "Plugin-store auto-rejection escape hatch" routes blockers to mini-phase 22.1. |
| T-22-03-04 | Information disclosure | manifest.json description exceeds 250 char limit | mitigate | RESEARCH §9 confirms current 60 chars; D-manifest-01 task preserves the description verbatim during version bump. |
| T-22-03-05 | Denial of service | BRAT user reports a P0 that author dogfood missed (e.g., Windows-specific path delimiter regression) | mitigate | D-gate-04 7-day window + GitHub Issues monitoring + hotfix re-tag loop. CONTEXT mini-phase 22.1 escalation path for non-trivial blockers. |
| T-22-03-06 | Tampering | Tag mismatch between in-tree manifest version and the git tag | mitigate | Pitfall 6: lockstep bump (manifest + package + commit + tag) in a single task. The release-workflow auto-patch becomes a no-op for in-tree consistency. |
| T-22-03-SC | Tampering | npm/pip/cargo installs | n/a | No new packages installed in Plan 22-03. RESEARCH "Standard Stack" confirms. |
</threat_model>

<verification>
**Aggregate plan verification** (run after Task 22-03-08 commits):

- `node scripts/check-bundle-size.mjs` exits 0; HARD_LIMIT = 1_706_000; SOFT_WARN = 1_500_000.
- `npm run lint` clean; `grep -rn 'innerHTML' src/widget/` returns only comment hits.
- `22-VERIFICATION.md` contains: `## 22-02-01 Vim-Tab Probe` (from Plan 22-02), `## 22-02-02 Widget Hover Border` (from Plan 22-02), `## 22-02-03 Action Row Font` (from Plan 22-02), `## 22-03-02 ESLint + innerHTML Scan`, `## 22-03-03 THEME-05 Manual Checklist`, `## 22-03-07 BRAT 7-Day Dogfood`, `## 22-03-07 Plugin-Store Re-Review Submission`.
- `README.md` contains v1.3 callout, Migration subsection, Sync interaction notes, Cmd-Z scoping, Cmd-F scoping.
- `CLAUDE.md` `## Architecture` section is populated (no `Architecture not yet mapped` placeholder).
- `manifest.json` version is `1.3.0`; `package.json` version is `1.3.0`.
- Local + remote tags `1.3.0-beta.1` and `1.3.0` exist.
- `.planning/REQUIREMENTS.md` Traceability: 16 Phase 22 rows marked `Resolved by Phase 22`; VIM-03 marked `Resolved by Phase 20 live-reconfigure`.
- Plugin-store re-review submission filed (community-plugins.json status documented in `22-VERIFICATION.md`).
- 6-7 commits land on the working branch matching the Pattern S-01 commit-message templates.

**Wave verification gate**: Plan 22-03 closes the v1.3 milestone. After this plan, ROADMAP §22 status changes to `Complete`; STATE.md current-position advances; the v1.3 milestone summary is appended.
</verification>

<success_criteria>
- POLISH-02 satisfied: bundle-size CI gate hard-cap at v1.2 baseline (1,706,000 B); post-22-01+22-02 build passes the gate.
- POLISH-03 satisfied: `npm run lint` clean; zero active `innerHTML` assignments in `src/widget/`.
- POLISH-04 satisfied: README v1.3 architecture, migration, sync, Cmd-Z/Cmd-F scoping content added; CLAUDE.md `## Architecture` section filled.
- POLISH-05 satisfied: full vitest suite green at every wave gate.
- POLISH-06 satisfied: BRAT alpha tag shipped; 7-day dogfood passed; GA tag shipped; plugin-store re-review submission filed.
- THEME-05 satisfied: 5 themes × 3 views manual checklist executed; all pairs PASS or regressions fixed before GA.
- VIM-03 traceability resolved: marked `Resolved by Phase 20 live-reconfigure (no banner shipped — CONTEXT L7)` in REQUIREMENTS.md.
- All 6-7 commits follow Pattern S-01.
- v1.3 milestone closed.
</success_criteria>

<output>
Create `.planning/phases/22-v1-2-path-removal-polish/22-03-SUMMARY.md` when done. The SUMMARY must include:
- Bundle-size delta: post-22-01+22-02 build size (`wc -c main.js`) vs. v1.2 baseline 1,707,327 B.
- THEME-05 result per theme (5 PASS or any FAIL with description).
- BRAT 7-day window: start date, end date, P0/P1 count (must be 0 for GA).
- Plugin-store re-review submission timestamp + community-plugins.json entry status.
- Commit hashes for all 6-7 commits.
- Tags pushed: `1.3.0-beta.1`, `1.3.0`.
- v1.3 milestone close declaration.
</output>
