---
phase: 22-v1-2-path-removal-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/settings/SettingsStore.ts
  - src/settings/SettingsTab.ts
  - src/main.ts
  - src/main/childEditorSync.ts
  - src/main/sectionLockExtension.ts
  - src/main/nestedEditorExtension.ts
  - src/main/childEditorRegistry.ts
  - src/main/codeActionsEditorExtension.ts
  - src/main/readingModeMigrationHook.ts
  - src/main/readingModeLegacyBannerPostProcessor.ts
  - src/notes/NoteTemplate.ts
  - src/notes/NoteWriter.ts
  - src/solve/codeExtractor.ts
  - src/solve/starterCodeInjector.ts
  - src/contest/ContestFinalizer.ts
  - src/widget/codeBlockProcessor.ts
  - src/widget/liveModeBannerStateField.ts
  - src/widget/liveModeViewPlugin.ts
  - src/widget/themeListener.ts
  - src/widget/multiPaneCoordinator.ts
  - src/widget/WidgetController.ts
  - src/widget/migrationBackupGc.ts
  - src/widget/fenceMigrator.ts
  - src/widget/childParentSync.ts
  - src/solve/resetCodeWithConfirm.ts
  - src/main/sectionProtectionExtension.ts
  - tests/main/childEditorSync.test.ts
  - tests/main/childEditorSync.repair.test.ts
  - tests/main/sectionLockExtension.test.ts
  - tests/main/nestedEditorExtension.test.ts
  - tests/main/codeActionsEditorExtension.test.ts
  - tests/main/childEditorRegistry.test.ts
  - tests/main/resetCommand.childDispatch.test.ts
  - tests/main/tabMidLine.test.ts
  - CLAUDE.md
autonomous: false
requirements:
  - DELETE-01
  - DELETE-02
  - DELETE-03
  - DELETE-04
  - DELETE-05
  - DELETE-06
  - DELETE-07
  - DELETE-08
  - POLISH-01
  - PROTECT-03
must_haves:
  truths:
    - "On first 1.3.x boot with a 1.2.x `data.json` (`useInlineWidget: false`, `useNestedEditor: true`), the plugin force-flips `useInlineWidget=true`, emits a one-time Notice, and mounts the v1.3 widget — the v1.2 nested-editor extensions are never registered."
    - "On a fresh install (no `data.json` yet), `useInlineWidget` defaults to `true` and the v1.3 widget is the only mount path."
    - "The 5 v1.2 source files (`childEditorSync.ts`, `sectionLockExtension.ts`, `nestedEditorExtension.ts`, `childEditorRegistry.ts`, `codeActionsEditorExtension.ts`) no longer exist in `src/main/`."
    - "The 8 dead test files (plus any audit-expand additions) no longer exist in `tests/main/` or `tests/integration/`."
    - "`src/main.ts` contains zero references to `useInlineWidget`, `useNestedEditor`, `childEditorRegistry`, `nestedEditorRebuildEffect`, `ECHO_PRONE_USER_EVENTS`, `buildNestedEditorExtension`, or `buildSectionLockExtension`."
    - "Repo-wide grep for `userEvent: 'leetcode\\.` returns zero hits."
    - "Repo-wide grep for `useInlineWidget|useNestedEditor` returns zero hits across `src/`."
    - "`CLAUDE.md` `## Conventions` section (the two paragraphs at lines ~195 + ~197) is removed; section heading itself removed if both paragraphs were the entire section."
    - "`npm test` passes; `npm run build` produces a clean `main.js` smaller than v1.2's 1,707,327-byte ship size."
  artifacts:
    - path: "src/settings/SettingsStore.ts"
      provides: "Settings store WITHOUT `useInlineWidget` and `useNestedEditor` fields, getters, setters, mapper entries, or `DEFAULT_DATA` entries"
      contains: "autoMigrateOnOpen"
    - path: "src/main.ts"
      provides: "Plugin lifecycle wiring with v1.3 widget as the unconditional path; mutual-exclusion inversion Notice for 1.2.x carry-over `data.json`"
      contains: "registerMarkdownCodeBlockProcessor('leetcode-solve'"
    - path: "CLAUDE.md"
      provides: "Documentation matching post-Phase-22 reality (no obsolete `'leetcode.*'` userEvent paragraph; no obsolete write-path paragraph)"
  key_links:
    - from: "src/main.ts (Plugin.onload mutual-exclusion guard, was line 1139)"
      to: "src/settings/SettingsStore.ts setUseInlineWidget setter"
      via: "boot-time inversion: `if (!useInlineWidget && useNestedEditor) → setUseInlineWidget(true)`"
      pattern: "v1\\.2 nested-editor path retired"
    - from: "src/main.ts (Plugin.onload widget registration block)"
      to: "src/widget/codeBlockProcessor.ts + src/widget/liveModeViewPlugin.ts"
      via: "registerMarkdownCodeBlockProcessor + registerEditorExtension"
      pattern: "registerMarkdownCodeBlockProcessor\\('leetcode-solve'"
---

<objective>
Plan 22-01 is the **hard cutover** from v1.2 to v1.3. It executes CONTEXT D-cutover-01's five sub-steps (A → E) in order, each as its own atomic commit:

- **A — Flip default + invert mutual-exclusion Notice** (per D-cutover-01, D-cutover-02). Flip `DEFAULT_DATA.useInlineWidget` from `false` → `true` in `src/settings/SettingsStore.ts:287`. Invert the mutual-exclusion guard at `src/main.ts:1128-1144` so 1.2.x users carrying `useInlineWidget: false` AND `useNestedEditor: true` get force-flipped to v1.3 with a one-time Notice. Single atomic commit.
- **B — 1-day dev-vault dogfood** (per D-cutover-01 sub-step B; **non-negotiable safety net** per RESEARCH Pitfall 3). `checkpoint:human-verify` gate: deploy to dev vault, exercise problem-open/solve/run/submit/AI-debug/language-switch/vim-toggle/theme-swap on real LC notes for ~24 hours wall-clock. Document outcome in `22-VERIFICATION.md`. If a P0/P1 surfaces, fix BEFORE proceeding to deletions — recovery is a flag toggle while v1.2 path is alive.
- **C — Delete 5 v1.2 source files** (per DELETE-01..05). `git rm` `childEditorSync.ts` (809 LOC), `sectionLockExtension.ts` (527 LOC), `nestedEditorExtension.ts` (395 LOC), `childEditorRegistry.ts` (114 LOC), `codeActionsEditorExtension.ts` (401 LOC) = 2,246 LOC.
- **D — Delete 8+ dead tests** (per DELETE-07; audit-expand per RESEARCH §14). The CONTEXT-listed 8 files are the floor — the audit-expand candidates (`mutualExclusion.test.ts`, `tests/integration/sectionLockIntegration.test.ts`, possibly `lifecycle.test.ts`, `childEditorFactory.test.ts`, `fileOpenRetrofit.test.ts`, `switchFenceLanguage.test.ts`) are verified at task start by running the suite and identifying which surviving tests fail compilation post-source-deletion.
- **E — Atomic main.ts unwiring + 16-file `useInlineWidget` strip + userEvent annotation strip + CLAUDE.md `## Conventions` strip + SettingsStore/SettingsTab field deletion** (per D-unwire-01, D-unwire-02, D-settings-01, D-claude-01, DELETE-06, DELETE-08, PROTECT-03). Single atomic commit because TypeScript enforces atomicity — half-unwired `main.ts` does not compile.

Purpose: deliver POLISH-01 (default-ON v1.3 on first 1.3.x release), DELETE-01..08 (source + test + main.ts + CLAUDE.md cleanup), PROTECT-03 (`'leetcode.*'` userEvent convention retired).

Output: 5 atomic commits, a tree where v1.3 is the only path, a passing test suite, a clean `npm run build`, and a documented dogfood log entry in `22-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-v1-2-path-removal-polish/22-CONTEXT.md
@.planning/phases/22-v1-2-path-removal-polish/22-RESEARCH.md
@.planning/phases/22-v1-2-path-removal-polish/22-PATTERNS.md
@.planning/phases/22-v1-2-path-removal-polish/22-VALIDATION.md
@CLAUDE.md
</context>

## Phase Goal (Plan 22-01 contribution)

Hard cutover. Default flip; deletion of v1.2 sources, dead tests, and `src/main.ts` v1.2 wiring; retirement of the `'leetcode.*'` userEvent convention; deletion of obsolete CLAUDE.md `## Conventions` paragraphs. After this plan, the v1.3 widget is the only mount path; recovery is `git revert`.

<artifacts_this_phase_produces>

This plan is **subtractive-dominant**. It produces a small set of net-new strings (the inversion Notice text; commit messages) and otherwise removes existing code. The artifact surface this plan creates:

- **Inversion Notice string** in `src/main.ts` — `'v1.2 nested-editor path retired in 1.3.0 — using v1.3 widget'` (5-second timeout, standard `new Notice(...)` shape from CONTEXT D-cutover-02 + RESEARCH §1).
- **Commit messages** (5 atomic commits per Pattern S-01):
  1. `feat(22-01): flip useInlineWidget default to true (POLISH-01)`
  2. (no commit — dogfood checkpoint produces a `22-VERIFICATION.md` entry only)
  3. `chore(22-01): delete v1.2 source files (DELETE-01..05)`
  4. `chore(22-01): delete dead v1.2 test files (DELETE-07)`
  5. `chore(22-01): unwire v1.2 path from src/main.ts + retire 'leetcode.*' userEvent convention (DELETE-06, DELETE-08, PROTECT-03)`
- **Dogfood log entry** in `22-VERIFICATION.md` documenting Phase 22-01-B outcome (problem-open, solve, run, submit, AI debug, language switch, vim toggle, theme swap — pass/fail per workflow).

No new exported symbols. No new files. Sub-step E may produce `tests/main/` directory removal if the directory becomes empty after audit-expanded deletions (per CONTEXT Claude's Discretion).

</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 22-01-A: Flip useInlineWidget default + invert mutual-exclusion Notice</name>
  <files>src/settings/SettingsStore.ts, src/main.ts</files>
  <read_first>
    - src/settings/SettingsStore.ts (lines 79-95 for `LeetCodeSettings` interface; lines 280-300 for `DEFAULT_DATA`; lines 700-725 for `loadFromRaw`; lines 895-925 for getters/setters)
    - src/main.ts (lines 1100-1180 for `Plugin.onload` mutual-exclusion guard and v1.2/v1.3 registration blocks)
    - .planning/phases/22-v1-2-path-removal-polish/22-RESEARCH.md (§1 mutual-exclusion inversion verbatim shape; §10 read-and-ignore mechanism)
    - .planning/phases/22-v1-2-path-removal-polish/22-CONTEXT.md (D-cutover-01 sub-step A; D-cutover-02 inversion logic)
    - .planning/phases/22-v1-2-path-removal-polish/22-PATTERNS.md (Pattern S-06 Notice invocation shape)
  </read_first>
  <behavior>
    - Test 1 (existing or new): `SettingsStore.loadFromRaw({})` returns an object whose `useInlineWidget` field is `true` (was `false` pre-flip; per D-cutover-01 sub-step A).
    - Test 2 (existing or new): `SettingsStore.loadFromRaw({ useInlineWidget: false })` still returns `useInlineWidget: false` (the persisted-true-or-false-honors-user-data invariant from `loadFromRaw` lines 718-720 still applies; the default flip only affects fresh installs missing the field).
    - Test 3 (existing or new): the mutual-exclusion inversion in `Plugin.onload` — when `getUseInlineWidget()` returns `false` AND `getUseNestedEditor()` returns `true`, the plugin calls `settings.setUseInlineWidget(true)` and emits the inversion Notice exactly once. Mock `Notice` constructor; assert message matches `/v1\.2 nested-editor path retired/`.
    - Test 4: when both return `true`, neither setter nor Notice fires (no-op path).
    - Test 5: when `useInlineWidget=true` and `useNestedEditor=false` (the post-flip default-fresh-install state), neither setter nor Notice fires.
  </behavior>
  <action>
    Per D-cutover-01 sub-step A (CONTEXT) and RESEARCH §1, in a single atomic commit:

    **Edit `src/settings/SettingsStore.ts`**:
    1. At the `DEFAULT_DATA` object literal (line 287 per RESEARCH §1), change `useInlineWidget: false,` to `useInlineWidget: true,`. Leave `useNestedEditor: true,` at line 284 unchanged (it dies wholesale in sub-step E per D-settings-01).
    2. Update the surrounding `// useInlineWidget remains "no migration"...` comment block at lines 287-291 to reflect the new default semantic — rewrite the comment to describe the v1.3 widget as the canonical path; mention that 1.2.x users carrying explicit `false` are inverted via the `Plugin.onload` guard at sub-step A.

    **Edit `src/main.ts` mutual-exclusion guard** (lines ~1128-1144 per RESEARCH §1, line numbers may have shifted slightly):
    1. Locate the existing block: `if (useInlineWidget && useNestedEditor) { new Notice('useInlineWidget is ON — disabling useNestedEditor (mutually exclusive)', 5000); await this.settings.setUseNestedEditor(false); useNestedEditor = false; }`.
    2. Replace with the inversion: `if (!useInlineWidget && useNestedEditor) { new Notice('v1.2 nested-editor path retired in 1.3.0 — using v1.3 widget', 5000); await this.settings.setUseInlineWidget(true); }`. Drop the `useNestedEditor = false` line — the value is irrelevant once we force `useInlineWidget=true` (the v1.2 registration block dies in sub-step E and the field itself disappears via D-settings-01).
    3. Update the surrounding Phase 19 D-06 comment to describe the new inversion semantic (1.2.x carry-over `data.json` upgrade-path).
    4. Preserve the `5000` timeout per Pattern S-06.

    **DO NOT** touch any other `useInlineWidget` reference in this commit — gate-removal is sub-step E. Sub-step A is precisely 2 file edits + 1 commit.

    **Run** `npm run build` and `npm test` after the edits; both MUST be green before committing. If `npm test` fails because an existing test asserted the old `DEFAULT_DATA.useInlineWidget = false`, update that test in the same commit (the test's intent was to verify the default; the new default is `true`).

    **Commit**: `feat(22-01): flip useInlineWidget default to true (POLISH-01)`. Body: 2-3 lines explaining the flip + the inversion guard rationale (1.2.x carry-over `data.json` upgrade safety net).
  </action>
  <verify>
    <automated>npm test &amp;&amp; npm run build &amp;&amp; node -e 'const s = require("./src/settings/SettingsStore.ts"); /* type-only */ ' ; grep -c 'useInlineWidget: true,' src/settings/SettingsStore.ts | grep -q '^1$' &amp;&amp; grep -c 'v1\.2 nested-editor path retired' src/main.ts | grep -q '^1$' &amp;&amp; ! grep -q 'useInlineWidget is ON — disabling useNestedEditor' src/main.ts</automated>
  </verify>
  <done>
    - `src/settings/SettingsStore.ts` line 287 reads `useInlineWidget: true,`.
    - `src/main.ts` mutual-exclusion guard predicate reads `!useInlineWidget && useNestedEditor` (negated `useInlineWidget`).
    - `src/main.ts` Notice message reads `'v1.2 nested-editor path retired in 1.3.0 — using v1.3 widget'`.
    - Old Notice text `'useInlineWidget is ON — disabling useNestedEditor (mutually exclusive)'` is gone.
    - `npm test` passes; `npm run build` clean.
    - Single commit on `gsd/v1.3-inline-widget-architecture` branch with subject `feat(22-01): flip useInlineWidget default to true (POLISH-01)`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 22-01-B: 1-day dev-vault dogfood gate</name>
  <files>.planning/phases/22-v1-2-path-removal-polish/22-VERIFICATION.md (creates if missing)</files>
  <read_first>
    - .planning/phases/22-v1-2-path-removal-polish/22-CONTEXT.md (D-cutover-01 sub-step B; non-negotiable safety net)
    - .planning/phases/22-v1-2-path-removal-polish/22-RESEARCH.md (Pitfall 3: dogfood-skip risk)
    - $HOME/.claude/projects/-Users-moxu-projects-obsidian-leetcode/memory/MEMORY.md (dev-vault path: `~/Documents/Obsidian Vault/.obsidian/plugins/obsidian-leetcode/`)
  </read_first>
  <action>
    Task 22-01-A flipped `useInlineWidget` default to `true` and inverted the mutual-exclusion guard so 1.2.x users carrying `useInlineWidget: false, useNestedEditor: true` in `data.json` get force-flipped to v1.3 with a one-time Notice. The v1.2 nested-editor source code is **still in tree** (sub-steps C/D/E haven't run yet) — this is the safety net per D-cutover-01: if a regression surfaces, toggle `useInlineWidget` OFF in dev-vault settings and the v1.2 path still works.
  </action>
  <how-to-verify>
    Deploy to dev vault, then exercise the v1.3 default-ON path on real LC notes for ~24 hours wall-clock:

    1. **Build + deploy**: `npm run build` from repo root; copy `main.js`, `manifest.json`, `styles.css` into `~/Documents/Obsidian Vault/.obsidian/plugins/obsidian-leetcode/` (confirmed dev-vault path per `MEMORY.md`).
    2. **Reload Obsidian** (Cmd-R or Settings → Restart). Disable + re-enable the plugin to force a fresh `Plugin.onload` cycle. If your `data.json` had `useInlineWidget: false`, you should see the inversion Notice: `'v1.2 nested-editor path retired in 1.3.0 — using v1.3 widget'`.
    3. **Workflow checklist** — exercise each of the following on at least one real LC note (mix of recently-solved + AC'd + fresh-create):
       - **Problem open**: open a problem from the LeetCode browser command palette → fresh note created with v1.3 widget mounted (no flicker, no Notice fallback to Python).
       - **Solve**: type into the widget; Tab inserts indent; backspace works; Cmd-Z is per-widget undo.
       - **Run**: click Run button inside widget → result panel populates without crash.
       - **Submit**: click Submit button inside widget → status polls + verdict appears.
       - **AI Debug**: click AI Debug button → AI provider responds without disk-roundtrip error.
       - **Language switch**: click chevron inside widget → language flips; `lc-language` frontmatter updates; widget re-themes.
       - **Vim toggle**: in Obsidian Settings → Editor → Vim Mode, toggle ON; widget activates vim without note reload (Phase 20 live-reconfigure).
       - **Theme swap**: in Obsidian Settings → Appearance, swap themes; widget re-themes live.
       - **Reading mode**: switch to Reading mode (Cmd-E) → widget renders read-only with same theme.
       - **Migration sanity**: open a v1.2-format note (if any in dev vault); Phase 21's lazy migration runs; widget mounts post-migration; no flicker (Phase 21.1 R10 baseline).
    4. **Watch for ~24 hours of normal use** (no rush; iterate on real problems if available). Monitor the developer console for errors.
    5. **Document outcome** in `.planning/phases/22-v1-2-path-removal-polish/22-VERIFICATION.md` (create file if it does not yet exist) under heading `## 22-01-B Dogfood Log` with one bulleted entry per workflow item: `- [PASS|FAIL] {workflow}: {observation}`. If any workflow FAILs, **STOP** and reply `failed: {workflow} {brief root cause}` so the orchestrator can route a fix BEFORE deletions land. If all workflows pass, reply `approved`.
  </how-to-verify>
  <resume-signal>Type `approved` (all workflows passed; proceed to sub-step C deletions) or `failed: {workflow} {observation}` (P0/P1 surfaced; fix BEFORE proceeding — toggle `useInlineWidget` OFF in dev-vault to recover via v1.2 path while diagnosing).</resume-signal>
  <verify>
    <human-check>Resume signal received: `approved` (all workflows in the dogfood checklist passed) or `failed: {workflow} {observation}` (a P0/P1 surfaced; fix before proceeding to sub-step C). The 1-day wall-clock window has elapsed and the dogfood log entry exists in `.planning/phases/22-v1-2-path-removal-polish/22-VERIFICATION.md` under `## 22-01-B Dogfood Log` heading.</human-check>
  </verify>
  <done>
    - `.planning/phases/22-v1-2-path-removal-polish/22-VERIFICATION.md` contains a `## 22-01-B Dogfood Log` section with one bulleted line per workflow item (problem-open, solve, run, submit, AI debug, language switch, vim toggle, theme swap, reading mode, migration sanity).
    - Every workflow line is annotated `[PASS]` (or, if any line is `[FAIL]`, the orchestrator was alerted via the `failed: ...` resume signal and a fix landed before sub-step C).
    - The 1-day wall-clock dogfood window elapsed.
    - User typed `approved` (or `failed: ...`) to advance to sub-step C.
  </done>

</task>

<task type="auto" tdd="false">
  <name>Task 22-01-C: Delete 5 v1.2 source files</name>
  <files>src/main/childEditorSync.ts, src/main/sectionLockExtension.ts, src/main/nestedEditorExtension.ts, src/main/childEditorRegistry.ts, src/main/codeActionsEditorExtension.ts</files>
  <read_first>
    - .planning/phases/22-v1-2-path-removal-polish/22-RESEARCH.md (Phase Requirements table for DELETE-01..05; verified import sites)
    - .planning/phases/22-v1-2-path-removal-polish/22-PATTERNS.md (Deletion Targets table; export surfaces)
    - .planning/phases/22-v1-2-path-removal-polish/22-CONTEXT.md (D-cutover-01 sub-step C)
  </read_first>
  <action>
    Per DELETE-01..05 + D-cutover-01 sub-step C, in a single atomic commit:

    1. `git rm src/main/childEditorSync.ts` (DELETE-01; 809 LOC).
    2. `git rm src/main/sectionLockExtension.ts` (DELETE-02; 527 LOC; replaced by `src/main/sectionProtectionExtension.ts` which stays).
    3. `git rm src/main/nestedEditorExtension.ts` (DELETE-03; 395 LOC).
    4. `git rm src/main/childEditorRegistry.ts` (DELETE-04; 114 LOC; replaced by `src/widget/widgetRegistry.ts` already in tree).
    5. `git rm src/main/codeActionsEditorExtension.ts` (DELETE-05; 401 LOC; `findCodeFence` already lifted to `src/widget/fenceLocator.ts` per Phase 19 carry-over).

    Total: 2,246 LOC of v1.2 sources removed.

    **`npm run build` will fail at this point** — `src/main.ts` still imports the deleted modules (e.g., `buildNestedEditorExtension`, `buildSectionLockExtension`, `ChildEditorRegistry`, `nestedEditorRebuildEffect`, `ECHO_PRONE_USER_EVENTS`, `buildCodeActionsEditorExtension`). **This is expected.** Sub-step E (Task 22-01-E) is the atomic unwiring commit that removes those imports. Sub-step D (Task 22-01-D) deletes the dead test files first to keep the test suite compilable in isolation; sub-step E then unwires `main.ts`.

    Do NOT `npm run build` or `npm test` between sub-steps C and E — they are partial states. Run them after sub-step E completes and produces a clean tree.

    **Verification at this sub-step is structural only**: confirm the 5 files are gone via `ls`, confirm `git status` shows them as `deleted` and stages them for the commit.

    **Audit expand check**: also survey `src/main/childEditorFactory.ts` per RESEARCH §14 — it imports `createScrollIntoViewExtension` from the deleted `childEditorSync.ts`. Verify whether `childEditorFactory.ts` has any v1.3 callers post-Phase-22; if NOT (i.e., the factory is a v1.2-only seam), include `git rm src/main/childEditorFactory.ts` in this same commit and add `tests/main/childEditorFactory.test.ts` to sub-step D's deletion list. If it DOES have v1.3 callers, leave it for now — sub-step E will need to either move `createScrollIntoViewExtension` into the factory inline or delete the surviving factory in a follow-up commit. The decision is grep-driven: `grep -rn 'childEditorFactory' src/` post-sub-step-C and pre-commit; if the only callers are inside `src/main.ts` v1.2 branches that die in sub-step E, mark factory as v1.2-only and include in this commit.

    **Commit**: `chore(22-01): delete v1.2 source files (DELETE-01..05)`. Body: 2-3 lines listing the 5 files + LOC totals.
  </action>
  <verify>
    <automated>! ls src/main/childEditorSync.ts 2>/dev/null &amp;&amp; ! ls src/main/sectionLockExtension.ts 2>/dev/null &amp;&amp; ! ls src/main/nestedEditorExtension.ts 2>/dev/null &amp;&amp; ! ls src/main/childEditorRegistry.ts 2>/dev/null &amp;&amp; ! ls src/main/codeActionsEditorExtension.ts 2>/dev/null &amp;&amp; git log -1 --format=%s | grep -q '^chore(22-01): delete v1.2 source files'</automated>
  </verify>
  <done>
    - All 5 listed files deleted from `src/main/`.
    - `git log -1` shows commit subject `chore(22-01): delete v1.2 source files (DELETE-01..05)`.
    - Tree intentionally fails `npm run build` here — this is expected mid-cutover. Build cleanliness restored in sub-step E.
    - If `childEditorFactory.ts` audit found it to be v1.2-only, it is also deleted in this commit and `tests/main/childEditorFactory.test.ts` is added to sub-step D's deletion list.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 22-01-D: Delete 8+ dead test files (audit-expand)</name>
  <files>tests/main/childEditorSync.test.ts, tests/main/childEditorSync.repair.test.ts, tests/main/sectionLockExtension.test.ts, tests/main/nestedEditorExtension.test.ts, tests/main/codeActionsEditorExtension.test.ts, tests/main/childEditorRegistry.test.ts, tests/main/resetCommand.childDispatch.test.ts, tests/main/tabMidLine.test.ts, tests/main/mutualExclusion.test.ts (likely audit-expand), tests/integration/sectionLockIntegration.test.ts (likely audit-expand)</files>
  <read_first>
    - .planning/phases/22-v1-2-path-removal-polish/22-RESEARCH.md (§14 codebase post-deletion sanity; survivor list; audit-expand candidates)
    - .planning/phases/22-v1-2-path-removal-polish/22-PATTERNS.md (Audit-expand candidates list)
    - tests/main/ (full directory listing — `ls tests/main/`)
  </read_first>
  <action>
    Per DELETE-07 + D-cutover-01 sub-step D + RESEARCH §14 audit-expand, in a single atomic commit:

    1. Delete the **CONTEXT-locked 8 dead-test files**:
       - `tests/main/childEditorSync.test.ts` (DELETE-01 surface)
       - `tests/main/childEditorSync.repair.test.ts` (DELETE-01 surface)
       - `tests/main/sectionLockExtension.test.ts` (DELETE-02 surface)
       - `tests/main/nestedEditorExtension.test.ts` (DELETE-03 surface)
       - `tests/main/codeActionsEditorExtension.test.ts` (DELETE-05 surface)
       - `tests/main/childEditorRegistry.test.ts` (DELETE-04 surface)
       - `tests/main/resetCommand.childDispatch.test.ts` (v1.2 reset child-dispatch path)
       - `tests/main/tabMidLine.test.ts` (v1.2 nested-editor Tab handling)

    2. **Audit-expand**: run `npm test 2>&1 | grep -E "Cannot find module|Failed to resolve"` to identify test files that fail compilation because they import the just-deleted v1.2 sources. Per RESEARCH §14 the expected surviving-but-broken candidates are:
       - `tests/main/mutualExclusion.test.ts` — mutual-exclusion logic dies in sub-step E; the test surface dissolves. **DELETE.**
       - `tests/integration/sectionLockIntegration.test.ts` — imports `sectionLockExtension`; v1.2 fence-opener protection is removed in v1.3 (the surviving narrow `sectionProtectionExtension.test.ts` covers the v1.0 requirement). **DELETE.**
       - `tests/main/lifecycle.test.ts` — imports `ChildEditorRegistry`. **VERIFY**: if the test exclusively exercises `ChildEditorRegistry` lifecycle, **DELETE**. If it exercises broader `Plugin.onload`/`onunload` lifecycle that survives in v1.3, **port** the import out (replace `ChildEditorRegistry` references with `widgetRegistry` equivalents OR remove the registry-specific assertions). Use `grep -c ChildEditorRegistry tests/main/lifecycle.test.ts` and `grep -c WidgetRegistry tests/main/lifecycle.test.ts` to make the call.
       - `tests/main/childEditorFactory.test.ts` — only delete IF sub-step C's audit found `childEditorFactory.ts` to be v1.2-only and deleted it. Otherwise leave (the factory survives and serves the v1.3 widget mount path).
       - `tests/main/fileOpenRetrofit.test.ts` — likely tests the legacy `retrofitStarterCode` path that becomes unreachable. **VERIFY**: `grep -c useInlineWidget tests/main/fileOpenRetrofit.test.ts` — if all assertions are gated on `useInlineWidget=false`, **DELETE**. If the test covers v1.3-relevant fresh-create behavior, **port** the assertions to drop the `useInlineWidget=false` gate (assume always-on).
       - `tests/main/switchFenceLanguage.test.ts` — does this test exercise the parent-CM6 `'leetcode.lang-switch'` dispatch path that dies in sub-step E (the `cm.dispatch` annotation is stripped but the dispatch survives)? Likely **port** (remove userEvent assertions; keep the lang-switch behavior assertions). VERIFY before deciding.

    3. **Decision principle for VERIFY items**: if the test's targets all die in sub-step E (post-E `grep` of the test's imported symbols returns zero hits in surviving `src/`), DELETE. If targets survive but the test asserted on v1.2-only behavior (e.g., userEvent annotation), PORT (drop the v1.2-only assertions, keep the v1.3 assertions).

    4. After deletions/ports, **`npm test` will still fail to compile** because `src/main.ts` still imports the deleted v1.2 sources. **This is expected** — sub-step E unwires main.ts.

    5. After sub-step E completes, the test suite as a whole compiles and runs; verify there.

    6. **Discretion sweep — `tests/main/` empty-directory check** (per CONTEXT Discretion item): if `ls tests/main/` returns zero files post-deletion, `git rm -r tests/main/` and note in commit body. If non-empty, leave the directory.

    **Commit**: `chore(22-01): delete dead v1.2 test files (DELETE-07)`. Body: list every deleted file plus the audit-expand expansion (e.g., `+ mutualExclusion.test.ts (mutual-exclusion logic dies in sub-step E)`).
  </action>
  <verify>
    <automated>! ls tests/main/childEditorSync.test.ts 2>/dev/null &amp;&amp; ! ls tests/main/childEditorSync.repair.test.ts 2>/dev/null &amp;&amp; ! ls tests/main/sectionLockExtension.test.ts 2>/dev/null &amp;&amp; ! ls tests/main/nestedEditorExtension.test.ts 2>/dev/null &amp;&amp; ! ls tests/main/codeActionsEditorExtension.test.ts 2>/dev/null &amp;&amp; ! ls tests/main/childEditorRegistry.test.ts 2>/dev/null &amp;&amp; ! ls tests/main/resetCommand.childDispatch.test.ts 2>/dev/null &amp;&amp; ! ls tests/main/tabMidLine.test.ts 2>/dev/null &amp;&amp; git log -1 --format=%s | grep -q '^chore(22-01): delete dead v1.2 test files'</automated>
  </verify>
  <done>
    - All 8 CONTEXT-locked test files deleted.
    - Audit-expand additions documented in commit body.
    - `git log -1` shows commit subject `chore(22-01): delete dead v1.2 test files (DELETE-07)`.
    - Tree still intentionally fails `npm test` here (sub-step E pending). Build cleanliness restored at sub-step E.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 22-01-E: Atomic main.ts unwiring + 16-file useInlineWidget strip + userEvent annotation strip + SettingsStore/SettingsTab field deletion + CLAUDE.md Conventions strip</name>
  <files>src/main.ts, src/settings/SettingsStore.ts, src/settings/SettingsTab.ts, src/notes/NoteTemplate.ts, src/notes/NoteWriter.ts, src/solve/codeExtractor.ts, src/solve/starterCodeInjector.ts, src/solve/resetCodeWithConfirm.ts, src/contest/ContestFinalizer.ts, src/widget/codeBlockProcessor.ts, src/widget/liveModeBannerStateField.ts, src/widget/liveModeViewPlugin.ts, src/widget/themeListener.ts, src/widget/multiPaneCoordinator.ts, src/widget/WidgetController.ts, src/widget/migrationBackupGc.ts, src/widget/fenceMigrator.ts, src/widget/childParentSync.ts, src/main/sectionProtectionExtension.ts, src/main/readingModeMigrationHook.ts, src/main/readingModeLegacyBannerPostProcessor.ts, CLAUDE.md</files>
  <read_first>
    - src/main.ts (the full file — heaviest unwiring site, 4,688 LOC; 33 `useInlineWidget`/`useNestedEditor` mention sites + 4 `userEvent: 'leetcode.*'` annotation sites)
    - src/settings/SettingsStore.ts (lines 79-95 type interface; lines 280-300 DEFAULT_DATA; lines 700-725 loadFromRaw; lines 895-925 getters/setters — all field-deletion sites for `useInlineWidget` AND `useNestedEditor`)
    - src/settings/SettingsTab.ts (locate the `useInlineWidget` toggle UI block at lines 278-305 per RESEARCH §2; locate the `useNestedEditor` toggle UI block — likely adjacent)
    - src/notes/NoteTemplate.ts (lines 128, 216, 237, 241 per RESEARCH §2 — `BuildNoteBodyInput.useInlineWidget?` field + ternary `codeBlockForV13(...)` vs `codeBlockFor(...)`)
    - src/notes/NoteWriter.ts (lines 186, 350, 366, 370-374, 397, 404, 428-430, 501-502, 525-535, 582-597, 629-632 — `useInlineWidgetOverride`, `retrofitStarterCode` legacy guard, post-write rerender gate)
    - src/solve/starterCodeInjector.ts (lines 273, ~284-290 — `getUseInlineWidget?(): boolean` field on `SettingsHost`, ternary collapse)
    - src/solve/codeExtractor.ts (line 32 — comment + dual-path branch collapse to single-path)
    - src/contest/ContestFinalizer.ts (lines 308, 315 — drop `useInlineWidget: settings.getUseInlineWidget?.() ?? false` arg)
    - src/widget/codeBlockProcessor.ts (lines 67, 180, 198, 202, 292 — `useInlineWidget` self-gates become unconditional)
    - src/widget/liveModeBannerStateField.ts (line 276 gate; line 288 comment)
    - src/widget/liveModeViewPlugin.ts (line 55 — `useInlineWidget` getter type-arg on interface)
    - src/widget/WidgetController.ts (lines 21, 139, 741, 1365 — comments)
    - src/widget/themeListener.ts, src/widget/multiPaneCoordinator.ts, src/widget/migrationBackupGc.ts, src/widget/fenceMigrator.ts, src/widget/childParentSync.ts (comment-only sites per RESEARCH §2)
    - src/solve/resetCodeWithConfirm.ts (line 114 — userEvent convention reference comment)
    - src/main/sectionProtectionExtension.ts (line 25 — userEvent convention reference comment)
    - src/main/readingModeMigrationHook.ts (line 124 — comment about master gate)
    - src/main/readingModeLegacyBannerPostProcessor.ts (lines 88, 92 — comments)
    - CLAUDE.md (lines 195 + 197 — the two `## Conventions` paragraphs)
    - .planning/phases/22-v1-2-path-removal-polish/22-RESEARCH.md (§2 full enumeration; §3 userEvent enumeration; §10 read-and-ignore mechanism; §11 CLAUDE.md state; Code Examples)
    - .planning/phases/22-v1-2-path-removal-polish/22-PATTERNS.md (Pattern S-03 gate-removal; Pattern S-04 cm.dispatch shape; Pattern S-05 atomic unwire)
    - .planning/phases/22-v1-2-path-removal-polish/22-CONTEXT.md (D-unwire-01, D-unwire-02, D-settings-01, D-claude-01, D-cutover-01 sub-step E)
  </read_first>
  <action>
    Per D-unwire-01 + D-unwire-02 + D-settings-01 + D-claude-01 + DELETE-06 + DELETE-08 + PROTECT-03, execute as a **single atomic commit**. The TypeScript compiler enforces atomicity (per Pitfall 1 + Pattern S-05) — half-unwired `main.ts` does not compile because every `useInlineWidget=false` branch references either deleted v1.2 code or surviving v1.3 code; both paths must collapse simultaneously. Run `npm run build` repeatedly during the working state (no commits between) to catch broken references; commit only when the full suite + `tsc` pass cleanly.

    **Phase 1 — Strip `useInlineWidget` master gate from `src/main.ts`** (per Pattern S-03 gate-removal):

    1. Locate every `if (useInlineWidget) { ... }` site (per RESEARCH §2's 26 hits in main.ts). For each: delete the `if` line and its closing `}`; the body becomes unconditional. Indent-flatten the body block.
    2. Locate every `if (!useInlineWidget) { ... }` and `if (useInlineWidget === false) { ... }` site. Delete the entire block (body unreachable post-flip).
    3. Locate every ternary `useInlineWidget ? A : B` and `getUseInlineWidget?.() === true ? A : B`. Collapse to `A`.
    4. Locate every `useInlineWidget !== false` / `getUseInlineWidget?.() !== false`. Replace with `true` (or remove the gate entirely if it was a guard).
    5. Delete the read at line 1130: `const useInlineWidget = this.settings.getUseInlineWidget();` (the variable becomes unused after gate-removal).
    6. Delete the inversion-Notice block introduced in sub-step A — once both `useInlineWidget` and `useNestedEditor` fields die in this commit (Phase 2 below), the inversion guard has no fields to read. Replace the `if (!useInlineWidget && useNestedEditor) { ... }` block with nothing (delete the entire 4-line block).
    7. Delete the `useNestedEditor` read at line 1128: `let useNestedEditor = this.settings.getUseNestedEditor();` and any subsequent `if (useNestedEditor) { this.registerEditorExtension(buildNestedEditorExtension(this)); }` block.
    8. Delete every `// useInlineWidget=ON` / `// CONTEXT D-05` / `// per L9` comment that referenced the gate. Either delete the comment line or rewrite to drop the `useInlineWidget` reference.

    **Phase 2 — Delete `childEditorRegistry` field + lookups + `nestedEditorRebuildEffect` + `ECHO_PRONE_USER_EVENTS`** in `src/main.ts`:

    1. Delete the `import` line(s) at the top of `src/main.ts` for `ChildEditorRegistry`, `buildNestedEditorExtension`, `nestedEditorRebuildEffect`, `ECHO_PRONE_USER_EVENTS`, `buildSectionLockExtension`, `buildCodeActionsEditorExtension`, `wireSyncIfNeeded`, `unwireSync`, `detectAndPropagateExternalChange`, `createScrollIntoViewExtension`, `createChildSyncExtension` (these are now broken — the modules deleted in sub-step C).
    2. Delete the `Plugin.childEditorRegistry` field declaration.
    3. Delete the `this.childEditorRegistry = new ChildEditorRegistry();` instantiation in `Plugin.onload`.
    4. Delete every `this.childEditorRegistry?.get(file.path)` lookup site. The whole `if (childEditor) { dispatch on child }` arm dies; the dispatch path falls back to `widgetRegistry`-based dispatch already in tree.
    5. Delete every `cm.dispatch({ effects: nestedEditorRebuildEffect.of(...) })` site (the effect is gone).
    6. Delete every `if (ECHO_PRONE_USER_EVENTS.has(...))` reference.

    **Phase 3 — Strip `'leetcode.*'` userEvent annotations from `src/main.ts`** (per D-unwire-02; 4 active sites per RESEARCH §3):

    1. Line 3395: in the `cm.dispatch({...})` call inside `switchFenceLanguage`, delete the property `userEvent: 'leetcode.lang-switch',` from the spec object. Preserve `changes` and `selection` keys; preserve `addToHistory.of(false)` annotation if present.
    2. Line 3905: same shape — strip `userEvent: 'leetcode.lang-switch',` in `dispatchChildLanguageReconfigure`.
    3. Line 3963: same shape — strip `userEvent: 'leetcode.lang-switch',` at the second child-CM6 dispatch.
    4. Line 4546: this site is inside the `childEditorRegistry?.get(file.path)` lookup branch (line ~4528), which dies wholesale in Phase 2. The whole enclosing `if (childEditor) { ... }` arm deletes — the userEvent strip at this line is moot.
    5. Strip prose comments referencing the convention: search `src/main.ts` for `'leetcode\.` and `userEvent: 'leetcode\.` in comment lines (e.g., line 3384 per RESEARCH §3) and rewrite or delete each.

    **Phase 4 — Delete `useNestedEditor` AND `useInlineWidget` from `SettingsStore.ts`** (per D-settings-01 read-and-ignore):

    1. `src/settings/SettingsStore.ts` line 79: delete `useNestedEditor: boolean;` field from `LeetCodeSettings` interface.
    2. Same file line 86: delete `useInlineWidget: boolean;` field.
    3. Line 284: delete `useNestedEditor: true,` from `DEFAULT_DATA`.
    4. Line 287: delete `useInlineWidget: true,` from `DEFAULT_DATA` (this was flipped in sub-step A; now the field itself dies).
    5. Lines 711-713: delete the `useNestedEditor: typeof raw.useNestedEditor === 'boolean' ? raw.useNestedEditor : DEFAULT_DATA.useNestedEditor,` mapper block from `loadFromRaw`'s return-object literal.
    6. Lines 718-720: delete the analogous `useInlineWidget:` mapper block.
    7. Line 900: delete `getUseNestedEditor()` getter.
    8. Line ~906: delete `setUseNestedEditor()` setter.
    9. Line 913: delete `getUseInlineWidget()` getter.
    10. Line ~919: delete `setUseInlineWidget()` setter.
    11. Update surrounding doc comments at lines 83-84, 91-92 — rewrite to drop the `useNestedEditor`/`useInlineWidget`/`D-06`/`L9` references; the comment should describe only surviving settings.

    **Phase 5 — Delete `useInlineWidget` AND `useNestedEditor` toggle UI from `SettingsTab.ts`**:

    1. Locate the `useInlineWidget` toggle UI block (lines 278-305 per RESEARCH §2). Delete the entire `new Setting(containerEl).setName(...).setDesc(...).addToggle(...)` chain.
    2. Locate the `useNestedEditor` toggle UI block (likely adjacent). Delete wholesale.
    3. Per CONTEXT Discretion: verify `autoMigrateOnOpen` toggle still mounts cleanly; if it was under an "Experimental" subsection that becomes empty, rename the subsection heading to "Migration" or remove the heading wrapper. Surveying `SettingsTab.ts` post-deletion answers this.

    **Phase 6 — Strip `useInlineWidget` references from 14 consumer files** (per RESEARCH §2 enumeration):

    1. **`src/notes/NoteTemplate.ts`**: delete `useInlineWidget?: boolean` field from `BuildNoteBodyInput` (line 237). Collapse the ternary at line 241 from `input.useInlineWidget ? codeBlockForV13(...) : codeBlockFor(...)` to `codeBlockForV13(...)`. Delete the legacy `codeBlockFor` emitter function entirely (or rename `codeBlockForV13` → `codeBlockFor` and delete the original — pick whichever produces cleaner imports). Update comments at lines 128, 216 to drop the `useInlineWidget` reference.
    2. **`src/notes/NoteWriter.ts`**: delete `useInlineWidgetOverride` parameter (lines 370-374) and all pass-through call sites (lines 397, 404, 428-430, 501-502, 525-535). Delete the entire `if (!useInlineWidget) { retrofitStarterCode(...) }` legacy retrofit guard at lines 582-597 (Phase 21 plan 21-13 already gated this — the unreachable `!` branch dies). Delete the `if (!useInlineWidget) { ... }` post-write rerender gate at lines 629-632 (the v1.3 rerender path is unconditional). Update comments at lines 186, 350, 366 to drop the reference.
    3. **`src/solve/codeExtractor.ts`**: at line 32, the comment + dual-path branch collapses. The legacy fence-tag branch (`if (!useInlineWidget)` body that read language from fence tag) dies; the v1.3 path (read language from `lc-language` frontmatter — Phase 21 MIGRATE-09 SSoT refactor) becomes unconditional. Delete the dual-path conditional; preserve the frontmatter-source extraction.
    4. **`src/solve/starterCodeInjector.ts`**: delete `getUseInlineWidget?(): boolean` from the `SettingsHost` interface (line ~284). Collapse the ternary `settings.getUseInlineWidget?.() === true ? 'leetcode-solve' : 'legacy'` to `'leetcode-solve'`. Update comment at line 273. The legacy fence-emission path becomes unreachable.
    5. **`src/contest/ContestFinalizer.ts`**: at lines 308 and 315, delete `useInlineWidget: settings.getUseInlineWidget?.() ?? false` from the call-site arg (the consumer no longer accepts the field).
    6. **`src/widget/codeBlockProcessor.ts`**: at lines 202 and 292, the `useInlineWidget` self-gates become unconditional `true` (or remove the gates entirely). Update comments at lines 67, 180, 198 to drop the `useInlineWidget=ON` reference.
    7. **`src/widget/liveModeBannerStateField.ts`**: at line ~276, the `getUseInlineWidget?.() !== false` check becomes unconditional `true` or removed. Update comment at line 288.
    8. **`src/widget/liveModeViewPlugin.ts`**: at line 55, delete `getUseInlineWidget?(): boolean` from the interface type-arg.
    9. **`src/widget/WidgetController.ts`**: rewrite comments at lines 21 (also references `useNestedEditor`), 139, 741, 1365 to drop the references.
    10. **`src/widget/themeListener.ts` (lines 40, 65), `src/widget/multiPaneCoordinator.ts` (lines 51, 193), `src/widget/migrationBackupGc.ts`, `src/widget/fenceMigrator.ts` (lines 94-95)**: rewrite comments to drop the `useInlineWidget` references; do NOT touch surrounding code.
    11. **`src/widget/childParentSync.ts` (line 15)**: rewrite the `'leetcode.*'` userEvent doc-comment to describe the v1.3 sync semantic without referencing the retired convention.
    12. **`src/solve/resetCodeWithConfirm.ts` (line 114)**: rewrite the userEvent convention reference comment.
    13. **`src/main/sectionProtectionExtension.ts` (line 25)**: rewrite the userEvent convention reference comment to describe the v1.3 narrow-protection semantic without the retired convention.
    14. **`src/main/readingModeMigrationHook.ts` (line 124), `src/main/readingModeLegacyBannerPostProcessor.ts` (lines 88, 92)**: rewrite the master-gate / `PHASE_22_DELETE_WITH_V1_2_PATH` comments. **DO NOT delete these files** — per CONTEXT L2 migration infrastructure stays in tree indefinitely.

    **Phase 7 — Strip `## Conventions` paragraphs from `CLAUDE.md`** (per D-claude-01 + DELETE-08):

    1. Locate the `## Conventions` heading and its two bullet paragraphs (`'leetcode.*' userEvent annotation is the bypass convention...` at line ~195; `Canonical plugin write-path pattern (Phase 17 D-05)...` at line ~197). Per RESEARCH §11 these are the entire section content.
    2. Delete both bullet paragraphs in their entirety (multi-line spans).
    3. Delete the `## Conventions` heading itself (the section is empty post-deletion; per RESEARCH §11 recommendation).
    4. **Do NOT add any replacement content here** — D-claude-02's `## Architecture` v1.3 sketch lands in Plan 22-03, not in this commit (because the architecture description should match post-deletion reality, and reality only stabilizes after this commit lands).

    **Phase 8 — Discretion sweeps**:

    1. Run `grep -rn 'useInlineWidget\|useNestedEditor' src/` post-edits — should return zero hits. If any remain, hunt them down (likely a comment, an interface, or a forgotten branch).
    2. Run `grep -rn "userEvent: 'leetcode\\." src/` — should return zero hits.
    3. Run `npm ls 2>&1 | grep -i unmet` — verify no orphan transitive deps surface from the deletions (per CONTEXT Discretion `package.json` cleanup item). If any surface, remove from `package.json` `dependencies` in the same commit. None expected per RESEARCH §"Standard Stack" / "Lockfile/package.json cleanup".
    4. Run `npm run build` — MUST be clean. If not, address every type error before committing.
    5. Run `npm test` — MUST be green. If a previously-green test fails, audit whether it was an audit-expand candidate sub-step D missed; if so, delete or port in this commit (acceptable scope creep — the commit's invariant is "tree compiles + tests green," not "test deletions only happen in sub-step D").

    **Commit**: `chore(22-01): unwire v1.2 path from src/main.ts + retire 'leetcode.*' userEvent convention (DELETE-06, DELETE-08, PROTECT-03)`. Body: 5-7 lines summarizing the LOC delta, the field deletions, the userEvent strip, and the CLAUDE.md update.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm test &amp;&amp; ! grep -rn 'useInlineWidget\|useNestedEditor' src/ &amp;&amp; ! grep -rn "userEvent: 'leetcode\." src/ &amp;&amp; ! grep -q "'leetcode\.\*'" CLAUDE.md &amp;&amp; ! grep -q "Canonical plugin write-path pattern" CLAUDE.md &amp;&amp; git log -1 --format=%s | grep -q '^chore(22-01): unwire v1.2 path'</automated>
  </verify>
  <done>
    - `npm run build` clean; `npm test` green.
    - `grep -rn 'useInlineWidget\|useNestedEditor' src/` returns zero hits.
    - `grep -rn "userEvent: 'leetcode\\." src/` returns zero hits.
    - `CLAUDE.md` no longer contains either obsolete `## Conventions` paragraph; section heading itself removed.
    - `src/main.ts` no longer imports `ChildEditorRegistry`, `buildNestedEditorExtension`, `nestedEditorRebuildEffect`, `ECHO_PRONE_USER_EVENTS`, `buildSectionLockExtension`, `buildCodeActionsEditorExtension`, `wireSyncIfNeeded`, `unwireSync`, `detectAndPropagateExternalChange`, `createScrollIntoViewExtension`, or `createChildSyncExtension`.
    - `src/settings/SettingsStore.ts` `LeetCodeSettings` interface no longer includes `useInlineWidget` or `useNestedEditor` fields; getters/setters for both are gone.
    - `src/settings/SettingsTab.ts` no longer renders toggles for either deleted field; `autoMigrateOnOpen` toggle still mounts cleanly.
    - 14 consumer files have `useInlineWidget` references removed (gates collapsed; ternaries flattened; comments rewritten).
    - Single commit on branch with subject `chore(22-01): unwire v1.2 path from src/main.ts + retire 'leetcode.*' userEvent convention (DELETE-06, DELETE-08, PROTECT-03)`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 1.2.x `data.json` → 1.3.0 boot | User's persisted `useInlineWidget: false` overrides the new default until the inversion guard force-flips it. |
| Plugin renderer → vault file system | `vault.process` write path; `requestUrl` for LeetCode HTTP. **Unchanged in Phase 22** (no new mutation paths). |
| `npm install` / lockfile | No new packages installed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | Tampering | 1.2.x carry-over `data.json` upgrade path | mitigate | Sub-step A inversion guard at `src/main.ts:~1139` force-flips `useInlineWidget=true` and emits Notice; without the inversion the plugin would mount deleted v1.2 code in sub-step C+. |
| T-22-01-02 | Denial of service | Half-unwired tree fails `tsc` between commits | mitigate | D-unwire-01 + Pattern S-05: single atomic commit for sub-step E; `npm run build` is the working-state gate before committing. |
| T-22-01-03 | Information disclosure | `useNestedEditor` field carries forward in user `data.json` after deletion | accept | D-settings-01 read-and-ignore: persisted field is silently dropped on next `saveData()`; no PII at risk; no exfiltration vector. |
| T-22-01-04 | Repudiation | User reports a regression but unsure whether Phase 22 caused it | mitigate | Atomic commit per concern (Pattern S-01) means `git revert` + `gsd-undo` granularity per sub-step — bisection is fine-grained. |
| T-22-01-05 | Elevation of privilege | Deleted v1.2 source files contained no auth/AI provider code; deletion does not affect privilege boundaries | n/a | — |
| T-22-01-SC | Tampering | npm/pip/cargo installs | n/a | No new packages installed in Plan 22-01. RESEARCH "Package Legitimacy Audit" section confirms skip. |
</threat_model>

<verification>
**Aggregate plan verification** (run after Task 22-01-E commits):

- `npm run build` → clean (esbuild + tsc).
- `npm test` → green.
- `npm run lint` → clean.
- `! grep -rn 'useInlineWidget\|useNestedEditor' src/` → zero hits.
- `! grep -rn "userEvent: 'leetcode\\." src/` → zero hits.
- `! ls src/main/childEditorSync.ts 2>/dev/null` (and the other 4 deletion targets).
- `! ls tests/main/childEditorSync.test.ts 2>/dev/null` (and the other 7 deletion targets).
- `git log --oneline -10` → 4 atomic commits matching the Pattern S-01 commit messages (sub-step B is a checkpoint, no commit).
- `wc -c main.js` after `npm run build` → less than v1.2 baseline (1,707,327 bytes); the bundle hard cap at 1,706,000 is enforced in Plan 22-03 D-gate-01.

**Wave verification gate** (Plan 22-01 must pass before Plan 22-02 starts per ROADMAP wave structure):
- All four sub-step commits land on the working branch.
- The dogfood log entry exists in `22-VERIFICATION.md` under `## 22-01-B Dogfood Log` heading.
- Tree compiles + suite green.
</verification>

<success_criteria>
- POLISH-01 satisfied: default plugin behavior is the v1.3 widget on first 1.3.x release; the inversion guard catches 1.2.x carry-over `data.json`.
- DELETE-01..05 satisfied: 5 v1.2 source files (2,246 LOC) deleted from `src/main/`.
- DELETE-06 satisfied: `src/main.ts` ~800 LOC of v1.2 wiring deleted; gate-removal complete; field+lookup+annotation deletion complete.
- DELETE-07 satisfied: 8+ dead test files (audit-expanded) deleted from `tests/main/` and `tests/integration/`.
- DELETE-08 satisfied: CLAUDE.md `## Conventions` paragraphs (and section heading) removed.
- PROTECT-03 satisfied: `'leetcode.*'` userEvent convention retired from code annotations and CLAUDE.md.
- 1-day dev-vault dogfood log entry recorded in `22-VERIFICATION.md` documenting the safety-net gate (Pitfall 3 mitigation).
- Tree compiles + full vitest suite green + `eslint-plugin-obsidianmd` clean (Plan 22-03 codifies the lint gate).
</success_criteria>

<output>
Create `.planning/phases/22-v1-2-path-removal-polish/22-01-SUMMARY.md` when done. The SUMMARY must include:
- Commit hashes for the 4 atomic commits.
- Pre/post bundle size deltas (`wc -c main.js` from sub-step A vs sub-step E).
- Pre/post test count delta (vitest passing count from before sub-step D vs after sub-step E).
- Audit-expand outcome: which test files beyond the CONTEXT-locked 8 were deleted, and rationale per file.
- Sub-step B dogfood outcome (PASS / FAIL per workflow item, with FAIL details if any).
- LOC delta: actual deletion totals, including audit-expand contributions.
</output>
