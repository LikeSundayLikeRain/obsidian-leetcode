---
phase: 07-ai-provider-foundation
plan: 07
type: execute
wave: 5
depends_on: [07-01, 07-02, 07-03, 07-04, 07-05, 07-06]
files_modified:
  - src/shared/logger.ts
  - tests/shared/logger.test.ts
  - src/ai/providers/openaiCompatible.ts
  - src/ai/providers/ollama.ts
  - src/main.ts
  - tests/ai/probes.test.ts
  - src/ai/AIClient.ts
  - tests/ai/aiClient.test.ts
  - src/ai/disclosure.ts
  - tests/ai/disclosure.test.ts
  - tests/ai/probe-debounce.test.ts
autonomous: true
gap_closure: true
requirements: [AIPROV-02, AIPROV-03, AIPROV-04]
tags: [ai, gap-closure, logger, probe-guard, await-bug, freeze-disclosure]
must_haves:
  truths:
    - "Logger redactString does not garble log output — 'Authorization: Bearer sk-xyz' redacts cleanly without double-replacement (input → 'Authorization: Bearer [REDACTED]' or equivalent clean form, never 'Authorization=[REDACTED] [REDACTED]')."
    - "probeCustom returns a clean error when baseUrl is empty — no network call issued with a relative URL ({ok:false, errorMessage:/Base URL is required/i})."
    - "probeOllama returns a clean error when baseUrl is empty — no network call issued with a relative URL ({ok:false, errorMessage:/Base URL is required/i})."
    - "testActiveAIConnection blocks empty baseUrl for custom and ollama with a Notice — does NOT invoke aiClient.probe in that path."
    - "AIClient.invoke uses await on adapter.invoke so adapter rejections propagate through the surrounding try/catch (or, equivalently, are observable as rejected promises with their original message preserved)."
    - "DISCLOSURE_BASE_COPY arrays are frozen so mutation attempts throw in strict mode (or fail silently in non-strict) — modal renders observe a stable snapshot regardless of future-phase mutation attempts."
  artifacts:
    - path: "src/shared/logger.ts"
      provides: "redactString without double-replacement bug"
      contains: "SECRET_VALUE_PATTERN"
    - path: "src/ai/providers/openaiCompatible.ts"
      provides: "probeCustom with empty-baseUrl early-return guard"
      contains: "Base URL is required"
    - path: "src/ai/providers/ollama.ts"
      provides: "probeOllama with empty-baseUrl early-return guard"
      contains: "Base URL is required"
    - path: "src/main.ts"
      provides: "testActiveAIConnection empty-baseUrl guard for custom + ollama"
      contains: "baseUrl"
    - path: "src/ai/AIClient.ts"
      provides: "invoke() awaits adapter.invoke so try/catch can observe adapter rejections"
      contains: "return await adapter.invoke"
    - path: "src/ai/disclosure.ts"
      provides: "DISCLOSURE_BASE_COPY frozen at module load"
      contains: "Object.freeze"
  key_links:
    - from: "redactString('Authorization: Bearer sk-xyz')"
      to: "Single clean redaction output"
      via: "Either single-pattern alternation OR exclude '[' from value char class"
      pattern: "no '=' substituted for ':', no double '[REDACTED]'"
    - from: "probeCustom({baseUrl: ''})"
      to: "{ok:false, errorMessage:/Base URL is required/i}"
      via: "Early-return guard at top of function"
      pattern: "fetcher mock asserts zero calls"
    - from: "AIClient.invoke try/catch"
      to: "adapter.invoke rejection"
      via: "return await adapter.invoke(req)"
      pattern: "rejected promise message preserved end-to-end"
---

<objective>
Close 4 gaps from 07-VERIFICATION.md and 07-REVIEW.md without regressing any of the 12 existing must-have truths or the 157 passing AI suite tests.

The phase-level requirements (AIPROV-01..07) are all SATISFIED at the behavioral level. These fixes harden correctness inside already-shipped requirements:
- CR-01 (BLOCKER): logger redactString double-replacement garbles `Authorization: Bearer <token>` log lines.
- CR-02 (BLOCKER): probeCustom issues a relative-URL fetch when baseUrl is empty (reachable on first click of Test connection after selecting Custom).
- WR-01 (WARNING): AIClient.invoke missing `await` on adapter.invoke breaks the try/catch contract.
- WR-02 (WARNING): DISCLOSURE_BASE_COPY iterated as live reference allows mid-render mutation.

Purpose: Phase 07 closeout for code review + verifier sign-off before Phase 08 work begins. CR-01 and CR-02 are observable defects in shipped code paths; WR-01 prevents a Phase 08 integration bug; WR-02 hardens against future-phase mutation patterns documented in PATTERNS.md Pattern 4.

Output:
- Fixed redactString implementation (single-pattern alternation OR exclude-`[` value class — executor picks cleaner option after reading current code).
- New logger test fixture asserting clean output for `Authorization: Bearer sk-proj-abc`.
- probeCustom + probeOllama empty-baseUrl guards.
- testActiveAIConnection empty-baseUrl guard for custom + ollama (defense-in-depth, matches existing apiKey-guard symmetry).
- AIClient.invoke uses `return await adapter.invoke(req)`.
- DISCLOSURE_BASE_COPY frozen via Object.freeze at module load.
- New tests for each fix.
- All existing tests still pass (no regression).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-provider-foundation/07-VERIFICATION.md
@.planning/phases/07-ai-provider-foundation/07-REVIEW.md
@.planning/phases/07-ai-provider-foundation/07-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from current source. -->

From src/ai/types.ts:
```typescript
export type AIProvider = 'anthropic' | 'openai' | 'openrouter' | 'ollama' | 'custom';

export interface ProviderConfig {
  apiKey: string;
  baseUrl: string;
  model: string;
  disclosureAcknowledged: boolean;
}

export interface ProbeResult {
  ok: boolean;
  modelCount?: number | null;
  errorMessage?: string;
}
```

From src/ai/obsidianFetch.ts:
```typescript
export type FetchFn = (url: string, init?: RequestInit) => Promise<Response>;
```

From src/shared/logger.ts (current — to be fixed in Task 1):
```typescript
const REDACT = /session|csrf|cookie|token|apikey|api[_-]?key|bearer|authorization/i;
const SECRET_VALUE_PATTERN = /\b(LEETCODE_SESSION|csrftoken|session|csrf|cookie|token|authorization|apikey|api[_-]?key|x-api-key)\s*[=:]\s*[^\s;,"'&}\]]+/gi;
const BEARER_VALUE_PATTERN = /\b(bearer)\s+([^\s;,"'&}\]]+)/gi;
function redactString(s: string): string { ... two-pass replace producing garbled output ... }
```

From src/ai/providers/openaiCompatible.ts:
```typescript
export async function probeCustom(cfg: ProviderConfig, fetcher: FetchFn): Promise<ProbeResult>;
// Currently: cfg.baseUrl.replace(/\/$/, '') → '' when baseUrl=''; fetch('/models') is relative.
```

From src/ai/providers/ollama.ts (similar shape — needs same guard):
```typescript
export async function probeOllama(cfg: ProviderConfig, fetcher: FetchFn): Promise<ProbeResult>;
```

From src/main.ts:776 (testActiveAIConnection):
```typescript
// Existing apiKey guard (lines 775-782):
if (
  (provider === 'anthropic' || provider === 'openai' || provider === 'openrouter') &&
  cfg.apiKey === ''
) {
  new Notice(`Enter an API key for ${prettyName(provider)} first.`, 3000);
  return;
}
// MISSING: parallel guard for empty baseUrl on custom + ollama.
```

From src/ai/AIClient.ts:150 (current — to be fixed in Task 3):
```typescript
async invoke(req: AIRequest): Promise<AIResponse> {
  ...
  try {
    ...
    return adapter.invoke(req);  // ← missing await
  } catch (err) { ... }
}
```

From src/ai/disclosure.ts:43-56:
```typescript
// Currently NOT frozen — comment says intentional, but verifier WR-02 wants snapshot/freeze.
export const DISCLOSURE_BASE_COPY: { willSend: string[]; neverSends: string[] } = { ... };
```

From src/ai/disclosure.ts:115, 122 (modal render — iterates live reference):
```typescript
for (const line of DISCLOSURE_BASE_COPY.willSend) {
  willList.createEl('li', { text: line });
}
for (const line of DISCLOSURE_BASE_COPY.neverSends) {
  neverList.createEl('li', { text: line });
}
```
</interfaces>

<existing_test_files>
- tests/shared/logger.test.ts — Phase 07 logger redaction tests (Categories 1, 2, 3 all green; add a Category 4 fixture for CR-01).
- tests/ai/probes.test.ts — cross-provider happy-path roll-up (add empty-baseUrl tests for probeCustom + probeOllama).
- tests/ai/aiClient.test.ts — AIClient facade tests (extend invoke section with adapter-rejection await test).
- tests/ai/disclosure.test.ts — modal copy + behavior tests (add freeze assertion).
</existing_test_files>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix CR-01 logger double-replacement + add Category 4 test fixture</name>
  <files>src/shared/logger.ts, tests/shared/logger.test.ts</files>
  <read_first>
    - src/shared/logger.ts (current state — lines 17-50: REDACT, SECRET_VALUE_PATTERN, BEARER_VALUE_PATTERN, redactString two-pass implementation)
    - tests/shared/logger.test.ts (existing Category 1/2/3 tests — confirm they still pass after fix; do not break the captured() helper or warnSpy pattern)
  </read_first>
  <behavior>
    - Test 1 (NEW — Category 4): logger.warn('http err', 'Authorization: Bearer sk-proj-abcdef') produces output that does NOT contain 'sk-proj-abcdef', does NOT contain 'Authorization=[REDACTED] [REDACTED]', and DOES contain '[REDACTED]'. The output must preserve the colon separator OR transform to a clean key=value form — but never contain both ':' and '=' for the same authorization pair, and never contain the substring '[REDACTED] [REDACTED]'.
    - Test 2 (NEW — Category 4): logger.warn('http err', 'authorization: bearer sk-xyz') (lowercase variant) produces clean redacted output without double-replacement artifacts.
    - Test 3 (NEW — Category 4): assert no occurrence of '=[REDACTED] [REDACTED]' substring after redacting an Authorization header (regression guard).
    - Categories 1, 2, 3: ALL existing tests continue to pass (no regression).
  </behavior>
  <action>
    Fix redactString in src/shared/logger.ts to eliminate the double-replacement bug confirmed by CR-01 and 07-VERIFICATION.md (truth #4 FAILED). The two-pass at lines 47-49 (BEARER_VALUE_PATTERN then SECRET_VALUE_PATTERN) re-consumes the [REDACTED] token placed by pass 1; the SECRET_VALUE_PATTERN's authorization alternate at line 32 matches 'authorization: Bearer' as key=value with 'Bearer' as the value char class match, then re-consumes adjacent characters.

    Pick ONE of two cleaner approaches after reading current logger.ts (executor judgment — both produce clean output, both keep existing tests green):

    Approach A (RECOMMENDED — minimal-targeted fix, smaller diff): Exclude the open-bracket '[' character from SECRET_VALUE_PATTERN's value character class so an already-placed '[REDACTED]' token is never re-consumed by the second pass. Change the value char class from `[^\s;,"'&}\]]+` to `[^\s;,"'&}\]\[]+`. The bearer pass runs first as before, replaces the token with [REDACTED]; the secret-value pass then encounters 'authorization: Bearer [REDACTED]' but its value class refuses to match '[REDACTED]' because '[' is now excluded — leaving the bearer-pass output untouched.

    Approach B (single-pass alternation — slightly larger diff but eliminates the order dependency): Replace the two-pass approach with one ordered alternation regex per CR-01 in 07-REVIEW.md lines 67-81. The authorization+bearer shape is listed first so it wins before the bare authorization alternate consumes Bearer. Remove BEARER_VALUE_PATTERN entirely and combine its logic into SECRET_VALUE_PATTERN with a capture group + replacement function.

    Whichever approach is chosen:
    - Update the doc comment at lines 38-46 to describe the new behavior (no more two-pass narrative if Approach B; clarify the bracket-exclusion intent if Approach A).
    - Preserve the REDACT regex at line 17 (key-name redaction in objects — unrelated to CR-01).
    - Preserve the case-insensitive flag and word-boundary anchors.

    Then add a fourth describe() block to tests/shared/logger.test.ts titled 'Phase 07 logger Category 4 — CR-01 double-replacement regression' with the three tests defined in <behavior>. Use the existing captured() helper and warnSpy pattern. Pass the secret string as the second arg ctx (the slot redaction runs on, per the existing Category 2 convention documented at lines 75-78 of the test file).

    Run the full logger test file plus the AI suite to confirm zero regression: `npx vitest run tests/shared/logger.test.ts tests/ai/`.
  </action>
  <verify>
    <automated>npx vitest run tests/shared/logger.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/shared/logger.test.ts passes (all 4 categories) — exit 0.
    - tests/logger-redact.test.ts (v1.0 file, if present) still passes if it exists in the repo.
    - Fixture assertion: redacting 'Authorization: Bearer sk-proj-abcdef' produces output containing '[REDACTED]', NOT containing 'sk-proj-abcdef', and NOT containing the substring '=[REDACTED] [REDACTED]'.
    - grep -n "BEARER_VALUE_PATTERN\|SECRET_VALUE_PATTERN" src/shared/logger.ts shows the chosen approach (single pattern OR two patterns where the value class excludes '[').
    - The full AI suite still passes: `npx vitest run tests/ai/` exits 0 with no new failures.
  </acceptance_criteria>
  <done>Authorization: Bearer headers redact to a clean form; the four logger test categories all pass; no AI test regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix CR-02 empty-baseUrl guards in probeCustom, probeOllama, and testActiveAIConnection</name>
  <files>src/ai/providers/openaiCompatible.ts, src/ai/providers/ollama.ts, src/main.ts, tests/ai/probes.test.ts</files>
  <read_first>
    - src/ai/providers/openaiCompatible.ts (current probeCustom at lines 59-85; confirm cfg.baseUrl.replace usage at line 64 and fetcher call at line 67)
    - src/ai/providers/ollama.ts (probeOllama signature + fetch shape — need same defensive guard)
    - src/main.ts:767-829 (testActiveAIConnection — confirm existing apiKey guard at lines 775-782 to mirror style)
    - tests/ai/probes.test.ts (existing roll-up test — confirm mockResponse helper signature; new tests must use mock fetcher that asserts zero calls in the empty-baseUrl path)
    - tests/ai/helpers/mockProvider (or similar — find FetchFn mock pattern already in use)
  </read_first>
  <behavior>
    - Test 1 (NEW): probeCustom({apiKey:'x', baseUrl:'', model:'y', disclosureAcknowledged:true}, fetcherSpy) returns {ok:false, errorMessage:/Base URL is required/i} — the errorMessage should match a case-insensitive regex 'base url is required' or similar. fetcherSpy is a vi.fn() that the test asserts was called zero times.
    - Test 2 (NEW): probeOllama({apiKey:'', baseUrl:'', model:'y', disclosureAcknowledged:true}, fetcherSpy) returns {ok:false, errorMessage:/Base URL is required/i}. fetcherSpy called zero times.
    - Test 3 (NEW): probeCustom with a non-empty baseUrl (e.g. 'https://example.com/v1') still calls the fetcher exactly once (regression guard — the guard is empty-only, not always-on).
    - Test 4 (NEW): probeOllama with a non-empty baseUrl still calls the fetcher (regression guard).
    - testActiveAIConnection guard: when activeProvider='custom' and cfg.baseUrl='', the existing aiClient.probe path is NOT invoked; a Notice is shown with 'Enter a Base URL for Custom first.' (or matching prettyName output). The aiProbeInflight map is NOT touched. (This test may live in tests/ai/probe-debounce.test.ts or a new file at the executor's discretion — DO add it.)
  </behavior>
  <action>
    Add an empty-baseUrl early-return guard at the top of probeCustom in src/ai/providers/openaiCompatible.ts (line ~62, before the try block). The guard text must match the must_haves regex /Base URL is required/i — recommend exactly 'Base URL is required for Custom provider.' to match the verifier's suggested message. Place it as the first statement inside the function body, before the existing try { const baseUrl = cfg.baseUrl.replace(...) } block.

    Mirror the same guard at the top of probeOllama in src/ai/providers/ollama.ts. Use the message 'Base URL is required for Ollama provider.' (provider-specific text but matching the same regex). Read the existing probeOllama function shape first to confirm the right insertion point and matching ProbeResult return shape.

    Mirror the guard in src/main.ts testActiveAIConnection at lines 774-782, immediately after the existing apiKey guard. The check should be: `if ((provider === 'custom' || provider === 'ollama') && cfg.baseUrl === '') { new Notice(`Enter a Base URL for ${prettyName(provider)} first.`, 3000); return; }`. This is defense-in-depth — the probe-side guards alone close the bug, but the main.ts guard provides symmetry with the existing apiKey guards and surfaces a friendlier Notice copy ('Enter a Base URL for X first.' vs the probe's 'Base URL is required.').

    Extend tests/ai/probes.test.ts with a new describe() block titled 'CR-02 empty-baseUrl guards' covering the four behaviors above. The fetcher in these tests must be a vi.fn() that the test then asserts was called zero times in the guard path — this proves no relative-URL fetch is issued.

    Optionally add a testActiveAIConnection guard test in tests/ai/probe-debounce.test.ts or a new tests/ai/probe-baseurl-guard.test.ts. The test must mock SettingsStore + AIClient + Notice (use the existing patterns from tests/ai/clearKey.test.ts or tests/ai/probe-debounce.test.ts as a reference).

    Do NOT touch any other probe (probeOpenAI, probeOpenRouter, probeAnthropic) — those providers' baseUrls have non-empty defaults and an apiKey guard already covers their unconfigured-state path.
  </action>
  <verify>
    <automated>npx vitest run tests/ai/probes.test.ts tests/ai/probe-debounce.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "Base URL is required" src/ai/providers/openaiCompatible.ts returns a hit inside probeCustom.
    - grep -n "Base URL is required" src/ai/providers/ollama.ts returns a hit inside probeOllama.
    - grep -n "Base URL" src/main.ts returns a hit inside testActiveAIConnection (line ~780-790).
    - tests/ai/probes.test.ts exits 0 and includes the four new tests (greppable: `grep -c "CR-02\|empty-baseUrl\|baseUrl: ''" tests/ai/probes.test.ts | grep -v '^#'` >= 4).
    - The full AI suite still passes: `npx vitest run tests/ai/` exits 0.
    - Behavior assertion: `probeCustom({apiKey:'x',baseUrl:'',model:'y',disclosureAcknowledged:true}, vi.fn()) === {ok:false, errorMessage:/Base URL is required/i}` and the vi.fn was called 0 times.
  </acceptance_criteria>
  <done>Empty-baseUrl returns a clean ProbeResult with no fetcher call; testActiveAIConnection blocks the empty-baseUrl path with a Notice; existing happy-path tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix WR-01 missing await on adapter.invoke + add adapter-rejection test</name>
  <files>src/ai/AIClient.ts, tests/ai/aiClient.test.ts</files>
  <read_first>
    - src/ai/AIClient.ts (current state — confirm line 150 reads `return adapter.invoke(req)` without await; confirm the surrounding try/catch at lines 130-146)
    - tests/ai/aiClient.test.ts (existing 'invoke throws when activeAIProvider is null' test at lines 97-106 — extend the 'AIClient — invoke' describe block, do not modify the existing test)
  </read_first>
  <behavior>
    - Test 1 (NEW): When adapter.invoke rejects with `new Error('adapter-boom')`, AIClient.invoke rejects with an Error whose message contains 'adapter-boom' (the original adapter message must be preserved end-to-end). The test should mock resolveAdapter to return an adapter whose invoke rejects, then assert `await expect(client.invoke({} as never)).rejects.toThrow(/adapter-boom/)`.
    - Test 2 (NEW — optional but recommended): assert that AIClient.invoke awaits the adapter (vs returning the unawaited promise) by spying on a resolved promise's resolution timing — OR more simply, assert the rejection is observable via `rejects.toThrow` rather than via an unhandledRejection event. The simpler form is sufficient.
    - The existing 'invoke throws when activeAIProvider is null' test (line 97) MUST still pass.
  </behavior>
  <action>
    Change line 150 of src/ai/AIClient.ts from `return adapter.invoke(req);` to `return await adapter.invoke(req);`. This is a 1-character (well, 6-character) source change that fixes the contract documented in the JSDoc at lines 109-129: 'Re-throws on adapter error (mirrors LeetCodeClient.getProblemDetail) so feature-layer callers in Phase 08/09 can branch on the error type'.

    Note: the function is `async invoke(...)` so the return type is already Promise<AIResponse>; adding await does NOT change the return type. The await ensures any rejection from adapter.invoke is observable to the surrounding try/catch IF a try/catch were ever added in future work — currently the body has no outer try/catch wrapping the adapter.invoke call (only the disclosure check). The fix is therefore primarily about contract/observability: future maintainers wrapping the adapter call in a try/catch will not be silently betrayed.

    More immediately, the await affects how rejections propagate: without await, the rejection is the returned promise. With await, the rejection unwinds synchronously through any surrounding try blocks. Either way, the caller's `.catch` or `await ... .catch` observes the same rejected promise — so the runtime symptom is subtle. The test in <behavior> Test 1 confirms the rejection is observable via `rejects.toThrow` regardless.

    Add a new test inside the existing 'Phase 07 AIClient — invoke' describe block in tests/ai/aiClient.test.ts (lines 92-107). Title: 'invoke awaits adapter.invoke so adapter rejections propagate as a rejected promise with the original message preserved'. Mock resolveAdapter to return `{ probe: vi.fn(), invoke: vi.fn(async () => { throw new Error('adapter-boom'); }) }`. Configure makeMockSettings() to return a non-null activeAIProvider (the default 'openai' is fine). Assert `await expect(client.invoke({} as never)).rejects.toThrow(/adapter-boom/)`.

    Do NOT add a try/catch around the adapter.invoke call in AIClient.invoke — that would change the throw posture (which is currently re-throw, per JSDoc). The fix is JUST the await keyword.
  </action>
  <verify>
    <automated>npx vitest run tests/ai/aiClient.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "return await adapter.invoke" src/ai/AIClient.ts returns one hit on or near line 150.
    - tests/ai/aiClient.test.ts exits 0 with the new adapter-rejection test passing.
    - The existing 'invoke throws when activeAIProvider is null' test still passes.
    - Behavior assertion: `client.invoke({})` with an adapter whose invoke throws 'adapter-boom' produces a rejected promise whose .message matches /adapter-boom/.
    - The full AI suite still passes: `npx vitest run tests/ai/` exits 0.
  </acceptance_criteria>
  <done>AIClient.invoke awaits adapter.invoke; adapter rejections preserve the original error message; new test passes; existing tests untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Fix WR-02 freeze DISCLOSURE_BASE_COPY at module load + add freeze-assertion test</name>
  <files>src/ai/disclosure.ts, tests/ai/disclosure.test.ts</files>
  <read_first>
    - src/ai/disclosure.ts (current state — confirm lines 32-56 declare DISCLOSURE_BASE_COPY without Object.freeze and the comment at lines 38-41 explicitly says NOT frozen; the WR-02 fix supersedes that comment)
    - src/ai/disclosure.ts:115, 122 (modal render — iterates DISCLOSURE_BASE_COPY.willSend / neverSends directly; freeze does not change render correctness, just immutability)
    - tests/ai/disclosure.test.ts (existing tests — confirm shape assertions on willSend/neverSends still pass after freeze; freeze only forbids mutation, not iteration)
    - .planning/phases/07-ai-provider-foundation/07-PATTERNS.md Pattern 4 (the comment refers to it — read so the executor understands the trade-off being made: pattern 4 originally allowed Phase 08/09/11 to push entries; the freeze forces those phases to use Object.assign or a different extension mechanism, which is acceptable per WR-02's recommendation)
  </read_first>
  <behavior>
    - Test 1 (NEW): Object.isFrozen(DISCLOSURE_BASE_COPY) === true.
    - Test 2 (NEW): Object.isFrozen(DISCLOSURE_BASE_COPY.willSend) === true (deep freeze of the inner array).
    - Test 3 (NEW): Object.isFrozen(DISCLOSURE_BASE_COPY.neverSends) === true.
    - Test 4 (NEW — strict-mode mutation throws): `expect(() => { 'use strict'; DISCLOSURE_BASE_COPY.willSend.push('x'); }).toThrow();` (vitest tests run in strict mode by default — the push throws a TypeError on a frozen array).
    - All existing disclosure tests continue to pass — the verbatim base entries assertion (4 willSend, 4 neverSends per 07-UI-SPEC) must still be greppable.
  </behavior>
  <action>
    Add `Object.freeze(DISCLOSURE_BASE_COPY.willSend); Object.freeze(DISCLOSURE_BASE_COPY.neverSends); Object.freeze(DISCLOSURE_BASE_COPY);` immediately after the const declaration in src/ai/disclosure.ts (after line 56). This is a deep freeze: the outer object AND both inner arrays are frozen. Order matters — freeze the inner arrays before the outer object.

    Update the doc comment at lines 32-42 to reflect the new posture. The current comment ('NOT Object.freeze\'d intentionally — the downstream phases mutate willSend to add their own line') is now wrong. Replace with a comment explaining the WR-02 mitigation: Phase 08/09/11 must extend the disclosure copy via composition (e.g. constructing a new object with extra entries and passing it as a constructor arg) rather than via mutation — and reference 07-VERIFICATION.md WR-02 in the inline comment so future developers understand the constraint.

    Modal render at lines 113-124 does NOT need to change — iteration of a frozen array works identically. The freeze just prevents mid-render mutation (defense-in-depth per WR-02's verifier guidance).

    Extend tests/ai/disclosure.test.ts with a new describe() block 'WR-02 freeze regression' containing the four behaviors above. Use Object.isFrozen for tests 1-3; for test 4, wrap the mutation in a try/catch or use vitest's `expect(...).toThrow()` directly.

    DO NOT modify the verbatim base entries — the 4-item willSend list and 4-item neverSends list at lines 44-55 must stay byte-identical (locked verbatim per 07-UI-SPEC).
  </action>
  <verify>
    <automated>npx vitest run tests/ai/disclosure.test.ts</automated>
    <automated>npx vitest run tests/ai/ tests/shared/logger.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "Object.freeze" src/ai/disclosure.ts returns at least 3 hits (outer object + two inner arrays).
    - tests/ai/disclosure.test.ts exits 0 with all new freeze assertions passing.
    - Object.isFrozen(DISCLOSURE_BASE_COPY) === true at runtime.
    - Object.isFrozen(DISCLOSURE_BASE_COPY.willSend) === true.
    - Object.isFrozen(DISCLOSURE_BASE_COPY.neverSends) === true.
    - The verbatim base entries are unchanged: grep -c "Problem text\|Your \`## Code\`\|last run/submit verdict\|Optionally your \`## Notes\`" src/ai/disclosure.ts returns 4 (one per locked willSend bullet); same for the 4 neverSends bullets.
    - The full AI suite still passes: `npx vitest run tests/ai/` exits 0.
  </acceptance_criteria>
  <done>DISCLOSURE_BASE_COPY frozen at module load; mutation attempts throw in strict mode; existing disclosure tests untouched; verbatim copy preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → Settings → ProviderConfig.baseUrl | Empty-string baseUrl crosses into probeCustom/probeOllama and was reachable on first click of Test connection. |
| Provider SDK → AIClient.invoke → caller (Phase 08+) | Adapter rejections must be observable to the caller for branching on error type. |
| Module-load DISCLOSURE_BASE_COPY → modal render | A frozen base prevents mid-render mutation hazards. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Information Disclosure | src/shared/logger.ts redactString | mitigate | CR-01 fix: single-pattern redaction OR exclude '[' from value class. Prevents malformed log output that obscures debuggability. Secret was never leaked, but the malformed `=[REDACTED] [REDACTED]` shape made parsing/scanning logs harder for ops. Tests: tests/shared/logger.test.ts Category 4 fixture asserts no double-replacement. |
| T-07-07-02 | Denial of Service | src/ai/providers/openaiCompatible.ts probeCustom | mitigate | CR-02 fix: empty-baseUrl guard returns a clean ProbeResult instead of issuing a relative-URL fetch (which on `requestUrl` likely throws or produces undefined behavior). Without the guard, Test connection could surface a confusing 404-shaped error against `app://obsidian.md/models` instead of a clear 'enter a URL first' message — bad UX, not a security incident. |
| T-07-07-03 | Tampering | src/ai/disclosure.ts DISCLOSURE_BASE_COPY | mitigate | WR-02 fix: Object.freeze prevents future-phase mutation that could race with in-flight modal renders. Phase 08/09/11 must extend via composition rather than mutation. |
| T-07-07-04 | Tampering | src/ai/AIClient.ts invoke | accept | WR-01 fix: missing await is a contract bug, not a security issue — adapter rejections are still observable as rejected promises; the await just makes the surrounding try/catch (currently absent, but reserved for future maintenance) work as documented. Disposition: accept that the underlying risk surface is small; mitigate the contract bug to prevent future Phase 08 integration confusion. |
| T-07-07-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs in this plan — all four fixes are edits to existing source files. Package legitimacy gate not applicable. |
</threat_model>

<verification>
After all 4 tasks complete:

1. Run the full test suite: `npm test` exits 0.
2. Run the AI test suite specifically: `npx vitest run tests/ai/ tests/shared/logger.test.ts` exits 0.
3. Run the LC isolation gate: `bash scripts/check-no-obsidianfetch-in-lc.sh` exits 0 (regression guard — no new obsidianFetch imports added to LC code).
4. Build the plugin: `npm run build` exits 0 with no type errors (Tasks 1-3 add behavior; Task 3 introduces `await` which is type-compatible).
5. Re-run the verifier checklist for the two FAILED truths from 07-VERIFICATION.md:
   - Truth #4 (logger redactString clean output): now passes — `node -e "const{logger}=...; ..."` runtime trace shows clean redaction.
   - Truth #8 (probeCustom empty baseUrl): now passes — `probeCustom({baseUrl:''}, vi.fn())` returns clean error without invoking the fetcher.

Behavioral spot-checks (one-liners — runnable from repo root):
- Logger: `node -e "require('ts-node/register'); const {logger}=require('./src/shared/logger'); logger.warn('test', 'Authorization: Bearer sk-proj-abc')"` — output should NOT contain `=[REDACTED] [REDACTED]` and should NOT contain 'sk-proj-abc'. (If ts-node is not available, write a temp .mjs that imports the compiled main.js — or rely on the vitest test in Task 1.)
- probeCustom: covered by tests/ai/probes.test.ts new tests in Task 2.
- AIClient.invoke: covered by tests/ai/aiClient.test.ts new test in Task 3.
- DISCLOSURE_BASE_COPY freeze: covered by tests/ai/disclosure.test.ts new tests in Task 4.
</verification>

<success_criteria>
- src/shared/logger.ts redactString produces clean output for `Authorization: Bearer <token>` (no `=` substituted for `:`, no double `[REDACTED]` token). Confirmed by tests/shared/logger.test.ts Category 4.
- src/ai/providers/openaiCompatible.ts probeCustom early-returns `{ok:false, errorMessage:/Base URL is required/i}` when cfg.baseUrl is ''. Confirmed by tests/ai/probes.test.ts new tests + zero-call assertion on fetcher mock.
- src/ai/providers/ollama.ts probeOllama early-returns the same shape. Confirmed by parallel test.
- src/main.ts testActiveAIConnection blocks empty baseUrl for custom + ollama with a Notice. Confirmed by new test (probe-debounce.test.ts or new file).
- src/ai/AIClient.ts:150 reads `return await adapter.invoke(req);`. Confirmed by grep + new test in tests/ai/aiClient.test.ts.
- src/ai/disclosure.ts DISCLOSURE_BASE_COPY (and its inner arrays) are frozen at module load. Confirmed by Object.isFrozen test + grep for `Object.freeze`.
- All 12 must-have truths from 07-VERIFICATION.md (10 previously verified + 2 previously failed) are now verified.
- `npm test` exits 0. `npx vitest run tests/ai/` exits 0. `npm run build` exits 0.
- No regression in the 157 existing AI suite tests.
- The verbatim base entries in DISCLOSURE_BASE_COPY are byte-identical (locked per 07-UI-SPEC).
- No new files outside the plan markdown — all 4 fixes are edits to existing source + test files.
</success_criteria>

<output>
Create `.planning/phases/07-ai-provider-foundation/07-07-SUMMARY.md` when done. The summary must:
- Reference the 4 fixes by their CR/WR codes (CR-01, CR-02, WR-01, WR-02).
- Note which approach was chosen for CR-01 (Approach A bracket-exclusion vs Approach B single-pattern alternation) and why.
- Confirm zero regression in the 157 existing AI suite tests.
- Confirm the original FAILED truths #4 and #8 from 07-VERIFICATION.md now pass.
- List any unexpected issues encountered (e.g. test mock helpers needed updating, type errors, etc.).
</output>
