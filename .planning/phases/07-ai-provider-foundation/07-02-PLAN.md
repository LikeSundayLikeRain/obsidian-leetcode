---
phase: 07-ai-provider-foundation
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - package.json
  - src/ai/obsidianFetch.ts
  - src/ai/AIClient.ts
  - src/ai/pricing.ts
  - src/ai/providers/index.ts
  - src/ai/providers/anthropic.ts
  - src/ai/providers/openai.ts
  - src/ai/providers/openaiCompatible.ts
  - src/ai/providers/ollama.ts
  - scripts/check-no-obsidianfetch-in-lc.sh
  - tests/ai/obsidianFetch.test.ts
  - tests/ai/lc-isolation.test.ts
  - tests/ai/aiClient.test.ts
  - tests/ai/pricing.test.ts
autonomous: true
requirements: [AIPROV-05]
tags: [ai, obsidian-fetch, ai-client, providers, bundle-size, security]
must_haves:
  truths:
    - "Production bundle stays under 500 KB CI ceiling with @ai-sdk/* statically imported (verified by bundle-size CI gate as the FIRST task of this plan, BEFORE any feature wiring)"
    - "obsidianFetch('request') bridges to requestUrl and returns a Fetch-API-compatible Response"
    - "obsidianFetch('stream') bridges to electron.net.fetch (existence verified; not actively exercised in Phase 07)"
    - "Every fetch call inside obsidianFetch sets credentials: 'omit' to prevent default-session cookie leak (T-07-02)"
    - "leetcode.com calls NEVER use obsidianFetch — enforced by both a CI grep gate AND a runtime regression test"
    - "AIClient facade exposes probe(provider), invoke(req), addCost(usd) — all three route through SettingsStore for state"
    - "Each of 5 providers (anthropic, openai, openrouter, ollama, custom) has a createModel() + probe() pair"
    - "Pricing table returns 0 for unknown models, correct rate for the four locked default models"
  artifacts:
    - path: "src/ai/obsidianFetch.ts"
      provides: "FetchFn factory with mode='stream'|'request' switch"
      contains: "credentials: 'omit'"
    - path: "src/ai/AIClient.ts"
      provides: "AIClient facade — probe(), invoke(), addCost()"
      contains: "class AIClient"
    - path: "src/ai/providers/index.ts"
      provides: "resolveAdapter(provider, cfg, fetcher) dispatch"
      contains: "switch (provider)"
    - path: "src/ai/providers/anthropic.ts"
      provides: "createAnthropicModel + probeAnthropic"
      contains: "createAnthropic"
    - path: "src/ai/providers/openai.ts"
      provides: "createOpenAIModel + probeOpenAI"
      contains: "createOpenAI"
    - path: "src/ai/providers/openaiCompatible.ts"
      provides: "createOpenAICompatibleModel + probeOpenRouter + probeCustom"
      contains: "createOpenAICompatible"
    - path: "src/ai/providers/ollama.ts"
      provides: "createOllamaModel + probeOllama (uses /api/tags)"
      contains: "/api/tags"
    - path: "src/ai/pricing.ts"
      provides: "PRICING table + estimateCostUsd(model, usage)"
      contains: "PRICING"
    - path: "scripts/check-no-obsidianfetch-in-lc.sh"
      provides: "CI grep gate enforcing AIPROV-05 isolation invariant"
      contains: "obsidianFetch"
  key_links:
    - from: "AIClient.probe(provider)"
      to: "obsidianFetch('request') + resolveAdapter(provider, cfg, fetcher).probe()"
      via: "constructor-injected SettingsStore + per-call adapter resolution"
      pattern: "obsidianFetch\\('request'\\)"
    - from: "obsidianFetch('stream')"
      to: "require('electron').net.fetch"
      via: "lazy require inside the stream branch closure"
      pattern: "require\\('electron'\\)"
    - from: "obsidianFetch (both branches)"
      to: "credentials: 'omit'"
      via: "init shallow-merge before invoking the underlying primitive"
      pattern: "credentials.*omit"
---

<objective>
Ship the seam every Phase 08+ AI feature plugs into: a single `obsidianFetch(mode)` adapter (stream | request) and an `AIClient` facade with five per-provider adapter files. Add the bundle-size CI gate verification as Wave 1 of this plan — if the bundle blows past 450 KB on first build, escalate to dynamic-import per CONTEXT decision A's escape hatch BEFORE wiring any feature surface.

Purpose: Phase 08 streaming Debug, Phase 09 AI Review, and Phase 11 KG classifier all import `AIClient` and never see a raw provider package. This plan constructs the seam, verifies it preserves the AIPROV-05 LC-isolation invariant (leetcode.com never touches obsidianFetch — enforced by grep gate + runtime test), and locks the cookie-leak mitigation (`credentials: 'omit'`).

Output: 5 new source files in `src/ai/` (excluding types.ts from Plan 07-01); 4 provider adapters under `src/ai/providers/`; pricing table; barrel + dispatch (`providers/index.ts`); CI grep gate script; 4 new test files. `package.json` adds 5 new runtime deps (`@ai-sdk/anthropic@3.0.78`, `@ai-sdk/openai@3.0.64`, `@ai-sdk/openai-compatible@2.0.47`, `ai@6.0.183`, `zod@^4.1.8`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-provider-foundation/07-CONTEXT.md
@.planning/phases/07-ai-provider-foundation/07-RESEARCH.md
@.planning/phases/07-ai-provider-foundation/07-PATTERNS.md
@.planning/phases/07-ai-provider-foundation/07-01-SUMMARY.md

@src/api/LeetCodeClient.ts
@src/api/requestUrlFetcher.ts
@src/ai/types.ts
@scripts/grep-no-vault-modify.sh
@scripts/check-bundle-size.mjs
@esbuild.config.mjs
@manifest.json
@package.json

<interfaces>
<!-- Imports the executor will use directly. -->

From src/ai/types.ts (Plan 07-01 output):
- `type AIProvider = 'anthropic' | 'openai' | 'openrouter' | 'ollama' | 'custom';`
- `interface ProviderConfig { apiKey: string; baseUrl: string; model: string; disclosureAcknowledged: boolean; }`
- `interface ProbeResult { ok: boolean; modelCount?: number | null; errorMessage?: string; }`
- `interface AIRequest {}` / `interface AIResponse {}` (placeholder; Phase 08 expands).

From src/settings/SettingsStore.ts (Plan 07-01 output):
- `getActiveAIProvider(): AIProvider | null`
- `getProviderConfig(p: AIProvider): ProviderConfig`
- `addCostLedger(usd: number): Promise<void>`

From obsidian (built-in):
- `requestUrl(params: RequestUrlParam): Promise<RequestUrlResponse>` (existing v1.0 use at src/api/requestUrlFetcher.ts:54)

From electron (host-provided, esbuild external):
- `net.fetch(input: string | Request, init?: RequestInit & { bypassCustomProtocolHandlers?: boolean }): Promise<GlobalResponse>` (Electron 25+, available in Obsidian 1.10+)

From @ai-sdk/anthropic@3.0.78:
- `createAnthropic({ apiKey, baseURL, fetch, headers? }): AnthropicProvider` — provider factory; the result is callable with a model id to return a `LanguageModel`.

From @ai-sdk/openai@3.0.64:
- `createOpenAI({ apiKey, baseURL, fetch }): OpenAIProvider`

From @ai-sdk/openai-compatible@2.0.47:
- `createOpenAICompatible({ name, apiKey, baseURL, fetch, headers? }): OpenAICompatibleProvider`

From ai@6.0.183:
- `generateText({ model, prompt, maxOutputTokens? }): Promise<{ text, usage: { inputTokens, outputTokens } }>` — used by Anthropic probe (1-token chat) and Custom-fallback probe.

</interfaces>

<canonical_decisions>
- D-A: Static imports of `@ai-sdk/*` packages. Validate bundle size <500 KB BEFORE wiring features. If >450 KB at the verification step, ESCALATE — do not silently proceed.
- D-B: Per-provider adapter files (not strategy table). One file per provider. Each exports `create{X}Model(cfg, fetcher)` + `probe{X}(cfg, fetcher)`.
- D-E: Probe matrix:
  - openai: `GET {baseUrl}/models` with `Authorization: Bearer {apiKey}`
  - openrouter: `GET {baseUrl}/models` (no auth required for public list)
  - ollama: `GET {baseHost}/api/tags` (strip trailing `/v1` from baseUrl)
  - custom: `GET {baseUrl}/models` first; on 404/405/501 fall back to 1-token chat
  - anthropic: 1-token chat completion via `generateText({ model, prompt: 'ping', maxOutputTokens: 1 })`
- D-F: `addCost(usd)` delegates to `SettingsStore.addCostLedger(usd)`. NO cap enforcement here.
- T-07-02 mitigation (CRITICAL): every fetch call inside obsidianFetch sets `credentials: 'omit'` so `electron.net.fetch` does NOT carry default-session cookies (e.g. leetcode.com cookies) to AI provider hosts. Both branches must enforce this — research labels this "Phase 07.02 MUST" (07-RESEARCH.md line 1217).
- AIPROV-05 invariant: leetcode.com NEVER goes through obsidianFetch. Enforced by `scripts/check-no-obsidianfetch-in-lc.sh` grep gate + `tests/ai/lc-isolation.test.ts` runtime test.
- Pitfall 1 (Wave 0 dep): zod@^4.1.8 MUST be in `package.json` `dependencies` (NOT `devDependencies`) — peer of all `@ai-sdk/*` packages. Without it the SDK's module-init throws.
</canonical_decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AI SDK dependencies, build, verify bundle size under 500 KB ceiling</name>
  <files>package.json</files>
  <read_first>
    - package.json (target — extend `dependencies`; do NOT touch `devDependencies` or scripts)
    - scripts/check-bundle-size.mjs (existing CI gate; reads `main.js` in repo root after `npm run build`)
    - esbuild.config.mjs (production build entry; confirms `electron` + `obsidian` external; AI SDK packages bundled)
    - .planning/phases/07-ai-provider-foundation/07-RESEARCH.md §"Standard Stack" (verified versions 2026-05-15)
    - .planning/phases/07-ai-provider-foundation/07-RESEARCH.md §"Open Questions" Q1 (bundle-delta validation gate)
    - .planning/phases/07-ai-provider-foundation/07-RESEARCH.md §"Common Pitfalls" Pitfall 1 (zod peer dep)
  </read_first>
  <action>
    Add to `package.json` `dependencies` (alphabetized; preserve existing entries):
    - `"@ai-sdk/anthropic": "3.0.78"`
    - `"@ai-sdk/openai": "3.0.64"`
    - `"@ai-sdk/openai-compatible": "2.0.47"`
    - `"ai": "6.0.183"`
    - `"zod": "^4.1.8"`

    All four AI SDK versions are EXACT pins (no caret) per RESEARCH §Standard Stack — the SDK family makes breaking changes mid-minor; pin to the verified-2026-05-15 version. `zod` uses caret per the SDK peer-range `^3.25.76 || ^4.1.8` — track upstream within the major.

    Run:
    ```
    npm install
    npm run build
    npm run check:bundle-size
    ```

    `check-bundle-size.mjs` reads `main.js` and exits 1 if >500_000 bytes, exits 0 with stderr WARN if >400_000.

    DECISION GATE — based on the bundle size reported:
    - **If size < 450_000 bytes**: proceed to Task 2 with static imports as planned.
    - **If 450_000 ≤ size < 500_000**: proceed to Task 2 with static imports, but ADD a note to `07-02-SUMMARY.md` "Bundle size landed at {N} KB — within 50 KB of ceiling. Track future SDK updates carefully; dynamic-import escape hatch (CONTEXT decision A) deferred to v1.2 backlog."
    - **If size ≥ 500_000**: STOP. Do NOT continue this plan. Surface the failure: print the actual size, the exact dependency-by-dependency size breakdown (`du -sh node_modules/@ai-sdk/* node_modules/ai node_modules/zod` for context), and HALT execution by exiting non-zero from this task. The orchestrator will trigger a replan with the dynamic-import escape hatch.

    Do NOT modify `esbuild.config.mjs`. Do NOT add or remove `external` entries. Do NOT touch `tsconfig.json`. Do NOT add eslint overrides. The existing build pipeline must accept the new deps unchanged — if it doesn't, that's a Pitfall-8 (TLA / dynamic-import warning) signal and gets surfaced in the SUMMARY for orchestrator follow-up.
  </action>
  <verify>
    <automated>npm install && npm run build && npm run check:bundle-size</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies` contains exactly the 5 new entries with the pinned versions listed in <action>.
    - `npm install` exits 0 with no peer-dependency warnings about zod.
    - `npm run build` exits 0 with no TLA / dynamic-import warnings (Pitfall 8 signal — surface if present).
    - `npm run check:bundle-size` reports `main.js: ${N} bytes (${KB} KB)` and exits 0.
    - The reported size is < 500_000 bytes. If ≥ 500_000, this task FAILS and the plan halts.
    - `node_modules/@ai-sdk/anthropic/package.json` exists and shows `"version": "3.0.78"`.
    - `node_modules/zod/package.json` exists and reports a 4.x version.
  </acceptance_criteria>
  <done>
    Five new dependencies installed at the locked versions; production bundle builds cleanly; size verified under 500 KB CI ceiling. Decision gate above resolved (proceed / annotate / halt).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build obsidianFetch(mode), pricing table, AIClient facade — the runtime core</name>
  <files>src/ai/obsidianFetch.ts, src/ai/pricing.ts, src/ai/AIClient.ts, src/ai/providers/index.ts, src/ai/providers/anthropic.ts, src/ai/providers/openai.ts, src/ai/providers/openaiCompatible.ts, src/ai/providers/ollama.ts, tests/ai/obsidianFetch.test.ts, tests/ai/aiClient.test.ts, tests/ai/pricing.test.ts</files>
  <read_first>
    - src/api/requestUrlFetcher.ts:50-93 (verbatim shape for the requestUrl→Fetch-API bridge — copy structure, drop throttle + 429 retry; AI providers handle their own rate limits)
    - src/api/LeetCodeClient.ts:42-130 (facade pattern: ctor takes SettingsStore, methods stateless, never-throw `fetchUsername` vs re-throw `getProblemDetail` pattern)
    - src/ai/types.ts (Plan 07-01 output)
    - src/settings/SettingsStore.ts (Plan 07-01 output — `getActiveAIProvider`, `getProviderConfig`, `addCostLedger`)
    - .planning/phases/07-ai-provider-foundation/07-PATTERNS.md §"src/ai/AIClient.ts" through §"src/ai/pricing.ts" (verbatim shapes)
    - .planning/phases/07-ai-provider-foundation/07-RESEARCH.md §"Code Examples" Example 1 (obsidianFetch), Example 2 (probes), Example 3 (pricing), §"Architecture Patterns" Patterns 1–3
    - .planning/phases/07-ai-provider-foundation/07-RESEARCH.md §"Security Domain" — threat #6 + #7 (`credentials: 'omit'` is mandatory)
    - manifest.json — confirm `minAppVersion: 1.10.0` (Electron 28+, `net.fetch` available)
  </read_first>
  <behavior>
    - `obsidianFetch('request')(url, init)` calls `requestUrl({ url, method, headers, body, throw: false })` and returns a Fetch-API `Response` (`new Response(res.text, { status, headers })`); `init.credentials` (or absence) is overridden to `'omit'` before the call.
    - `obsidianFetch('stream')(url, init)` calls `require('electron').net.fetch(input, { ...init, credentials: 'omit' })` and returns the resulting `Response` directly. The stream branch is exercised live only in Phase 08; in Phase 07 it must EXIST and TYPE-CHECK and pass a unit test that mocks `require('electron')`.
    - Both branches set `credentials: 'omit'` even if the caller passes `init.credentials = 'include'` — the obsidianFetch layer is the security boundary, not the provider adapter.
    - `pricing.PRICING` table contains exactly 4 entries: `'claude-haiku-4-5'`, `'gpt-5-mini'`, `'anthropic/claude-haiku-4.5'`, `'llama3.2'`.
    - `estimateCostUsd(model, { inputTokens, outputTokens })` returns 0 for unknown models; for known models returns `rate.input * inputTokens + rate.output * outputTokens`.
    - `AIClient(settings)` constructor stores settings; no eager network.
    - `AIClient.probe(provider)` resolves config + adapter, calls `adapter.probe()`. Disclosure gate is NOT in this plan (Plan 07-05 wires it). `probe()` here calls the adapter directly — Plan 07-05 will inject the gate.
    - `AIClient.invoke(req)` is shipped but Phase 07 has no caller. Method signature exists, body uses active provider via SettingsStore + `obsidianFetch('request')` (Phase 08 changes mode based on `req.stream`). Re-throws on error.
    - `AIClient.addCost(usd)` delegates to `settings.addCostLedger(usd)`.
    - `resolveAdapter(provider, cfg, fetcher)` returns `{ probe(): Promise<ProbeResult>, invoke(req): Promise<AIResponse> }` for the given provider. `switch` exhaustive across all 5 cases.
    - Each provider file exports two functions: `create{X}Model(cfg, fetcher)` and `probe{X}(cfg, fetcher)`. The `probe*` functions never throw — they return `{ ok: false, errorMessage }` on any error path (mirrors LeetCodeClient.fetchUsername never-throw posture). Errors are caught locally and converted to `errorMessage` via `extractProviderError(err).slice(0, 200)`.
    - `probeOllama(cfg, fetcher)` strips trailing `/v1` from `cfg.baseUrl` and hits `${baseHost}/api/tags`. Returns `{ ok: true, modelCount: json.models?.length ?? 0 }` on 200.
    - `probeOpenAI`, `probeOpenRouter`, `probeCustom` (success path), and the Custom-fallback `probeViaOneTokenChat` all live here. Anthropic uses `generateText({ model, prompt: 'ping', maxOutputTokens: 1 })` from `'ai'` package.
    - `extractFromJson(body)` and `extractProviderError(err)` are co-located in `src/ai/providers/index.ts` (or a shared helper module within `src/ai/`) — both per RESEARCH §Open Question 5 + §Pattern 3.
  </behavior>
  <action>
    1. Create `src/ai/obsidianFetch.ts` with EXACTLY this shape (verbatim from 07-PATTERNS §"src/ai/obsidianFetch.ts" + 07-RESEARCH §Code Examples Example 1, with `credentials: 'omit'` injected in BOTH branches):

    Header: file-level JSDoc citing AIPROV-05 invariant ("leetcode.com NEVER goes through this function — see scripts/check-no-obsidianfetch-in-lc.sh"), Pitfall 7 contract ("If your AI SDK call needs streaming, pass mode='stream'. mode='request' returns a fully-buffered response"), and the cookie-leak mitigation T-07-02 ("`credentials: 'omit'` is mandatory in both branches").

    Export: `type FetchFn = (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;` and `function obsidianFetch(mode: 'stream' | 'request'): FetchFn`.

    Stream branch:
    ```typescript
    if (mode === 'stream') {
      // eslint-disable-next-line @typescript-eslint/no-var-requires -- electron is host-provided, external in esbuild
      const { net } = require('electron') as typeof import('electron');
      return async (input, init) => {
        const safeInit: RequestInit = { ...(init ?? {}), credentials: 'omit' };
        return net.fetch(input as string | Request, safeInit);
      };
    }
    ```

    Request branch:
    ```typescript
    return async (input, init) => {
      const url = typeof input === 'string'
        ? input
        : (input instanceof URL ? input.toString() : (input as Request).url);
      // credentials: 'omit' is implicit for requestUrl (it does not carry session cookies),
      // but document the mitigation here for parity with the stream branch.
      const res = await requestUrl({
        url,
        method: (init?.method as string) ?? 'GET',
        headers: (init?.headers as Record<string, string>) ?? {},
        body: init?.body as string | undefined,
        throw: false,
      });
      return new Response(res.text, {
        status: res.status,
        statusText: '',
        headers: res.headers as HeadersInit,
      });
    };
    ```

    Do NOT add throttling, do NOT add 429 retry, do NOT add a Pitfall-8 TLA workaround. The throttle in `requestUrlFetcher.ts` is for LC ONLY — AI providers handle their own rate limits (CONTEXT deferred §"Per-provider rate-limit awareness" — Phase 08 territory). The 429 special-casing in `requestUrlFetcher.ts:62-70` is also LC-only; AI SDK normalizes provider 4xx/5xx via its own error class.

    2. Create `src/ai/pricing.ts` per 07-PATTERNS §"src/ai/pricing.ts" + 07-RESEARCH §Code Examples Example 3. Header comment must cite the four sources (Anthropic Haiku 4.5 pricing, OpenAI gpt-5-mini, OpenRouter API, Ollama=0) verified 2026-05-15 + the rot caveat. The PRICING object literal, in order, is:

    ```typescript
    'claude-haiku-4-5':            { input: 1e-6, output: 5e-6 },
    'gpt-5-mini':                  { input: 0.25e-6, output: 2e-6 },
    'anthropic/claude-haiku-4.5':  { input: 1e-6, output: 5e-6 },
    'llama3.2':                    { input: 0, output: 0 },
    ```

    `estimateCostUsd(model, usage)`: returns 0 for unknown models (the safe-default per CONTEXT decision F's "Phase 09 polishes" wording — unknown means "we can't price, so don't accumulate fake numbers").

    3. Create the four provider files in `src/ai/providers/`. Each follows the 07-PATTERNS §"src/ai/providers/{...}.ts" recipe:

    `anthropic.ts` exports `createAnthropicModel(cfg: ProviderConfig, fetcher: FetchFn)` that calls `createAnthropic({ apiKey: cfg.apiKey, baseURL: cfg.baseUrl, fetch: fetcher })` and returns `provider(cfg.model || 'claude-haiku-4-5')`. Plus `probeAnthropic(cfg, fetcher)` that calls `generateText({ model: createAnthropicModel(cfg, fetcher), prompt: 'ping', maxOutputTokens: 1 })` inside a try/catch, returning `{ ok: true, modelCount: null }` on success and `{ ok: false, errorMessage: extractProviderError(err).slice(0, 200) }` on error.

    `openai.ts` exports `createOpenAIModel(cfg, fetcher)` calling `createOpenAI(...)`. `probeOpenAI(cfg, fetcher)` does `GET ${cfg.baseUrl.replace(/\/$/,'')}/models` with `Authorization: Bearer ${cfg.apiKey}` header. On 200: parse JSON, return `{ ok: true, modelCount: Array.isArray(json.data) ? json.data.length : null }`. On non-OK: `await res.text()`, return `{ ok: false, errorMessage: extractFromJson(body).slice(0, 200) }`.

    `openaiCompatible.ts` exports `createOpenAICompatibleModel(cfg, fetcher, name = 'custom')` calling `createOpenAICompatible({ name, apiKey: cfg.apiKey || 'placeholder', baseURL: cfg.baseUrl, fetch: fetcher })`. Plus `probeOpenRouter(cfg, fetcher)` (same shape as OpenAI but no Bearer required for OpenRouter's public list — header omitted) and `probeCustom(cfg, fetcher)` that does `GET /models` first; on 404/405/501 falls back to `probeViaOneTokenChat(cfg, fetcher)` (a shared helper that uses `generateText` with a placeholder OpenAI-compatible model).

    `ollama.ts` exports `createOllamaModel(cfg, fetcher)` calling `createOpenAICompatible({ name: 'ollama', apiKey: cfg.apiKey || 'ollama', baseURL: cfg.baseUrl, fetch: fetcher })`. `probeOllama(cfg, fetcher)`: `const baseHost = cfg.baseUrl.replace(/\/v1\/?$/, ''); const url = ${baseHost}/api/tags;` — `GET url`. Returns `{ ok: true, modelCount: json.models?.length ?? 0 }`. On non-OK: `{ ok: false, errorMessage: 'Ollama not reachable (HTTP ${status})' }`. On thrown network error: `{ ok: false, errorMessage: 'Ollama not reachable on this host' }`.

    4. Create `src/ai/providers/index.ts` as the barrel + dispatch:
    - Re-export each provider file's two functions.
    - Export shared helpers `extractFromJson(body: string): string` and `extractProviderError(err: unknown): string` per 07-PATTERNS §"Error Handling" (verbatim).
    - Export `resolveAdapter(provider: AIProvider, cfg: ProviderConfig, fetcher: FetchFn)` with an exhaustive `switch` mirroring the LeetCodeClient.ts:159-181 isSessionExpired overload-dispatch posture. Each branch returns `{ probe: () => Promise<ProbeResult>, invoke: (req: AIRequest) => Promise<AIResponse> }`. For Phase 07, `invoke` may delegate to the AI SDK's `generateText` with a stub body OR throw `new Error('AIClient.invoke: Phase 08 wires the real call')` — either is acceptable; Phase 08 will replace.

    5. Create `src/ai/AIClient.ts` per 07-PATTERNS §"src/ai/AIClient.ts" verbatim shape. Methods:
    - `probe(provider: AIProvider): Promise<ProbeResult>` — fetches `cfg = settings.getProviderConfig(provider)`, resolves adapter with `obsidianFetch('request')` as fetcher, returns `adapter.probe()`. Never throws.
    - `invoke(req: AIRequest): Promise<AIResponse>` — `provider = settings.getActiveAIProvider()`. If null, throw `new Error('No AI provider configured')`. Resolves adapter with `obsidianFetch(req.stream ? 'stream' : 'request')` (the AIRequest interface from 07-01 is empty for now — type-cast `(req as { stream?: boolean })` is acceptable; Phase 08 expands the type). Re-throws on error (matches LeetCodeClient.getProblemDetail).
    - `addCost(usd: number): Promise<void>` — `await this.settings.addCostLedger(usd)`.

    6. Create three test files under `tests/ai/`:

    `tests/ai/obsidianFetch.test.ts`:
    - `'request mode bridges requestUrl and returns a Response'` — mock `obsidian` module's `requestUrl` to resolve `{ status: 200, text: '{"hello": 1}', headers: { 'content-type': 'application/json' } }`; call `obsidianFetch('request')('https://example.com/x', { method: 'POST', body: 'hi' })`; assert returned object is a `Response`, `await res.json()` returns `{ hello: 1 }`, `res.status === 200`.
    - `'request mode forces credentials: \'omit\''` — even though requestUrl ignores credentials, document this in a comment AND assert the underlying requestUrl call was invoked WITHOUT a `credentials` field leaking through (irrelevant for requestUrl which doesn't accept it; assertion is "no `credentials` key on the requestUrl call params").
    - `'stream mode delegates to electron.net.fetch with credentials: omit'` — mock `require('electron')` via `vi.mock('electron', () => ({ net: { fetch: vi.fn().mockResolvedValue(new Response('streamed')) } }))`; call `obsidianFetch('stream')('https://example.com', { method: 'GET', credentials: 'include' })`; assert the mock fetch was called with init that has `credentials: 'omit'` (overriding the caller's `'include'`).
    - `'request mode passes through non-OK status codes'` — mock requestUrl returning `{ status: 401, text: '{"error":"unauthorized"}' }`; call `obsidianFetch('request')`; assert `res.status === 401` AND `res.ok === false` AND no exception thrown.
    - `'request mode resolves URL from input variants'` — assert the bridge handles `string`, `URL` instance, and `Request` instance correctly.

    `tests/ai/aiClient.test.ts`:
    - `'probe(provider) routes through resolveAdapter with request-mode fetcher'` — construct AIClient with a mock SettingsStore returning a fixed ProviderConfig for 'openai'; mock `resolveAdapter` (via `vi.doMock('../../src/ai/providers')`); call `client.probe('openai')`; assert resolveAdapter was called with `('openai', cfg, fetcherInstance)` where `fetcherInstance` is a function (the obsidianFetch('request') return).
    - `'invoke throws when activeAIProvider is null'` — mock SettingsStore returning null active provider; call `await expect(client.invoke({})).rejects.toThrow(/No AI provider configured/)`.
    - `'addCost delegates to settings.addCostLedger'` — spy on `settings.addCostLedger`; call `client.addCost(0.5)`; assert spy called once with 0.5.
    - `'probe never throws even when adapter throws'` — make resolveAdapter return an adapter whose `probe()` throws; call `client.probe('anthropic')`; assert returned `{ ok: false, errorMessage: <truncated> }` (not a thrown promise rejection). NOTE: this test asserts AIClient's wrapper guarantees the never-throw contract; if AIClient relies on the adapter to never throw, ensure the adapter contract is also tested in step 7 below.

    `tests/ai/pricing.test.ts`:
    - `'returns 0 for unknown model'` — `estimateCostUsd('unknown-model', { inputTokens: 100, outputTokens: 100 }) === 0`.
    - `'computes cost for claude-haiku-4-5'` — `estimateCostUsd('claude-haiku-4-5', { inputTokens: 1_000_000, outputTokens: 1_000_000 }) === 6.0` (1.0 + 5.0).
    - `'computes cost for gpt-5-mini'` — `estimateCostUsd('gpt-5-mini', { inputTokens: 1_000_000, outputTokens: 1_000_000 }) === 2.25` (0.25 + 2.0).
    - `'returns 0 for ollama llama3.2'` — both rates are 0.
    - `'OpenRouter slug uses dot not dash'` — `expect(PRICING['anthropic/claude-haiku-4.5']).toBeDefined()` AND `expect(PRICING['anthropic/claude-haiku-4-5']).toBeUndefined()` (regression on RESEARCH Assumption A4).

    7. Use `tests/ai/helpers/mockProvider.ts` (Plan 07-01 output) — `makeFetcherMock()` and `mockResponse(body, init?)` are the canonical mock helpers. Do NOT add new helpers in this plan; if you need additional fixtures, append to `mockProvider.ts`.

    Do NOT wire the disclosure gate (Plan 07-05). Do NOT touch `src/main.ts` (Plan 07-03 + Plan 07-06). Do NOT touch SettingsTab.ts (Plan 07-03). Do NOT add the per-provider probe matrix tests beyond pricing/aiClient/obsidianFetch — those live in Plan 07-04.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/ai/obsidianFetch.test.ts tests/ai/aiClient.test.ts tests/ai/pricing.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - All 9 source files under `src/ai/` exist (types.ts from Plan 07-01 + obsidianFetch.ts + AIClient.ts + pricing.ts + providers/index.ts + providers/{anthropic,openai,openaiCompatible,ollama}.ts).
    - `grep -c "credentials: 'omit'" src/ai/obsidianFetch.ts` returns >= 2 (one per branch).
    - `grep -c "require('electron')" src/ai/obsidianFetch.ts` returns 1 (only inside the stream branch).
    - `grep -c "createAnthropic\b" src/ai/providers/anthropic.ts` returns 1.
    - `grep -c "createOpenAI\b" src/ai/providers/openai.ts` returns 1.
    - `grep -c "createOpenAICompatible" src/ai/providers/openaiCompatible.ts` returns 1.
    - `grep -c "/api/tags" src/ai/providers/ollama.ts` returns 1.
    - `grep -c "switch (provider)" src/ai/providers/index.ts` returns 1.
    - `grep -c "extractFromJson\|extractProviderError" src/ai/providers/index.ts` returns >= 2 (both helpers exported).
    - `grep -c "claude-haiku-4-5" src/ai/pricing.ts` returns 1.
    - `grep -c "anthropic/claude-haiku-4.5" src/ai/pricing.ts` returns 1 (DOT, regression on RESEARCH A4).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run tests/ai/obsidianFetch.test.ts tests/ai/aiClient.test.ts tests/ai/pricing.test.ts -x` reports >= 13 passing tests (5 obsidianFetch + 4 aiClient + 4 pricing — adjust to the exact count above).
  </acceptance_criteria>
  <done>
    obsidianFetch + AIClient + 4 providers + pricing all compile and unit-test green; cookie-leak mitigation present in both branches; pricing dot-vs-dash regression locked.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ship LC-isolation regression — grep gate + runtime test for AIPROV-05 invariant</name>
  <files>scripts/check-no-obsidianfetch-in-lc.sh, tests/ai/lc-isolation.test.ts, package.json</files>
  <read_first>
    - scripts/grep-no-vault-modify.sh (existing analog grep gate — same shape, same shebang, same `set -e`)
    - .planning/phases/07-ai-provider-foundation/07-PATTERNS.md §"scripts/check-no-obsidianfetch-in-lc.sh"
    - .planning/phases/07-ai-provider-foundation/07-RESEARCH.md §"Validation Architecture" §"Regression Test Spec — leetcode.com still uses requestUrl"
    - package.json (target — extend `scripts.lint` chain to include the new gate)
    - src/api/LeetCodeClient.ts, src/api/requestUrlFetcher.ts (verify these files do NOT import obsidianFetch — the gate exists to prevent regression)
  </read_first>
  <behavior>
    - `scripts/check-no-obsidianfetch-in-lc.sh` exits 1 with a clear error message if any file under `src/api/`, `src/auth/`, `src/browse/`, `src/notes/`, `src/solve/`, `src/graph/`, `src/preview/` contains the substring `obsidianFetch`.
    - The script exits 0 silently on clean state (no LC-side imports of obsidianFetch).
    - `package.json` `scripts.lint` (or a sibling chain) invokes the new script — wired into the existing CI pipeline.
    - `tests/ai/lc-isolation.test.ts` provides a runtime layer-2 regression: spies on the obsidianFetch module export and asserts that exercising the LC public API surface (constructor + a representative method like `fetchUsername` if mockable, or just module load) does NOT call obsidianFetch.
  </behavior>
  <action>
    1. Create `scripts/check-no-obsidianfetch-in-lc.sh` per 07-PATTERNS §"scripts/check-no-obsidianfetch-in-lc.sh" verbatim shape:

    ```bash
    #!/usr/bin/env bash
    # Phase 07 AIPROV-05 invariant: leetcode.com calls NEVER use obsidianFetch.
    # Fails CI if any non-AI source file imports the AI fetch adapter.
    set -e
    if grep -rn "obsidianFetch" src/api/ src/auth/ src/browse/ src/notes/ src/solve/ src/graph/ src/preview/ 2>/dev/null; then
      echo "ERROR: obsidianFetch is for AI calls only — leetcode.com paths must use requestUrl."
      exit 1
    fi
    exit 0
    ```

    `chmod +x` the file. The grep is recursive and explicitly enumerates the LC-side directories — do NOT use `src/` wildcard (would match `src/ai/` itself and false-positive). The existing `scripts/grep-no-vault-modify.sh` is the structural analog.

    2. Extend `package.json` `scripts`:
    - Add: `"check:lc-isolation": "bash scripts/check-no-obsidianfetch-in-lc.sh"`.
    - Modify `scripts.lint` to chain the new check, e.g. `"lint": "<existing> && npm run check:lc-isolation"` if `lint` is a single command, or add the new check as a separate script invoked in CI. Inspect the existing `scripts.lint` value first; if it's already a chain, append; if it's a single eslint invocation, leave eslint as-is and add `"prelint": "npm run check:lc-isolation"` so the gate runs BEFORE eslint and fails fast.

    Whichever approach you choose, document it in the SUMMARY: "LC-isolation gate wired into <pipeline> at <pre|post>-lint hook."

    3. Create `tests/ai/lc-isolation.test.ts` per 07-RESEARCH §Regression Test Spec layer-2:

    Test 1: `'LeetCodeClient module does not statically import obsidianFetch'` — read `src/api/LeetCodeClient.ts` from disk via `fs.readFileSync` (vitest can use Node fs); assert the file content does NOT contain the string `'obsidianFetch'` AND does NOT contain `'../ai/obsidianFetch'`. This is a static-text test, not a module-load test, so it runs in isolation without spinning up the full plugin.

    Test 2: `'requestUrlFetcher module does not statically import obsidianFetch'` — same pattern for `src/api/requestUrlFetcher.ts`.

    Test 3: `'No file under src/api/, src/auth/, src/browse/, src/notes/, src/solve/, src/graph/, src/preview/ imports obsidianFetch'` — use `fs.readdirSync` recursively across all 7 directories, assert no `.ts` file contains `'obsidianFetch'` (mirrors what the bash script enforces, but the runtime test ensures the bash gate hasn't been silently disabled or skipped in CI). Walk recursively with a small helper; skip `*.test.ts` and `*.d.ts` files.

    Test 4: `'src/ai/obsidianFetch.ts is the only file that defines obsidianFetch'` — assert exactly one `export function obsidianFetch` exists across the entire `src/` tree.

    Use Node's `fs.promises.readdir` with `withFileTypes` for the recursive walk; the test should complete in < 200 ms even on a cold cache.
  </action>
  <verify>
    <automated>chmod +x scripts/check-no-obsidianfetch-in-lc.sh && bash scripts/check-no-obsidianfetch-in-lc.sh && npm run check:lc-isolation && npx vitest run tests/ai/lc-isolation.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-no-obsidianfetch-in-lc.sh` exists and is executable (`-rwxr-xr-x`).
    - Running `bash scripts/check-no-obsidianfetch-in-lc.sh` exits 0 on the clean post-Plan-07-02 tree (no LC-side file imports obsidianFetch).
    - Running `bash scripts/check-no-obsidianfetch-in-lc.sh` after artificially adding `import { obsidianFetch } from '../ai/obsidianFetch';` to `src/api/LeetCodeClient.ts` (then reverting) exits 1 with the locked error message — verify by running once with the temp edit, confirming exit 1, then reverting. **Optional smoke-test only — do NOT commit the temp edit.**
    - `package.json` `scripts.check:lc-isolation` exists.
    - `npm run check:lc-isolation` exits 0.
    - `npx vitest run tests/ai/lc-isolation.test.ts -x` reports 4 passing tests.
    - `npm run lint` exits 0 (the new gate runs as part of the lint chain).
  </acceptance_criteria>
  <done>
    Two-layer regression in place: bash grep gate wired into npm-scripts lint chain, runtime fs-walk test asserts the same invariant. AIPROV-05 isolation proven and CI-enforced.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin renderer → AI provider host (cross-host HTTP) | All AI HTTP exits the plugin via `obsidianFetch` (one of two branches). The `init` object may carry default-session state (e.g. cookies for unrelated hosts); the boundary MUST strip credentials. |
| obsidianFetch → leetcode.com (must NEVER occur) | The AIPROV-05 invariant — leetcode.com is on a separate HTTP path (`requestUrl` direct + `installRequestUrlFetcher` shim). Any future regression that funnels LC traffic through obsidianFetch is a security AND correctness violation. |
| `electron.net.fetch` default-session pool → AI provider host | `net.fetch` honors default-session cookies unless `credentials: 'omit'` is set. Default-session may contain leetcode.com cookies from other tabs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Information Disclosure | `obsidianFetch('stream')` (electron.net.fetch path) | mitigate | Both branches set `credentials: 'omit'` BEFORE invoking the underlying primitive. The stream branch overrides `init.credentials` even if the caller passes `'include'`. Test in `tests/ai/obsidianFetch.test.ts` asserts the override. RESEARCH §Security Domain threat #6 + #7 — mandatory. |
| T-07-AIPROV-05 | Tampering / Information Disclosure | LC code paths (src/api/, src/auth/, src/browse/, etc) | mitigate | Two-layer regression: (a) `scripts/check-no-obsidianfetch-in-lc.sh` greps 7 LC-side directories for the `obsidianFetch` token, fails CI on hit; (b) `tests/ai/lc-isolation.test.ts` performs the same assertion at vitest runtime. Wired into `npm run lint` chain. |
| T-07-bundle | Availability / Compliance | Production bundle vs 500 KB CI ceiling | mitigate | Task 1 verifies bundle BEFORE any feature wiring. Decision gate: <450 KB proceed silently, 450–500 KB proceed + annotate, ≥500 KB HALT and escalate to dynamic-import escape hatch. Prevents a "ship-then-discover" bundle blowout. |
| T-07-pitfall-1 | Availability | `@ai-sdk/*` packages requiring `zod` peer dep | mitigate | `zod@^4.1.8` added to `package.json` `dependencies` (NOT `devDependencies`) in Task 1. Without this the SDK throws on module load. RESEARCH Pitfall 1 — verified via `npm view @ai-sdk/anthropic peerDependencies` 2026-05-15. |

</threat_model>

<verification>
Run after all tasks complete:

```bash
# Type-check (Tasks 1 + 2 + 3 must compile together with Plan 07-01)
npx tsc --noEmit

# Phase 07 Wave 2 test suite — Tasks 2 + 3 test files
npx vitest run tests/ai/obsidianFetch.test.ts tests/ai/aiClient.test.ts tests/ai/pricing.test.ts tests/ai/lc-isolation.test.ts -x

# Plan 07-01 regression — must still pass
npx vitest run tests/ai/types.test.ts tests/ai/settingsStore.test.ts tests/shared/logger.test.ts -x

# v1.0 regression — existing api + settings + shared suites must still be green
npx vitest run tests/api/ tests/settings/ tests/shared/ -x

# AIPROV-05 isolation invariant — bash grep gate
npm run check:lc-isolation

# Lint — full chain (includes the new check)
npm run lint

# Build — CI bundle gate
npm run build && npm run check:bundle-size
```

All commands exit 0. Bundle size < 500 KB (verified <450 KB ideally; 450–500 KB acceptable with SUMMARY annotation).
</verification>

<success_criteria>

This plan is complete when:

1. Production bundle builds, size verified under 500 KB ceiling (Task 1 decision gate resolved).
2. `obsidianFetch(mode)` exists with both branches working; `credentials: 'omit'` enforced in both; tests green.
3. `AIClient` exposes `probe`, `invoke`, `addCost`; constructor takes `SettingsStore`; tests green.
4. Five provider files exist (anthropic, openai, openaiCompatible — covering OpenRouter + Custom — ollama, plus the index barrel/dispatch); each exports `create*Model` + `probe*`.
5. Pricing table contains exactly the 4 locked default models; `estimateCostUsd` returns 0 for unknowns.
6. AIPROV-05 isolation invariant enforced by both bash script + runtime test; wired into `npm run lint`.
7. All Plan 07-02 + Plan 07-01 tests pass; v1.0 regression suites stay green.
8. `tests/ai/lc-isolation.test.ts` reports 4 passing tests.

</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-provider-foundation/07-02-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Capture: bundle size landed at ${N} KB / ${headroom} KB headroom, dynamic-import status (deferred / triggered), pricing dot-vs-dash regression locked, the four-test LC-isolation count, the cookie-leak mitigation locations (both obsidianFetch branches), and gates downstream (Plan 07-03 may now wire AIClient into onload + Settings UI; Plan 07-04 may now use AIClient.probe; Plan 07-05 may now wrap probe/invoke with disclosure gate).
</output>
