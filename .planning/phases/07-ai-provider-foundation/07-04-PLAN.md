---
phase: 07-ai-provider-foundation
plan: 04
type: execute
wave: 4
depends_on: [07-01, 07-02, 07-03]
files_modified:
  - src/main.ts
  - src/ai/types.ts
  - src/settings/SettingsTab.ts
  - tests/ai/probes.test.ts
  - tests/ai/probe-anthropic.test.ts
  - tests/ai/probe-openai.test.ts
  - tests/ai/probe-openrouter.test.ts
  - tests/ai/probe-ollama.test.ts
  - tests/ai/probe-custom-fallback.test.ts
  - tests/ai/probe-error-extraction.test.ts
  - tests/ai/probe-debounce.test.ts
autonomous: true
requirements: [AIPROV-03]
tags: [ai, test-connection, probe-matrix, debounce, notices]
must_haves:
  truths:
    - "Test connection button in Settings invokes AIClient.probe(activeProvider) with single in-flight debounce per provider"
    - "Test connection palette command 'test-ai-connection' invokes the same probe path; both surface identical Notice text"
    - "OpenAI probe sends GET {baseUrl}/models with Authorization: Bearer {apiKey}; reports model count on success"
    - "OpenRouter probe sends GET {baseUrl}/models with NO Authorization header; reports model count on success"
    - "Ollama probe sends GET {baseHost}/api/tags (baseUrl with /v1 stripped); reports model count on success"
    - "Anthropic probe sends generateText({ prompt: 'ping', maxOutputTokens: 1 }) via the SDK; reports success without model count"
    - "Custom probe tries GET {baseUrl}/models first; on 404/405/501 falls back to 1-token chat via the SDK"
    - "Probe failure surfaces vendor error message verbatim, truncated to <=200 chars total (provider prefix + vendor message combined)"
    - "Notice copy matches 07-UI-SPEC §'Notice copy' verbatim — success/failure/no-provider/empty-key all locked"
    - "Test connection button label flips to 'Testing...' while probe is in flight; button is disabled; subsequent clicks are no-ops"
  artifacts:
    - path: "src/main.ts"
      provides: "test-ai-connection palette command + LeetCodePlugin.testActiveAIConnection() method"
      contains: "test-ai-connection"
    - path: "src/settings/SettingsTab.ts"
      provides: "Test connection button onClick wired to LeetCodePlugin.testActiveAIConnection()"
      contains: "testActiveAIConnection"
    - path: "src/ai/types.ts"
      provides: "exported prettyName(provider: AIProvider): string helper (single source of truth)"
      contains: "export function prettyName"
  key_links:
    - from: "Test connection button onClick"
      to: "LeetCodePlugin.testActiveAIConnection() -> AIClient.probe(active)"
      via: "single in-flight gate (Map<AIProvider, Promise> on plugin instance)"
      pattern: "testActiveAIConnection"
    - from: "Palette command 'test-ai-connection'"
      to: "Same testActiveAIConnection() method"
      via: "addCommand callback delegates to plugin method"
      pattern: "id: 'test-ai-connection'"
    - from: "AIClient.probe failure"
      to: "Notice with `${prettyName}: ${vendorError.slice(0, 200)}`"
      via: "ProbeResult.errorMessage already-truncated by provider adapter"
      pattern: "{provider name}:"
---

<objective>
Wire Test connection end-to-end. Plan 07-03 shipped the placeholder button + AIClient + provider adapters; Plan 07-04 connects the button onClick (and the palette command) to `AIClient.probe(activeProvider)`, surfaces the per-UI-SPEC Notice copy on success/failure, and adds the single-in-flight debounce per CONTEXT decision E + RESEARCH Open Question 9.

Purpose: AIPROV-03 ("Test connection issues a tiny round-trip and reports success or error message") is the first user-facing AI behavior in v1.1. It is the validation path the user runs after pasting a key — the foundational confidence-builder before any disclosure modal or palette command matters.

Output: Plugin method `testActiveAIConnection()`; palette command `test-ai-connection`; Settings button onClick wired (replaces the Plan 07-03 placeholder); 8 new test files exhaustively covering the probe matrix + error extraction + debouncing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-provider-foundation/07-CONTEXT.md
@.planning/phases/07-ai-provider-foundation/07-RESEARCH.md
@.planning/phases/07-ai-provider-foundation/07-PATTERNS.md
@.planning/phases/07-ai-provider-foundation/07-UI-SPEC.md
@.planning/phases/07-ai-provider-foundation/07-01-SUMMARY.md
@.planning/phases/07-ai-provider-foundation/07-02-SUMMARY.md
@.planning/phases/07-ai-provider-foundation/07-03-SUMMARY.md

@src/main.ts
@src/settings/SettingsTab.ts
@src/ai/AIClient.ts
@src/ai/providers/index.ts
@src/ai/providers/anthropic.ts
@src/ai/providers/openai.ts
@src/ai/providers/openaiCompatible.ts
@src/ai/providers/ollama.ts
@src/ai/types.ts

<interfaces>
<!-- Methods + types the executor will use directly. -->

From src/ai/AIClient.ts (Plan 07-02):
- `probe(provider: AIProvider): Promise<ProbeResult>` — never throws; returns `{ ok, modelCount?, errorMessage? }`.

From src/ai/types.ts:
- `ProbeResult = { ok: boolean; modelCount?: number | null; errorMessage?: string }`. Per UI-SPEC: `modelCount === null` for Anthropic (no list endpoint); `modelCount === 0` for Ollama with zero installed models (special-cased Notice copy).

From src/main.ts:
- Existing `addCommand` patterns at lines 255-405. Callbacks are short — delegate to a plugin method.
- `this.aiClient` field (Plan 07-03 Task 1).
- `this.settings` field (existing).

From obsidian:
- `Notice(text: string, duration?: number)` — for the success / failure / guard notices.

From 07-UI-SPEC §"Notice copy":
- Success with model count: `'AI provider connection OK ({provider name}, {model count} models available)'` 4000ms
- Success Anthropic (no list): `'AI provider connection OK (Anthropic)'` 4000ms
- Success Ollama with 0 models: `'Ollama reachable, 0 models installed — run \`ollama pull llama3.2\`'` 6000ms
- Failure: `'{provider name}: {vendor error verbatim, truncated to 200 chars total}'` 6000ms
- No provider: `'Pick an AI provider first.'` 3000ms
- Empty key (Anthropic / OpenAI / OpenRouter): `'Enter an API key for {provider name} first.'` 3000ms

</interfaces>

<canonical_decisions>
- D-E: Probe matrix locked. OpenAI: GET /models + Bearer; OpenRouter: GET /models no auth; Ollama: GET /api/tags (strip /v1); Custom: GET /models then 1-tok-chat fallback on 404/405/501; Anthropic: 1-token chat only.
- 07-UI-SPEC §"Test connection — debouncing": single in-flight per provider. While running, button label is `'Testing...'`, button disabled, subsequent clicks are no-ops. NO global rate limiter, NO token bucket.
- Pitfall 6: Test connection IS an AI call subject to disclosure. Plan 07-05 wraps `probe()` with the disclosure gate — Plan 07-04 calls `aiClient.probe(provider)` directly; the gate is layered on top in 07-05 by modifying AIClient.probe's body, NOT this plan's call site.
- 07-UI-SPEC §Layout: empty-key guard for Anthropic/OpenAI/OpenRouter (Ollama allows empty; Custom allows empty when the user is using a no-auth backend). The guard fires BEFORE invoking probe, so the empty-key Notice does not consume a network call.
- AIPROV-03: "tiny round-trip ... reports success or the error message". Notice (not modal) is the result UI per 07-UI-SPEC.
</canonical_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add testActiveAIConnection() method + palette command + wire Settings button</name>
  <files>src/main.ts, src/ai/types.ts, src/settings/SettingsTab.ts, tests/ai/probe-debounce.test.ts</files>
  <read_first>
    - src/main.ts:255-259 (open-problem-browser addCommand pattern — analog for test-ai-connection)
    - src/main.ts:25 (existing import block — add AIProvider import if not yet present)
    - src/settings/SettingsTab.ts (Plan 07-03 output — locate the Test connection button placeholder containing 'Plan 07-04' string)
    - src/ai/AIClient.ts, src/ai/types.ts
    - .planning/phases/07-ai-provider-foundation/07-UI-SPEC.md §"Notice copy"
    - .planning/phases/07-ai-provider-foundation/07-UI-SPEC.md §"Test connection — debouncing"
    - .planning/phases/07-ai-provider-foundation/07-PATTERNS.md §"src/main.ts (modify)" §"Plugin-method delegation"
  </read_first>
  <behavior>
    - `LeetCodePlugin` has a private `aiProbeInflight: Map<AIProvider, Promise<ProbeResult>>` field tracking in-flight probes per provider.
    - `LeetCodePlugin.testActiveAIConnection(): Promise<void>` — public method, called from both the Settings button and the palette command.
      - Step 1: read `provider = this.settings.getActiveAIProvider()`. If null, emit `new Notice('Pick an AI provider first.', 3000)` and return.
      - Step 2: read `cfg = this.settings.getProviderConfig(provider)`. If `provider in {'anthropic', 'openai', 'openrouter'}` AND `cfg.apiKey === ''`, emit `new Notice(\`Enter an API key for \${prettyName(provider)} first.\`, 3000)` and return.
      - Step 3: if `aiProbeInflight.has(provider)`, return early (subsequent clicks during in-flight are no-ops).
      - Step 4: store the probe promise in the map: `aiProbeInflight.set(provider, this.aiClient.probe(provider))`.
      - Step 5: `await` the probe; emit Notice based on result; clear the map entry in `finally`.
    - Notice emission rules (matches 07-UI-SPEC verbatim):
      - On `ok: true, modelCount: number > 0`: `new Notice(\`AI provider connection OK (\${prettyName(provider)}, \${result.modelCount} models available)\`, 4000)`.
      - On `ok: true, modelCount: null` (Anthropic): `new Notice(\`AI provider connection OK (Anthropic)\`, 4000)`.
      - On `ok: true, modelCount: 0` AND provider === 'ollama': `new Notice('Ollama reachable, 0 models installed — run \`ollama pull llama3.2\`', 6000)`.
      - On `ok: false`: `new Notice(\`\${prettyName(provider)}: \${result.errorMessage}\`, 6000)`. Truncate the WHOLE Notice text to 200 chars per UI-SPEC §"Error state copy posture" ("provider prefix + vendor message COMBINED").
    - Palette command `test-ai-connection` exists with clean ID + name `'Test AI connection'` + `callback: () => { void this.testActiveAIConnection(); }`.
    - Settings Test connection button onClick replaces the Plan 07-03 placeholder body with: button label flips to `'Testing...'`, `b.setDisabled(true)`, await `this.plugin.testActiveAIConnection()`, button label restores to `'Test connection'`, `b.setDisabled(false)`. Wrap in try/finally.
    - The palette command path does NOT need a button-label flip; the Notice IS the feedback. The debounce map handles cross-surface concurrency (Settings button + palette).
  </behavior>
  <action>
    1. In `src/main.ts`, add to the imports: `import type { AIProvider, ProbeResult } from './ai/types';` and `import { prettyName } from './ai/types';` if not already imported. Add `Notice` to the existing `obsidian` import line if not present.

    2. Add `prettyName` to `src/ai/types.ts` (single source of truth). The locked switch:
    ```typescript
    export function prettyName(p: AIProvider): string {
      switch (p) {
        case 'anthropic': return 'Anthropic';
        case 'openai': return 'OpenAI';
        case 'openrouter': return 'OpenRouter';
        case 'ollama': return 'Ollama';
        case 'custom': return 'Custom (OpenAI-compatible)';
      }
    }
    ```
    Update `src/settings/SettingsTab.ts` (Plan 07-03 Task 2) to import `prettyName` from `'../ai/types'` instead of redeclaring locally — delete the local helper.

    3. Add the in-flight tracker as a private field on `LeetCodePlugin`:
    ```typescript
    private aiProbeInflight = new Map<AIProvider, Promise<ProbeResult>>();
    ```

    4. Add the new method `testActiveAIConnection()` to `LeetCodePlugin` (place near other plugin-action methods such as `submitFromActive`):

    ```typescript
    async testActiveAIConnection(): Promise<void> {
      const provider = this.settings.getActiveAIProvider();
      if (!provider) {
        new Notice('Pick an AI provider first.', 3000);
        return;
      }
      const cfg = this.settings.getProviderConfig(provider);
      if ((provider === 'anthropic' || provider === 'openai' || provider === 'openrouter') && cfg.apiKey === '') {
        new Notice(`Enter an API key for ${prettyName(provider)} first.`, 3000);
        return;
      }
      if (this.aiProbeInflight.has(provider)) {
        return;
      }
      const probePromise = this.aiClient.probe(provider);
      this.aiProbeInflight.set(provider, probePromise);
      try {
        const result = await probePromise;
        if (result.ok) {
          if (result.modelCount === null) {
            new Notice('AI provider connection OK (Anthropic)', 4000);
          } else if (result.modelCount === 0 && provider === 'ollama') {
            new Notice('Ollama reachable, 0 models installed — run `ollama pull llama3.2`', 6000);
          } else {
            new Notice(`AI provider connection OK (${prettyName(provider)}, ${result.modelCount ?? 0} models available)`, 4000);
          }
        } else {
          const combined = `${prettyName(provider)}: ${result.errorMessage ?? 'unknown error'}`;
          new Notice(combined.slice(0, 200), 6000);
        }
      } finally {
        this.aiProbeInflight.delete(provider);
      }
    }
    ```

    5. Add the palette command inside `onload()` (place near the existing `addCommand` block at line 255+; after `open-in-preview` is reasonable):

    ```typescript
    this.addCommand({
      id: 'test-ai-connection',
      name: 'Test AI connection',
      callback: () => { void this.testActiveAIConnection(); },
    });
    ```

    No `hotkeys` field. ID has no `obsidian`, no `leetcode`, no `command` substring — passes `eslint-plugin-obsidianmd@0.3.0` rules.

    6. In `src/settings/SettingsTab.ts`, locate the Test connection button placeholder (the line containing `'Plan 07-04'`). Replace its onClick body with:

    ```typescript
    .onClick(async () => {
      b.setButtonText('Testing...');
      b.setDisabled(true);
      try {
        await this.plugin.testActiveAIConnection();
      } finally {
        b.setButtonText('Test connection');
        b.setDisabled(false);
      }
    })
    ```

    Verify against the existing `Save cookies` button at SettingsTab.ts:123-144 — the Obsidian `ButtonComponent` API exposes `setButtonText(string)` and `setDisabled(boolean)`.

    7. Create `tests/ai/probe-debounce.test.ts` with these test cases (vitest + minimal LeetCodePlugin mock):

    - `'no active provider emits Pick-an-AI-provider Notice'` — call `testActiveAIConnection()` with mock SettingsStore returning null; spy on Notice; assert single Notice with text 'Pick an AI provider first.' and 3000ms.
    - `'empty key for Anthropic emits enter-key Notice'` — set active to anthropic, apiKey ''; assert Notice 'Enter an API key for Anthropic first.' 3000ms; aiClient.probe NOT called.
    - `'empty key for OpenAI emits enter-key Notice'` — same shape for openai.
    - `'empty key for OpenRouter emits enter-key Notice'` — same shape for openrouter.
    - `'empty key for Ollama does NOT block — probe runs'` — set active to ollama, apiKey ''; aiClient.probe('ollama') IS called.
    - `'empty key for Custom does NOT block — probe runs'` — same for custom.
    - `'concurrent calls deduplicate via inflight map'` — mock aiClient.probe to return a promise that resolves after 50ms; call testActiveAIConnection() twice in quick succession; assert aiClient.probe called exactly once.
    - `'inflight map is cleared after probe resolves'` — same mock; await first call; call again immediately; assert aiClient.probe called twice (second call goes through after first finishes).
    - `'success Anthropic emits AI provider connection OK (Anthropic) 4000ms'` — mock probe resolves to `{ ok: true, modelCount: null }`; assert Notice text and duration.
    - `'success OpenAI with 5 models emits OK with model count'` — mock returns `{ ok: true, modelCount: 5 }`; assert Notice text contains '(OpenAI, 5 models available)'.
    - `'Ollama with 0 models emits special pull-suggestion Notice'` — set active 'ollama'; mock returns `{ ok: true, modelCount: 0 }`; assert Notice contains 'ollama pull llama3.2' and duration 6000ms.
    - `'failure Notice combines prefix and vendor message, truncated to 200 chars'` — mock returns `{ ok: false, errorMessage: 'A'.repeat(300) }`; provider 'openai'; assert Notice text length === 200 AND starts with 'OpenAI: '.

    Use `vi.mock('obsidian', () => ({ Notice: vi.fn() }))` to capture Notice instances + args. The minimal `LeetCodePlugin` mock exposes `settings`, `aiClient`, `aiProbeInflight`, the `testActiveAIConnection` method (call via `LeetCodePlugin.prototype.testActiveAIConnection.call(mockInstance)`).

    Do NOT touch the disclosure gate (Plan 07-05). Do NOT add the `clear-ai-key` command (Plan 07-06). Do NOT modify provider adapter probe functions (Plan 07-02 already shipped them).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/ai/probe-debounce.test.ts -x && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "id: 'test-ai-connection'" src/main.ts` returns 1.
    - `grep -c "name: 'Test AI connection'" src/main.ts` returns 1.
    - `grep -c "testActiveAIConnection" src/main.ts` returns >= 2 (declaration + addCommand callback).
    - `grep -c "testActiveAIConnection" src/settings/SettingsTab.ts` returns 1 (button onClick).
    - `grep -c "Plan 07-04" src/settings/SettingsTab.ts` returns 0 (placeholder string removed).
    - `grep -c "aiProbeInflight" src/main.ts` returns >= 4 (field declaration + Map operations: .has, .set, .delete).
    - `grep -c "export function prettyName" src/ai/types.ts` returns 1.
    - `grep -v '^//' src/settings/SettingsTab.ts | grep -c "function prettyName"` returns 0 (local redeclaration deleted).
    - `grep -c "Pick an AI provider first" src/main.ts` returns 1.
    - `grep -c "Enter an API key for" src/main.ts` returns 1.
    - `grep -c "AI provider connection OK" src/main.ts` returns >= 2 (Anthropic-specific + general branches).
    - `grep -c "ollama pull llama3.2" src/main.ts` returns 1.
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run tests/ai/probe-debounce.test.ts -x` reports 12 passing tests.
    - `npm run lint` exits 0 (clean command ID rules pass).
    - `npm run check:lc-isolation` exits 0.
  </acceptance_criteria>
  <done>
    Test connection button + palette command both invoke testActiveAIConnection(); the method debounces per provider; Notice copy matches UI-SPEC verbatim; the placeholder is replaced.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-provider probe matrix tests — assert each adapter sends the right HTTP shape</name>
  <files>tests/ai/probe-anthropic.test.ts, tests/ai/probe-openai.test.ts, tests/ai/probe-openrouter.test.ts, tests/ai/probe-ollama.test.ts, tests/ai/probe-custom-fallback.test.ts, tests/ai/probe-error-extraction.test.ts, tests/ai/probes.test.ts</files>
  <read_first>
    - src/ai/providers/anthropic.ts, src/ai/providers/openai.ts, src/ai/providers/openaiCompatible.ts, src/ai/providers/ollama.ts, src/ai/providers/index.ts (Plan 07-02 outputs — the functions under test)
    - tests/ai/helpers/mockProvider.ts (Plan 07-01 fixture: makeFetcherMock + mockResponse)
    - .planning/phases/07-ai-provider-foundation/07-RESEARCH.md §"Phase Requirements -> Test Map" (test inventory)
    - .planning/phases/07-ai-provider-foundation/07-RESEARCH.md §"Code Examples" Examples 2 (probe shapes)
    - .planning/phases/07-ai-provider-foundation/07-RESEARCH.md §"Common Pitfalls" Pitfall 4 (provider-specific 401/403 patterns)
  </read_first>
  <behavior>
    - Each probe function is unit-tested against a mocked `FetchFn` (the second arg). The fetcher is a `vi.fn()` returning a configurable `Response`.
    - Anthropic probe is tested against a mocked `generateText` from `'ai'` package — use `vi.mock('ai', () => ({ generateText: vi.fn() }))`.
    - For each provider, tests cover: success path, 401 invalid-key path, 5xx server-error path, network-throw path. Plus provider-specific cases:
      - OpenAI: Bearer header is sent.
      - OpenRouter: NO Bearer header sent (probe is anonymous-friendly).
      - Ollama: URL is `${baseUrl-stripped-of-/v1}/api/tags`.
      - Anthropic: generateText called with `{ model: <SDK provider model>, prompt: 'ping', maxOutputTokens: 1 }`; success returns `{ ok: true, modelCount: null }`; thrown error returns `{ ok: false, errorMessage: <truncated> }`.
      - Custom fallback: GET /models 404/405/501 -> 1-token chat; GET /models 200 -> modelCount; GET /models 500 -> errorMessage (NOT fallback).
    - Error extraction (`extractFromJson`) is tested separately.
  </behavior>
  <action>
    Create six provider-specific test files plus an integration roll-up. Use vitest + the `tests/ai/helpers/mockProvider.ts` fixtures.

    1. `tests/ai/probe-openai.test.ts`:
       - `'sends GET /v1/models with Bearer auth'` — mock fetcher resolves `mockResponse({ data: [{ id: 'gpt-5-mini' }] }, { status: 200 })`. Call `probeOpenAI({ apiKey: 'sk-test', baseUrl: 'https://api.openai.com/v1', model: 'gpt-5-mini', disclosureAcknowledged: true }, fetcher)`. Assert fetcher called once with URL `'https://api.openai.com/v1/models'` and `headers.Authorization === 'Bearer sk-test'`. Assert result `{ ok: true, modelCount: 1 }`.
       - `'returns ok with modelCount on 200 with multiple entries'`.
       - `'returns errorMessage on 401 with vendor message verbatim'` — body `{"error":{"message":"Incorrect API key provided","type":"invalid_request_error"}}`; assert errorMessage === 'Incorrect API key provided' and length <= 200.
       - `'truncates long error messages to 200 chars'`.
       - `'handles 5xx with body fallback'` — plain-text body 'Internal Server Error'; assert errorMessage contains it.
       - `'strips trailing slash from baseUrl'` — pass `baseUrl: 'https://api.openai.com/v1/'`; assert URL has no double slash.

    2. `tests/ai/probe-openrouter.test.ts`:
       - `'sends GET /api/v1/models with NO Authorization header'` — fetcher mock asserts `init.headers` does NOT have `Authorization` key (OpenRouter public list is anonymous-friendly per RESEARCH).
       - `'returns ok with modelCount on 200'`.
       - `'returns errorMessage on non-OK'`.

    3. `tests/ai/probe-ollama.test.ts`:
       - `'strips /v1 suffix and queries /api/tags'` — baseUrl `'http://localhost:11434/v1'`; assert URL `'http://localhost:11434/api/tags'`.
       - `'strips trailing slash from /v1/ suffix'` — `'http://localhost:11434/v1/'`; assert URL `'http://localhost:11434/api/tags'`.
       - `'works without /v1 suffix'` — `'http://localhost:11434'`; assert URL `'http://localhost:11434/api/tags'`.
       - `'returns modelCount from json.models[]'` — body `{ models: [{ name: 'llama3.2' }, { name: 'mistral' }] }` 200; assert `modelCount: 2`.
       - `'returns 0 modelCount when models[] missing or empty'` — body `{}`; assert `modelCount: 0`.
       - `'returns ok=false with reachability message on non-200'` — status 503; assert errorMessage contains `'Ollama not reachable (HTTP 503)'`.
       - `'returns ok=false on network throw'` — fetcher rejects; assert errorMessage contains `'Ollama not reachable on this host'`.

    4. `tests/ai/probe-anthropic.test.ts`:
       - `vi.mock('ai', () => ({ generateText: vi.fn() }))` at the top.
       - `'calls generateText with prompt ping and maxOutputTokens 1'` — generateText resolves `{ text: 'pong', usage: { inputTokens: 5, outputTokens: 1 } }`. Call `probeAnthropic({ apiKey: 'sk-ant-test', baseUrl: 'https://api.anthropic.com/v1', model: 'claude-haiku-4-5', disclosureAcknowledged: true }, fetcher)`. Assert generateText called once with `{ model: <truthy>, prompt: 'ping', maxOutputTokens: 1 }`. Assert `result === { ok: true, modelCount: null }`.
       - `'returns errorMessage on SDK throw'` — generateText rejects; assert truncated errorMessage.
       - `'truncates long SDK errors to 200 chars'`.

    5. `tests/ai/probe-custom-fallback.test.ts`:
       - `'success path: GET /models 200 returns modelCount'` — assert no fallback (generateText spy called 0 times).
       - `'fallback on 404'` — first fetcher call returns 404; mock generateText to resolve; assert generateText called exactly once. Final result `{ ok: true, modelCount: null }`.
       - `'fallback on 405 (method not allowed)'` — same.
       - `'fallback on 501 (not implemented)'` — same.
       - `'NO fallback on 500 (server error) — surfaces error verbatim'` — assert generateText called 0 times AND result has errorMessage.
       - `'NO fallback on 401 (auth error) — surfaces error verbatim'` — same.
       - `'fallback also fails — both errors surfaced via the fallback error path'` — mock GET 404 then generateText rejects; final result `{ ok: false, errorMessage: <SDK error truncated> }`.

    6. `tests/ai/probe-error-extraction.test.ts` (helper coverage):
       - `'extracts error.message from OpenAI shape'` — `{ error: { message: 'X', type: 'Y', code: 'Z' } }` -> `'X'`.
       - `'extracts error.message from Anthropic shape'` — `{ error: { type: 'authentication_error', message: 'X' } }` -> `'X'`.
       - `'falls back to error string when shape is { error: "literal" }'` — `{ error: 'literal' }` -> `'literal'`.
       - `'falls back to message field when no error wrapper'` — `{ message: 'X' }` -> `'X'`.
       - `'returns truncated raw body when JSON parse fails'` — invalid JSON; assert raw body returned, truncated at 200.
       - `'extractProviderError handles Error instances'` — `new Error('X')` -> `'X'`.
       - `'extractProviderError handles strings'` — `'literal'` -> `'literal'`.
       - `'extractProviderError handles non-Error non-string (object, null)'` -> `'Unknown error'`.

    7. `tests/ai/probes.test.ts` — thin integration roll-up. For each of the 5 providers, instantiate with valid config, call probe with a mock fetcher returning a 200 (or for Anthropic mock generateText resolving), assert the return value has `ok: true`. Keep file < 50 lines.

    All test files import from the actual source modules at `src/ai/providers/...`. Use `vi.mock` only for `'ai'` (Anthropic + Custom-fallback paths); the fetcher is always a vi.fn() argument, never a module mock — keeps tests fast and the seam clean.
  </action>
  <verify>
    <automated>npx vitest run tests/ai/probe-anthropic.test.ts tests/ai/probe-openai.test.ts tests/ai/probe-openrouter.test.ts tests/ai/probe-ollama.test.ts tests/ai/probe-custom-fallback.test.ts tests/ai/probe-error-extraction.test.ts tests/ai/probes.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - All 7 test files exist under `tests/ai/`.
    - `npx vitest run tests/ai/probe-openai.test.ts -x` reports 6 passing tests.
    - `npx vitest run tests/ai/probe-openrouter.test.ts -x` reports 3 passing tests.
    - `npx vitest run tests/ai/probe-ollama.test.ts -x` reports 7 passing tests.
    - `npx vitest run tests/ai/probe-anthropic.test.ts -x` reports 3 passing tests.
    - `npx vitest run tests/ai/probe-custom-fallback.test.ts -x` reports 7 passing tests.
    - `npx vitest run tests/ai/probe-error-extraction.test.ts -x` reports 8 passing tests.
    - `npx vitest run tests/ai/probes.test.ts -x` reports 5 passing tests (one per provider).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run tests/ai/ -x` (full ai/ suite) reports >= 39 passing tests across all Phase 07 plans (counts roll up: 13 from 07-01 + 13 from 07-02 + 7 from 07-03 + 12 from this plan's Task 1 + 39 from this Task 2 = 84 total).
  </acceptance_criteria>
  <done>
    Each provider's probe HTTP shape (URL, headers, body) is locked by a unit test; error extraction handles all 4 vendor shapes plus invalid JSON; integration roll-up confirms cross-provider ProbeResult contract.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings UI / palette command -> AIClient.probe | The probe sends an HTTP request to the provider host. Pre-flight guards (no provider, empty key) prevent wasted/error-prone calls. |
| Probe response -> Notice UI | Vendor error messages may contain user-input echoes (e.g. their key fragment). Truncation at 200 chars limits Notice overflow but does NOT redact echoed key fragments — vendor responsibility, not plugin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering / Spoofing | testActiveAIConnection -> AIClient.probe | partial-mitigate | Plan 07-04 calls AIClient.probe directly; the disclosure gate that 07-RESEARCH Pitfall 6 + Security threat #5 (modal bypass) requires is wrapped around `AIClient.probe()` in Plan 07-05 — NOT in this plan's call site. The gate is layered correctly because it modifies AIClient itself, so all callers (including this plan's testActiveAIConnection AND any future Phase 08 invoker) inherit the protection without 07-04 caller-side changes. |
| T-07-04-debounce | Availability (cost-based, low) | AIClient.probe | mitigate | Single-in-flight Map per provider prevents accidental duplicate billing on rapid clicks. Anthropic 1-token chat is ~$0.0001/click; this is a UX nicety, not a security control. |
| T-07-empty-key | Availability | AIClient.probe entry | mitigate | Empty-key guard for Anthropic/OpenAI/OpenRouter prevents a wasted network call that would always 401. Ollama and Custom may legitimately have empty keys (Ollama default install + no-auth Custom backends). |

</threat_model>

<verification>
Run after both tasks complete:

```bash
# Whole-project type-check
npx tsc --noEmit

# Phase 07-04 unit tests (Tasks 1 + 2)
npx vitest run tests/ai/probe-debounce.test.ts tests/ai/probe-anthropic.test.ts tests/ai/probe-openai.test.ts tests/ai/probe-openrouter.test.ts tests/ai/probe-ollama.test.ts tests/ai/probe-custom-fallback.test.ts tests/ai/probe-error-extraction.test.ts tests/ai/probes.test.ts -x

# Plan 07-01 + 07-02 + 07-03 regression
npx vitest run tests/ai/types.test.ts tests/ai/settingsStore.test.ts tests/ai/obsidianFetch.test.ts tests/ai/aiClient.test.ts tests/ai/pricing.test.ts tests/ai/lc-isolation.test.ts tests/ai/settingsTab.test.ts tests/shared/logger.test.ts -x

# v1.0 regression
npx vitest run tests/api/ tests/settings/ tests/shared/ -x

# AIPROV-05 isolation gate
npm run check:lc-isolation

# Lint
npm run lint

# Build + bundle gate
npm run build && npm run check:bundle-size
```

All commands exit 0. Bundle size delta vs Plan 07-03 baseline: < +0.5 KB (only ~30 lines of TS code added in main.ts).

**Manual UAT** (from 07-VALIDATION.md §"Manual-Only Verifications"):
- Configure each provider with a valid key -> click Test connection -> confirm green Notice with the provider name + model count (or 'Anthropic' alone, or Ollama special-case copy).
- Configure with a bogus key -> confirm provider's verbatim error in Notice (truncated to ~200 chars).
- Click Test connection rapidly 3 times -> only one Notice appears (debounce holds).
- Run palette command 'Test AI connection' from Cmd+P -> same Notice path.
- Empty key + Anthropic active -> click Test connection -> Notice 'Enter an API key for Anthropic first.' fires WITHOUT a network call.
</verification>

<success_criteria>

This plan is complete when:

1. `testActiveAIConnection()` exists on LeetCodePlugin and is called from both the Settings button and the palette command.
2. `prettyName` is exported from `src/ai/types.ts` and imported by both main.ts and SettingsTab.ts (single source of truth).
3. Empty-key guard fires for Anthropic / OpenAI / OpenRouter without a network call.
4. Single in-flight per provider — debounce verified by test.
5. Notice copy matches 07-UI-SPEC verbatim across all 7 cases (success-with-count / success-Anthropic / Ollama-zero-models / failure-truncated / no-provider / empty-key / cancelled-by-Plan-07-05).
6. All 51 Plan 07-04 tests green (12 debounce + 6 openai + 3 openrouter + 7 ollama + 3 anthropic + 7 custom + 8 error-extraction + 5 integration).
7. `npm run check:lc-isolation`, `npm run lint`, `npm run build`, `npm run check:bundle-size` all exit 0.
8. v1.0 regression suites (tests/api/, tests/settings/, tests/shared/) green.

</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-provider-foundation/07-04-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Capture: where testActiveAIConnection lives (main.ts), call-site count (Settings button + palette command = 2), debounce mechanism (Map<AIProvider, Promise>), test count per provider file, the placeholder removal (Plan 07-03 'Plan 07-04' string is gone), and gates downstream (Plan 07-05 wraps AIClient.probe with disclosure gate; Plan 07-06 adds clear-ai-key + README docs).
</output>
