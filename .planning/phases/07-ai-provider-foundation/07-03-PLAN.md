---
phase: 07-ai-provider-foundation
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - src/main.ts
  - src/settings/SettingsTab.ts
  - styles.css
  - tests/ai/settingsTab.test.ts
autonomous: true
requirements: [AIPROV-01, AIPROV-02]
tags: [ai, settings-ui, ai-client-wiring, masked-input, dropdown]
must_haves:
  truths:
    - "User sees an 'AI' section heading in Settings between Preview and Knowledge graph"
    - "User picks an AI provider from a 6-option dropdown (— Not configured — / Anthropic / OpenAI / OpenRouter / Ollama / Custom (OpenAI-compatible))"
    - "When activeAIProvider is null, only the dropdown row renders (no sub-form)"
    - "API key field renders as <input type='password'> for Anthropic / OpenAI / OpenRouter / Custom; field is omitted for Ollama"
    - "Base URL field is read-only descriptive text for Anthropic / OpenAI / OpenRouter; editable for Ollama and Custom"
    - "Model field is editable for all 5 providers with provider-default placeholder"
    - "Test connection button is rendered (clicking it is wired in Plan 07-04 — Phase 07-03 ships the button with a placeholder onClick that emits 'Test connection: wiring lands in Plan 07-04' Notice)"
    - "AIClient is constructed in onload after SettingsStore.load and before registerView, exposed as this.aiClient on LeetCodePlugin"
    - "Switching active provider preserves prior provider's apiKey + baseUrl + model + disclosureAcknowledged in providerConfigs map"
    - "Exactly ZERO new setCta() invocations in src/settings/SettingsTab.ts (the new section is all-neutral)"
  artifacts:
    - path: "src/main.ts"
      provides: "this.aiClient: AIClient field + Step 5.9 wiring"
      contains: "new AIClient(this.settings)"
    - path: "src/settings/SettingsTab.ts"
      provides: "AI section: heading + provider dropdown + per-provider sub-form + test-connection button"
      contains: "Active AI provider"
    - path: "styles.css"
      provides: "lc-ai-input class extending the .lc-cookie-input precedent"
      contains: ".lc-ai-input"
    - path: "tests/ai/settingsTab.test.ts"
      provides: "DOM-level unit tests for dropdown + masked input + provider-conditional rendering"
      contains: "type === 'password'"
  key_links:
    - from: "src/main.ts onload Step 5.9"
      to: "src/ai/AIClient (Plan 07-02 export)"
      via: "constructor injection of SettingsStore"
      pattern: "this\\.aiClient = new AIClient"
    - from: "SettingsTab AI section"
      to: "SettingsStore.getActiveAIProvider / setActiveAIProvider / getProviderConfig / setProviderConfig"
      via: "dropdown.onChange + addText.onChange triggers persist"
      pattern: "setActiveAIProvider|setProviderConfig"
    - from: "SettingsTab AI section"
      to: "AIClient (eventual probe call)"
      via: "Test connection button — placeholder until Plan 07-04 wires AIClient.probe"
      pattern: "Test connection"
---

<objective>
Wire the AI section into Settings UI and construct the `AIClient` in plugin onload. Phase 07-03 adds the user-visible surface for AIPROV-01 (active provider config) and AIPROV-02 (masked API key) WITHOUT yet invoking any HTTP — Test connection is shipped as a placeholder button to be wired by Plan 07-04, and the disclosure gate is wrapped around the probe in Plan 07-05.

Purpose: Plans 07-04, 07-05, 07-06 all assume `this.aiClient` exists on the plugin and that the AI Settings section renders. Without this plan, the Test connection button has nothing to click and the palette commands have no UI sibling to verify against.

Output: One new heading + ~5 Setting rows in `SettingsTab.display()`; one new field on the plugin class; one new CSS rule in `styles.css`; one new DOM-level test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-provider-foundation/07-CONTEXT.md
@.planning/phases/07-ai-provider-foundation/07-RESEARCH.md
@.planning/phases/07-ai-provider-foundation/07-PATTERNS.md
@.planning/phases/07-ai-provider-foundation/07-UI-SPEC.md
@.planning/phases/07-ai-provider-foundation/07-01-SUMMARY.md
@.planning/phases/07-ai-provider-foundation/07-02-SUMMARY.md

@src/main.ts
@src/settings/SettingsTab.ts
@src/settings/SettingsStore.ts
@src/ai/AIClient.ts
@src/ai/types.ts
@styles.css

<interfaces>
<!-- Imports + extension points the executor will use. -->

From src/ai/AIClient.ts (Plan 07-02 output):
- `class AIClient { constructor(settings: SettingsStore); probe(provider: AIProvider): Promise<ProbeResult>; invoke(req: AIRequest): Promise<AIResponse>; addCost(usd: number): Promise<void>; }`

From src/ai/types.ts:
- `type AIProvider`, `interface ProviderConfig`

From src/settings/SettingsStore.ts (Plan 07-01 output):
- `getActiveAIProvider(): AIProvider | null`
- `setActiveAIProvider(p: AIProvider | null): Promise<void>`
- `getProviderConfig(p: AIProvider): ProviderConfig`
- `setProviderConfig(p: AIProvider, cfg: ProviderConfig): Promise<void>`

From src/main.ts (existing fields + lifecycle):
- `class LeetCodePlugin extends Plugin` — already has `this.client`, `this.auth`, `this.list`, `this.notes`, `this.knowledgeGraph`. Add `this.aiClient: AIClient` alongside.
- `onload()` Step structure (per main.ts:152-310 + 07-PATTERNS §"src/main.ts"): SettingsStore.load (Step 1) → installRequestUrlFetcher (Step 2) → LeetCodeClient (Step 3) → AuthService (Step 4) → ProblemListService (Step 5) → NoteWriter (Step 5.5) → SubmissionHistoryStore + KnowledgeGraphWriter (Step 5.7) → EphemeralTabStore (Step 5.8) → **AIClient (Step 5.9 — NEW)** → registerView (Step 6a).

From src/settings/SettingsTab.ts (existing structure):
- `display()` method body — section ordering (Authentication / Manual cookie / Notes / Preview / Knowledge graph). Insertion site for AI section: AFTER Preview block (line ~196) BEFORE Knowledge graph heading (line ~205).
- Pattern for masked input at lines 106-121: `t.inputEl.type = 'password'` + `t.inputEl.addClass('lc-cookie-input')`.
- Pattern for dropdown at lines 184-196 (Preview click-behavior).
- Pattern for neutral button at lines 123-144 (Save cookies — NO setCta).

From styles.css existing rules:
- `.lc-cookie-input { font-family: var(--font-monospace); ... }` — pattern to extend.

</interfaces>

<canonical_decisions>
- D-C: Single 'AI' section in existing `LeetCodeSettingTab`, between Preview and Knowledge graph. Active-provider dropdown swaps form fields. NO accordion, NO second tab.
- 07-UI-SPEC §"Copywriting Contract" — every user-visible string is LOCKED VERBATIM. Variable substitutions allowed: `{provider name}`, `{base URL}`, `{model count}`. Paraphrasing forbidden.
- 07-UI-SPEC §"Layout Contract" — when activeAIProvider is null, only the dropdown row renders. Provider-conditional fields:
  - `'anthropic' | 'openai' | 'openrouter'`: API key (password) + Base URL (read-only desc) + Model + Test connection
  - `'ollama'`: Base URL (editable) + Model + Test connection (NO API key field)
  - `'custom'`: API key (password) + Base URL (editable, starts empty) + Model + Test connection
- 07-UI-SPEC §"Color" — exactly ZERO new `setCta()` invocations in this plan. The Test connection button is NEUTRAL. The Cancel button stays neutral. Only the disclosure modal's Continue button gets `setCta()` (Plan 07-05).
- AIClient is constructed AFTER SettingsStore.load (deps: settings store) and BEFORE registerView (so any view that wants AIClient access can grab it from `this.aiClient`).
</canonical_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire AIClient into LeetCodePlugin.onload as Step 5.9</name>
  <files>src/main.ts</files>
  <read_first>
    - src/main.ts (target — extend in place; field declaration at top of class, construction inside onload)
    - src/main.ts:25 (existing `installRequestUrlFetcher` import location — analog for AI imports)
    - src/main.ts:152-310 (onload step block)
    - src/main.ts:163 (LeetCodeClient construction — analog: ctor takes settings, no eager network, mirrored verbatim)
    - src/ai/AIClient.ts (Plan 07-02 output)
    - .planning/phases/07-ai-provider-foundation/07-PATTERNS.md §"src/main.ts (modify)" (insertion order documentation)
  </read_first>
  <behavior>
    - `LeetCodePlugin` class has a new field `aiClient: AIClient` (declared at the top of the class alongside `this.client`, `this.auth`, etc — same visibility, no `private` if siblings are public).
    - `onload()` constructs `this.aiClient = new AIClient(this.settings)` AFTER existing wiring of `EphemeralTabStore` (or whichever is the last service before `registerView`) and BEFORE `this.registerView(BROWSER_VIEW_TYPE, ...)`.
    - The construction is synchronous (no `await`); AIClient ctor takes only `SettingsStore` and does no eager network — mirrors `LeetCodeClient` ctor at line 163.
    - The construction is wrapped in a comment: `// Step 5.9 — Phase 07 AI client. Constructed AFTER SettingsStore.load (settings are required by AIClient ctor) and BEFORE registerView (so any view that wants AIClient access can grab it from this.aiClient).`
    - `onunload()` does NOT need a teardown for AIClient — it holds no listeners, no timers, no open sockets.
  </behavior>
  <action>
    Make exactly two edits to `src/main.ts`:

    1. Add `import { AIClient } from './ai/AIClient';` near the existing import block. Place it after the LeetCodeClient/SettingsStore imports — alphabetical-ish ordering already in use.

    2. Inside the `LeetCodePlugin` class body, declare the new field alongside existing service fields:
    ```typescript
    aiClient!: AIClient;
    ```
    Use the `!` definite-assignment assertion mirroring how `client!`, `auth!`, etc are declared (TypeScript-strict pattern; field assigned in onload, never accessed before).

    3. Inside `onload()`, locate the wiring sequence. Identify the last service constructor BEFORE `registerView`. Insert AFTER that and BEFORE `registerView`:

    ```typescript
    // Step 5.9 — Phase 07 AI client. Constructed AFTER SettingsStore.load
    // (settings are required by AIClient ctor) and BEFORE registerView (so any
    // future view that wants AIClient access can grab it from this.aiClient).
    this.aiClient = new AIClient(this.settings);
    ```

    Do NOT modify any existing wiring step. Do NOT add `addCommand` calls — those belong to Plans 07-04 (`test-ai-connection`), 07-05 (`reset-ai-disclosures`), and 07-06 (`clear-ai-key`). Do NOT add `onunload` cleanup for AIClient (it holds no resources).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npm run check:bundle-size</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { AIClient } from './ai/AIClient'" src/main.ts` returns 1.
    - `grep -c "aiClient!: AIClient" src/main.ts` returns 1.
    - `grep -c "this.aiClient = new AIClient" src/main.ts` returns 1.
    - `grep -c "Step 5.9" src/main.ts` returns 1.
    - `npx tsc --noEmit` exits 0.
    - `npm run build` exits 0; `npm run check:bundle-size` reports a value within ±2 KB of Plan 07-02's measurement (no significant delta from a single ctor call).
    - `npm run check:lc-isolation` exits 0 (the new import is from `./ai/AIClient`, NOT from any LC-side directory — gate must still pass).
  </acceptance_criteria>
  <done>
    AIClient constructed in onload at Step 5.9, accessible as `this.aiClient` from any plugin method, no bundle-size regression beyond Plan 07-02's baseline.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 'AI' section to SettingsTab between Preview and Knowledge graph</name>
  <files>src/settings/SettingsTab.ts, styles.css, tests/ai/settingsTab.test.ts</files>
  <read_first>
    - src/settings/SettingsTab.ts (target — extend `display()`)
    - src/settings/SettingsTab.ts:55 (Authentication heading — analog for `setName().setHeading()`)
    - src/settings/SettingsTab.ts:86 (existing setCta — DO NOT add a second one in this file; this is the lone allowed accent invocation)
    - src/settings/SettingsTab.ts:106-121 (LEETCODE_SESSION + csrftoken masked-input pattern — exact analog for API key field)
    - src/settings/SettingsTab.ts:123-144 (Save cookies neutral button — analog for Test connection)
    - src/settings/SettingsTab.ts:149 (Notes heading — analog for AI heading line)
    - src/settings/SettingsTab.ts:184-196 (Preview section + click-behavior dropdown — analog for active-provider dropdown)
    - src/settings/SettingsTab.ts:205 (Knowledge graph heading — INSERTION SITE: AI section goes BETWEEN line ~196 and line ~205)
    - styles.css — locate the `.lc-cookie-input` rule for the extension pattern
    - .planning/phases/07-ai-provider-foundation/07-UI-SPEC.md §"Copywriting Contract" (locked verbatim strings)
    - .planning/phases/07-ai-provider-foundation/07-UI-SPEC.md §"Layout Contract" (provider-conditional rendering matrix)
    - .planning/phases/07-ai-provider-foundation/07-PATTERNS.md §"src/settings/SettingsTab.ts (modify)"
  </read_first>
  <behavior>
    - The `display()` method, after re-rendering on `display()` re-invocation (Obsidian standard pattern — full clear + rebuild), produces the AI section between the Preview section and the Knowledge graph section.
    - Section heading line: `new Setting(containerEl).setName('AI').setHeading();` — verbatim from 07-UI-SPEC and analog `Notes`/`Preview`.
    - Dropdown row: name `'Active AI provider'`, desc verbatim from 07-UI-SPEC, options in this exact order: `''` → `'— Not configured —'`, `'anthropic'` → `'Anthropic'`, `'openai'` → `'OpenAI'`, `'openrouter'` → `'OpenRouter'`, `'ollama'` → `'Ollama'`, `'custom'` → `'Custom (OpenAI-compatible)'`. `setValue(this.plugin.settings.getActiveAIProvider() ?? '')`. `onChange` converts `''` → null, otherwise casts to AIProvider, calls `setActiveAIProvider`, then `this.display()` to re-render the conditional sub-form.
    - Sub-form rendering is conditional on `activeAIProvider`:
      - `null`: render only the dropdown — no sub-form rows.
      - `'anthropic' | 'openai' | 'openrouter'`: render API key (password) + Base URL (read-only desc text per UI-SPEC) + Model + Test connection.
      - `'ollama'`: render Base URL (editable text input, default `http://localhost:11434/v1`) + Model + Test connection. NO API key row.
      - `'custom'`: render API key (password) + Base URL (editable, placeholder `https://your-host.example.com/v1`, starts empty) + Model + Test connection.
    - API key Setting: name `'API key'`, desc `'Stored in plain text in data.json on this machine. Never transmitted anywhere except {provider name}.'` (interpolate {provider name} from the prettyName helper). `addText` with `t.inputEl.type = 'password'` + `t.inputEl.addClass('lc-ai-input')`. `setValue(cfg.apiKey).onChange((v) => settings.setProviderConfig(active, { ...cfg, apiKey: v }))`.
    - Base URL Setting (Anthropic / OpenAI / OpenRouter — read-only): name `'Base URL'`, desc `'Provider endpoint. Read-only; the plugin uses the canonical URL.'` — render the URL itself in `setDesc` as part of the desc string (`Provider endpoint. Read-only; the plugin uses the canonical URL: {base URL}`) OR via a `<code>` element appended to the setting-item. Use the simpler approach: include the URL in the desc text. NO `addText` — the row has only name + desc.
    - Base URL Setting (Ollama): name `'Base URL'`, desc verbatim from 07-UI-SPEC (`'Ollama host and port. Default is localhost:11434 — change if you run Ollama on another host.'`), `addText` editable, `setValue(cfg.baseUrl).onChange((v) => settings.setProviderConfig(active, { ...cfg, baseUrl: v }))`.
    - Base URL Setting (Custom): name `'Base URL'`, desc verbatim (`'OpenAI-compatible endpoint URL. Must include /v1 suffix if the server expects it.'`), `addText` editable with placeholder `'https://your-host.example.com/v1'`.
    - Model Setting (all providers): name `'Model'`, desc verbatim (`'Model identifier the provider expects. Defaults may rot — when "Test connection" reports "model not found", update this field.'`), `addText` editable with provider-specific placeholder (Anthropic: `claude-haiku-4-5`, OpenAI: `gpt-5-mini`, OpenRouter: `anthropic/claude-haiku-4.5`, Ollama: `llama3.2`, Custom: empty).
    - Test connection Setting: name omitted (or empty string for layout-only row), `addButton(b => b.setButtonText('Test connection').onClick(handler))`. **Phase 07-03 ships a PLACEHOLDER handler:** `async () => { new Notice('Test connection: wiring lands in Plan 07-04', 3000); }`. Plan 07-04 will replace the handler body. Do NOT add `setCta()` to this button under any circumstance — UI-SPEC §Color requires it to stay neutral.
    - **Empty-state guards (per 07-UI-SPEC §"Empty / unconfigured states"):** when `activeAIProvider === null`, the Test connection button row is NOT rendered (only the dropdown). When user clicks Test connection while `activeAIProvider === null` (impossible if guard is correct, but belt-and-suspenders): the placeholder handler is wrapped in `if (!provider) { new Notice('Pick an AI provider first.', 3000); return; }`.
    - `prettyName(provider: AIProvider): string` is a small helper inside `SettingsTab` (or imported from a small new export in `src/ai/types.ts`) returning the locked display names from 07-UI-SPEC: `'Anthropic' | 'OpenAI' | 'OpenRouter' | 'Ollama' | 'Custom (OpenAI-compatible)'`.
    - `styles.css` adds a single rule: `.leetcode-settings .lc-ai-input { font-family: var(--font-monospace); }` extending the existing `.lc-cookie-input` precedent. **Bundle delta < 0.5 KB** per UI-SPEC §"Bundle constraint".
  </behavior>
  <action>
    Make these surgical edits:

    1. In `src/settings/SettingsTab.ts`, add imports near the top: `import type { AIProvider } from '../ai/types';` and `import { Notice } from 'obsidian';` (if Notice isn't already imported in this file — check first; if it is, skip).

    2. Define `prettyName` as a module-private function above the `LeetCodeSettingTab` class (not a method — keeps the helper colocated and not a class API surface):
    ```typescript
    function prettyName(p: AIProvider): string {
      switch (p) {
        case 'anthropic': return 'Anthropic';
        case 'openai': return 'OpenAI';
        case 'openrouter': return 'OpenRouter';
        case 'ollama': return 'Ollama';
        case 'custom': return 'Custom (OpenAI-compatible)';
      }
    }
    ```
    Exhaustive switch — the AIProvider type guarantees all cases handled at compile time.

    3. Inside `display()`, locate the Preview section block (ends at line ~196 with closing `);` of the dropdown setting). After the Preview block and BEFORE the Knowledge graph heading at line ~205, insert the new AI section:

    ```typescript
    // ─── Phase 07 AI section ──────────────────────────────────────────
    new Setting(containerEl).setName('AI').setHeading();

    const active = this.plugin.settings.getActiveAIProvider();

    new Setting(containerEl)
      .setName('Active AI provider')
      .setDesc("Pick the provider for AI features. Switching providers preserves keys you've already entered for other providers.")
      .addDropdown((d) => d
        .addOption('',           '— Not configured —')
        .addOption('anthropic',  'Anthropic')
        .addOption('openai',     'OpenAI')
        .addOption('openrouter', 'OpenRouter')
        .addOption('ollama',     'Ollama')
        .addOption('custom',     'Custom (OpenAI-compatible)')
        .setValue(active ?? '')
        .onChange(async (v) => {
          const next = v === '' ? null : (v as AIProvider);
          await this.plugin.settings.setActiveAIProvider(next);
          this.display();
        }),
      );

    if (active !== null) {
      this.renderAIProviderForm(containerEl, active);
    }
    // ──────────────────────────────────────────────────────────────────
    ```

    4. Add a private method `renderAIProviderForm(containerEl: HTMLElement, active: AIProvider): void` on the class. The method:
    - Reads `const cfg = this.plugin.settings.getProviderConfig(active);`
    - For Anthropic / OpenAI / OpenRouter / Custom: renders the API key row with `t.inputEl.type = 'password'` + `t.inputEl.addClass('lc-ai-input')` + `setValue(cfg.apiKey).onChange(async (v) => { await this.plugin.settings.setProviderConfig(active, { ...cfg, apiKey: v }); })`.
    - For Ollama: skip the API key row entirely.
    - For all 5: render the Base URL row per the matrix in <behavior>. Anthropic/OpenAI/OpenRouter: name + desc only (URL embedded in desc text). Ollama/Custom: full editable input with `t.inputEl.addClass('lc-ai-input')`.
    - For all 5: render the Model row with `addText` + provider-default placeholder via `t.setPlaceholder(...)`.
    - For all 5: render the Test connection row with the placeholder onClick from <behavior>.

    Use a `switch (active)` block where conditional rendering differs. Do NOT branch on string equality with magic strings inline — use the AIProvider type.

    5. In `styles.css`, add (place near the existing `.lc-cookie-input` rule):
    ```css
    .leetcode-settings .lc-ai-input {
      font-family: var(--font-monospace);
    }
    ```

    Do NOT add color rules — UI-SPEC §"Color" reserves accent for the disclosure modal only. Do NOT add layout rules — Obsidian-native `Setting` rhythm is preserved.

    6. Create `tests/ai/settingsTab.test.ts` with these test cases (vitest + jsdom, mirroring any existing `tests/settings/*.test.ts`):

    - `'when activeAIProvider is null, only the dropdown row renders'` — instantiate `LeetCodeSettingTab`, call `display()`, assert `containerEl.querySelectorAll('.setting-item').length === 2` (heading row + dropdown row, no sub-form).
    - `'when activeAIProvider is anthropic, API key field is type=password'` — set active to 'anthropic', call `display()`, query `containerEl.querySelector('input[type="password"]')`, assert non-null AND assert it has class `lc-ai-input`.
    - `'when activeAIProvider is ollama, no API key field is rendered'` — set active to 'ollama', call `display()`, query `input[type="password"]`, assert null AND assert Base URL input is rendered (editable text input).
    - `'when activeAIProvider is custom, base URL is editable with placeholder'` — set active to 'custom', call `display()`, find the Base URL input, assert `placeholder === 'https://your-host.example.com/v1'`.
    - `'switching from Anthropic to OpenAI preserves Anthropic apiKey'` — set anthropic.apiKey to 'sk-ant', set active to 'anthropic', simulate dropdown change to 'openai' (call `setActiveAIProvider('openai')` directly + `display()`), then change back to 'anthropic' + `display()`, find the API key input, assert `inputEl.value === 'sk-ant'` (the value persists per AIPROV-01 + Plan 07-01 Task 2).
    - `'Test connection placeholder Notice fires on click'` — set active to 'anthropic', call `display()`, simulate click on the Test connection button, assert a Notice was instantiated with text containing 'Plan 07-04' (mock `Notice` constructor; spy on calls).
    - `'no setCta invocation in the AI section'` — read `src/settings/SettingsTab.ts` content via fs, count `setCta(` occurrences, assert exactly 1 (the pre-existing one at line ~86; AI section adds zero). Static-text assertion mirrors the `tests/ai/lc-isolation.test.ts` pattern from Plan 07-02.

    Use Obsidian's test patterns from existing `tests/settings/`; if jsdom polyfills are missing, mirror the setup from `tests/settings/<existing>.test.ts`. The plugin instance can be a minimal mock `{ settings: <real SettingsStore loaded with mock data> }`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/ai/settingsTab.test.ts -x && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "setName('AI').setHeading" src/settings/SettingsTab.ts` returns 1.
    - `grep -c "Active AI provider" src/settings/SettingsTab.ts` returns 1.
    - `grep -c "renderAIProviderForm" src/settings/SettingsTab.ts` returns >= 2 (declaration + invocation).
    - `grep -c "setCta(" src/settings/SettingsTab.ts` returns 1 (pre-existing only — AI section adds zero).
    - `grep -c "lc-ai-input" src/settings/SettingsTab.ts` returns >= 2 (API key field + base URL editable field for Ollama/Custom + model field — exact count depends on implementation; require >= 2).
    - `grep -c "lc-ai-input" styles.css` returns 1.
    - `grep -c "Plan 07-04" src/settings/SettingsTab.ts` returns 1 (placeholder Notice text — locked so Plan 07-04 can grep-replace).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run tests/ai/settingsTab.test.ts -x` reports >= 7 passing tests.
    - `npx vitest run tests/settings/ -x` (existing tests) still pass.
    - `npm run build` exits 0; bundle size within +1 KB of Plan 07-02 baseline (CSS + ~50 lines of TS code only).
    - `npm run lint` exits 0 (no innerHTML, no plugin-id-in-command-id violations — none of those rules apply here, but suite must stay green).
  </acceptance_criteria>
  <done>
    AI Settings section renders correctly across all 6 dropdown states, masked password input + provider-conditional rendering working, exactly one setCta in the file (pre-existing), CSS class extends the cookie-input precedent.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings UI input → SettingsStore | User-typed apiKey + baseUrl + model values flow into PluginData via setProviderConfig. SettingsStore (Plan 07-01) sanitizes on every set; UI-layer validation is "soft" (placeholder, masking) only. |
| AIClient construction → first method call | Field declared with `!` definite-assignment; if onload throws between Step 5.9 and registerView, accessing `this.aiClient` from a UI handler would `undefined.method` crash. Mitigation: AIClient ctor cannot throw (no eager network) — verified in Plan 07-02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-UI | Information Disclosure | API key Settings input | mitigate | `t.inputEl.type = 'password'` per AIPROV-02; `t.inputEl.addClass('lc-ai-input')` for monospace masking aesthetic. Visual masking only — actual key persists plain-text in data.json per Phase 07 G locked posture. README disclosure ships in Plan 07-06. |
| T-07-03-CTA | UX (defensive) | Test connection button | accept (after verification) | UI-SPEC §"Color" requires Test connection to stay neutral (NO setCta) — frequent action, low stakes. Grep gate in <acceptance_criteria> asserts exactly 1 setCta in SettingsTab.ts (pre-existing). Plan 07-05 adds the second setCta in `src/ai/disclosure.ts` (modal Continue button). |

</threat_model>

<verification>
Run after both tasks complete:

```bash
# Whole-project type-check
npx tsc --noEmit

# Phase 07-03 unit tests
npx vitest run tests/ai/settingsTab.test.ts -x

# Plan 07-01 + 07-02 regression — must still pass
npx vitest run tests/ai/types.test.ts tests/ai/settingsStore.test.ts tests/ai/obsidianFetch.test.ts tests/ai/aiClient.test.ts tests/ai/pricing.test.ts tests/ai/lc-isolation.test.ts tests/shared/logger.test.ts -x

# v1.0 regression
npx vitest run tests/api/ tests/settings/ tests/shared/ -x

# AIPROV-05 isolation — still green
npm run check:lc-isolation

# Lint full chain
npm run lint

# Build + bundle gate
npm run build && npm run check:bundle-size
```

All commands exit 0. Bundle size within +1 KB of Plan 07-02 baseline.

**Manual UAT (from 07-VALIDATION.md §"Manual-Only Verifications"):**
- Open Settings → confirm "AI" heading appears between "Preview" and "Knowledge graph".
- Click Active AI provider dropdown → confirm 6 options in locked order with locked labels.
- Pick "Anthropic" → confirm API key (masked •••••••), Base URL (read-only descriptive text), Model (placeholder `claude-haiku-4-5`), Test connection button render.
- Click Test connection → confirm Notice text matches `'Test connection: wiring lands in Plan 07-04'`.
- Switch to Ollama → confirm API key field is GONE; Base URL is editable with default `http://localhost:11434/v1`.
- Switch to Custom → confirm API key (masked) returns; Base URL is editable, empty, placeholder `https://your-host.example.com/v1`.
- Switch back to Anthropic → confirm prior API key value (if any) persists.
</verification>

<success_criteria>

This plan is complete when:

1. `this.aiClient = new AIClient(this.settings)` lives in onload Step 5.9; type-checks; bundle delta ≈ 0.
2. AI section renders between Preview and Knowledge graph headings.
3. All 5 dropdown values render the correct provider-conditional sub-form per UI-SPEC layout matrix.
4. API key inputs are `<input type="password">` with `.lc-ai-input` class.
5. Test connection placeholder Notice fires on click; the Notice text contains 'Plan 07-04' so the next plan can grep-replace cleanly.
6. Exactly ONE `setCta(` invocation exists in SettingsTab.ts (pre-existing only).
7. Switching provider preserves prior provider's apiKey + baseUrl + model in PluginData (asserted by test).
8. All Plan 07-03 + Plan 07-02 + Plan 07-01 tests green; v1.0 regression suites green.

</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-provider-foundation/07-03-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Capture: where AIClient is wired (Step 5.9 in main.ts), the section heading insertion point (between Preview at ~line 196 and Knowledge graph at ~line 205), the placeholder Notice grep hook for Plan 07-04, the setCta count invariant (still 1 in SettingsTab.ts), and gates downstream (Plan 07-04 may now wire AIClient.probe to the Test connection onClick; Plan 07-05 wraps both probe and invoke with disclosure gate; Plan 07-06 adds `clear-ai-key` palette command).
</output>
