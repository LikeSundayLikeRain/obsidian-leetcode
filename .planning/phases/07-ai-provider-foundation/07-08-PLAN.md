---
phase: 07-ai-provider-foundation
plan: 08
type: execute
wave: 6
depends_on: [07-07]
files_modified:
  - src/shared/logger.ts
  - tests/shared/logger.test.ts
  - src/main.ts
  - src/ai/providers/openaiCompatible.ts
  - src/ai/providers/ollama.ts
  - tests/ai/probes.test.ts
  - tests/ai/probe-debounce.test.ts
  - tests/ai/aiClient.test.ts
autonomous: true
gap_closure: true
requirements: [AIPROV-02, AIPROV-03]

must_haves:
  truths:
    - "redactString preserves the input separator: 'x-api-key: val' redacts to 'x-api-key: [REDACTED]' (colon kept), 'x-api-key=val' redacts to 'x-api-key=[REDACTED]' (equals kept) — no normalization to '='"
    - "redactString does not consume the 'Bearer' keyword as a value when no token follows: 'Authorization: Bearer' (no trailing token) is left clean (untouched OR normalized to 'Authorization: Bearer [REDACTED]'), and is NEVER rendered as 'Authorization=[REDACTED]'"
    - "All three empty-baseUrl guard sites (main.ts testActiveAIConnection, probeCustom, probeOllama) treat whitespace-only baseUrl as empty: single space, tab, mixed whitespace are all rejected with the friendly Notice / clean error and the network fetcher is NEVER invoked"
    - "MockSettings in aiClient.test.ts exposes setProviderConfig, and aiClient.test.ts has a unit test asserting that AIClient.probe persists disclosureAcknowledged:true via setProviderConfig after the disclosure helper returns true, and the next probe call does not re-fire the disclosure helper"
  artifacts:
    - path: "src/shared/logger.ts"
      provides: "SECRET_VALUE_PATTERN with separator-preserving second alternate + Bearer-keyword guard"
      contains: "[=:]"
    - path: "tests/shared/logger.test.ts"
      provides: "Category 5 (CR-01-A + WR-02-separator) regression suite"
      contains: "Authorization: Bearer"
    - path: "src/main.ts"
      provides: "testActiveAIConnection guard using !cfg.baseUrl?.trim() (whitespace-aware)"
      contains: "trim()"
    - path: "src/ai/providers/openaiCompatible.ts"
      provides: "probeCustom guard using !cfg.baseUrl?.trim()"
      contains: "trim()"
    - path: "src/ai/providers/ollama.ts"
      provides: "probeOllama guard using !cfg.baseUrl?.trim()"
      contains: "trim()"
    - path: "tests/ai/probes.test.ts"
      provides: "Whitespace-only baseUrl fixtures for probeCustom + probeOllama"
      contains: "whitespace"
    - path: "tests/ai/probe-debounce.test.ts"
      provides: "Whitespace-only baseUrl fixtures for testActiveAIConnection main.ts guard"
      contains: "whitespace"
    - path: "tests/ai/aiClient.test.ts"
      provides: "MockSettings.setProviderConfig + disclosure-gate persistence test"
      contains: "setProviderConfig"
  key_links:
    - from: "redactString"
      to: "captured separator group"
      via: "([:=]) capture in second alternate"
      pattern: "\\[=:\\]"
    - from: "testActiveAIConnection"
      to: "shared empty-baseUrl semantic"
      via: "!cfg.baseUrl?.trim()"
      pattern: "trim\\(\\)"
    - from: "probeCustom"
      to: "shared empty-baseUrl semantic"
      via: "!cfg.baseUrl?.trim()"
      pattern: "trim\\(\\)"
    - from: "probeOllama"
      to: "shared empty-baseUrl semantic"
      via: "!cfg.baseUrl?.trim()"
      pattern: "trim\\(\\)"
    - from: "AIClient.probe disclosure-gate Continue branch"
      to: "MockSettings.setProviderConfig"
      via: "settings.setProviderConfig(provider, {...cfg, disclosureAcknowledged: true})"
      pattern: "setProviderConfig"
---

<objective>
Round-2 advisory cleanup for Phase 07. Round 1 (Plan 07-07) closed the four blockers from the original 07-VERIFICATION.md (CR-01, CR-02, WR-01, WR-02), and the verifier marked the phase passed (12/12 truths). The standard-depth code review of 07-07 (07-07-REVIEW.md) surfaced four advisory findings that the verifier captured under `advisory_findings_from_07_07_review`. None of these block any of the original 12 truths — all four AIPROV-01..07 requirements remain SATISFIED — but each is a concrete contract violation or test gap that degrades log readability, leaves an edge case untested, or contradicts comments newly added in 07-07.

This plan closes all four advisories in three tasks. Scope is intentionally tight: regex separator handling in the logger, whitespace-aware empty-baseUrl semantics across the three guard sites, and a test-fixture upgrade that exercises the previously-untested disclosure-gate persistence path.

Purpose: Honor the contract documented in 07-07's CR-01 fix comments ("preserves the original `:` separator and the `Bearer` keyword"), align the three guard sites on a single empty-baseUrl semantic that handles whitespace inputs, and remove the latent test crash where a future test exercising the disclosure-ack Continue path with `disclosureAcknowledged: false` would throw `setProviderConfig is not a function`.

Output: Logger redaction that preserves the input separator and never consumes 'Bearer' as a value; three empty-baseUrl guard sites that uniformly reject whitespace-only inputs; an aiClient.test.ts MockSettings that supports the disclosure-gate persistence assertion; full vitest suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@.planning/phases/07-ai-provider-foundation/07-CONTEXT.md
@.planning/phases/07-ai-provider-foundation/07-RESEARCH.md
@.planning/phases/07-ai-provider-foundation/07-VERIFICATION.md
@.planning/phases/07-ai-provider-foundation/07-07-REVIEW.md
@.planning/phases/07-ai-provider-foundation/07-07-SUMMARY.md

<interfaces>
<!-- Pulled directly from the source files the executor will edit. -->
<!-- Executor should treat these as the load-bearing contracts; no exploration needed. -->

From src/shared/logger.ts (current — to be edited in Task 1):

  // Single-pattern ordered alternation (post-CR-01 fix at 07-07).
  // First alternate: `\b(authorization)\s*:\s*(bearer)\s+([^\s;,"'&}\]\[]+)` — fires only when
  //   `Authorization: Bearer <token>` has a trailing token. Replacement preserves `:` separator
  //   and `Bearer` keyword, emits `<authKey>: <bearerKey> [REDACTED]`.
  // Second alternate: `\b(LEETCODE_SESSION|csrftoken|session|csrf|cookie|token|authorization|apikey|api[_-]?key|x-api-key)\s*[=:]\s*([^\s;,"'&}\]\[]+)` —
  //   fallback for env-var-style and other auth-ish kv pairs. CURRENTLY hardcodes `=` separator
  //   in the replacement: `${otherKey ?? ''}=[REDACTED]` — this is the WR-02-separator finding.
  //   ALSO matches `'Authorization: Bearer'` (no trailing token) with `Bearer` as the value,
  //   producing `'Authorization=[REDACTED]'` — this is the CR-01-A finding.
  //
  // The replacement function:
  //   return s.replace(SECRET_VALUE_PATTERN, (_m, authKey, bearerKey, _bearerTok, otherKey) => {
  //     if (authKey !== undefined && bearerKey !== undefined) {
  //       return `${authKey}: ${bearerKey} [REDACTED]`;
  //     }
  //     return `${otherKey ?? ''}=[REDACTED]`;  // <-- hardcoded '='; WR-02-separator
  //   });

From src/main.ts (current testActiveAIConnection guard at lines 789-795 — to be edited in Task 2):

  if (
    (provider === 'custom' || provider === 'ollama') &&
    cfg.baseUrl === ''   // <-- strict empty; WR-03-whitespace finding (whitespace-only passes through)
  ) {
    new Notice(`Enter a Base URL for ${prettyName(provider)} first.`, 3000);
    return;
  }

From src/ai/providers/openaiCompatible.ts (current probeCustom guard at line 70 — to be edited in Task 2):

  if (!cfg.baseUrl) {   // <-- falsy; passes whitespace-only through; WR-03-whitespace finding
    return { ok: false, errorMessage: 'Base URL is required for Custom provider.' };
  }

From src/ai/providers/ollama.ts (current probeOllama guard at line 32 — to be edited in Task 2):

  if (!cfg.baseUrl) {   // <-- falsy; passes whitespace-only through; WR-03-whitespace finding
    return { ok: false, errorMessage: 'Base URL is required for Ollama provider.' };
  }

From src/ai/AIClient.ts (lines 86-99 — disclosure-gate Continue path, NOT being edited):

  async probe(provider: AIProvider): Promise<ProbeResult> {
    try {
      let cfg = this.settings.getProviderConfig(provider);
      if (!cfg.disclosureAcknowledged) {
        const ack = await this.requireDisclosure(provider, cfg);
        if (!ack) {
          return { ok: false, errorMessage: 'AI call cancelled' };
        }
        await this.settings.setProviderConfig(provider, {  // <-- persistence call; WR-01-test-gap
          ...cfg,
          disclosureAcknowledged: true,
        });
        cfg = this.settings.getProviderConfig(provider);
      }
      // ... resolveAdapter + adapter.probe()
    }
  }

From tests/ai/aiClient.test.ts (MockSettings interface — to be extended in Task 3):

  interface MockSettings {
    getActiveAIProvider: () => string | null;
    getProviderConfig: (p: string) => Record<string, unknown>;
    addCostLedger: (usd: number) => Promise<void>;
    // MISSING: setProviderConfig — WR-01-test-gap finding.
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Logger contract fix — separator preservation + Bearer-no-token guard (CR-01-A + WR-02-separator)</name>
  <files>src/shared/logger.ts, tests/shared/logger.test.ts</files>
  <read_first>
    - src/shared/logger.ts (entire file; the SECRET_VALUE_PATTERN at lines 51-70 and the redactString replacement function are the only edit sites)
    - tests/shared/logger.test.ts (entire file; the new test fixtures live in a new "Category 5" describe block appended at the end)
    - .planning/phases/07-ai-provider-foundation/07-07-REVIEW.md (CR-01 critical section at lines 55-88; WR-02 at lines 124-138 — recommended fix sketches included there)
  </read_first>
  <behavior>
    - Test 1 (separator preservation, colon header): `logger.warn('http err', 'x-api-key: sk-ant-xyz')` → captured output contains `'x-api-key: [REDACTED]'` (colon preserved). Output MUST NOT contain `'x-api-key=[REDACTED]'`. (WR-02-separator)
    - Test 2 (separator preservation, equals env-var): `logger.warn('env', 'LEETCODE_SESSION=cookie-val')` → captured output contains `'LEETCODE_SESSION=[REDACTED]'` (equals preserved — regression guard for v1.0 shape). (WR-02-separator regression guard)
    - Test 3 (Bearer keyword preserved when no token follows): `logger.warn('http err', 'Authorization: Bearer')` → captured output MUST NOT contain `'Authorization=[REDACTED]'`. The acceptable output shapes are EITHER (a) the input untouched (`'Authorization: Bearer'` survives — preferred when there's no secret to redact), OR (b) `'Authorization: Bearer [REDACTED]'` (normalized — also acceptable). Asserting both forms via OR-of-toContain checks. (CR-01-A)
    - Test 4 (existing CR-01 primary case still passes): `logger.warn('http err', 'Authorization: Bearer sk-proj-abcdef')` → output contains `'Authorization: Bearer [REDACTED]'`, does NOT contain `'sk-proj-abcdef'`, does NOT contain `'=[REDACTED] [REDACTED]'`. (CR-01 primary regression guard — already in Category 4, restated here so the fix doesn't break it)
    - Test 5 (token redaction still works for non-Bearer headers using `=`): `logger.warn('env', 'token=abc123')` → output contains `'token=[REDACTED]'`, does NOT contain `'abc123'`. (regression guard for env-var shape)
  </behavior>
  <action>
    Edit `src/shared/logger.ts`:

    1. Modify `SECRET_VALUE_PATTERN` (line 52) so the second alternate captures the separator (`[:=]`) as a numbered group instead of allowing it inline. Concretely, restructure the second alternate from `\s*[=:]\s*` to `\s*([=:])\s*` so the captured separator becomes available in the replacement function. The first alternate (`\b(authorization)\s*:\s*(bearer)\s+([^\s;,"'&}\]\[]+)`) is unchanged. Note: the second-alternate keyword list currently includes `authorization` and `bearer`-adjacent terms — leave the keyword list as-is. The behavior change comes from the replacement, not the pattern's keyword set.

    2. Modify the `redactString` replacement function (lines 65-70) so it accepts the new captured separator argument and emits it in the output. The argument list becomes `(_m, authKey, bearerKey, _bearerTok, otherKey, otherSep, _otherVal)` (the new `otherSep` is the captured `:` or `=`). The replacement returns `` `${otherKey ?? ''}${otherSep ?? '='}[REDACTED]` `` — preserving the original separator. The first-alternate branch (when both `authKey` and `bearerKey` are defined) is unchanged.

    3. CR-01-A handling — Bearer-no-token edge case. The first alternate already requires `\s+([^\s;,"'&}\]\[]+)` (whitespace + at least one token char), so it does not match `'Authorization: Bearer'` with no trailing token. The second alternate currently matches with name=`authorization`, value=`Bearer`, producing `'Authorization=[REDACTED]'` (or after fix above, `'Authorization: [REDACTED]'`) — still wrong because `Bearer` is the scheme keyword, not a secret. Fix by adding a negative lookahead inside the second alternate that prevents it from firing when the captured value would be a literal `bearer` keyword (case-insensitive). Concretely, after the separator capture, change the value char class from `([^\s;,"'&}\]\[]+)` to a form that explicitly excludes a bare `bearer` token: `(?!bearer\b)([^\s;,"'&}\]\[]+)`. The negative lookahead `(?!bearer\b)` plus the case-insensitive flag (`/i` already on the regex) prevents the second alternate from firing on `'Authorization: Bearer'` (no token). After this change, `'Authorization: Bearer'` (no trailing token) survives the regex untouched — output is the input string verbatim.

    4. Update the doc comment block above SECRET_VALUE_PATTERN (lines 30-50) to:
       - Document the new captured separator group and its purpose ("preserves the input separator: `:` for header style, `=` for env-var style").
       - Document the `(?!bearer\b)` negative lookahead and the CR-01-A edge case it closes ("`'Authorization: Bearer'` with no trailing token is left untouched — `Bearer` is a scheme keyword, not a secret").
       - Cross-reference Plan 07-08 in the comment (the CR-01 reference at line 30 currently says "Phase 07 Plan 07"; add a follow-up paragraph noting "Phase 07 Plan 08 (CR-01-A + WR-02-separator)" before the `s` flag note).

    Edit `tests/shared/logger.test.ts`:

    5. Append a new `describe('Phase 07 logger Category 5 — CR-01-A + WR-02-separator advisory fixes', ...)` block AFTER the existing Category 4 block (after line 155). The block contains the five test cases above. For Test 3 (Bearer-no-token), assert the output is one of the two acceptable shapes via:
       ```
       const out = captured();
       const acceptableA = !out.includes('Authorization=[REDACTED]') && out.includes('Bearer');
       const acceptableB = out.includes('Authorization: Bearer [REDACTED]');
       expect(acceptableA || acceptableB).toBe(true);
       expect(out).not.toContain('Authorization=[REDACTED]');
       ```
       The hard contract is `not.toContain('Authorization=[REDACTED]')` — that is the v1.0+07-07 broken shape. The "or" handles whichever clean form the regex fix happens to produce.

    Anti-requirements:
    - Do NOT change the first alternate's keyword (`authorization`) — that branch is correct and load-bearing for the primary CR-01 case.
    - Do NOT remove `authorization` from the second alternate's keyword list — keep it as fallback for env-var-style `AUTHORIZATION=xyz` shapes (no `Bearer` keyword present).
    - Do NOT touch the `REDACT` key-name pattern (line 17) — that operates on object keys, not string values, and is orthogonal to the separator issue.
    - Do NOT introduce a second pass over the string — the CR-01 fix at 07-07 explicitly removed the two-pass approach because it produced the `=[REDACTED] [REDACTED]` garbling. Stay single-pass.

    Per CR-01-A advisory, per WR-02-separator advisory.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/shared/logger.test.ts` passes all 16 tests (the existing 11 + the 5 new Category 5 tests).
    - The original CR-01 Category 4 regression guards still pass (`'=[REDACTED] [REDACTED]'` never appears).
    - Mechanical confirmation via Node: `node -e "const s = require('./src/shared/logger.ts').redactString('Authorization: Bearer'); console.log(s)"` does NOT print `'Authorization=[REDACTED]'`. (If `redactString` is not exported, the test suite IS the contract — running it is sufficient.)
    - Source assertion: `grep -E '\(\?!bearer\\\\b\)' src/shared/logger.ts` returns 1 line (the negative lookahead is present).
    - Source assertion: `grep -c '\[=:\]' src/shared/logger.ts` returns ≥ 1 (separator capture group present).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/shared/logger.test.ts</automated>
  </verify>
  <done>
    Logger redaction preserves the input separator across both colon-and-equals forms; `'Authorization: Bearer'` (no trailing token) is no longer rendered as `'Authorization=[REDACTED]'`; all CR-01 / WR-02-separator regression guards green; documented contract in source comments matches actual behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Empty-baseUrl whitespace symmetry across all three guard sites (WR-03-whitespace)</name>
  <files>src/main.ts, src/ai/providers/openaiCompatible.ts, src/ai/providers/ollama.ts, tests/ai/probes.test.ts, tests/ai/probe-debounce.test.ts</files>
  <read_first>
    - src/main.ts lines 770-820 (the testActiveAIConnection method; the empty-baseUrl guard at lines 789-795 is the edit site; the surrounding apiKey guard at line 780 shows the established pattern)
    - src/ai/providers/openaiCompatible.ts lines 59-95 (probeCustom; the empty-baseUrl guard at line 70 is the edit site)
    - src/ai/providers/ollama.ts lines 23-48 (probeOllama; the empty-baseUrl guard at line 32 is the edit site)
    - tests/ai/probes.test.ts lines 78-125 (CR-02 empty-baseUrl describe block; the new whitespace fixtures append here)
    - tests/ai/probe-debounce.test.ts lines 170-213 (CR-02 main.ts guard describe block; the new whitespace fixtures append here)
    - .planning/phases/07-ai-provider-foundation/07-07-REVIEW.md WR-03 section at lines 140-161 (recommended fix sketch + test fixture pattern included there)
  </read_first>
  <behavior>
    - Test 1 (probeCustom whitespace single-space): `probeCustom({ ...emptyCfg, baseUrl: ' ' }, fetcherSpy)` → returns `{ ok: false, errorMessage: 'Base URL is required for Custom provider.' }`, fetcher invoked 0 times.
    - Test 2 (probeCustom whitespace tab): `probeCustom({ ...emptyCfg, baseUrl: '\t' }, fetcherSpy)` → same as Test 1, fetcher invoked 0 times.
    - Test 3 (probeCustom whitespace mixed): `probeCustom({ ...emptyCfg, baseUrl: '  \t  ' }, fetcherSpy)` → same as Test 1, fetcher invoked 0 times.
    - Test 4 (probeOllama whitespace single-space): `probeOllama({ ...emptyCfg, apiKey: '', baseUrl: ' ' }, fetcherSpy)` → returns `{ ok: false, errorMessage: 'Base URL is required for Ollama provider.' }`, fetcher invoked 0 times.
    - Test 5 (probeOllama whitespace mixed): `probeOllama({ ...emptyCfg, apiKey: '', baseUrl: '\t \t' }, fetcherSpy)` → same as Test 4, fetcher invoked 0 times.
    - Test 6 (testActiveAIConnection main.ts guard whitespace, custom): provider=custom + cfg.baseUrl=' ' → emits Notice `'Enter a Base URL for Custom (OpenAI-compatible) first.'` (3000ms), probe NOT called, aiProbeInflight remains size 0.
    - Test 7 (testActiveAIConnection main.ts guard whitespace, ollama): provider=ollama + cfg.baseUrl='\t' → emits Notice `'Enter a Base URL for Ollama first.'` (3000ms), probe NOT called.
    - Test 8 (regression — non-empty baseUrl with leading/trailing whitespace surrounding real content STILL probes): `baseUrl: ' https://example.com/v1 '` (real URL with leading/trailing whitespace) → fetcher IS called. (Confirms the guard rejects "all whitespace" only, not "any whitespace" — important so users with copy-paste artifacts don't get false-rejected.)
    - Test 9 (regression — existing `baseUrl: ''` strict-empty case still rejected at all three sites): existing CR-02 fixtures from 07-07 must continue to pass without modification.
  </behavior>
  <action>
    Edit `src/main.ts` (lines 789-795):

    1. Change the guard condition from `cfg.baseUrl === ''` to `!cfg.baseUrl?.trim()`. The full conditional becomes:
       ```
       if (
         (provider === 'custom' || provider === 'ollama') &&
         !cfg.baseUrl?.trim()
       ) {
       ```
       The `?.` is defensive: in the data-on-disk shape `cfg.baseUrl` is always a string (sanitizeProviderConfig coerces to '' for missing/non-string), so the optional-chain is belt-and-braces — leave it because it costs nothing and makes the guard robust against any future shape drift. The `!` flips both `undefined` (treat as missing) and `''` (after trim of whitespace-only) to `true`, which is exactly the rejection semantic we want.

    2. Update the comment block above the guard (lines 783-788) to mention the whitespace handling and the cross-site symmetry:
       - Replace the existing comment with text that names this Plan 07-08 / WR-03 fix and explicitly states "uses `!cfg.baseUrl?.trim()` to reject single-space / tab / mixed-whitespace inputs symmetrically with probeCustom and probeOllama".

    Edit `src/ai/providers/openaiCompatible.ts` (line 70 inside `probeCustom`):

    3. Change the guard from `if (!cfg.baseUrl) {` to `if (!cfg.baseUrl?.trim()) {`. The comment above the guard (lines 63-69) currently refers to "Phase 07 Plan 07 — CR-02 BLOCKER fix"; append a sentence noting "Phase 07 Plan 08 — WR-03-whitespace tightens this from `!cfg.baseUrl` (falsy) to `!cfg.baseUrl?.trim()` so single-space, tab, and mixed-whitespace inputs are also rejected — symmetric with main.ts testActiveAIConnection and probeOllama."

    Edit `src/ai/providers/ollama.ts` (line 32 inside `probeOllama`):

    4. Change the guard from `if (!cfg.baseUrl) {` to `if (!cfg.baseUrl?.trim()) {`. Append the same Plan 07-08 cross-reference sentence to the comment above (lines 27-31).

    Edit `tests/ai/probes.test.ts`:

    5. Append a new `describe('WR-03-whitespace empty-baseUrl guards — Plan 07-08 Task 2', ...)` block AFTER the existing CR-02 block (after line 125). Add Tests 1-5 from the Behavior list. The fixtures reuse the existing `emptyCfg` pattern with the `baseUrl` override set to the whitespace string under test. The `fetcherSpy.toHaveBeenCalledTimes(0)` assertion is the critical check — same shape as the existing CR-02 fixtures.

    Edit `tests/ai/probe-debounce.test.ts`:

    6. Append two new tests inside the existing `describe('LeetCodePlugin.testActiveAIConnection — Plan 07-04 Task 1', ...)` block, immediately after the two existing CR-02 main.ts-guard tests (after line 213). Tests 6 and 7 from the Behavior list. Use the existing `makeFake` + `callTestActiveAIConnection` helpers; the only change from the existing CR-02 tests is the `baseUrl` value (`' '` for Test 6, `'\t'` for Test 7) and the test name describing the whitespace edge.

    7. Add Test 8 (regression — leading/trailing whitespace around a real URL STILL probes) inside `tests/ai/probes.test.ts`'s new describe block. Use a fetcher mock that returns a happy 200 response and assert `fetcherSpy.toHaveBeenCalledTimes(1)` and `r.ok === true`. Note: probeCustom/probeOllama do NOT trim before constructing the URL, so this test will technically pass `' https://example.com/v1 '` to the fetcher with the leading/trailing whitespace. That is acceptable — the user input is malformed, but the guard's job is only to reject "all-whitespace" inputs that produce relative URLs. URL trimming is out of scope for this advisory cleanup. The fetcher mock just confirms `toHaveBeenCalledTimes(1)` to prove the guard didn't reject.

    Anti-requirements:
    - Do NOT trim `cfg.baseUrl` BEFORE storing it or BEFORE calling the fetcher. The advisory is about guard symmetry, not URL sanitization. Sanitization is a settings-store concern that's out of scope for this round.
    - Do NOT change the error-message strings at any of the three sites — the existing test fixtures assert exact text; any string change ripples into failed assertions in the existing CR-02 tests from 07-07.
    - Do NOT add a centralized helper (e.g. `isBlank(s)`) — three call sites is below the threshold for extraction, and inlining keeps the diff localized + easy to review.
    - Do NOT change the `prettyName(provider)` lookup — it already produces the correct user-facing labels ("Custom (OpenAI-compatible)", "Ollama").

    Per WR-03-whitespace advisory.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/ai/probes.test.ts` passes all tests (existing CR-02 + 4 new whitespace fixtures + 1 regression).
    - `npx vitest run tests/ai/probe-debounce.test.ts` passes all tests (existing CR-02 main.ts guard + 2 new whitespace fixtures).
    - Source assertion: `grep -c 'cfg\.baseUrl?\.trim()' src/main.ts` returns 1.
    - Source assertion: `grep -c 'cfg\.baseUrl?\.trim()' src/ai/providers/openaiCompatible.ts` returns 1.
    - Source assertion: `grep -c 'cfg\.baseUrl?\.trim()' src/ai/providers/ollama.ts` returns 1.
    - Source assertion: `grep -c "cfg\.baseUrl === ''" src/main.ts` returns 0 (the strict-equality guard is gone).
    - Existing CR-02 tests from 07-07 (the empty-string fixtures) continue to pass — `!cfg.baseUrl?.trim()` is true for `''` because trim of '' is '', and `!''` is true.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/ai/probes.test.ts tests/ai/probe-debounce.test.ts</automated>
  </verify>
  <done>
    All three empty-baseUrl guard sites use `!cfg.baseUrl?.trim()`; whitespace-only baseUrl is uniformly rejected (single space, tab, mixed); the existing CR-02 strict-empty fixtures continue to pass; no fetcher invoked for any whitespace-only input.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MockSettings.setProviderConfig + disclosure-gate persistence test (WR-01-test-gap) + full-suite check</name>
  <files>tests/ai/aiClient.test.ts</files>
  <read_first>
    - tests/ai/aiClient.test.ts entire file (the MockSettings interface at lines 25-29 and makeMockSettings at lines 31-43 are the edit sites; the new test appends to the `describe('Phase 07 AIClient — probe', ...)` block after line 90)
    - src/ai/AIClient.ts lines 60-110 (the `probe` method with its disclosure-gate Continue path at lines 86-99 — this is the code under test for the new fixture)
    - .planning/phases/07-ai-provider-foundation/07-07-REVIEW.md WR-01 section at lines 94-122 (recommended MockSettings extension included there)
  </read_first>
  <behavior>
    - Test 1 (MockSettings exposes setProviderConfig + persistence call shape): mock `requireDisclosure` returns `true`, `disclosureAcknowledged: false` in starting cfg, then call `client.probe('anthropic')`. Assert `setProviderConfigSpy` was called exactly once with `('anthropic', { ...startingCfg, disclosureAcknowledged: true })`. Also assert the adapter's probe was called (i.e., the gate Continue path proceeded to the adapter — not just early-returned). (WR-01-test-gap)
    - Test 2 (second probe call does not re-fire the disclosure helper): after Test 1 successfully persisted ack=true, the MockSettings's stateful `setProviderConfig` mutates the in-memory cfg map so subsequent `getProviderConfig('anthropic')` returns ack=true. Call `client.probe('anthropic')` a second time. Assert `requireDisclosure` mock was called exactly 1 time across both probes (the second call hits ack=true and skips the gate entirely). (Persistence wiring proof)
    - Test 3 (disclosure helper returns false → setProviderConfig NOT called, errorMessage === 'AI call cancelled'): mock `requireDisclosure` returns `false`, ack=false in cfg. Assert `setProviderConfigSpy` NOT called and probe returns `{ ok: false, errorMessage: 'AI call cancelled' }`. (Negative-path coverage — the cancel branch must NOT persist anything.)
  </behavior>
  <action>
    Edit `tests/ai/aiClient.test.ts`:

    1. Extend the `MockSettings` interface (lines 25-29) by adding `setProviderConfig: (p: string, cfg: Record<string, unknown>) => Promise<void>;` as a fourth method.

    2. Extend `makeMockSettings` (lines 31-43) to provide a STATEFUL default `setProviderConfig` that mutates an in-memory cfg map. Concretely, restructure the factory so an internal `Map<string, Record<string, unknown>>` (e.g. `cfgs`) backs both `getProviderConfig` and `setProviderConfig`:
       - `cfgs` is initialized from a starting cfg parameter (default to the existing 5-field shape used today).
       - `getProviderConfig(p)` returns `cfgs.get(p) ?? <default>`.
       - `setProviderConfig(p, cfg)` is `vi.fn(async (provider, next) => { cfgs.set(provider, next); })`.
       - The factory accepts an optional `startingCfg` override so individual tests can seed `disclosureAcknowledged: false`.

       Do NOT break the existing tests (probe routing, probe never throws, invoke null-provider, addCost delegation, WR-01 await test). All five existing tests use `disclosureAcknowledged: true` and don't reference setProviderConfig — their behavior is unchanged when the new method is present but unused.

    3. The `AIClient` constructor signature is `new AIClient(settings, requireDisclosure?)`. The existing tests don't pass `requireDisclosure`, which means the gate falls back to its default. For the three new tests, the constructor MUST pass an explicit `requireDisclosure` mock so the test controls the gate's outcome. Use `const requireDisclosureMock = vi.fn(async () => true);` (or false for Test 3) and pass it as the second constructor arg.

    4. Append three new tests to the `describe('Phase 07 AIClient — probe', ...)` block (after line 90 / before the closing `});`). Each test:
       - Calls `vi.clearAllMocks()` or uses a fresh `beforeEach` if needed (the existing `beforeEach` only resets `resolveAdapterMock` — that's still correct; the new mocks are scoped inside each `it`).
       - Constructs settings via `makeMockSettings({ startingCfg: { ...defaultCfg, disclosureAcknowledged: false } })`.
       - Mocks `resolveAdapter` to return an adapter with a `vi.fn() probe` that resolves `{ ok: true, modelCount: 1 }` (Tests 1+2) or is irrelevant (Test 3 short-circuits at the gate).
       - Test 2 specifically: after the first probe completes, assert the stateful map has been mutated (`getProviderConfigSpy` returns ack=true on second call). Then invoke `client.probe('anthropic')` again and assert `requireDisclosureMock` was still only called 1 time total.

    5. Add a Vitest assertion comment at the top of the new fixture block explaining the WR-01-test-gap closure: "These tests close the WR-01-test-gap from 07-07-REVIEW: the disclosure-gate Continue path was previously untestable because MockSettings lacked setProviderConfig. The persistence call (and the second-probe re-read) is now exercised at the unit level."

    6. The final task in this plan also runs the full vitest suite as a regression guard. The verify block below includes both the targeted file and the full suite.

    Anti-requirements:
    - Do NOT change `AIClient.ts` — all behavior under test is already correct; the gap is purely test-side.
    - Do NOT add a setProviderConfig assertion to the existing tests (probe routing, probe never throws, invoke null-provider, addCost) — those are happy-path with ack=true, the gate doesn't fire, and asserting `setProviderConfigSpy NOT called` would clutter unrelated tests.
    - Do NOT add an integration-style probe-debounce test for the disclosure persistence path — the existing probe-debounce coverage is for the testActiveAIConnection orchestration, not for the gate. Keep WR-01 closure scoped to aiClient.test.ts.

    Per WR-01-test-gap advisory.
  </action>
  <acceptance_criteria>
    - `npx vitest run tests/ai/aiClient.test.ts` passes all 7 tests (existing 4 + 3 new).
    - Source assertion: `grep -c 'setProviderConfig' tests/ai/aiClient.test.ts` returns ≥ 4 (interface decl + factory impl + at least 2 test assertions).
    - Source assertion: `grep -c "disclosureAcknowledged: false" tests/ai/aiClient.test.ts` returns ≥ 1 (the starting cfg for the gate-Continue test).
    - Full-suite regression: `npm test` returns exit 0 with the same passing count as 07-VERIFICATION reports (925+) plus the new tests added in this plan (Tasks 1, 2, 3 add ~10 new tests across 4 test files; expect ~935 passing, 3 skipped, 0 failed).
    - LC-isolation grep gate still passes: `bash scripts/check-no-obsidianfetch-in-lc.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/ai/aiClient.test.ts tests/ai/probes.test.ts tests/ai/probe-debounce.test.ts tests/shared/logger.test.ts && npm test && bash scripts/check-no-obsidianfetch-in-lc.sh</automated>
  </verify>
  <done>
    MockSettings exposes setProviderConfig with stateful semantics; AIClient.probe disclosure-gate Continue path is unit-tested for both persistence shape and second-call ack-respect; cancel branch is unit-tested for non-persistence; full vitest suite green with no regressions; LC-isolation gate green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| log-line emitter → log consumer | Redacted log strings cross from the redaction pipeline to console / file consumers. Untrusted: the source string content (may contain real secrets). |
| user-typed Settings input → testActiveAIConnection / probe* | User-controlled `cfg.baseUrl` crosses from Settings UI to network-call construction. Untrusted: malformed/whitespace inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-01 | Information Disclosure | redactString output for `'Authorization: Bearer'` (no token) | mitigate | Negative lookahead `(?!bearer\b)` in second alternate prevents the `Bearer` keyword from being consumed as a value; output is left clean. No real secret was ever exposed by the v07-07 shape; this fix is contract-fidelity, not a confidentiality fix. |
| T-07-08-02 | Information Disclosure | redactString separator output for non-Bearer headers | mitigate | Captured-separator group `([:=])` in second alternate replays the original separator in output — no information lost (the secret value is still redacted), but log readability and contract fidelity are restored. No new redaction surface; the value char class is unchanged. |
| T-07-08-03 | Tampering | testActiveAIConnection guard for whitespace-only baseUrl | mitigate | `!cfg.baseUrl?.trim()` rejects whitespace-only inputs at all three sites; aligned with provider-side guards. Pre-existing risk: a whitespace-only `baseUrl` falling through to fetcher could log a malformed URL containing the user's typed whitespace — not a secret leak (no auth header attached for empty-key custom/ollama), but a UX defect. Mitigation closes the UX defect symmetrically. |
| T-07-08-04 | Tampering | npm/pip/cargo installs | accept | No new dependencies introduced. All edits are to existing source files; no `npm install` runs in this plan. Skip Package Legitimacy gate. |
</threat_model>

<verification>
**Phase-level verification on plan completion:**

1. Logger regex behavior — Tasks 1's Category 5 suite is the contract; rerun `npx vitest run tests/shared/logger.test.ts` after each edit.
2. Empty-baseUrl guard symmetry — Task 2's WR-03-whitespace suite + the existing CR-02 fixtures must all be green: `npx vitest run tests/ai/probes.test.ts tests/ai/probe-debounce.test.ts`.
3. Disclosure-gate persistence — Task 3's three new tests in `tests/ai/aiClient.test.ts` exercise the previously-untested Continue path, the second-call ack-respect, and the cancel-branch non-persistence.
4. Full-suite regression — `npm test` passes with the new tests integrated; existing 925-test baseline grows by ~10 tests (5 logger, 5 whitespace, 3 disclosure-gate, ~13 net) — final count expected in the ~935-940 range.
5. CI gate — `bash scripts/check-no-obsidianfetch-in-lc.sh` continues to exit 0 (LC-isolation invariant unchanged by this plan).
6. No new TypeScript errors — `npx tsc --noEmit` exits 0 (the regex pattern change + guard expression change are type-safe; the `setProviderConfig` interface extension matches the actual SettingsStore method signature).
</verification>

<success_criteria>
- All four advisory findings closed: CR-01-A, WR-02-separator, WR-03-whitespace, WR-01-test-gap.
- All 12 original phase-07 truths from 07-VERIFICATION still hold (no regression to the existing 925-test passing count).
- `npx vitest run` exits 0 across all four touched test files.
- `npm test` exits 0 (full suite).
- `bash scripts/check-no-obsidianfetch-in-lc.sh` exits 0.
- `npx tsc --noEmit` exits 0.
- All four edit sites' inline comments cross-reference Plan 07-08 + the specific advisory ID being closed (CR-01-A / WR-02-separator / WR-03-whitespace / WR-01-test-gap) for traceability.
</success_criteria>

<output>
Create `.planning/phases/07-ai-provider-foundation/07-08-SUMMARY.md` when done. The SUMMARY should:
- Confirm all four advisory IDs are closed with the specific code change made for each.
- Note the final test counts before and after this plan's tests landed.
- Note any deviations from this plan's prescribed regex/guard form (e.g. if the executor chose normalization-form for CR-01-A vs. untouched-form, or chose a different lookahead structure).
- Confirm `bash scripts/check-no-obsidianfetch-in-lc.sh` and `npx tsc --noEmit` both pass.
</output>
