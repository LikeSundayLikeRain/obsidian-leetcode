---
phase: 07-ai-provider-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ai/types.ts
  - src/settings/SettingsStore.ts
  - src/shared/logger.ts
  - tests/ai/settingsStore.test.ts
  - tests/ai/types.test.ts
  - tests/shared/logger.test.ts
  - tests/ai/helpers/mockProvider.ts
autonomous: true
requirements: [AIPROV-01, AIPROV-02, AIPROV-06]
tags: [ai, settings-store, shape-guard, logger-redaction, foundation]
must_haves:
  truths:
    - "AIProvider, ProviderConfig, AICostLedger, ProbeResult, AIRequest, AIResponse types exported from src/ai/types.ts"
    - "PluginData carries activeAIProvider, providerConfigs (per-provider map), aiCostLedger after load"
    - "Malformed AI fields in data.json (non-string apiKey, non-https baseUrl, missing model, non-boolean disclosureAcknowledged, malformed ledger) all collapse to per-provider safe defaults — no crash, no partial state"
    - "Active provider switch preserves prior providers' apiKey/baseUrl/model/disclosureAcknowledged via providerConfigs map"
    - "Day-rollover-on-read: addCostLedger(usd) resets usdToday when local-day differs from ledger.date, then adds"
    - "Logger redacts apiKey, api_key, x-api-key, bearer, authorization at both object-key (REDACT) and value-level (SECRET_VALUE_PATTERN)"
  artifacts:
    - path: "src/ai/types.ts"
      provides: "AIProvider union + ProviderConfig + AICostLedger + ProbeResult + AIRequest + AIResponse interfaces"
      contains: "export type AIProvider"
    - path: "src/settings/SettingsStore.ts"
      provides: "PluginData fields activeAIProvider, providerConfigs, aiCostLedger + getters/setters + sanitize* shape-guards"
      contains: "providerConfigs"
    - path: "src/shared/logger.ts"
      provides: "Extended REDACT regex + SECRET_VALUE_PATTERN covering AI key field names + Bearer values"
      contains: "apikey"
  key_links:
    - from: "src/settings/SettingsStore.ts (load)"
      to: "isValidProviderId + sanitizeProviderConfig + sanitizeAICostLedger"
      via: "per-field shape-guard with safe-default fallback"
      pattern: "isValidProviderId|sanitizeProviderConfig|sanitizeAICostLedger"
    - from: "src/shared/logger.ts (sanitize / redactSecrets)"
      to: "extended REDACT + SECRET_VALUE_PATTERN"
      via: "regex match against apiKey/bearer/authorization tokens"
      pattern: "apikey|api_key|api-key|x-api-key|bearer"
---

<objective>
Lay the foundation every other Phase 07 plan plugs into: AI domain types, locked-schema PluginData fields with shape-guards, and logger redaction. This plan ships ZERO UI and ZERO HTTP — it produces the data + safety layer the rest of the phase consumes.

Purpose: Plans 02–06 must not log AI keys (Pitfall 5 / T-07-05) and must not crash on malformed `data.json` (T-07-01). Both protections must land BEFORE any provider adapter, dropdown, or palette command exists. Logger redaction is non-negotiable per CONTEXT line 232 — gates Plan 07-02 (which logs adapter errors).

Output: New `src/ai/types.ts`; extended `SettingsStore` (3 new PluginData fields + 5 new shape-guards + 4 getter/setter pairs + `addCostLedger`); extended `src/shared/logger.ts` (REDACT + SECRET_VALUE_PATTERN). Three new test files plus an extended `tests/shared/logger.test.ts`. No source file outside `src/ai/types.ts`, `src/settings/SettingsStore.ts`, `src/shared/logger.ts` is touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ai-provider-foundation/07-CONTEXT.md
@.planning/phases/07-ai-provider-foundation/07-RESEARCH.md
@.planning/phases/07-ai-provider-foundation/07-PATTERNS.md

@src/settings/SettingsStore.ts
@src/shared/logger.ts
@src/auth/types.ts
@src/browse/types.ts

<interfaces>
<!-- Existing exports executors will extend or import. Extracted from codebase. -->

From src/settings/SettingsStore.ts:
- `interface PluginData` — adds 3 new fields (activeAIProvider, providerConfigs, aiCostLedger) ALONGSIDE existing fields. Do NOT remove or reorder.
- `class SettingsStore` — `static async load(plugin: Plugin): Promise<SettingsStore>` (extend the per-field validation block at lines 306-343); `private async persist()`; existing getter/setter pattern at lines 368-435.
- `const DEFAULT_DATA: PluginData` (line 117) — extend with 3 new defaults.
- Existing shape-guards: `isValidAuthCookies`, `sanitizeFolder`, `isValidIndexedProblem`, `isValidProblemIndex`, `isValidFilterRule`, `isValidCompoundFilter`, `sanitizeCompoundFilter`, `isValidDetailCacheEntry`, `sanitizeProblemDetails`. Mirror the strictness; do NOT generalize them.
- Locked-schema posture exemplar (line 342): `previewClickBehavior: raw.previewClickBehavior === 'open' ? 'open' : 'preview'` — anything not literally a known string collapses to a safe default.

From src/shared/logger.ts:
- `const REDACT = /session|csrf|cookie|token/i;` (line 12)
- `const SECRET_VALUE_PATTERN = /\b(LEETCODE_SESSION|csrftoken|session|csrf|cookie|token|authorization)\s*[=:]\s*[^\s;,"'&}\]]+/gi;` (line 17)
- The redactSecrets walker iterates object keys; if `REDACT.test(k)` is true the value is replaced with `'[REDACTED]'`.

From src/auth/types.ts (analog for `src/ai/types.ts` shape):
```typescript
export interface AuthCookies {
  LEETCODE_SESSION: string;
  csrftoken: string;
}
```

From src/browse/types.ts (string-union analog):
- `diff: 'Easy' | 'Medium' | 'Hard'` — same pattern for `AIProvider` union.

</interfaces>

<canonical_decisions>
- D-A: Vercel AI SDK family — types must match SDK shape (apiKey + baseUrl + model). Phase 07-02 imports the SDK; Phase 07-01 only declares the data contract.
- D-B: `src/ai/` is the new module home. `types.ts` is the only file in this plan.
- D-C: Default base URLs / models per provider:
  - anthropic: `https://api.anthropic.com/v1`, `claude-haiku-4-5`
  - openai: `https://api.openai.com/v1`, `gpt-5-mini`
  - openrouter: `https://openrouter.ai/api/v1`, `anthropic/claude-haiku-4.5` (DOT, not dash)
  - ollama: `http://localhost:11434/v1`, `llama3.2`
  - custom: `''`, `''` (user must type)
- D-F: aiCostLedger { date: 'YYYY-MM-DD', usdToday: number }; `addCost(usd)` rolls over on read. NO cap enforcement, NO UI in Phase 07.
- T-07-01 mitigation: every new field has a shape-guard with safe-default fallback per CONTEXT line 226 ("anything not-literally-the-known-string falls through to default").
- T-07-05 mitigation: logger redaction MUST land in this plan, BEFORE Plan 07-02 imports any adapter that may log error/request payloads.
</canonical_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/ai/types.ts and Wave 0 test scaffolds</name>
  <files>src/ai/types.ts, tests/ai/types.test.ts, tests/ai/helpers/mockProvider.ts, tests/ai/settingsStore.test.ts, tests/shared/logger.test.ts</files>
  <read_first>
    - src/ai/types.ts (target — does not yet exist; new file)
    - src/auth/types.ts (analog — minimal interface shape, lines 1-4)
    - src/browse/types.ts (analog — string-union pattern for AIProvider, see `diff: 'Easy' | 'Medium' | 'Hard'`)
    - src/settings/SettingsStore.ts:117-132 (DEFAULT_DATA shape that Task 2 will extend)
    - tests/shared/errors.isNetworkError.test.ts (analog — vitest test layout used in this repo)
    - .planning/phases/07-ai-provider-foundation/07-PATTERNS.md §"src/ai/types.ts" (decision matrix)
    - .planning/phases/07-ai-provider-foundation/07-VALIDATION.md (Wave 0 file checklist)
  </read_first>
  <behavior>
    - `AIProvider` is a string union of exactly: `'anthropic' | 'openai' | 'openrouter' | 'ollama' | 'custom'`. No other values valid.
    - `ProviderConfig` interface has exactly four required string/boolean fields: `apiKey: string`, `baseUrl: string`, `model: string`, `disclosureAcknowledged: boolean`. No optionals.
    - `AICostLedger` interface has `date: string` (must look like 'YYYY-MM-DD'), `usdToday: number`.
    - `ProbeResult` interface has `ok: boolean`, optional `modelCount: number | null`, optional `errorMessage: string`.
    - `AIRequest` and `AIResponse` are placeholder interfaces (Phase 08 expands). Export them empty-but-named so Plan 07-02's AIClient.invoke() type-checks.
    - Wave 0 test files exist as scaffolds with at least one passing assertion each (placeholder `expect(true).toBe(true)` is acceptable; Tasks 2 + 3 will fill them with real cases).
    - `tests/ai/helpers/mockProvider.ts` exports a `makeFetcherMock()` factory returning `vi.fn()` typed as `FetchFn` (signature `(input: RequestInfo | URL, init?: RequestInit) => Promise<Response>`) — used by Plans 02–06 tests.
  </behavior>
  <action>
    Create `src/ai/types.ts` with the EXACT exports listed in <behavior> — no extra fields, no JSDoc beyond a one-line file header citing CONTEXT decision A + 07-PATTERNS §"src/ai/types.ts". The string union order is locked: `'anthropic' | 'openai' | 'openrouter' | 'ollama' | 'custom'` so iteration order is deterministic across consumers (per D-04 reuse precedent in browse/types.ts).

    Use lower-case-string union for `AIProvider` (mirrors `IndexedProblem.diff`'s `'Easy' | 'Medium' | 'Hard'` precedent in browse/types.ts:6 — string-union, not enum, no const-as-const).

    Create the empty-but-named `interface AIRequest {}` and `interface AIResponse {}` so they round-trip through Plan 02's `AIClient.invoke(req: AIRequest): Promise<AIResponse>` signature without a type-only modifier. Add a JSDoc on each saying "Phase 08 expands this — see Phase 07 plan_hints". Do NOT add fields speculatively.

    Create the four Wave 0 test files (`tests/ai/types.test.ts`, `tests/ai/settingsStore.test.ts`, `tests/shared/logger.test.ts`, `tests/ai/helpers/mockProvider.ts`) so Tasks 2 and 3 can populate them. `tests/ai/types.test.ts` must contain ONE real assertion: `expect(['anthropic','openai','openrouter','ollama','custom'] as AIProvider[]).toHaveLength(5);` (the cast itself is the type test — fails compile if the union changes).

    `tests/ai/helpers/mockProvider.ts` exports `makeFetcherMock()` returning `vi.fn()` with the FetchFn signature, plus a `mockResponse(body: unknown, init?: ResponseInit)` helper that returns `new Response(JSON.stringify(body), init)` for use in Plan 04 probe tests.

    Do NOT create `src/ai/AIClient.ts`, `src/ai/obsidianFetch.ts`, or any provider adapter — those belong to Plan 07-02. Do NOT add anything to `src/settings/` or `src/shared/` — those belong to Tasks 2 and 3 below.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/ai/types.test.ts tests/ai/helpers/mockProvider.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - File `src/ai/types.ts` exists.
    - `grep -c "^export type AIProvider" src/ai/types.ts` returns 1.
    - `grep -c "'anthropic' | 'openai' | 'openrouter' | 'ollama' | 'custom'" src/ai/types.ts` returns 1.
    - `grep -c "^export interface ProviderConfig" src/ai/types.ts` returns 1.
    - `grep -c "^export interface AICostLedger" src/ai/types.ts` returns 1.
    - `grep -c "^export interface ProbeResult" src/ai/types.ts` returns 1.
    - `grep -c "^export interface AIRequest" src/ai/types.ts` returns 1.
    - `grep -c "^export interface AIResponse" src/ai/types.ts` returns 1.
    - `npx tsc --noEmit` exits 0 (whole-project type-check).
    - `tests/ai/types.test.ts`, `tests/ai/settingsStore.test.ts`, `tests/shared/logger.test.ts`, `tests/ai/helpers/mockProvider.ts` all exist.
  </acceptance_criteria>
  <done>
    types.ts compiles, exports the locked union and three interfaces, Wave 0 test scaffolds exist, and `tests/ai/helpers/mockProvider.ts` exports a working `makeFetcherMock()` + `mockResponse()` helper consumable by later plans.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend SettingsStore — PluginData fields + shape-guards + getters/setters + addCostLedger</name>
  <files>src/settings/SettingsStore.ts, tests/ai/settingsStore.test.ts</files>
  <read_first>
    - src/settings/SettingsStore.ts (target — extend in place)
    - src/ai/types.ts (Task 1 output — import AIProvider, ProviderConfig, AICostLedger)
    - src/settings/SettingsStore.ts:88-95 (`previewClickBehavior` PluginData field — exact analog for the locked-schema posture)
    - src/settings/SettingsStore.ts:117-132 (DEFAULT_DATA literal — extend, do NOT replace)
    - src/settings/SettingsStore.ts:139-143 (`isValidAuthCookies` — minimal shape-guard analog)
    - src/settings/SettingsStore.ts:240-281 (`isValidDetailCacheEntry` — full-field shape-guard analog)
    - src/settings/SettingsStore.ts:298-365 (`load` per-field validation block — insertion site)
    - src/settings/SettingsStore.ts:368-435 (existing getter/setter pattern)
    - .planning/phases/07-ai-provider-foundation/07-PATTERNS.md §"src/settings/SettingsStore.ts (modify)" (verbatim shape-guard recipes)
    - .planning/phases/07-ai-provider-foundation/07-RESEARCH.md §"Security Domain" V5 row (validation rules per field)
  </read_first>
  <behavior>
    - PluginData has three new required fields: `activeAIProvider: AIProvider | null`, `providerConfigs: Record<AIProvider, ProviderConfig>`, `aiCostLedger: AICostLedger`. All three present after `SettingsStore.load` for fresh installs and corrupt-data recovery alike.
    - `DEFAULT_PROVIDER_CONFIGS` constant defines exactly the 5 entries from <canonical_decisions> with `apiKey: ''`, `disclosureAcknowledged: false` for each.
    - `isValidProviderId(v)` returns true only for the 5 literal strings; any other value (including `''`, `null`, undefined, numbers, objects) returns false.
    - `sanitizeProviderConfig(raw, defaults)` returns `defaults` if raw is null/non-object; otherwise builds a clean object where: `apiKey` non-string → `''`; `baseUrl` non-string OR not matching `/^https?:\/\//` → `defaults.baseUrl`; `model` non-string → `defaults.model`; `disclosureAcknowledged` strict-true (only literal `true` accepted, mirrors `legacyBaseNoticeShown` at line 320).
    - `sanitizeAICostLedger(raw)` returns `{ date: today, usdToday: 0 }` if raw is null/non-object; otherwise: `date` non-`/^\d{4}-\d{2}-\d{2}$/` → today's local-day; `usdToday` non-finite or negative → 0.
    - `load()` per-field block applies sanitizers BEFORE constructing the `data` literal — every malformed field falls back to its default WITHOUT crashing the load.
    - Getters: `getActiveAIProvider(): AIProvider | null`, `getProviderConfig(p: AIProvider): ProviderConfig`, `getAICostLedger(): AICostLedger`. Setters: `setActiveAIProvider(p: AIProvider | null)`, `setProviderConfig(p: AIProvider, cfg: ProviderConfig)`. All async, all call `persist()`.
    - `addCostLedger(usd: number): Promise<void>` reads `new Date().toISOString().slice(0,10)` AT CALL TIME; if it differs from `aiCostLedger.date`, replaces ledger with `{ date: today, usdToday: 0 }` BEFORE adding `usd`. Adds nothing if `usd` is non-finite or negative (silent guard — matches v1.0 throttle posture).
    - Switching `activeAIProvider` from X→Y leaves `providerConfigs[X]` untouched (incl. apiKey, disclosureAcknowledged). Only `activeAIProvider` itself changes.
    - Test `tests/ai/settingsStore.test.ts` covers each malformed-input → default-fallback case as a separate `test()` block (one assertion per behavior — no test consolidation).
  </behavior>
  <action>
    Insert in `src/settings/SettingsStore.ts` per CONTEXT line 226 + 07-PATTERNS §"src/settings/SettingsStore.ts". The file currently ends at line 517 with `private async persist()`. Make these surgical edits:

    1. Add `import type { AIProvider, ProviderConfig, AICostLedger } from '../ai/types';` near the existing type imports (after line 7).

    2. Inside `interface PluginData` (line 49-96), AFTER `previewClickBehavior` (line 95), add the three new fields with JSDoc citing Phase 07 AIPROV-01 / AIPROV-06 / decision F.

    3. Above the existing `DEFAULT_DATA` literal (before line 117), add `const DEFAULT_PROVIDER_CONFIGS: Record<AIProvider, ProviderConfig> = { ... };` with the 5 entries verbatim from <canonical_decisions>.

    4. Inside `DEFAULT_DATA` (lines 117-132), AFTER `previewClickBehavior: 'preview'`, add: `activeAIProvider: null`, `providerConfigs: DEFAULT_PROVIDER_CONFIGS` (reference, not spread), `aiCostLedger: { date: new Date().toISOString().slice(0, 10), usdToday: 0 }`.

    5. Add three module-private functions BELOW the existing shape-guards (after `sanitizeProblemDetails` at line 293):
       - `isValidProviderId(v: unknown): v is AIProvider`
       - `sanitizeProviderConfig(raw: unknown, defaults: ProviderConfig): ProviderConfig`
       - `sanitizeAICostLedger(raw: unknown): AICostLedger`

    Each must be a one-direction guard (no exceptions thrown) per the strictness exemplar at line 342. Use the EXACT regex `/^https?:\/\//` for baseUrl validation (admits both http and https — Ollama and any localhost custom URL need http; Anthropic/OpenAI/OpenRouter all use https).

    6. Inside `static async load` (lines 298-365), AFTER `previewClickBehavior` line 342, append three new field assignments inside the `data: PluginData = { ... }` literal:
       - `activeAIProvider: isValidProviderId(raw.activeAIProvider) ? raw.activeAIProvider : null,`
       - `providerConfigs: { anthropic: sanitizeProviderConfig((raw.providerConfigs as Record<string, unknown> | undefined)?.anthropic, DEFAULT_PROVIDER_CONFIGS.anthropic), openai: sanitizeProviderConfig(..., DEFAULT_PROVIDER_CONFIGS.openai), openrouter: ..., ollama: ..., custom: ... },`
       - `aiCostLedger: sanitizeAICostLedger(raw.aiCostLedger),`

    Each line must structurally match the surrounding pattern at lines 308-342 (single-line ternary or one-line function call).

    7. Add five new public methods to the SettingsStore class, INSERTED before `persist()` at line 514 and grouped as `// --- Phase 07 AI ---` block:
       - `getActiveAIProvider(): AIProvider | null { return this.data.activeAIProvider; }`
       - `async setActiveAIProvider(p: AIProvider | null): Promise<void> { this.data.activeAIProvider = p; await this.persist(); }`
       - `getProviderConfig(p: AIProvider): ProviderConfig { return this.data.providerConfigs[p]; }`
       - `async setProviderConfig(p: AIProvider, cfg: ProviderConfig): Promise<void> { this.data.providerConfigs[p] = sanitizeProviderConfig(cfg, DEFAULT_PROVIDER_CONFIGS[p]); await this.persist(); }` — note: setter ALSO sanitizes so a future caller that passes a malformed object (e.g. command-layer typo) doesn't poison data.json.
       - `getAICostLedger(): AICostLedger { return this.data.aiCostLedger; }`
       - `async addCostLedger(usd: number): Promise<void> { if (!Number.isFinite(usd) || usd < 0) return; const today = new Date().toISOString().slice(0,10); if (this.data.aiCostLedger.date !== today) { this.data.aiCostLedger = { date: today, usdToday: 0 }; } this.data.aiCostLedger.usdToday += usd; await this.persist(); }`

    8. Populate `tests/ai/settingsStore.test.ts` (created in Task 1) with these exact test cases (one `test()` block per case, all using vitest `describe`):
       - `'load: missing activeAIProvider falls back to null'` — pass `{}` raw data, assert `getActiveAIProvider() === null`.
       - `'load: invalid activeAIProvider string falls back to null'` — pass `{ activeAIProvider: 'gemini' }`, assert null.
       - `'load: missing providerConfigs hydrates with all 5 defaults'` — pass `{}`, assert `getProviderConfig('anthropic').baseUrl === 'https://api.anthropic.com/v1'` and same for the other 4.
       - `'load: malformed providerConfigs.anthropic.apiKey (number) collapses to empty string'` — pass `{ providerConfigs: { anthropic: { apiKey: 42, baseUrl: 'https://x', model: 'm', disclosureAcknowledged: true } } }`, assert `getProviderConfig('anthropic').apiKey === ''`.
       - `'load: non-https baseUrl falls back to provider default'` — pass `{ providerConfigs: { anthropic: { apiKey: 'k', baseUrl: 'ftp://evil', model: 'm', disclosureAcknowledged: true } } }`, assert `getProviderConfig('anthropic').baseUrl === 'https://api.anthropic.com/v1'`.
       - `'load: http://localhost is accepted (Ollama)'` — pass `{ providerConfigs: { ollama: { apiKey: '', baseUrl: 'http://localhost:11434/v1', model: 'm', disclosureAcknowledged: false } } }`, assert preserved.
       - `'load: disclosureAcknowledged accepts only literal true'` — pass `'yes'`, assert resulting `disclosureAcknowledged === false`.
       - `'load: malformed aiCostLedger.date falls back to today'` — pass `{ aiCostLedger: { date: 'yesterday', usdToday: 5 } }`, assert `getAICostLedger().date === new Date().toISOString().slice(0,10)` AND `usdToday === 0` (the date+usd reset together).
       - `'load: negative usdToday falls back to 0'` — pass `{ aiCostLedger: { date: '2026-05-15', usdToday: -1 } }`, assert `usdToday === 0`.
       - `'addCostLedger same day accumulates'` — set ledger to today + 1.0, call `addCostLedger(0.5)`, assert `usdToday === 1.5`.
       - `'addCostLedger different day rolls over before adding'` — set ledger to date '2020-01-01' + usdToday 999, call `addCostLedger(0.1)`, assert `date === today AND usdToday === 0.1` (NOT 999.1).
       - `'addCostLedger non-finite usd is silently ignored'` — call `addCostLedger(NaN)`, assert ledger unchanged.
       - `'switching activeAIProvider preserves prior provider apiKey'` — set anthropic.apiKey = 'sk-ant', then `setActiveAIProvider('openai')`, then `setActiveAIProvider('anthropic')`, assert `getProviderConfig('anthropic').apiKey === 'sk-ant'` (T-07-01 + AIPROV-01 invariant).

    Use `Plugin` test double from existing `tests/settings/*.test.ts` patterns; if no helper exists, inline a minimal `{ loadData: async () => raw, saveData: async () => {} }` mock in the test file.

    Do NOT modify the logger (Task 3). Do NOT add UI (Plan 07-03). Do NOT add an API key clearing helper (Plan 07-06). Do NOT add `clearActiveKey()` to SettingsStore — that command lives on the plugin class in Plan 07-06; SettingsStore exposes only the per-field setters.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/ai/settingsStore.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "activeAIProvider" src/settings/SettingsStore.ts` returns >= 6 (interface + DEFAULT_DATA + load + 2 getter/setter + 1+ comment).
    - `grep -c "providerConfigs" src/settings/SettingsStore.ts` returns >= 6.
    - `grep -c "aiCostLedger" src/settings/SettingsStore.ts` returns >= 5.
    - `grep -c "DEFAULT_PROVIDER_CONFIGS" src/settings/SettingsStore.ts` returns >= 6 (declaration + 5 references in load).
    - `grep -E "isValidProviderId|sanitizeProviderConfig|sanitizeAICostLedger" src/settings/SettingsStore.ts | wc -l` returns >= 7 (3 declarations + 4 callsites: load uses sanitizeProviderConfig × 5 + sanitizeAICostLedger × 1 + isValidProviderId × 1, plus setProviderConfig uses sanitizeProviderConfig).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run tests/ai/settingsStore.test.ts -x` reports >= 13 passing tests (one per case enumerated in <action>).
    - `npx vitest run tests/settings/ -x` (existing tests) still pass — no regression.
  </acceptance_criteria>
  <done>
    SettingsStore loads malformed AI fields safely, switching provider preserves prior keys, day-rollover works, all 13 unit tests green, existing test suite green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend logger redaction for AI key fields and Authorization values</name>
  <files>src/shared/logger.ts, tests/shared/logger.test.ts</files>
  <read_first>
    - src/shared/logger.ts (target — file is small, read entirely)
    - .planning/phases/07-ai-provider-foundation/07-PATTERNS.md §"src/shared/logger.ts (modify)"
    - .planning/phases/07-ai-provider-foundation/07-RESEARCH.md §"Common Pitfalls" Pitfall 5 (plain-text key storage) and §"Security Domain" V8 (Data Protection control list)
    - tests/shared/errors.isNetworkError.test.ts (analog vitest layout for tests/shared/)
  </read_first>
  <behavior>
    - `REDACT` regex matches the existing v1.0 tokens AND new AI tokens: `apikey`, `api_key` (treat the underscore variant), `bearer`. The regex is case-insensitive.
    - `SECRET_VALUE_PATTERN` matches the existing v1.0 tokens AND `apikey`, `api_key`, `api-key`, `x-api-key`, `bearer` as the key-side of a `key=value` or `key: value` capture, replacing the value with `[REDACTED]` while preserving the key.
    - Object-key redaction: `{ apiKey: 'sk-ant-xyz' }` → `{ apiKey: '[REDACTED]' }`. Key matching is case-insensitive (so `ApiKey`, `APIKEY` all hit).
    - Nested-object redaction at depth >= 3: `{ providerConfigs: { anthropic: { apiKey: 'sk-...' } } }` walks into the nested object and redacts at the leaf. The existing walker already handles depth — verify it still does after the regex extension.
    - Value-level redaction in stringified messages: `Authorization: Bearer sk-xyz` and `x-api-key: sk-ant` both become `[REDACTED]`-suffixed forms.
    - Non-secret keys (e.g. `model`, `baseUrl`, `provider`) are NOT redacted — only fields that match the REDACT regex.
  </behavior>
  <action>
    Make exactly two edits in `src/shared/logger.ts`:

    1. Replace line 12: `const REDACT = /session|csrf|cookie|token/i;`
       with:           `const REDACT = /session|csrf|cookie|token|apikey|api[_-]?key|bearer|authorization/i;`

       The `api[_-]?key` form covers `apikey`, `api_key`, `api-key`, and `apiKey` (case-insensitive flag handles the camel case). The `authorization` token is added so any field literally named `authorization` (e.g. raw HTTP header captures) redacts. The old tokens stay in the same order so v1.0 redaction tests remain green.

    2. Replace line 17: `const SECRET_VALUE_PATTERN = /\b(LEETCODE_SESSION|csrftoken|session|csrf|cookie|token|authorization)\s*[=:]\s*[^\s;,"'&}\]]+/gi;`
       with:           `const SECRET_VALUE_PATTERN = /\b(LEETCODE_SESSION|csrftoken|session|csrf|cookie|token|authorization|apikey|api[_-]?key|x-api-key|bearer)\s*[=:]\s*[^\s;,"'&}\]]+/gi;`

       The new alternates capture: `apikey=...`, `api_key=...`, `api-key=...`, `x-api-key: ...` (Anthropic header), `bearer sk-...` (Authorization header value substring captured by the previous `authorization` AND a separate `bearer` rule for stack traces that flatten the header into `Bearer sk-...` without the `authorization:` prefix). Note: Anthropic uses `x-api-key` (with hyphen) — explicit alternate; the regex's `\b(...)` boundary handles it.

    Do NOT change anything else in the file. Do NOT add new exported helpers. Do NOT modify the `redactSecrets` walker — it already iterates keys.

    Populate `tests/shared/logger.test.ts` with the following test cases (vitest `describe` per category):

    Category 1 — REDACT key-name redaction:
    - `'redacts a top-level apiKey field'` — `logger.warn({ apiKey: 'sk-ant-xyz' })`; spy on console.warn; assert the recorded payload has `apiKey: '[REDACTED]'` AND the literal `'sk-ant-xyz'` does NOT appear in the captured string.
    - `'redacts a top-level api_key field (snake_case)'` — same as above with key `api_key`.
    - `'redacts an x-api-key header object key'` — `logger.warn({ headers: { 'x-api-key': 'sk-ant-xyz' } })`; assert nested redaction works (the existing walker handles nesting; this confirms the new regex hits hyphenated keys via the case-insensitive flag).
    - `'redacts deeply nested providerConfigs.anthropic.apiKey'` — `logger.warn({ providerConfigs: { anthropic: { apiKey: 'sk-ant-xyz' } } })`; assert depth-3 redaction.
    - `'does NOT redact a model field'` — `logger.warn({ model: 'claude-haiku-4-5' })`; assert `'claude-haiku-4-5'` IS present in the output (no false-positive on non-secret fields).
    - `'does NOT redact a baseUrl field'` — `logger.warn({ baseUrl: 'https://api.anthropic.com/v1' })`; assert URL preserved.

    Category 2 — SECRET_VALUE_PATTERN value-level redaction in strings:
    - `'redacts Bearer token in Authorization header string'` — `logger.warn('Authorization: Bearer sk-xyz')`; assert `'sk-xyz'` not present, `'[REDACTED]'` present.
    - `'redacts x-api-key value in stringified header'` — `logger.warn('x-api-key: sk-ant-xyz')`; assert `'sk-ant-xyz'` not present.
    - `'preserves the key name on value-level redaction'` — `logger.warn('Authorization: Bearer sk-xyz')`; assert output still contains `'Authorization'`.

    Category 3 — v1.0 regression (existing redaction must still work):
    - `'still redacts LEETCODE_SESSION (v1.0 regression)'` — `logger.warn({ LEETCODE_SESSION: 'cookie-value' })`; assert `'cookie-value'` not present.
    - `'still redacts csrftoken value-level (v1.0 regression)'` — `logger.warn('csrftoken=abc123')`; assert `'abc123'` not present.

    Spy via `vi.spyOn(console, 'warn').mockImplementation(() => {})` and inspect `mockCalls` for assertions. Use a `beforeEach` to reset the spy and an `afterEach` to restore.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/shared/logger.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "apikey|api[_-]?key|bearer|authorization" src/shared/logger.ts | wc -l` returns >= 2 (one in REDACT, one in SECRET_VALUE_PATTERN).
    - `grep -c "x-api-key" src/shared/logger.ts` returns 1 (only in SECRET_VALUE_PATTERN; not as a separate constant).
    - `npx vitest run tests/shared/logger.test.ts -x` reports >= 11 passing tests (6 in Category 1, 3 in Category 2, 2 in Category 3).
    - `npx vitest run tests/shared/ -x` (full shared suite) green — no v1.0 regression.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    Logger redacts every AI key field name and Bearer/x-api-key value at both the object-key and string-value levels; v1.0 redaction tests continue to pass.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → plugin runtime | `data.json` may have been edited (manually, by another tool, or by a prior buggy version). Every field is untrusted on load. |
| plugin runtime → console / log files | Errors with provider state may be logged via `console.warn` / `console.error` / `console.debug`; Obsidian and external tools may capture these streams. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `SettingsStore.load` (PluginData hydration) | mitigate | Per-field shape-guards (`isValidProviderId`, `sanitizeProviderConfig`, `sanitizeAICostLedger`) collapse every malformed input to a per-provider safe default. Posture mirrors `previewClickBehavior` at line 342. Tests in Task 2 cover non-string apiKey, non-https baseUrl, malformed ledger date, negative usdToday, unknown activeAIProvider value. Same posture as v1.0 D-14 + Phase 06 PREVIEW-02. |
| T-07-05 | Information Disclosure | `src/shared/logger.ts` (any console output containing AI state) | mitigate | REDACT regex extended to match `apikey`, `api[_-]?key`, `bearer`, `authorization` keys; SECRET_VALUE_PATTERN extended to match the same names plus `x-api-key` for Anthropic-specific value-level redaction. Logger redaction MUST land BEFORE Plan 07-02 imports any provider adapter (CONTEXT line 232 — non-negotiable). Task 3 ships the regex extension; tests assert no AI key string survives a stringified `console.warn` call. |
| T-07-01-b | Tampering | `setProviderConfig(p, cfg)` (write path) | mitigate | Setter calls `sanitizeProviderConfig(cfg, DEFAULT_PROVIDER_CONFIGS[p])` BEFORE persisting, so a buggy command-layer caller in Plan 07-06 cannot poison data.json with malformed input. |

</threat_model>

<verification>
Run after all tasks complete:

```bash
# Whole-project type-check (Task 1 + 2 + 3 must compile together)
npx tsc --noEmit

# Phase 07 Wave 1 test suite — all three task test files
npx vitest run tests/ai/types.test.ts tests/ai/settingsStore.test.ts tests/shared/logger.test.ts -x

# v1.0 regression — existing settings + shared suites must still be green
npx vitest run tests/settings/ tests/shared/ -x

# Lint — clean ID + no innerHTML rules don't fire here, but the suite must be green
npm run lint

# Build — confirm types.ts + SettingsStore + logger don't break the production bundle
# (No size delta expected; AI SDK packages don't land until Plan 07-02)
npm run build
```

All five commands exit 0. Bundle size unchanged from Phase 06 baseline (~163 KB ± 1 KB).
</verification>

<success_criteria>

This plan is complete when:

1. `src/ai/types.ts` exists and exports `AIProvider`, `ProviderConfig`, `AICostLedger`, `ProbeResult`, `AIRequest`, `AIResponse`.
2. `PluginData` has three new fields (`activeAIProvider`, `providerConfigs`, `aiCostLedger`); `DEFAULT_DATA` includes their defaults; `SettingsStore.load` applies the three new shape-guards.
3. `SettingsStore` exposes `getActiveAIProvider`, `setActiveAIProvider`, `getProviderConfig`, `setProviderConfig`, `getAICostLedger`, `addCostLedger`.
4. Switching `activeAIProvider` from X to Y leaves `providerConfigs[X]` byte-for-byte unchanged (asserted by test).
5. `addCostLedger(usd)` rolls over on local-day change before adding (asserted by test).
6. `src/shared/logger.ts` redacts AI key fields and Bearer/x-api-key values (asserted by 11+ tests).
7. All three test files pass; v1.0 settings + shared regression suites stay green.
8. `npm run lint` and `npm run build` exit 0; bundle size unchanged.

</success_criteria>

<output>
After completion, create `.planning/phases/07-ai-provider-foundation/07-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Capture: files changed (types.ts, SettingsStore.ts, logger.ts), test count added, posture decisions (locked-schema like previewClickBehavior, strict-true for booleans, day-rollover-on-read), gates downstream (Plan 07-02 may now log adapter errors safely; Plan 07-03 may now read PluginData fields).
</output>
