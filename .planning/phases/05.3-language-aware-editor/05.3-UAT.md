---
status: in_progress
phase: 05.3-language-aware-editor
source: [05.3-04-PLAN.md]
started: 2026-05-11T20:54:51Z
updated: 2026-05-11T20:54:51Z
plugin_path: /Users/moxu/Documents/Leetcode/.obsidian/plugins/leetcode/
main_js_bytes: 532496
main_js_kb: 520
baseline_kb: 145
delta_kb: 374
ship_cap_kb: 700
---

# Phase 05.3 — D-14 LOCKED Live-Smoke UAT

**Test environment:** /Users/moxu/Documents/Leetcode (vault) — `leetcode` plugin

## Pre-flight (already verified by orchestrator)

- ✓ esbuild build → single main.js (532,496 bytes / 520 KB)
- ✓ `bash scripts/check-bundle-size.sh` → exits 0
- ✓ `npm test -- --run` → 560/562 (2 pre-existing skips)
- ✓ `ls main.js*.js | wc -l` → 0 chunks (D-08 / A8 verified)
- ⚠ `tsc -noEmit` — TS18048 errors in `tests/solve/SessionExpiredNotice.test.ts` are pre-existing on base commit `644e811` (out of scope)
- ⚠ `npm run lint` — 74 errors / 16 warnings, all pre-existing in `src/browse`, `src/graph`, `src/solve` (none in 5.3 sources)

---

## Current Test

[Section A — start here]

## Tests

### Section A — Supported languages in Live Preview (core behavior)

#### Python (python3 slug)
1. **A1.** Open an lc-slug Python note in Live Preview. Click inside the `## Code` fence.
   expected: Caret lands inside fence; no errors
   result: [pending]

2. **A2.** Verify syntax highlighting in the fence
   expected: Keywords (`def`, `class`, `return`, `if`, `else`, `for`, `while`, `import`, `from`) render in theme keyword color; strings in string color; `# comment` lines in comment color
   result: [pending]

3. **A3.** Type `def foo():` then Enter
   expected: Next line indents 4 spaces (or the user's configured Editor→Tab indent width)
   result: [pending]

4. **A4.** Continue: type `return 1` then Enter
   expected: Cursor stays at same indent
   result: [pending]

5. **A5.** Type `else:` at start of a new line
   expected: Dedents relative to its matching `if:` block (lang-python `indentNodeProp` handles this)
   result: [pending]

6. **A6.** Type `(` — matching `)` auto-inserted; same for `[`, `{`, `"`, `'`
   expected: Matching close character auto-inserted in all 5 cases
   result: [pending]

#### Java
7. **A7.** Open an lc-slug Java note (or change a fence tag to `java`); verify keyword coloring
   expected: `class`, `public`, `static`, `return` highlighted in keyword color
   result: [pending]

8. **A8.** Type `public class Solution {` then Enter
   expected: Next line indents one level
   result: [pending]

9. **A9.** Type `}` at start of a new line
   expected: Dedents to match the opening `{`
   result: [pending]

10. **A10.** Type `(` → `)` inserted; type `"` → `"` inserted
    expected: Matching close inserted
    result: [pending]

#### C++ (cpp slug) and C (c slug — same parser)
11. **A11.** Open an lc-slug note with a `cpp` fence; type `#include <vector>`
    expected: `#include` highlights as a preprocessor directive
    result: [pending]

12. **A12.** Type `int main() {` then Enter
    expected: Indent on Enter; `}` dedents
    result: [pending]

13. **A13.** Open an lc-slug note with a `c` fence
    expected: Same C-family indent behavior (shared `@codemirror/lang-cpp` grammar per D-05)
    result: [pending]

#### JavaScript / TypeScript
14. **A14.** `javascript` fence: type `const arr = [1, 2, 3];`
    expected: Brackets highlighted, numbers colored
    result: [pending]

15. **A15.** `javascript` fence: type `function f() {` then Enter
    expected: Indent; `}` dedents
    result: [pending]

16. **A16.** `typescript` fence: type `interface Foo { bar: number; }`
    expected: `interface` and `number` colored as TS types (`lang-javascript({ typescript: true })`)
    result: [pending]

#### Go (golang slug)
17. **A17.** Open an lc-slug note with a `golang` fence; type `func main() {` then Enter
    expected: Indent; `}` dedents
    result: [pending]

18. **A18.** Verify Go keywords colored distinctly
    expected: `func`, `package`, `import`, `return`, `if`, `for`, `var` colored
    result: [pending]

#### Rust
19. **A19.** Open an lc-slug note with a `rust` fence; type `fn main() {` then Enter
    expected: Indent
    result: [pending]

20. **A20.** Type `let x: &'a str = "hi";`
    expected: Lifetime `'a` colored distinctly from string literal
    result: [pending]

### Section B — Unsupported languages (D-06 whitespace-copy fallback)

21. **B1.** Open an lc-slug note and change fence tag to `csharp`
    expected: Fence content renders as plain monospace (no keyword coloring)
    result: [pending]

22. **B2.** Inside the `csharp` fence, type `public class Foo` Enter (from column 0)
    expected: Next line inherits leading whitespace from previous line (zero → starts at col 0)
    result: [pending]

23. **B3.** Indent 4 spaces + type some text Enter
    expected: Next line has 4 leading spaces (whitespace-copy works for non-zero indents)
    result: [pending]

24. **B4.** Type `{` in the csharp fence
    expected: NO matching `}` auto-inserted (D-06 — fallback is whitespace-copy ONLY; no bracket auto-close for unsupported)
    result: [pending]

25. **B5.** Repeat 21–24 for `ruby` and `mysql` fences
    expected: Both fall back identically to csharp
    result: [pending]

26. **B6.** Critical fail check
    expected: NO crash, NO error Notice, NO disabled editor; editor continues to accept input
    result: [pending]

### Section C — lc-slug gate + `## Code` scope (D-12 + D-13)

27. **C1.** Open a NON-lc-slug note (no `lc-slug:` frontmatter) and add a `python` fence; type `def foo():` Enter
    expected: Next line should NOT auto-indent using Python rules — Obsidian's markdown default applies. No Lezer syntax highlighting (some themes use Prism — that's fine; Lezer should NOT paint)
    result: [pending]

28. **C2.** Open an lc-slug note with python fence under `## Problem` (example code) but NO fence under `## Code`. Caret in `## Problem` python fence
    expected: Either (a) no language pack active — strict D-13, OR (b) language pack active — D-13 widening fallback. Both acceptable per CONTEXT §D-13. RECORD which one was observed.
    result: [pending]

29. **C3.** In an lc-slug note, move caret OUT of the `## Code` fence into prose; type `{`
    expected: NO matching `}` auto-closed in prose; Enter follows Obsidian's markdown default
    result: [pending]

### Section D — Source Mode parity + mode toggling

30. **D1.** In an lc-slug Python note in Live Preview with fence active, press `Cmd-E` to toggle to Source Mode
    expected: Content preserved, no re-flow, no data loss; typing inside fence still uses Python indent rules; CM6 syntax highlighting still visible
    result: [pending]

31. **D2.** Toggle back to Live Preview with `Cmd-E`
    expected: Behavior continues uninterrupted
    result: [pending]

32. **D3.** Toggle to Reading Mode (`Cmd-E` again)
    expected: Python3 fence still renders with Prism highlighting (Phase 5.2 unchanged); Reading-Mode Run/Submit buttons (Phase 5 Plan 05) still appear and still work
    result: [pending]

### Section E — Phase 5.1 + 5.2 + 5 regression

33. **E1.** In Live Preview on lc-slug Python note: Run + Submit buttons appear below the closer fence line (Phase 5.1 widget). Caret enters fence → Python pack activates
    expected: Buttons do NOT reposition, do NOT flicker, do NOT disappear (RESEARCH Pitfall 12). Caret exits fence → compartment clears → buttons still in place
    result: [pending]

34. **E2.** Click Run
    expected: Run modal opens (dispatches to `plugin.runFromActive()`); close the modal
    result: [pending]

35. **E3.** Click Submit
    expected: Submission flow kicks off (`plugin.submitFromActive()`); cancel before actual submission OR let it run if convenient
    result: [pending]

### Section F — Theme parity (light + dark)

36. **F1.** Obsidian Settings → Appearance → Theme: "Default" (light); open lc-slug Python note
    expected: Keyword/string/comment colors readable against light background
    result: [pending]

37. **F2.** Switch theme to dark
    expected: Colors automatically adapt via Obsidian's bundled HighlightStyle; readable against dark
    result: [pending]

38. **F3.** (Optional) Switch to a community theme (e.g., Minimal, Obsidian Nord)
    expected: Token colors adapt via theme's HighlightStyle; no plugin-authored color rule present
    result: [pending]

### Section G — Bundle-size + performance spot checks

39. **G1.** First time caret enters a Python fence in the session
    expected: There may be a ~50–150 ms imperceptible pause while pack loads; subsequent entries instant. Subjective: does first-entry feel ≤ 200 ms? (RESEARCH Pitfall 4, 11)
    result: [pending]

40. **G2.** main.js byte count
    expected: 532,496 bytes (520 KB) ≤ 700 KB hard cap; 600 < size ≤ 700 KB acceptable but notable. Already measured by `bash scripts/check-bundle-size.sh` — exits 0
    result: PASS — 520 KB measured, under 600 KB warn line

41. **G3.** `ls main.js*.js | wc -l ≤ 1` (A8 — single CJS, no chunk split)
    expected: 1 file (just `main.js`), no chunk files. Already verified — count is 0 (no `main.js*.js` matches because esbuild emits only the bare `main.js`)
    result: PASS — single main.js, zero chunks

### Section H — Assumption close-out (observational)

42. **H1.** A10 — Phase 5.1 widget position stable through Compartment swap
    expected: Verified in items 33–35 above
    result: [pending — observation from items 33–35]

43. **H2.** Pitfall 8 — Python colon-indent edge cases
    expected: Type a multi-line triple-quote string; observe whether indent behavior is reasonable. Flag any weirdness
    result: [pending — observation only]

44. **H3.** Pitfall 11 — Visible flash on pack load
    expected: First time entering a non-default-language fence, is there a visible un-highlighted flash? Subjective. Non-blocking for ship; flag for v1.1 if pronounced
    result: [pending — observation only]

## Summary

total: 44
passed: 2
issues: 0
pending: 42
skipped: 0
blocked: 0

## Gaps

(none recorded yet)

## Environment (record at end)

- OS:
- Obsidian version:
- Theme used (items 36–37):
- main.js bytes: 532,496 (520 KB)
- baseline (Plan 01): 149,167 (145 KB)
- Δ: +383,329 (+374 KB)
- warmDefaultPack wired: yes (per Plan 02 SUMMARY — Step 6i in src/main.ts)
- D-13 outcome (item C2): [pending]
- Pitfall 4/8/11 observations: [pending]
