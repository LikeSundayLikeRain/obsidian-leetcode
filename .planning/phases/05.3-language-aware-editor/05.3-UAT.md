---
status: pending
phase: 05.3-language-aware-editor
source: [05.3-04-PLAN.md]
started: 2026-05-12T00:00:00Z
updated: 2026-05-12T00:00:00Z
plugin_path: /Users/moxu/Documents/Leetcode/.obsidian/plugins/leetcode/
main_js_bytes: [pending]
main_js_kb: [pending]
baseline_kb: 149
delta_kb: [pending]
ship_cap_kb: 250
---

# Phase 05.3 — Live-Smoke UAT (D-14 Successor — Chevron + Remap)

This is the **successor** to the original Phase 5.3 D-14 LOCKED live-smoke. The original UAT verified the **failed Compartment-swap** implementation (D-10/D-11), which passed every unit test but failed the empirical "does highlighting actually appear?" check at the human-verify checkpoint. The pivot — write-time fence-tag remap (`python3 → python`, `golang → go`, `c → cpp`) + an Edit-Mode language chevron dropdown — has the **same testability constraint** (per RESEARCH §Q11 Bucket 4): unit tests prove `lcSlugToFenceTag('python3')` returns `'python'`, but they CANNOT prove that Obsidian's `lang-markdown` nested parser actually renders Python keywords as colored. **Live smoke is the only proof.**

This file drives the human verifier through that proof, in a real Obsidian dev vault, against the chevron+remap implementation that landed across Plans 01 (revert), 02 (fence-tag remap + frontmatter cleanup), and 03 (chevron widget + atomic dispatch).

---

## DEPRECATED — Original Failed-Implementation UAT Replaced

The previous version of this file (44 numbered items, 2026-05-11, `main_js_bytes: 532496`) tested the failed compartment-swap approach (per-view CM6 outer-compartment swap to a registered LanguageSupport). That checklist surfaced empirically that swapping the outer CM6 LanguageSupport compartment with `python()` does not affect tokens inside markdown fences (Obsidian's `lang-markdown` nested parser owns the fence sub-tree).

The original 44-item checklist is preserved in git history; this file replaces it with the new chevron+remap checklist (≥30 numbered items across Sections A–I). The failed-implementation vocabulary (the failed wrapper modules and the per-language CM6 wrappers they pulled in) is intentionally absent from this rewrite — those subsystems were deleted by Wave 0 and are no longer testable.

---

## Pre-flight (auto-completed by Claude before delegating to human)

These five items are mechanical; Claude runs them before the checkpoint surfaces and marks each PASS/FAIL. The human verifier should see green marks on these before starting Section A.

1. **PF1.** `npm run build` succeeds and emits a single `main.js` (no chunk-split files).
   expected: build exits 0; `ls main.js*.js | wc -l` reports 0
   result: [pending]

2. **PF2.** `bash scripts/check-bundle-size.sh` exits 0 against the new tightened cap (250 KB hard / 200 KB warn).
   expected: exit 0; line `BUNDLE CHECK OK: main.js {N} KB within budget`
   result: [pending]

3. **PF3.** `npm test --run` is green.
   expected: 550+ tests passing, 2 skipped, 0 failed
   result: [pending]

4. **PF4.** `npx tsc --noEmit` produces no NEW TypeScript errors beyond the pre-existing `tests/solve/SessionExpiredNotice.test.ts` TS18048 (DEF-05.3-01-A — out of scope).
   expected: only pre-existing test-file errors; no errors in src/
   result: [pending]

5. **PF5.** `wc -c < main.js` recorded for the Environment block.
   expected: ≤ 250 KB (256,000 bytes); record exact byte count
   result: [pending]

---

## Tests

### Section A — Native fence-tag highlighting (Edit + Reading Mode)

The chevron+remap pivot's load-bearing claim: when the fence opener tag is markdown-recognized (after the D-04 write-time remap), Obsidian's bundled `lang-markdown` nested parser renders the fence body with colored keywords/strings/comments — no plugin-side highlighting code required. Each item below verifies one LC language renders correctly in Live Preview, Source Mode, AND Reading Mode.

1. **A1.** Open lc-slug Python note (`lc-language: python3`) created post-replan. Live Preview: `## Code` fence shows Python keywords (`def`, `class`, `return`, `if`) colored, strings colored, `# comment` lines colored.
   expected: all three token classes visibly colored against theme background
   result: [pending]

2. **A2.** Same Python note. Cmd-E once → Source Mode. Fence still shows Python highlighting (same lang-markdown nested parser path).
   expected: identical token coloring to Live Preview
   result: [pending]

3. **A3.** Same Python note. Cmd-E again → Reading Mode. Fence shows Python highlighting via Prism (Phase 5.2 `python3 → python` alias unchanged).
   expected: Prism-styled keywords + strings + comments visible
   result: [pending]

4. **A4.** Open lc-slug Java note. Live Preview + Source: `class`, `public`, `static`, `return` colored.
   expected: keywords colored in both modes
   result: [pending]

5. **A5.** Same Java note. Reading Mode: same highlighting via Prism.
   expected: Prism-styled keywords visible
   result: [pending]

6. **A6.** Open lc-slug C++ note (`lc-language: cpp`). Live Preview + Source: `#include`, `int`, `class`, `return` colored; preprocessor directives visibly distinct.
   expected: C++ keywords + preprocessor directive coloring
   result: [pending]

7. **A7.** Same C++ note. Reading Mode: same highlighting.
   expected: Prism-styled C++ tokens visible
   result: [pending]

8. **A8.** Open lc-slug C note (`lc-language: c`). The fence opener should be ` ```cpp ` (D-04 remap c → cpp). Live Preview + Source: idiomatic C code (`int main()`, `printf`, `#include <stdio.h>`) tokenizes with cpp grammar; keywords + strings + preprocessor visibly colored.
   expected: cpp grammar tokenizes plain C cleanly — no obvious mis-highlighting on standard C syntax
   result: [pending]

9. **A9.** Same C note. Reading Mode: cpp grammar via Prism.
   expected: same highlighting
   result: [pending]

10. **A10.** Open lc-slug JavaScript note. Live Preview + Source: `const`, `let`, `function`, `return`, brackets, numbers colored.
    expected: JavaScript keyword + literal coloring
    result: [pending]

11. **A11.** Same JavaScript note. Reading Mode: Prism-styled JavaScript.
    expected: same highlighting
    result: [pending]

12. **A12.** Open lc-slug TypeScript note. Live Preview + Source: `interface`, `type`, type annotations colored as TS-specific tokens.
    expected: TS-specific coloring (type names distinct from values)
    result: [pending]

13. **A13.** Same TypeScript note. Reading Mode: Prism-styled TypeScript.
    expected: same highlighting
    result: [pending]

14. **A14.** Open lc-slug Go note (`lc-language: golang`). Fence opener should be ` ```go ` (D-04 remap golang → go). Live Preview + Source: `func`, `package`, `import`, `return`, `var` colored.
    expected: Go keyword coloring visible
    result: [pending]

15. **A15.** Same Go note. Reading Mode: Prism-styled Go.
    expected: same highlighting
    result: [pending]

16. **A16.** Open lc-slug Rust note. Live Preview + Source: `fn`, `let`, `mut`, lifetimes (`'a`), `match` colored.
    expected: Rust keyword + lifetime coloring visible
    result: [pending]

17. **A17.** Same Rust note. Reading Mode: Prism-styled Rust.
    expected: same highlighting
    result: [pending]

18. **A18.** Open lc-slug C# note (`lc-language: csharp`) — C# is unsupported (D-04 passthrough). Fence opener is ` ```csharp `. Live Preview + Source + Reading: fence renders as plain monospace, no keyword colors. **Critical: no crash, no error Notice, editor continues to accept input.**
    expected: plain monospace fallback; no errors
    result: [pending]

### Section B — Chevron rendering (Edit Mode only)

The chevron is the user-facing language picker. It must render LEFT of Phase 5.1's Run+Submit row in Edit Mode (Live Preview + Source) and must NOT render in Reading Mode (D-09 lock). Render gating mirrors Phase 5.1 exactly: lc-slug note + `## Code` fence present.

19. **B1.** Live Preview lc-slug Python note: chevron `▼ Python` appears LEFT of Run + Submit row. Run + Submit cling RIGHT.
    expected: 3-element row with chevron left, Run+Submit right (CSS `space-between` flip)
    result: [pending]

20. **B2.** Source Mode (Cmd-E once): chevron renders identically to Live Preview.
    expected: same `▼ Python` chevron + Run + Submit layout
    result: [pending]

21. **B3.** Reading Mode (Cmd-E twice): **chevron does NOT appear** (D-09 lock). Run + Submit appear right-aligned (single right group, no chevron).
    expected: chevron absent; Run+Submit visible and right-aligned
    result: [pending]

22. **B4.** Open a NON-lc-slug note (no `lc-slug` frontmatter) that contains a python fence: chevron does NOT render. Run + Submit do NOT render.
    expected: no chevron, no Run, no Submit (Phase 5.1 gate parity)
    result: [pending]

23. **B5.** Open an lc-slug note that does NOT contain a `## Code` fence: chevron does NOT render. Run + Submit do NOT render.
    expected: no chevron, no Run, no Submit (Phase 5.1 gate parity)
    result: [pending]

### Section C — Chevron click → dropdown

24. **C1.** Click the chevron on an lc-slug Python note (Live Preview). Dropdown opens immediately, anchored top-left below the chevron button. `aria-expanded="true"` on the chevron button.
    expected: dropdown popover visible; aria-expanded toggles
    result: [pending]

25. **C2.** Dropdown lists exactly 8 items in `LC_CHEVRON_LANG_ORDER`: Python, Java, C++, C, JavaScript, TypeScript, Go, Rust.
    expected: 8 items, exact order, exact `LC_LANG_DISPLAY_LABELS` text
    result: [pending]

26. **C3.** Currently-selected language (Python) renders with `.is-current` class — visually: accent color (`var(--text-accent)`) + bold (font-weight 600). Size unchanged from non-current items (current emphasis comes from weight + color, not size).
    expected: Python item highlighted distinctively
    result: [pending]

27. **C4.** Click outside the dropdown (e.g., on the editor body): dropdown closes (`display: none`, `aria-expanded="false"`).
    expected: dropdown dismisses on outside click
    result: [pending]

28. **C5.** Re-click the chevron while dropdown is open: dropdown closes (toggle behavior).
    expected: same chevron click closes the open dropdown
    result: [pending]

29. **C6.** Press Esc while dropdown is open: dropdown closes.
    expected: keyboard dismissal works
    result: [pending]

30. **C7.** **Stretch — full keyboard accessibility (Tab + arrow keys + Enter to select).** Tab to chevron; Space/Enter toggles dropdown; arrow Up/Down navigates items; Enter on focused item selects.
    expected: full a11y per UI-SPEC §Accessibility
    result: [pending] OR `stretch — not implemented this phase`

### Section D — Language switch (destructive — D-08)

The chevron's destructive behavior is the load-bearing UX claim. Picking a different language must replace the fence body atomically (one CM6 dispatch carrying opener + body changes), so a single Cmd-Z reverts both. The frontmatter `lc-language` write lands on a separate vault-undo stack — Pitfall 1 documented divergence (a SECOND Cmd-Z reverts the frontmatter).

31. **D1.** Lc-slug Python note. Type some custom code in fence (e.g. `# my custom solution\nclass Solution: pass`). Click chevron → click Java. Within ~500 ms (cache miss) or instant (cache hit): fence opener becomes ` ```java `; fence body becomes Java starter code (LC's `class Solution { ... }`); your custom Python code is gone.
    expected: destructive replace; both opener tag AND body rewritten
    result: [pending]

32. **D2.** Cmd-Z once → fence opener reverts to ` ```python ` (or ` ```python ` for the post-D-04 python3 slug); fence body reverts to your custom Python code. **Editor undo is ONE step (atomic dispatch reverts both ranges together).**
    expected: single Cmd-Z reverts opener + body simultaneously
    result: [pending]

33. **D3.** Cmd-Z second time → `lc-language` frontmatter reverts from `java` to previous value (e.g. `python3`). (Two Cmd-Z's total — Pitfall 1 documented divergence; vault undo is a separate stack from CM6 editor undo.)
    expected: second Cmd-Z reverts frontmatter (acceptable two-stack divergence)
    result: [pending]

34. **D4.** Click chevron → click currently-selected language → no-op (no fetch, no fence rewrite, no Notice, no frontmatter write).
    expected: same-language click is silent and inert
    result: [pending]

### Section E — Frontmatter updates

35. **E1.** After successful switch to Java (D1): open the property panel → `lc-language` shows `java`. (D-03 — chevron writes here.)
    expected: lc-language updated to new slug
    result: [pending]

36. **E2.** After replan + first AC submission on a post-cleanup note: `lc-runtime-ms` and `lc-memory-mb` are absent from the frontmatter. (D-01/D-02 cleanup — write paths deleted.)
    expected: only lc-status, lc-solved-date, lc-language present (no runtime/memory keys)
    result: [pending]

37. **E3.** Pre-replan notes' orphan `lc-runtime-ms` / `lc-memory-mb` values do NOT cause errors. (No reader code; harmless residue.)
    expected: legacy notes open and render normally; no errors in console
    result: [pending]

### Section F — Run / Submit regression (Phase 5.1)

38. **F1.** Live Preview, lc-slug note, chevron + Run + Submit visible: caret enters fence → no flicker, no widget reposition. Caret exits → no flicker, no reposition.
    expected: stable widget through caret movement
    result: [pending]

39. **F2.** Click Run → Run modal opens (dispatches to `runFromActive`). After switch to Java in this session, Run uses Java code (chevron and `lc-language` are in sync).
    expected: Run modal opens; uses current language
    result: [pending]

40. **F3.** Click Submit → submission flow starts (dispatches to `submitFromActive`). Verdict modal opens normally.
    expected: Submit flow works end-to-end
    result: [pending]

41. **F4.** Repeat F1–F3 in Source Mode: same behavior.
    expected: Source-Mode parity for chevron + Run + Submit
    result: [pending]

42. **F5.** Reading Mode: chevron absent (D-09 verified in B3); Run + Submit visible and functional (Phase 5 Reading-Mode buttons unchanged).
    expected: Reading-Mode Run+Submit work; no chevron
    result: [pending]

### Section G — Network failure

43. **G1.** Disconnect from network (turn off Wi-Fi or use a network-blocking proxy). Click chevron → click Java. Notice fires with the **locked copy**: `Couldn't fetch starter code for Java.` Fence body unchanged. `lc-language` unchanged.
    expected: exact Notice copy fires; fence + frontmatter intact
    result: [pending]

44. **G2.** Reconnect network. Click chevron → click Java. Switch succeeds normally (fence rewritten, frontmatter updated).
    expected: post-recovery switch works
    result: [pending]

### Section H — Theme parity (light + dark)

45. **H1.** Settings → Appearance → Theme: Default (light). Chevron renders with secondary background (`var(--background-secondary)`), muted text (`var(--text-muted)`), border (`var(--background-modifier-border)`). Dropdown opens on white surface with normal text. Current-language item: accent + bold.
    expected: light theme renders all colors via Obsidian theme variables; no plugin-authored color overrides
    result: [pending]

46. **H2.** Switch to Default (dark). All variables shift; chevron readable; dropdown popover readable; current-language item visible against dark background.
    expected: dark theme parity; no rules hard-coded for light
    result: [pending]

47. **H3.** Switch to Minimal community theme (light + dark). Same checks; no plugin-authored color overrides.
    expected: Minimal theme parity
    result: [pending] OR `blocked: theme not available`

### Section I — Bundle-size + automated regression

48. **I1.** After all of the above: `npm run build` succeeds.
    expected: build exits 0
    result: [pending]

49. **I2.** `bash scripts/check-bundle-size.sh` exits 0. Reported `main.js` size ≤ 250 KB (record exact value in Environment block).
    expected: hard cap respected
    result: [pending]

50. **I3.** Warn-line trip status: is `main.js` > 200 KB (warn threshold)? Record outcome — non-blocking, observational only.
    expected: typically NOT tripped (Wave 2 measured ~152 KB); record actual size
    result: [pending] OR `not tripped: {N} KB`

51. **I4.** `wc -c < main.js` value recorded for the Environment block.
    expected: exact byte count captured
    result: [pending]

52. **I5.** `ls main.js*.js | wc -l` reports 0 (no chunk-split; single CJS bundle).
    expected: 0 chunks
    result: [pending]

53. **I6.** Full vitest suite green: `npm test --run` reports 0 failures.
    expected: 550+ passing, 2 skipped, 0 failed
    result: [pending]

54. **I7.** Deleted failed-implementation test files are gone: per Plan 01 SUMMARY's "Files Deleted" table, run `for f in $(awk -F\| '/Files Deleted/,/^$/' .planning/phases/05.3-language-aware-editor/05.3-01-SUMMARY.md | grep "tests/main" | awk -F\` '{print $2}'); do test ! -f "$f" || echo "ORPHAN: $f"; done` and confirm no `ORPHAN:` lines print.
    expected: no orphan test files for deleted source modules
    result: [pending]

55. **I8.** No source still imports a deleted `@codemirror/lang-*` pack: `grep -RnE "from '@codemirror/lang-(python|java|cpp|javascript|go|rust)'" src/` returns no matches.
    expected: 0 matches
    result: [pending]

---

## Summary

total: 55 (5 pre-flight + 50 numbered + lettered as A1..I8 spanning sections A–I)
passed: [pending]
failed: [pending]
pending: [pending]
blocked: [pending]
skipped: [pending]

## Gaps

(none recorded yet — verifier records here on any FAIL of a blocking item; `/gsd-plan-phase 5.3 --gaps` consumes this list)

## Environment (record at end)

- OS: [pending]
- Obsidian version: [pending]
- Theme used (H1 / H2): [pending]
- Theme used (H3 — Minimal): [pending] OR `blocked: theme not available`
- main.js bytes: [pending]
- baseline (Wave 0): 149,167 (~149 KB)
- delta from baseline: [pending]
- D-08 atomic-undo outcome (D2): [pending — confirm single Cmd-Z reverts opener+body]
- Pitfall 1 outcome (D3): [pending — confirm second Cmd-Z reverts frontmatter]
- Pitfall 4 outcome (G1): [pending — confirm Notice fires exactly once on network failure]
- Pitfall 7 outcome (Section A): [pending — any visible un-highlighted flash on first fence open?]
