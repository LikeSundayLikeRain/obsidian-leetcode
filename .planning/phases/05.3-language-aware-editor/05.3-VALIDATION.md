---
phase: 05.3
slug: language-aware-editor
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-11
---

# Phase 05.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 4.1.5 |
| **Config file** | `vitest.config.ts` (or default) |
| **Quick run command** | `npm test -- --run` |
| **Full suite command** | `npm test -- --run && npm run build` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npm test -- --run`
- **After every plan wave:** Run `npm test -- --run && npm run build`
- **Before `/gsd-verify-work`:** Full suite must be green + bundle-size check passes
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

Populated by planner from PLAN.md task list. Each task maps to an `<automated>` command or a Wave 0 infrastructure stub.

---

## Wave 0 Requirements

- [ ] Test stubs for `languagePackRegistry` (registry lookup, fallback behavior)
- [ ] Test stubs for `whitespaceCopyIndent` (previous-line leading whitespace copy)
- [ ] Test stubs for `codeFenceLanguageExtension` (compartment swap on fence language change)
- [ ] `scripts/check-bundle-size.sh` scaffold (700 KB cap per D-09)
- [ ] Baseline `npm run build` bundle-size measurement captured before lang-pack install

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Enter inside fence produces language-aware indent (Java/C++/JS `{` bumps in) | POLISH-09 | Requires live Obsidian Live Preview + CM6 view | D-14 live-smoke checklist — type `public class Foo {⏎` in Java fence, confirm next line indented one level |
| Python `:` bumps indent; `else`/`elif`/`except`/`finally` dedent | POLISH-09 | Same — requires live CM6 editor behavior | D-14 checklist — type `if x:⏎` in Python fence, confirm indent; type `else:` on new line, confirm dedent |
| Unsupported language fallback (whitespace-copy, no crash) | POLISH-09 | Needs live view with unsupported language slug | D-14 checklist — set fence to unsupported language (e.g., `erlang`), press Enter, confirm whitespace copy works with no errors |
| No regression in Phase 5.1 Run/Submit buttons or Phase 5 reading-mode buttons | POLISH-09 | Requires live UI interaction | D-14 checklist — exercise Run/Submit in edit mode + reading-mode buttons on same note |
| Scoping: auto-indent only inside `## Code` fence on `lc-slug` notes | POLISH-09 | Requires live view switching between prose, other fences, and non-LC notes | D-14 checklist — verify Enter in prose and non-LC fences does NOT trigger language-aware indent |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
