---
phase: 05.3
slug: language-aware-editor
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-11
replanned: 2026-05-11
---

# Phase 05.3 — Validation Strategy (REPLAN)

> Per-phase validation contract for feedback sampling during execution. Replanned 2026-05-11 after D-14 live-smoke failure of the original Compartment-swap approach. New approach: write-time fence-tag remap + chevron dropdown.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 4.1.5 |
| **Config file** | `vitest.config.ts` (existing — aliases `obsidian` → `tests/helpers/obsidian-stub.ts`; env `happy-dom`) |
| **Quick run command** | `npm test -- --run tests/main/languageChevronWidget.test.ts tests/main/codeBlockButtonRow.test.ts tests/solve/languages.test.ts` |
| **Full suite command** | `npm test --run && npm run build && bash scripts/check-bundle-size.sh` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run quick run command (~2s).
- **After every plan wave:** Run full suite (test + build + bundle-size gate).
- **Before `/gsd-verify-work`:** Full suite green + live-smoke checkpoint passed (D-14-equivalent).
- **Max feedback latency:** 30 seconds.

---

## Per-Task Verification Map

> Populated by planner from PLAN.md task list. Each task maps to an `<automated>` command from RESEARCH §Validation Architecture or a Wave 0 infrastructure stub.

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| _populated by planner_ | | | POLISH-09 | | | | |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/main/languageChevronWidget.test.ts` — covers chevron DOM render + click toggle + item-click → switchLanguage spy + current-language no-op
- [ ] `tests/main/codeBlockButtonRow.test.ts` — covers `opts.prefix` undefined (2 children: Run, Submit) vs provided (3 children: prefix, Run, Submit); existing Run/Submit click handlers stay green
- [ ] `tests/solve/languages.test.ts` — extend with `lcSlugToFenceTag()` (10 cases) + `LC_LANG_DISPLAY_LABELS` (9 cases) + `LC_CHEVRON_LANG_ORDER` shape assertions
- [ ] `tests/graph/onAccepted.frontmatter.test.ts` — UPDATE: remove `lc-runtime-ms` / `lc-memory-mb` assertions (lines 70, 71, 107, 108, 119)
- [ ] `tests/graph/onAccepted.reAccepted.test.ts` — UPDATE: remove `lc-runtime-ms` / `lc-memory-mb` assertions (lines 39, 40, 67, 68)
- [ ] `tests/graph/fixture-integration.test.ts` — UPDATE: remove `lc-runtime-ms` / `lc-memory-mb` assertions (lines 149, 150)
- [ ] No new framework install; no new helper file. happy-dom + vitest already cover the test surface.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Native python highlighting in fence | POLISH-09 | Requires live Obsidian Live Preview + `lang-markdown` nested parser | Open lc-slug python note → ` ```python ` fence shows Python keywords colored |
| Native java highlighting in fence | POLISH-09 | Same | Same for ` ```java ` |
| Native cpp highlighting in fence | POLISH-09 | Same | Same for ` ```cpp ` (also covers `c → cpp` remap) |
| Native javascript / typescript / go / rust highlighting | POLISH-09 | Same | Same for each remaining native tag |
| Chevron renders LEFT of Run/Submit (Edit Mode) | POLISH-09 | Visual layout assertion | Open lc-slug note in Live Preview → confirm `[▼ Lang]` clings left, `[Run] [Submit]` clings right |
| Chevron does NOT appear in Reading Mode | POLISH-09 | Cross-mode UI check | Cmd-E to Reading Mode → only Run + Submit visible, no chevron |
| Chevron switch destructively replaces fence body | POLISH-09 | Live destructive-write behavior | Click Java → fence shows Java starter code; original Python code is gone |
| Cmd-Z reverts fence atomically (one undo step) | POLISH-09 | Editor undo-stack behavior | After switch, single Cmd-Z reverts both fence opener tag AND fence body to pre-switch state |
| `lc-language` frontmatter updates on switch | POLISH-09 | Vault-undo divergence acceptable | After switch, property panel shows new `lc-language` slug |
| GraphQL refetch + cache behavior | POLISH-09 | Network observation | First switch ~200–500 ms; subsequent switches in same session are instant (cache hit) |
| Network failure: keep current body + Notice | POLISH-09 | Offline safety | With network off, click chevron → Notice fires; fence body unchanged |
| Phase 5.1 Run/Submit regression (Edit + Reading) | POLISH-09 | Live UI interaction | Run + Submit work end-to-end in both modes |
| Phase 5.2 python3 Prism alias still works | POLISH-09 | Reading-Mode unchanged | Reading-mode python3 fence still highlights via Prism alias |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending (planner repopulates per-task map; reviewer flips to approved after plan-check passes)
