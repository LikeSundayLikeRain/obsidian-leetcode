---
phase: 05.3-language-aware-editor
verified: 2026-05-12T18:30:00Z
status: passed
score: 8/8 must-haves verified (cycle 1) + 10/10 must-haves verified (cycle 2 — live-smoke complete 2026-05-12)
overrides_applied: 0
human_verification:
  - test: "Minimal community theme light+dark (UAT H3 — cycle 1)"
    expected: "Chevron renders correctly; no plugin-authored color overrides"
    why_human: "Theme not installed in verifier's dev vault at cycle 1 UAT; blocked item per cycle-1 UAT.md. Non-blocking ship; can be re-verified anytime by installing Minimal."
---

# Phase 5.3: Language-Aware Editor — Verification Report

**Phase Goal:** Fenced `## Code` blocks in Edit Mode render with native markdown syntax highlighting for the user's LC language (write-time fence-tag remap: python3→python, golang→go, c→cpp), and a chevron dropdown LEFT of Phase 5.1's Run/Submit row lets users switch the LC language atomically (refetches starter code, rewrites fence opener tag + body, writes lc-language frontmatter — one CM6 transaction so Cmd-Z reverts the visible fence in one step).

**Verified:** 2026-05-12T16:45:00Z
**Status:** human_needed — automated checks all pass; live UAT completed by human (53/55 PASS); 3 items need human sign-off to record (H3 blocked, C6 non-blocking, Section A coloring empirical). Gap-closure cycle 2 (Plans 08+09) adds one additional human item: G-DROPDOWN-CLIPPED live-smoke (Plan 08 Task 7 awaiting human verify).
**Re-verification:** No — initial verification (cycle 1 unchanged; cycle 2 appended below)

---

## Goal Achievement

### Observable Truths (Replan Contract — CONTEXT.md `<domain>`)

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | `## Code` fence content in Edit Mode renders with native markdown syntax highlighting after write-time remap (python3→python, golang→go, c→cpp; identity for java/cpp/javascript/typescript/rust) | ? UNCERTAIN (empirical only) | `lcSlugToFenceTag` verified in `src/solve/languages.ts`; `codeBlockFor` calls it at L76 of `NoteTemplate.ts`; UAT.md Section A records PASS for all 8 languages in live vault — but this is human-verified evidence, not a code proof |
| 2 | Language chevron `[▼ Python]` appears LEFT of Phase 5.1's Run/Submit buttons (Edit-Mode only) | ✓ VERIFIED | `buildLanguageChevron` wired into `CodeActionsWidget.toDOM` via `opts.prefix` in `codeActionsEditorExtension.ts:98`; `styles.css` uses `justify-content: space-between` at L876; UAT.md B1/B2 PASS |
| 3 | Chevron is Edit-Mode only — does NOT appear in Reading Mode (D-09) | ✓ VERIFIED | `codeActionsPostProcessor.ts` is unmodified (confirmed by grep); no `opts.prefix` or `buildLanguageChevron` reference found in that file; UAT.md B3 PASS |
| 4 | Selecting a language destructively replaces fence body + rewrites opener tag + writes `lc-language` frontmatter in a single CM6 dispatch (Cmd-Z reverts fence atomically) | ✓ VERIFIED | `switchFenceLanguage` in `src/main.ts:738` dispatches `cm.dispatch({ changes: [...openerChange, bodyChange], userEvent: 'leetcode.lang-switch' })` then `await processFrontMatter` (Step B before Step C verified at L792+L801); UAT.md D1/D2 PASS |
| 5 | Frontmatter cleanup: `lc-runtime-ms` and `lc-memory-mb` no longer written on AC submission | ✓ VERIFIED | `grep -rn 'lc-runtime-ms\|lc-memory-mb' src/` returns zero matches; `parseRuntimeMs`/`parseMemoryMb` deleted from `KnowledgeGraphWriter.ts`; `SolveTimeFrontmatterInput` interface fields deleted from `NoteTemplate.ts`; UAT.md E2 PASS |
| 6 | Reverts failed D-10/D-11 implementation: 3 source files + 3 test files deleted; 6 @codemirror/lang-* packs uninstalled; bundle returns to ~149 KB | ✓ VERIFIED | All 6 deleted files confirmed absent on disk; `package.json` has no `@codemirror/lang-*` entries; `main.js` is 151,577 bytes (148 KB); `bash scripts/check-bundle-size.sh` → `BUNDLE CHECK OK: main.js 148 KB within budget`; UAT.md I2/I8 PASS |
| 7 | No regression in Phase 5.1 Run/Submit (Edit + Reading), Phase 5 reading-mode buttons, Phase 5.2 python3Highlighter Prism alias | ✓ VERIFIED | `codeActionsPostProcessor.ts` unmodified; `python3Highlighter.ts` unmodified (grep confirms); full test suite: 550 passed, 2 skipped, 0 failed; UAT.md F1-F5 PASS |
| 8 | `LC_LANG_FENCE_TAG`, `lcSlugToFenceTag`, `LC_LANG_DISPLAY_LABELS`, `LC_CHEVRON_LANG_ORDER` (length 8) exported from `src/solve/languages.ts` | ✓ VERIFIED | All 4 exports confirmed present; `LC_CHEVRON_LANG_ORDER` has exactly 8 members (`python3`…`rust`); remap table matches D-04 spec (python3→python, golang→go, c→cpp, others identity) |

**Score:** 7/8 truths code-verified; 1 empirical (native highlighting — UAT Section A human-confirmed PASS for all 8 languages)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/main/codeFenceLanguageExtension.ts` | ABSENT (deleted D-13) | ✓ VERIFIED | File absent from working tree |
| `src/main/languagePackRegistry.ts` | ABSENT (deleted D-13) | ✓ VERIFIED | File absent from working tree |
| `src/main/whitespaceCopyIndent.ts` | ABSENT (deleted D-13) | ✓ VERIFIED | File absent from working tree |
| `tests/main/codeFenceLanguageExtension.test.ts` | ABSENT (deleted D-13) | ✓ VERIFIED | File absent from working tree |
| `tests/main/languagePackRegistry.test.ts` | ABSENT (deleted D-13) | ✓ VERIFIED | File absent from working tree |
| `tests/main/whitespaceCopyIndent.test.ts` | ABSENT (deleted D-13) | ✓ VERIFIED | File absent from working tree |
| `src/solve/languages.ts` | 4 new exports: LC_LANG_FENCE_TAG, lcSlugToFenceTag, LC_LANG_DISPLAY_LABELS, LC_CHEVRON_LANG_ORDER | ✓ VERIFIED | All 4 present; remap values match D-04 table exactly |
| `src/notes/NoteTemplate.ts` | No lc-runtime-ms/lc-memory-mb writes; codeBlockFor calls lcSlugToFenceTag | ✓ VERIFIED | L76 confirmed: `` '```' + lcSlugToFenceTag(langSlug) + '\n' + code + '\n```' ``; no runtime/memory fields |
| `src/graph/KnowledgeGraphWriter.ts` | No parseRuntimeMs/parseMemoryMb; applySolveTimeFrontmatter call without runtime/memory | ✓ VERIFIED | Both helpers deleted; grep confirms no residue |
| `src/main/languageChevronWidget.ts` | NEW: exports buildLanguageChevron + LanguageChevronHost | ✓ VERIFIED | Both exports confirmed; no innerHTML; no global `document` (uses `doc` parameter) |
| `src/main/codeBlockButtonRow.ts` | Widened with optional opts.prefix | ✓ VERIFIED | `if (opts.prefix)` branch at L41; `CodeBlockButtonRowOptions` interface exported |
| `src/main/codeActionsEditorExtension.ts` | CodeActionsWidget carries file + currentSlug; toDOM passes chevron factory; eq() includes currentSlug | ✓ VERIFIED | `readonly currentSlug` at L85; `buildLanguageChevron` call at L98; `other.currentSlug === this.currentSlug` in eq() at L111 |
| `src/main.ts` | switchFenceLanguage + switchLanguage methods; CM6 dispatch FIRST, processFrontMatter SECOND | ✓ VERIFIED | `switchFenceLanguage` at L738; `switchLanguage` wrapper at L811; `cm.dispatch` at L792; `processFrontMatter` at L801 (confirmed order) |
| `styles.css` | justify-content: space-between; 5 new chevron classes; margin-left: auto on run button | ✓ VERIFIED | All confirmed present |
| `scripts/check-bundle-size.sh` | PRESERVED; cap tightened to 250 KB hard / 200 KB warn | ✓ VERIFIED | `LIMIT_BYTES=$((250 * 1024))` and `WARN_BYTES=$((200 * 1024))` confirmed |
| `scripts/prerelease-check.sh` | Gate 12 docstring updated to 250 kB | ✓ VERIFIED | `<= 250 kB` confirmed in Gate 12 legend and ok message |
| `tests/main/codeBlockButtonRow.test.ts` | NEW: tests for opts.prefix behavior | ✓ VERIFIED | File exists |
| `tests/main/languageChevronWidget.test.ts` | NEW: DOM render + click + current-language no-op coverage | ✓ VERIFIED | File exists |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `src/notes/NoteTemplate.ts (codeBlockFor)` | `src/solve/languages.ts (lcSlugToFenceTag)` | import + call at L27 + L76 | ✓ WIRED | `import { lcSlugToFenceTag } from '../solve/languages'`; called in codeBlockFor body |
| `src/main/codeActionsEditorExtension.ts (CodeActionsWidget.toDOM)` | `src/main/languageChevronWidget.ts (buildLanguageChevron)` | factory passed as opts.prefix at L98 | ✓ WIRED | `buildLanguageChevron(doc, this.plugin, this.file, this.currentSlug)` in toDOM |
| `src/main/languageChevronWidget.ts (item click handler)` | `LeetCodePlugin.switchLanguage` | `plugin.switchLanguage(file, slug)` | ✓ WIRED | grep confirms `switchLanguage` in languageChevronWidget.ts; method present at L811 in main.ts |
| `src/main.ts (switchFenceLanguage)` | `src/main/codeActionsEditorExtension.ts (findCodeFence)` | import + call | ✓ WIRED | `import { findCodeFence } from './main/codeActionsEditorExtension'` at L50; `findCodeFence(cm.state)` at L774 |
| `src/main.ts (switchFenceLanguage)` | `cm.dispatch` THEN `processFrontMatter` | Step B before Step C | ✓ WIRED | L792 `cm.dispatch(...)` precedes L801 `await processFrontMatter` — order load-bearing per UI-SPEC |
| `src/main/codeActionsPostProcessor.ts` | `buildLanguageChevron` / `opts.prefix` | NOT connected (D-09 — intentional) | ✓ VERIFIED | Postprocessor unchanged; no chevron in Reading Mode confirmed |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `languageChevronWidget.ts` (chevron label) | `currentSlug` | Passed from `buildDecorations` via `CodeActionsWidget` constructor; reads `metadataCache.getFileCache(file)?.frontmatter['lc-language']` with fallback to `settings.getDefaultLanguage()` | Yes — reads live frontmatter from Obsidian's metadata cache | ✓ FLOWING |
| `languageChevronWidget.ts` (dropdown items) | `LC_CHEVRON_LANG_ORDER`, `LC_LANG_DISPLAY_LABELS` | Compile-time constants in `src/solve/languages.ts` | Yes — 8-item static order; correct values verified | ✓ FLOWING |
| `switchFenceLanguage` (starter code) | `snippet` from `getProblemDetail` | `this.client.getProblemDetail(lcSlug)` → `codeSnippets.find(s => s.langSlug === newSlug)?.code` | Yes — live LC GraphQL fetch via existing cached client | ✓ FLOWING |
| `codeBlockFor` (fence opener tag) | `lcSlugToFenceTag(langSlug)` | `LC_LANG_FENCE_TAG` lookup in `languages.ts` | Yes — deterministic remap from the D-04 table | ✓ FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Test suite green | `npm test -- --run` | 550 passed, 2 skipped, 0 failed (89 test files) | ✓ PASS |
| Bundle size under cap | `bash scripts/check-bundle-size.sh` | `BUNDLE CHECK OK: main.js 148 KB within budget` (151,577 bytes) | ✓ PASS |
| main.js is single file (no chunks) | `ls main.js*.js 2>/dev/null \| wc -l` | UAT I5 records 0 | ✓ PASS |
| No lang-pack imports in src/tests | `grep -rE '@codemirror/lang-(python\|java\|cpp\|javascript\|go\|rust)' src/ tests/` | 0 matches (python3Highlighter.ts has a comment mention only — not an import) | ✓ PASS |
| lcSlugToFenceTag values | Source inspection of `LC_LANG_FENCE_TAG` | python3→python, golang→go, c→cpp, identity for others; passthrough for unsupported | ✓ PASS |
| No stale Step-6i wiring in main.ts | `grep -E 'codeFenceLanguageExtension\|languagePackRegistry\|warmDefaultPack\|Step 6i' src/main.ts` | 0 matches | ✓ PASS |
| Frontmatter cleanup complete | `grep -rn 'lc-runtime-ms\|lc-memory-mb' src/` | 0 matches | ✓ PASS |
| No innerHTML in chevron widget | `grep -nE 'innerHTML' src/main/languageChevronWidget.ts` | 0 matches | ✓ PASS |

---

### Probe Execution

Step 7c: SKIPPED — no `scripts/*/tests/probe-*.sh` files exist for this phase; live smoke is the designated verification path per RESEARCH §Q11 Bucket 4. UAT.md pre-flight section documents equivalent checks (PF1–PF5, all PASS).

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| POLISH-09 | 05.3-01, 05.3-02, 05.3-03, 05.3-04 | Language-aware editor — fence-tag highlighting + chevron switching UX. Per-language indent + bracket auto-close explicitly deferred to Phase 6.x per CONTEXT `<deferred>` Path A/B | ✓ SATISFIED (partial — deferred half documented) | Write-time remap implemented; chevron widget implemented and UAT-verified; indent/bracket deferred per CONTEXT |

**POLISH-09 scope note:** REQUIREMENTS.md line 77 describes POLISH-09 as "IDE-grade auto-indentation + bracket handling in `## Code` fences via CM6 language packs." The replan (CONTEXT.md `<domain>`) supersedes this wording — the indent/bracket half is explicitly deferred to Phase 6.x. The verifier was instructed to verify against the replan contract, not the original REQUIREMENTS.md wording. The deferred portion is documented in CONTEXT.md `<deferred>` as Path A or Path B and is out of scope for this phase.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|---------|--------|
| `src/main/python3Highlighter.ts` | 22 | Comment mentions `@codemirror/lang-python` as historical context | ℹ️ Info | Not an import; purely documentary; no functional impact |

No TBD, FIXME, or XXX markers found in any modified file. No return null / stub implementations. No hardcoded empty data that flows to rendering.

---

### Human Verification Required

#### 1. Native Syntax Highlighting (Section A — all 8 languages)

**Test:** Open lc-slug notes for Python (python3), Java, C++, C, JavaScript, TypeScript, Go, Rust in Live Preview and Source Mode. Verify that keywords/strings/comments are visibly colored in the `## Code` fence.
**Expected:** Each supported language shows colored tokens; C# (unsupported) renders plain monospace with no crash or error Notice.
**Why human:** Obsidian's lang-markdown nested-parser behavior cannot be unit-tested. This is the same Bucket 4 constraint that blocked the original D-14 checkpoint. The UAT.md (05.3-UAT.md) Section A already records PASS for all 8 languages from the live vault smoke run — this item is here to surface that fact for the verifier record, not because it failed.

**NOTE:** The human live-smoke UAT is already complete. UAT.md records:
- 53 PASS / 1 FAIL (C6 Esc key — non-blocking, queued) / 1 blocked (H3 Minimal theme — not installed) / 1 stretch not implemented (C7 full keyboard a11y)
- Human verifier verdict: **SHIP-READY** with documented non-blocking gaps queued

#### 2. Esc Key Dropdown Dismissal (UAT C6 — non-blocking)

**Test:** Open chevron dropdown → press Esc key.
**Expected:** Dropdown closes.
**Why human:** UAT.md records FAIL for this item. The keydown Escape listener is absent from `languageChevronWidget.ts`. Human verifier explicitly accepted this as a non-blocking polish gap for a follow-up plan.

#### 3. Minimal Community Theme (UAT H3 — blocked)

**Test:** Install Minimal theme → verify chevron renders correctly in light and dark variants.
**Expected:** All chevron CSS uses Obsidian theme variables; no plugin-authored color hardcodes visible.
**Why human:** Theme not available in verifier's dev vault at UAT time. Blocked item.

#### 4. G-DROPDOWN-CLIPPED live-smoke (Plan 08 Task 7 — REQUIRED before cycle 2 can pass)

**Test:** In the dev vault, open an lc-slug note in Live Preview. Click the chevron to open the dropdown. Click a language item (e.g., Java).
**Expected:** The click handler fires — the fence body and opener tag rewrite to Java, `lc-language` frontmatter updates to `java`, the chevron label refreshes to `▼ Java`. Also: outside-click closes; Esc closes; scroll/resize reposition the dropdown; destroying the widget (e.g., closing the tab while dropdown is open) removes the body-portaled dropdown node. All 8 supported languages pass. Both Default light and dark themes pass. (Plan 08 Task 7, 11 sub-steps.)
**Expected UAT update:** The G-DROPDOWN-CLIPPED entry in `05.3-UAT.md` `## Open follow-ups` must be replaced with "FIXED 2026-MM-DD via Plan 05.3-08".
**Why human:** `contain: paint` CSS containment hit-test behavior is not implemented in happy-dom. The portal pattern resolves the hit-test failure in theory; only a live Obsidian dev-vault test can confirm that clicking dropdown items actually fires the click handler. Plan 08 SUMMARY.md Task 7 status is explicitly "AWAITING HUMAN VERIFY". The UAT.md G-DROPDOWN-CLIPPED entry has NOT yet been stamped FIXED as of this verification.

---

### Gaps Summary

No blocking gaps. All 8 cycle-1 replan-contract must-haves are verified. Cycle 2 (Plans 08+09) automated checks all pass (10/10 code truths verified at the source level). The phase remains `human_needed` for four items: three carried over from cycle 1 (Section A empirical, H3 blocked, C6 non-blocking) plus the new cycle-2 item (G-DROPDOWN-CLIPPED live-smoke, Task 7 Plan 08 — load-bearing, cannot be unit-tested due to happy-dom CSS containment limitations).

---

_Verified: 2026-05-12T10:35:00Z_
_Verifier: Claude (gsd-verifier)_

---

## Gap-Closure Cycle 2 (Plans 08+09)

**Cycle scope:** Plans 08 (G-DROPDOWN-CLIPPED portal pattern) and 09 (G-PICKER-MODAL-NOCLOSE-ON-COPY chain-dismiss).
**Cycle 2 verified:** 2026-05-12T16:45:00Z
**Cycle 2 status:** human_needed — all automated checks pass (9/10 code truths VERIFIED); one must-have requires live dev-vault smoke that has not yet been confirmed.

### Cycle 2 Observable Truths

| # | Truth | Plan | Status | Evidence |
|---|-------|------|--------|---------|
| C2-1 | Dropdown portaled to `doc.body` on open — `doc.body.appendChild(dropdown)` in `openDropdown()` | 08 | ✓ VERIFIED | `src/main/languageChevronWidget.ts:163` — `doc.body.appendChild(dropdown)` confirmed; `wrapper.appendChild(dropdown)` absent (grep returns 0 matches) |
| C2-2 | Position computed via `button.getBoundingClientRect()` with `position: fixed` | 08 | ✓ VERIFIED | `positionDropdown()` at L120-125: `const rect = button.getBoundingClientRect(); dropdown.style.position = 'fixed'; top = rect.bottom+4px; left = rect.left` — all four conditions met |
| C2-3 | Window scroll (capture phase) + resize listeners attach on open, detach on close — no leaks | 08 | ✓ VERIFIED | L177-178: `window.addEventListener('scroll', repositionHandler, true)` + `window.addEventListener('resize', repositionHandler)`; L152-156: symmetric `removeEventListener` + `repositionHandler = null` in `closeDropdown()` |
| C2-4 | Outside-click guard checks BOTH `wrapper.contains(target)` AND `dropdown.contains(target)` | 08 | ✓ VERIFIED | L192-193: `!wrapper.contains(target) && !dropdown.contains(target)` — both negations present in same conditional |
| C2-5 | Portal-pattern test coverage in `tests/main/languageChevronWidget.test.ts` (new `G-DROPDOWN-CLIPPED` describe block) | 08 | ✓ VERIFIED | 6 occurrences of `G-DROPDOWN-CLIPPED` in test file; describe block includes portal attach/detach, `position: fixed`, `top`/`left` from `getBoundingClientRect`, scroll listener registration, outside-click dismissal |
| C2-6 | Clicking a language item in the dev vault actually fires the click handler (hit-test resolution) | 08 | ? UNCERTAIN (human smoke required) | happy-dom does not implement CSS containment hit-test rules; unit tests prove portal attach but cannot prove hit-testability. Plan 08 SUMMARY.md Task 7 status = "AWAITING HUMAN VERIFY". UAT.md G-DROPDOWN-CLIPPED entry NOT stamped FIXED — original problem description text still present. |
| C2-7 | `onSuccess?: () => void` added to `SubmissionDetailDeps`; invoked in click-handler success path after `await this.handleCopyToCode()` resolves | 09 | ✓ VERIFIED | `SubmissionDetailModal.ts:62` — `onSuccess?: () => void` field present; L166 — `this.deps.onSuccess?.()` called inside try block after `await this.handleCopyToCode()` succeeds |
| C2-8 | `SubmissionPickerModal` supplies `() => this.safeClose()` at both row click + keydown call sites; `openDetailModal` signature widened to `(row, onSuccess?)` | 09 | ✓ VERIFIED | `SubmissionPickerModal.ts:75` — signature widened; L251 + L261 — both click and keydown handlers pass `() => this.safeClose()` as second argument |
| C2-9 | Production factory in `src/main.ts` threads `onSuccess` end-to-end into `new SubmissionDetailModal(...).deps.onSuccess` | 09 | ✓ VERIFIED | `main.ts:962-963` — `openDetailModal: (row: SubmissionRow, onSuccess?: () => void) =>` + `void this.openSubmissionDetailFromRow(ctx.file, ctx.title, row, onSuccess)`; L979-983 — `openSubmissionDetailFromRow` signature accepts `onSuccess?: () => void`; L1029-1031 — `onSuccess` passed into `new SubmissionDetailModal(...)` deps |
| C2-10 | `SubmissionDetailModal.ts` MUST NOT import `SubmissionPickerModal` (decoupling discipline) | 09 | ✓ VERIFIED | `grep -nE "from\s+['\"]\.\/SubmissionPickerModal" src/graph/SubmissionDetailModal.ts` returns 0 matches; decoupling header at lines 26-28 present verbatim |

**Cycle 2 Score:** 9/10 code truths verified; 1 requires live dev-vault smoke (C2-6)

---

### Cycle 2 Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/main/languageChevronWidget.ts` | Portal pattern: `doc.body.appendChild(dropdown)` + `positionDropdown()` + scroll/resize listeners + updated outside-click guard | ✓ VERIFIED | All 4 structural changes confirmed by grep; file header documents G-DROPDOWN-CLIPPED rationale |
| `tests/main/languageChevronWidget.test.ts` | `G-DROPDOWN-CLIPPED` describe block; `findDropdown()` helper queries document-level; portal assertions including `parentElement === document.body` | ✓ VERIFIED | 6 G-DROPDOWN-CLIPPED occurrences; `findDropdown()` helper uses `document.querySelector`; portal assertion at L413 `expect(dropdown!.parentElement).toBe(document.body)` |
| `src/graph/SubmissionDetailModal.ts` | `onSuccess?: () => void` in `SubmissionDetailDeps`; click handler invokes it after success; decoupling header intact | ✓ VERIFIED | Field at L62; invocation at L166; header at L26-28 preserved verbatim |
| `src/graph/SubmissionPickerModal.ts` | `openDetailModal` signature widened; both renderRow handlers pass `() => this.safeClose()` | ✓ VERIFIED | Signature at L75; both call sites at L251 + L261 |
| `src/main.ts` (factory + `openSubmissionDetailFromRow`) | `onSuccess` threaded from picker factory lambda through `openSubmissionDetailFromRow` into `SubmissionDetailModal` constructor deps | ✓ VERIFIED | All three layers confirmed by grep; grep confirms `onSuccess` passed at L963, accepted at L983, forwarded at L1030 |
| `tests/graph/SubmissionDetailModal.silent-copy.test.ts` | New `G-PICKER-MODAL-NOCLOSE-ON-COPY` describe block | ✓ VERIFIED | 3 occurrences of `G-PICKER-MODAL-NOCLOSE-ON-COPY` in file |
| `tests/graph/SubmissionPickerModal.test.ts` | New `G-PICKER-MODAL-NOCLOSE-ON-COPY` describe block | ✓ VERIFIED | 1 occurrence in file; 3 behavioral it-blocks confirmed |
| `src/main/codeActionsPostProcessor.ts` | UNMODIFIED (D-09 lock) | ✓ VERIFIED | `git diff HEAD -- src/main/codeActionsPostProcessor.ts \| wc -l` = 0 |

---

### Cycle 2 Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `openDropdown()` | `doc.body.appendChild(dropdown)` | portal attach | ✓ WIRED | `languageChevronWidget.ts:163` confirmed |
| `openDropdown()` | `button.getBoundingClientRect()` | absolute coordinate computation | ✓ WIRED | `positionDropdown()` at L121 confirmed |
| `openDropdown()` | `window.addEventListener('scroll', repositionHandler, true)` + `window.addEventListener('resize', repositionHandler)` | scroll/resize listeners | ✓ WIRED | L177-178 confirmed; capture phase for scroll confirmed |
| `closeDropdown()` | `dropdown.remove()` + `window.removeEventListener(...)` | symmetric detach | ✓ WIRED | L135-156 confirmed; `if (dropdown.parentElement === doc.body) dropdown.remove()` guards idempotency |
| `SubmissionDetailModal` click handler success path | `deps.onSuccess?.()` | optional callback invocation AFTER safeClose | ✓ WIRED | L163-170: IIFE `await this.handleCopyToCode(); this.deps.onSuccess?.()` in try block — fires only on success |
| `SubmissionPickerModal renderRow` (click + keydown) | `() => this.safeClose()` | deps wiring at detail modal construction | ✓ WIRED | L251 + L261: `this.deps.openDetailModal(row, () => this.safeClose())` at both call sites |
| `src/main.ts openSubmissionPickerFromActive openDetailModal factory` | `new SubmissionDetailModal(...)` deps including `onSuccess` | factory threads onSuccess down | ✓ WIRED | L962-963 lambda accepts `onSuccess?`; L983 `openSubmissionDetailFromRow` accepts it; L1030 `onSuccess` included in constructor deps |
| `SubmissionDetailModal.ts` header | No import of `SubmissionPickerModal` | decoupling discipline | ✓ VERIFIED | 0 matches for `from './SubmissionPickerModal'` in file; header comment at L26-28 preserved |

---

### Cycle 2 Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full test suite (cycle 2 baseline) | `npm test -- --run` | 584 passed / 2 skipped / 0 failed (90 test files; 89 passed + 1 skipped) | ✓ PASS |
| Bundle size | `bash scripts/check-bundle-size.sh` | `BUNDLE CHECK OK: main.js 149 KB within budget` | ✓ PASS |
| Bundle byte count | `wc -c < main.js` | 152,852 bytes (+551 vs Plan-07 baseline 152,301; under 1,024-byte cap) | ✓ PASS |
| Lang-pack guard | `grep -RnE "from '@codemirror/lang-(python\|java\|cpp\|javascript\|go\|rust)'" src/` | 0 matches | ✓ PASS |
| D-09 Reading-Mode lock | `git diff HEAD -- src/main/codeActionsPostProcessor.ts \| wc -l` | 0 lines | ✓ PASS |
| Decoupling discipline | `grep -E "from.*SubmissionPickerModal" src/graph/SubmissionDetailModal.ts` | 0 matches | ✓ PASS |
| No debt markers (TBD/FIXME/XXX) in modified files | `grep -nE "TBD\|FIXME\|XXX" src/main/languageChevronWidget.ts src/graph/SubmissionDetailModal.ts src/graph/SubmissionPickerModal.ts src/main.ts` | 0 matches | ✓ PASS |
| G-DROPDOWN-CLIPPED test block present | `grep -c "G-DROPDOWN-CLIPPED" tests/main/languageChevronWidget.test.ts` | 6 | ✓ PASS |
| G-PICKER-MODAL-NOCLOSE-ON-COPY test blocks present | `grep -c "G-PICKER-MODAL-NOCLOSE-ON-COPY" tests/graph/SubmissionDetailModal.silent-copy.test.ts tests/graph/SubmissionPickerModal.test.ts` | 3 + 1 = 4 total | ✓ PASS |
| UAT G-PICKER-MODAL-NOCLOSE-ON-COPY stamped FIXED | `grep "FIXED.*05.3-09" .planning/phases/05.3-language-aware-editor/05.3-UAT.md` | FIXED stamp present at line 343 | ✓ PASS |
| UAT G-DROPDOWN-CLIPPED stamped FIXED | `grep -c "FIXED.*05.3-08\|05.3-08.*FIXED" .planning/phases/05.3-language-aware-editor/05.3-UAT.md` | 0 — NOT stamped | ✗ MISSING (human smoke pending) |

---

### Cycle 2 Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|---------|--------|
| `src/main/languageChevronWidget.ts` | 179-180 | Comment says "Defer attaching the outside-click listener so the click that OPENED the dropdown doesn't immediately close it via the same event-loop tick" — but `addEventListener` is synchronous, not deferred | ⚠️ Warning (CR-01 from REVIEW.md) | Misleading comment; behavior is safe because `wrapper.contains(target)` guard prevents spurious close. No functional impact in current code. Fix: either add actual `setTimeout(fn, 0)` or replace comment with accurate description of the guard. Advisory only per verification instructions. |
| `tests/graph/SubmissionDetailModal.silent-copy.test.ts` | 157-182 | Test titled "behavioral: deps.onSuccess fires after successful copy click" only asserts `modal.deps.onSuccess === onSuccess` (reference equality) — never triggers the click handler IIFE, never calls `onSuccess`, never asserts `onSuccess.toHaveBeenCalledTimes(1)` | ⚠️ Warning (CR-02 from REVIEW.md) | False confidence: if `this.deps.onSuccess?.()` were removed from the click handler, this test would still pass. The source-grep proxy tests in the same describe block cover the structural wiring; the behavioral invocation is not covered at the unit level. Advisory only per verification instructions. |
| `src/main/languageChevronWidget.ts` | 153-154, 177-178 | `window.addEventListener`/`removeEventListener` uses global `window` instead of `doc.defaultView` — breaks scroll/resize repositioning in Obsidian popout windows | ⚠️ Warning (WR-01 from REVIEW.md) | In a popout window, `doc.defaultView` is the popout's window, not main window. Scroll/resize events from the popout would not reposition the dropdown. Fix: `const win = doc.defaultView ?? window`. Advisory only. |

No TBD, FIXME, or XXX markers found in any cycle-2 modified file.

---

### Cycle 2 Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| POLISH-09 | 05.3-08, 05.3-09 | Gap-closure for language-aware editor UX polish — both plans declare `requirements: [POLISH-09]` and `gap_closure: true` | ✓ SATISFIED (gap-closure contribution) | Plans 08+09 close two of the UAT-documented non-blocking gaps that were deferred from Plans 01-07; POLISH-09 was already satisfied for its core scope by Plans 01-07. Cycle 2 extends robustness. |

---

### Cycle 2 Human Verification Required

#### 4. G-DROPDOWN-CLIPPED live-smoke (Plan 08 Task 7 — load-bearing for cycle 2 sign-off)

**Test:** In the dev vault, open an lc-slug note in Live Preview. Click `▼ Python 3`. The dropdown opens. Click any language item (e.g., Java).
**Expected (sub-step 4 — load-bearing):** The click handler fires. The fence body and opener tag rewrite to Java starter code. `lc-language` frontmatter updates to `java`. The chevron label refreshes to `▼ Java`. Also verify: outside-click closes (sub-step 5); Esc closes (sub-step 6); scroll repositions dropdown (sub-step 7); resize repositions (sub-step 8); closing the note tab while dropdown is open removes the body-portaled node from `document.body` (sub-step 9); all 8 supported languages work (sub-step 10); Default light + dark themes both pass (sub-step 11).
**Expected UAT update after passing:** Replace the G-DROPDOWN-CLIPPED entry in `05.3-UAT.md` `## Open follow-ups` with "FIXED 2026-MM-DD via Plan 05.3-08".
**Why human:** `contain: paint` CSS containment hit-test behavior is not implemented in happy-dom. The portal pattern resolves the hit-test failure architecturally; only a live Obsidian dev-vault test confirms items are actually clickable. This is the same Bucket 4 testability constraint as the original UAT Section A coloring check.

---

### Cycle 2 Gaps Summary

No blocking code gaps — all source-level must-haves for Plans 08+09 are verified in the codebase. The cycle-2 `human_needed` status is driven entirely by Plan 08's Task 7 live-smoke checkpoint, which was explicitly marked "AWAITING HUMAN VERIFY" in the SUMMARY.md and whose UAT entry has not yet been stamped FIXED.

Once the live-smoke passes and the human records the result in `05.3-UAT.md`, the G-DROPDOWN-CLIPPED UAT entry is stamped, and the three advisory warnings from REVIEW.md (CR-01 misleading comment, CR-02 incomplete behavioral test, WR-01 global window) are acknowledged as non-blocking polish items, this cycle may be promoted to `passed` by a human reviewer.

**Advisory items (non-blocking, no action required for cycle 2 closure):**
- CR-01: Misleading "Defer" comment in `openDropdown()` — behavior is safe; comment should be corrected in a future polish pass
- CR-02: "behavioral" onSuccess test asserts reference equality only, not invocation — the source-grep proxies cover structural wiring; actual invocation coverage deferred
- WR-01: Global `window` instead of `doc.defaultView` for scroll/resize — functional issue only in popout-window contexts; main-window behavior correct

---

_Cycle 2 Verified: 2026-05-12T16:45:00Z_
_Verifier: Claude (gsd-verifier)_
