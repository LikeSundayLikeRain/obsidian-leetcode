---
phase: 05.3
plan: 01
subsystem: language-aware-editor
tags:
  - codemirror
  - obsidian-plugin
  - language-pack
  - lazy-import
  - tdd-red
  - wave-0
requires:
  - "@codemirror/lang-python@6.2.1"
  - "@codemirror/lang-java@6.0.2"
  - "@codemirror/lang-cpp@6.0.3"
  - "@codemirror/lang-javascript@6.2.5"
  - "@codemirror/lang-go@6.0.1"
  - "@codemirror/lang-rust@6.0.2"
provides:
  - "Wave 0 RED contract for src/main/codeFenceLanguageExtension.ts (mapSlugToPack + computeFenceState + buildCodeFenceLanguageExtension)"
  - "Wave 0 RED contract for src/main/languagePackRegistry.ts (getLanguagePack cache + slugToPackId + error recovery + typescript pass-through)"
  - "Wave 0 RED contract for src/main/whitespaceCopyIndent.ts (computeWhitespaceCopyIndent + whitespaceCopyIndentExtension)"
  - "Pre-install main.js baseline (149,167 bytes / 145 KB) — A9 measurement input for Plan 03"
  - "A1 peer-dep verification: @codemirror/language@6.12.3 satisfies highest demanded ^6.8.0; @codemirror/autocomplete@6.20.2 satisfies highest demanded ^6.3.2"
affects:
  - package.json (6 new direct deps)
  - package-lock.json
  - tests/main/codeFenceLanguageExtension.test.ts (NEW)
  - tests/main/languagePackRegistry.test.ts (NEW)
  - tests/main/whitespaceCopyIndent.test.ts (NEW)
tech-stack:
  added:
    - "@codemirror/lang-python@6.2.1"
    - "@codemirror/lang-java@6.0.2"
    - "@codemirror/lang-cpp@6.0.3"
    - "@codemirror/lang-javascript@6.2.5"
    - "@codemirror/lang-go@6.0.1"
    - "@codemirror/lang-rust@6.0.2"
  patterns:
    - "Wave 0 TDD RED: tests fail at import-resolution because Wave 1 SUT modules do not yet exist"
    - "vi.doMock-before-dynamic-import for @codemirror/lang-* in registry tests"
    - "5.1 analog scaffolding (makeState / makeStateWithFile) reused for fence-state tests"
key-files:
  created:
    - tests/main/codeFenceLanguageExtension.test.ts
    - tests/main/languagePackRegistry.test.ts
    - tests/main/whitespaceCopyIndent.test.ts
  modified:
    - package.json
    - package-lock.json
decisions:
  - "Pin all 6 lang-* packs to exact versions (no caret) to prevent silent supply-chain upgrades — matches @leetnotion/leetcode-api and turndown precedent in package.json"
  - "Re-built esbuild output before/after install at 149,167 bytes both — confirms tree-shaking drops unused packs (no src/ imports yet); establishes Plan 03's measurement protocol"
  - "Did NOT modify esbuild.config.mjs — verified the 6 lang-* packs absent from externals (correct per D-08; they MUST bundle inline)"
  - "Did NOT modify the 5.1 analog test file — Wave 0 RED is purely additive; existing 86 test files remain green"
metrics:
  duration_minutes: 7
  duration_seconds: 449
  completed_date: "2026-05-11"
  commits: 3
  tasks_completed: 3
  files_created: 3
  files_modified: 2
  lines_added_tests: 1057
---

# Phase 5.3 Plan 01: Wave 0 — Lang-Pack Install + RED Test Contracts Summary

Land Wave 0 for Phase 5.3: install the 6 `@codemirror/lang-*` packs as direct dependencies pinned exact, write 3 RED-state vitest files that pin Wave 1's behavioral contract for every public export, verify peer-dep assumption A1 against Obsidian's runtime `@codemirror/language` major, and capture a pre-install `main.js` baseline so Plan 03 can validate A9 (expected landed 500–550 KB).

## Bottom Line

3 RED test files in place (1,057 new lines of test code total) with `vitest --run` failing on each for the load-bearing "Failed to resolve" reason against Wave 1's not-yet-existent SUT modules; 6 lang-packs pinned at exact verified versions; A1 peer-dep verified with concrete numbers (runtime 6.12.3 ≥ demanded ^6.8.0); pre-install + post-install `main.js` byte count both 149,167 (tree-shaking confirmed); zero regression in the existing 86-file Phase 5.1/5.2 test suite (507 tests pass, 2 skipped).

## What Got Done

### Task 1 (commit `c610552`) — Install 6 packs, verify A1, capture baseline

**Pre-install baseline (input for Plan 03 A9 check):**
- `main.js` bytes: **149,167** (145 KB)
- Captured via `node esbuild.config.mjs production && wc -c < main.js` BEFORE any install.

**Post-install verification:**
- `main.js` bytes: **149,167** (145 KB) — identical, confirming esbuild tree-shaking drops the unused packs (no `src/` imports yet; Wave 1 owns wiring).
- `npm ls` reports all 6 packs at exact pinned versions; zero peer-dep WARNs that block resolution.

**A1 peer-dep verification (RESEARCH Pitfall 6 — load-bearing):**

| Resolved package | Version | Highest peer demanded | Demanded by | Satisfied? |
|---|---|---|---|---|
| `@codemirror/language` | **6.12.3** | `^6.8.0` | `lang-python@6.2.1` | ✓ (6.12.3 ⊂ ^6.8.0) |
| `@codemirror/autocomplete` | **6.20.2** | `^6.3.2` | `lang-python@6.2.1` | ✓ (6.20.2 ⊂ ^6.3.2) |

Other packs declare lower lower-bounds (`^6.0.0` or `^6.6.0` for language; `^6.0.0` or not declared for autocomplete) — all satisfied by the resolved 6.12.3 / 6.20.2. **A1 verified.**

**esbuild externals verification:**
- `grep -E '@codemirror/lang-(python|java|cpp|javascript|go|rust)' esbuild.config.mjs` → **0 matches** (correct per D-08; packs must bundle inline).
- No edits to `esbuild.config.mjs` in this task; existing externals (`@codemirror/autocomplete`, `@codemirror/language`, `@codemirror/state`, `@codemirror/view`, `@codemirror/commands`, `@codemirror/lint`, `@lezer/common`, `@lezer/highlight`, `@lezer/lr`) untouched.

**Version-pinning correction:** `npm install --save` initially wrote caret-prefixed versions (`^6.0.3` etc.). Re-edited `package.json` to exact pins (no caret) per the plan's hygiene requirement — matches existing `@leetnotion/leetcode-api: 3.0.0` and `turndown: 7.2.4` precedent in the same file. `npm install` re-run to refresh `package-lock.json`; resolution unchanged.

### Task 2 (commit `a88e3ab`) — RED contract for `codeFenceLanguageExtension`

`tests/main/codeFenceLanguageExtension.test.ts` — **543 lines, 26 it() across 5 describe()**:

| Group | Cases | Behavior pinned |
|---|---|---|
| `mapSlugToPack` | 13 | python/python3 → python; c → cpp (D-05 shared parser); golang → go; csharp/ruby/mysql/empty → fallback |
| `computeFenceState — lc-slug gate` | 3 | Missing fm; empty slug; no `## Code` fence — all return EMPTY |
| `computeFenceState — caret-in-fence` | 5 | Pattern 1 strict-inside check (opener-line/closer-line/above/below all return false; strictly between returns true) |
| `computeFenceState — language routing` | 4 | python3→python, c→cpp, golang→go, csharp→fallback |
| `buildCodeFenceLanguageExtension — public API exists` | 1 | Sanity check (5.1 analog parity) |

`vi.mock('obsidian', ...)` → `tests/helpers/obsidian-stub`. `makeState` / `makeStateWithFile` helpers copied from the 5.1 analog. Zero `.skip` / `.todo`. RED reason captured at vitest run: `Failed to resolve import "../../src/main/codeFenceLanguageExtension"`.

### Task 3 (commit `bb93602`) — RED contract for `languagePackRegistry` + `whitespaceCopyIndent`

`tests/main/languagePackRegistry.test.ts` — **226 lines, 20 it() across 3 describe()**:
- 7 cache-hit tests (one per PackId: python, java, cpp, javascript, typescript, go, rust). Each does `vi.doMock('@codemirror/lang-XXX', factory)` BEFORE the dynamic SUT import (vitest hoisting rule).
- 1 typescript pass-through test asserts `javascript({ typescript: true })` (D-05 + RESEARCH line 800).
- 1 error-recovery test (RESEARCH lines 957–971): first import throws → `getLanguagePack('python')` rejects; second call retries, succeeds. Pins the `cache.delete` on `.catch` invariant.
- 12 `slugToPackId` cases: pure slug→PackId|null mapping (python3 alias, c→cpp, golang→go, csharp/ruby/empty → null).
- `beforeEach(vi.resetModules)` wipes the module-level cache between tests.

`tests/main/whitespaceCopyIndent.test.ts` — **129 lines, 7 it() across 1 describe()**:
- 6 column-math cases per RESEARCH §Wave 0 Gaps + PATTERNS lines 511–519: first line, 4-space prefix, 2-tab prefix (tab→2-space normalisation), mixed `\t  `, all-empty preceding, walks back past empty intermediate lines.
- 1 public-API sanity check that `whitespaceCopyIndentExtension` is truthy.
- Tests target the pure `computeWhitespaceCopyIndent(state, pos)` function per PATTERNS Option A.

Both files RED for the load-bearing reason: `Failed to resolve import "../../src/main/{languagePackRegistry,whitespaceCopyIndent}"`.

## Acceptance Criteria

| Criterion | Status |
|---|---|
| `npm ls @codemirror/lang-python @codemirror/lang-java @codemirror/lang-cpp @codemirror/lang-javascript @codemirror/lang-go @codemirror/lang-rust` exits 0 with all at exact pinned versions | ✓ |
| `node_modules/@codemirror/language` major satisfies highest peer-range demanded | ✓ (6.12.3 ⊂ ^6.8.0) |
| `node_modules/@codemirror/autocomplete` major satisfies highest peer-range demanded | ✓ (6.20.2 ⊂ ^6.3.2) |
| `grep -E '@codemirror/lang-(python\|java\|cpp\|javascript\|go\|rust)' esbuild.config.mjs` returns zero matches | ✓ |
| esbuild build (`node esbuild.config.mjs production`) exits 0 after install | ✓ |
| SUMMARY records pre/post `main.js` bytes, observed `@codemirror/language` + `@codemirror/autocomplete` versions, highest peer-range demanded for each | ✓ |
| No changes to `src/` or pre-existing `tests/` files | ✓ |
| `tests/main/codeFenceLanguageExtension.test.ts` exists, ≥180 lines | ✓ (543 lines) |
| `tests/main/codeFenceLanguageExtension.test.ts` has ≥20 it() and ≥5 describe() | ✓ (26 it / 5 describe) |
| `tests/main/codeFenceLanguageExtension.test.ts` imports from `../../src/main/codeFenceLanguageExtension` | ✓ (line 33–37, multi-line import) |
| Zero `.skip`/`.todo` across the 3 new files | ✓ |
| `tests/main/languagePackRegistry.test.ts` ≥90 lines, ≥12 it() | ✓ (226 / 20) |
| `tests/main/languagePackRegistry.test.ts` ≥6 `vi.doMock('@codemirror/lang-...')` calls | ✓ (9 matches) |
| `tests/main/languagePackRegistry.test.ts` has `beforeEach(vi.resetModules)` and `javascript({ typescript: true })` assertion | ✓ |
| `tests/main/whitespaceCopyIndent.test.ts` ≥60 lines, ≥6 it() | ✓ (129 / 7) |
| All 3 new files fail with module-resolution against the not-yet-existent SUT | ✓ |
| Phase 5.1/5.2 regression: 86 test files green, 507 tests pass | ✓ |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 — Bug] `npm install --save` wrote caret-prefixed versions instead of exact pins**
- **Found during:** Task 1 verification (after first `npm install --save`)
- **Issue:** `npm install --save @codemirror/lang-python@6.2.1 ...` resolved to `^6.2.1` in `package.json`, contrary to the plan's explicit "Do NOT use version ranges like `^6.2.1` — pin exact per version-pinning hygiene" requirement.
- **Fix:** Edited `package.json` to strip the carets, re-ran `npm install` to refresh `package-lock.json`. Resolution unchanged; lockfile now reflects exact pins.
- **Files modified:** `package.json` (6 lines), `package-lock.json` (refresh).
- **Commit:** `c610552` (single commit — caught and fixed before Task 1 was committed).

### Acknowledged Pre-existing Issues (out of scope per scope-boundary rule)

- `npm run build` fails the `tsc -noEmit -skipLibCheck` step due to ~28 pre-existing TS errors across `src/browse/ProblemBrowserView.ts`, `src/main/codeActionsPostProcessor.ts`, and several test files. These exist on the base commit (`644e811`) and are unrelated to Plan 5.3-01. The plan's "build still succeeds after install" criterion was interpreted to mean the **esbuild output stays viable** (it does — `node esbuild.config.mjs production` succeeds and emits a valid `main.js`); the pre-existing tsc errors are logged here as a future cleanup item but not addressed in this plan per the SCOPE BOUNDARY rule.

## Self-Check: PASSED

**Files verified:**
- `tests/main/codeFenceLanguageExtension.test.ts`: FOUND (543 lines)
- `tests/main/languagePackRegistry.test.ts`: FOUND (226 lines)
- `tests/main/whitespaceCopyIndent.test.ts`: FOUND (129 lines)
- `package.json`: FOUND (modified — 6 new exact-pinned lang-* deps)
- `package-lock.json`: FOUND (modified)

**Commits verified (in `git log`):**
- `c610552` chore(05.3-01): install 6 @codemirror/lang-* packs (Wave 0 deps): FOUND
- `a88e3ab` test(05.3-01): RED-state vitest contract for codeFenceLanguageExtension: FOUND
- `bb93602` test(05.3-01): RED-state vitest contracts for languagePackRegistry + whitespaceCopyIndent: FOUND

**Wave 0 RED signal verified (load-bearing reason):**
- `tests/main/codeFenceLanguageExtension.test.ts` → `Failed to resolve import "../../src/main/codeFenceLanguageExtension"`
- `tests/main/languagePackRegistry.test.ts` → `Failed to resolve import "../../src/main/languagePackRegistry"`
- `tests/main/whitespaceCopyIndent.test.ts` → `Failed to resolve import "../../src/main/whitespaceCopyIndent"`

**Regression verified:**
- Full `npm test --run`: 3 failed (the 3 new RED files only), 86 passed, 1 skipped (90 test files total); 507 tests pass, 2 skipped.

## Inputs to Plan 03

- **Pre-Phase-5.3 main.js baseline:** 149,167 bytes (145 KB).
- **Plan 03 measurement protocol:** Re-run `node esbuild.config.mjs production && wc -c < main.js` after Wave 1 source-code wiring (Plan 02). The Δ from 149,167 is the **landed cost** of the language packs. A9 expects ~350–400 KB Δ (landed 500–550 KB total). D-09 ceiling is 700 KB.
- **A1 verified:** Obsidian runtime `@codemirror/language@6.12.3` and `@codemirror/autocomplete@6.20.2` satisfy every pack's peer-dep range. Wave 1 will not hit a peer-dep import error.
