---
phase: 05.3
plan: 02
type: execute
wave: 2
depends_on:
  - 05.3-01
files_modified:
  - src/main/languagePackRegistry.ts
  - src/main/whitespaceCopyIndent.ts
  - src/main/codeFenceLanguageExtension.ts
  - src/main.ts
autonomous: true
requirements:
  - POLISH-09
tags:
  - codemirror
  - obsidian-plugin
  - language-pack
  - lazy-import
  - compartment
  - editor-extension

must_haves:
  truths:
    - "All 12 Wave 0 unit tests (mapSlugToPack, computeFenceState, getLanguagePack cache + error, whitespaceCopyIndent) transition from RED to GREEN"
    - "Opening an lc-slug note in Live Preview with a supported language fence activates the matching @codemirror/lang-* LanguageSupport inside the ## Code fence via per-view Compartment"
    - "Opening an lc-slug note with an unsupported language (csharp, ruby, etc.) installs the whitespace-copy indentService extension — no crash, no disabled editor"
    - "Non-lc-slug notes receive zero plugin-side language-compartment behavior (empty compartment)"
    - "Caret exiting the ## Code fence de-installs the LanguageSupport (compartment reconfigured to empty)"
    - "Phase 5.1 codeActionsEditorExtension and Phase 5.2 python3Highlighter continue to operate without regression"
    - "Pack-init failure is silently swallowed — compartment falls back to whitespace-copy; no Notice surfaced (CF-19)"
    - "View-destroyed race between pack-load and note-close is guarded — no uncaught dispatch errors"
  artifacts:
    - path: "src/main/languagePackRegistry.ts"
      provides: "Module-level lazy import() + Promise cache of LanguageSupport per PackId; exports getLanguagePack, slugToPackId, warmDefaultPack, PackId type"
      min_lines: 60
      exports: ["getLanguagePack", "slugToPackId", "warmDefaultPack", "PackId"]
    - path: "src/main/whitespaceCopyIndent.ts"
      provides: "Pure indentService extension + testable inner computeWhitespaceCopyIndent function for D-06 fallback"
      min_lines: 35
      exports: ["whitespaceCopyIndentExtension", "computeWhitespaceCopyIndent"]
    - path: "src/main/codeFenceLanguageExtension.ts"
      provides: "Per-view Compartment extension with StateField<FenceState> + ViewPlugin that swaps compartment based on caret-in-fence + language; lc-slug frontmatter gated; reuses findCodeFence from 5.1"
      min_lines: 140
      exports: ["buildCodeFenceLanguageExtension", "mapSlugToPack", "computeFenceState"]
    - path: "src/main.ts"
      provides: "registerEditorExtension(buildCodeFenceLanguageExtension(this)) wired at a new Step 6i alongside Phase 5.1's Step 6f, plus optional warmDefaultPack() call using settings.defaultLanguage"
      contains: "buildCodeFenceLanguageExtension"
  key_links:
    - from: "src/main/codeFenceLanguageExtension.ts"
      to: "src/main/codeActionsEditorExtension.ts"
      via: "import { findCodeFence }"
      pattern: "import \\{ findCodeFence \\} from './codeActionsEditorExtension'"
    - from: "src/main/codeFenceLanguageExtension.ts"
      to: "src/solve/languages.ts"
      via: "import { resolveLangSlug }"
      pattern: "import \\{ resolveLangSlug \\} from '\\.\\./solve/languages'"
    - from: "src/main/codeFenceLanguageExtension.ts"
      to: "src/main/languagePackRegistry.ts"
      via: "import { getLanguagePack, type PackId }"
      pattern: "from './languagePackRegistry'"
    - from: "src/main/codeFenceLanguageExtension.ts"
      to: "src/main/whitespaceCopyIndent.ts"
      via: "import { whitespaceCopyIndentExtension }"
      pattern: "from './whitespaceCopyIndent'"
    - from: "src/main.ts"
      to: "src/main/codeFenceLanguageExtension.ts"
      via: "this.registerEditorExtension(buildCodeFenceLanguageExtension(this))"
      pattern: "registerEditorExtension\\(buildCodeFenceLanguageExtension"
---

<objective>
Ship the Phase 5.3 language-aware editor implementation end-to-end in a single Wave-1 plan: the lazy pack registry, the whitespace-copy fallback, the per-view Compartment extension that swaps language on caret-in-fence, and the main.ts wiring. This is RESEARCH §Primary Recommendation (per-view Compartment swap as Plan B of D-11, promoted to primary after Plan A was empirically blocked — see RESEARCH Pitfall 1 + assumption A4).

Purpose: Satisfy POLISH-09 and ROADMAP §Phase 5.3 success criteria 1–5 and 7 (criterion 6 — bundle cap — enforced by Plan 03). All 12 Wave 0 RED tests transition to GREEN. Phase 5.1 / 5.2 regression surfaces stay green.

Output: 3 new source files (`src/main/{languagePackRegistry,whitespaceCopyIndent,codeFenceLanguageExtension}.ts`), 1 modified file (`src/main.ts` Step 6i + imports), and a green `npm test` full-suite run.
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.3-language-aware-editor/05.3-CONTEXT.md
@.planning/phases/05.3-language-aware-editor/05.3-RESEARCH.md
@.planning/phases/05.3-language-aware-editor/05.3-PATTERNS.md
@.planning/phases/05.3-language-aware-editor/05.3-UI-SPEC.md
@.planning/phases/05.3-language-aware-editor/05.3-01-SUMMARY.md
@src/main/codeActionsEditorExtension.ts
@src/main/python3Highlighter.ts
@src/solve/languages.ts
@src/main.ts
@esbuild.config.mjs
@tests/main/codeFenceLanguageExtension.test.ts
@tests/main/languagePackRegistry.test.ts
@tests/main/whitespaceCopyIndent.test.ts

<interfaces>
<!-- Extracted from prior-phase source + Phase 5.3 Wave 0 tests. Executor uses these directly. -->

From src/main/codeActionsEditorExtension.ts — REUSE via import, do NOT re-implement:
```typescript
export function findCodeFence(state: EditorState): { openerLine: number; closerLine: number } | null;
```
(Pure function. 1-indexed line numbers. Handles empty-doc case. See RESEARCH Pitfall 7.)

From src/solve/languages.ts — REUSE via import:
```typescript
export function resolveLangSlug(tag: string | null, fallback: string): string;
```

From tests/helpers/obsidian-stub.ts — ALREADY provides (no extension needed):
```typescript
export const editorInfoField: StateField<{ file: { path: string } | null }>;
export const editorLivePreviewField: StateField<boolean>;
```

New exports this plan creates (must match Wave 0 test imports):

From src/main/languagePackRegistry.ts:
```typescript
export type PackId = 'python' | 'java' | 'cpp' | 'javascript' | 'typescript' | 'go' | 'rust';
export function getLanguagePack(id: PackId): Promise<LanguageSupport>;
export function slugToPackId(slug: string): PackId | null;
export function warmDefaultPack(defaultLangSlug: string): void;
```

From src/main/whitespaceCopyIndent.ts:
```typescript
export const whitespaceCopyIndentExtension: Extension;
export function computeWhitespaceCopyIndent(
  state: { doc: { lineAt(pos: number): { number: number }; line(n: number): { text: string } } },
  pos: number
): number;
```

From src/main/codeFenceLanguageExtension.ts:
```typescript
export type DesiredPack = 'none' | 'fallback' | PackId;
export interface FenceState { caretInCodeFence: boolean; langSlug: string | null; desiredPack: DesiredPack; }
export function mapSlugToPack(slug: string): DesiredPack;
export function computeFenceState(state: EditorState, plugin: Plugin): FenceState;
export function buildCodeFenceLanguageExtension(plugin: Plugin): Extension;
```
</interfaces>

<design_decisions_anchored>
- D-01 (full LanguageSupport, not indent-only): load each `@codemirror/lang-*`'s default factory (e.g., `python()`) — returns LanguageSupport bundling grammar + indent + closeBrackets + highlighting.
- D-02 (Prism↔Lezer mismatch accepted): zero attempt at theme parity.
- D-03 (bracket auto-close scoped): closeBrackets is bundled inside LanguageSupport — activates only when the compartment is pointed at a pack; outside the fence, empty compartment restores Obsidian default.
- D-04 (C# excluded): no third-party pack import; C# routes to 'fallback' via mapSlugToPack default branch.
- D-05 (standard slug→pack map): python/python3→'python', java→'java', cpp/c→'cpp', javascript→'javascript', typescript→'typescript', golang→'go', rust→'rust'.
- D-06 (unsupported → whitespace-copy): single shared `whitespaceCopyIndentExtension` via `indentService`.
- D-07 (lazy import): dynamic `import()` inside registry's `loadPack` switch.
- D-08 (esbuild inline-bundle): no config change; `splitting: false` is default.
- D-10 SKIPPED / D-11 PRIMARY (per RESEARCH Pitfall 1 + assumption A4): per-view Compartment swap driven by caret-in-fence StateField.
- D-12 (lc-slug gate): `state.field(editorInfoField)?.file` + `plugin.app.metadataCache.getFileCache(file)?.frontmatter['lc-slug']`.
- D-13 (## Code-only scope): reuse `findCodeFence` from 5.1; caret must be strictly inside (caretLine > openerLine && caretLine < closerLine).
- D-16 regression: no edits to `codeActionsEditorExtension.ts`, `python3Highlighter.ts`, `codeActionsPostProcessor.ts`, `fileOpenHook.ts`.
- CF-19 (silent-on-failure): pack-load errors swallowed in swap(); fallback to whitespace-copy, no Notice.
</design_decisions_anchored>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement src/main/languagePackRegistry.ts + src/main/whitespaceCopyIndent.ts (turn Plan 01 RED → GREEN for both)</name>
  <files>src/main/languagePackRegistry.ts, src/main/whitespaceCopyIndent.ts</files>
  <read_first>
    tests/main/languagePackRegistry.test.ts,
    tests/main/whitespaceCopyIndent.test.ts,
    src/solve/languages.ts,
    src/main/python3Highlighter.ts,
    esbuild.config.mjs,
    .planning/phases/05.3-language-aware-editor/05.3-RESEARCH.md,
    .planning/phases/05.3-language-aware-editor/05.3-PATTERNS.md
  </read_first>
  <behavior>
    languagePackRegistry.ts (≥ 60 lines):
    - Exports `PackId` type = union of 'python' | 'java' | 'cpp' | 'javascript' | 'typescript' | 'go' | 'rust' (7 members — matches D-05 pack set; 'c' and 'python3' map INTO 'cpp' / 'python' at the slugToPackId layer, they are not separate PackIds).
    - Exports `slugToPackId(slug: string): PackId | null` with switch body per RESEARCH lines 496–510 and PATTERNS §"Type + switch pattern" (lines 162–175). `python`/`python3` → 'python'; `java` → 'java'; `cpp`/`c` → 'cpp'; `javascript` → 'javascript'; `typescript` → 'typescript'; `golang` → 'go'; `rust` → 'rust'; default → null.
    - Exports `getLanguagePack(id: PackId): Promise<LanguageSupport>` backed by module-level `const cache = new Map<PackId, Promise<LanguageSupport>>()`. First call for each id triggers `loadPack(id)`; subsequent calls return the cached promise. On rejection, `cache.delete(id)` is called so a retry is possible (RESEARCH lines 783–790 + PATTERNS lines 183–192).
    - Internal `async function loadPack(id: PackId): Promise<LanguageSupport>` uses the switch body from RESEARCH lines 792–803 / PATTERNS §"Cache pattern":
      * `python` → `(await import('@codemirror/lang-python')).python()`
      * `java` → `(await import('@codemirror/lang-java')).java()`
      * `cpp` → `(await import('@codemirror/lang-cpp')).cpp()`
      * `javascript` → `(await import('@codemirror/lang-javascript')).javascript()`
      * `typescript` → `(await import('@codemirror/lang-javascript')).javascript({ typescript: true })`
      * `go` → `(await import('@codemirror/lang-go')).go()`
      * `rust` → `(await import('@codemirror/lang-rust')).rust()`
      No default branch (union-exhaustive; TS enforces).
    - Exports `warmDefaultPack(defaultLangSlug: string): void` — if `slugToPackId(defaultLangSlug)` returns a non-null PackId, fire-and-forget `void getLanguagePack(id)`. Used by main.ts onload to pre-warm.
    - Imports `LanguageSupport` as a TYPE-ONLY import from `@codemirror/language` (keeps runtime zero-cost; package is external in esbuild).
    - Module file header copied stylistically from `src/solve/languages.ts` lines 1–9 and Phase 5.2's `python3Highlighter.ts` lines 1–23 — PATTERNS §"File header" lines 144–153.
    - No `innerHTML`, no `new Notice`, no console.error, no network — pure module.

    whitespaceCopyIndent.ts (≥ 35 lines):
    - Exports `computeWhitespaceCopyIndent(state, pos): number` as a named function whose body matches RESEARCH lines 525–540 and PATTERNS §"Core pattern" lines 226–237:
      * If `line.number === 1`, return 0.
      * Walk back from `line.number - 1` to 1, skipping lines where `prev.text.trim().length === 0`.
      * For the first non-empty previous line, extract leading `[\t ]*` via regex and return `leading.replace(/\t/g, '  ').length` (tab-to-2-space convention).
      * If no non-empty previous line found, return 0.
    - Exports `whitespaceCopyIndentExtension: Extension = indentService.of((ctx, pos) => computeWhitespaceCopyIndent(ctx.state as never, pos))`.
    - State parameter typed as a minimal structural interface `{ doc: { lineAt(pos: number): { number: number }; line(n: number): { text: string } } }` so Wave 0 tests can pass a fake state without constructing a real EditorState (PATTERNS §"Option A (preferred)" lines 494–504).
    - Imports `indentService` from `@codemirror/language` with the `// eslint-disable-next-line import/no-extraneous-dependencies -- transitive peer of obsidian; external in esbuild` comment pattern from PATTERNS §"Pattern: CM6 Transitive-Peer-Dep Lint Suppression".
    - Imports `Extension` as a type-only import from `@codemirror/state` with the same lint-suppression comment.
  </behavior>
  <action>
    Create `src/main/languagePackRegistry.ts` and `src/main/whitespaceCopyIndent.ts` using PATTERNS §Pattern Assignments as verbatim templates. Body contents come from RESEARCH §Code Examples (lines 771–824 for registry; lines 826–847 for indent). Do NOT import `@codemirror/language` or `@codemirror/state` at runtime — use `import type { ... }` for type-only symbols (LanguageSupport, Extension) and named `import { indentService }` (runtime; externalised by esbuild) for the single runtime symbol. Add the transitive-peer-dep lint-suppression comment above every external-CM6 import per the pattern. Do not add a default export; use named exports exclusively. The function body signatures must exactly match the `<interfaces>` block above so the Wave 0 tests resolve without modification.
  </action>
  <verify>
    <automated>npm test -- --run tests/main/languagePackRegistry.test.ts tests/main/whitespaceCopyIndent.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/languagePackRegistry.ts` exists, ≥ 60 lines.
    - `grep -E "^export (function|type|const) (getLanguagePack|slugToPackId|warmDefaultPack|PackId)" src/main/languagePackRegistry.ts` returns 4 matches (all 4 exports present).
    - `grep -cE "await import\\('@codemirror/lang-(python|java|cpp|javascript|go|rust)'\\)" src/main/languagePackRegistry.ts` returns exactly 7 (one per pack; javascript imported twice — once for JS, once for TS case).
    - `grep -E "javascript\\(\\{ typescript: true \\}\\)" src/main/languagePackRegistry.ts` returns ≥ 1 match.
    - `grep -E "cache\\.delete\\(id\\)" src/main/languagePackRegistry.ts` returns ≥ 1 match (error-recovery drop).
    - `src/main/whitespaceCopyIndent.ts` exists, ≥ 35 lines.
    - `grep -E "^export (const|function) (whitespaceCopyIndentExtension|computeWhitespaceCopyIndent)" src/main/whitespaceCopyIndent.ts` returns 2 matches.
    - `grep -E "indentService\\.of\\(" src/main/whitespaceCopyIndent.ts` returns ≥ 1 match.
    - Running `npm test -- --run tests/main/languagePackRegistry.test.ts tests/main/whitespaceCopyIndent.test.ts` reports ALL GREEN (≥ 12 registry tests + ≥ 6 indent tests).
    - `grep -v '^//' src/main/languagePackRegistry.ts src/main/whitespaceCopyIndent.ts | grep -c -E "new Notice|innerHTML|console\\.error"` returns 0 (CF-19 silent posture + no innerHTML).
    - `grep -c "eslint-disable.*no-extraneous-dependencies -- transitive peer of obsidian" src/main/whitespaceCopyIndent.ts src/main/languagePackRegistry.ts` returns ≥ 1 (required for the `indentService` runtime import in whitespaceCopyIndent; may be 0 in registry if only type imports are used).
  </acceptance_criteria>
  <done>
    Both Wave 0 test files transition from RED (module-not-found) to GREEN (all assertions pass); no Phase 5.1/5.2 regression; no innerHTML / no Notices / no remote code eval.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement src/main/codeFenceLanguageExtension.ts (per-view Compartment swap — D-11 primary path)</name>
  <files>src/main/codeFenceLanguageExtension.ts</files>
  <read_first>
    tests/main/codeFenceLanguageExtension.test.ts,
    src/main/codeActionsEditorExtension.ts,
    src/main/languagePackRegistry.ts,
    src/main/whitespaceCopyIndent.ts,
    src/solve/languages.ts,
    src/settings/SettingsStore.ts,
    .planning/phases/05.3-language-aware-editor/05.3-RESEARCH.md,
    .planning/phases/05.3-language-aware-editor/05.3-PATTERNS.md
  </read_first>
  <behavior>
    - File header + transitive-peer-dep lint suppression block mirror PATTERNS §"Imports pattern" lines 39–66.
    - Exports `type DesiredPack = 'none' | 'fallback' | PackId;` and `interface FenceState { caretInCodeFence: boolean; langSlug: string | null; desiredPack: DesiredPack; }` plus a module-level `const EMPTY: FenceState = { caretInCodeFence: false, langSlug: null, desiredPack: 'none' };`.
    - Exports `function mapSlugToPack(slug: string): DesiredPack` whose body is a switch per RESEARCH lines 381–394: 'python'/'python3' → 'python'; 'java' → 'java'; 'cpp'/'c' → 'cpp'; 'javascript' → 'javascript'; 'typescript' → 'typescript'; 'golang' → 'go'; 'rust' → 'rust'; default → 'fallback'. Note this differs from `slugToPackId` (returns `null` for unsupported) — this one returns 'fallback' for unsupported to drive the whitespace-copy path.
    - Exports `function computeFenceState(state: EditorState, plugin: Plugin): FenceState` — the pure function whose behavior matches RESEARCH lines 354–379 and PATTERNS §"Frontmatter lc-slug gate pattern" + §"Caret-in-fence math":
      1. Read `state.field(editorInfoField)?.file`; if null, return EMPTY.
      2. Read `plugin.app.metadataCache.getFileCache(file)?.frontmatter` cast to `Record<string, unknown> | undefined`; read `['lc-slug']`; if not a non-empty string, return EMPTY.
      3. Call `findCodeFence(state)` (imported from `./codeActionsEditorExtension`); if null, return EMPTY.
      4. Compute `caretLine = state.doc.lineAt(state.selection.main.head).number` (1-indexed).
      5. Compute `inFence = caretLine > fence.openerLine && caretLine < fence.closerLine`; if false, return EMPTY.
      6. Extract opener-line text via `state.doc.line(fence.openerLine).text`; regex `/^\s*```\s*(\S+)?/` extracts tag; `fenceTag = tagMatch?.[1] ?? null`.
      7. `langSlug = resolveLangSlug(fenceTag, settings.defaultLanguage ?? '')` — use `plugin.app` cast to access settings if the plugin exposes them via a public accessor; otherwise pass an empty-string fallback and document the limitation in SUMMARY. (Phase 5.1 analog passes empty fallback; mirror that. Keeping parity with 5.1 avoids coupling 5.3 to SettingsStore internals.)
      8. Return `{ caretInCodeFence: true, langSlug, desiredPack: mapSlugToPack(langSlug) }`.
    - Exports `function buildCodeFenceLanguageExtension(plugin: Plugin): Extension` returning the array `[fenceStateField, viewPlugin, languageCompartment.of([])]` per RESEARCH lines 396–450 and PATTERNS §"StateField factory shape":
      * `const languageCompartment = new Compartment();`
      * `const fenceStateField = StateField.define<FenceState>({ create, update })` with update running `computeFenceState(tr.state, plugin)` only when `tr.docChanged || tr.selection || modeFlipped` (modeFlipped = `tr.state.field(editorLivePreviewField) !== tr.startState.field(editorLivePreviewField)`).
      * `const viewPlugin = ViewPlugin.fromClass(class { ... })` whose `constructor(view)` triggers initial `void this.swap(s)` if mounted caret is in fence, and whose `update(vu)` guards `if (prev.desiredPack !== next.desiredPack) void this.swap(next);` (RESEARCH Pitfall 5 — load-bearing guard).
      * `private async swap(target: FenceState)` body per RESEARCH lines 425–442:
        - `desiredPack === 'none'` → `ext = []` (empty array).
        - `desiredPack === 'fallback'` → `ext = whitespaceCopyIndentExtension`.
        - otherwise `ext = await getLanguagePack(target.desiredPack)` inside try/catch; on catch, `ext = whitespaceCopyIndentExtension` (silent CF-19 fallback).
        - Guard `if ((this.view as EditorView & { destroyed?: boolean }).destroyed) return;` before dispatch (RESEARCH Pitfall 3).
        - `this.view.dispatch({ effects: languageCompartment.reconfigure(ext) });`
    - Transitive-peer imports:
      ```typescript
      // eslint-disable-next-line import/no-extraneous-dependencies -- transitive peer of obsidian; external in esbuild
      import { StateField, Compartment, type EditorState, type Extension } from '@codemirror/state';
      // eslint-disable-next-line import/no-extraneous-dependencies -- transitive peer of obsidian; external in esbuild
      import { ViewPlugin, type EditorView, type ViewUpdate } from '@codemirror/view';
      import { editorInfoField, editorLivePreviewField, type Plugin } from 'obsidian';
      import { findCodeFence } from './codeActionsEditorExtension';
      import { resolveLangSlug } from '../solve/languages';
      import { getLanguagePack, type PackId } from './languagePackRegistry';
      import { whitespaceCopyIndentExtension } from './whitespaceCopyIndent';
      ```
    - No `view.dispatch(...)` called from inside `update(vu)` synchronously (RESEARCH Pitfall 2 — must defer via `void this.swap(...)`).
    - No default export; named exports only.
    - No `new Notice`, no `innerHTML`, no console.error on the swap path; all error handling is a silent catch that installs whitespace-copy.
  </behavior>
  <action>
    Create `src/main/codeFenceLanguageExtension.ts` following PATTERNS §"src/main/codeFenceLanguageExtension.ts (CM6 extension, event-driven)" as verbatim template (the entire file structure — imports, type declarations, EMPTY sentinel, computeFenceState body, StateField factory, ViewPlugin, return — is prescribed). Body contents come from RESEARCH §Architecture Patterns §Pattern 1 (lines 333–450) — this is a canonical CM6 pattern composed with Phase 5.1's lc-slug gate discipline. Reuse `findCodeFence` from the Phase 5.1 file (do NOT duplicate its regex). Apply the view-destroyed guard per RESEARCH Pitfall 3. Apply the `prev.desiredPack !== next.desiredPack` early-exit guard per RESEARCH Pitfall 5.
  </action>
  <verify>
    <automated>npm test -- --run tests/main/codeFenceLanguageExtension.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/codeFenceLanguageExtension.ts` exists, ≥ 140 lines.
    - `grep -E "^export (function|type|interface) (mapSlugToPack|computeFenceState|buildCodeFenceLanguageExtension|DesiredPack|FenceState)" src/main/codeFenceLanguageExtension.ts` returns ≥ 3 matches (at minimum the 3 callable/type exports test-referenced).
    - `grep "import { findCodeFence } from './codeActionsEditorExtension'" src/main/codeFenceLanguageExtension.ts` returns ≥ 1 (5.1 reuse anchor — D-16 regression protection).
    - `grep "import { resolveLangSlug } from '../solve/languages'" src/main/codeFenceLanguageExtension.ts` returns ≥ 1.
    - `grep "import { getLanguagePack" src/main/codeFenceLanguageExtension.ts` returns ≥ 1.
    - `grep "import { whitespaceCopyIndentExtension" src/main/codeFenceLanguageExtension.ts` returns ≥ 1.
    - `grep -E "new Compartment\\(\\)" src/main/codeFenceLanguageExtension.ts` returns ≥ 1.
    - `grep -E "StateField\\.define<FenceState>" src/main/codeFenceLanguageExtension.ts` returns ≥ 1.
    - `grep -E "ViewPlugin\\.fromClass" src/main/codeFenceLanguageExtension.ts` returns ≥ 1.
    - `grep -E "prev\\.desiredPack !== next\\.desiredPack" src/main/codeFenceLanguageExtension.ts` returns ≥ 1 (Pitfall 5 guard).
    - `grep -E "destroyed" src/main/codeFenceLanguageExtension.ts` returns ≥ 1 (Pitfall 3 guard).
    - `grep -v '^[[:space:]]*//' src/main/codeFenceLanguageExtension.ts | grep -c -E "new Notice\\(|innerHTML"` returns 0.
    - `grep -E "editorLivePreviewField" src/main/codeFenceLanguageExtension.ts` returns ≥ 1 (mode-flip detection).
    - Running `npm test -- --run tests/main/codeFenceLanguageExtension.test.ts` reports ALL GREEN (all Wave-0 RED tests from Plan 01 Task 2 now pass).
    - `tests/main/codeActionsEditorExtension.test.ts` continues to pass (D-16 regression).
  </acceptance_criteria>
  <done>
    Wave-0 RED test file transitions to GREEN. 5.1 `codeActionsEditorExtension` file and its test are unmodified and still green. Per-view Compartment swap is the primary implementation path (Plan B of D-11) — no attempt at Plan A markdown-codeLanguages augmentation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire src/main.ts Step 6i — register the 5.3 editor extension + warmDefaultPack + verify no regression across full suite and npm run build</name>
  <files>src/main.ts</files>
  <read_first>
    src/main.ts,
    src/main/codeFenceLanguageExtension.ts,
    src/main/languagePackRegistry.ts,
    src/settings/SettingsStore.ts,
    .planning/phases/05.3-language-aware-editor/05.3-PATTERNS.md
  </read_first>
  <behavior>
    - `src/main.ts` at the import block (near existing `import { buildCodeActionsEditorExtension } from './main/codeActionsEditorExtension';` on ~line 61) gains two new imports:
      `import { buildCodeFenceLanguageExtension } from './main/codeFenceLanguageExtension';`
      `import { warmDefaultPack } from './main/languagePackRegistry';`
    - Inside `onload()`, immediately AFTER Step 6h's `registerPython3Highlighter(this)` call (~line 390) and BEFORE the existing onload epilogue, a new Step 6i block is added containing:
      * A comment block naming the step "Step 6i — Phase 5.3 (POLISH-09) language-aware editor for `## Code` fence." per PATTERNS §"New Step 6i" lines 302–310.
      * `this.registerEditorExtension(buildCodeFenceLanguageExtension(this));`
      * `warmDefaultPack(this.settings.getDefaultLanguage());` — fire-and-forget pack warm for the user's configured default language. If `this.settings.getDefaultLanguage()` is not available (Settings accessor shape check during execution), use `this.settings.defaultLanguage` OR skip the warm call and record in SUMMARY. Warm call is optional per RESEARCH Claude's Discretion; skipping is acceptable.
    - Existing Step 6f (`this.registerEditorExtension(buildCodeActionsEditorExtension(this));`) must remain unchanged — D-16 regression protection.
    - Existing Step 6h (`registerPython3Highlighter(this);`) must remain unchanged.
    - `onunload()` requires no change — Obsidian tears down `registerEditorExtension` automatically (verified by inspecting src/main.ts::onunload and prior 5.1 pattern).
    - After wiring, `npm test` full-suite passes + `npm run build` exits 0.
    - Post-build, `ls -la main.js*` shows only `main.js` and (optionally) `main.js.map` — NO additional `.js` chunk files (validates assumption A8 — esbuild `splitting: false` inlines dynamic imports).
  </behavior>
  <action>
    Edit `src/main.ts` to add the two new imports at the import block (preserve existing import order style) and add the Step 6i block inside `onload()` directly after the existing Step 6h `registerPython3Highlighter(this);` call. Do NOT touch any other section of `src/main.ts`. Do NOT touch `src/main/codeActionsEditorExtension.ts`, `src/main/codeActionsPostProcessor.ts`, `src/main/python3Highlighter.ts`, `src/main/fileOpenHook.ts` (D-16). After the edit, run `npm test` + `npm run build` and confirm both green. If `this.settings.getDefaultLanguage()` does not exist on the Settings accessor surface, switch to `this.settings.defaultLanguage` or omit the warm call; record in SUMMARY whichever path was taken. Do NOT create a new SettingsStore accessor in this task — that would be scope creep.
  </action>
  <verify>
    <automated>npm test -- --run &amp;&amp; npm run build &amp;&amp; test -f main.js &amp;&amp; [ $(ls main.js*.js 2>/dev/null | wc -l) -le 1 ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep "buildCodeFenceLanguageExtension" src/main.ts` returns ≥ 2 matches (import + registerEditorExtension call).
    - `grep "this.registerEditorExtension(buildCodeFenceLanguageExtension(this))" src/main.ts` returns exactly 1.
    - `grep "this.registerEditorExtension(buildCodeActionsEditorExtension(this))" src/main.ts` returns exactly 1 (D-16 Phase 5.1 registration unchanged).
    - `grep "registerPython3Highlighter(this)" src/main.ts` returns ≥ 1 (D-16 Phase 5.2 registration unchanged).
    - `grep -E "Step 6i.*Phase 5\\.3" src/main.ts` returns ≥ 1 (step-numbering discipline).
    - `npm run build` exits 0.
    - After build, `ls main.js*.js 2>/dev/null | wc -l` is ≤ 1 (only `main.js`; A8 satisfied — esbuild did not emit chunk files).
    - `main.js` exists and is a single CJS file.
    - `npm test -- --run` reports all tests GREEN across the full suite (Phase 5.1, 5.2, 5.3 Wave 0 tests now all pass).
    - `grep -c -E "^import\\s" src/main.ts` is at most 3 greater than the pre-Task-3 baseline (bounded import-surface growth).
    - Git diff on `src/main.ts` touches only the import block and the Step 6i addition — no other lines modified.
  </acceptance_criteria>
  <done>
    Phase 5.3 extension is registered in onload() alongside Phase 5.1 / 5.2 surfaces. Full `npm test` suite green. `npm run build` produces a single `main.js` (A8 verified — no chunk files). No regression in 5.1 edit-mode buttons or 5.2 python3 Prism alias wiring.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User document → CM6 grammar parser | User-typed code inside the ## Code fence fed to @lezer/* grammar tables; untrusted text input |
| Lang-pack bundle → Obsidian editor | Grammar tables loaded via dynamic import() resolve to code bundled into main.js at build time (D-08 inline bundle, NOT runtime fetch) |
| Frontmatter → Plugin gate | lc-slug key read from app.metadataCache — plugin already trusts this surface (Phase 5.1 uses identical gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.3-02-01 | Tampering | User crafts malformed fence content to trigger grammar parser crash | accept | Lezer grammars are tolerant by design: malformed input yields `⚠` error nodes in the tree; indent math + bracket handling continue against the last-valid partial tree. No crash path documented upstream across 6 packs. UI-SPEC §"Error surfacing (by deliberate non-decision)" confirms. |
| T-05.3-02-02 | Denial of Service | Rapid caret-move keystrokes trigger excessive Compartment reconfigure → editor re-highlight storm | mitigate | ViewPlugin.update gates on `prev.desiredPack !== next.desiredPack` (RESEARCH Pitfall 5) — the load-bearing equality check that collapses every scroll/focus/phantom selectionSet to a no-op when the target pack hasn't changed. |
| T-05.3-02-03 | Denial of Service | Async pack-load races view-destroy; uncaught dispatch crashes Obsidian | mitigate | `swap()` guards `if ((this.view as EditorView & { destroyed?: boolean }).destroyed) return;` before dispatching (RESEARCH Pitfall 3). Silent no-op on destroyed view. |
| T-05.3-02-04 | Information Disclosure | Pack-load error message leaked via Notice / console | mitigate | Silent swallow in swap() try/catch per CF-19 + UI-SPEC §"Error surfacing". No `new Notice`, no `console.error`. Fallback to whitespace-copy is the user-visible signal. |
| T-05.3-02-05 | Elevation of Privilege | Dynamic import() interpreted as remote code eval by plugin reviewers | accept | All imports are bundled into main.js at build time by esbuild (D-08 `splitting: false`). No runtime fetch, no `new Function()`, no `eval()`. Plugin review precedent: many store-approved plugins use `import()` for the same pattern. |
| T-05.3-02-06 | Spoofing | Non-LC note impersonates an lc-slug note | N/A | lc-slug gate reads `app.metadataCache.getFileCache(file)?.frontmatter['lc-slug']` — Obsidian's authoritative metadata cache, not user-suppliable at runtime. Same gate Phase 5.1 uses; no new surface. |
| T-05.3-02-07 | Repudiation | N/A — no audit-logged action | N/A | Phase 5.3 emits no user-facing action; nothing to log. |
</threat_model>

<verification>
After all three tasks:

```bash
# Full unit test suite green (RED → GREEN transition for 3 new test files)
npm test -- --run

# Build green and single-file
npm run build
test -f main.js
[ $(ls main.js*.js 2>/dev/null | wc -l) -le 1 ]   # A8: no chunk files

# 5.1 / 5.2 file invariants (D-16)
git diff --name-only src/main/codeActionsEditorExtension.ts src/main/python3Highlighter.ts src/main/codeActionsPostProcessor.ts src/main/fileOpenHook.ts
# Expected: zero output (no edits in this plan)

# main.ts edits bounded
git diff --stat src/main.ts
# Expected: additions ≈ 3–8 lines; deletions 0

# Lint passes
npm run lint
```

All must pass before proceeding to Plan 03.
</verification>

<success_criteria>
1. 3 new source files compile under strict TS; exports match `<interfaces>` block exactly.
2. 12+ Wave 0 unit tests from Plan 01 all GREEN.
3. 5.1 + 5.2 test files green, no edits to their source files (D-16).
4. `npm run build` exits 0; single `main.js` produced (A8 chunk-less verified).
5. Step 6i wired in `src/main.ts::onload()` alongside existing Steps 6f + 6h.
6. No `new Notice`, no `innerHTML`, no `console.error` in any new source file (CF-05 + CF-19).
7. Per-view Compartment is Plan B of D-11 (Plan A skipped per RESEARCH Pitfall 1 + A4).
</success_criteria>

<output>
After completion, create `.planning/phases/05.3-language-aware-editor/05.3-02-SUMMARY.md` recording:
- Final post-Wave-1 `main.js` byte count (feeds Plan 03 bundle-gate validation + A9 check vs. the 500–550 KB estimate)
- Whether `warmDefaultPack(settings.getDefaultLanguage())` was wired (or omitted — note the shape of the Settings accessor encountered)
- Confirmation all 5.1 / 5.2 regression source files were not touched (D-16)
- Output of `ls main.js*.js 2>/dev/null | wc -l` (must be ≤ 1 for A8)
- Full test-suite pass count
- Line count of each new source file
</output>
