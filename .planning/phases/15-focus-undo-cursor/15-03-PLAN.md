---
phase: 15-focus-undo-cursor
plan: 03
type: execute
wave: 3
depends_on: [15-01, 15-02]
files_modified:
  - src/main/childEditorFactory.ts
  - src/main/nestedEditorExtension.ts
autonomous: false
requirements: [INDENT-01, INDENT-02, INDENT-03, INDENT-04]

must_haves:
  truths:
    - "Scroll-into-view extension is wired into the child editor at creation time"
    - "Child editor has no height constraint in DOM (auto-grows visually)"
    - "Full integration works: Tab indents, undo isolated, buttons retain focus, scroll works"
  artifacts:
    - path: "src/main/childEditorFactory.ts"
      provides: "createScrollIntoViewExtension wired into child extensions"
      contains: "createScrollIntoViewExtension"
  key_links:
    - from: "src/main/childEditorFactory.ts"
      to: "src/main/childEditorSync.ts"
      via: "import createScrollIntoViewExtension"
      pattern: "createScrollIntoViewExtension"
---

<objective>
Wire the scroll-into-view extension into the child editor factory and perform human verification of the complete Phase 15 UX: Tab indent, undo isolation, focus retention, auto-grow, and scroll behavior.

Purpose: Connects Plan 01 (indent/undo) and Plan 02 (buttons/CSS/scroll extension) into a working integrated experience, then verifies with real user interaction.
Output: Modified childEditorFactory.ts with scroll extension wired in; human-verified UX.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-focus-undo-cursor/15-CONTEXT.md
@.planning/phases/15-focus-undo-cursor/15-RESEARCH.md
@.planning/phases/15-focus-undo-cursor/15-01-SUMMARY.md
@.planning/phases/15-focus-undo-cursor/15-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plans 01 and 02 -->

From src/main/childEditorSync.ts (after Plan 02):
- `export function createScrollIntoViewExtension(): Extension`
- `export const syncAnnotation = Annotation.define<boolean>()`

From src/main/childEditorFactory.ts (after Plan 01):
- `export function createChildEditor(content: string, parent: HTMLElement, syncExtensions?: Extension[]): EditorView`
- Extensions array includes: indentWithTab keymap, indentUnit.of("    "), history(), bracketMatching(), etc.
- syncExtensions spread at end of extensions array

From src/main/nestedEditorExtension.ts:
- `NestedEditorWidget.toDOM()` calls `createChildEditor` — may need scroll extension passed via syncExtensions or added directly in factory
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire scroll-into-view extension into child editor factory</name>
  <files>src/main/childEditorFactory.ts</files>
  <read_first>
    - src/main/childEditorFactory.ts (current state after Plan 01 modifications — imports and extensions array)
    - src/main/childEditorSync.ts (createScrollIntoViewExtension export signature)
    - src/main/nestedEditorExtension.ts (how createChildEditor is called — what syncExtensions are passed)
  </read_first>
  <action>
    In childEditorFactory.ts:
    1. Add import: `import { createScrollIntoViewExtension } from './childEditorSync';`
    2. Add `createScrollIntoViewExtension()` to the extensions array — place it BEFORE the `...(syncExtensions ?? [])` spread (last position before spread). This ensures scroll-into-view is always active in the child regardless of whether sync extensions are passed.

    Rationale for placement in factory (not passed via syncExtensions): The scroll-into-view is a core child editor capability (D-14), not an optional sync addon. It should always be present. Placing it in the factory extensions array (like indentWithTab) makes it unconditional.
  </action>
  <verify>
    <automated>cd /Users/moxu/projects/obsidian-leetcode && npx tsc --noEmit --skipLibCheck 2>&1 | head -20 && grep -c "createScrollIntoViewExtension" src/main/childEditorFactory.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/main/childEditorFactory.ts imports `createScrollIntoViewExtension` from `./childEditorSync`
    - src/main/childEditorFactory.ts extensions array includes `createScrollIntoViewExtension()` call
    - TypeScript compiles without errors
    - grep -c "createScrollIntoViewExtension" src/main/childEditorFactory.ts returns >= 2 (import + usage)
  </acceptance_criteria>
  <done>Scroll-into-view extension is active in every child editor instance</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 15 integration: Tab/Shift-Tab indent (4 spaces), undo isolation (child Cmd-Z doesn't affect parent), button focus retention (clicking Run/Submit keeps cursor in code), auto-grow (no inner scrollbar), and scroll-into-view (parent scrolls when typing at bottom of code area).
  </what-built>
  <how-to-verify>
    1. Open Obsidian with the dev vault containing the plugin
    2. Open any lc-slug note (e.g., two-sum) that has the nested editor visible
    3. **Tab indent test:** Click into the child code editor. Press Tab — verify 4 spaces are inserted (not focus change). Press Shift-Tab — verify indentation is removed. Select multiple lines, press Tab — all indent together.
    4. **Undo isolation test:** Type something in the child editor. Press Cmd-Z — verify only the child edit is undone. Click into ## Notes and type something. Press Cmd-Z — verify only the Notes edit is undone (not a child-sync change).
    5. **Focus retention test:** While cursor is in the child editor, click the Run button. Verify the run executes but focus/cursor remains in the child editor (cursor should still be blinking in code area).
    6. **Auto-grow test:** Paste or type 20+ lines of code into the child editor. Verify no inner scrollbar appears — the code area grows and the note scrolls as one document.
    7. **Scroll-into-view test:** Scroll the note so the code area is partially below the viewport. Click at the bottom line of the code and type. Verify the parent note auto-scrolls to keep the cursor visible.
    8. **No Escape exit:** Press Escape in the child editor — verify nothing happens (no focus change).
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Integration wiring only — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | (none applicable) | (none) | accept | No security-relevant changes in this plan |
</threat_model>

<verification>
```bash
cd /Users/moxu/projects/obsidian-leetcode && npm test && npx tsc --noEmit --skipLibCheck
```
Full test suite green + TypeScript compiles + human verification of UX.
</verification>

<success_criteria>
- Scroll-into-view is wired unconditionally into every child editor
- Human verifies: Tab indents 4 spaces, Shift-Tab dedents, multi-line indent works
- Human verifies: Cmd-Z in child undoes child only, Cmd-Z in parent undoes parent only
- Human verifies: Button click does not steal focus
- Human verifies: No inner scrollbar on long code
- Human verifies: Parent auto-scrolls when typing at bottom
- Human verifies: Escape does nothing (D-03)
- Full test suite passes
</success_criteria>

<output>
Create `.planning/phases/15-focus-undo-cursor/15-03-SUMMARY.md` when done
</output>
