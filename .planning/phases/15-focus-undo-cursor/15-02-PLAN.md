---
phase: 15-focus-undo-cursor
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/main/codeBlockButtonRow.ts
  - src/main/childEditorSync.ts
  - styles.css
  - tests/main/codeBlockButtonRow.test.ts
autonomous: true
requirements: [INDENT-01, INDENT-02, INDENT-03, INDENT-04]

must_haves:
  truths:
    - "Run/Submit/AI Solution buttons do not steal focus from the child editor when clicked"
    - "Child editor grows with content — no inner scrollbar ever appears"
    - "Typing at the bottom of the code area auto-scrolls the parent viewport to keep cursor visible"
    - "Parent-to-child sync does NOT trigger scroll-into-view"
  artifacts:
    - path: "src/main/codeBlockButtonRow.ts"
      provides: "mousedown preventDefault on all action buttons"
      contains: "mousedown"
    - path: "styles.css"
      provides: "Auto-grow CSS for nested editor"
      contains: "overflow"
    - path: "src/main/childEditorSync.ts"
      provides: "createScrollIntoViewExtension function"
      contains: "createScrollIntoViewExtension"
  key_links:
    - from: "src/main/codeBlockButtonRow.ts"
      to: "DOM mousedown event"
      via: "addEventListener('mousedown')"
      pattern: "mousedown.*preventDefault"
    - from: "src/main/childEditorSync.ts"
      to: "syncAnnotation"
      via: "scroll-into-view gating"
      pattern: "syncAnnotation"
    - from: "styles.css"
      to: ".lc-nested-editor .cm-scroller"
      via: "CSS overflow rule"
      pattern: "overflow.*visible"
---

<objective>
Deliver focus retention on button click, auto-grow CSS, and scroll-into-view extension so the child editor behaves as an integrated part of the note rather than a foreign widget.

Purpose: Completes D-02 (button focus retention), D-13/D-15 (auto-grow), and D-14 (scroll-into-view) — the visual/scroll UX that makes the editor feel native.
Output: Modified codeBlockButtonRow.ts with mousedown handlers, new CSS rules in styles.css, new createScrollIntoViewExtension in childEditorSync.ts, and updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-focus-undo-cursor/15-CONTEXT.md
@.planning/phases/15-focus-undo-cursor/15-RESEARCH.md
@.planning/phases/15-focus-undo-cursor/15-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/main/codeBlockButtonRow.ts:
- `export function buildCodeBlockButtonRow(doc: Document, plugin: Plugin & CodeBlockButtonRowHost, opts?: CodeBlockButtonRowOptions): HTMLDivElement`
- Three buttons created: aiSolBtn (line 48), runBtn (line 58), submitBtn (line 68)
- Each already has click handler with `e.preventDefault()` + `e.stopPropagation()`

From src/main/childEditorSync.ts:
- `export const syncAnnotation = Annotation.define<boolean>()`
- `export function createChildSyncExtension(parentView, filePath, registry): Extension`
- Echo prevention gate pattern: `if (update.transactions.some((tr) => tr.annotation(syncAnnotation))) return;`
- Export pattern: module exports functions with JSDoc, naming convention `create<Capability>Extension`

From styles.css:
- Lines 1899-1918: existing `.cm-editor .lc-nested-editor` rules
- Line 1904: `.cm-editor .lc-nested-editor .cm-editor { background: transparent; }`
- Comment convention: Phase header comment + capability description

From tests/main/codeBlockButtonRow.test.ts:
- `withHostMethods()` helper creates mock plugin
- Event testing pattern: create MouseEvent, override preventDefault, dispatchEvent, check flag
- Import: `buildCodeBlockButtonRow` from source, `createFakePlugin` from test helpers
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mousedown preventDefault to all action buttons for focus retention</name>
  <files>src/main/codeBlockButtonRow.ts, tests/main/codeBlockButtonRow.test.ts</files>
  <read_first>
    - src/main/codeBlockButtonRow.ts (button creation pattern, existing event handlers)
    - tests/main/codeBlockButtonRow.test.ts (MouseEvent dispatch test pattern at lines 88-106)
    - .planning/phases/15-focus-undo-cursor/15-PATTERNS.md (mousedown handler pattern, test pattern)
  </read_first>
  <behavior>
    - Test: Run button mousedown event has defaultPrevented = true
    - Test: Submit button mousedown event has defaultPrevented = true
    - Test: AI Solution button mousedown event has defaultPrevented = true
  </behavior>
  <action>
    In codeBlockButtonRow.ts:
    1. Add mousedown handler to aiSolBtn (after line 49, before click handler): `aiSolBtn.addEventListener('mousedown', (e) => { e.preventDefault(); });`
    2. Add mousedown handler to runBtn (after line 58, before click handler): `runBtn.addEventListener('mousedown', (e) => { e.preventDefault(); });`
    3. Add mousedown handler to submitBtn (after line 68, before click handler): `submitBtn.addEventListener('mousedown', (e) => { e.preventDefault(); });`
    Each mousedown handler calls only `e.preventDefault()` — this prevents focus transfer when the button is clicked, per D-02. The existing click handlers continue to fire and invoke plugin methods.

    In tests/main/codeBlockButtonRow.test.ts:
    1. Add a new describe block "button focus retention (D-02)".
    2. Add test for each button (Run, Submit, AI Solution) that dispatches a mousedown MouseEvent and asserts `defaultPrevented` is true. Follow the existing pattern from lines 88-106 (AI Solution click test) but use event type 'mousedown'.
  </action>
  <verify>
    <automated>cd /Users/moxu/projects/obsidian-leetcode && npm test -- --run tests/main/codeBlockButtonRow.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "mousedown" src/main/codeBlockButtonRow.ts returns exactly 3 (one per button)
    - Each button (aiSolBtn, runBtn, submitBtn) has addEventListener('mousedown', ...) with e.preventDefault()
    - Test "Run button mousedown preventDefault" passes
    - Test "Submit button mousedown preventDefault" passes
    - Test "AI Solution button mousedown preventDefault" passes
    - All existing click handler tests still pass (no regression)
  </acceptance_criteria>
  <done>Clicking Run/Submit/AI Solution does not steal focus from child editor per D-02</done>
</task>

<task type="auto">
  <name>Task 2: Add auto-grow CSS and scroll-into-view extension</name>
  <files>styles.css, src/main/childEditorSync.ts</files>
  <read_first>
    - styles.css lines 1895-1918 (existing nested editor CSS rules)
    - src/main/childEditorSync.ts (syncAnnotation export, updateListener pattern, echo prevention gate)
    - .planning/phases/15-focus-undo-cursor/15-RESEARCH.md (Pattern 3: auto-grow CSS, Pattern 4: scroll-into-view, Pitfall 3 and 4)
    - .planning/phases/15-focus-undo-cursor/15-PATTERNS.md (CSS comment convention, export naming convention)
  </read_first>
  <action>
    In styles.css:
    1. After line 1918 (end of existing nested editor rules), add a Phase 15 comment block and auto-grow rules:
       - Comment: `/* Phase 15 -- Auto-grow: disable child independent scrolling so parent scroll container handles all scrolling. Child height tracks content (D-13). */`
       - Rule: `.cm-editor .lc-nested-editor .cm-scroller { overflow: visible !important; }`
       - Merge into existing rule at line 1904: `.cm-editor .lc-nested-editor .cm-editor` — add `height: auto !important; max-height: none !important;` to the existing `background: transparent` declaration.

    In childEditorSync.ts:
    1. Add a new exported function `createScrollIntoViewExtension(): Extension` at the end of the file (before the fence repair section, after the wireSyncIfNeeded/unwireSync section).
    2. Implementation: `EditorView.updateListener.of((update) => { ... })` that:
       a. Early-returns if `!update.docChanged && !update.selectionSet`
       b. Early-returns if any transaction carries `syncAnnotation` (echo prevention gate — Pitfall 4)
       c. Gets cursor coordinates via `update.view.coordsAtPos(update.state.selection.main.head)`
       d. Early-returns if coords is null
       e. Finds the parent scroll container: traverse up from `update.view.dom` using `.closest('.cm-scroller')` to find the CHILD scroller, then `.closest('.cm-editor')?.parentElement?.closest('.cm-scroller')` to find the PARENT scroller. If parentScroller is null, try fallback `.closest('.workspace-leaf-content')`.
       f. Gets `scrollerRect = parentScroller.getBoundingClientRect()`
       g. If `coords.bottom > scrollerRect.bottom - 40` (40px margin), scrolls: `parentScroller.scrollTop += (coords.bottom - scrollerRect.bottom + 40)`
    3. Wrap the function body in try/catch (silently ignore — defensive per project convention).
    4. Add JSDoc comment per module convention: explains purpose, gating, and the D-14 reference.
  </action>
  <verify>
    <automated>cd /Users/moxu/projects/obsidian-leetcode && npx tsc --noEmit --skipLibCheck 2>&1 | head -20 && grep -c "createScrollIntoViewExtension" src/main/childEditorSync.ts && grep -c "overflow" styles.css | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - styles.css contains `.cm-editor .lc-nested-editor .cm-scroller { overflow: visible !important; }`
    - styles.css rule for `.cm-editor .lc-nested-editor .cm-editor` contains `height: auto !important`
    - src/main/childEditorSync.ts exports `createScrollIntoViewExtension` function
    - The function checks `syncAnnotation` before firing scroll logic (Pitfall 4 prevention)
    - The function uses `coordsAtPos` and `closest('.cm-scroller')` for parent scroller detection
    - TypeScript compiles without errors (`npx tsc --noEmit --skipLibCheck` exits 0)
    - grep -c "createScrollIntoViewExtension" src/main/childEditorSync.ts returns >= 2 (definition + export)
  </acceptance_criteria>
  <done>Child editor grows unbounded with no inner scrollbar (D-13); parent auto-scrolls to keep cursor visible when typing at bottom (D-14)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | No network calls, no user data, no auth — DOM event handlers and CSS rules only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Tampering | DOM event handlers | accept | Standard browser APIs; no user input processed beyond preventDefault |
</threat_model>

<verification>
```bash
cd /Users/moxu/projects/obsidian-leetcode && npm test -- --run tests/main/codeBlockButtonRow.test.ts && npx tsc --noEmit --skipLibCheck
```
All tests pass and TypeScript compiles cleanly.
</verification>

<success_criteria>
- Run/Submit/AI Solution buttons do not steal focus from child editor (D-02)
- Child editor has no inner scrollbar — auto-grows with content (D-13)
- Parent viewport auto-scrolls to keep cursor visible when typing at bottom (D-14)
- Parent-to-child sync does NOT trigger scroll (Pitfall 4 gate)
- No TypeScript compilation errors
- All existing tests continue to pass
</success_criteria>

<output>
Create `.planning/phases/15-focus-undo-cursor/15-02-SUMMARY.md` when done
</output>
