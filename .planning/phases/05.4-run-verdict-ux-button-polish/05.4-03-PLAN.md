---
phase: 05.4
plan: 03
type: execute
wave: 2
depends_on: ["05.4-01", "05.4-02"]
files_modified:
  - src/solve/verdictModalRenderer.ts
  - src/solve/VerdictModal.ts
  - src/main.ts
  - tests/solve/verdictModalRenderer.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "renderRunResult always renders a tab strip (per-case tabs visible even when arity===1) — D-05"
    - "Each per-case tab carries a PASS or FAIL chip computed via per-index trim-compare on code_answer/expected_code_answer — D-04"
    - "Title chrome shows '{verdict}  Runtime: N ms' WITHOUT problem title; verdict color follows D-07 (success on AC, error on WA/TLE/MLE/RE/CE) — D-13"
    - "When compile_error or runtime_error is present, renderRunResult renders a single .leetcode-verdict-error-pre block, no tab strip — D-15"
    - "Run path renders zero buttons matching /Copy.*test.*case/i — D-16 (Run side)"
    - "Input section renders 'paramName = value' lines when metaData.params is parsed; falls back to raw joinedDataInput dump when metaData is missing or malformed — D-08"
    - "Output value text and Expected value text get class-gated coloring: --fail variant applies var(--text-error)/var(--text-success); --pass variant applies var(--text-normal) — D-07"
    - "Footer Close button has data-lc-role='close' attribute (load-bearing for VerdictModal.focusCloseButton)"
    - "renderSubmitVerdict (lines 140+ of verdictModalRenderer.ts) is byte-identical to its pre-5.4 state — D-14 LOCKED"
  artifacts:
    - path: "src/solve/verdictModalRenderer.ts"
      provides: "Expanded renderRunResult per D-05/D-07/D-08/D-13/D-15/D-16"
      contains: "leetcode-verdict-case-tab"
    - path: "src/solve/VerdictModal.ts"
      provides: "Forwards metaData + joinedDataInput from caller into renderer"
      contains: "metaData"
    - path: "src/main.ts"
      provides: "Threads detail.metaData + dataInput through to VerdictModal.renderVerdict"
      contains: "metaData"
  key_links:
    - from: "src/main.ts (runInterpretedInput)"
      to: "VerdictModal.renderVerdict"
      via: "{ metaData, joinedDataInput } options"
      pattern: "metaData"
    - from: "src/solve/VerdictModal.ts (renderVerdict adapter)"
      to: "verdictModalRenderer.renderVerdict"
      via: "RenderVerdictArgs.metaData + .joinedDataInput"
      pattern: "metaData"
    - from: "src/solve/verdictModalRenderer.ts (renderRunResult)"
      to: "src/solve/runArity.ts"
      via: "named imports parseMetaData, deriveArity, splitOutput"
      pattern: "from '\\./runArity'"
---

<objective>
Implement D-04, D-05, D-07, D-08, D-13, D-15, D-16 by expanding the existing pure-DOM renderer `renderRunResult` in `verdictModalRenderer.ts`. This is the primary edit of Phase 5.4 — the entire LC-style verdict modal redesign lives here. The Submit path (`renderSubmitVerdict` lines 140+) MUST stay byte-identical per D-14.

Purpose: Match LC.com's per-case Run-mode verdict layout — header with verdict + runtime, always-visible tab strip with PASS/FAIL chips, three stacked Input/Output/Expected sections per active case, Input section labeled by metaData.params, theme-aware coloring driven by per-case pass state, single error-block rendering for compile/runtime errors, no copy-failing-testcase button on the Run path. Wires the two new optional renderer args (`metaData` + `joinedDataInput`) through `VerdictModal.renderVerdict` and `runInterpretedInput` so the renderer has the inputs it needs without breaking the renderer/modal split (renderer stays pure-DOM, no obsidian imports).

Output: Expanded `renderRunResult` + new helper `renderRunErrorBlock`. `RenderVerdictArgs` interface gains optional `metaData?: string` and `joinedDataInput?: string`. `VerdictModal.ts` and `main.ts` thread these args end-to-end. All 8 RED renderer tests from Plan 01 Task 3 go GREEN. Submit-path tests remain unchanged.
</objective>

<execution_context>
@/Users/moxu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/moxu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/moxu/projects/obsidian-leetcode/.planning/PROJECT.md
@/Users/moxu/projects/obsidian-leetcode/.planning/ROADMAP.md
@/Users/moxu/projects/obsidian-leetcode/.planning/STATE.md
@/Users/moxu/projects/obsidian-leetcode/.planning/phases/05.4-run-verdict-ux-button-polish/05.4-CONTEXT.md
@/Users/moxu/projects/obsidian-leetcode/.planning/phases/05.4-run-verdict-ux-button-polish/05.4-RESEARCH.md
@/Users/moxu/projects/obsidian-leetcode/.planning/phases/05.4-run-verdict-ux-button-polish/05.4-PATTERNS.md
@/Users/moxu/projects/obsidian-leetcode/.planning/phases/05.4-run-verdict-ux-button-polish/05.4-01-SUMMARY.md
@/Users/moxu/projects/obsidian-leetcode/.planning/phases/05.4-run-verdict-ux-button-polish/05.4-02-SUMMARY.md
@/Users/moxu/projects/obsidian-leetcode/CLAUDE.md

<interfaces>
<!-- Contracts the executor consumes — no codebase exploration needed. -->

From src/solve/verdictModalRenderer.ts (lines 38-46) — RenderVerdictArgs interface to extend:
```typescript
export interface RenderVerdictArgs {
  titleEl: HTMLElement;
  contentEl: HTMLElement;
  payload: TerminalResponse;
  problemTitle?: string;
  onCopyFailingInput?: (input: string) => void;
  // Phase 5.4 ADDS (optional, backward-compatible):
  // metaData?: string;        // raw LC questionData.metaData JSON string (D-08)
  // joinedDataInput?: string; // exact data_input string sent to LC (D-08 input split)
}
```

From src/solve/verdictModalRenderer.ts (lines 103-136) — current renderRunResult to expand:
```typescript
function renderRunResult(
  titleEl: HTMLElement,
  contentEl: HTMLElement,
  res: RunCheckResponse,
  problemTitle: string | undefined,
): void {
  // Currently: simple Output + Expected display via makeSection.
  // Phase 5.4: full LC-style redesign per D-05/D-07/D-08/D-13/D-15/D-16.
}
```

From src/solve/verdictModalRenderer.ts existing helpers (DO NOT redefine — reuse):
- `appendEl(parent, tag, cls?)` — line 317; uses parent.ownerDocument; CSP-safe
- `setText(el, text)` — line 324; textContent only
- `clear(el)` — line 313
- `firstNonEmpty(...values)` — line 369
- `toLines(v)` — lines 376-380; normalizes string | string[] | undefined → string[]
- `makeSection(parent, labelText, value, preCls, labelModifier?)` — lines 330-347; supports state-driven label modifier class
- `addClass(el, cls)` — patterned helper

From src/solve/runArity.ts (Plan 01 Task 2 output):
```typescript
export function parseMetaData(raw: string | undefined): MetaData | null;
export function deriveArity(metaData: string | undefined, sampleTestCase: string | undefined): number;
export function splitInput(joined: string, arity: number): string[];
export function splitOutput(arr: string | string[] | undefined, arity: number): string[];
```

From src/solve/VerdictModal.ts (lines 102-116) — adapter to expand:
```typescript
renderVerdict(res: TerminalResponse, problemTitle: string): void {
  this.clearPendingStateClass();
  renderVerdict({
    titleEl: this.titleEl,
    contentEl: this.contentEl,
    payload: res,
    problemTitle,
    onCopyFailingInput: (input) => {
      this.args.onCopyFailingInput?.(input);
      this.close();
    },
  });
  this.focusCloseButton();
}
// Phase 5.4 expands signature to: renderVerdict(res, problemTitle, opts?: { metaData?: string; joinedDataInput?: string })
```

From src/main.ts (runInterpretedInput around lines 846-892) — already has access to detail.metaData via `this.settings.getProblemDetail(ctx.slug)` (verified at line 826 in the existing openRunModalWithSeedAppended). The orchestrator forwards `dataInput` to interpretSolution (the same string Plan 02's join produces) — that string IS the `joinedDataInput`.

From src/solve/statusMap.ts:
```typescript
export function classifyStatus(code: number, msg?: string): { kind: VerdictKind; displayName: string };
```
classifyStatus returns `{ kind: 'ac' | 'wa' | 'tle' | 'mle' | 'ce' | 'runtime_error' | 'unknown', displayName: string }`. D-07 title coloring uses the kind.

CSS class names introduced (D-09/D-10/D-12 in Plan 04 ports tokens — Plan 03 only EMITS the class names, styling is Plan 04's job):
- `.leetcode-verdict-case-tabs` — wrapper (D-05)
- `.leetcode-verdict-case-tab` — each tab button (D-05)
- `.leetcode-verdict-case-tab.is-active` — active tab modifier
- `.leetcode-verdict-case-chip` — base chip
- `.leetcode-verdict-case-chip--pass` / `--fail` — D-04 chip variant
- `.leetcode-verdict-case-body` — section container per active tab
- `.leetcode-verdict-case-body.leetcode-verdict-case--fail` / `--pass` — D-07 state class
- `.leetcode-verdict-input-section` — D-08 input wrapper
- `.leetcode-verdict-input-param` — single name=value row
- `.leetcode-verdict-input-param-name` / `-value` — sub-spans
- `.leetcode-verdict-output-value` / `-expected-value` — D-07 value-text targets
- `.leetcode-verdict-runtime` — D-13 runtime line
- `.leetcode-verdict-title--success` / `--error` — D-07 title-color modifier
- `.leetcode-verdict-error-pre` — REUSE existing class (styles.css:581-584)

Anti-patterns to AVOID (Pitfalls 1, 2, 3, 4, 6, 7 from RESEARCH.md):
- Do NOT use `code_answer.length` as the arity source — use `deriveArity(metaData, sampleTestCase)` (Pitfall 1)
- Do NOT assume `code_answer` is always an array — go through `toLines` or `splitOutput` (Pitfall 2)
- Do NOT use raw `metaData.params` — always go through `parseMetaData` so malformed JSON is handled (Pitfall 4)
- Do NOT trust `res.correct_answer` for per-case pass — compute via per-index trim-compare (Pitfall 7, D-04 explicit)
- Do NOT introduce a `.leetcode-verdict button { ... }` rule (Pitfall 6); Plan 04 will scope all rules
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend RenderVerdictArgs and thread metaData + joinedDataInput from main.ts → VerdictModal → renderer</name>
  <files>src/solve/verdictModalRenderer.ts, src/solve/VerdictModal.ts, src/main.ts</files>
  <read_first>
    - src/solve/verdictModalRenderer.ts (full file — confirm RenderVerdictArgs interface line range and renderVerdict dispatch)
    - src/solve/VerdictModal.ts (full file — confirm renderVerdict adapter at lines 102-116, focusCloseButton at lines 160-171, sampleTestCase / metaData wiring sites)
    - src/main.ts lines 820-890 (openRunModalWithSeedAppended + runInterpretedInput — confirm where the verdict modal is constructed and renderVerdict is called)
    - src/settings/SettingsStore.ts or wherever ProblemDetail is defined (confirm whether metaData and sampleTestCase are already cached fields; if not, surface as a follow-up — D-08 fallback path covers absence)
    - .planning/phases/05.4-run-verdict-ux-button-polish/05.4-CONTEXT.md (D-08, D-13, D-14)
    - .planning/phases/05.4-run-verdict-ux-button-polish/05.4-PATTERNS.md (Pattern Assignments §VerdictModal.ts, §src/main.ts)
  </read_first>
  <behavior>
    - `RenderVerdictArgs` interface gains two new optional fields: `metaData?: string` and `joinedDataInput?: string`. Existing call sites (Submit path, Pending path, Timeout path, Unknown path) work unchanged because both fields are optional.
    - `verdictModalRenderer.renderVerdict` forwards `args.metaData` and `args.joinedDataInput` into `renderRunResult` only on the Run branch. The Submit branch and other branches do not see the new fields.
    - `VerdictModal.renderVerdict` gains an optional third arg `opts?: { metaData?: string; joinedDataInput?: string }` which is spread into the renderer call. Existing callers that omit `opts` continue to work.
    - `src/main.ts` Run-path call site (`runInterpretedInput` final step where it constructs/opens the VerdictModal — locate by reading) passes `{ metaData: detail?.metaData, joinedDataInput: dataInput }` into the modal's renderVerdict call. The Submit path does NOT pass `metaData`/`joinedDataInput` (D-14: Submit modal stays as-is).
    - All Submit-path test it-blocks at tests/solve/verdictModalRenderer.test.ts remain GREEN with zero modification (D-14 regression oracle).
  </behavior>
  <action>Edit `src/solve/verdictModalRenderer.ts`: locate the `RenderVerdictArgs` interface (~lines 38-46) and add two optional fields `metaData?: string` and `joinedDataInput?: string` immediately after `onCopyFailingInput`. In the Run branch of `renderVerdict` (line 67), pass both new fields into the new `renderRunResult` signature. Edit `src/solve/VerdictModal.ts`: extend the `renderVerdict` method signature with an optional third arg `opts?: { metaData?: string; joinedDataInput?: string }` and forward `metaData: opts?.metaData, joinedDataInput: opts?.joinedDataInput` into the renderer args bag. Edit `src/main.ts`: locate the Run-path verdict-modal construction site (the modal that displays the result of `interpretSolution` after polling resolves — find by searching for `renderVerdict` / `VerdictModal` references in `runInterpretedInput`). Pass `{ metaData: detail?.metaData, joinedDataInput: dataInput }` into the verdict modal's `renderVerdict` call. Do NOT modify the Submit-path call site. Do NOT modify `openRunModalWithSeedAppended` (D-16 append path stays as-is). Do NOT touch `pollingOrchestrator.ts`. Confirm tsc compilation (`npx tsc --noEmit`) reports zero errors. Confirm Submit-path tests remain green by running `npm test -- --run tests/solve/verdictModalRenderer.test.ts` and verifying every it-block whose name does NOT mention D-04/D-05/D-07/D-08/D-13/D-15/D-16 is still green (the new D-* tests stay RED until Task 2 lands).</action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json &amp;&amp; npm test -- --run tests/solve/verdictModalRenderer.test.ts -t "Submit"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `RenderVerdictArgs` interface contains `metaData?: string` and `joinedDataInput?: string` (verify: `grep -E "(metaData|joinedDataInput)\\?: string" src/solve/verdictModalRenderer.ts | grep -v '^#' | wc -l` ≥ 2)
    - `VerdictModal.renderVerdict` accepts a third optional `opts` arg (verify: `grep -E "renderVerdict.*opts" src/solve/VerdictModal.ts`)
    - `src/main.ts` Run-path passes both fields (verify: `grep -E "metaData.*joinedDataInput|joinedDataInput.*metaData" src/main.ts | wc -l` ≥ 1)
    - Submit-path it-blocks (those whose names contain "submit" / "WA" / "AC" / "CE" / "RE" / "TLE" / "MLE" — case-insensitive — and do NOT mention "Run" or "D-0[4578]" or "D-1[356]") remain green; the verify command exits 0
    - D-14 guardrail: `renderSubmitVerdict` body byte-identical (verify by reading lines 140-219 and confirming no source-level changes)
    - 05.4-03-SUMMARY.md documents whether `detail?.metaData` is present in the current `ProblemDetail` cache shape; if absent, surfaces it as a follow-up gap item (so Plan 05 live-smoke knows whether the D-08 Input section will hit the metaData path or the raw-dump fallback in production)
  </acceptance_criteria>
  <done>The two new renderer args flow end-to-end through main.ts → VerdictModal → renderer; Submit-path tests remain green; tsc clean; SUMMARY documents ProblemDetail.metaData cache-shape presence/absence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite renderRunResult per D-04 / D-05 / D-07 / D-08 / D-13 / D-15 / D-16</name>
  <files>src/solve/verdictModalRenderer.ts, tests/solve/verdictModalRenderer.test.ts</files>
  <read_first>
    - src/solve/verdictModalRenderer.ts (full file — especially renderRunResult lines 103-136, renderWaBody lines 233-240, renderCeBody lines 257-263, renderReBody lines 265-275, makeSection lines 330-347, helpers lines 313-380)
    - src/solve/runArity.ts (Plan 01 output — full API)
    - src/solve/statusMap.ts (classifyStatus signature + VerdictKind union)
    - src/solve/types.ts lines 71-87 (RunCheckResponse fields, especially compile_error / runtime_error / status_runtime / status_code / code_answer / expected_code_answer)
    - tests/solve/verdictModalRenderer.test.ts (Plan 01 Task 3 RED scaffolding — confirm new it-blocks at the bottom; reuse the same fixture imports)
    - tests/solve/fixtures/run-multi-case.json (Plan 01 Task 1 output — primary 2-case oracle)
    - tests/solve/fixtures/run-sample.json (existing 3-case AC oracle — must still pass the existing "renders code_answer output array" assertion)
    - tests/solve/fixtures/compile-error.json + runtime-error.json (D-15 oracle)
    - .planning/phases/05.4-run-verdict-ux-button-polish/05.4-CONTEXT.md (D-04 through D-16)
    - .planning/phases/05.4-run-verdict-ux-button-polish/05.4-PATTERNS.md (§verdictModalRenderer.ts pattern assignments — copy from renderWaBody / renderCeBody / renderReBody for the three stacked-section + error-state shapes)
  </read_first>
  <behavior>
    - `renderRunResult` rewritten with the structure documented in RESEARCH.md §Architecture Patterns "System Architecture Diagram":
      Step 1: Sniff D-15 errors (`res.compile_error`, `res.runtime_error`, `res.full_compile_error`, `res.full_runtime_error`). If any error string is non-empty, delegate to a new helper `renderRunErrorBlock(titleEl, contentEl, res)` and RETURN. The error block:
        - sets title text via `classifyStatus(res.status_code, res.status_msg).displayName`
        - applies class `leetcode-verdict-title--error` to titleEl
        - emits a `<pre class="leetcode-verdict-error-pre">` whose textContent is `firstNonEmpty(res.full_compile_error, res.compile_error, res.full_runtime_error, res.runtime_error)`
        - emits the standard footer with one Close button (`mod-cta`, `data-lc-role="close"`)
        - emits ZERO `.leetcode-verdict-case-tab` elements (no tab strip in error path)
      Step 2: Compute arity via `deriveArity(args.metaData, ???sampleTestCase???)`. Note: `sampleTestCase` is NOT today plumbed through the renderer; if the renderer can derive arity from `args.metaData` alone (use `parseMetaData(args.metaData)?.params.length`), prefer that; otherwise fall back to `Math.max(toLines(res.code_answer).length, toLines(res.expected_code_answer).length, 1)`. Always return at least 1 (D-05 always-on tab strip).
      Step 3: Compute per-case pass mask: `outputs[i].trim() === expected[i].trim()` for each i in [0, arity). When `expected[i]` is the empty string (custom-input run with empty expected_code_answer array), mark that index as `null` (no chip rendered for that case).
      Step 4: Compute aggregate pass = every non-null mask entry true. Aggregate pass → title `"Accepted"` with `leetcode-verdict-title--success`; aggregate fail → title `"Wrong Answer"` (or the classifyStatus displayName when `status_code` indicates a non-WA fail) with `leetcode-verdict-title--error`.
      Step 5: D-13 chrome — set titleEl text WITHOUT problemTitle (problemTitle arg is intentionally unused; keep the parameter for backward compat). Append a `<div class="leetcode-verdict-runtime">` to contentEl whose text is `Runtime: ${res.status_runtime ?? '—'}`.
      Step 6: D-05 always-on tab strip — emit `<div class="leetcode-verdict-case-tabs">`. For each case index i, emit a `<button class="leetcode-verdict-case-tab">` whose textContent is `Case ${i+1}`. If pass mask is non-null, append a child `<span class="leetcode-verdict-case-chip leetcode-verdict-case-chip--{pass|fail}">` with textContent `PASS` / `FAIL` (D-04). The first tab gets `is-active` modifier class. Each tab's click handler swaps the active class and re-renders the body.
      Step 7: D-08 + D-07 active-case body — emit `<div class="leetcode-verdict-case-body leetcode-verdict-case--{pass|fail}">` (the modifier is `--pass` when `passMask[activeIdx]` is true, `--fail` when false, neither when null). Inside the body, three stacked sections in this order:
        - Input section: parse `args.metaData` via `parseMetaData`; split `args.joinedDataInput` into per-case chunks via `splitInput(joined, arity)`. If metaData parses, render each param `name = value` as a row in `<div class="leetcode-verdict-input-section">` containing `<div class="leetcode-verdict-input-param">` rows (each row: `<span class="leetcode-verdict-input-param-name">name</span>` + `<pre class="leetcode-verdict-input-param-value">value</pre>`); split the per-case input by the metaData params count (so for two-sum, the 2-line case `[2,7,11,15]\n9` becomes `nums = [2,7,11,15]` and `target = 9`). If metaData fails to parse OR the input split doesn't align, fall back to a single `<pre>` block with the raw per-case input dump (D-08 fallback mandate).
        - Output section: `makeSection(body, 'Output', outputs[activeIdx] ?? '', 'leetcode-verdict-output-value')` — the value-text class is `leetcode-verdict-output-value` so the D-07 class-gated CSS rule (added in Plan 04) targets it.
        - Expected section: same shape with class `leetcode-verdict-expected-value`. Suppress the Expected section entirely when `passMask[activeIdx] === null` (custom-input run; no expected available).
      Step 8: Footer — emit `<div class="leetcode-verdict-footer leetcode-verdict-action-row">` with a single `<button class="mod-cta" data-lc-role="close">Close</button>`. NO copy button (D-16: Run path drops the copy button).
    - The new helper `renderRunErrorBlock` is a private function inside `verdictModalRenderer.ts` (file-scope `function`, not exported). It mirrors the shape of `renderCeBody` / `renderReBody` but is renderer-entry-point-shaped (takes titleEl + contentEl, not just body).
    - Renderer remains pure-DOM: no `import` from `obsidian` at the renderer module level. The file-header comment about purity (line 23) stays load-bearing.
    - All 8 RED it-blocks from Plan 01 Task 3 go GREEN.
    - All existing passing tests stay green: renderSubmitVerdict tests, AC/WA/TLE/MLE/CE/RE submit-side body tests, the two existing run-sample / run-custom "renders code_answer output array" assertions (since the new layout still surfaces `code_answer[i]` text inside the active case body).
  </behavior>
  <action>Rewrite the body of `renderRunResult` in `src/solve/verdictModalRenderer.ts` as described in `<behavior>` Steps 1-8. Add a private helper `renderRunErrorBlock(titleEl, contentEl, res)` immediately after `renderRunResult`. Add new named imports from `./runArity`: `parseMetaData`, `deriveArity`, `splitInput`, `splitOutput`. Reuse existing helpers (`appendEl`, `setText`, `clear`, `firstNonEmpty`, `toLines`, `addClass`, `makeSection`) — do NOT duplicate. Add a tab-switch event handler that re-renders only the case-body div (not the whole content); the simplest correct implementation is to clear and rebuild the case-body div on each click. CSS class names emitted by the renderer MUST exactly match the list in `<interfaces>` above so Plan 04's CSS rules attach. Every new `createElement` MUST go through `appendEl` (which already uses `parent.ownerDocument`) — never the global `document`. Every new text content MUST go through `setText` (textContent only) — never `innerHTML`. The existing `renderSubmitVerdict` body (lines 140 onward, including renderAcBody / renderWaBody / renderTleBody / renderMleBody / renderCeBody / renderReBody) MUST be byte-identical (D-14). Update tests/solve/verdictModalRenderer.test.ts ONLY to fix any RED-state test that needs better selectors/assertions to align with the implementation (e.g., if Plan 01 Task 3 used `.leetcode-verdict-input-param` and the implementation diverges, prefer to update the implementation to match the test rather than the test — tests are the contract). Run `npm test -- --run tests/solve/verdictModalRenderer.test.ts` and confirm 100% green. Run `npm test -- --run` and confirm zero new regressions outside this phase's RED scaffolding.</action>
  <verify>
    <automated>npm test -- --run tests/solve/verdictModalRenderer.test.ts tests/solve/runArity.test.ts tests/solve/RunModal.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run tests/solve/verdictModalRenderer.test.ts` exits 0 with all D-04/D-05/D-07/D-08/D-13/D-15/D-16 it-blocks GREEN
    - The verify command exits 0 (renderer + runArity + RunModal all green)
    - Renderer file does NOT import from `obsidian` (verify: `! grep -E "from ['\"]obsidian['\"]" src/solve/verdictModalRenderer.ts`)
    - Renderer file does NOT contain `innerHTML` (verify: `! grep "innerHTML" src/solve/verdictModalRenderer.ts`)
    - Renderer file does NOT contain hardcoded hex/rgb colors (verify: `! grep -vE '^\\s*//' src/solve/verdictModalRenderer.ts | grep -E '#[0-9a-fA-F]{3,6}\\b|rgb\\('` returns no match — D-06 grep gate; comments allowed to mention them, code body must not)
    - `renderSubmitVerdict` body is unchanged: `git diff src/solve/verdictModalRenderer.ts` shows no edits between the line containing `function renderSubmitVerdict` and the line containing `function renderAcBody` — verify by inspecting the diff hunk boundaries (D-14 LOCKED)
    - The Run path emits zero buttons whose textContent matches `/Copy.*test.*case/i` — asserted by the D-16 RED test which is now GREEN
    - The Run path emits exactly `arity` `.leetcode-verdict-case-tab` elements when no error is present, and zero when compile_error/runtime_error is present — asserted by the D-05 + D-15 RED tests
    - The D-16 Submit-side regression guards from Plan 01 Task 3 it-blocks (9) and (10) are GREEN (Submit-mode WA payload renders exactly one Copy-failing button + clicking it calls onCopyFailingInput once with last_testcase). Since renderSubmitVerdict stays bytes-identical per D-14, these should flip GREEN with zero new code — simultaneously proving D-14 (Submit path unchanged) and D-16 Submit side (Copy preserved)
    - Footer Close button has attribute `data-lc-role="close"` — asserted by reading the renderer source AND verified by `VerdictModal.focusCloseButton()` continuing to work in existing tests
  </acceptance_criteria>
  <done>renderRunResult delivers the LC-style per-case verdict layout with class-gated theming hooks; all 8 D-* RED tests are GREEN; renderSubmitVerdict byte-unchanged; full renderer test suite GREEN.</done>
</task>

</tasks>

<verification>
- `npm test -- --run tests/solve/verdictModalRenderer.test.ts tests/solve/runArity.test.ts tests/solve/RunModal.test.ts` exits 0
- `npx tsc --noEmit` exits 0
- D-14 guardrail: `renderSubmitVerdict` body byte-identical (visual diff inspection)
- D-06 grep gate: no hardcoded hex/rgb in renderer source body (comments allowed)
- D-16 (Run side): zero "Copy"-named buttons in Run-path render output
- D-05 invariant: tab strip is rendered even when arity===1
- Submit-path tests at tests/solve/verdictModalRenderer.test.ts (those whose names mention WA / AC / TLE / MLE / CE / RE / Submit) all GREEN
</verification>

<success_criteria>
- LC-style verdict modal redesign live for the Run path (D-05/D-07/D-13)
- Per-case PASS/FAIL chips drive coloring and labels (D-04)
- Input section labels params from metaData with raw-dump fallback (D-08)
- Compile/runtime errors render as a single error block (D-15)
- No copy-failing-testcase button on Run path (D-16 Run side)
- D-14 LOCKED: renderSubmitVerdict and all submit-body helpers byte-identical
- Renderer remains pure-DOM, no obsidian imports, no innerHTML, no hardcoded colors
- 8 D-* RED tests from Plan 01 Task 3 are GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/05.4-run-verdict-ux-button-polish/05.4-03-SUMMARY.md` documenting:
- New CSS class names emitted by the renderer (full list, for Plan 04 to attach styles)
- Whether sampleTestCase was plumbed through or arity was derived metaData-only / response-length-only
- Confirmation that renderSubmitVerdict and submit body helpers are byte-identical (cite line ranges)
- Test count delta (RED → GREEN: 8 D-* tests)
- Any open questions surfaced from running against the live multi-case fixture (e.g., per-case error edge cases from RESEARCH Open Question 3)
</output>
